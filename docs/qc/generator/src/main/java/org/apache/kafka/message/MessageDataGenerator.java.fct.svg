<?xml version="1.0" encoding="UTF-8" standalone="no"?><svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" contentStyleType="text/css" height="544px" preserveAspectRatio="none" style="width:1094px;height:544px;background:#000000;" version="1.1" viewBox="0 0 1094 544" width="1094px" zoomAndPan="magnify"><defs/><g><rect fill="#0A2701" height="1" style="stroke:#0A2701;stroke-width:1.0;" width="1" x="0" y="0"/><text fill="#33FF02" font-family="sans-serif" font-size="12" font-style="italic" font-weight="bold" lengthAdjust="spacing" textLength="127" x="5" y="17">PlantUML 1.2022.7</text><rect fill="#33FF02" height="21.2969" style="stroke:#33FF02;stroke-width:1.0;" width="1082" x="5" y="26.9688"/><text fill="#000000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="188" x="6" y="41.9688">[From string (line 416) ]</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="5" x="5" y="62.2656"> </text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="79" x="5" y="78.5625">@startuml</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="190" x="5" y="94.8594">!pragma layout smetana</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="111.1563">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="127.4531">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="453" x="5" y="143.75">constructor MessageDataGenerator(StringpackageName)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="15" x="5" y="160.0469">...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="185" x="5" y="176.3438">... ( skipping 391 lines )</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="15" x="5" y="192.6406">...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="208" x="5" y="208.9375">:buffer.incrementIndent();</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="271" x="5" y="225.2344">:buffer.printf("return %d;%n",val);</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="214" x="5" y="241.5313">:buffer.decrementIndent();</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="169" x="5" y="257.8281">:buffer.printf("}%n");</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="30" x="5" y="274.125">end</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="290.4219">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="306.7188">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="350" x="5" y="323.0156">void generateClassReader(StringclassName,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="337" x="5" y="339.3125">StructSpecstruct,VersionsparentVersions)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="71" x="5" y="355.6094">end note</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="540" x="5" y="371.9063">:headerGenerator.addImport(MessageGenerator.READABLE_CLASS);</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="242" x="5" y="388.2031">:buffer.printf("@Override%n");</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="406" x="5" y="404.5">:buffer.printf("public void read(Readable _readable,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="177" x="10" y="420.7969">short _version) {%n");</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="208" x="5" y="437.0938">:buffer.incrementIndent();</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="386" x="5" y="453.3906">:VersionConditional.forVersions(parentVersions,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="477" x="5" y="469.6875">struct.versions()).allowMembershipCheckAlwaysFalse(false)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="1082" x="5" y="485.9844">.ifNotMember(__-&gt;{headerGenerator.addImport(MessageGenerator.UNSUPPORTED_VERSION_EXCEPTION_CLASS);buffer.printf("throw n ...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="455" x="5" y="502.2813">if (!field.taggedVersions().intersect(fieldFlexibleVersions)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" text-decoration="wavy underline" textLength="337" x="5" y="518.5781">.equals(field.taggedVersions())) then (yes)</text><text fill="#FF0000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="106" x="10" y="534.875">Syntax Error?</text><!--MD5=[636e512165e49e45819a19149e0a4478]
@startuml
!pragma layout smetana
start
note right
constructor MessageDataGenerator(StringpackageName)
end note
:this.structRegistry=new StructRegistry();
:this.headerGenerator=new HeaderGenerator(packageName);
:this.schemaGenerator=new SchemaGenerator(headerGenerator,
structRegistry);
:this.buffer=new CodeBuffer();
end
start
note right
StringoutputName(MessageSpecspec)
end note
:return spec.dataClassName();;
stop
end
start
note right
void generateAndWrite(MessageSpecmessage,
BufferedWriterwriter)
end note
:generate(message);
:write(writer);
end
start
note right
void generate(MessageSpecmessage)
end note
if (message.struct().versions().contains(Short.MAX_VALUE)) then (yes) 
:throw new RuntimeException("Message "+message.name()+
" does "+"not specify a maximum version.");;
endif 
:structRegistry.register(message);
:schemaGenerator.generateSchemas(message);
:messageFlexibleVersions=message.flexibleVersions();
:generateClass(Optional.of(message),message.dataClassName(),
message.struct(),message.struct().versions());
:headerGenerator.generate();
end
start
note right
void write(BufferedWriterwriter)
end note
:headerGenerator.buffer().write(writer);
:buffer.write(writer);
end
start
note right
void generateClass(Optional<MessageSpec>topLevelMessageSpec,
StringclassName,StructSpecstruct,VersionsparentVersions)
end note
:buffer.printf("%n");
if (isTopLevel&&isSetElement) then (yes) 
:throw new RuntimeException("Cannot set mapKey on top level fields.");;
endif 
:generateClassHeader(className,isTopLevel,
isSetElement);
:buffer.incrementIndent();
:generateFieldDeclarations(struct,isSetElement);
:buffer.printf("%n");
:schemaGenerator.writeSchema(className,buffer);
:generateClassConstructors(className,struct,
isSetElement);
:buffer.printf("%n");
if (isTopLevel) then (yes) 
:generateShortAccessor("apiKey",topLevelMessageSpec.get()
.apiKey().orElse((short)-1));
endif 
:buffer.printf("%n");
:generateShortAccessor("lowestSupportedVersion",
parentVersions.lowest());
:buffer.printf("%n");
:generateShortAccessor("highestSupportedVersion",
parentVersions.highest());
:buffer.printf("%n");
:generateClassReader(className,struct,parentVersions);
:buffer.printf("%n");
:generateClassWriter(className,struct,parentVersions);
:buffer.printf("%n");
:generateClassMessageSize(className,struct,
parentVersions);
if (isSetElement) then (yes) 
:buffer.printf("%n");
:generateClassEquals(className,struct,true);
endif 
:buffer.printf("%n");
:generateClassEquals(className,struct,false);
:buffer.printf("%n");
:generateClassHashCode(struct,isSetElement);
:buffer.printf("%n");
:generateClassDuplicate(className,struct);
:buffer.printf("%n");
:generateClassToString(className,struct);
:generateFieldAccessors(struct,isSetElement);
:buffer.printf("%n");
:generateUnknownTaggedFieldsAccessor();
:generateFieldMutators(struct,className,isSetElement);
if (!isTopLevel) then (yes) 
:buffer.decrementIndent();
:buffer.printf("}%n");
endif 
:generateSubclasses(className,struct,parentVersions,
isSetElement);
if (isTopLevel) then (yes) 
:Iterator<StructSpec>iter=structRegistry.commonStructs();
:generateClass(Optional.empty(),commonStruct.name(),
commonStruct,commonStruct.versions());
:buffer.decrementIndent();
:buffer.printf("}%n");
endif 
end
start
note right
void generateClassHeader(StringclassName,
booleanisTopLevel,booleanisSetElement)
end note
if (isTopLevel) then (yes) 
:implementedInterfaces.add("ApiMessage");
:headerGenerator.addImport(MessageGenerator.API_MESSAGE_CLASS);
else (no)
:implementedInterfaces.add("Message");
:headerGenerator.addImport(MessageGenerator.MESSAGE_CLASS);
endif 
if (isSetElement) then (yes) 
:headerGenerator.addImport(MessageGenerator.IMPLICIT_LINKED_HASH_MULTI_COLLECTION_CLASS);
:implementedInterfaces.add("ImplicitLinkedHashMultiCollection.Element");
endif 
:classModifiers.add("public");
if (!isTopLevel) then (yes) 
:classModifiers.add("static");
endif 
:buffer.printf("%s class %s implements %s {%n",
String.join(" ",classModifiers),className,
String.join(", ",implementedInterfaces));
end
start
note right
void generateSubclasses(StringclassName,StructSpecstruct,
VersionsparentVersions,booleanisSetElement)
end note
if (field.type().isStructArray()) then (yes) 
if (!structRegistry.commonStructNames().contains(arrayType.elementName())) then (yes) 
:generateClass(Optional.empty(),arrayType.elementType().toString(),
structRegistry.findStruct(field),parentVersions.intersect(struct.versions()));
endif 
endif 
if (isSetElement) then (yes) 
:generateHashSet(className,struct);
endif 
end
start
note right
void generateHashSet(StringclassName,StructSpecstruct)
end note
:buffer.printf("%n");
:headerGenerator.addImport(MessageGenerator.IMPLICIT_LINKED_HASH_MULTI_COLLECTION_CLASS);
:buffer.printf("public static class %s extends ImplicitLinkedHashMultiCollection<%s> {%n",
FieldSpec.collectionType(className),className);
:buffer.incrementIndent();
:generateHashSetZeroArgConstructor(className);
:buffer.printf("%n");
:generateHashSetSizeArgConstructor(className);
:buffer.printf("%n");
:generateHashSetIteratorConstructor(className);
:buffer.printf("%n");
:generateHashSetFindMethod(className,struct);
:buffer.printf("%n");
:generateHashSetFindAllMethod(className,struct);
:buffer.printf("%n");
:generateCollectionDuplicateMethod(className);
:buffer.decrementIndent();
:buffer.printf("}%n");
end
start
note right
void generateHashSetZeroArgConstructor(StringclassName)
end note
:buffer.printf("public %s() {%n",FieldSpec.collectionType(className));
:buffer.incrementIndent();
:buffer.printf("super();%n");
:buffer.decrementIndent();
:buffer.printf("}%n");
end
start
note right
void generateHashSetSizeArgConstructor(StringclassName)
end note
:buffer.printf("public %s(int expectedNumElements) {%n",
FieldSpec.collectionType(className));
:buffer.incrementIndent();
:buffer.printf("super(expectedNumElements);
:%n");
:buffer.decrementIndent();
:buffer.printf("}%n");
end
start
note right
void generateHashSetIteratorConstructor(StringclassName)
end note
:headerGenerator.addImport(MessageGenerator.ITERATOR_CLASS);
:buffer.printf("public %s(Iterator<%s> iterator) {%n",
FieldSpec.collectionType(className),className);
:buffer.incrementIndent();
:buffer.printf("super(iterator);%n");
:buffer.decrementIndent();
:buffer.printf("}%n");
end
start
note right
void generateHashSetFindMethod(StringclassName,
StructSpecstruct)
end note
:headerGenerator.addImport(MessageGenerator.LIST_CLASS);
:buffer.printf("public %s find(%s) {%n",className,
commaSeparatedHashSetFieldAndTypes(struct));
:buffer.incrementIndent();
:generateKeyElement(className,struct);
:headerGenerator.addImport(MessageGenerator.IMPLICIT_LINKED_HASH_MULTI_COLLECTION_CLASS);
:buffer.printf("return find(_key);%n");
:buffer.decrementIndent();
:buffer.printf("}%n");
end
start
note right
void generateHashSetFindAllMethod(StringclassName,
StructSpecstruct)
end note
:headerGenerator.addImport(MessageGenerator.LIST_CLASS);
:buffer.printf("public List<%s> findAll(%s) {%n",
className,commaSeparatedHashSetFieldAndTypes(struct));
:buffer.incrementIndent();
:generateKeyElement(className,struct);
:headerGenerator.addImport(MessageGenerator.IMPLICIT_LINKED_HASH_MULTI_COLLECTION_CLASS);
:buffer.printf("return findAll(_key);%n");
:buffer.decrementIndent();
:buffer.printf("}%n");
end
start
note right
void generateKeyElement(StringclassName,StructSpecstruct)
end note
:buffer.printf("%s _key = new %s();%n",className,
className);
if (field.mapKey()) then (yes) 
:buffer.printf("_key.set%s(%s);%n",field.capitalizedCamelCaseName(),
field.camelCaseName());
endif 
end
start
note right
StringcommaSeparatedHashSetFieldAndTypes(StructSpecstruct)
end note
:return struct.fields().stream().filter(f->f.mapKey()).map(f->String.format("%s %s",
f.concreteJavaType(headerGenerator,structRegistry),
f.camelCaseName())).collect(Collectors.joining(",
 "));;
stop
end
start
note right
void generateCollectionDuplicateMethod(StringclassName)
end note
:headerGenerator.addImport(MessageGenerator.LIST_CLASS);
:buffer.printf("public %s duplicate() {%n",
FieldSpec.collectionType(className));
:buffer.incrementIndent();
:buffer.printf("%s _duplicate = new %s(size());%n",
FieldSpec.collectionType(className),FieldSpec.collectionType(className));
:buffer.printf("for (%s _element : this) {%n",
className);
:buffer.incrementIndent();
:buffer.printf("_duplicate.add(_element.duplicate());
:%n");
:buffer.decrementIndent();
:buffer.printf("}%n");
:buffer.printf("return _duplicate;%n");
:buffer.decrementIndent();
:buffer.printf("}%n");
end
start
note right
void generateFieldDeclarations(StructSpecstruct,
booleanisSetElement)
end note
:generateFieldDeclaration(field);
:headerGenerator.addImport(MessageGenerator.LIST_CLASS);
:headerGenerator.addImport(MessageGenerator.RAW_TAGGED_FIELD_CLASS);
:buffer.printf("private List<RawTaggedField> _unknownTaggedFields;
:%n");
if (isSetElement) then (yes) 
:buffer.printf("private int next;%n");
:buffer.printf("private int prev;%n");
endif 
end
start
note right
void generateFieldDeclaration(FieldSpecfield)
end note
:buffer.printf("%s %s;%n",field.fieldAbstractJavaType(headerGenerator,
structRegistry),field.camelCaseName());
end
start
note right
void generateFieldAccessors(StructSpecstruct,
booleanisSetElement)
end note
:generateFieldAccessor(field);
if (isSetElement) then (yes) 
:buffer.printf("%n");
:buffer.printf("@Override%n");
:generateAccessor("int","next","next");
:buffer.printf("%n");
:buffer.printf("@Override%n");
:generateAccessor("int","prev","prev");
endif 
end
start
note right
void generateUnknownTaggedFieldsAccessor()
end note
:buffer.printf("@Override%n");
:headerGenerator.addImport(MessageGenerator.LIST_CLASS);
:headerGenerator.addImport(MessageGenerator.RAW_TAGGED_FIELD_CLASS);
:buffer.printf("public List<RawTaggedField> unknownTaggedFields() {%n");
:buffer.incrementIndent();
:buffer.printf("if (_unknownTaggedFields == null) {%n");
:buffer.incrementIndent();
:headerGenerator.addImport(MessageGenerator.ARRAYLIST_CLASS);
:buffer.printf("_unknownTaggedFields = new ArrayList<>(0);
:%n");
:buffer.decrementIndent();
:buffer.printf("}%n");
:buffer.printf("return _unknownTaggedFields;
:%n");
:buffer.decrementIndent();
:buffer.printf("}%n");
end
start
note right
void generateFieldMutators(StructSpecstruct,
StringclassName,booleanisSetElement)
end note
:generateFieldMutator(className,field);
if (isSetElement) then (yes) 
:buffer.printf("%n");
:buffer.printf("@Override%n");
:generateSetter("int","setNext","next");
:buffer.printf("%n");
:buffer.printf("@Override%n");
:generateSetter("int","setPrev","prev");
endif 
end
start
note right
void generateClassConstructors(StringclassName,
StructSpecstruct,booleanisSetElement)
end note
:headerGenerator.addImport(MessageGenerator.READABLE_CLASS);
:buffer.printf("public %s(Readable _readable,
 short _version) {%n",className);
:buffer.incrementIndent();
:buffer.printf("read(_readable, _version);
:%n");
:generateConstructorEpilogue(isSetElement);
:buffer.decrementIndent();
:buffer.printf("}%n");
:buffer.printf("%n");
:buffer.printf("public %s() {%n",className);
:buffer.incrementIndent();
:buffer.printf("this.%s = %s;%n",field.camelCaseName(),
field.fieldDefault(headerGenerator,structRegistry));
:generateConstructorEpilogue(isSetElement);
:buffer.decrementIndent();
:buffer.printf("}%n");
end
start
note right
void generateConstructorEpilogue(booleanisSetElement)
end note
if (isSetElement) then (yes) 
:headerGenerator.addImport(MessageGenerator.IMPLICIT_LINKED_HASH_COLLECTION_CLASS);
:buffer.printf("this.prev = ImplicitLinkedHashCollection.INVALID_INDEX;
:%n");
:buffer.printf("this.next = ImplicitLinkedHashCollection.INVALID_INDEX;
:%n");
endif 
end
start
note right
void generateShortAccessor(Stringname,shortval)
end note
:buffer.printf("@Override%n");
:buffer.printf("public short %s() {%n",name);
:buffer.incrementIndent();
:buffer.printf("return %d;%n",val);
:buffer.decrementIndent();
:buffer.printf("}%n");
end
start
note right
void generateClassReader(StringclassName,
StructSpecstruct,VersionsparentVersions)
end note
:headerGenerator.addImport(MessageGenerator.READABLE_CLASS);
:buffer.printf("@Override%n");
:buffer.printf("public void read(Readable _readable,
 short _version) {%n");
:buffer.incrementIndent();
:VersionConditional.forVersions(parentVersions,
struct.versions()).allowMembershipCheckAlwaysFalse(false)
.ifNotMember(__->{headerGenerator.addImport(MessageGenerator.UNSUPPORTED_VERSION_EXCEPTION_CLASS);buffer.printf("throw new UnsupportedVersionException(\"Can't read "+"version \" + _version + \" of %s\");%n",className);}).generate(buffer);
if (!field.taggedVersions().intersect(fieldFlexibleVersions)
.equals(field.taggedVersions())) then (yes) 
:throw new RuntimeException("Field "+field.name()+
" specifies tagged "+"versions "+field.taggedVersions()+
" that are not a subset of the "+"flexible versions "+
fieldFlexibleVersions);;
endif 
:VersionConditional.forVersions(mandatoryVersions,
curVersions).alwaysEmitBlockScope(field.type().isVariableLength()).ifNotMember(__->{buffer.printf("this.%s = %s;%n",
field.camelCaseName(),field.fieldDefault(headerGenerator,
structRegistry));}).ifMember(presentAndUntaggedVersions->{if (field.type()
.isVariableLength()&&!field.type().isStruct()){ClauseGeneratorcallGenerateVariableLengthReader=versions->{generateVariableLengthReader(fieldFlexibleVersions(field),field.camelCaseName(),field.type(),versions,field.nullableVersions(),String.format("this.%s = ",field.camelCaseName()),String.format(";%n"),structRegistry.isStructArrayWithKeys(field),field.zeroCopy());};if (field.type().isArray()&&((FieldType.ArrayType)field.type()).elementType().serializationIsDifferentInFlexibleVersions()){VersionConditional.forVersions(fieldFlexibleVersions(field),presentAndUntaggedVersions).ifMember(callGenerateVariableLengthReader).ifNotMember(callGenerateVariableLengthReader).generate(buffer);}else {callGenerateVariableLengthReader.generate(presentAndUntaggedVersions);}}else {buffer.printf("this.%s = %s;%n",field.camelCaseName(),primitiveReadExpression(field.type()));}}).generate(buffer);
:buffer.printf("this._unknownTaggedFields = null;
:%n");
:VersionConditional.forVersions(messageFlexibleVersions,
curVersions).ifMember(curFlexibleVersions->{buffer.printf("int _numTaggedFields = _readable.readUnsignedVarint();%n");buffer.printf("for (int _i = 0; _i < _numTaggedFields; _i++) {%n");buffer.incrementIndent();buffer.printf("int _tag = _readable.readUnsignedVarint();%n");buffer.printf("int _size = _readable.readUnsignedVarint();%n");buffer.printf("switch (_tag) {%n");buffer.incrementIndent();for (FieldSpecfield:struct.fields()){VersionsvalidTaggedVersions=field.versions().intersect(field.taggedVersions());if (!validTaggedVersions.empty()){if (!field.tag().isPresent()){throw new RuntimeException("Field "+field.name()+" has tagged versions, but no tag.");}buffer.printf("case %d: {%n",field.tag().get());buffer.incrementIndent();VersionConditional.forVersions(validTaggedVersions,curFlexibleVersions).ifMember(presentAndTaggedVersions->{if (field.type().isVariableLength()&&!field.type().isStruct()){generateVariableLengthReader(fieldFlexibleVersions(field),field.camelCaseName(),field.type(),presentAndTaggedVersions,field.nullableVersions(),String.format("this.%s = ",field.camelCaseName()),String.format(";%n"),structRegistry.isStructArrayWithKeys(field),field.zeroCopy());}else {buffer.printf("this.%s = %s;%n",field.camelCaseName(),primitiveReadExpression(field.type()));}buffer.printf("break;%n");}).ifNotMember(__->{buffer.printf("throw new RuntimeException(\"Tag %d is not "+"valid for version \" + _version);%n",field.tag().get());}).generate(buffer);buffer.decrementIndent();buffer.printf("}%n");}}buffer.printf("default:%n");buffer.incrementIndent();buffer.printf("this._unknownTaggedFields = _readable.readUnknownTaggedField(this._unknownTaggedFields, _tag, _size);%n");buffer.printf("break;%n");buffer.decrementIndent();buffer.decrementIndent();buffer.printf("}%n");buffer.decrementIndent();buffer.printf("}%n");}).generate(buffer);
:buffer.decrementIndent();
:buffer.printf("}%n");
end
start
note right
StringprimitiveReadExpression(FieldTypetype)
end note
if (type instanceof FieldType.BoolFieldType) then (yes) 
:return "_readable.readByte() != 0";;
stop
else (no)
:throw new RuntimeException("Unsupported field type "+
type);;
endif 
end
start
note right
void generateVariableLengthReader(VersionsfieldFlexibleVersions,
Stringname,FieldTypetype,VersionspossibleVersions,
VersionsnullableVersions,StringassignmentPrefix,
StringassignmentSuffix,booleanisStructArrayWithKeys,
booleanzeroCopy)
end note
:buffer.printf("int %s;%n",lengthVar);
:VersionConditional.forVersions(fieldFlexibleVersions,
possibleVersions).ifMember(__->{buffer.printf("%s = _readable.readUnsignedVarint() - 1;%n",
lengthVar);}).ifNotMember(__->{if (type.isString()){buffer.printf("%s = _readable.readShort();%n",
lengthVar);}else if (type.isBytes()||type.isArray()||type.isRecords()){buffer.printf("%s = _readable.readInt();%n",
lengthVar);}else {throw new RuntimeException("Can't handle variable length type "+
type);}}).generate(buffer);
:buffer.printf("if (%s < 0) {%n",lengthVar);
:buffer.incrementIndent();
:VersionConditional.forVersions(nullableVersions,
possibleVersions).ifNotMember(__->{buffer.printf("throw new RuntimeException(\"non-nullable field %s "+
"was serialized as null\");%n",name);}).ifMember(__->{buffer.printf("%snull%s",
assignmentPrefix,assignmentSuffix);}).generate(buffer);
:buffer.decrementIndent();
if (type.isString()) then (yes) 
:buffer.printf("} else if (%s > 0x7fff) {%n",
lengthVar);
:buffer.incrementIndent();
:buffer.printf("throw new RuntimeException(\"string field %s "+
"had invalid length \" + %s);%n",name,lengthVar);
:buffer.decrementIndent();
endif 
:buffer.printf("} else {%n");
:buffer.incrementIndent();
if (type.isString()) then (yes) 
:buffer.printf("%s_readable.readString(%s)%s",
assignmentPrefix,lengthVar,assignmentSuffix);
else (no)
:throw new RuntimeException("Can't handle variable length type "+
type);;
endif 
:buffer.decrementIndent();
:buffer.printf("}%n");
end
start
note right
void generateClassWriter(StringclassName,
StructSpecstruct,VersionsparentVersions)
end note
:headerGenerator.addImport(MessageGenerator.WRITABLE_CLASS);
:headerGenerator.addImport(MessageGenerator.OBJECT_SERIALIZATION_CACHE_CLASS);
:buffer.printf("@Override%n");
:buffer.printf("public void write(Writable _writable,
 ObjectSerializationCache _cache, short _version) {%n");
:buffer.incrementIndent();
:VersionConditional.forVersions(struct.versions(),
parentVersions).allowMembershipCheckAlwaysFalse(false)
.ifNotMember(__->{headerGenerator.addImport(MessageGenerator.UNSUPPORTED_VERSION_EXCEPTION_CLASS);buffer.printf("throw new UnsupportedVersionException(\"Can't write "+"version \" + _version + \" of %s\");%n",className);}).generate(buffer);
:buffer.printf("int _numTaggedFields = 0;
:%n");
:VersionConditional.forVersions(field.taggedVersions(),
presentVersions).ifNotMember(presentAndUntaggedVersions->{if (field.type()
.isVariableLength()&&!field.type().isStruct()){ClauseGeneratorcallGenerateVariableLengthWriter=versions->{generateVariableLengthWriter(fieldFlexibleVersions(field),field.camelCaseName(),field.type(),versions,field.nullableVersions(),field.zeroCopy());};if (field.type().isArray()&&((FieldType.ArrayType)field.type()).elementType().serializationIsDifferentInFlexibleVersions()){VersionConditional.forVersions(fieldFlexibleVersions(field),presentAndUntaggedVersions).ifMember(callGenerateVariableLengthWriter).ifNotMember(callGenerateVariableLengthWriter).generate(buffer);}else {callGenerateVariableLengthWriter.generate(presentAndUntaggedVersions);}}else {buffer.printf("%s;%n",primitiveWriteExpression(field.type(),field.camelCaseName()));}}).ifMember(__->{field.generateNonDefaultValueCheck(headerGenerator,structRegistry,buffer,"this.",field.nullableVersions());buffer.incrementIndent();buffer.printf("_numTaggedFields++;%n");buffer.decrementIndent();buffer.printf("}%n");if (taggedFields.put(field.tag().get(),field)!=null ){throw new RuntimeException("Field "+field.name()+" has tag "+field.tag()+", but another field already used that tag.");}}).generate(buffer);
if (!field.ignorable()) then (yes) 
:cond.ifNotMember(__->{field.generateNonIgnorableFieldCheck(headerGenerator,
structRegistry,"this.",buffer);});
endif 
:cond.generate(buffer);
:headerGenerator.addImport(MessageGenerator.RAW_TAGGED_FIELD_WRITER_CLASS);
:buffer.printf("RawTaggedFieldWriter _rawWriter = RawTaggedFieldWriter.forFields(_unknownTaggedFields);
:%n");
:buffer.printf("_numTaggedFields += _rawWriter.numFields();
:%n");
:VersionConditional.forVersions(messageFlexibleVersions,
curVersions).ifNotMember(__->{generateCheckForUnsupportedNumTaggedFields("_numTaggedFields > 0");})
.ifMember(__->{buffer.printf("_writable.writeUnsignedVarint(_numTaggedFields);%n");intprevTag=-1;for (FieldSpecfield:taggedFields.values()){if (prevTag+1!=field.tag().get()){buffer.printf("_rawWriter.writeRawTags(_writable, %d);%n",field.tag().get());}VersionConditional.forVersions(field.versions(),field.taggedVersions().intersect(field.versions())).allowMembershipCheckAlwaysFalse(false).ifMember(presentAndTaggedVersions->{IsNullConditionalcond=IsNullConditional.forName(field.camelCaseName()).nullableVersions(field.nullableVersions()).possibleVersions(presentAndTaggedVersions).alwaysEmitBlockScope(true).ifShouldNotBeNull(()->{if (!field.defaultString().equals("null")){field.generateNonDefaultValueCheck(headerGenerator,structRegistry,buffer,"this.",Versions.NONE);buffer.incrementIndent();}buffer.printf("_writable.writeUnsignedVarint(%d);%n",field.tag().get());if (field.type().isString()){buffer.printf("byte[] _stringBytes = _cache.getSerializedValue(this.%s);%n",field.camelCaseName());headerGenerator.addImport(MessageGenerator.BYTE_UTILS_CLASS);buffer.printf("_writable.writeUnsignedVarint(_stringBytes.length + "+"ByteUtils.sizeOfUnsignedVarint(_stringBytes.length + 1));%n");buffer.printf("_writable.writeUnsignedVarint(_stringBytes.length + 1);%n");buffer.printf("_writable.writeByteArray(_stringBytes);%n");}else if (field.type().isBytes()){headerGenerator.addImport(MessageGenerator.BYTE_UTILS_CLASS);buffer.printf("_writable.writeUnsignedVarint(this.%s.length + "+"ByteUtils.sizeOfUnsignedVarint(this.%s.length + 1));%n",field.camelCaseName(),field.camelCaseName());buffer.printf("_writable.writeUnsignedVarint(this.%s.length + 1);%n",field.camelCaseName());buffer.printf("_writable.writeByteArray(this.%s);%n",field.camelCaseName());}else if (field.type().isArray()){headerGenerator.addImport(MessageGenerator.BYTE_UTILS_CLASS);buffer.printf("_writable.writeUnsignedVarint(_cache.getArraySizeInBytes(this.%s));%n",field.camelCaseName());generateVariableLengthWriter(fieldFlexibleVersions(field),field.camelCaseName(),field.type(),presentAndTaggedVersions,Versions.NONE,field.zeroCopy());}else if (field.type().isStruct()){buffer.printf("_writable.writeUnsignedVarint(this.%s.size(_cache, _version));%n",field.camelCaseName());buffer.printf("%s;%n",primitiveWriteExpression(field.type(),field.camelCaseName()));}else if (field.type().isRecords()){throw new RuntimeException("Unsupported attempt to declare field `"+field.name()+"` with `records` type as a tagged field.");}else {buffer.printf("_writable.writeUnsignedVarint(%d);%n",field.type().fixedLength().get());buffer.printf("%s;%n",primitiveWriteExpression(field.type(),field.camelCaseName()));}if (!field.defaultString().equals("null")){buffer.decrementIndent();buffer.printf("}%n");}});if (!field.defaultString().equals("null")){cond.ifNull(()->{buffer.printf("_writable.writeUnsignedVarint(%d);%n",field.tag().get());buffer.printf("_writable.writeUnsignedVarint(1);%n");buffer.printf("_writable.writeUnsignedVarint(0);%n");});}cond.generate(buffer);}).generate(buffer);prevTag=field.tag().get();}if (prevTag<Integer.MAX_VALUE){buffer.printf("_rawWriter.writeRawTags(_writable, Integer.MAX_VALUE);%n");}}).generate(buffer);
:buffer.decrementIndent();
:buffer.printf("}%n");
end
start
note right
void generateCheckForUnsupportedNumTaggedFields(Stringconditional)
end note
:buffer.printf("if (%s) {%n",conditional);
:buffer.incrementIndent();
:headerGenerator.addImport(MessageGenerator.UNSUPPORTED_VERSION_EXCEPTION_CLASS);
:buffer.printf("throw new UnsupportedVersionException(\"Tagged fields were set, "+
"but version \" + _version + \" of this message does not support them.\");%n");
:buffer.decrementIndent();
:buffer.printf("}%n");
end
start
note right
StringprimitiveWriteExpression(FieldTypetype,
Stringname)
end note
if (type instanceof FieldType.BoolFieldType) then (yes) 
:return String.format("_writable.writeByte(%s ? (byte) 1 : (byte) 0)",
name);;
stop
else (no)
:throw new RuntimeException("Unsupported field type "+
type);;
endif 
end
start
note right
void generateVariableLengthWriter(VersionsfieldFlexibleVersions,
Stringname,FieldTypetype,VersionspossibleVersions,
VersionsnullableVersions,booleanzeroCopy)
end note
:IsNullConditional.forName(name).possibleVersions(possibleVersions)
.nullableVersions(nullableVersions).alwaysEmitBlockScope(type.isString())
.ifNull(()->{VersionConditional.forVersions(nullableVersions,
possibleVersions).ifMember(presentVersions->{VersionConditional.forVersions(fieldFlexibleVersions,
presentVersions).ifMember(___->{buffer.printf("_writable.writeUnsignedVarint(0);%n");})
.ifNotMember(___->{if (type.isString()){buffer.printf("_writable.writeShort((short) -1);%n");}else {buffer.printf("_writable.writeInt(-1);%n");}}).generate(buffer);}).ifNotMember(__->{buffer.printf("throw new NullPointerException();%n");}).generate(buffer);}).ifShouldNotBeNull(()->{final StringlengthExpression;if (type.isString()){buffer.printf("byte[] _stringBytes = _cache.getSerializedValue(%s);%n",name);lengthExpression="_stringBytes.length";}else if (type.isBytes()){if (zeroCopy){lengthExpression=String.format("%s.remaining()",name);}else {lengthExpression=String.format("%s.length",name);}}else if (type.isRecords()){lengthExpression=String.format("%s.sizeInBytes()",name);}else if (type.isArray()){lengthExpression=String.format("%s.size()",name);}else {throw new RuntimeException("Unhandled type "+type);}VersionConditional.forVersions(fieldFlexibleVersions,possibleVersions).ifMember(ifMemberVersions->{buffer.printf("_writable.writeUnsignedVarint(%s + 1);%n",lengthExpression);}).ifNotMember(ifNotMemberVersions->{if (type.isString()){buffer.printf("_writable.writeShort((short) %s);%n",lengthExpression);}else {buffer.printf("_writable.writeInt(%s);%n",lengthExpression);}}).generate(buffer);if (type.isString()){buffer.printf("_writable.writeByteArray(_stringBytes);%n");}else if (type.isBytes()){if (zeroCopy){buffer.printf("_writable.writeByteBuffer(%s);%n",name);}else {buffer.printf("_writable.writeByteArray(%s);%n",name);}}else if (type.isRecords()){buffer.printf("_writable.writeRecords(%s);%n",name);}else if (type.isArray()){FieldType.ArrayTypearrayType=(FieldType.ArrayType)type;FieldTypeelementType=arrayType.elementType();StringelementName=String.format("%sElement",name);buffer.printf("for (%s %s : %s) {%n",elementType.getBoxedJavaType(headerGenerator),elementName,name);buffer.incrementIndent();if (elementType.isArray()){throw new RuntimeException("Nested arrays are not supported.  "+"Use an array of structures containing another array.");}else if (elementType.isBytes()||elementType.isString()){generateVariableLengthWriter(fieldFlexibleVersions,elementName,elementType,possibleVersions,Versions.NONE,false);}else {buffer.printf("%s;%n",primitiveWriteExpression(elementType,elementName));}buffer.decrementIndent();buffer.printf("}%n");}}).generate(buffer);
end
start
note right
void generateClassMessageSize(StringclassName,
StructSpecstruct,VersionsparentVersions)
end note
:headerGenerator.addImport(MessageGenerator.OBJECT_SERIALIZATION_CACHE_CLASS);
:headerGenerator.addImport(MessageGenerator.MESSAGE_SIZE_ACCUMULATOR_CLASS);
:buffer.printf("@Override%n");
:buffer.printf("public void addSize(MessageSizeAccumulator _size,
 ObjectSerializationCache _cache, short _version) {%n");
:buffer.incrementIndent();
:buffer.printf("int _numTaggedFields = 0;
:%n");
:VersionConditional.forVersions(parentVersions,
struct.versions()).allowMembershipCheckAlwaysFalse(false)
.ifNotMember(__->{headerGenerator.addImport(MessageGenerator.UNSUPPORTED_VERSION_EXCEPTION_CLASS);buffer.printf("throw new UnsupportedVersionException(\"Can't size "+"version \" + _version + \" of %s\");%n",className);}).generate(buffer);
:VersionConditional.forVersions(field.versions(),
curVersions).ifMember(presentVersions->{VersionConditional.forVersions(field.taggedVersions(),
presentVersions).ifMember(presentAndTaggedVersions->{generateFieldSize(field,
presentAndTaggedVersions,true);}).ifNotMember(presentAndUntaggedVersions->{generateFieldSize(field,
presentAndUntaggedVersions,false);}).generate(buffer);})
.generate(buffer);
:buffer.printf("if (_unknownTaggedFields != null) {%n");
:buffer.incrementIndent();
:buffer.printf("_numTaggedFields += _unknownTaggedFields.size();
:%n");
:buffer.printf("for (RawTaggedField _field : _unknownTaggedFields) {%n");
:buffer.incrementIndent();
:headerGenerator.addImport(MessageGenerator.BYTE_UTILS_CLASS);
:buffer.printf("_size.addBytes(ByteUtils.sizeOfUnsignedVarint(_field.tag()));
:%n");
:buffer.printf("_size.addBytes(ByteUtils.sizeOfUnsignedVarint(_field.size()));
:%n");
:buffer.printf("_size.addBytes(_field.size());
:%n");
:buffer.decrementIndent();
:buffer.printf("}%n");
:buffer.decrementIndent();
:buffer.printf("}%n");
:VersionConditional.forVersions(messageFlexibleVersions,
curVersions).ifNotMember(__->{generateCheckForUnsupportedNumTaggedFields("_numTaggedFields > 0");})
.ifMember(__->{headerGenerator.addImport(MessageGenerator.BYTE_UTILS_CLASS);buffer.printf("_size.addBytes(ByteUtils.sizeOfUnsignedVarint(_numTaggedFields));%n");}).generate(buffer);
:buffer.decrementIndent();
:buffer.printf("}%n");
end
start
note right
void generateVariableLengthArrayElementSize(VersionsflexibleVersions,
StringfieldName,FieldTypetype,Versionsversions)
end note
if (type instanceof FieldType.StringFieldType) then (yes) 
:generateStringToBytes(fieldName);
:VersionConditional.forVersions(flexibleVersions,
versions).ifNotMember(__->{buffer.printf("_size.addBytes(_stringBytes.length +
 2);%n");}).ifMember(__->{headerGenerator.addImport(MessageGenerator.BYTE_UTILS_CLASS);buffer.printf("_size.addBytes(_stringBytes.length + "+"ByteUtils.sizeOfUnsignedVarint(_stringBytes.length + 1));%n");}).generate(buffer);
else (no)
:throw new RuntimeException("Unsupported type "+
type);;
endif 
end
start
note right
void generateFieldSize(FieldSpecfield,VersionspossibleVersions,
booleantagged)
end note
if (field.type().fixedLength().isPresent()) then (yes) 
:generateFixedLengthFieldSize(field,tagged);
else (no)
:generateVariableLengthFieldSize(field,possibleVersions,
tagged);
endif 
end
start
note right
void generateFixedLengthFieldSize(FieldSpecfield,
booleantagged)
end note
if (tagged) then (yes) 
:field.generateNonDefaultValueCheck(headerGenerator,
structRegistry,buffer,"this.",field.nullableVersions());
:buffer.incrementIndent();
:buffer.printf("_numTaggedFields++;%n");
:buffer.printf("_size.addBytes(%d);%n",MessageGenerator.sizeOfUnsignedVarint(field.tag()
.get()));
:buffer.printf("_size.addBytes(%d);%n",MessageGenerator.sizeOfUnsignedVarint(field.type()
.fixedLength().get()));
:buffer.printf("_size.addBytes(%d);%n",field.type()
.fixedLength().get());
:buffer.decrementIndent();
:buffer.printf("}%n");
else (no)
:buffer.printf("_size.addBytes(%d);%n",field.type()
.fixedLength().get());
endif 
end
start
note right
void generateVariableLengthFieldSize(FieldSpecfield,
VersionspossibleVersions,booleantagged)
end note
:IsNullConditional.forField(field).alwaysEmitBlockScope(true)
.possibleVersions(possibleVersions).nullableVersions(field.nullableVersions())
.ifNull(()->{if (!tagged||!field.defaultString()
.equals("null")){VersionConditional.forVersions(fieldFlexibleVersions(field),
possibleVersions).ifMember(__->{if (tagged){buffer.printf("_numTaggedFields+
+;%n");buffer.printf("_size.addBytes(%d);%n",
MessageGenerator.sizeOfUnsignedVarint(field.tag().get()));buffer.printf("_size.addBytes(%d);%n",
MessageGenerator.sizeOfUnsignedVarint(MessageGenerator.sizeOfUnsignedVarint(0)));}buffer.printf("_size.addBytes(%d);%n",
MessageGenerator.sizeOfUnsignedVarint(0));}).ifNotMember(__->{if (tagged){throw new RuntimeException("Tagged field "+
field.name()+" should not be present in non-flexible versions.");}if (field.type()
.isString()){buffer.printf("_size.addBytes(2);%n");}else {buffer.printf("_size.addBytes(4);%n");}})
.generate(buffer);}}).ifShouldNotBeNull(()->{if (tagged){if (!field.defaultString()
.equals("null")){field.generateNonDefaultValueCheck(headerGenerator,
structRegistry,buffer,"this.",Versions.NONE);buffer.incrementIndent();}buffer.printf("_numTaggedFields+
+;%n");buffer.printf("_size.addBytes(%d);%n",
MessageGenerator.sizeOfUnsignedVarint(field.tag()
.get()));}if (field.type().isString()){generateStringToBytes(field.camelCaseName());VersionConditional.forVersions(fieldFlexibleVersions(field),possibleVersions).ifMember(__->{headerGenerator.addImport(MessageGenerator.BYTE_UTILS_CLASS);if (tagged){buffer.printf("int _stringPrefixSize = "+"ByteUtils.sizeOfUnsignedVarint(_stringBytes.length + 1);%n");buffer.printf("_size.addBytes(_stringBytes.length + _stringPrefixSize + "+"ByteUtils.sizeOfUnsignedVarint(_stringPrefixSize + _stringBytes.length));%n");}else {buffer.printf("_size.addBytes(_stringBytes.length + "+"ByteUtils.sizeOfUnsignedVarint(_stringBytes.length + 1));%n");}}).ifNotMember(__->{if (tagged){throw new RuntimeException("Tagged field "+field.name()+" should not be present in non-flexible versions.");}buffer.printf("_size.addBytes(_stringBytes.length + 2);%n");}).generate(buffer);}else if (field.type().isArray()){if (tagged){buffer.printf("int _sizeBeforeArray = _size.totalSize();%n");}VersionConditional.forVersions(fieldFlexibleVersions(field),possibleVersions).ifMember(__->{headerGenerator.addImport(MessageGenerator.BYTE_UTILS_CLASS);buffer.printf("_size.addBytes(ByteUtils.sizeOfUnsignedVarint(%s.size() + 1));%n",field.camelCaseName());}).ifNotMember(__->{buffer.printf("_size.addBytes(4);%n");}).generate(buffer);FieldType.ArrayTypearrayType=(FieldType.ArrayType)field.type();FieldTypeelementType=arrayType.elementType();if (elementType.fixedLength().isPresent()){buffer.printf("_size.addBytes(%s.size() * %d);%n",field.camelCaseName(),elementType.fixedLength().get());}else if (elementType instanceof FieldType.ArrayType){throw new RuntimeException("Arrays of arrays are not supported "+"(use a struct).");}else {buffer.printf("for (%s %sElement : %s) {%n",elementType.getBoxedJavaType(headerGenerator),field.camelCaseName(),field.camelCaseName());buffer.incrementIndent();generateVariableLengthArrayElementSize(fieldFlexibleVersions(field),String.format("%sElement",field.camelCaseName()),elementType,possibleVersions);buffer.decrementIndent();buffer.printf("}%n");}if (tagged){headerGenerator.addImport(MessageGenerator.BYTE_UTILS_CLASS);buffer.printf("int _arraySize = _size.totalSize() - _sizeBeforeArray;%n");buffer.printf("_cache.setArraySizeInBytes(%s, _arraySize);%n",field.camelCaseName());buffer.printf("_size.addBytes(ByteUtils.sizeOfUnsignedVarint(_arraySize));%n");}}else if (field.type().isBytes()){if (tagged){buffer.printf("int _sizeBeforeBytes = _size.totalSize();%n");}if (field.zeroCopy()){buffer.printf("_size.addZeroCopyBytes(%s.remaining());%n",field.camelCaseName());}else {buffer.printf("_size.addBytes(%s.length);%n",field.camelCaseName());}VersionConditional.forVersions(fieldFlexibleVersions(field),possibleVersions).ifMember(__->{headerGenerator.addImport(MessageGenerator.BYTE_UTILS_CLASS);if (field.zeroCopy()){buffer.printf("_size.addBytes("+"ByteUtils.sizeOfUnsignedVarint(%s.remaining() + 1));%n",field.camelCaseName());}else {buffer.printf("_size.addBytes(ByteUtils.sizeOfUnsignedVarint(%s.length + 1));%n",field.camelCaseName());}}).ifNotMember(__->{buffer.printf("_size.addBytes(4);%n");}).generate(buffer);if (tagged){headerGenerator.addImport(MessageGenerator.BYTE_UTILS_CLASS);buffer.printf("int _bytesSize = _size.totalSize() - _sizeBeforeBytes;%n");buffer.printf("_size.addBytes(ByteUtils.sizeOfUnsignedVarint(_bytesSize));%n");}}else if (field.type().isRecords()){buffer.printf("_size.addZeroCopyBytes(%s.sizeInBytes());%n",field.camelCaseName());VersionConditional.forVersions(fieldFlexibleVersions(field),possibleVersions).ifMember(__->{headerGenerator.addImport(MessageGenerator.BYTE_UTILS_CLASS);buffer.printf("_size.addBytes("+"ByteUtils.sizeOfUnsignedVarint(%s.sizeInBytes() + 1));%n",field.camelCaseName());}).ifNotMember(__->{buffer.printf("_size.addBytes(4);%n");}).generate(buffer);}else if (field.type().isStruct()){buffer.printf("int _sizeBeforeStruct = _size.totalSize();%n",field.camelCaseName());buffer.printf("this.%s.addSize(_size, _cache, _version);%n",field.camelCaseName());buffer.printf("int _structSize = _size.totalSize() - _sizeBeforeStruct;%n",field.camelCaseName());if (tagged){buffer.printf("_size.addBytes(ByteUtils.sizeOfUnsignedVarint(_structSize));%n");}}else {throw new RuntimeException("unhandled type "+field.type());}if (tagged&&!field.defaultString().equals("null")){buffer.decrementIndent();buffer.printf("}%n");}}).generate(buffer);
end
start
note right
void generateStringToBytes(Stringname)
end note
:headerGenerator.addImport(MessageGenerator.STANDARD_CHARSETS);
:buffer.printf("byte[] _stringBytes = %s.getBytes(StandardCharsets.UTF_8);%n",
name);
:buffer.printf("if (_stringBytes.length > 0x7fff) {%n");
:buffer.incrementIndent();
:buffer.printf("throw new RuntimeException(\"'%s' field is too long to "+
"be serialized\");%n",name);
:buffer.decrementIndent();
:buffer.printf("}%n");
:buffer.printf("_cache.cacheSerializedValue(%s,
 _stringBytes);%n",name);
end
start
note right
void generateClassEquals(StringclassName,
StructSpecstruct,booleanelementKeysAreEqual)
end note
:buffer.printf("@Override%n");
:buffer.printf("public boolean %s(Object obj) {%n",
elementKeysAreEqual? String.valueOf("elementKeysAreEqual"):String.valueOf("equals"));
:buffer.incrementIndent();
:buffer.printf("if (!(obj instanceof %s)) return false;%n",
className);
:buffer.printf("%s other = (%s) obj;%n",className,
className);
if (!struct.fields().isEmpty()) then (yes) 
if (!elementKeysAreEqual||field.mapKey()) then (yes) 
:generateFieldEquals(field);
endif 
endif 
if (elementKeysAreEqual) then (yes) 
:buffer.printf("return true;%n");
else (no)
:headerGenerator.addImport(MessageGenerator.MESSAGE_UTIL_CLASS);
:buffer.printf("return MessageUtil.compareRawTaggedFields(_unknownTaggedFields, "+
"other._unknownTaggedFields);%n");
endif 
:buffer.decrementIndent();
:buffer.printf("}%n");
end
start
note right
void generateFieldEquals(FieldSpecfield)
end note
if (field.type() instanceof FieldType.UUIDFieldType) then (yes) 
:buffer.printf("if (!this.%s.equals(other.%s)) return false;%n",
field.camelCaseName(),field.camelCaseName());
else (no)
:buffer.printf("if (%s != other.%s) return false;%n",
field.camelCaseName(),field.camelCaseName());
endif 
end
start
note right
void generateClassHashCode(StructSpecstruct,
booleanonlyMapKeys)
end note
:buffer.printf("@Override%n");
:buffer.printf("public int hashCode() {%n");
:buffer.incrementIndent();
:buffer.printf("int hashCode = 0;%n");
if ((!onlyMapKeys)||field.mapKey()) then (yes) 
:generateFieldHashCode(field);
endif 
:buffer.printf("return hashCode;%n");
:buffer.decrementIndent();
:buffer.printf("}%n");
end
start
note right
void generateFieldHashCode(FieldSpecfield)
end note
if (field.type() instanceof FieldType.BoolFieldType) then (yes) 
:buffer.printf("hashCode = 31 * hashCode +
 (%s ? 1231 : 1237);%n",field.camelCaseName());
else (no)
:throw new RuntimeException("Unsupported field type "+
field.type());;
endif 
end
start
note right
void generateClassDuplicate(StringclassName,
StructSpecstruct)
end note
:buffer.printf("@Override%n");
:buffer.printf("public %s duplicate() {%n",
className);
:buffer.incrementIndent();
:buffer.printf("%s _duplicate = new %s();%n",
className,className);
:generateFieldDuplicate(new Target(field,
field.camelCaseName(),field.camelCaseName(),
input->String.format("_duplicate.%s = %s",
field.camelCaseName(),input)));
:buffer.printf("return _duplicate;%n");
:buffer.decrementIndent();
:buffer.printf("}%n");
end
start
note right
void generateFieldDuplicate(Targettarget)
end note
if ((field.type() instanceof FieldType.BoolFieldType)||(field.type() instanceof FieldType.Int8FieldType)||(field.type() instanceof FieldType.Int16FieldType)||(field.type() instanceof FieldType.Uint16FieldType)||(field.type() instanceof FieldType.Uint32FieldType)||(field.type() instanceof FieldType.Int32FieldType)||(field.type() instanceof FieldType.Int64FieldType)||(field.type() instanceof FieldType.Float64FieldType)||(field.type() instanceof FieldType.UUIDFieldType)) then (yes) 
:buffer.printf("%s;%n",target.assignmentStatement(target.sourceVariable()));
else (no)
if (field.type().isBytes()) then (yes) 
if (field.zeroCopy()) then (yes) 
:cond.ifShouldNotBeNull(()->buffer.printf("%s;%n",
target.assignmentStatement(String.format("%s.duplicate()",
target.sourceVariable()))));
else (no)
:cond.ifShouldNotBeNull(()->{headerGenerator.addImport(MessageGenerator.MESSAGE_UTIL_CLASS);buffer.printf("%s;%n",
target.assignmentStatement(String.format("MessageUtil.duplicate(%s)",
target.sourceVariable())));});
endif 
else (no)
:throw new RuntimeException("Unhandled field type "+
field.type());;
endif 
:cond.generate(buffer);
endif 
end
start
note right
void generateClassToString(StringclassName,
StructSpecstruct)
end note
:buffer.printf("@Override%n");
:buffer.printf("public String toString() {%n");
:buffer.incrementIndent();
:buffer.printf("return \"%s(\"%n",className);
:buffer.incrementIndent();
:generateFieldToString(prefix,field);
:prefix=", ";
:buffer.printf("+ \")\";%n");
:buffer.decrementIndent();
:buffer.decrementIndent();
:buffer.printf("}%n");
end
start
note right
void generateFieldToString(Stringprefix,FieldSpecfield)
end note
if (field.type() instanceof FieldType.BoolFieldType) then (yes) 
:buffer.printf("+ \"%s%s=\" + (%s ? \"true\" : \"false\")%n",
prefix,field.camelCaseName(),field.camelCaseName());
else (no)
:throw new RuntimeException("Unsupported field type "+
field.type());;
endif 
end
start
note right
void generateFieldAccessor(FieldSpecfield)
end note
:buffer.printf("%n");
:generateAccessor(field.fieldAbstractJavaType(headerGenerator,
structRegistry),field.camelCaseName(),field.camelCaseName());
end
start
note right
void generateAccessor(StringjavaType,StringfunctionName,
StringmemberName)
end note
:buffer.printf("public %s %s() {%n",javaType,
functionName);
:buffer.incrementIndent();
:buffer.printf("return this.%s;%n",memberName);
:buffer.decrementIndent();
:buffer.printf("}%n");
end
start
note right
void generateFieldMutator(StringclassName,
FieldSpecfield)
end note
:buffer.printf("%n");
:buffer.printf("public %s set%s(%s v) {%n",
className,field.capitalizedCamelCaseName(),
field.fieldAbstractJavaType(headerGenerator,
structRegistry));
:buffer.incrementIndent();
if (field.type() instanceof FieldType.Uint16FieldType) then (yes) 
:buffer.printf("if (v < 0 || v > %d) {%n",
MessageGenerator.UNSIGNED_SHORT_MAX);
:buffer.incrementIndent();
:buffer.printf("throw new RuntimeException(\"Invalid value \" +
 v + "+"\" for unsigned short field.\");%n");
:buffer.decrementIndent();
:buffer.printf("}%n");
endif 
if (field.type() instanceof FieldType.Uint32FieldType) then (yes) 
:buffer.printf("if (v < 0 || v > %dL) {%n",
MessageGenerator.UNSIGNED_INT_MAX);
:buffer.incrementIndent();
:buffer.printf("throw new RuntimeException(\"Invalid value \" +
 v + "+"\" for unsigned int field.\");%n");
:buffer.decrementIndent();
:buffer.printf("}%n");
endif 
:buffer.printf("this.%s = v;%n",field.camelCaseName());
:buffer.printf("return this;%n");
:buffer.decrementIndent();
:buffer.printf("}%n");
end
start
note right
void generateSetter(StringjavaType,StringfunctionName,
StringmemberName)
end note
:buffer.printf("public void %s(%s v) {%n",
functionName,javaType);
:buffer.incrementIndent();
:buffer.printf("this.%s = v;%n",memberName);
:buffer.decrementIndent();
:buffer.printf("}%n");
end
start
note right
VersionsfieldFlexibleVersions(FieldSpecfield)
end note
if (field.flexibleVersions().isPresent()) then (yes) 
if (!messageFlexibleVersions.intersect(field.flexibleVersions()
.get()).equals(field.flexibleVersions().get())) then (yes) 
:throw new RuntimeException("The flexible versions for field "+
field.name()+" are "+field.flexibleVersions().get()+
", which are not a subset of the flexible versions for the "+
"message as a whole, which are "+messageFlexibleVersions);;
endif 
:return field.flexibleVersions().get();;
stop
else (no)
:return messageFlexibleVersions;;
stop
endif 
end
@enduml

PlantUML version 1.2022.7(Mon Aug 22 17:01:30 UTC 2022)
(GPL source distribution)
Java Runtime: OpenJDK Runtime Environment
JVM: OpenJDK 64-Bit Server VM
Default Encoding: UTF-8
Language: en
Country: null
--></g></svg>