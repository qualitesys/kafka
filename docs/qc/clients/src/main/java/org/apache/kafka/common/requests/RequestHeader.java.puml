@startuml
!pragma layout smetana
AbstractRequestResponse <|-- org.apache.kafka.common.requests.RequestHeader
class org.apache.kafka.common.requests.RequestHeader {
+  RequestHeader(ApiKeys;short;String;int)
+  RequestHeader(RequestHeaderData;short)
+  apiKey()
+  apiVersion()
+  headerVersion()
+  clientId()
+  correlationId()
+  data()
+  write(ByteBuffer;ObjectSerializationCache)
+  size(ObjectSerializationCache)
+  toResponseHeader()
+  parse(ByteBuffer)
+  toString()
+  equals(Object)
+  hashCode()
} 
@enduml
