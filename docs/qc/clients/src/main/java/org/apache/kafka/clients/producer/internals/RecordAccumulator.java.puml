@startuml
!pragma layout smetana
java.lang.Object <|-- org.apache.kafka.clients.producer.internals.RecordAccumulator
class org.apache.kafka.clients.producer.internals.RecordAccumulator {
+  RecordAccumulator(LogContext;int;CompressionType;int;long;int;PartitionerConfig;Metrics;String;Time;ApiVersions;TransactionManager;BufferPool)
+  RecordAccumulator(LogContext;int;CompressionType;int;long;int;Metrics;String;Time;ApiVersions;TransactionManager;BufferPool)
+  registerMetrics(Metrics;String)
+  setPartition(AppendCallbacks;int)
+  append(String;int;long;byte[];byte[];Header[];AppendCallbacks;long;boolean;long;Cluster)
+  appendNewBatch(String;int;Deque<ProducerBatch>;long;byte[];byte[];Header[];AppendCallbacks;ByteBuffer)
+  recordsBuilder(ByteBuffer;byte)
+  tryAppend(long;byte[];byte[];Header[];Callback;Deque<ProducerBatch>;long)
+  isMuted(TopicPartition)
+  resetNextBatchExpiryTime()
+  maybeUpdateNextBatchExpiryTime(ProducerBatch)
+  expiredBatches(long)
+  getDeliveryTimeoutMs()
+  reenqueue(ProducerBatch;long)
+  splitAndReenqueue(ProducerBatch)
+  insertInSequenceOrder(Deque<ProducerBatch>;ProducerBatch)
+  batchReady(long;boolean;TopicPartition;Node;long;boolean;boolean;long;Set<Node>)
+  partitionReady(Cluster;long;String;TopicInfo;long;Set<Node>;Set<String>)
+  ready(Cluster;long)
+  hasUndrained()
+  shouldStopDrainBatchesForPartition(ProducerBatch;TopicPartition)
+  drainBatchesForOneNode(Cluster;Node;int;long)
+  getDrainIndex(String)
+  updateDrainIndex(String;int)
+  drain(Cluster;Set<Node>;int;long)
+  updateNodeLatencyStats(Integer;long;boolean)
+  getNodeLatencyStats(Integer)
+  getBuiltInPartitioner(String)
+  nextExpiryTimeMs()
+  getDeque(TopicPartition)
+  getOrCreateDeque(TopicPartition)
+  deallocate(ProducerBatch)
+  bufferPoolAvailableMemory()
+  flushInProgress()
+  beginFlush()
+  appendsInProgress()
+  awaitFlushCompletion()
+  hasIncomplete()
+  abortIncompleteBatches()
+  abortBatches()
+  abortBatches(RuntimeException)
+  abortUndrainedBatches(RuntimeException)
+  mutePartition(TopicPartition)
+  unmutePartition(TopicPartition)
+  close()
} 
@enduml
