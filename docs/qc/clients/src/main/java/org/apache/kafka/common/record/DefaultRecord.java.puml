@startuml
!pragma layout smetana
Record <|-- org.apache.kafka.common.record.DefaultRecord
class org.apache.kafka.common.record.DefaultRecord {
+  DefaultRecord(int;byte;long;long;int;ByteBuffer;ByteBuffer;Header[])
+  offset()
+  sequence()
+  sizeInBytes()
+  timestamp()
+  attributes()
+  ensureValid()
+  keySize()
+  valueSize()
+  hasKey()
+  key()
+  hasValue()
+  value()
+  headers()
+  writeTo(DataOutputStream;int;long;ByteBuffer;ByteBuffer;Header[])
+  hasMagic(byte)
+  isCompressed()
+  hasTimestampType(TimestampType)
+  toString()
+  equals(Object)
+  hashCode()
+  readFrom(DataInput;long;long;int;Long)
+  readFrom(ByteBuffer;long;long;int;Long)
+  readFrom(ByteBuffer;int;int;long;long;int;Long)
+  readPartiallyFrom(DataInput;byte[];long;long;int;Long)
+  readPartiallyFrom(DataInput;byte[];int;int;long;long;int;Long)
+  readByte(ByteBuffer;DataInput;IntRef)
+  readVarLong(ByteBuffer;DataInput;IntRef)
+  readVarInt(ByteBuffer;DataInput;IntRef)
+  skipLengthDelimitedField(ByteBuffer;DataInput;IntRef)
+  readMore(ByteBuffer;DataInput;IntRef)
+  readHeaders(ByteBuffer;int)
+  sizeInBytes(int;long;ByteBuffer;ByteBuffer;Header[])
+  sizeInBytes(int;long;int;int;Header[])
+  sizeOfBodyInBytes(int;long;ByteBuffer;ByteBuffer;Header[])
+  sizeOfBodyInBytes(int;long;int;int;Header[])
+  sizeOf(int;int;Header[])
+  recordSizeUpperBound(ByteBuffer;ByteBuffer;Header[])
} 
@enduml
