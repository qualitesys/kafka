@startuml
!pragma layout smetana
Producer<K,V> <|-- org.apache.kafka.clients.producer.KafkaProducer
class org.apache.kafka.clients.producer.KafkaProducer {
+  KafkaProducer(Map<String,Object>)
+  KafkaProducer(Map<String,Object>;Serializer<K>;Serializer<V>)
+  KafkaProducer(Properties)
+  KafkaProducer(Properties;Serializer<K>;Serializer<V>)
+  warnIfPartitionerDeprecated()
+  KafkaProducer(ProducerConfig;Serializer<K>;Serializer<V>;ProducerMetadata;KafkaClient;ProducerInterceptors<K,V>;Time)
+  KafkaProducer(ProducerConfig;LogContext;Metrics;Serializer<K>;Serializer<V>;ProducerMetadata;RecordAccumulator;TransactionManager;Sender;ProducerInterceptors<K,V>;Partitioner;Time;KafkaThread)
+  newSender(LogContext;KafkaClient;ProducerMetadata)
+  lingerMs(ProducerConfig)
+  configureDeliveryTimeout(ProducerConfig;Logger)
+  configureTransactionState(ProducerConfig;LogContext)
+  initTransactions()
+  beginTransaction()
+  sendOffsetsToTransaction(Map<TopicPartition,OffsetAndMetadata>;String)
+  sendOffsetsToTransaction(Map<TopicPartition,OffsetAndMetadata>;ConsumerGroupMetadata)
+  commitTransaction()
+  abortTransaction()
+  send(ProducerRecord<K,V>)
+  send(ProducerRecord<K,V>;Callback)
+  throwIfProducerClosed()
+  onNewBatch(String;Cluster;int)
+  doSend(ProducerRecord<K,V>;Callback)
+  setReadOnly(Headers)
+  waitOnMetadata(String;Integer;long;long)
+  ensureValidRecordSize(int)
+  flush()
+  partitionsFor(String)
+  metrics()
+  close()
+  close(Duration)
+  close(Duration;boolean)
+  configureClusterResourceListeners(Serializer<K>;Serializer<V>;List<?>...)
+  partition(ProducerRecord<K,V>;byte[];byte[];Cluster)
+  throwIfInvalidGroupMetadata(ConsumerGroupMetadata)
+  throwIfNoTransactionManager()
+  getClientId()
} 
@enduml
