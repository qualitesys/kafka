<?xml version="1.0" encoding="UTF-8" standalone="no"?><svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" contentStyleType="text/css" height="586px" preserveAspectRatio="none" style="width:851px;height:586px;background:#000000;" version="1.1" viewBox="0 0 851 586" width="851px" zoomAndPan="magnify"><defs/><g><rect fill="#080114" height="1" style="stroke:#080114;stroke-width:1.0;" width="1" x="0" y="0"/><text fill="#33FF02" font-family="sans-serif" font-size="12" font-style="italic" font-weight="bold" lengthAdjust="spacing" textLength="127" x="5" y="17">PlantUML 1.2022.7</text><text fill="#33FF02" font-family="sans-serif" font-size="12" font-style="italic" font-weight="bold" lengthAdjust="spacing" textLength="4" x="5" y="30.9688"> </text><text fill="#33FF02" font-family="sans-serif" font-size="12" font-style="italic" font-weight="bold" lengthAdjust="spacing" textLength="399" x="5" y="44.9375">&lt;b&gt;This version of PlantUML is 138 days old, so you should</text><text fill="#33FF02" font-family="sans-serif" font-size="12" font-style="italic" font-weight="bold" lengthAdjust="spacing" textLength="408" x="5" y="58.9063">&lt;b&gt;consider upgrading from https://plantuml.com/download</text><rect fill="#33FF02" height="21.2969" style="stroke:#33FF02;stroke-width:1.0;" width="839" x="5" y="68.875"/><text fill="#000000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="188" x="6" y="83.875">[From string (line 368) ]</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="5" x="5" y="104.1719"> </text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="79" x="5" y="120.4688">@startuml</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="190" x="5" y="136.7656">!pragma layout smetana</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="153.0625">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="169.3594">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="452" x="5" y="185.6563">constructor KafkaConsumer(Map&lt;String,Object&gt;configs)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="15" x="5" y="201.9531">...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="185" x="5" y="218.25">... ( skipping 343 lines )</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="15" x="5" y="234.5469">...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="35" x="5" y="250.8438">stop</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="30" x="5" y="267.1406">end</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="283.4375">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="299.7344">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="360" x="5" y="316.0313">ConsumerRecords&lt;K,V&gt;poll(final Timertimer,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="314" x="5" y="332.3281">final booleanincludeMetadataInTimeout)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="71" x="5" y="348.625">end note</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="217" x="5" y="364.9219">:acquireAndEnsureOpen();;</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="39" x="5" y="381.2188">:try ;</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="546" x="5" y="397.5156">:this.kafkaConsumerMetrics.recordPollStart(timer.currentTimeMs());;</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="568" x="5" y="413.8125">if (this.subscriptions.hasNoSubscriptionOrUserAssignment()) then (yes)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="839" x="5" y="430.1094">:throw new IllegalStateException("Consumer is not subscribed to any topics or assigned any partitions");;</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="35" x="5" y="446.4063">stop</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="40" x="5" y="462.7031">endif</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="52" x="5" y="479">repeat</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="251" x="5" y="495.2969">:client.maybeTriggerWakeup();;</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="320" x="5" y="511.5938">if (includeMetadataInTimeout) then (yes)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="405" x="5" y="527.8906">:updateAssignmentMetadataIfNeeded(timer,false);;</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="70" x="5" y="544.1875">else (no)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" text-decoration="wavy underline" textLength="589" x="5" y="560.4844">while (!updateAssignmentMetadataIfNeeded(time.timer(Long.MAX_VALUE),</text><text fill="#FF0000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="106" x="10" y="576.7813">Syntax Error?</text><!--MD5=[972b4a3f1c61942032fe847b36e88daf]
@startuml
!pragma layout smetana
start
note right
constructor KafkaConsumer(Map<String,Object>configs)
end note
:this(configs,null ,null );;
end
start
note right
constructor KafkaConsumer(Propertiesproperties)
end note
:this(properties,null ,null );;
end
start
note right
constructor KafkaConsumer(Propertiesproperties,
Deserializer<K>keyDeserializer,Deserializer<V>valueDeserializer)
end note
:this(Utils.propsToMap(properties),keyDeserializer,
valueDeserializer);;
end
start
note right
constructor KafkaConsumer(Map<String,Object>configs,
Deserializer<K>keyDeserializer,Deserializer<V>valueDeserializer)
end note
:this(new ConsumerConfig(ConsumerConfig.appendDeserializerToConfig(configs,
keyDeserializer,valueDeserializer)),keyDeserializer,
valueDeserializer);;
end
start
note right
constructor KafkaConsumer(ConsumerConfigconfig,
Deserializer<K>keyDeserializer,Deserializer<V>valueDeserializer)
end note
:try ;
:GroupRebalanceConfiggroupRebalanceConfig=new GroupRebalanceConfig(config,
GroupRebalanceConfig.ProtocolType.CONSUMER);;
:this.groupId=Optional.ofNullable(groupRebalanceConfig.groupId);;
:this.clientId=config.getString(CommonClientConfigs.CLIENT_ID_CONFIG);;
:LogContextlogContext;;
if (groupRebalanceConfig.groupInstanceId.isPresent()) then (yes) 
:logContext=new LogContext("[Consumer instanceId="+
groupRebalanceConfig.groupInstanceId.get()+
", clientId="+clientId+", groupId="+groupId.orElse("null")+
"] ");;
else (no)
:logContext=new LogContext("[Consumer clientId="+
clientId+", groupId="+groupId.orElse("null")+
"] ");;
endif 
:this.log=logContext.logger(getClass());;
:booleanenableAutoCommit=config.maybeOverrideEnableAutoCommit();;
:groupId.ifPresent(groupIdStr->{if (groupIdStr.isEmpty()){log.warn("Support for using the empty group id by consumers is deprecated and will be removed in the next major release.");
:}});;
:log.debug("Initializing the Kafka consumer");;
:this.requestTimeoutMs=config.getInt(ConsumerConfig.REQUEST_TIMEOUT_MS_CONFIG);;
:this.defaultApiTimeoutMs=config.getInt(ConsumerConfig.DEFAULT_API_TIMEOUT_MS_CONFIG);;
:this.time=Time.SYSTEM;;
:this.metrics=buildMetrics(config,time,clientId);;
:this.retryBackoffMs=config.getLong(ConsumerConfig.RETRY_BACKOFF_MS_CONFIG);;
:List<ConsumerInterceptor<K,V>>interceptorList=(List)config.getConfiguredInstances(ConsumerConfig.INTERCEPTOR_CLASSES_CONFIG,
ConsumerInterceptor.class ,Collections.singletonMap(ConsumerConfig.CLIENT_ID_CONFIG,
clientId));;
:this.interceptors=new ConsumerInterceptors<>(interceptorList);;
if (keyDeserializer==null ) then (yes) 
:this.keyDeserializer=config.getConfiguredInstance(ConsumerConfig.KEY_DESERIALIZER_CLASS_CONFIG,
Deserializer.class );;
:this.keyDeserializer.configure(config.originals(Collections.singletonMap(ConsumerConfig.CLIENT_ID_CONFIG,
clientId)),true);;
else (no)
:config.ignore(ConsumerConfig.KEY_DESERIALIZER_CLASS_CONFIG);;
:this.keyDeserializer=keyDeserializer;;
endif 
if (valueDeserializer==null ) then (yes) 
:this.valueDeserializer=config.getConfiguredInstance(ConsumerConfig.VALUE_DESERIALIZER_CLASS_CONFIG,
Deserializer.class );;
:this.valueDeserializer.configure(config.originals(Collections.singletonMap(ConsumerConfig.CLIENT_ID_CONFIG,
clientId)),false);;
else (no)
:config.ignore(ConsumerConfig.VALUE_DESERIALIZER_CLASS_CONFIG);;
:this.valueDeserializer=valueDeserializer;;
endif 
:OffsetResetStrategyoffsetResetStrategy=OffsetResetStrategy.valueOf(config.getString(ConsumerConfig.AUTO_OFFSET_RESET_CONFIG)
.toUpperCase(Locale.ROOT));;
:this.subscriptions=new SubscriptionState(logContext,
offsetResetStrategy);;
:ClusterResourceListenersclusterResourceListeners=configureClusterResourceListeners(keyDeserializer,
valueDeserializer,metrics.reporters(),interceptorList);;
:this.metadata=new ConsumerMetadata(retryBackoffMs,
config.getLong(ConsumerConfig.METADATA_MAX_AGE_CONFIG),
!config.getBoolean(ConsumerConfig.EXCLUDE_INTERNAL_TOPICS_CONFIG),
config.getBoolean(ConsumerConfig.ALLOW_AUTO_CREATE_TOPICS_CONFIG),
subscriptions,logContext,clusterResourceListeners);;
:List<InetSocketAddress>addresses=ClientUtils.parseAndValidateAddresses(config.getList(ConsumerConfig.BOOTSTRAP_SERVERS_CONFIG),
config.getString(ConsumerConfig.CLIENT_DNS_LOOKUP_CONFIG));;
:this.metadata.bootstrap(addresses);;
:StringmetricGrpPrefix="consumer";;
:FetcherMetricsRegistrymetricsRegistry=new FetcherMetricsRegistry(Collections.singleton(CLIENT_ID_METRIC_TAG),
metricGrpPrefix);;
:ChannelBuilderchannelBuilder=ClientUtils.createChannelBuilder(config,
time,logContext);;
:this.isolationLevel=IsolationLevel.valueOf(config.getString(ConsumerConfig.ISOLATION_LEVEL_CONFIG)
.toUpperCase(Locale.ROOT));;
:SensorthrottleTimeSensor=Fetcher.throttleTimeSensor(metrics,
metricsRegistry);;
:intheartbeatIntervalMs=config.getInt(ConsumerConfig.HEARTBEAT_INTERVAL_MS_CONFIG);;
:ApiVersionsapiVersions=new ApiVersions();;
:NetworkClientnetClient=new NetworkClient(new Selector(config.getLong(ConsumerConfig.CONNECTIONS_MAX_IDLE_MS_CONFIG),
metrics,time,metricGrpPrefix,channelBuilder,
logContext),this.metadata,clientId,100,config.getLong(ConsumerConfig.RECONNECT_BACKOFF_MS_CONFIG),
config.getLong(ConsumerConfig.RECONNECT_BACKOFF_MAX_MS_CONFIG),
config.getInt(ConsumerConfig.SEND_BUFFER_CONFIG),
config.getInt(ConsumerConfig.RECEIVE_BUFFER_CONFIG),
config.getInt(ConsumerConfig.REQUEST_TIMEOUT_MS_CONFIG),
config.getLong(ConsumerConfig.SOCKET_CONNECTION_SETUP_TIMEOUT_MS_CONFIG),
config.getLong(ConsumerConfig.SOCKET_CONNECTION_SETUP_TIMEOUT_MAX_MS_CONFIG),
time,true,apiVersions,throttleTimeSensor,
logContext);;
:this.client=new ConsumerNetworkClient(logContext,
netClient,metadata,time,retryBackoffMs,config.getInt(ConsumerConfig.REQUEST_TIMEOUT_MS_CONFIG),
heartbeatIntervalMs);;
:this.assignors=ConsumerPartitionAssignor.getAssignorInstances(config.getList(ConsumerConfig.PARTITION_ASSIGNMENT_STRATEGY_CONFIG),
config.originals(Collections.singletonMap(ConsumerConfig.CLIENT_ID_CONFIG,
clientId)));;
if (!groupId.isPresent()) then (yes) 
:config.ignore(ConsumerConfig.AUTO_COMMIT_INTERVAL_MS_CONFIG);;
:config.ignore(ConsumerConfig.THROW_ON_FETCH_STABLE_OFFSET_UNSUPPORTED);;
:this.coordinator=null ;;
else (no)
:this.coordinator=new ConsumerCoordinator(groupRebalanceConfig,
logContext,this.client,assignors,this.metadata,
this.subscriptions,metrics,metricGrpPrefix,
this.time,enableAutoCommit,config.getInt(ConsumerConfig.AUTO_COMMIT_INTERVAL_MS_CONFIG),
this.interceptors,config.getBoolean(ConsumerConfig.THROW_ON_FETCH_STABLE_OFFSET_UNSUPPORTED));;
endif 
:this.fetcher=new Fetcher<>(logContext,this.client,
config.getInt(ConsumerConfig.FETCH_MIN_BYTES_CONFIG),
config.getInt(ConsumerConfig.FETCH_MAX_BYTES_CONFIG),
config.getInt(ConsumerConfig.FETCH_MAX_WAIT_MS_CONFIG),
config.getInt(ConsumerConfig.MAX_PARTITION_FETCH_BYTES_CONFIG),
config.getInt(ConsumerConfig.MAX_POLL_RECORDS_CONFIG),
config.getBoolean(ConsumerConfig.CHECK_CRCS_CONFIG),
config.getString(ConsumerConfig.CLIENT_RACK_CONFIG),
this.keyDeserializer,this.valueDeserializer,
this.metadata,this.subscriptions,metrics,
metricsRegistry,this.time,this.retryBackoffMs,
this.requestTimeoutMs,isolationLevel,apiVersions);;
:this.kafkaConsumerMetrics=new KafkaConsumerMetrics(metrics,
metricGrpPrefix);;
:config.logUnused();;
:AppInfoParser.registerAppInfo(JMX_PREFIX,
clientId,metrics,time.milliseconds());;
:log.debug("Kafka consumer initialized");;
:catch (Throwablet);
if (this.log!=null ) then (yes) 
:close(0,true);;
endif 
:throw new KafkaException("Failed to construct kafka consumer",t);;
stop
end
start
note right
constructor KafkaConsumer(LogContextlogContext,
StringclientId,ConsumerCoordinatorcoordinator,
Deserializer<K>keyDeserializer,Deserializer<V>valueDeserializer,
Fetcher<K,V>fetcher,ConsumerInterceptors<K,
V>interceptors,Timetime,ConsumerNetworkClientclient,
Metricsmetrics,SubscriptionStatesubscriptions,
ConsumerMetadatametadata,longretryBackoffMs,
longrequestTimeoutMs,intdefaultApiTimeoutMs,
List<ConsumerPartitionAssignor>assignors,
StringgroupId)
end note
:this.log=logContext.logger(getClass());;
:this.clientId=clientId;;
:this.coordinator=coordinator;;
:this.keyDeserializer=keyDeserializer;;
:this.valueDeserializer=valueDeserializer;;
:this.fetcher=fetcher;;
:this.isolationLevel=IsolationLevel.READ_UNCOMMITTED;;
:this.interceptors=Objects.requireNonNull(interceptors);;
:this.time=time;;
:this.client=client;;
:this.metrics=metrics;;
:this.subscriptions=subscriptions;;
:this.metadata=metadata;;
:this.retryBackoffMs=retryBackoffMs;;
:this.requestTimeoutMs=requestTimeoutMs;;
:this.defaultApiTimeoutMs=defaultApiTimeoutMs;;
:this.assignors=assignors;;
:this.groupId=Optional.ofNullable(groupId);;
:this.kafkaConsumerMetrics=new KafkaConsumerMetrics(metrics,
"consumer");;
end
start
note right
MetricsbuildMetrics(ConsumerConfigconfig,
Timetime,StringclientId)
end note
:Map<String,String>metricsTags=Collections.singletonMap(CLIENT_ID_METRIC_TAG,
clientId);;
:MetricConfigmetricConfig=new MetricConfig().samples(config.getInt(ConsumerConfig.METRICS_NUM_SAMPLES_CONFIG)).timeWindow(config.getLong(ConsumerConfig.METRICS_SAMPLE_WINDOW_MS_CONFIG),
TimeUnit.MILLISECONDS).recordLevel(Sensor.RecordingLevel.forName(config.getString(ConsumerConfig.METRICS_RECORDING_LEVEL_CONFIG))).tags(metricsTags);;
:List<MetricsReporter>reporters=config.getConfiguredInstances(ConsumerConfig.METRIC_REPORTER_CLASSES_CONFIG,
MetricsReporter.class ,Collections.singletonMap(ConsumerConfig.CLIENT_ID_CONFIG,
clientId));;
:JmxReporterjmxReporter=new JmxReporter();;
:jmxReporter.configure(config.originals(Collections.singletonMap(ConsumerConfig.CLIENT_ID_CONFIG,
clientId)));;
:reporters.add(jmxReporter);;
:MetricsContextmetricsContext=new KafkaMetricsContext(JMX_PREFIX,
config.originalsWithPrefix(CommonClientConfigs.METRICS_CONTEXT_PREFIX));;
:return new Metrics(metricConfig,reporters,
time,metricsContext);;
stop
end
start
note right
Set<TopicPartition>assignment()
end note
:acquireAndEnsureOpen();;
:try ;
:return Collections.unmodifiableSet(this.subscriptions.assignedPartitions());;
stop
end
start
note right
Set<String>subscription()
end note
:acquireAndEnsureOpen();;
:try ;
:return Collections.unmodifiableSet(new HashSet<>(this.subscriptions.subscription()));;
stop
end
start
note right
void subscribe(Collection<String>topics,ConsumerRebalanceListenerlistener)
end note
:acquireAndEnsureOpen();;
:try ;
:maybeThrowInvalidGroupIdException();;
if (topics==null ) then (yes) 
:throw new IllegalArgumentException("Topic collection to subscribe to cannot be null");;
stop
endif 
if (topics.isEmpty()) then (yes) 
:this.unsubscribe();;
else (no)
if (Utils.isBlank(topic)) then (yes) 
:throw new IllegalArgumentException("Topic collection to subscribe to cannot contain null or empty topic");;
stop
endif 
:throwIfNoAssignorsConfigured();;
:fetcher.clearBufferedDataForUnassignedTopics(topics);;
:log.info("Subscribed to topic(s): {}",Utils.join(topics,
", "));;
if (this.subscriptions.subscribe(new HashSet<>(topics),
listener)) then (yes) 
:metadata.requestUpdateForNewTopics();;
endif 
endif 
end
start
note right
void subscribe(Collection<String>topics)
end note
:subscribe(topics,new NoOpConsumerRebalanceListener());;
end
start
note right
void subscribe(Patternpattern,ConsumerRebalanceListenerlistener)
end note
:maybeThrowInvalidGroupIdException();;
if (pattern==null ||pattern.toString().equals("")) then (yes) 
:throw new IllegalArgumentException("Topic pattern to subscribe to cannot be "+
(pattern==null ? String.valueOf("null"):String.valueOf("empty")));;
stop
endif 
:acquireAndEnsureOpen();;
:try ;
:throwIfNoAssignorsConfigured();;
:log.info("Subscribed to pattern: '{}'",pattern);;
:this.subscriptions.subscribe(pattern,listener);;
:this.coordinator.updatePatternSubscription(metadata.fetch());;
:this.metadata.requestUpdateForNewTopics();;
end
start
note right
void subscribe(Patternpattern)
end note
:subscribe(pattern,new NoOpConsumerRebalanceListener());;
end
start
note right
void unsubscribe()
end note
:acquireAndEnsureOpen();;
:try ;
:fetcher.clearBufferedDataForUnassignedPartitions(Collections.emptySet());;
if (this.coordinator!=null ) then (yes) 
:this.coordinator.onLeavePrepare();;
:this.coordinator.maybeLeaveGroup("the consumer unsubscribed from all topics");;
endif 
:this.subscriptions.unsubscribe();;
:log.info("Unsubscribed all topics or patterns and assigned partitions");;
end
start
note right
void assign(Collection<TopicPartition>partitions)
end note
:acquireAndEnsureOpen();;
:try ;
if (partitions==null ) then (yes) 
:throw new IllegalArgumentException("Topic partition collection to assign to cannot be null");;
stop
else if (partitions.isEmpty()) then (yes) 
:this.unsubscribe();;
else (no)
:Stringtopic=(tp!=null )? tp.topic():null ;;
if (Utils.isBlank(topic)) then (yes) 
:throw new IllegalArgumentException("Topic partitions to assign to cannot have null or empty topic");;
stop
endif 
:fetcher.clearBufferedDataForUnassignedPartitions(partitions);;
if (coordinator!=null ) then (yes) 
:this.coordinator.maybeAutoCommitOffsetsAsync(time.milliseconds());;
endif 
:log.info("Assigned to partition(s): {}",
Utils.join(partitions,", "));;
if (this.subscriptions.assignFromUser(new HashSet<>(partitions))) then (yes) 
:metadata.requestUpdateForNewTopics();;
endif 
endif 
end
start
note right
ConsumerRecords<K,V>poll(final longtimeoutMs)
end note
:return poll(time.timer(timeoutMs),false);;
stop
end
start
note right
ConsumerRecords<K,V>poll(final Durationtimeout)
end note
:return poll(time.timer(timeout),true);;
stop
end
start
note right
ConsumerRecords<K,V>poll(final Timertimer,
final booleanincludeMetadataInTimeout)
end note
:acquireAndEnsureOpen();;
:try ;
:this.kafkaConsumerMetrics.recordPollStart(timer.currentTimeMs());;
if (this.subscriptions.hasNoSubscriptionOrUserAssignment()) then (yes) 
:throw new IllegalStateException("Consumer is not subscribed to any topics or assigned any partitions");;
stop
endif 
repeat
:client.maybeTriggerWakeup();;
if (includeMetadataInTimeout) then (yes) 
:updateAssignmentMetadataIfNeeded(timer,false);;
else (no)
while (!updateAssignmentMetadataIfNeeded(time.timer(Long.MAX_VALUE),
true))
:log.warn("Still waiting for metadata");;
endwhile
endif 
:final Fetch<K,V>fetch=pollForFetches(timer);;
if (!fetch.isEmpty()) then (yes) 
if (fetcher.sendFetches()>0||client.hasPendingRequests()) then (yes) 
:client.transmitSends();;
endif 
if (fetch.records().isEmpty()) then (yes) 
:log.trace("Returning empty records from  poll()  "+
"since the consumer's position has advanced for at least one topic partition");;
endif 
:return this.interceptors.onConsume(new ConsumerRecords<>(fetch.records()));;
stop
endif 
repeat while (timer.notExpired())
:return ConsumerRecords.empty();;
stop
end
start
note right
booleanupdateAssignmentMetadataIfNeeded(final Timertimer,
final booleanwaitForJoinGroup)
end note
if (coordinator!=null &&!coordinator.poll(timer,
waitForJoinGroup)) then (yes) 
:return false;;
stop
endif 
:return updateFetchPositions(timer);;
stop
end
start
note right
Fetch<K,V>pollForFetches(Timertimer)
end note
:longpollTimeout=coordinator==null ? timer.remainingMs():Math.min(coordinator.timeToNextPoll(timer.currentTimeMs()),
timer.remainingMs());;
:final Fetch<K,V>fetch=fetcher.collectFetch();;
if (!fetch.isEmpty()) then (yes) 
:return fetch;;
stop
endif 
:fetcher.sendFetches();;
if (!cachedSubscriptionHasAllFetchPositions&&pollTimeout>retryBackoffMs) then (yes) 
:pollTimeout=retryBackoffMs;;
endif 
:log.trace("Polling for fetches with timeout {}",
pollTimeout);;
:TimerpollTimer=time.timer(pollTimeout);;
:client.poll(pollTimer,()->{return !fetcher.hasAvailableFetches();});;
:timer.update(pollTimer.currentTimeMs());;
:return fetcher.collectFetch();;
stop
end
start
note right
void commitSync()
end note
:commitSync(Duration.ofMillis(defaultApiTimeoutMs));;
end
start
note right
void commitSync(Durationtimeout)
end note
:commitSync(subscriptions.allConsumed(),timeout);;
end
start
note right
void commitSync(final Map<TopicPartition,
OffsetAndMetadata>offsets)
end note
:commitSync(offsets,Duration.ofMillis(defaultApiTimeoutMs));;
end
start
note right
void commitSync(final Map<TopicPartition,
OffsetAndMetadata>offsets,final Durationtimeout)
end note
:acquireAndEnsureOpen();;
:longcommitStart=time.nanoseconds();;
:try ;
:maybeThrowInvalidGroupIdException();;
:offsets.forEach(this::updateLastSeenEpochIfNewer);;
if (!coordinator.commitOffsetsSync(new HashMap<>(offsets),
time.timer(timeout))) then (yes) 
:throw new TimeoutException("Timeout of "+
timeout.toMillis()+"ms expired before successfully "+
"committing offsets "+offsets);;
stop
endif 
end
start
note right
void commitAsync()
end note
:commitAsync(null );;
end
start
note right
void commitAsync(OffsetCommitCallbackcallback)
end note
:commitAsync(subscriptions.allConsumed(),
callback);;
end
start
note right
void commitAsync(final Map<TopicPartition,
OffsetAndMetadata>offsets,OffsetCommitCallbackcallback)
end note
:acquireAndEnsureOpen();;
:try ;
:maybeThrowInvalidGroupIdException();;
:log.debug("Committing offsets: {}",offsets);;
:offsets.forEach(this::updateLastSeenEpochIfNewer);;
:coordinator.commitOffsetsAsync(new HashMap<>(offsets),
callback);;
end
start
note right
void seek(TopicPartitionpartition,longoffset)
end note
if (offset<0) then (yes) 
:throw new IllegalArgumentException("seek offset must not be a negative number");;
stop
endif 
:acquireAndEnsureOpen();;
:try ;
:log.info("Seeking to offset {} for partition {}",
offset,partition);;
:SubscriptionState.FetchPositionnewPosition=new SubscriptionState.FetchPosition(offset,
Optional.empty(),this.metadata.currentLeader(partition));;
:this.subscriptions.seekUnvalidated(partition,
newPosition);;
end
start
note right
void seek(TopicPartitionpartition,OffsetAndMetadataoffsetAndMetadata)
end note
:longoffset=offsetAndMetadata.offset();;
if (offset<0) then (yes) 
:throw new IllegalArgumentException("seek offset must not be a negative number");;
stop
endif 
:acquireAndEnsureOpen();;
:try ;
if (offsetAndMetadata.leaderEpoch().isPresent()) then (yes) 
:log.info("Seeking to offset {} for partition {} with epoch {}",
offset,partition,offsetAndMetadata.leaderEpoch().get());;
else (no)
:log.info("Seeking to offset {} for partition {}",
offset,partition);;
endif 
:Metadata.LeaderAndEpochcurrentLeaderAndEpoch=this.metadata.currentLeader(partition);;
:SubscriptionState.FetchPositionnewPosition=new SubscriptionState.FetchPosition(offsetAndMetadata.offset(),
offsetAndMetadata.leaderEpoch(),currentLeaderAndEpoch);;
:this.updateLastSeenEpochIfNewer(partition,
offsetAndMetadata);;
:this.subscriptions.seekUnvalidated(partition,
newPosition);;
end
start
note right
void seekToBeginning(Collection<TopicPartition>partitions)
end note
if (partitions==null ) then (yes) 
:throw new IllegalArgumentException("Partitions collection cannot be null");;
stop
endif 
:acquireAndEnsureOpen();;
:try ;
:Collection<TopicPartition>parts=partitions.size()==0? this.subscriptions.assignedPartitions():partitions;;
:subscriptions.requestOffsetReset(parts,OffsetResetStrategy.EARLIEST);;
end
start
note right
void seekToEnd(Collection<TopicPartition>partitions)
end note
if (partitions==null ) then (yes) 
:throw new IllegalArgumentException("Partitions collection cannot be null");;
stop
endif 
:acquireAndEnsureOpen();;
:try ;
:Collection<TopicPartition>parts=partitions.size()==0? this.subscriptions.assignedPartitions():partitions;;
:subscriptions.requestOffsetReset(parts,OffsetResetStrategy.LATEST);;
end
start
note right
longposition(TopicPartitionpartition)
end note
:return position(partition,Duration.ofMillis(defaultApiTimeoutMs));;
stop
end
start
note right
longposition(TopicPartitionpartition,final Durationtimeout)
end note
:acquireAndEnsureOpen();;
:try ;
if (!this.subscriptions.isAssigned(partition)) then (yes) 
:throw new IllegalStateException("You can only check the position for partitions assigned to this consumer.");;
stop
endif 
:Timertimer=time.timer(timeout);;
repeat
:SubscriptionState.FetchPositionposition=this.subscriptions.validPosition(partition);;
if (position!=null ) then (yes) 
:return position.offset;;
stop
endif 
:updateFetchPositions(timer);;
:client.poll(timer);;
repeat while (timer.notExpired())
:throw new TimeoutException("Timeout of "+
timeout.toMillis()+"ms expired before the position "+
"for partition "+partition+" could be determined");;
stop
end
start
note right
OffsetAndMetadatacommitted(TopicPartitionpartition)
end note
:return committed(partition,Duration.ofMillis(defaultApiTimeoutMs));;
stop
end
start
note right
OffsetAndMetadatacommitted(TopicPartitionpartition,
final Durationtimeout)
end note
:return committed(Collections.singleton(partition),
timeout).get(partition);;
stop
end
start
note right
Map<TopicPartition,OffsetAndMetadata>committed(final Set<TopicPartition>partitions)
end note
:return committed(partitions,Duration.ofMillis(defaultApiTimeoutMs));;
stop
end
start
note right
Map<TopicPartition,OffsetAndMetadata>committed(final Set<TopicPartition>partitions,
final Durationtimeout)
end note
:acquireAndEnsureOpen();;
:longstart=time.nanoseconds();;
:try ;
:maybeThrowInvalidGroupIdException();;
:final Map<TopicPartition,OffsetAndMetadata>offsets;;
:offsets=coordinator.fetchCommittedOffsets(partitions,
time.timer(timeout));;
if (offsets==null ) then (yes) 
:throw new TimeoutException("Timeout of "+
timeout.toMillis()+"ms expired before the last "+
"committed offset for partitions "+partitions+
" could be determined. Try tuning default.api.timeout.ms "+
"larger to relax the threshold.");;
stop
else (no)
:offsets.forEach(this::updateLastSeenEpochIfNewer);;
:return offsets;;
stop
endif 
end
start
note right
Map<MetricName,?  extends Metric>metrics()
end note
:return Collections.unmodifiableMap(this.metrics.metrics());;
stop
end
start
note right
List<PartitionInfo>partitionsFor(Stringtopic)
end note
:return partitionsFor(topic,Duration.ofMillis(defaultApiTimeoutMs));;
stop
end
start
note right
List<PartitionInfo>partitionsFor(Stringtopic,
Durationtimeout)
end note
:acquireAndEnsureOpen();;
:try ;
:Clustercluster=this.metadata.fetch();;
:List<PartitionInfo>parts=cluster.partitionsForTopic(topic);;
if (!parts.isEmpty()) then (yes) 
:return parts;;
stop
endif 
:Timertimer=time.timer(timeout);;
:Map<String,List<PartitionInfo>>topicMetadata=fetcher.getTopicMetadata(new MetadataRequest.Builder(Collections.singletonList(topic),
metadata.allowAutoTopicCreation()),timer);;
:return topicMetadata.getOrDefault(topic,
Collections.emptyList());;
stop
end
start
note right
Map<String,List<PartitionInfo>>listTopics()
end note
:return listTopics(Duration.ofMillis(defaultApiTimeoutMs));;
stop
end
start
note right
Map<String,List<PartitionInfo>>listTopics(Durationtimeout)
end note
:acquireAndEnsureOpen();;
:try ;
:return fetcher.getAllTopicMetadata(time.timer(timeout));;
stop
end
start
note right
void pause(Collection<TopicPartition>partitions)
end note
:acquireAndEnsureOpen();;
:try ;
:log.debug("Pausing partitions {}",partitions);;
:subscriptions.pause(partition);;
end
start
note right
void resume(Collection<TopicPartition>partitions)
end note
:acquireAndEnsureOpen();;
:try ;
:log.debug("Resuming partitions {}",partitions);;
:subscriptions.resume(partition);;
end
start
note right
Set<TopicPartition>paused()
end note
:acquireAndEnsureOpen();;
:try ;
:return Collections.unmodifiableSet(subscriptions.pausedPartitions());;
stop
end
start
note right
Map<TopicPartition,OffsetAndTimestamp>offsetsForTimes(Map<TopicPartition,
Long>timestampsToSearch)
end note
:return offsetsForTimes(timestampsToSearch,
Duration.ofMillis(defaultApiTimeoutMs));;
stop
end
start
note right
Map<TopicPartition,OffsetAndTimestamp>offsetsForTimes(Map<TopicPartition,
Long>timestampsToSearch,Durationtimeout)
end note
:acquireAndEnsureOpen();;
:try ;
if (entry.getValue()<0) then (yes) 
:throw new IllegalArgumentException("The target time for partition "+
entry.getKey()+" is "+entry.getValue()+". The target time cannot be negative.");;
stop
endif 
:return fetcher.offsetsForTimes(timestampsToSearch,
time.timer(timeout));;
stop
end
start
note right
Map<TopicPartition,Long>beginningOffsets(Collection<TopicPartition>partitions)
end note
:return beginningOffsets(partitions,Duration.ofMillis(defaultApiTimeoutMs));;
stop
end
start
note right
Map<TopicPartition,Long>beginningOffsets(Collection<TopicPartition>partitions,
Durationtimeout)
end note
:acquireAndEnsureOpen();;
:try ;
:return fetcher.beginningOffsets(partitions,
time.timer(timeout));;
stop
end
start
note right
Map<TopicPartition,Long>endOffsets(Collection<TopicPartition>partitions)
end note
:return endOffsets(partitions,Duration.ofMillis(defaultApiTimeoutMs));;
stop
end
start
note right
Map<TopicPartition,Long>endOffsets(Collection<TopicPartition>partitions,
Durationtimeout)
end note
:acquireAndEnsureOpen();;
:try ;
:return fetcher.endOffsets(partitions,time.timer(timeout));;
stop
end
start
note right
OptionalLongcurrentLag(TopicPartitiontopicPartition)
end note
:acquireAndEnsureOpen();;
:try ;
:final Longlag=subscriptions.partitionLag(topicPartition,
isolationLevel);;
if (lag==null ) then (yes) 
if (subscriptions.partitionEndOffset(topicPartition,
isolationLevel)==null &&!subscriptions.partitionEndOffsetRequested(topicPartition)) then (yes) 
:log.info("Requesting the log end offset for {} in order to compute lag",
topicPartition);;
:subscriptions.requestPartitionEndOffset(topicPartition);;
:fetcher.endOffsets(Collections.singleton(topicPartition),
time.timer(0L));;
endif 
:return OptionalLong.empty();;
stop
endif 
:return OptionalLong.of(lag);;
stop
end
start
note right
ConsumerGroupMetadatagroupMetadata()
end note
:acquireAndEnsureOpen();;
:try ;
:maybeThrowInvalidGroupIdException();;
:return coordinator.groupMetadata();;
stop
end
start
note right
void enforceRebalance(final Stringreason)
end note
:acquireAndEnsureOpen();;
:try ;
if (coordinator==null ) then (yes) 
:throw new IllegalStateException("Tried to force a rebalance but consumer does not have a group.");;
stop
endif 
:coordinator.requestRejoin(reason==null ||reason.isEmpty()? DEFAULT_REASON:reason);;
end
start
note right
void enforceRebalance()
end note
:enforceRebalance(null );;
end
start
note right
void close()
end note
:close(Duration.ofMillis(DEFAULT_CLOSE_TIMEOUT_MS));;
end
start
note right
void close(Durationtimeout)
end note
if (timeout.toMillis()<0) then (yes) 
:throw new IllegalArgumentException("The timeout cannot be negative.");;
stop
endif 
:acquire();;
:try ;
if (!closed) then (yes) 
:close(timeout.toMillis(),false);;
endif 
end
start
note right
void wakeup()
end note
:this.client.wakeup();;
end
start
note right
ClusterResourceListenersconfigureClusterResourceListeners(Deserializer<K>keyDeserializer,
Deserializer<V>valueDeserializer,List<? >... candidateLists)
end note
:ClusterResourceListenersclusterResourceListeners=new ClusterResourceListeners();;
:clusterResourceListeners.maybeAddAll(candidateList);;
:clusterResourceListeners.maybeAdd(keyDeserializer);;
:clusterResourceListeners.maybeAdd(valueDeserializer);;
:return clusterResourceListeners;;
stop
end
start
note right
void close(longtimeoutMs,booleanswallowException)
end note
:log.trace("Closing the Kafka consumer");;
:AtomicReference<Throwable>firstException=new AtomicReference<>();;
:try ;
if (coordinator!=null ) then (yes) 
:coordinator.close(time.timer(Math.min(timeoutMs,
requestTimeoutMs)));;
endif 
:catch (Throwablet);
:firstException.compareAndSet(null ,t);;
:log.error("Failed to close coordinator",t);;
:Utils.closeQuietly(fetcher,"fetcher",firstException);;
:Utils.closeQuietly(interceptors,"consumer interceptors",
firstException);;
:Utils.closeQuietly(kafkaConsumerMetrics,
"kafka consumer metrics",firstException);;
:Utils.closeQuietly(metrics,"consumer metrics",
firstException);;
:Utils.closeQuietly(client,"consumer network client",
firstException);;
:Utils.closeQuietly(keyDeserializer,"consumer key deserializer",
firstException);;
:Utils.closeQuietly(valueDeserializer,"consumer value deserializer",
firstException);;
:AppInfoParser.unregisterAppInfo(JMX_PREFIX,
clientId,metrics);;
:log.debug("Kafka consumer has been closed");;
:Throwableexception=firstException.get();;
if (exception!=null &&!swallowException) then (yes) 
if (exception instanceof InterruptException) then (yes) 
:throw (InterruptException)exception;;
stop
endif 
:throw new KafkaException("Failed to close kafka consumer",
exception);;
stop
endif 
end
start
note right
booleanupdateFetchPositions(final Timertimer)
end note
:fetcher.validateOffsetsIfNeeded();;
:cachedSubscriptionHasAllFetchPositions=subscriptions.hasAllFetchPositions();;
if (cachedSubscriptionHasAllFetchPositions) then (yes) 
:return true;;
stop
endif 
if (coordinator!=null &&!coordinator.refreshCommittedOffsetsIfNeeded(timer)) then (yes) 
:return false;;
stop
endif 
:subscriptions.resetInitializingPositions();;
:fetcher.resetOffsetsIfNeeded();;
:return true;;
stop
end
start
note right
void acquireAndEnsureOpen()
end note
:acquire();;
if (this.closed) then (yes) 
:release();;
:throw new IllegalStateException("This consumer has already been closed.");;
stop
endif 
end
start
note right
void acquire()
end note
:longthreadId=Thread.currentThread().getId();;
if (threadId!=currentThread.get()&&!currentThread.compareAndSet(NO_CURRENT_THREAD,
threadId)) then (yes) 
:throw new ConcurrentModificationException("KafkaConsumer is not safe for multi-threaded access");;
stop
endif 
:refcount.incrementAndGet();;
end
start
note right
void release()
end note
if (refcount.decrementAndGet()==0) then (yes) 
:currentThread.set(NO_CURRENT_THREAD);;
endif 
end
start
note right
void throwIfNoAssignorsConfigured()
end note
if (assignors.isEmpty()) then (yes) 
:throw new IllegalStateException("Must configure at least one partition assigner class name to "+
ConsumerConfig.PARTITION_ASSIGNMENT_STRATEGY_CONFIG+
" configuration property");;
stop
endif 
end
start
note right
void maybeThrowInvalidGroupIdException()
end note
if (!groupId.isPresent()) then (yes) 
:throw new InvalidGroupIdException("To use the group management or offset commit APIs, you must "+
"provide a valid "+ConsumerConfig.GROUP_ID_CONFIG+
" in the consumer configuration.");;
stop
endif 
end
start
note right
void updateLastSeenEpochIfNewer(TopicPartitiontopicPartition,
OffsetAndMetadataoffsetAndMetadata)
end note
if (offsetAndMetadata!=null ) then (yes) 
:offsetAndMetadata.leaderEpoch().ifPresent(epoch->metadata.updateLastSeenEpochIfNewer(topicPartition,
epoch));;
endif 
end
start
note right
StringgetClientId()
end note
:return clientId;;
stop
end
start
note right
booleanupdateAssignmentMetadataIfNeeded(final Timertimer)
end note
:return updateAssignmentMetadataIfNeeded(timer,
true);;
stop
end
@enduml

PlantUML version 1.2022.7(Mon Aug 22 17:01:30 UTC 2022)
(GPL source distribution)
Java Runtime: OpenJDK Runtime Environment
JVM: OpenJDK 64-Bit Server VM
Default Encoding: UTF-8
Language: en
Country: null
--></g></svg>