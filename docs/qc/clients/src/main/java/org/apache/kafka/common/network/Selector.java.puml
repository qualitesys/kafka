@startuml
!pragma layout smetana
Selectable <|-- org.apache.kafka.common.network.Selector
class org.apache.kafka.common.network.Selector {
+  Selector(int;long;int;Metrics;Time;String;Map<String,String>;boolean;boolean;ChannelBuilder;MemoryPool;LogContext)
+  Selector(int;long;Metrics;Time;String;Map<String,String>;boolean;boolean;ChannelBuilder;MemoryPool;LogContext)
+  Selector(int;long;int;Metrics;Time;String;Map<String,String>;boolean;ChannelBuilder;LogContext)
+  Selector(int;long;Metrics;Time;String;Map<String,String>;boolean;ChannelBuilder;LogContext)
+  Selector(long;Metrics;Time;String;ChannelBuilder;LogContext)
+  Selector(long;int;Metrics;Time;String;ChannelBuilder;LogContext)
+  connect(String;InetSocketAddress;int;int)
+  doConnect(SocketChannel;InetSocketAddress)
+  configureSocketChannel(SocketChannel;int;int)
+  register(String;SocketChannel)
+  ensureNotRegistered(String)
+  registerChannel(String;SocketChannel;int)
+  buildAndAttachKafkaChannel(SocketChannel;String;SelectionKey)
+  wakeup()
+  close()
+  send(NetworkSend)
+  poll(long)
+  pollSelectionKeys(Set<SelectionKey>;boolean;long)
+  attemptWrite(SelectionKey;KafkaChannel;long)
+  write(KafkaChannel)
+  determineHandlingOrder(Set<SelectionKey>)
+  attemptRead(KafkaChannel)
+  maybeReadFromClosingChannel(KafkaChannel)
+  maybeRecordTimePerConnection(KafkaChannel;long)
+  completedSends()
+  completedReceives()
+  disconnected()
+  connected()
+  mute(String)
+  mute(KafkaChannel)
+  unmute(String)
+  unmute(KafkaChannel)
+  muteAll()
+  unmuteAll()
+  completeDelayedChannelClose(long)
+  maybeCloseOldestConnection(long)
+  clearCompletedReceives()
+  clearCompletedSends()
+  clear()
+  select(long)
+  close(String)
+  maybeDelayCloseOnAuthenticationFailure(KafkaChannel)
+  handleCloseOnAuthenticationFailure(KafkaChannel)
+  close(KafkaChannel;CloseMode)
+  doClose(KafkaChannel;boolean)
+  isChannelReady(String)
+  openOrClosingChannelOrFail(String)
+  channels()
+  channel(String)
+  closingChannel(String)
+  lowestPriorityChannel()
+  channel(SelectionKey)
+  hasCompletedReceive(KafkaChannel)
+  addToCompletedReceives(KafkaChannel;NetworkReceive;long)
+  keys()
+  isOutOfMemory()
+  isMadeReadProgressLastPoll()
+  delayedClosingChannels()
} 
@enduml
