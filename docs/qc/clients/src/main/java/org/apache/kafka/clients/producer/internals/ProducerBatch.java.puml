@startuml
!pragma layout smetana
java.lang.Object <|-- org.apache.kafka.clients.producer.internals.ProducerBatch
class org.apache.kafka.clients.producer.internals.ProducerBatch {
+  ProducerBatch(TopicPartition;MemoryRecordsBuilder;long)
+  ProducerBatch(TopicPartition;MemoryRecordsBuilder;long;boolean)
+  tryAppend(long;byte[];byte[];Header[];Callback;long)
+  tryAppendForSplit(long;ByteBuffer;ByteBuffer;Header[];Thunk)
+  abort(RuntimeException)
+  isDone()
+  complete(long;long)
+  completeExceptionally(RuntimeException;Function<Integer,RuntimeException>)
+  done(long;long;RuntimeException;Function<Integer,RuntimeException>)
+  completeFutureAndFireCallbacks(long;long;Function<Integer,RuntimeException>)
+  split(int)
+  createBatchOffAccumulatorForRecord(Record;int)
+  isCompressed()
+  toString()
+  hasReachedDeliveryTimeout(long;long)
+  finalState()
+  attempts()
+  reenqueued(long)
+  queueTimeMs()
+  waitedTimeMs(long)
+  drained(long)
+  isSplitBatch()
+  inRetry()
+  records()
+  estimatedSizeInBytes()
+  compressionRatio()
+  isFull()
+  setProducerState(ProducerIdAndEpoch;int;boolean)
+  resetProducerState(ProducerIdAndEpoch;int;boolean)
+  closeForRecordAppends()
+  close()
+  abortRecordAppends()
+  isClosed()
+  buffer()
+  initialCapacity()
+  isWritable()
+  magic()
+  producerId()
+  producerEpoch()
+  baseSequence()
+  lastSequence()
+  hasSequence()
+  isTransactional()
+  sequenceHasBeenReset()
} 
@enduml
