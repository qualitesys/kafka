@startuml
!pragma layout smetana
AbstractRecordsCloseable <|-- org.apache.kafka.common.record.FileRecords
class org.apache.kafka.common.record.FileRecords {
+  FileRecords(File;FileChannel;int;int;boolean)
+  sizeInBytes()
+  file()
+  channel()
+  readInto(ByteBuffer;int)
+  slice(int;int)
+  sliceUnaligned(int;int)
+  availableBytes(int;int)
+  append(MemoryRecords)
+  flush()
+  close()
+  closeHandlers()
+  deleteIfExists()
+  trim()
+  updateParentDir(File)
+  renameTo(File)
+  truncateTo(int)
+  downConvert(byte;long;Time)
+  writeTo(TransferableChannel;long;int)
+  searchForOffsetWithSize(long;int)
+  searchForTimestamp(long;int;long)
+  largestTimestampAfter(int)
+  maybeLeaderEpoch(int)
+  batches()
+  toString()
+  batchesFrom(int)
+  batchIterator()
+  batchIterator(int)
+  open(File;boolean;boolean;int;boolean)
+  open(File;boolean;int;boolean)
+  open(File;boolean)
+  open(File)
+  openChannel(File;boolean;boolean;int;boolean)
} 
@enduml
