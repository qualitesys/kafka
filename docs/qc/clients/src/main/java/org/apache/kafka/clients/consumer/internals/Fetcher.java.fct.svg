<?xml version="1.0" encoding="UTF-8" standalone="no"?><svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" contentStyleType="text/css" height="1234px" preserveAspectRatio="none" style="width:939px;height:1234px;background:#FFFFFF;" version="1.1" viewBox="0 0 939 1234" width="939px" zoomAndPan="magnify"><defs/><g><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="603" x="5" y="16.1387">An error has occured : java.lang.IndexOutOfBoundsException: Index 0 out of bounds for length 0</text><text fill="#000000" font-family="sans-serif" font-size="12" font-style="italic" lengthAdjust="spacing" textLength="332" x="5" y="30.1074">What do you mean? An African or European swallow?</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="44.0762"> </text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="280" x="5" y="58.0449">Diagram size: 1404 lines / 55393 characters.</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="72.0137"> </text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="374" x="5" y="85.9824">PlantUML (1.2022.7) cannot parse result from dot/GraphViz.</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="99.9512"> </text><text fill="#000000" font-family="sans-serif" font-size="12" font-weight="bold" lengthAdjust="spacing" textLength="398" x="5" y="113.9199">This version of PlantUML is 1184 days old, so you should</text><text fill="#000000" font-family="sans-serif" font-size="12" font-weight="bold" lengthAdjust="spacing" textLength="398" x="5" y="127.8887">consider upgrading from https://plantuml.com/download</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="141.8574"> </text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="492" x="5" y="155.8262">Please go to https://plantuml.com/graphviz-dot to check your GraphViz version.</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="169.7949"> </text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="277" x="5" y="183.7637">Java Runtime: OpenJDK Runtime Environment</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="187" x="5" y="197.7324">JVM: OpenJDK 64-Bit Server VM</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="150" x="5" y="211.7012">Default Encoding: UTF-8</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="86" x="5" y="225.6699">Language: en</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="78" x="5" y="239.6387">Country: null</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="253.6074"> </text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="172" x="5" y="267.5762">PLANTUML_LIMIT_SIZE: 4096</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="281.5449"> </text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="150" x="5" y="295.5137">This may be caused by :</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="120" x="9" y="309.4824">- a bug in PlantUML</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="144" x="9" y="323.4512">- a problem in GraphViz</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="337.4199"> </text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="300" x="5" y="351.3887">You should send this diagram and this image to</text><text fill="#000000" font-family="sans-serif" font-size="12" font-weight="bold" lengthAdjust="spacing" textLength="145" x="309" y="351.3887">plantuml@gmail.com</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="13" x="458" y="351.3887">or</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="45" x="5" y="365.3574">post to</text><text fill="#000000" font-family="sans-serif" font-size="12" font-weight="bold" lengthAdjust="spacing" textLength="168" x="54" y="365.3574">https://plantuml.com/qa</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="117" x="226" y="365.3574">to solve this issue.</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="405" x="5" y="379.3262">You can try to turn arround this issue by simplifing your diagram.</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="393.2949"> </text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="458" x="5" y="407.2637">java.lang.IndexOutOfBoundsException: Index 0 out of bounds for length 0</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="477" x="13" y="421.2324">java.base/jdk.internal.util.Preconditions.outOfBounds(Preconditions.java:64)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="548" x="13" y="435.2012">java.base/jdk.internal.util.Preconditions.outOfBoundsCheckIndex(Preconditions.java:70)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="474" x="13" y="449.1699">java.base/jdk.internal.util.Preconditions.checkIndex(Preconditions.java:266)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="351" x="13" y="463.1387">java.base/java.util.Objects.checkIndex(Objects.java:361)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="314" x="13" y="477.1074">java.base/java.util.ArrayList.get(ArrayList.java:427)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="878" x="13" y="491.0762">net.sourceforge.plantuml.activitydiagram3.ftile.vcompact.FtileFactoryDelegatorSwitch.createWithLinks(FtileFactoryDelegatorSwitch.java:103)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="852" x="13" y="505.0449">net.sourceforge.plantuml.activitydiagram3.ftile.vcompact.FtileFactoryDelegatorSwitch.createSwitch(FtileFactoryDelegatorSwitch.java:79)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="716" x="13" y="519.0137">net.sourceforge.plantuml.activitydiagram3.ftile.FtileFactoryDelegator.createSwitch(FtileFactoryDelegator.java:185)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="716" x="13" y="532.9824">net.sourceforge.plantuml.activitydiagram3.ftile.FtileFactoryDelegator.createSwitch(FtileFactoryDelegator.java:185)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="716" x="13" y="546.9512">net.sourceforge.plantuml.activitydiagram3.ftile.FtileFactoryDelegator.createSwitch(FtileFactoryDelegator.java:185)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="716" x="13" y="560.9199">net.sourceforge.plantuml.activitydiagram3.ftile.FtileFactoryDelegator.createSwitch(FtileFactoryDelegator.java:185)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="716" x="13" y="574.8887">net.sourceforge.plantuml.activitydiagram3.ftile.FtileFactoryDelegator.createSwitch(FtileFactoryDelegator.java:185)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="621" x="13" y="588.8574">net.sourceforge.plantuml.activitydiagram3.InstructionSwitch.createFtile(InstructionSwitch.java:119)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="583" x="13" y="602.8262">net.sourceforge.plantuml.activitydiagram3.InstructionList.createFtile(InstructionList.java:131)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="536" x="13" y="616.7949">net.sourceforge.plantuml.activitydiagram3.ftile.Swimlanes.drawU(Swimlanes.java:229)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="805" x="13" y="630.7637">net.sourceforge.plantuml.ugraphic.comp.CompressionXorYBuilder.getPiecewiseAffineTransform(CompressionXorYBuilder.java:51)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="655" x="13" y="644.7324">net.sourceforge.plantuml.ugraphic.comp.CompressionXorYBuilder.build(CompressionXorYBuilder.java:44)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="686" x="13" y="658.7012">net.sourceforge.plantuml.activitydiagram3.ActivityDiagram3.exportDiagramInternal(ActivityDiagram3.java:210)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="502" x="13" y="672.6699">net.sourceforge.plantuml.UmlDiagram.exportDiagramNow(UmlDiagram.java:135)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="538" x="13" y="686.6387">net.sourceforge.plantuml.AbstractPSystem.exportDiagram(AbstractPSystem.java:178)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="566" x="13" y="700.6074">net.sourceforge.plantuml.SourceStringReader.outputImage(SourceStringReader.java:172)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="566" x="13" y="714.5762">net.sourceforge.plantuml.SourceStringReader.outputImage(SourceStringReader.java:143)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="693" x="13" y="728.5449">com.qualitesys.qcrmavenplugin.commun.QcrCommunPlantUml.genererFichierSvg(QcrCommunPlantUml.java:82)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="585" x="13" y="742.5137">com.qualitesys.qcrmavenplugin.QcrReporting01Mojo.lireFichier(QcrReporting01Mojo.java:927)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="556" x="13" y="756.4824">com.qualitesys.qcrmavenplugin.QcrReporting01Mojo.lister(QcrReporting01Mojo.java:453)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="591" x="13" y="770.4512">com.qualitesys.qcrmavenplugin.QcrReporting01Mojo.doReport(QcrReporting01Mojo.java:2037)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="625" x="13" y="784.4199">com.qualitesys.qcrmavenplugin.QcrReporting01Mojo.executeReport(QcrReporting01Mojo.java:1739)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="582" x="13" y="798.3887">org.apache.maven.reporting.AbstractMavenReport.generate(AbstractMavenReport.java:251)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="748" x="13" y="812.3574">org.apache.maven.plugins.site.render.ReportDocumentRenderer.renderDocument(ReportDocumentRenderer.java:226)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="607" x="13" y="826.3262">org.apache.maven.doxia.siterenderer.DefaultSiteRenderer.render(DefaultSiteRenderer.java:348)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="502" x="13" y="840.2949">org.apache.maven.plugins.site.render.SiteMojo.renderLocale(SiteMojo.java:194)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="470" x="13" y="854.2637">org.apache.maven.plugins.site.render.SiteMojo.execute(SiteMojo.java:143)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="649" x="13" y="868.2324">org.apache.maven.plugin.DefaultBuildPluginManager.executeMojo(DefaultBuildPluginManager.java:126)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="539" x="13" y="882.2012">org.apache.maven.lifecycle.internal.MojoExecutor.doExecute2(MojoExecutor.java:328)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="531" x="13" y="896.1699">org.apache.maven.lifecycle.internal.MojoExecutor.doExecute(MojoExecutor.java:316)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="515" x="13" y="910.1387">org.apache.maven.lifecycle.internal.MojoExecutor.execute(MojoExecutor.java:212)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="515" x="13" y="924.1074">org.apache.maven.lifecycle.internal.MojoExecutor.execute(MojoExecutor.java:174)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="533" x="13" y="938.0762">org.apache.maven.lifecycle.internal.MojoExecutor.access$000(MojoExecutor.java:75)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="502" x="13" y="952.0449">org.apache.maven.lifecycle.internal.MojoExecutor$1.run(MojoExecutor.java:162)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="666" x="13" y="966.0137">org.apache.maven.plugin.DefaultMojosExecutionStrategy.execute(DefaultMojosExecutionStrategy.java:39)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="515" x="13" y="979.9824">org.apache.maven.lifecycle.internal.MojoExecutor.execute(MojoExecutor.java:159)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="651" x="13" y="993.9512">org.apache.maven.lifecycle.internal.LifecycleModuleBuilder.buildProject(LifecycleModuleBuilder.java:105)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="643" x="13" y="1007.9199">org.apache.maven.lifecycle.internal.LifecycleModuleBuilder.buildProject(LifecycleModuleBuilder.java:73)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="747" x="13" y="1021.8887">org.apache.maven.lifecycle.internal.builder.singlethreaded.SingleThreadedBuilder.build(SingleThreadedBuilder.java:53)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="539" x="13" y="1035.8574">org.apache.maven.lifecycle.internal.LifecycleStarter.execute(LifecycleStarter.java:118)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="428" x="13" y="1049.8262">org.apache.maven.DefaultMaven.doExecute(DefaultMaven.java:261)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="428" x="13" y="1063.7949">org.apache.maven.DefaultMaven.doExecute(DefaultMaven.java:173)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="412" x="13" y="1077.7637">org.apache.maven.DefaultMaven.execute(DefaultMaven.java:101)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="367" x="13" y="1091.7324">org.apache.maven.cli.MavenCli.execute(MavenCli.java:906)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="362" x="13" y="1105.7012">org.apache.maven.cli.MavenCli.doMain(MavenCli.java:283)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="347" x="13" y="1119.6699">org.apache.maven.cli.MavenCli.main(MavenCli.java:206)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="502" x="13" y="1133.6387">java.base/jdk.internal.reflect.NativeMethodAccessorImpl.invoke0(Native Method)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="621" x="13" y="1147.6074">java.base/jdk.internal.reflect.NativeMethodAccessorImpl.invoke(NativeMethodAccessorImpl.java:77)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="679" x="13" y="1161.5762">java.base/jdk.internal.reflect.DelegatingMethodAccessorImpl.invoke(DelegatingMethodAccessorImpl.java:43)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="373" x="13" y="1175.5449">java.base/java.lang.reflect.Method.invoke(Method.java:569)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="562" x="13" y="1189.5137">org.codehaus.plexus.classworlds.launcher.Launcher.launchEnhanced(Launcher.java:255)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="499" x="13" y="1203.4824">org.codehaus.plexus.classworlds.launcher.Launcher.launch(Launcher.java:201)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="568" x="13" y="1217.4512">org.codehaus.plexus.classworlds.launcher.Launcher.mainWithExitCode(Launcher.java:361)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="487" x="13" y="1231.4199">org.codehaus.plexus.classworlds.launcher.Launcher.main(Launcher.java:314)</text><image height="22" width="19" x="920" xlink:href="data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAABMAAAAWCAYAAAAinad/AAAAc0lEQVR4Xu2PQQqAMAwE+yX//we/VDUgNtMk3aOgA3tJdxba2s+76fvWx/BdgiMM+ykUs9CboLAKfYeVbigHd/qOx4BIVmP2eHauTFJyTwcrKbuXY+5Lg1DduWOwpIY7BktquGOwpIY7RlRSbyFRQb19hAPzSO+oaVUC+wAAAABJRU5ErkJggg==" y="6"/><!--MD5=[9ceb023e8c477b7fd70792b22501ce8b]
@startuml
!pragma layout smetana
start
note right
constructor Fetcher(LogContextlogContext,
ConsumerNetworkClientclient,intminBytes,intmaxBytes,
intmaxWaitMs,intfetchSize,intmaxPollRecords,
booleancheckCrcs,StringclientRackId,Deserializer<K>keyDeserializer,
Deserializer<V>valueDeserializer,ConsumerMetadatametadata,
SubscriptionStatesubscriptions,Metricsmetrics,
FetcherMetricsRegistrymetricsRegistry,Timetime,
longretryBackoffMs,longrequestTimeoutMs,IsolationLevelisolationLevel,
ApiVersionsapiVersions)
end note
:this.log=logContext.logger(Fetcher.class );;
:this.logContext=logContext;;
:this.time=time;;
:this.client=client;;
:this.metadata=metadata;;
:this.subscriptions=subscriptions;;
:this.minBytes=minBytes;;
:this.maxBytes=maxBytes;;
:this.maxWaitMs=maxWaitMs;;
:this.fetchSize=fetchSize;;
:this.maxPollRecords=maxPollRecords;;
:this.checkCrcs=checkCrcs;;
:this.clientRackId=clientRackId;;
:this.keyDeserializer=keyDeserializer;;
:this.valueDeserializer=valueDeserializer;;
:this.completedFetches=new ConcurrentLinkedQueue<>();;
:this.sensors=new FetchManagerMetrics(metrics,
metricsRegistry);;
:this.retryBackoffMs=retryBackoffMs;;
:this.requestTimeoutMs=requestTimeoutMs;;
:this.isolationLevel=isolationLevel;;
:this.apiVersions=apiVersions;;
:this.sessionHandlers=new HashMap<>();;
:this.offsetsForLeaderEpochClient=new OffsetsForLeaderEpochClient(client,
logContext);;
:this.nodesWithPendingFetchRequests=new HashSet<>();;
end
start
note right
constructor ListOffsetData(longoffset,Longtimestamp,
Optional<Integer>leaderEpoch)
end note
:this.offset=offset;;
:this.timestamp=timestamp;;
:this.leaderEpoch=leaderEpoch;;
end
start
note right
booleanhasCompletedFetches()
end note
:return !completedFetches.isEmpty();;
stop
end
start
note right
booleanhasAvailableFetches()
end note
:return completedFetches.stream().anyMatch(fetch->subscriptions.isFetchable(fetch.partition));;
stop
end
start
note right
intsendFetches()
end note
:sensors.maybeUpdateAssignment(subscriptions);;
:Map<Node,FetchSessionHandler.FetchRequestData>fetchRequestMap=prepareFetchRequests();;
:final NodefetchTarget=entry.getKey();;
:final FetchSessionHandler.FetchRequestDatadata=entry.getValue();;
:final shortmaxVersion;;
if (!data.canUseTopicIds()) then (yes) 
:maxVersion=(short)12;;
else (no)
:maxVersion=ApiKeys.FETCH.latestVersion();;
endif 
:final FetchRequest.Builderrequest=FetchRequest.Builder.forConsumer(maxVersion,
this.maxWaitMs,this.minBytes,data.toSend()).isolationLevel(isolationLevel).setMaxBytes(this.maxBytes).metadata(data.metadata()).removed(data.toForget()).replaced(data.toReplace()).rackId(clientRackId);;
if (log.isDebugEnabled()) then (yes) 
:log.debug("Sending {} {} to broker {}",isolationLevel,
data.toString(),fetchTarget);;
endif 
:RequestFuture<ClientResponse>future=client.send(fetchTarget,
request);;
:this.nodesWithPendingFetchRequests.add(entry.getKey()
.id());;
:future.addListener(new RequestFutureListener<ClientResponse>(){@Override public void onSuccess(ClientResponseresp){synchronized (Fetcher.this){try {FetchResponseresponse=(FetchResponse)resp.responseBody();FetchSessionHandlerhandler=sessionHandler(fetchTarget.id());if (handler==null ){log.error("Unable to find FetchSessionHandler for node {}. Ignoring fetch response.",fetchTarget.id());return ;}if (!handler.handleResponse(response,resp.requestHeader().apiVersion())){if (response.error()==Errors.FETCH_SESSION_TOPIC_ID_ERROR){metadata.requestUpdate();}return ;}Map<TopicPartition,FetchResponseData.PartitionData>responseData=response.responseData(handler.sessionTopicNames(),resp.requestHeader().apiVersion());Set<TopicPartition>partitions=new HashSet<>(responseData.keySet());FetchResponseMetricAggregatormetricAggregator=new FetchResponseMetricAggregator(sensors,partitions);for (Map.Entry<TopicPartition,FetchResponseData.PartitionData>entry:responseData.entrySet()){TopicPartitionpartition=entry.getKey();FetchRequest.PartitionDatarequestData=data.sessionPartitions().get(partition);if (requestData==null ){Stringmessage;if (data.metadata().isFull()){message=MessageFormatter.arrayFormat("Response for missing full request partition: partition={}; metadata={}",new Object[]{partition,data.metadata()}).getMessage();}else {message=MessageFormatter.arrayFormat("Response for missing session request partition: partition={}; metadata={}; toSend={}; toForget={}; toReplace={}",new Object[]{partition,data.metadata(),data.toSend(),data.toForget(),data.toReplace()}).getMessage();}throw new IllegalStateException(message);}else {longfetchOffset=requestData.fetchOffset;FetchResponseData.PartitionDatapartitionData=entry.getValue();log.debug("Fetch {} at offset {} for partition {} returned fetch data {}",isolationLevel,fetchOffset,partition,partitionData);Iterator<?  extends RecordBatch>batches=FetchResponse.recordsOrFail(partitionData).batches().iterator();shortresponseVersion=resp.requestHeader().apiVersion();completedFetches.add(new CompletedFetch(partition,partitionData,metricAggregator,batches,fetchOffset,responseVersion));}}sensors.fetchLatency.record (resp.requestLatencyMs());}finally {nodesWithPendingFetchRequests.remove(fetchTarget.id());}}}@Override public void onFailure(RuntimeExceptione){synchronized (Fetcher.this){try {FetchSessionHandlerhandler=sessionHandler(fetchTarget.id());if (handler!=null ){handler.handleError(e);}}finally {nodesWithPendingFetchRequests.remove(fetchTarget.id());}}}});;
:return fetchRequestMap.size();;
stop
end
start
note right
Map<String,List<PartitionInfo>>getAllTopicMetadata(Timertimer)
end note
:return getTopicMetadata(MetadataRequest.Builder.allTopics(),
timer);;
stop
end
start
note right
Map<String,List<PartitionInfo>>getTopicMetadata(MetadataRequest.Builderrequest,
Timertimer)
end note
if (!request.isAllTopics()&&request.emptyTopicList()) then (yes) 
:return Collections.emptyMap();;
stop
endif 
repeat
:RequestFuture<ClientResponse>future=sendMetadataRequest(request);;
:client.poll(future,timer);;
if (future.failed()&&!future.isRetriable()) then (yes) 
:throw future.exception();;
stop
endif 
if (future.succeeded()) then (yes) 
:MetadataResponseresponse=(MetadataResponse)future.value()
.responseBody();;
:Clustercluster=response.buildCluster();;
:Set<String>unauthorizedTopics=cluster.unauthorizedTopics();;
if (!unauthorizedTopics.isEmpty()) then (yes) 
:throw new TopicAuthorizationException(unauthorizedTopics);;
stop
endif 
:booleanshouldRetry=false;;
:Map<String,Errors>errors=response.errors();;
if (!errors.isEmpty()) then (yes) 
:log.debug("Topic metadata fetch included errors: {}",
errors);;
:Stringtopic=errorEntry.getKey();;
:Errorserror=errorEntry.getValue();;
if (error==Errors.INVALID_TOPIC_EXCEPTION) then (yes) 
:throw new InvalidTopicException("Topic '"+
topic+"' is invalid");;
stop
else if (error==Errors.UNKNOWN_TOPIC_OR_PARTITION) then (yes) 
:continue ;;
else if (error.exception() instanceof RetriableException) then (yes) 
:shouldRetry=true;;
else (no)
:throw new KafkaException("Unexpected error fetching metadata for topic "+
topic,error.exception());;
stop
endif 
endif 
if (!shouldRetry) then (yes) 
:HashMap<String,List<PartitionInfo>>topicsPartitionInfos=new HashMap<>();;
:topicsPartitionInfos.put(topic,cluster.partitionsForTopic(topic));;
:return topicsPartitionInfos;;
stop
endif 
endif 
:timer.sleep(retryBackoffMs);;
repeat while (timer.notExpired())
:throw new TimeoutException("Timeout expired while fetching topic metadata");;
stop
end
start
note right
RequestFuture<ClientResponse>sendMetadataRequest(MetadataRequest.Builderrequest)
end note
:final Nodenode=client.leastLoadedNode();;
if (node==null ) then (yes) 
:return RequestFuture.noBrokersAvailable();;
stop
else (no)
:return client.send(node,request);;
stop
endif 
end
start
note right
LongoffsetResetStrategyTimestamp(final TopicPartitionpartition)
end note
:OffsetResetStrategystrategy=subscriptions.resetStrategy(partition);;
if (strategy==OffsetResetStrategy.EARLIEST) then (yes) 
:return ListOffsetsRequest.EARLIEST_TIMESTAMP;;
stop
else if (strategy==OffsetResetStrategy.LATEST) then (yes) 
:return ListOffsetsRequest.LATEST_TIMESTAMP;;
stop
else (no)
:return null ;;
stop
endif 
end
start
note right
OffsetResetStrategytimestampToOffsetResetStrategy(longtimestamp)
end note
if (timestamp==ListOffsetsRequest.EARLIEST_TIMESTAMP) then (yes) 
:return OffsetResetStrategy.EARLIEST;;
stop
else if (timestamp==ListOffsetsRequest.LATEST_TIMESTAMP) then (yes) 
:return OffsetResetStrategy.LATEST;;
stop
else (no)
:return null ;;
stop
endif 
end
start
note right
void resetOffsetsIfNeeded()
end note
:RuntimeExceptionexception=cachedListOffsetsException.getAndSet(null );;
if (exception!=null ) then (yes) 
:throw exception;;
stop
endif 
:Set<TopicPartition>partitions=subscriptions.partitionsNeedingReset(time.milliseconds());;
if (partitions.isEmpty()) then (yes) 
:return ;;
stop
endif 
:final Map<TopicPartition,Long>offsetResetTimestamps=new HashMap<>();;
:Longtimestamp=offsetResetStrategyTimestamp(partition);;
if (timestamp!=null ) then (yes) 
:offsetResetTimestamps.put(partition,timestamp);;
endif 
:resetOffsetsAsync(offsetResetTimestamps);;
end
start
note right
void validateOffsetsIfNeeded()
end note
:RuntimeExceptionexception=cachedOffsetForLeaderException.getAndSet(null );;
if (exception!=null ) then (yes) 
:throw exception;;
stop
endif 
:validatePositionsOnMetadataChange();;
:Map<TopicPartition,FetchPosition>partitionsToValidate=subscriptions.partitionsNeedingValidation(time.milliseconds())
.stream().filter(tp->subscriptions.position(tp)!=null ).collect(Collectors.toMap(Function.identity(),
subscriptions::position));;
:validateOffsetsAsync(partitionsToValidate);;
end
start
note right
Map<TopicPartition,OffsetAndTimestamp>offsetsForTimes(Map<TopicPartition,
Long>timestampsToSearch,Timertimer)
end note
:metadata.addTransientTopics(topicsForPartitions(timestampsToSearch.keySet()));;
switch (try/catch block)
case (try)
:Map<TopicPartition,ListOffsetData>fetchedOffsets=fetchOffsetsByTimes(timestampsToSearch,
timer,true).fetchedOffsets;;
:HashMap<TopicPartition,OffsetAndTimestamp>offsetsByTimes=new HashMap<>(timestampsToSearch.size());;
:offsetsByTimes.put(entry.getKey(),null );;
:ListOffsetDataoffsetData=entry.getValue();;
:offsetsByTimes.put(entry.getKey(),new OffsetAndTimestamp(offsetData.offset,
offsetData.timestamp,offsetData.leaderEpoch));;
:return offsetsByTimes;;
stop
endswitch
end
start
note right
ListOffsetResultfetchOffsetsByTimes(Map<TopicPartition,
Long>timestampsToSearch,Timertimer,booleanrequireTimestamps)
end note
:ListOffsetResultresult=new ListOffsetResult();;
if (timestampsToSearch.isEmpty()) then (yes) 
:return result;;
stop
endif 
:Map<TopicPartition,Long>remainingToSearch=new HashMap<>(timestampsToSearch);;
repeat
:RequestFuture<ListOffsetResult>future=sendListOffsetsRequests(remainingToSearch,
requireTimestamps);;
:future.addListener(new RequestFutureListener<ListOffsetResult>(){@Override public void onSuccess(ListOffsetResultvalue){synchronized (future){result.fetchedOffsets.putAll(value.fetchedOffsets);
:remainingToSearch.keySet().retainAll(value.partitionsToRetry);for (final Map.Entry<TopicPartition,
ListOffsetData>entry:value.fetchedOffsets.entrySet()){final TopicPartitionpartition=entry.getKey();if (subscriptions.isAssigned(partition)){final longoffset=entry.getValue().offset;if (isolationLevel==IsolationLevel.READ_COMMITTED){log.trace("Updating last stable offset for partition {} to {}",partition,offset);subscriptions.updateLastStableOffset(partition,offset);}else {log.trace("Updating high watermark for partition {} to {}",partition,offset);subscriptions.updateHighWatermark(partition,offset);}}}}}@Override public void onFailure(RuntimeExceptione){if (!(e instanceof RetriableException)){throw future.exception();}}});;
if (timer.timeoutMs()==0L) then (yes) 
:return result;;
stop
endif 
:client.poll(future,timer);;
if (!future.isDone()) then (yes) 
:break ;;
else if (remainingToSearch.isEmpty()) then (yes) 
:return result;;
stop
else (no)
:client.awaitMetadataUpdate(timer);;
endif 
repeat while (timer.notExpired())
:throw new TimeoutException("Failed to get offsets by times in "+
timer.elapsedMs()+"ms");;
stop
end
start
note right
Map<TopicPartition,Long>beginningOffsets(Collection<TopicPartition>partitions,
Timertimer)
end note
:return beginningOrEndOffset(partitions,ListOffsetsRequest.EARLIEST_TIMESTAMP,
timer);;
stop
end
start
note right
Map<TopicPartition,Long>endOffsets(Collection<TopicPartition>partitions,
Timertimer)
end note
:return beginningOrEndOffset(partitions,ListOffsetsRequest.LATEST_TIMESTAMP,
timer);;
stop
end
start
note right
Map<TopicPartition,Long>beginningOrEndOffset(Collection<TopicPartition>partitions,
longtimestamp,Timertimer)
end note
:metadata.addTransientTopics(topicsForPartitions(partitions));;
switch (try/catch block)
case (try)
:Map<TopicPartition,Long>timestampsToSearch=partitions.stream().distinct().collect(Collectors.toMap(Function.identity(),
tp->timestamp));;
:ListOffsetResultresult=fetchOffsetsByTimes(timestampsToSearch,
timer,false);;
:return result.fetchedOffsets.entrySet().stream().collect(Collectors.toMap(Map.Entry::getKey,
entry->entry.getValue().offset));;
stop
endswitch
end
start
note right
Fetch<K,V>collectFetch()
end note
:Fetch<K,V>fetch=Fetch.empty();;
:Queue<CompletedFetch>pausedCompletedFetches=new ArrayDeque<>();;
:intrecordsRemaining=maxPollRecords;;
switch (try/catch block)
case (try)
while (recordsRemaining>0)
if (nextInLineFetch==null ||nextInLineFetch.isConsumed) then (yes) 
:CompletedFetchrecords=completedFetches.peek();;
if (records==null ) then (yes) 
:break ;;
endif 
if (records.notInitialized()) then (yes) 
switch (try/catch block)
case (try)
:nextInLineFetch=initializeCompletedFetch(records);;
case (catch (Exceptione))
:FetchResponseData.PartitionDatapartition=records.partitionData;;
if (fetch.isEmpty()&&FetchResponse.recordsOrFail(partition).sizeInBytes()==0) then (yes) 
:completedFetches.poll();;
endif 
:throw e;;
stop
endswitch
else (no)
:nextInLineFetch=records;;
endif 
:completedFetches.poll();;
else if (subscriptions.isPaused(nextInLineFetch.partition)) then (yes) 
:log.debug("Skipping fetching records for assigned partition {} because it is paused",
nextInLineFetch.partition);;
:pausedCompletedFetches.add(nextInLineFetch);;
:nextInLineFetch=null ;;
else (no)
:Fetch<K,V>nextFetch=fetchRecords(nextInLineFetch,
recordsRemaining);;
:recordsRemaining-=nextFetch.numRecords();;
:fetch.add(nextFetch);;
endif 
endwhile
endswitch
:return fetch;;
stop
end
start
note right
Fetch<K,V>fetchRecords(CompletedFetchcompletedFetch,
intmaxRecords)
end note
if (!subscriptions.isAssigned(completedFetch.partition)) then (yes) 
:log.debug("Not returning fetched records for partition {} since it is no longer assigned",
completedFetch.partition);;
else if (!subscriptions.isFetchable(completedFetch.partition)) then (yes) 
:log.debug("Not returning fetched records for assigned partition {} since it is no longer fetchable",
completedFetch.partition);;
else (no)
:FetchPositionposition=subscriptions.position(completedFetch.partition);;
if (position==null ) then (yes) 
:throw new IllegalStateException("Missing position for fetchable partition "+
completedFetch.partition);;
stop
endif 
if (completedFetch.nextFetchOffset==position.offset) then (yes) 
:List<ConsumerRecord<K,V>>partRecords=completedFetch.fetchRecords(maxRecords);;
:log.trace("Returning {} fetched records at offset {} for assigned partition {}",
partRecords.size(),position,completedFetch.partition);;
:booleanpositionAdvanced=false;;
if (completedFetch.nextFetchOffset>position.offset) then (yes) 
:FetchPositionnextPosition=new FetchPosition(completedFetch.nextFetchOffset,
completedFetch.lastEpoch,position.currentLeader);;
:log.trace("Updating fetch position from {} to {} for partition {} and returning {} records from  poll() ",
position,nextPosition,completedFetch.partition,
partRecords.size());;
:subscriptions.position(completedFetch.partition,
nextPosition);;
:positionAdvanced=true;;
endif 
:LongpartitionLag=subscriptions.partitionLag(completedFetch.partition,
isolationLevel);;
if (partitionLag!=null ) then (yes) 
:this.sensors.recordPartitionLag(completedFetch.partition,
partitionLag);;
endif 
:Longlead=subscriptions.partitionLead(completedFetch.partition);;
if (lead!=null ) then (yes) 
:this.sensors.recordPartitionLead(completedFetch.partition,
lead);;
endif 
:return Fetch.forPartition(completedFetch.partition,
partRecords,positionAdvanced);;
stop
else (no)
:log.debug("Ignoring fetched records for {} at offset {} since the current position is {}",
completedFetch.partition,completedFetch.nextFetchOffset,
position);;
endif 
endif 
:log.trace("Draining fetched records for partition {}",
completedFetch.partition);;
:completedFetch.drain();;
:return Fetch.empty();;
stop
end
start
note right
void resetOffsetIfNeeded(TopicPartitionpartition,
OffsetResetStrategyrequestedResetStrategy,
ListOffsetDataoffsetData)
end note
:FetchPositionposition=new FetchPosition(offsetData.offset,
Optional.empty(),metadata.currentLeader(partition));;
:offsetData.leaderEpoch.ifPresent(epoch->metadata.updateLastSeenEpochIfNewer(partition,
epoch));;
:subscriptions.maybeSeekUnvalidated(partition,
position,requestedResetStrategy);;
end
start
note right
void resetOffsetsAsync(Map<TopicPartition,
Long>partitionResetTimestamps)
end note
:Map<Node,Map<TopicPartition,ListOffsetsPartition>>timestampsToSearchByNode=groupListOffsetRequests(partitionResetTimestamps,
new HashSet<>());;
:Nodenode=entry.getKey();;
:final Map<TopicPartition,ListOffsetsPartition>resetTimestamps=entry.getValue();;
:subscriptions.setNextAllowedRetry(resetTimestamps.keySet(),time.milliseconds()+
requestTimeoutMs);;
:RequestFuture<ListOffsetResult>future=sendListOffsetRequest(node,
resetTimestamps,false);;
:future.addListener(new RequestFutureListener<ListOffsetResult>(){@Override public void onSuccess(ListOffsetResultresult){if (!result.partitionsToRetry.isEmpty()){subscriptions.requestFailed(result.partitionsToRetry,time.milliseconds()+retryBackoffMs);metadata.requestUpdate();}for (Map.Entry<TopicPartition,ListOffsetData>fetchedOffset:result.fetchedOffsets.entrySet()){TopicPartitionpartition=fetchedOffset.getKey();ListOffsetDataoffsetData=fetchedOffset.getValue();ListOffsetsPartitionrequestedReset=resetTimestamps.get(partition);resetOffsetIfNeeded(partition,timestampToOffsetResetStrategy(requestedReset.timestamp()),offsetData);}}@Override public void onFailure(RuntimeExceptione){subscriptions.requestFailed(resetTimestamps.keySet(),time.milliseconds()+retryBackoffMs);metadata.requestUpdate();if (!(e instanceof RetriableException)&&!cachedListOffsetsException.compareAndSet(null ,e))log.error("Discarding error in ListOffsetResponse because another error is pending",e);}});;
end
start
note right
booleanhasUsableOffsetForLeaderEpochVersion(NodeApiVersionsnodeApiVersions)
end note
:ApiVersionapiVersion=nodeApiVersions.apiVersion(ApiKeys.OFFSET_FOR_LEADER_EPOCH);;
if (apiVersion==null ) then (yes) 
:return false;;
stop
endif 
:return OffsetsForLeaderEpochRequest.supportsTopicPermission(apiVersion.maxVersion());;
stop
end
start
note right
void validateOffsetsAsync(Map<TopicPartition,
FetchPosition>partitionsToValidate)
end note
:final Map<Node,Map<TopicPartition,FetchPosition>>regrouped=regroupFetchPositionsByLeader(partitionsToValidate);;
:longnextResetTimeMs=time.milliseconds()+
requestTimeoutMs;;
:regrouped.forEach((node,fetchPositions)->{if (node.isEmpty()){metadata.requestUpdate();
:return ;}NodeApiVersionsnodeApiVersions=apiVersions.get(node.idString());
:if (nodeApiVersions==null ){client.tryConnect(node);
:return ;}if (!hasUsableOffsetForLeaderEpochVersion(nodeApiVersions)){log.debug("Skipping validation of fetch offsets for partitions {} since the broker does not "+
"support the required protocol version (introduced in Kafka 2.3)",
fetchPositions.keySet());for (TopicPartitionpartition:fetchPositions.keySet()){subscriptions.completeValidation(partition);}return ;}subscriptions.setNextAllowedRetry(fetchPositions.keySet(),
nextResetTimeMs);RequestFuture<OffsetForEpochResult>future=offsetsForLeaderEpochClient.sendAsyncRequest(node,
fetchPositions);future.addListener(new RequestFutureListener<OffsetForEpochResult>(){@Override public void onSuccess(OffsetForEpochResultoffsetsResult){List<SubscriptionState.LogTruncation>truncations=new ArrayList<>();if (!offsetsResult.partitionsToRetry().isEmpty()){subscriptions.setNextAllowedRetry(offsetsResult.partitionsToRetry(),time.milliseconds()+retryBackoffMs);metadata.requestUpdate();}offsetsResult.endOffsets().forEach((topicPartition,respEndOffset)->{FetchPositionrequestPosition=fetchPositions.get(topicPartition);Optional<SubscriptionState.LogTruncation>truncationOpt=subscriptions.maybeCompleteValidation(topicPartition,requestPosition,respEndOffset);truncationOpt.ifPresent(truncations::add);});if (!truncations.isEmpty()){maybeSetOffsetForLeaderException(buildLogTruncationException(truncations));}}@Override public void onFailure(RuntimeExceptione){subscriptions.requestFailed(fetchPositions.keySet(),time.milliseconds()+retryBackoffMs);metadata.requestUpdate();if (!(e instanceof RetriableException)){maybeSetOffsetForLeaderException(e);}}});});;
end
start
note right
LogTruncationExceptionbuildLogTruncationException(List<SubscriptionState.LogTruncation>truncations)
end note
:Map<TopicPartition,OffsetAndMetadata>divergentOffsets=new HashMap<>();;
:Map<TopicPartition,Long>truncatedFetchOffsets=new HashMap<>();;
:truncation.divergentOffsetOpt.ifPresent(divergentOffset->divergentOffsets.put(truncation.topicPartition,
divergentOffset));;
:truncatedFetchOffsets.put(truncation.topicPartition,
truncation.fetchPosition.offset);;
:return new LogTruncationException("Detected truncated partitions: "+
truncations,truncatedFetchOffsets,divergentOffsets);;
stop
end
start
note right
void maybeSetOffsetForLeaderException(RuntimeExceptione)
end note
if (!cachedOffsetForLeaderException.compareAndSet(null ,e)) then (yes) 
:log.error("Discarding error in OffsetsForLeaderEpoch because another error is pending",e);;
endif 
end
start
note right
RequestFuture<ListOffsetResult>sendListOffsetsRequests(final Map<TopicPartition,
Long>timestampsToSearch,final booleanrequireTimestamps)
end note
:final Set<TopicPartition>partitionsToRetry=new HashSet<>();;
:Map<Node,Map<TopicPartition,ListOffsetsPartition>>timestampsToSearchByNode=groupListOffsetRequests(timestampsToSearch,
partitionsToRetry);;
if (timestampsToSearchByNode.isEmpty()) then (yes) 
:return RequestFuture.failure(new StaleMetadataException());;
stop
endif 
:final RequestFuture<ListOffsetResult>listOffsetRequestsFuture=new RequestFuture<>();;
:final Map<TopicPartition,ListOffsetData>fetchedTimestampOffsets=new HashMap<>();;
:final AtomicIntegerremainingResponses=new AtomicInteger(timestampsToSearchByNode.size());;
:RequestFuture<ListOffsetResult>future=sendListOffsetRequest(entry.getKey(),
entry.getValue(),requireTimestamps);;
:future.addListener(new RequestFutureListener<ListOffsetResult>(){@Override public void onSuccess(ListOffsetResultpartialResult){synchronized (listOffsetRequestsFuture){fetchedTimestampOffsets.putAll(partialResult.fetchedOffsets);partitionsToRetry.addAll(partialResult.partitionsToRetry);if (remainingResponses.decrementAndGet()==0&&!listOffsetRequestsFuture.isDone()){ListOffsetResultresult=new ListOffsetResult(fetchedTimestampOffsets,partitionsToRetry);listOffsetRequestsFuture.complete(result);}}}@Override public void onFailure(RuntimeExceptione){synchronized (listOffsetRequestsFuture){if (!listOffsetRequestsFuture.isDone())listOffsetRequestsFuture.raise(e);}}});;
:return listOffsetRequestsFuture;;
stop
end
start
note right
Map<Node,Map<TopicPartition,ListOffsetsPartition>>groupListOffsetRequests(Map<TopicPartition,
Long>timestampsToSearch,Set<TopicPartition>partitionsToRetry)
end note
:final Map<TopicPartition,ListOffsetsPartition>partitionDataMap=new HashMap<>();;
:TopicPartitiontp=entry.getKey();;
:Longoffset=entry.getValue();;
:Metadata.LeaderAndEpochleaderAndEpoch=metadata.currentLeader(tp);;
if (!leaderAndEpoch.leader.isPresent()) then (yes) 
:log.debug("Leader for partition {} is unknown for fetching offset {}",
tp,offset);;
:metadata.requestUpdate();;
:partitionsToRetry.add(tp);;
else (no)
:Nodeleader=leaderAndEpoch.leader.get();;
if (client.isUnavailable(leader)) then (yes) 
:client.maybeThrowAuthFailure(leader);;
:log.debug("Leader {} for partition {} is unavailable for fetching offset until reconnect backoff expires",
leader,tp);;
:partitionsToRetry.add(tp);;
else (no)
:intcurrentLeaderEpoch=leaderAndEpoch.epoch.orElse(ListOffsetsResponse.UNKNOWN_EPOCH);;
:partitionDataMap.put(tp,new ListOffsetsPartition()
.setPartitionIndex(tp.partition()).setTimestamp(offset).setCurrentLeaderEpoch(currentLeaderEpoch));;
endif 
endif 
:return regroupPartitionMapByNode(partitionDataMap);;
stop
end
start
note right
RequestFuture<ListOffsetResult>sendListOffsetRequest(final Nodenode,
final Map<TopicPartition,ListOffsetsPartition>timestampsToSearch,
booleanrequireTimestamp)
end note
:ListOffsetsRequest.Builderbuilder=ListOffsetsRequest.Builder.forConsumer(requireTimestamp,
isolationLevel,false).setTargetTimes(ListOffsetsRequest.toListOffsetsTopics(timestampsToSearch));;
:log.debug("Sending ListOffsetRequest {} to broker {}",
builder,node);;
:return client.send(node,builder).compose(new RequestFutureAdapter<ClientResponse,
ListOffsetResult>(){@Override public void onSuccess(ClientResponseresponse,
RequestFuture<ListOffsetResult>future){ListOffsetsResponselor=(ListOffsetsResponse)response.responseBody();log.trace("Received ListOffsetResponse {} from broker {}",
lor,node);handleListOffsetResponse(lor,future);}});;
stop
end
start
note right
void handleListOffsetResponse(ListOffsetsResponselistOffsetsResponse,
RequestFuture<ListOffsetResult>future)
end note
:Map<TopicPartition,ListOffsetData>fetchedOffsets=new HashMap<>();;
:Set<TopicPartition>partitionsToRetry=new HashSet<>();;
:Set<String>unauthorizedTopics=new HashSet<>();;
:TopicPartitiontopicPartition=new TopicPartition(topic.name(),
partition.partitionIndex());;
:Errorserror=Errors.forCode(partition.errorCode());;
switch (switch error)
endswitch
if (!unauthorizedTopics.isEmpty()) then (yes) 
:future.raise(new TopicAuthorizationException(unauthorizedTopics));;
else (no)
:future.complete(new ListOffsetResult(fetchedOffsets,
partitionsToRetry));;
endif 
end
start
note right
constructor ListOffsetResult(Map<TopicPartition,
ListOffsetData>fetchedOffsets,Set<TopicPartition>partitionsNeedingRetry)
end note
:this.fetchedOffsets=fetchedOffsets;;
:this.partitionsToRetry=partitionsNeedingRetry;;
end
start
note right
constructor ListOffsetResult()
end note
:this.fetchedOffsets=new HashMap<>();;
:this.partitionsToRetry=new HashSet<>();;
end
start
note right
List<TopicPartition>fetchablePartitions()
end note
:Set<TopicPartition>exclude=new HashSet<>();;
if (nextInLineFetch!=null &&!nextInLineFetch.isConsumed) then (yes) 
:exclude.add(nextInLineFetch.partition);;
endif 
:exclude.add(completedFetch.partition);;
:return subscriptions.fetchablePartitions(tp->!exclude.contains(tp));;
stop
end
start
note right
NodeselectReadReplica(TopicPartitionpartition,
NodeleaderReplica,longcurrentTimeMs)
end note
:Optional<Integer>nodeId=subscriptions.preferredReadReplica(partition,
currentTimeMs);;
if (nodeId.isPresent()) then (yes) 
:Optional<Node>node=nodeId.flatMap(id->metadata.fetch().nodeIfOnline(partition,
id));;
if (node.isPresent()) then (yes) 
:return node.get();;
stop
else (no)
:log.trace("Not fetching from {} for partition {} since it is marked offline or is missing from our metadata,"+
" using the leader instead.",nodeId,partition);;
:subscriptions.clearPreferredReadReplica(partition);;
:return leaderReplica;;
stop
endif 
else (no)
:return leaderReplica;;
stop
endif 
end
start
note right
void validatePositionsOnMetadataChange()
end note
:intnewMetadataUpdateVersion=metadata.updateVersion();;
if (metadataUpdateVersion.getAndSet(newMetadataUpdateVersion)!=newMetadataUpdateVersion) then (yes) 
:subscriptions.assignedPartitions().forEach(topicPartition->{ConsumerMetadata.LeaderAndEpochleaderAndEpoch=metadata.currentLeader(topicPartition);
:subscriptions.maybeValidatePositionForCurrentLeader(apiVersions,
topicPartition,leaderAndEpoch);});;
endif 
end
start
note right
Map<Node,FetchSessionHandler.FetchRequestData>prepareFetchRequests()
end note
:Map<Node,FetchSessionHandler.Builder>fetchable=new LinkedHashMap<>();;
:validatePositionsOnMetadataChange();;
:longcurrentTimeMs=time.milliseconds();;
:Map<String,Uuid>topicIds=metadata.topicIds();;
:FetchPositionposition=this.subscriptions.position(partition);;
if (position==null ) then (yes) 
:throw new IllegalStateException("Missing position for fetchable partition "+
partition);;
stop
endif 
:Optional<Node>leaderOpt=position.currentLeader.leader;;
if (!leaderOpt.isPresent()) then (yes) 
:log.debug("Requesting metadata update for partition {} since the position {} is missing the current leader node",
partition,position);;
:metadata.requestUpdate();;
:continue ;;
endif 
:Nodenode=selectReadReplica(partition,leaderOpt.get(),
currentTimeMs);;
if (client.isUnavailable(node)) then (yes) 
:client.maybeThrowAuthFailure(node);;
:log.trace("Skipping fetch for partition {} because node {} is awaiting reconnect backoff",
partition,node);;
else if (this.nodesWithPendingFetchRequests.contains(node.id())) then (yes) 
:log.trace("Skipping fetch for partition {} because previous request to {} has not been processed",
partition,node);;
else (no)
:FetchSessionHandler.Builderbuilder=fetchable.get(node);;
if (builder==null ) then (yes) 
:intid=node.id();;
:FetchSessionHandlerhandler=sessionHandler(id);;
if (handler==null ) then (yes) 
:handler=new FetchSessionHandler(logContext,
id);;
:sessionHandlers.put(id,handler);;
endif 
:builder=handler.newBuilder();;
:fetchable.put(node,builder);;
endif 
:builder.add(partition,new FetchRequest.PartitionData(topicIds.getOrDefault(partition.topic(),
Uuid.ZERO_UUID),position.offset,FetchRequest.INVALID_LOG_START_OFFSET,
this.fetchSize,position.currentLeader.epoch,
Optional.empty()));;
:log.debug("Added {} fetch request for partition {} at position {} to node {}",
isolationLevel,partition,position,node);;
endif 
:Map<Node,FetchSessionHandler.FetchRequestData>reqs=new LinkedHashMap<>();;
:reqs.put(entry.getKey(),entry.getValue()
.build());;
:return reqs;;
stop
end
start
note right
Map<Node,Map<TopicPartition,FetchPosition>>regroupFetchPositionsByLeader(Map<TopicPartition,
FetchPosition>partitionMap)
end note
:return partitionMap.entrySet().stream().filter(entry->entry.getValue().currentLeader.leader.isPresent()).collect(Collectors.groupingBy(entry->entry.getValue().currentLeader.leader.get(),
Collectors.toMap(Map.Entry::getKey,Map.Entry::getValue)));;
stop
end
:return partitionMap.entrySet().stream().collect(Collectors.groupingBy(entry->metadata.fetch().leaderFor(entry.getKey()),
Collectors.toMap(Map.Entry::getKey,Map.Entry::getValue)));;
stop
start
note right
CompletedFetchinitializeCompletedFetch(CompletedFetchnextCompletedFetch)
end note
:TopicPartitiontp=nextCompletedFetch.partition;;
:FetchResponseData.PartitionDatapartition=nextCompletedFetch.partitionData;;
:longfetchOffset=nextCompletedFetch.nextFetchOffset;;
:CompletedFetchcompletedFetch=null ;;
:Errorserror=Errors.forCode(partition.errorCode());;
switch (try/catch block)
case (try)
if (!subscriptions.hasValidPosition(tp)) then (yes) 
:log.debug("Ignoring fetched records for partition {} since it no longer has valid position",
tp);;
else if (error==Errors.NONE) then (yes) 
:FetchPositionposition=subscriptions.position(tp);;
if (position==null ||position.offset!=fetchOffset) then (yes) 
:log.debug("Discarding stale fetch response for partition {} since its offset {} does not match "+
"the expected offset {}",tp,fetchOffset,position);;
:return null ;;
stop
endif 
:log.trace("Preparing to read {} bytes of data for partition {} with offset {}",
FetchResponse.recordsSize(partition),tp,position);;
:Iterator<?  extends RecordBatch>batches=FetchResponse.recordsOrFail(partition)
.batches().iterator();;
:completedFetch=nextCompletedFetch;;
if (!batches.hasNext()&&FetchResponse.recordsSize(partition)>0) then (yes) 
if (completedFetch.responseVersion<3) then (yes) 
:Map<TopicPartition,Long>recordTooLargePartitions=Collections.singletonMap(tp,
fetchOffset);;
:throw new RecordTooLargeException("There are some messages at [Partition=Offset]: "+
recordTooLargePartitions+" whose size is larger than the fetch size "+
this.fetchSize+" and hence cannot be returned. Please considering upgrading your broker to 0.10.1.0 or "+
"newer to avoid this issue. Alternately, increase the fetch size on the client (using "+
ConsumerConfig.MAX_PARTITION_FETCH_BYTES_CONFIG+
")",recordTooLargePartitions);;
stop
else (no)
:throw new KafkaException("Failed to make progress reading messages at "+
tp+"="+fetchOffset+". Received a non-empty fetch response from the server, but no "+
"complete records were found.");;
stop
endif 
endif 
if (partition.highWatermark()>=0) then (yes) 
:log.trace("Updating high watermark for partition {} to {}",
tp,partition.highWatermark());;
:subscriptions.updateHighWatermark(tp,partition.highWatermark());;
endif 
if (partition.logStartOffset()>=0) then (yes) 
:log.trace("Updating log start offset for partition {} to {}",
tp,partition.logStartOffset());;
:subscriptions.updateLogStartOffset(tp,partition.logStartOffset());;
endif 
if (partition.lastStableOffset()>=0) then (yes) 
:log.trace("Updating last stable offset for partition {} to {}",
tp,partition.lastStableOffset());;
:subscriptions.updateLastStableOffset(tp,
partition.lastStableOffset());;
endif 
if (FetchResponse.isPreferredReplica(partition)) then (yes) 
:subscriptions.updatePreferredReadReplica(completedFetch.partition,partition.preferredReadReplica(),()->{longexpireTimeMs=time.milliseconds()+
metadata.metadataExpireMs();log.debug("Updating preferred read replica for partition {} to {},
 set to expire at {}",tp,partition.preferredReadReplica(),
expireTimeMs);return expireTimeMs;});;
endif 
:nextCompletedFetch.initialized=true;;
else if (error==Errors.NOT_LEADER_OR_FOLLOWER||error==Errors.REPLICA_NOT_AVAILABLE||error==Errors.KAFKA_STORAGE_ERROR||error==Errors.FENCED_LEADER_EPOCH||error==Errors.OFFSET_NOT_AVAILABLE) then (yes) 
:log.debug("Error in fetch for partition {}: {}",
tp,error.exceptionName());;
:this.metadata.requestUpdate();;
else if (error==Errors.UNKNOWN_TOPIC_OR_PARTITION) then (yes) 
:log.warn("Received unknown topic or partition error in fetch for partition {}",
tp);;
:this.metadata.requestUpdate();;
else if (error==Errors.UNKNOWN_TOPIC_ID) then (yes) 
:log.warn("Received unknown topic ID error in fetch for partition {}",
tp);;
:this.metadata.requestUpdate();;
else if (error==Errors.INCONSISTENT_TOPIC_ID) then (yes) 
:log.warn("Received inconsistent topic ID error in fetch for partition {}",
tp);;
:this.metadata.requestUpdate();;
else if (error==Errors.OFFSET_OUT_OF_RANGE) then (yes) 
:Optional<Integer>clearedReplicaId=subscriptions.clearPreferredReadReplica(tp);;
if (!clearedReplicaId.isPresent()) then (yes) 
:FetchPositionposition=subscriptions.position(tp);;
if (position==null ||fetchOffset!=position.offset) then (yes) 
:log.debug("Discarding stale fetch response for partition {} since the fetched offset {} "+
"does not match the current offset {}",tp,
fetchOffset,position);;
else (no)
:handleOffsetOutOfRange(position,tp);;
endif 
else (no)
:log.debug("Unset the preferred read replica {} for partition {} since we got {} when fetching {}",
clearedReplicaId.get(),tp,error,fetchOffset);;
endif 
else if (error==Errors.TOPIC_AUTHORIZATION_FAILED) then (yes) 
:log.warn("Not authorized to read from partition {}.",
tp);;
:throw new TopicAuthorizationException(Collections.singleton(tp.topic()));;
stop
else if (error==Errors.UNKNOWN_LEADER_EPOCH) then (yes) 
:log.debug("Received unknown leader epoch error in fetch for partition {}",
tp);;
else if (error==Errors.UNKNOWN_SERVER_ERROR) then (yes) 
:log.warn("Unknown server error while fetching offset {} for topic-partition {}",
fetchOffset,tp);;
else if (error==Errors.CORRUPT_MESSAGE) then (yes) 
:throw new KafkaException("Encountered corrupt message when fetching offset "+
fetchOffset+" for topic-partition "+tp);;
stop
else (no)
:throw new IllegalStateException("Unexpected error code "+
error.code()+" while fetching at offset "+
fetchOffset+" from topic-partition "+tp);;
stop
endif 
endswitch
:return completedFetch;;
stop
end
start
note right
void handleOffsetOutOfRange(FetchPositionfetchPosition,
TopicPartitiontopicPartition)
end note
:StringerrorMessage="Fetch position "+fetchPosition+
" is out of range for partition "+topicPartition;;
if (subscriptions.hasDefaultOffsetResetPolicy()) then (yes) 
:log.info("{}, resetting offset",errorMessage);;
:subscriptions.requestOffsetReset(topicPartition);;
else (no)
:log.info("{}, raising error to the application since no reset policy is configured",
errorMessage);;
:throw new OffsetOutOfRangeException(errorMessage,
Collections.singletonMap(topicPartition,fetchPosition.offset));;
stop
endif 
end
start
note right
ConsumerRecord<K,V>parseRecord(TopicPartitionpartition,
RecordBatchbatch,Recordrecord )
end note
switch (try/catch block)
case (try)
:longoffset=record .offset();;
:longtimestamp=record .timestamp();;
:Optional<Integer>leaderEpoch=maybeLeaderEpoch(batch.partitionLeaderEpoch());;
:TimestampTypetimestampType=batch.timestampType();;
:Headersheaders=new RecordHeaders(record .headers());;
:ByteBufferkeyBytes=record .key();;
:byte[]keyByteArray=keyBytes==null ? null :Utils.toArray(keyBytes);;
:Kkey=keyBytes==null ? null :this.keyDeserializer.deserialize(partition.topic(),
headers,keyByteArray);;
:ByteBuffervalueBytes=record .value();;
:byte[]valueByteArray=valueBytes==null ? null :Utils.toArray(valueBytes);;
:Vvalue=valueBytes==null ? null :this.valueDeserializer.deserialize(partition.topic(),
headers,valueByteArray);;
:return new ConsumerRecord<>(partition.topic(),
partition.partition(),offset,timestamp,timestampType,
keyByteArray==null ? ConsumerRecord.NULL_SIZE:keyByteArray.length,
valueByteArray==null ? ConsumerRecord.NULL_SIZE:valueByteArray.length,
key,value,headers,leaderEpoch);;
stop
case (catch (RuntimeExceptione))
:throw new RecordDeserializationException(partition,record .offset(),"Error deserializing key/value for partition "+
partition+" at offset "+record .offset()+
". If needed, please seek past the record to continue consumption.",e);;
stop
endswitch
end
start
note right
Optional<Integer>maybeLeaderEpoch(intleaderEpoch)
end note
:return leaderEpoch==RecordBatch.NO_PARTITION_LEADER_EPOCH? Optional.empty():Optional.of(leaderEpoch);;
stop
end
start
note right
void clearBufferedDataForUnassignedPartitions(Collection<TopicPartition>assignedPartitions)
end note
:Iterator<CompletedFetch>completedFetchesItr=completedFetches.iterator();;
while (completedFetchesItr.hasNext())
:CompletedFetchrecords=completedFetchesItr.next();;
:TopicPartitiontp=records.partition;;
if (!assignedPartitions.contains(tp)) then (yes) 
:records.drain();;
:completedFetchesItr.remove();;
endif 
endwhile
if (nextInLineFetch!=null &&!assignedPartitions.contains(nextInLineFetch.partition)) then (yes) 
:nextInLineFetch.drain();;
:nextInLineFetch=null ;;
endif 
end
start
note right
void clearBufferedDataForUnassignedTopics(Collection<String>assignedTopics)
end note
:Set<TopicPartition>currentTopicPartitions=new HashSet<>();;
if (assignedTopics.contains(tp.topic())) then (yes) 
:currentTopicPartitions.add(tp);;
endif 
:clearBufferedDataForUnassignedPartitions(currentTopicPartitions);;
end
start
note right
FetchSessionHandlersessionHandler(intnode)
end note
:return sessionHandlers.get(node);;
stop
end
start
note right
SensorthrottleTimeSensor(Metricsmetrics,FetcherMetricsRegistrymetricsRegistry)
end note
:SensorfetchThrottleTimeSensor=metrics.sensor("fetch-throttle-time");;
:fetchThrottleTimeSensor.add(metrics.metricInstance(metricsRegistry.fetchThrottleTimeAvg),
new Avg());;
:fetchThrottleTimeSensor.add(metrics.metricInstance(metricsRegistry.fetchThrottleTimeMax),
new Max());;
:return fetchThrottleTimeSensor;;
stop
end
start
note right
constructor CompletedFetch(TopicPartitionpartition,
FetchResponseData.PartitionDatapartitionData,
FetchResponseMetricAggregatormetricAggregator,
Iterator<?  extends RecordBatch>batches,LongfetchOffset,
shortresponseVersion)
end note
:this.partition=partition;;
:this.partitionData=partitionData;;
:this.metricAggregator=metricAggregator;;
:this.batches=batches;;
:this.nextFetchOffset=fetchOffset;;
:this.responseVersion=responseVersion;;
:this.lastEpoch=Optional.empty();;
:this.abortedProducerIds=new HashSet<>();;
:this.abortedTransactions=abortedTransactions(partitionData);;
end
start
note right
void drain()
end note
if (!isConsumed) then (yes) 
:maybeCloseRecordStream();;
:cachedRecordException=null ;;
:this.isConsumed=true;;
:this.metricAggregator.record (partition,
bytesRead,recordsRead);;
if (bytesRead>0) then (yes) 
:subscriptions.movePartitionToEnd(partition);;
endif 
endif 
end
start
note right
void maybeEnsureValid(RecordBatchbatch)
end note
if (checkCrcs&&currentBatch.magic()>=RecordBatch.MAGIC_VALUE_V2) then (yes) 
switch (try/catch block)
case (try)
:batch.ensureValid();;
case (catch (CorruptRecordExceptione))
:throw new KafkaException("Record batch for partition "+
partition+" at offset "+batch.baseOffset()+
" is invalid, cause: "+e.getMessage());;
stop
endswitch
endif 
end
start
note right
void maybeEnsureValid(Recordrecord )
end note
if (checkCrcs) then (yes) 
switch (try/catch block)
case (try)
:record .ensureValid();;
case (catch (CorruptRecordExceptione))
:throw new KafkaException("Record for partition "+
partition+" at offset "+record .offset()+
" is invalid, cause: "+e.getMessage());;
stop
endswitch
endif 
end
start
note right
void maybeCloseRecordStream()
end note
if (records!=null ) then (yes) 
:records.close();;
:records=null ;;
endif 
end
start
note right
RecordnextFetchedRecord()
end note
while (true)
if (records==null ||!records.hasNext()) then (yes) 
:maybeCloseRecordStream();;
if (!batches.hasNext()) then (yes) 
if (currentBatch!=null ) then (yes) 
:nextFetchOffset=currentBatch.nextOffset();;
endif 
:drain();;
:return null ;;
stop
endif 
:currentBatch=batches.next();;
:lastEpoch=currentBatch.partitionLeaderEpoch()==RecordBatch.NO_PARTITION_LEADER_EPOCH? Optional.empty():Optional.of(currentBatch.partitionLeaderEpoch());;
:maybeEnsureValid(currentBatch);;
if (isolationLevel==IsolationLevel.READ_COMMITTED&&currentBatch.hasProducerId()) then (yes) 
:consumeAbortedTransactionsUpTo(currentBatch.lastOffset());;
:longproducerId=currentBatch.producerId();;
if (containsAbortMarker(currentBatch)) then (yes) 
:abortedProducerIds.remove(producerId);;
else if (isBatchAborted(currentBatch)) then (yes) 
:log.debug("Skipping aborted record batch from partition {} with producerId {} and "+
"offsets {} to {}",partition,producerId,currentBatch.baseOffset(),
currentBatch.lastOffset());;
:nextFetchOffset=currentBatch.nextOffset();;
:continue ;;
endif 
endif 
:records=currentBatch.streamingIterator(decompressionBufferSupplier);;
else (no)
:Recordrecord =records.next();;
if (record .offset()>=nextFetchOffset) then (yes) 
:maybeEnsureValid(record );;
if (!currentBatch.isControlBatch()) then (yes) 
:return record ;;
stop
else (no)
:nextFetchOffset=record .offset()+1;;
endif 
endif 
endif 
endwhile
end
start
note right
List<ConsumerRecord<K,V>>fetchRecords(intmaxRecords)
end note
if (corruptLastRecord) then (yes) 
:throw new KafkaException("Received exception when fetching the next record from "+
partition+". If needed, please seek past the record to "+
"continue consumption.",cachedRecordException);;
stop
endif 
if (isConsumed) then (yes) 
:return Collections.emptyList();;
stop
endif 
:List<ConsumerRecord<K,V>>records=new ArrayList<>();;
switch (try/catch block)
case (try)
if (cachedRecordException==null ) then (yes) 
:corruptLastRecord=true;;
:lastRecord=nextFetchedRecord();;
:corruptLastRecord=false;;
endif 
if (lastRecord==null ) then (yes) 
:break ;;
endif 
:records.add(parseRecord(partition,currentBatch,
lastRecord));;
:recordsRead++;;
:bytesRead+=lastRecord.sizeInBytes();;
:nextFetchOffset=lastRecord.offset()+1;;
:cachedRecordException=null ;;
case (catch (SerializationExceptionse))
:cachedRecordException=se;;
if (records.isEmpty()) then (yes) 
:throw se;;
stop
endif 
case (catch (KafkaExceptione))
:cachedRecordException=e;;
if (records.isEmpty()) then (yes) 
:throw new KafkaException("Received exception when fetching the next record from "+
partition+". If needed, please seek past the record to "+
"continue consumption.",e);;
stop
endif 
endswitch
:return records;;
stop
end
start
note right
void consumeAbortedTransactionsUpTo(longoffset)
end note
if (abortedTransactions==null ) then (yes) 
:return ;;
stop
endif 
while (!abortedTransactions.isEmpty()&&abortedTransactions.peek().firstOffset()<=offset)
:FetchResponseData.AbortedTransactionabortedTransaction=abortedTransactions.poll();;
:abortedProducerIds.add(abortedTransaction.producerId());;
endwhile
end
start
note right
booleanisBatchAborted(RecordBatchbatch)
end note
:return batch.isTransactional()&&abortedProducerIds.contains(batch.producerId());;
stop
end
start
note right
PriorityQueue<FetchResponseData.AbortedTransaction>abortedTransactions(FetchResponseData.PartitionDatapartition)
end note
if (partition.abortedTransactions()==null ||partition.abortedTransactions().isEmpty()) then (yes) 
:return null ;;
stop
endif 
:PriorityQueue<FetchResponseData.AbortedTransaction>abortedTransactions=new PriorityQueue<>(partition.abortedTransactions().size(),
Comparator.comparingLong(FetchResponseData.AbortedTransaction::firstOffset));;
:abortedTransactions.addAll(partition.abortedTransactions());;
:return abortedTransactions;;
stop
end
start
note right
booleancontainsAbortMarker(RecordBatchbatch)
end note
if (!batch.isControlBatch()) then (yes) 
:return false;;
stop
endif 
:Iterator<Record>batchIterator=batch.iterator();;
if (!batchIterator.hasNext()) then (yes) 
:return false;;
stop
endif 
:RecordfirstRecord=batchIterator.next();;
:return ControlRecordType.ABORT==ControlRecordType.parse(firstRecord.key());;
stop
end
start
note right
booleannotInitialized()
end note
:return !this.initialized;;
stop
end
start
note right
constructor FetchResponseMetricAggregator(FetchManagerMetricssensors,
Set<TopicPartition>partitions)
end note
:this.sensors=sensors;;
:this.unrecordedPartitions=partitions;;
end
start
note right
void record (TopicPartitionpartition,intbytes,
intrecords)
end note
:this.unrecordedPartitions.remove(partition);;
:this.fetchMetrics.increment(bytes,records);;
:Stringtopic=partition.topic();;
:FetchMetricstopicFetchMetric=this.topicFetchMetrics.get(topic);;
if (topicFetchMetric==null ) then (yes) 
:topicFetchMetric=new FetchMetrics();;
:this.topicFetchMetrics.put(topic,topicFetchMetric);;
endif 
:topicFetchMetric.increment(bytes,records);;
if (this.unrecordedPartitions.isEmpty()) then (yes) 
:this.sensors.bytesFetched.record (this.fetchMetrics.fetchBytes);;
:this.sensors.recordsFetched.record (this.fetchMetrics.fetchRecords);;
:FetchMetricsmetric=entry.getValue();;
:this.sensors.recordTopicFetchMetrics(entry.getKey(),
metric.fetchBytes,metric.fetchRecords);;
endif 
end
start
note right
void increment(intbytes,intrecords)
end note
:this.fetchBytes+=bytes;;
:this.fetchRecords+=records;;
end
start
note right
constructor FetchManagerMetrics(Metricsmetrics,
FetcherMetricsRegistrymetricsRegistry)
end note
:this.metrics=metrics;;
:this.metricsRegistry=metricsRegistry;;
:this.bytesFetched=metrics.sensor("bytes-fetched");;
:this.bytesFetched.add(metrics.metricInstance(metricsRegistry.fetchSizeAvg),
new Avg());;
:this.bytesFetched.add(metrics.metricInstance(metricsRegistry.fetchSizeMax),
new Max());;
:this.bytesFetched.add(new Meter(metrics.metricInstance(metricsRegistry.bytesConsumedRate),
metrics.metricInstance(metricsRegistry.bytesConsumedTotal)));;
:this.recordsFetched=metrics.sensor("records-fetched");;
:this.recordsFetched.add(metrics.metricInstance(metricsRegistry.recordsPerRequestAvg),
new Avg());;
:this.recordsFetched.add(new Meter(metrics.metricInstance(metricsRegistry.recordsConsumedRate),
metrics.metricInstance(metricsRegistry.recordsConsumedTotal)));;
:this.fetchLatency=metrics.sensor("fetch-latency");;
:this.fetchLatency.add(metrics.metricInstance(metricsRegistry.fetchLatencyAvg),
new Avg());;
:this.fetchLatency.add(metrics.metricInstance(metricsRegistry.fetchLatencyMax),
new Max());;
:this.fetchLatency.add(new Meter(new WindowedCount(),
metrics.metricInstance(metricsRegistry.fetchRequestRate),
metrics.metricInstance(metricsRegistry.fetchRequestTotal)));;
:this.recordsFetchLag=metrics.sensor("records-lag");;
:this.recordsFetchLag.add(metrics.metricInstance(metricsRegistry.recordsLagMax),
new Max());;
:this.recordsFetchLead=metrics.sensor("records-lead");;
:this.recordsFetchLead.add(metrics.metricInstance(metricsRegistry.recordsLeadMin),
new Min());;
end
start
note right
void recordTopicFetchMetrics(Stringtopic,
intbytes,intrecords)
end note
:Stringname="topic."+topic+".bytes-fetched";;
:SensorbytesFetched=this.metrics.getSensor(name);;
if (bytesFetched==null ) then (yes) 
:Map<String,String>metricTags=Collections.singletonMap("topic",
topic.replace('.','_'));;
:bytesFetched=this.metrics.sensor(name);;
:bytesFetched.add(this.metrics.metricInstance(metricsRegistry.topicFetchSizeAvg,
metricTags),new Avg());;
:bytesFetched.add(this.metrics.metricInstance(metricsRegistry.topicFetchSizeMax,
metricTags),new Max());;
:bytesFetched.add(new Meter(this.metrics.metricInstance(metricsRegistry.topicBytesConsumedRate,
metricTags),this.metrics.metricInstance(metricsRegistry.topicBytesConsumedTotal,
metricTags)));;
endif 
:bytesFetched.record (bytes);;
:name="topic."+topic+".records-fetched";;
:SensorrecordsFetched=this.metrics.getSensor(name);;
if (recordsFetched==null ) then (yes) 
:Map<String,String>metricTags=new HashMap<>(1);;
:metricTags.put("topic",topic.replace('.',
'_'));;
:recordsFetched=this.metrics.sensor(name);;
:recordsFetched.add(this.metrics.metricInstance(metricsRegistry.topicRecordsPerRequestAvg,
metricTags),new Avg());;
:recordsFetched.add(new Meter(this.metrics.metricInstance(metricsRegistry.topicRecordsConsumedRate,
metricTags),this.metrics.metricInstance(metricsRegistry.topicRecordsConsumedTotal,
metricTags)));;
endif 
:recordsFetched.record (records);;
end
start
note right
void maybeUpdateAssignment(SubscriptionStatesubscription)
end note
:intnewAssignmentId=subscription.assignmentId();;
if (this.assignmentId!=newAssignmentId) then (yes) 
:Set<TopicPartition>newAssignedPartitions=subscription.assignedPartitions();;
if (!newAssignedPartitions.contains(tp)) then (yes) 
:metrics.removeSensor(partitionLagMetricName(tp));;
:metrics.removeSensor(partitionLeadMetricName(tp));;
:metrics.removeMetric(partitionPreferredReadReplicaMetricName(tp));;
endif 
if (!this.assignedPartitions.contains(tp)) then (yes) 
:MetricNamemetricName=partitionPreferredReadReplicaMetricName(tp);;
:metrics.addMetricIfAbsent(metricName,null ,
(Gauge<Integer>)(config,now)->subscription.preferredReadReplica(tp,
0L).orElse(-1));;
endif 
:this.assignedPartitions=newAssignedPartitions;;
:this.assignmentId=newAssignmentId;;
endif 
end
start
note right
void recordPartitionLead(TopicPartitiontp,
longlead)
end note
:this.recordsFetchLead.record (lead);;
:Stringname=partitionLeadMetricName(tp);;
:SensorrecordsLead=this.metrics.getSensor(name);;
if (recordsLead==null ) then (yes) 
:Map<String,String>metricTags=topicPartitionTags(tp);;
:recordsLead=this.metrics.sensor(name);;
:recordsLead.add(this.metrics.metricInstance(metricsRegistry.partitionRecordsLead,
metricTags),new Value());;
:recordsLead.add(this.metrics.metricInstance(metricsRegistry.partitionRecordsLeadMin,
metricTags),new Min());;
:recordsLead.add(this.metrics.metricInstance(metricsRegistry.partitionRecordsLeadAvg,
metricTags),new Avg());;
endif 
:recordsLead.record (lead);;
end
start
note right
void recordPartitionLag(TopicPartitiontp,
longlag)
end note
:this.recordsFetchLag.record (lag);;
:Stringname=partitionLagMetricName(tp);;
:SensorrecordsLag=this.metrics.getSensor(name);;
if (recordsLag==null ) then (yes) 
:Map<String,String>metricTags=topicPartitionTags(tp);;
:recordsLag=this.metrics.sensor(name);;
:recordsLag.add(this.metrics.metricInstance(metricsRegistry.partitionRecordsLag,
metricTags),new Value());;
:recordsLag.add(this.metrics.metricInstance(metricsRegistry.partitionRecordsLagMax,
metricTags),new Max());;
:recordsLag.add(this.metrics.metricInstance(metricsRegistry.partitionRecordsLagAvg,
metricTags),new Avg());;
endif 
:recordsLag.record (lag);;
end
start
note right
StringpartitionLagMetricName(TopicPartitiontp)
end note
:return tp+".records-lag";;
stop
end
start
note right
StringpartitionLeadMetricName(TopicPartitiontp)
end note
:return tp+".records-lead";;
stop
end
start
note right
MetricNamepartitionPreferredReadReplicaMetricName(TopicPartitiontp)
end note
:Map<String,String>metricTags=topicPartitionTags(tp);;
:return this.metrics.metricInstance(metricsRegistry.partitionPreferredReadReplica,
metricTags);;
stop
end
start
note right
Map<String,String>topicPartitionTags(TopicPartitiontp)
end note
:Map<String,String>metricTags=new HashMap<>(2);;
:metricTags.put("topic",tp.topic().replace('.',
'_'));;
:metricTags.put("partition",String.valueOf(tp.partition()));;
:return metricTags;;
stop
end
start
note right
void close()
end note
if (nextInLineFetch!=null ) then (yes) 
:nextInLineFetch.drain();;
endif 
:decompressionBufferSupplier.close();;
end
start
note right
Set<String>topicsForPartitions(Collection<TopicPartition>partitions)
end note
:return partitions.stream().map(TopicPartition::topic)
.collect(Collectors.toSet());;
stop
end
@enduml

@startuml
!pragma layout smetana
start
note right
constructor Fetcher(LogContextlogContext,
ConsumerNetworkClientclient,intminBytes,intmaxBytes,
intmaxWaitMs,intfetchSize,intmaxPollRecords,
booleancheckCrcs,StringclientRackId,Deserializer<K>keyDeserializer,
Deserializer<V>valueDeserializer,ConsumerMetadatametadata,
SubscriptionStatesubscriptions,Metricsmetrics,
FetcherMetricsRegistrymetricsRegistry,Timetime,
longretryBackoffMs,longrequestTimeoutMs,IsolationLevelisolationLevel,
ApiVersionsapiVersions)
end note
:this.log=logContext.logger(Fetcher.class );;
:this.logContext=logContext;;
:this.time=time;;
:this.client=client;;
:this.metadata=metadata;;
:this.subscriptions=subscriptions;;
:this.minBytes=minBytes;;
:this.maxBytes=maxBytes;;
:this.maxWaitMs=maxWaitMs;;
:this.fetchSize=fetchSize;;
:this.maxPollRecords=maxPollRecords;;
:this.checkCrcs=checkCrcs;;
:this.clientRackId=clientRackId;;
:this.keyDeserializer=keyDeserializer;;
:this.valueDeserializer=valueDeserializer;;
:this.completedFetches=new ConcurrentLinkedQueue<>();;
:this.sensors=new FetchManagerMetrics(metrics,
metricsRegistry);;
:this.retryBackoffMs=retryBackoffMs;;
:this.requestTimeoutMs=requestTimeoutMs;;
:this.isolationLevel=isolationLevel;;
:this.apiVersions=apiVersions;;
:this.sessionHandlers=new HashMap<>();;
:this.offsetsForLeaderEpochClient=new OffsetsForLeaderEpochClient(client,
logContext);;
:this.nodesWithPendingFetchRequests=new HashSet<>();;
end
start
note right
constructor ListOffsetData(longoffset,Longtimestamp,
Optional<Integer>leaderEpoch)
end note
:this.offset=offset;;
:this.timestamp=timestamp;;
:this.leaderEpoch=leaderEpoch;;
end
start
note right
booleanhasCompletedFetches()
end note
:return !completedFetches.isEmpty();;
stop
end
start
note right
booleanhasAvailableFetches()
end note
:return completedFetches.stream().anyMatch(fetch->subscriptions.isFetchable(fetch.partition));;
stop
end
start
note right
intsendFetches()
end note
:sensors.maybeUpdateAssignment(subscriptions);;
:Map<Node,FetchSessionHandler.FetchRequestData>fetchRequestMap=prepareFetchRequests();;
:final NodefetchTarget=entry.getKey();;
:final FetchSessionHandler.FetchRequestDatadata=entry.getValue();;
:final shortmaxVersion;;
if (!data.canUseTopicIds()) then (yes) 
:maxVersion=(short)12;;
else (no)
:maxVersion=ApiKeys.FETCH.latestVersion();;
endif 
:final FetchRequest.Builderrequest=FetchRequest.Builder.forConsumer(maxVersion,
this.maxWaitMs,this.minBytes,data.toSend()).isolationLevel(isolationLevel).setMaxBytes(this.maxBytes).metadata(data.metadata()).removed(data.toForget()).replaced(data.toReplace()).rackId(clientRackId);;
if (log.isDebugEnabled()) then (yes) 
:log.debug("Sending {} {} to broker {}",isolationLevel,
data.toString(),fetchTarget);;
endif 
:RequestFuture<ClientResponse>future=client.send(fetchTarget,
request);;
:this.nodesWithPendingFetchRequests.add(entry.getKey()
.id());;
:future.addListener(new RequestFutureListener<ClientResponse>(){@Override public void onSuccess(ClientResponseresp){synchronized (Fetcher.this){try {FetchResponseresponse=(FetchResponse)resp.responseBody();FetchSessionHandlerhandler=sessionHandler(fetchTarget.id());if (handler==null ){log.error("Unable to find FetchSessionHandler for node {}. Ignoring fetch response.",fetchTarget.id());return ;}if (!handler.handleResponse(response,resp.requestHeader().apiVersion())){if (response.error()==Errors.FETCH_SESSION_TOPIC_ID_ERROR){metadata.requestUpdate();}return ;}Map<TopicPartition,FetchResponseData.PartitionData>responseData=response.responseData(handler.sessionTopicNames(),resp.requestHeader().apiVersion());Set<TopicPartition>partitions=new HashSet<>(responseData.keySet());FetchResponseMetricAggregatormetricAggregator=new FetchResponseMetricAggregator(sensors,partitions);for (Map.Entry<TopicPartition,FetchResponseData.PartitionData>entry:responseData.entrySet()){TopicPartitionpartition=entry.getKey();FetchRequest.PartitionDatarequestData=data.sessionPartitions().get(partition);if (requestData==null ){Stringmessage;if (data.metadata().isFull()){message=MessageFormatter.arrayFormat("Response for missing full request partition: partition={}; metadata={}",new Object[]{partition,data.metadata()}).getMessage();}else {message=MessageFormatter.arrayFormat("Response for missing session request partition: partition={}; metadata={}; toSend={}; toForget={}; toReplace={}",new Object[]{partition,data.metadata(),data.toSend(),data.toForget(),data.toReplace()}).getMessage();}throw new IllegalStateException(message);}else {longfetchOffset=requestData.fetchOffset;FetchResponseData.PartitionDatapartitionData=entry.getValue();log.debug("Fetch {} at offset {} for partition {} returned fetch data {}",isolationLevel,fetchOffset,partition,partitionData);Iterator<?  extends RecordBatch>batches=FetchResponse.recordsOrFail(partitionData).batches().iterator();shortresponseVersion=resp.requestHeader().apiVersion();completedFetches.add(new CompletedFetch(partition,partitionData,metricAggregator,batches,fetchOffset,responseVersion));}}sensors.fetchLatency.record (resp.requestLatencyMs());}finally {nodesWithPendingFetchRequests.remove(fetchTarget.id());}}}@Override public void onFailure(RuntimeExceptione){synchronized (Fetcher.this){try {FetchSessionHandlerhandler=sessionHandler(fetchTarget.id());if (handler!=null ){handler.handleError(e);}}finally {nodesWithPendingFetchRequests.remove(fetchTarget.id());}}}});;
:return fetchRequestMap.size();;
stop
end
start
note right
Map<String,List<PartitionInfo>>getAllTopicMetadata(Timertimer)
end note
:return getTopicMetadata(MetadataRequest.Builder.allTopics(),
timer);;
stop
end
start
note right
Map<String,List<PartitionInfo>>getTopicMetadata(MetadataRequest.Builderrequest,
Timertimer)
end note
if (!request.isAllTopics()&&request.emptyTopicList()) then (yes) 
:return Collections.emptyMap();;
stop
endif 
repeat
:RequestFuture<ClientResponse>future=sendMetadataRequest(request);;
:client.poll(future,timer);;
if (future.failed()&&!future.isRetriable()) then (yes) 
:throw future.exception();;
stop
endif 
if (future.succeeded()) then (yes) 
:MetadataResponseresponse=(MetadataResponse)future.value()
.responseBody();;
:Clustercluster=response.buildCluster();;
:Set<String>unauthorizedTopics=cluster.unauthorizedTopics();;
if (!unauthorizedTopics.isEmpty()) then (yes) 
:throw new TopicAuthorizationException(unauthorizedTopics);;
stop
endif 
:booleanshouldRetry=false;;
:Map<String,Errors>errors=response.errors();;
if (!errors.isEmpty()) then (yes) 
:log.debug("Topic metadata fetch included errors: {}",
errors);;
:Stringtopic=errorEntry.getKey();;
:Errorserror=errorEntry.getValue();;
if (error==Errors.INVALID_TOPIC_EXCEPTION) then (yes) 
:throw new InvalidTopicException("Topic '"+
topic+"' is invalid");;
stop
else if (error==Errors.UNKNOWN_TOPIC_OR_PARTITION) then (yes) 
:continue ;;
else if (error.exception() instanceof RetriableException) then (yes) 
:shouldRetry=true;;
else (no)
:throw new KafkaException("Unexpected error fetching metadata for topic "+
topic,error.exception());;
stop
endif 
endif 
if (!shouldRetry) then (yes) 
:HashMap<String,List<PartitionInfo>>topicsPartitionInfos=new HashMap<>();;
:topicsPartitionInfos.put(topic,cluster.partitionsForTopic(topic));;
:return topicsPartitionInfos;;
stop
endif 
endif 
:timer.sleep(retryBackoffMs);;
repeat while (timer.notExpired())
:throw new TimeoutException("Timeout expired while fetching topic metadata");;
stop
end
start
note right
RequestFuture<ClientResponse>sendMetadataRequest(MetadataRequest.Builderrequest)
end note
:final Nodenode=client.leastLoadedNode();;
if (node==null ) then (yes) 
:return RequestFuture.noBrokersAvailable();;
stop
else (no)
:return client.send(node,request);;
stop
endif 
end
start
note right
LongoffsetResetStrategyTimestamp(final TopicPartitionpartition)
end note
:OffsetResetStrategystrategy=subscriptions.resetStrategy(partition);;
if (strategy==OffsetResetStrategy.EARLIEST) then (yes) 
:return ListOffsetsRequest.EARLIEST_TIMESTAMP;;
stop
else if (strategy==OffsetResetStrategy.LATEST) then (yes) 
:return ListOffsetsRequest.LATEST_TIMESTAMP;;
stop
else (no)
:return null ;;
stop
endif 
end
start
note right
OffsetResetStrategytimestampToOffsetResetStrategy(longtimestamp)
end note
if (timestamp==ListOffsetsRequest.EARLIEST_TIMESTAMP) then (yes) 
:return OffsetResetStrategy.EARLIEST;;
stop
else if (timestamp==ListOffsetsRequest.LATEST_TIMESTAMP) then (yes) 
:return OffsetResetStrategy.LATEST;;
stop
else (no)
:return null ;;
stop
endif 
end
start
note right
void resetOffsetsIfNeeded()
end note
:RuntimeExceptionexception=cachedListOffsetsException.getAndSet(null );;
if (exception!=null ) then (yes) 
:throw exception;;
stop
endif 
:Set<TopicPartition>partitions=subscriptions.partitionsNeedingReset(time.milliseconds());;
if (partitions.isEmpty()) then (yes) 
:return ;;
stop
endif 
:final Map<TopicPartition,Long>offsetResetTimestamps=new HashMap<>();;
:Longtimestamp=offsetResetStrategyTimestamp(partition);;
if (timestamp!=null ) then (yes) 
:offsetResetTimestamps.put(partition,timestamp);;
endif 
:resetOffsetsAsync(offsetResetTimestamps);;
end
start
note right
void validateOffsetsIfNeeded()
end note
:RuntimeExceptionexception=cachedOffsetForLeaderException.getAndSet(null );;
if (exception!=null ) then (yes) 
:throw exception;;
stop
endif 
:validatePositionsOnMetadataChange();;
:Map<TopicPartition,FetchPosition>partitionsToValidate=subscriptions.partitionsNeedingValidation(time.milliseconds())
.stream().filter(tp->subscriptions.position(tp)!=null ).collect(Collectors.toMap(Function.identity(),
subscriptions::position));;
:validateOffsetsAsync(partitionsToValidate);;
end
start
note right
Map<TopicPartition,OffsetAndTimestamp>offsetsForTimes(Map<TopicPartition,
Long>timestampsToSearch,Timertimer)
end note
:metadata.addTransientTopics(topicsForPartitions(timestampsToSearch.keySet()));;
switch (try/catch block)
case (try)
:Map<TopicPartition,ListOffsetData>fetchedOffsets=fetchOffsetsByTimes(timestampsToSearch,
timer,true).fetchedOffsets;;
:HashMap<TopicPartition,OffsetAndTimestamp>offsetsByTimes=new HashMap<>(timestampsToSearch.size());;
:offsetsByTimes.put(entry.getKey(),null );;
:ListOffsetDataoffsetData=entry.getValue();;
:offsetsByTimes.put(entry.getKey(),new OffsetAndTimestamp(offsetData.offset,
offsetData.timestamp,offsetData.leaderEpoch));;
:return offsetsByTimes;;
stop
endswitch
end
start
note right
ListOffsetResultfetchOffsetsByTimes(Map<TopicPartition,
Long>timestampsToSearch,Timertimer,booleanrequireTimestamps)
end note
:ListOffsetResultresult=new ListOffsetResult();;
if (timestampsToSearch.isEmpty()) then (yes) 
:return result;;
stop
endif 
:Map<TopicPartition,Long>remainingToSearch=new HashMap<>(timestampsToSearch);;
repeat
:RequestFuture<ListOffsetResult>future=sendListOffsetsRequests(remainingToSearch,
requireTimestamps);;
:future.addListener(new RequestFutureListener<ListOffsetResult>(){@Override public void onSuccess(ListOffsetResultvalue){synchronized (future){result.fetchedOffsets.putAll(value.fetchedOffsets);
:remainingToSearch.keySet().retainAll(value.partitionsToRetry);for (final Map.Entry<TopicPartition,
ListOffsetData>entry:value.fetchedOffsets.entrySet()){final TopicPartitionpartition=entry.getKey();if (subscriptions.isAssigned(partition)){final longoffset=entry.getValue().offset;if (isolationLevel==IsolationLevel.READ_COMMITTED){log.trace("Updating last stable offset for partition {} to {}",partition,offset);subscriptions.updateLastStableOffset(partition,offset);}else {log.trace("Updating high watermark for partition {} to {}",partition,offset);subscriptions.updateHighWatermark(partition,offset);}}}}}@Override public void onFailure(RuntimeExceptione){if (!(e instanceof RetriableException)){throw future.exception();}}});;
if (timer.timeoutMs()==0L) then (yes) 
:return result;;
stop
endif 
:client.poll(future,timer);;
if (!future.isDone()) then (yes) 
:break ;;
else if (remainingToSearch.isEmpty()) then (yes) 
:return result;;
stop
else (no)
:client.awaitMetadataUpdate(timer);;
endif 
repeat while (timer.notExpired())
:throw new TimeoutException("Failed to get offsets by times in "+
timer.elapsedMs()+"ms");;
stop
end
start
note right
Map<TopicPartition,Long>beginningOffsets(Collection<TopicPartition>partitions,
Timertimer)
end note
:return beginningOrEndOffset(partitions,ListOffsetsRequest.EARLIEST_TIMESTAMP,
timer);;
stop
end
start
note right
Map<TopicPartition,Long>endOffsets(Collection<TopicPartition>partitions,
Timertimer)
end note
:return beginningOrEndOffset(partitions,ListOffsetsRequest.LATEST_TIMESTAMP,
timer);;
stop
end
start
note right
Map<TopicPartition,Long>beginningOrEndOffset(Collection<TopicPartition>partitions,
longtimestamp,Timertimer)
end note
:metadata.addTransientTopics(topicsForPartitions(partitions));;
switch (try/catch block)
case (try)
:Map<TopicPartition,Long>timestampsToSearch=partitions.stream().distinct().collect(Collectors.toMap(Function.identity(),
tp->timestamp));;
:ListOffsetResultresult=fetchOffsetsByTimes(timestampsToSearch,
timer,false);;
:return result.fetchedOffsets.entrySet().stream().collect(Collectors.toMap(Map.Entry::getKey,
entry->entry.getValue().offset));;
stop
endswitch
end
start
note right
Fetch<K,V>collectFetch()
end note
:Fetch<K,V>fetch=Fetch.empty();;
:Queue<CompletedFetch>pausedCompletedFetches=new ArrayDeque<>();;
:intrecordsRemaining=maxPollRecords;;
switch (try/catch block)
case (try)
while (recordsRemaining>0)
if (nextInLineFetch==null ||nextInLineFetch.isConsumed) then (yes) 
:CompletedFetchrecords=completedFetches.peek();;
if (records==null ) then (yes) 
:break ;;
endif 
if (records.notInitialized()) then (yes) 
switch (try/catch block)
case (try)
:nextInLineFetch=initializeCompletedFetch(records);;
case (catch (Exceptione))
:FetchResponseData.PartitionDatapartition=records.partitionData;;
if (fetch.isEmpty()&&FetchResponse.recordsOrFail(partition).sizeInBytes()==0) then (yes) 
:completedFetches.poll();;
endif 
:throw e;;
stop
endswitch
else (no)
:nextInLineFetch=records;;
endif 
:completedFetches.poll();;
else if (subscriptions.isPaused(nextInLineFetch.partition)) then (yes) 
:log.debug("Skipping fetching records for assigned partition {} because it is paused",
nextInLineFetch.partition);;
:pausedCompletedFetches.add(nextInLineFetch);;
:nextInLineFetch=null ;;
else (no)
:Fetch<K,V>nextFetch=fetchRecords(nextInLineFetch,
recordsRemaining);;
:recordsRemaining-=nextFetch.numRecords();;
:fetch.add(nextFetch);;
endif 
endwhile
endswitch
:return fetch;;
stop
end
start
note right
Fetch<K,V>fetchRecords(CompletedFetchcompletedFetch,
intmaxRecords)
end note
if (!subscriptions.isAssigned(completedFetch.partition)) then (yes) 
:log.debug("Not returning fetched records for partition {} since it is no longer assigned",
completedFetch.partition);;
else if (!subscriptions.isFetchable(completedFetch.partition)) then (yes) 
:log.debug("Not returning fetched records for assigned partition {} since it is no longer fetchable",
completedFetch.partition);;
else (no)
:FetchPositionposition=subscriptions.position(completedFetch.partition);;
if (position==null ) then (yes) 
:throw new IllegalStateException("Missing position for fetchable partition "+
completedFetch.partition);;
stop
endif 
if (completedFetch.nextFetchOffset==position.offset) then (yes) 
:List<ConsumerRecord<K,V>>partRecords=completedFetch.fetchRecords(maxRecords);;
:log.trace("Returning {} fetched records at offset {} for assigned partition {}",
partRecords.size(),position,completedFetch.partition);;
:booleanpositionAdvanced=false;;
if (completedFetch.nextFetchOffset>position.offset) then (yes) 
:FetchPositionnextPosition=new FetchPosition(completedFetch.nextFetchOffset,
completedFetch.lastEpoch,position.currentLeader);;
:log.trace("Updating fetch position from {} to {} for partition {} and returning {} records from  poll() ",
position,nextPosition,completedFetch.partition,
partRecords.size());;
:subscriptions.position(completedFetch.partition,
nextPosition);;
:positionAdvanced=true;;
endif 
:LongpartitionLag=subscriptions.partitionLag(completedFetch.partition,
isolationLevel);;
if (partitionLag!=null ) then (yes) 
:this.sensors.recordPartitionLag(completedFetch.partition,
partitionLag);;
endif 
:Longlead=subscriptions.partitionLead(completedFetch.partition);;
if (lead!=null ) then (yes) 
:this.sensors.recordPartitionLead(completedFetch.partition,
lead);;
endif 
:return Fetch.forPartition(completedFetch.partition,
partRecords,positionAdvanced);;
stop
else (no)
:log.debug("Ignoring fetched records for {} at offset {} since the current position is {}",
completedFetch.partition,completedFetch.nextFetchOffset,
position);;
endif 
endif 
:log.trace("Draining fetched records for partition {}",
completedFetch.partition);;
:completedFetch.drain();;
:return Fetch.empty();;
stop
end
start
note right
void resetOffsetIfNeeded(TopicPartitionpartition,
OffsetResetStrategyrequestedResetStrategy,
ListOffsetDataoffsetData)
end note
:FetchPositionposition=new FetchPosition(offsetData.offset,
Optional.empty(),metadata.currentLeader(partition));;
:offsetData.leaderEpoch.ifPresent(epoch->metadata.updateLastSeenEpochIfNewer(partition,
epoch));;
:subscriptions.maybeSeekUnvalidated(partition,
position,requestedResetStrategy);;
end
start
note right
void resetOffsetsAsync(Map<TopicPartition,
Long>partitionResetTimestamps)
end note
:Map<Node,Map<TopicPartition,ListOffsetsPartition>>timestampsToSearchByNode=groupListOffsetRequests(partitionResetTimestamps,
new HashSet<>());;
:Nodenode=entry.getKey();;
:final Map<TopicPartition,ListOffsetsPartition>resetTimestamps=entry.getValue();;
:subscriptions.setNextAllowedRetry(resetTimestamps.keySet(),time.milliseconds()+
requestTimeoutMs);;
:RequestFuture<ListOffsetResult>future=sendListOffsetRequest(node,
resetTimestamps,false);;
:future.addListener(new RequestFutureListener<ListOffsetResult>(){@Override public void onSuccess(ListOffsetResultresult){if (!result.partitionsToRetry.isEmpty()){subscriptions.requestFailed(result.partitionsToRetry,time.milliseconds()+retryBackoffMs);metadata.requestUpdate();}for (Map.Entry<TopicPartition,ListOffsetData>fetchedOffset:result.fetchedOffsets.entrySet()){TopicPartitionpartition=fetchedOffset.getKey();ListOffsetDataoffsetData=fetchedOffset.getValue();ListOffsetsPartitionrequestedReset=resetTimestamps.get(partition);resetOffsetIfNeeded(partition,timestampToOffsetResetStrategy(requestedReset.timestamp()),offsetData);}}@Override public void onFailure(RuntimeExceptione){subscriptions.requestFailed(resetTimestamps.keySet(),time.milliseconds()+retryBackoffMs);metadata.requestUpdate();if (!(e instanceof RetriableException)&&!cachedListOffsetsException.compareAndSet(null ,e))log.error("Discarding error in ListOffsetResponse because another error is pending",e);}});;
end
start
note right
booleanhasUsableOffsetForLeaderEpochVersion(NodeApiVersionsnodeApiVersions)
end note
:ApiVersionapiVersion=nodeApiVersions.apiVersion(ApiKeys.OFFSET_FOR_LEADER_EPOCH);;
if (apiVersion==null ) then (yes) 
:return false;;
stop
endif 
:return OffsetsForLeaderEpochRequest.supportsTopicPermission(apiVersion.maxVersion());;
stop
end
start
note right
void validateOffsetsAsync(Map<TopicPartition,
FetchPosition>partitionsToValidate)
end note
:final Map<Node,Map<TopicPartition,FetchPosition>>regrouped=regroupFetchPositionsByLeader(partitionsToValidate);;
:longnextResetTimeMs=time.milliseconds()+
requestTimeoutMs;;
:regrouped.forEach((node,fetchPositions)->{if (node.isEmpty()){metadata.requestUpdate();
:return ;}NodeApiVersionsnodeApiVersions=apiVersions.get(node.idString());
:if (nodeApiVersions==null ){client.tryConnect(node);
:return ;}if (!hasUsableOffsetForLeaderEpochVersion(nodeApiVersions)){log.debug("Skipping validation of fetch offsets for partitions {} since the broker does not "+
"support the required protocol version (introduced in Kafka 2.3)",
fetchPositions.keySet());for (TopicPartitionpartition:fetchPositions.keySet()){subscriptions.completeValidation(partition);}return ;}subscriptions.setNextAllowedRetry(fetchPositions.keySet(),
nextResetTimeMs);RequestFuture<OffsetForEpochResult>future=offsetsForLeaderEpochClient.sendAsyncRequest(node,
fetchPositions);future.addListener(new RequestFutureListener<OffsetForEpochResult>(){@Override public void onSuccess(OffsetForEpochResultoffsetsResult){List<SubscriptionState.LogTruncation>truncations=new ArrayList<>();if (!offsetsResult.partitionsToRetry().isEmpty()){subscriptions.setNextAllowedRetry(offsetsResult.partitionsToRetry(),time.milliseconds()+retryBackoffMs);metadata.requestUpdate();}offsetsResult.endOffsets().forEach((topicPartition,respEndOffset)->{FetchPositionrequestPosition=fetchPositions.get(topicPartition);Optional<SubscriptionState.LogTruncation>truncationOpt=subscriptions.maybeCompleteValidation(topicPartition,requestPosition,respEndOffset);truncationOpt.ifPresent(truncations::add);});if (!truncations.isEmpty()){maybeSetOffsetForLeaderException(buildLogTruncationException(truncations));}}@Override public void onFailure(RuntimeExceptione){subscriptions.requestFailed(fetchPositions.keySet(),time.milliseconds()+retryBackoffMs);metadata.requestUpdate();if (!(e instanceof RetriableException)){maybeSetOffsetForLeaderException(e);}}});});;
end
start
note right
LogTruncationExceptionbuildLogTruncationException(List<SubscriptionState.LogTruncation>truncations)
end note
:Map<TopicPartition,OffsetAndMetadata>divergentOffsets=new HashMap<>();;
:Map<TopicPartition,Long>truncatedFetchOffsets=new HashMap<>();;
:truncation.divergentOffsetOpt.ifPresent(divergentOffset->divergentOffsets.put(truncation.topicPartition,
divergentOffset));;
:truncatedFetchOffsets.put(truncation.topicPartition,
truncation.fetchPosition.offset);;
:return new LogTruncationException("Detected truncated partitions: "+
truncations,truncatedFetchOffsets,divergentOffsets);;
stop
end
start
note right
void maybeSetOffsetForLeaderException(RuntimeExceptione)
end note
if (!cachedOffsetForLeaderException.compareAndSet(null ,e)) then (yes) 
:log.error("Discarding error in OffsetsForLeaderEpoch because another error is pending",e);;
endif 
end
start
note right
RequestFuture<ListOffsetResult>sendListOffsetsRequests(final Map<TopicPartition,
Long>timestampsToSearch,final booleanrequireTimestamps)
end note
:final Set<TopicPartition>partitionsToRetry=new HashSet<>();;
:Map<Node,Map<TopicPartition,ListOffsetsPartition>>timestampsToSearchByNode=groupListOffsetRequests(timestampsToSearch,
partitionsToRetry);;
if (timestampsToSearchByNode.isEmpty()) then (yes) 
:return RequestFuture.failure(new StaleMetadataException());;
stop
endif 
:final RequestFuture<ListOffsetResult>listOffsetRequestsFuture=new RequestFuture<>();;
:final Map<TopicPartition,ListOffsetData>fetchedTimestampOffsets=new HashMap<>();;
:final AtomicIntegerremainingResponses=new AtomicInteger(timestampsToSearchByNode.size());;
:RequestFuture<ListOffsetResult>future=sendListOffsetRequest(entry.getKey(),
entry.getValue(),requireTimestamps);;
:future.addListener(new RequestFutureListener<ListOffsetResult>(){@Override public void onSuccess(ListOffsetResultpartialResult){synchronized (listOffsetRequestsFuture){fetchedTimestampOffsets.putAll(partialResult.fetchedOffsets);partitionsToRetry.addAll(partialResult.partitionsToRetry);if (remainingResponses.decrementAndGet()==0&&!listOffsetRequestsFuture.isDone()){ListOffsetResultresult=new ListOffsetResult(fetchedTimestampOffsets,partitionsToRetry);listOffsetRequestsFuture.complete(result);}}}@Override public void onFailure(RuntimeExceptione){synchronized (listOffsetRequestsFuture){if (!listOffsetRequestsFuture.isDone())listOffsetRequestsFuture.raise(e);}}});;
:return listOffsetRequestsFuture;;
stop
end
start
note right
Map<Node,Map<TopicPartition,ListOffsetsPartition>>groupListOffsetRequests(Map<TopicPartition,
Long>timestampsToSearch,Set<TopicPartition>partitionsToRetry)
end note
:final Map<TopicPartition,ListOffsetsPartition>partitionDataMap=new HashMap<>();;
:TopicPartitiontp=entry.getKey();;
:Longoffset=entry.getValue();;
:Metadata.LeaderAndEpochleaderAndEpoch=metadata.currentLeader(tp);;
if (!leaderAndEpoch.leader.isPresent()) then (yes) 
:log.debug("Leader for partition {} is unknown for fetching offset {}",
tp,offset);;
:metadata.requestUpdate();;
:partitionsToRetry.add(tp);;
else (no)
:Nodeleader=leaderAndEpoch.leader.get();;
if (client.isUnavailable(leader)) then (yes) 
:client.maybeThrowAuthFailure(leader);;
:log.debug("Leader {} for partition {} is unavailable for fetching offset until reconnect backoff expires",
leader,tp);;
:partitionsToRetry.add(tp);;
else (no)
:intcurrentLeaderEpoch=leaderAndEpoch.epoch.orElse(ListOffsetsResponse.UNKNOWN_EPOCH);;
:partitionDataMap.put(tp,new ListOffsetsPartition()
.setPartitionIndex(tp.partition()).setTimestamp(offset).setCurrentLeaderEpoch(currentLeaderEpoch));;
endif 
endif 
:return regroupPartitionMapByNode(partitionDataMap);;
stop
end
start
note right
RequestFuture<ListOffsetResult>sendListOffsetRequest(final Nodenode,
final Map<TopicPartition,ListOffsetsPartition>timestampsToSearch,
booleanrequireTimestamp)
end note
:ListOffsetsRequest.Builderbuilder=ListOffsetsRequest.Builder.forConsumer(requireTimestamp,
isolationLevel,false).setTargetTimes(ListOffsetsRequest.toListOffsetsTopics(timestampsToSearch));;
:log.debug("Sending ListOffsetRequest {} to broker {}",
builder,node);;
:return client.send(node,builder).compose(new RequestFutureAdapter<ClientResponse,
ListOffsetResult>(){@Override public void onSuccess(ClientResponseresponse,
RequestFuture<ListOffsetResult>future){ListOffsetsResponselor=(ListOffsetsResponse)response.responseBody();log.trace("Received ListOffsetResponse {} from broker {}",
lor,node);handleListOffsetResponse(lor,future);}});;
stop
end
start
note right
void handleListOffsetResponse(ListOffsetsResponselistOffsetsResponse,
RequestFuture<ListOffsetResult>future)
end note
:Map<TopicPartition,ListOffsetData>fetchedOffsets=new HashMap<>();;
:Set<TopicPartition>partitionsToRetry=new HashSet<>();;
:Set<String>unauthorizedTopics=new HashSet<>();;
:TopicPartitiontopicPartition=new TopicPartition(topic.name(),
partition.partitionIndex());;
:Errorserror=Errors.forCode(partition.errorCode());;
switch (switch error)
endswitch
if (!unauthorizedTopics.isEmpty()) then (yes) 
:future.raise(new TopicAuthorizationException(unauthorizedTopics));;
else (no)
:future.complete(new ListOffsetResult(fetchedOffsets,
partitionsToRetry));;
endif 
end
start
note right
constructor ListOffsetResult(Map<TopicPartition,
ListOffsetData>fetchedOffsets,Set<TopicPartition>partitionsNeedingRetry)
end note
:this.fetchedOffsets=fetchedOffsets;;
:this.partitionsToRetry=partitionsNeedingRetry;;
end
start
note right
constructor ListOffsetResult()
end note
:this.fetchedOffsets=new HashMap<>();;
:this.partitionsToRetry=new HashSet<>();;
end
start
note right
List<TopicPartition>fetchablePartitions()
end note
:Set<TopicPartition>exclude=new HashSet<>();;
if (nextInLineFetch!=null &&!nextInLineFetch.isConsumed) then (yes) 
:exclude.add(nextInLineFetch.partition);;
endif 
:exclude.add(completedFetch.partition);;
:return subscriptions.fetchablePartitions(tp->!exclude.contains(tp));;
stop
end
start
note right
NodeselectReadReplica(TopicPartitionpartition,
NodeleaderReplica,longcurrentTimeMs)
end note
:Optional<Integer>nodeId=subscriptions.preferredReadReplica(partition,
currentTimeMs);;
if (nodeId.isPresent()) then (yes) 
:Optional<Node>node=nodeId.flatMap(id->metadata.fetch().nodeIfOnline(partition,
id));;
if (node.isPresent()) then (yes) 
:return node.get();;
stop
else (no)
:log.trace("Not fetching from {} for partition {} since it is marked offline or is missing from our metadata,"+
" using the leader instead.",nodeId,partition);;
:subscriptions.clearPreferredReadReplica(partition);;
:return leaderReplica;;
stop
endif 
else (no)
:return leaderReplica;;
stop
endif 
end
start
note right
void validatePositionsOnMetadataChange()
end note
:intnewMetadataUpdateVersion=metadata.updateVersion();;
if (metadataUpdateVersion.getAndSet(newMetadataUpdateVersion)!=newMetadataUpdateVersion) then (yes) 
:subscriptions.assignedPartitions().forEach(topicPartition->{ConsumerMetadata.LeaderAndEpochleaderAndEpoch=metadata.currentLeader(topicPartition);
:subscriptions.maybeValidatePositionForCurrentLeader(apiVersions,
topicPartition,leaderAndEpoch);});;
endif 
end
start
note right
Map<Node,FetchSessionHandler.FetchRequestData>prepareFetchRequests()
end note
:Map<Node,FetchSessionHandler.Builder>fetchable=new LinkedHashMap<>();;
:validatePositionsOnMetadataChange();;
:longcurrentTimeMs=time.milliseconds();;
:Map<String,Uuid>topicIds=metadata.topicIds();;
:FetchPositionposition=this.subscriptions.position(partition);;
if (position==null ) then (yes) 
:throw new IllegalStateException("Missing position for fetchable partition "+
partition);;
stop
endif 
:Optional<Node>leaderOpt=position.currentLeader.leader;;
if (!leaderOpt.isPresent()) then (yes) 
:log.debug("Requesting metadata update for partition {} since the position {} is missing the current leader node",
partition,position);;
:metadata.requestUpdate();;
:continue ;;
endif 
:Nodenode=selectReadReplica(partition,leaderOpt.get(),
currentTimeMs);;
if (client.isUnavailable(node)) then (yes) 
:client.maybeThrowAuthFailure(node);;
:log.trace("Skipping fetch for partition {} because node {} is awaiting reconnect backoff",
partition,node);;
else if (this.nodesWithPendingFetchRequests.contains(node.id())) then (yes) 
:log.trace("Skipping fetch for partition {} because previous request to {} has not been processed",
partition,node);;
else (no)
:FetchSessionHandler.Builderbuilder=fetchable.get(node);;
if (builder==null ) then (yes) 
:intid=node.id();;
:FetchSessionHandlerhandler=sessionHandler(id);;
if (handler==null ) then (yes) 
:handler=new FetchSessionHandler(logContext,
id);;
:sessionHandlers.put(id,handler);;
endif 
:builder=handler.newBuilder();;
:fetchable.put(node,builder);;
endif 
:builder.add(partition,new FetchRequest.PartitionData(topicIds.getOrDefault(partition.topic(),
Uuid.ZERO_UUID),position.offset,FetchRequest.INVALID_LOG_START_OFFSET,
this.fetchSize,position.currentLeader.epoch,
Optional.empty()));;
:log.debug("Added {} fetch request for partition {} at position {} to node {}",
isolationLevel,partition,position,node);;
endif 
:Map<Node,FetchSessionHandler.FetchRequestData>reqs=new LinkedHashMap<>();;
:reqs.put(entry.getKey(),entry.getValue()
.build());;
:return reqs;;
stop
end
start
note right
Map<Node,Map<TopicPartition,FetchPosition>>regroupFetchPositionsByLeader(Map<TopicPartition,
FetchPosition>partitionMap)
end note
:return partitionMap.entrySet().stream().filter(entry->entry.getValue().currentLeader.leader.isPresent()).collect(Collectors.groupingBy(entry->entry.getValue().currentLeader.leader.get(),
Collectors.toMap(Map.Entry::getKey,Map.Entry::getValue)));;
stop
end
:return partitionMap.entrySet().stream().collect(Collectors.groupingBy(entry->metadata.fetch().leaderFor(entry.getKey()),
Collectors.toMap(Map.Entry::getKey,Map.Entry::getValue)));;
stop
start
note right
CompletedFetchinitializeCompletedFetch(CompletedFetchnextCompletedFetch)
end note
:TopicPartitiontp=nextCompletedFetch.partition;;
:FetchResponseData.PartitionDatapartition=nextCompletedFetch.partitionData;;
:longfetchOffset=nextCompletedFetch.nextFetchOffset;;
:CompletedFetchcompletedFetch=null ;;
:Errorserror=Errors.forCode(partition.errorCode());;
switch (try/catch block)
case (try)
if (!subscriptions.hasValidPosition(tp)) then (yes) 
:log.debug("Ignoring fetched records for partition {} since it no longer has valid position",
tp);;
else if (error==Errors.NONE) then (yes) 
:FetchPositionposition=subscriptions.position(tp);;
if (position==null ||position.offset!=fetchOffset) then (yes) 
:log.debug("Discarding stale fetch response for partition {} since its offset {} does not match "+
"the expected offset {}",tp,fetchOffset,position);;
:return null ;;
stop
endif 
:log.trace("Preparing to read {} bytes of data for partition {} with offset {}",
FetchResponse.recordsSize(partition),tp,position);;
:Iterator<?  extends RecordBatch>batches=FetchResponse.recordsOrFail(partition)
.batches().iterator();;
:completedFetch=nextCompletedFetch;;
if (!batches.hasNext()&&FetchResponse.recordsSize(partition)>0) then (yes) 
if (completedFetch.responseVersion<3) then (yes) 
:Map<TopicPartition,Long>recordTooLargePartitions=Collections.singletonMap(tp,
fetchOffset);;
:throw new RecordTooLargeException("There are some messages at [Partition=Offset]: "+
recordTooLargePartitions+" whose size is larger than the fetch size "+
this.fetchSize+" and hence cannot be returned. Please considering upgrading your broker to 0.10.1.0 or "+
"newer to avoid this issue. Alternately, increase the fetch size on the client (using "+
ConsumerConfig.MAX_PARTITION_FETCH_BYTES_CONFIG+
")",recordTooLargePartitions);;
stop
else (no)
:throw new KafkaException("Failed to make progress reading messages at "+
tp+"="+fetchOffset+". Received a non-empty fetch response from the server, but no "+
"complete records were found.");;
stop
endif 
endif 
if (partition.highWatermark()>=0) then (yes) 
:log.trace("Updating high watermark for partition {} to {}",
tp,partition.highWatermark());;
:subscriptions.updateHighWatermark(tp,partition.highWatermark());;
endif 
if (partition.logStartOffset()>=0) then (yes) 
:log.trace("Updating log start offset for partition {} to {}",
tp,partition.logStartOffset());;
:subscriptions.updateLogStartOffset(tp,partition.logStartOffset());;
endif 
if (partition.lastStableOffset()>=0) then (yes) 
:log.trace("Updating last stable offset for partition {} to {}",
tp,partition.lastStableOffset());;
:subscriptions.updateLastStableOffset(tp,
partition.lastStableOffset());;
endif 
if (FetchResponse.isPreferredReplica(partition)) then (yes) 
:subscriptions.updatePreferredReadReplica(completedFetch.partition,partition.preferredReadReplica(),()->{longexpireTimeMs=time.milliseconds()+
metadata.metadataExpireMs();log.debug("Updating preferred read replica for partition {} to {},
 set to expire at {}",tp,partition.preferredReadReplica(),
expireTimeMs);return expireTimeMs;});;
endif 
:nextCompletedFetch.initialized=true;;
else if (error==Errors.NOT_LEADER_OR_FOLLOWER||error==Errors.REPLICA_NOT_AVAILABLE||error==Errors.KAFKA_STORAGE_ERROR||error==Errors.FENCED_LEADER_EPOCH||error==Errors.OFFSET_NOT_AVAILABLE) then (yes) 
:log.debug("Error in fetch for partition {}: {}",
tp,error.exceptionName());;
:this.metadata.requestUpdate();;
else if (error==Errors.UNKNOWN_TOPIC_OR_PARTITION) then (yes) 
:log.warn("Received unknown topic or partition error in fetch for partition {}",
tp);;
:this.metadata.requestUpdate();;
else if (error==Errors.UNKNOWN_TOPIC_ID) then (yes) 
:log.warn("Received unknown topic ID error in fetch for partition {}",
tp);;
:this.metadata.requestUpdate();;
else if (error==Errors.INCONSISTENT_TOPIC_ID) then (yes) 
:log.warn("Received inconsistent topic ID error in fetch for partition {}",
tp);;
:this.metadata.requestUpdate();;
else if (error==Errors.OFFSET_OUT_OF_RANGE) then (yes) 
:Optional<Integer>clearedReplicaId=subscriptions.clearPreferredReadReplica(tp);;
if (!clearedReplicaId.isPresent()) then (yes) 
:FetchPositionposition=subscriptions.position(tp);;
if (position==null ||fetchOffset!=position.offset) then (yes) 
:log.debug("Discarding stale fetch response for partition {} since the fetched offset {} "+
"does not match the current offset {}",tp,
fetchOffset,position);;
else (no)
:handleOffsetOutOfRange(position,tp);;
endif 
else (no)
:log.debug("Unset the preferred read replica {} for partition {} since we got {} when fetching {}",
clearedReplicaId.get(),tp,error,fetchOffset);;
endif 
else if (error==Errors.TOPIC_AUTHORIZATION_FAILED) then (yes) 
:log.warn("Not authorized to read from partition {}.",
tp);;
:throw new TopicAuthorizationException(Collections.singleton(tp.topic()));;
stop
else if (error==Errors.UNKNOWN_LEADER_EPOCH) then (yes) 
:log.debug("Received unknown leader epoch error in fetch for partition {}",
tp);;
else if (error==Errors.UNKNOWN_SERVER_ERROR) then (yes) 
:log.warn("Unknown server error while fetching offset {} for topic-partition {}",
fetchOffset,tp);;
else if (error==Errors.CORRUPT_MESSAGE) then (yes) 
:throw new KafkaException("Encountered corrupt message when fetching offset "+
fetchOffset+" for topic-partition "+tp);;
stop
else (no)
:throw new IllegalStateException("Unexpected error code "+
error.code()+" while fetching at offset "+
fetchOffset+" from topic-partition "+tp);;
stop
endif 
endswitch
:return completedFetch;;
stop
end
start
note right
void handleOffsetOutOfRange(FetchPositionfetchPosition,
TopicPartitiontopicPartition)
end note
:StringerrorMessage="Fetch position "+fetchPosition+
" is out of range for partition "+topicPartition;;
if (subscriptions.hasDefaultOffsetResetPolicy()) then (yes) 
:log.info("{}, resetting offset",errorMessage);;
:subscriptions.requestOffsetReset(topicPartition);;
else (no)
:log.info("{}, raising error to the application since no reset policy is configured",
errorMessage);;
:throw new OffsetOutOfRangeException(errorMessage,
Collections.singletonMap(topicPartition,fetchPosition.offset));;
stop
endif 
end
start
note right
ConsumerRecord<K,V>parseRecord(TopicPartitionpartition,
RecordBatchbatch,Recordrecord )
end note
switch (try/catch block)
case (try)
:longoffset=record .offset();;
:longtimestamp=record .timestamp();;
:Optional<Integer>leaderEpoch=maybeLeaderEpoch(batch.partitionLeaderEpoch());;
:TimestampTypetimestampType=batch.timestampType();;
:Headersheaders=new RecordHeaders(record .headers());;
:ByteBufferkeyBytes=record .key();;
:byte[]keyByteArray=keyBytes==null ? null :Utils.toArray(keyBytes);;
:Kkey=keyBytes==null ? null :this.keyDeserializer.deserialize(partition.topic(),
headers,keyByteArray);;
:ByteBuffervalueBytes=record .value();;
:byte[]valueByteArray=valueBytes==null ? null :Utils.toArray(valueBytes);;
:Vvalue=valueBytes==null ? null :this.valueDeserializer.deserialize(partition.topic(),
headers,valueByteArray);;
:return new ConsumerRecord<>(partition.topic(),
partition.partition(),offset,timestamp,timestampType,
keyByteArray==null ? ConsumerRecord.NULL_SIZE:keyByteArray.length,
valueByteArray==null ? ConsumerRecord.NULL_SIZE:valueByteArray.length,
key,value,headers,leaderEpoch);;
stop
case (catch (RuntimeExceptione))
:throw new RecordDeserializationException(partition,record .offset(),"Error deserializing key/value for partition "+
partition+" at offset "+record .offset()+
". If needed, please seek past the record to continue consumption.",e);;
stop
endswitch
end
start
note right
Optional<Integer>maybeLeaderEpoch(intleaderEpoch)
end note
:return leaderEpoch==RecordBatch.NO_PARTITION_LEADER_EPOCH? Optional.empty():Optional.of(leaderEpoch);;
stop
end
start
note right
void clearBufferedDataForUnassignedPartitions(Collection<TopicPartition>assignedPartitions)
end note
:Iterator<CompletedFetch>completedFetchesItr=completedFetches.iterator();;
while (completedFetchesItr.hasNext())
:CompletedFetchrecords=completedFetchesItr.next();;
:TopicPartitiontp=records.partition;;
if (!assignedPartitions.contains(tp)) then (yes) 
:records.drain();;
:completedFetchesItr.remove();;
endif 
endwhile
if (nextInLineFetch!=null &&!assignedPartitions.contains(nextInLineFetch.partition)) then (yes) 
:nextInLineFetch.drain();;
:nextInLineFetch=null ;;
endif 
end
start
note right
void clearBufferedDataForUnassignedTopics(Collection<String>assignedTopics)
end note
:Set<TopicPartition>currentTopicPartitions=new HashSet<>();;
if (assignedTopics.contains(tp.topic())) then (yes) 
:currentTopicPartitions.add(tp);;
endif 
:clearBufferedDataForUnassignedPartitions(currentTopicPartitions);;
end
start
note right
FetchSessionHandlersessionHandler(intnode)
end note
:return sessionHandlers.get(node);;
stop
end
start
note right
SensorthrottleTimeSensor(Metricsmetrics,FetcherMetricsRegistrymetricsRegistry)
end note
:SensorfetchThrottleTimeSensor=metrics.sensor("fetch-throttle-time");;
:fetchThrottleTimeSensor.add(metrics.metricInstance(metricsRegistry.fetchThrottleTimeAvg),
new Avg());;
:fetchThrottleTimeSensor.add(metrics.metricInstance(metricsRegistry.fetchThrottleTimeMax),
new Max());;
:return fetchThrottleTimeSensor;;
stop
end
start
note right
constructor CompletedFetch(TopicPartitionpartition,
FetchResponseData.PartitionDatapartitionData,
FetchResponseMetricAggregatormetricAggregator,
Iterator<?  extends RecordBatch>batches,LongfetchOffset,
shortresponseVersion)
end note
:this.partition=partition;;
:this.partitionData=partitionData;;
:this.metricAggregator=metricAggregator;;
:this.batches=batches;;
:this.nextFetchOffset=fetchOffset;;
:this.responseVersion=responseVersion;;
:this.lastEpoch=Optional.empty();;
:this.abortedProducerIds=new HashSet<>();;
:this.abortedTransactions=abortedTransactions(partitionData);;
end
start
note right
void drain()
end note
if (!isConsumed) then (yes) 
:maybeCloseRecordStream();;
:cachedRecordException=null ;;
:this.isConsumed=true;;
:this.metricAggregator.record (partition,
bytesRead,recordsRead);;
if (bytesRead>0) then (yes) 
:subscriptions.movePartitionToEnd(partition);;
endif 
endif 
end
start
note right
void maybeEnsureValid(RecordBatchbatch)
end note
if (checkCrcs&&currentBatch.magic()>=RecordBatch.MAGIC_VALUE_V2) then (yes) 
switch (try/catch block)
case (try)
:batch.ensureValid();;
case (catch (CorruptRecordExceptione))
:throw new KafkaException("Record batch for partition "+
partition+" at offset "+batch.baseOffset()+
" is invalid, cause: "+e.getMessage());;
stop
endswitch
endif 
end
start
note right
void maybeEnsureValid(Recordrecord )
end note
if (checkCrcs) then (yes) 
switch (try/catch block)
case (try)
:record .ensureValid();;
case (catch (CorruptRecordExceptione))
:throw new KafkaException("Record for partition "+
partition+" at offset "+record .offset()+
" is invalid, cause: "+e.getMessage());;
stop
endswitch
endif 
end
start
note right
void maybeCloseRecordStream()
end note
if (records!=null ) then (yes) 
:records.close();;
:records=null ;;
endif 
end
start
note right
RecordnextFetchedRecord()
end note
while (true)
if (records==null ||!records.hasNext()) then (yes) 
:maybeCloseRecordStream();;
if (!batches.hasNext()) then (yes) 
if (currentBatch!=null ) then (yes) 
:nextFetchOffset=currentBatch.nextOffset();;
endif 
:drain();;
:return null ;;
stop
endif 
:currentBatch=batches.next();;
:lastEpoch=currentBatch.partitionLeaderEpoch()==RecordBatch.NO_PARTITION_LEADER_EPOCH? Optional.empty():Optional.of(currentBatch.partitionLeaderEpoch());;
:maybeEnsureValid(currentBatch);;
if (isolationLevel==IsolationLevel.READ_COMMITTED&&currentBatch.hasProducerId()) then (yes) 
:consumeAbortedTransactionsUpTo(currentBatch.lastOffset());;
:longproducerId=currentBatch.producerId();;
if (containsAbortMarker(currentBatch)) then (yes) 
:abortedProducerIds.remove(producerId);;
else if (isBatchAborted(currentBatch)) then (yes) 
:log.debug("Skipping aborted record batch from partition {} with producerId {} and "+
"offsets {} to {}",partition,producerId,currentBatch.baseOffset(),
currentBatch.lastOffset());;
:nextFetchOffset=currentBatch.nextOffset();;
:continue ;;
endif 
endif 
:records=currentBatch.streamingIterator(decompressionBufferSupplier);;
else (no)
:Recordrecord =records.next();;
if (record .offset()>=nextFetchOffset) then (yes) 
:maybeEnsureValid(record );;
if (!currentBatch.isControlBatch()) then (yes) 
:return record ;;
stop
else (no)
:nextFetchOffset=record .offset()+1;;
endif 
endif 
endif 
endwhile
end
start
note right
List<ConsumerRecord<K,V>>fetchRecords(intmaxRecords)
end note
if (corruptLastRecord) then (yes) 
:throw new KafkaException("Received exception when fetching the next record from "+
partition+". If needed, please seek past the record to "+
"continue consumption.",cachedRecordException);;
stop
endif 
if (isConsumed) then (yes) 
:return Collections.emptyList();;
stop
endif 
:List<ConsumerRecord<K,V>>records=new ArrayList<>();;
switch (try/catch block)
case (try)
if (cachedRecordException==null ) then (yes) 
:corruptLastRecord=true;;
:lastRecord=nextFetchedRecord();;
:corruptLastRecord=false;;
endif 
if (lastRecord==null ) then (yes) 
:break ;;
endif 
:records.add(parseRecord(partition,currentBatch,
lastRecord));;
:recordsRead++;;
:bytesRead+=lastRecord.sizeInBytes();;
:nextFetchOffset=lastRecord.offset()+1;;
:cachedRecordException=null ;;
case (catch (SerializationExceptionse))
:cachedRecordException=se;;
if (records.isEmpty()) then (yes) 
:throw se;;
stop
endif 
case (catch (KafkaExceptione))
:cachedRecordException=e;;
if (records.isEmpty()) then (yes) 
:throw new KafkaException("Received exception when fetching the next record from "+
partition+". If needed, please seek past the record to "+
"continue consumption.",e);;
stop
endif 
endswitch
:return records;;
stop
end
start
note right
void consumeAbortedTransactionsUpTo(longoffset)
end note
if (abortedTransactions==null ) then (yes) 
:return ;;
stop
endif 
while (!abortedTransactions.isEmpty()&&abortedTransactions.peek().firstOffset()<=offset)
:FetchResponseData.AbortedTransactionabortedTransaction=abortedTransactions.poll();;
:abortedProducerIds.add(abortedTransaction.producerId());;
endwhile
end
start
note right
booleanisBatchAborted(RecordBatchbatch)
end note
:return batch.isTransactional()&&abortedProducerIds.contains(batch.producerId());;
stop
end
start
note right
PriorityQueue<FetchResponseData.AbortedTransaction>abortedTransactions(FetchResponseData.PartitionDatapartition)
end note
if (partition.abortedTransactions()==null ||partition.abortedTransactions().isEmpty()) then (yes) 
:return null ;;
stop
endif 
:PriorityQueue<FetchResponseData.AbortedTransaction>abortedTransactions=new PriorityQueue<>(partition.abortedTransactions().size(),
Comparator.comparingLong(FetchResponseData.AbortedTransaction::firstOffset));;
:abortedTransactions.addAll(partition.abortedTransactions());;
:return abortedTransactions;;
stop
end
start
note right
booleancontainsAbortMarker(RecordBatchbatch)
end note
if (!batch.isControlBatch()) then (yes) 
:return false;;
stop
endif 
:Iterator<Record>batchIterator=batch.iterator();;
if (!batchIterator.hasNext()) then (yes) 
:return false;;
stop
endif 
:RecordfirstRecord=batchIterator.next();;
:return ControlRecordType.ABORT==ControlRecordType.parse(firstRecord.key());;
stop
end
start
note right
booleannotInitialized()
end note
:return !this.initialized;;
stop
end
start
note right
constructor FetchResponseMetricAggregator(FetchManagerMetricssensors,
Set<TopicPartition>partitions)
end note
:this.sensors=sensors;;
:this.unrecordedPartitions=partitions;;
end
start
note right
void record (TopicPartitionpartition,intbytes,
intrecords)
end note
:this.unrecordedPartitions.remove(partition);;
:this.fetchMetrics.increment(bytes,records);;
:Stringtopic=partition.topic();;
:FetchMetricstopicFetchMetric=this.topicFetchMetrics.get(topic);;
if (topicFetchMetric==null ) then (yes) 
:topicFetchMetric=new FetchMetrics();;
:this.topicFetchMetrics.put(topic,topicFetchMetric);;
endif 
:topicFetchMetric.increment(bytes,records);;
if (this.unrecordedPartitions.isEmpty()) then (yes) 
:this.sensors.bytesFetched.record (this.fetchMetrics.fetchBytes);;
:this.sensors.recordsFetched.record (this.fetchMetrics.fetchRecords);;
:FetchMetricsmetric=entry.getValue();;
:this.sensors.recordTopicFetchMetrics(entry.getKey(),
metric.fetchBytes,metric.fetchRecords);;
endif 
end
start
note right
void increment(intbytes,intrecords)
end note
:this.fetchBytes+=bytes;;
:this.fetchRecords+=records;;
end
start
note right
constructor FetchManagerMetrics(Metricsmetrics,
FetcherMetricsRegistrymetricsRegistry)
end note
:this.metrics=metrics;;
:this.metricsRegistry=metricsRegistry;;
:this.bytesFetched=metrics.sensor("bytes-fetched");;
:this.bytesFetched.add(metrics.metricInstance(metricsRegistry.fetchSizeAvg),
new Avg());;
:this.bytesFetched.add(metrics.metricInstance(metricsRegistry.fetchSizeMax),
new Max());;
:this.bytesFetched.add(new Meter(metrics.metricInstance(metricsRegistry.bytesConsumedRate),
metrics.metricInstance(metricsRegistry.bytesConsumedTotal)));;
:this.recordsFetched=metrics.sensor("records-fetched");;
:this.recordsFetched.add(metrics.metricInstance(metricsRegistry.recordsPerRequestAvg),
new Avg());;
:this.recordsFetched.add(new Meter(metrics.metricInstance(metricsRegistry.recordsConsumedRate),
metrics.metricInstance(metricsRegistry.recordsConsumedTotal)));;
:this.fetchLatency=metrics.sensor("fetch-latency");;
:this.fetchLatency.add(metrics.metricInstance(metricsRegistry.fetchLatencyAvg),
new Avg());;
:this.fetchLatency.add(metrics.metricInstance(metricsRegistry.fetchLatencyMax),
new Max());;
:this.fetchLatency.add(new Meter(new WindowedCount(),
metrics.metricInstance(metricsRegistry.fetchRequestRate),
metrics.metricInstance(metricsRegistry.fetchRequestTotal)));;
:this.recordsFetchLag=metrics.sensor("records-lag");;
:this.recordsFetchLag.add(metrics.metricInstance(metricsRegistry.recordsLagMax),
new Max());;
:this.recordsFetchLead=metrics.sensor("records-lead");;
:this.recordsFetchLead.add(metrics.metricInstance(metricsRegistry.recordsLeadMin),
new Min());;
end
start
note right
void recordTopicFetchMetrics(Stringtopic,
intbytes,intrecords)
end note
:Stringname="topic."+topic+".bytes-fetched";;
:SensorbytesFetched=this.metrics.getSensor(name);;
if (bytesFetched==null ) then (yes) 
:Map<String,String>metricTags=Collections.singletonMap("topic",
topic.replace('.','_'));;
:bytesFetched=this.metrics.sensor(name);;
:bytesFetched.add(this.metrics.metricInstance(metricsRegistry.topicFetchSizeAvg,
metricTags),new Avg());;
:bytesFetched.add(this.metrics.metricInstance(metricsRegistry.topicFetchSizeMax,
metricTags),new Max());;
:bytesFetched.add(new Meter(this.metrics.metricInstance(metricsRegistry.topicBytesConsumedRate,
metricTags),this.metrics.metricInstance(metricsRegistry.topicBytesConsumedTotal,
metricTags)));;
endif 
:bytesFetched.record (bytes);;
:name="topic."+topic+".records-fetched";;
:SensorrecordsFetched=this.metrics.getSensor(name);;
if (recordsFetched==null ) then (yes) 
:Map<String,String>metricTags=new HashMap<>(1);;
:metricTags.put("topic",topic.replace('.',
:recordsFetched=this.metrics.sensor(name);;
:recordsFetched.add(this.metrics.metricInstance(metricsRegistry.topicRecordsPerRequestAvg,
metricTags),new Avg());;
:recordsFetched.add(new Meter(this.metrics.metricInstance(metricsRegistry.topicRecordsConsumedRate,
metricTags),this.metrics.metricInstance(metricsRegistry.topicRecordsConsumedTotal,
metricTags)));;
endif 
:recordsFetched.record (records);;
end
start
note right
void maybeUpdateAssignment(SubscriptionStatesubscription)
end note
:intnewAssignmentId=subscription.assignmentId();;
if (this.assignmentId!=newAssignmentId) then (yes) 
:Set<TopicPartition>newAssignedPartitions=subscription.assignedPartitions();;
if (!newAssignedPartitions.contains(tp)) then (yes) 
:metrics.removeSensor(partitionLagMetricName(tp));;
:metrics.removeSensor(partitionLeadMetricName(tp));;
:metrics.removeMetric(partitionPreferredReadReplicaMetricName(tp));;
endif 
if (!this.assignedPartitions.contains(tp)) then (yes) 
:MetricNamemetricName=partitionPreferredReadReplicaMetricName(tp);;
:metrics.addMetricIfAbsent(metricName,null ,
(Gauge<Integer>)(config,now)->subscription.preferredReadReplica(tp,
0L).orElse(-1));;
endif 
:this.assignedPartitions=newAssignedPartitions;;
:this.assignmentId=newAssignmentId;;
endif 
end
start
note right
void recordPartitionLead(TopicPartitiontp,
longlead)
end note
:this.recordsFetchLead.record (lead);;
:Stringname=partitionLeadMetricName(tp);;
:SensorrecordsLead=this.metrics.getSensor(name);;
if (recordsLead==null ) then (yes) 
:Map<String,String>metricTags=topicPartitionTags(tp);;
:recordsLead=this.metrics.sensor(name);;
:recordsLead.add(this.metrics.metricInstance(metricsRegistry.partitionRecordsLead,
metricTags),new Value());;
:recordsLead.add(this.metrics.metricInstance(metricsRegistry.partitionRecordsLeadMin,
metricTags),new Min());;
:recordsLead.add(this.metrics.metricInstance(metricsRegistry.partitionRecordsLeadAvg,
metricTags),new Avg());;
endif 
:recordsLead.record (lead);;
end
start
note right
void recordPartitionLag(TopicPartitiontp,
longlag)
end note
:this.recordsFetchLag.record (lag);;
:Stringname=partitionLagMetricName(tp);;
:SensorrecordsLag=this.metrics.getSensor(name);;
if (recordsLag==null ) then (yes) 
:Map<String,String>metricTags=topicPartitionTags(tp);;
:recordsLag=this.metrics.sensor(name);;
:recordsLag.add(this.metrics.metricInstance(metricsRegistry.partitionRecordsLag,
metricTags),new Value());;
:recordsLag.add(this.metrics.metricInstance(metricsRegistry.partitionRecordsLagMax,
metricTags),new Max());;
:recordsLag.add(this.metrics.metricInstance(metricsRegistry.partitionRecordsLagAvg,
metricTags),new Avg());;
endif 
:recordsLag.record (lag);;
end
start
note right
StringpartitionLagMetricName(TopicPartitiontp)
end note
:return tp+".records-lag";;
stop
end
start
note right
StringpartitionLeadMetricName(TopicPartitiontp)
end note
:return tp+".records-lead";;
stop
end
start
note right
MetricNamepartitionPreferredReadReplicaMetricName(TopicPartitiontp)
end note
:Map<String,String>metricTags=topicPartitionTags(tp);;
:return this.metrics.metricInstance(metricsRegistry.partitionPreferredReadReplica,
metricTags);;
stop
end
start
note right
Map<String,String>topicPartitionTags(TopicPartitiontp)
end note
:Map<String,String>metricTags=new HashMap<>(2);;
:metricTags.put("topic",tp.topic().replace('.',
:metricTags.put("partition",String.valueOf(tp.partition()));;
:return metricTags;;
stop
end
start
note right
void close()
end note
if (nextInLineFetch!=null ) then (yes) 
:nextInLineFetch.drain();;
endif 
:decompressionBufferSupplier.close();;
end
start
note right
Set<String>topicsForPartitions(Collection<TopicPartition>partitions)
end note
:return partitions.stream().map(TopicPartition::topic)
.collect(Collectors.toSet());;
stop
end
@enduml

PlantUML version 1.2022.7(Mon Aug 22 17:01:30 UTC 2022)
(GPL source distribution)
Java Runtime: OpenJDK Runtime Environment
JVM: OpenJDK 64-Bit Server VM
Default Encoding: UTF-8
Language: en
Country: null
--></g></svg>