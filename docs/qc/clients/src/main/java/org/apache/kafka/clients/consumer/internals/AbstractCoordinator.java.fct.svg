<?xml version="1.0" encoding="UTF-8" standalone="no"?><svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" contentStyleType="text/css" height="544px" preserveAspectRatio="none" style="width:635px;height:544px;background:#000000;" version="1.1" viewBox="0 0 635 544" width="635px" zoomAndPan="magnify"><defs/><g><rect fill="#102012" height="1" style="stroke:#102012;stroke-width:1.0;" width="1" x="0" y="0"/><text fill="#33FF02" font-family="sans-serif" font-size="12" font-style="italic" font-weight="bold" lengthAdjust="spacing" textLength="127" x="5" y="17">PlantUML 1.2022.7</text><rect fill="#33FF02" height="21.2969" style="stroke:#33FF02;stroke-width:1.0;" width="623" x="5" y="26.9688"/><text fill="#000000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="188" x="6" y="41.9688">[From string (line 268) ]</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="5" x="5" y="62.2656"> </text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="79" x="5" y="78.5625">@startuml</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="190" x="5" y="94.8594">!pragma layout smetana</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="111.1563">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="127.4531">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="227" x="5" y="143.75">booleanhasNotJoinedGroup()</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="15" x="5" y="160.0469">...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="185" x="5" y="176.3438">... ( skipping 243 lines )</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="15" x="5" y="192.6406">...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="436" x="5" y="208.9375">:log.debug("Sending JoinGroup ({}) to coordinator {}",</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="260" x="5" y="225.2344">requestBuilder,this.coordinator);</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="373" x="5" y="241.5313">:return client.send(coordinator,requestBuilder,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="623" x="5" y="257.8281">joinGroupTimeoutMs).compose(new JoinGroupResponseHandler(generation));;</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="35" x="5" y="274.125">stop</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="30" x="5" y="290.4219">end</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="306.7188">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="323.0156">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="538" x="5" y="339.3125">constructor JoinGroupResponseHandler(final Generationgeneration)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="71" x="5" y="355.6094">end note</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="161" x="5" y="371.9063">:super (generation);</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="30" x="5" y="388.2031">end</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="404.5">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="420.7969">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="363" x="5" y="437.0938">void handle(JoinGroupResponsejoinResponse,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="280" x="5" y="453.3906">RequestFuture&lt;ByteBuffer&gt;future)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="71" x="5" y="469.6875">end note</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="275" x="5" y="485.9844">if (error==Errors.NONE) then (yes)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="399" x="5" y="502.2813">if (isProtocolTypeInconsistent(joinResponse.data()</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" text-decoration="wavy underline" textLength="220" x="5" y="518.5781">.protocolType())) then (yes)</text><text fill="#FF0000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="106" x="10" y="534.875">Syntax Error?</text><!--MD5=[0cf2df9ccece131c2cc4083ba75224d8]
@startuml
!pragma layout smetana
start
note right
booleanhasNotJoinedGroup()
end note
:return equals(UNJOINED)||equals(PREPARING_REBALANCE);;
stop
end
start
note right
constructor AbstractCoordinator(GroupRebalanceConfigrebalanceConfig,
LogContextlogContext,ConsumerNetworkClientclient,
Metricsmetrics,StringmetricGrpPrefix,Timetime)
end note
:Objects.requireNonNull(rebalanceConfig.groupId,
"Expected a non-null group id for coordinator construction");
:this.rebalanceConfig=rebalanceConfig;
:this.log=logContext.logger(this.getClass());
:this.client=client;
:this.time=time;
:this.heartbeat=new Heartbeat(rebalanceConfig,
time);
:this.sensors=new GroupCoordinatorMetrics(metrics,
metricGrpPrefix);
end
start
note right
void onJoinComplete(intgeneration,StringmemberId,
Stringprotocol,ByteBuffermemberAssignment)
end note
end
start
note right
void onLeavePrepare()
end note
end
start
note right
booleanensureCoordinatorReady(final Timertimer)
end note
if (!coordinatorUnknown()) then (yes) 
:return true;;
stop
endif 
repeat
if (fatalFindCoordinatorException!=null ) then (yes) 
:fatalFindCoordinatorException=null ;
:throw fatalException;;
endif 
:client.poll(future,timer);
if (!future.isDone()) then (yes) 
endif 
if (future.failed()) then (yes) 
if (future.isRetriable()) then (yes) 
:log.debug("Coordinator discovery failed,
 refreshing metadata",future.exception());
:client.awaitMetadataUpdate(timer);
else (no)
:fatalException=future.exception();
:log.info("FindCoordinator request hit fatal exception",
fatalException);
endif 
endif 
:clearFindCoordinatorFuture();
if (fatalException!=null ) then (yes) 
:throw fatalException;;
endif 
repeat while (coordinatorUnknown()&&timer.notExpired())
:return !coordinatorUnknown();;
stop
end
start
note right
RequestFuture<Void>lookupCoordinator()
end note
if (findCoordinatorFuture==null ) then (yes) 
if (node==null ) then (yes) 
:log.debug("No broker available to send FindCoordinator request");
:return RequestFuture.noBrokersAvailable();;
stop
else (no)
:findCoordinatorFuture=sendFindCoordinatorRequest(node);
endif 
endif 
:return findCoordinatorFuture;;
stop
end
start
note right
void clearFindCoordinatorFuture()
end note
:findCoordinatorFuture=null ;
end
start
note right
booleanrejoinNeededOrPending()
end note
:return rejoinNeeded||joinFuture!=null ;;
stop
end
start
note right
void pollHeartbeat(longnow)
end note
if (heartbeatThread!=null ) then (yes) 
if (heartbeatThread.hasFailed()) then (yes) 
:heartbeatThread=null ;
:throw cause;;
endif 
if (heartbeat.shouldHeartbeat(now)) then (yes) 
:notify();
endif 
:heartbeat.poll(now);
endif 
end
start
note right
longtimeToNextHeartbeat(longnow)
end note
if (state.hasNotJoinedGroup()) then (yes) 
:return Long.MAX_VALUE;;
stop
endif 
:return heartbeat.timeToNextHeartbeat(now);;
stop
end
start
note right
void ensureActiveGroup()
end note
while (!ensureActiveGroup(time.timer(Long.MAX_VALUE)))
:log.warn("still waiting to ensure active group");
endwhile
end
start
note right
booleanensureActiveGroup(final Timertimer)
end note
if (!ensureCoordinatorReady(timer)) then (yes) 
:return false;;
stop
endif 
:startHeartbeatThreadIfNeeded();
:return joinGroupIfNeeded(timer);;
stop
end
start
note right
void startHeartbeatThreadIfNeeded()
end note
if (heartbeatThread==null ) then (yes) 
:heartbeatThread=new HeartbeatThread();
:heartbeatThread.start();
endif 
end
start
note right
void closeHeartbeatThread()
end note
if (heartbeatThread==null ) then (yes) 
:return ;;
stop
endif 
:heartbeatThread.close();
:thread=heartbeatThread;
:heartbeatThread=null ;
:thread.join();
:log.warn("Interrupted while waiting for consumer heartbeat thread to close");
:throw new InterruptException(e);;
end
start
note right
booleanjoinGroupIfNeeded(final Timertimer)
end note
while (rejoinNeededOrPending())
if (!ensureCoordinatorReady(timer)) then (yes) 
:return false;;
stop
endif 
if (needsJoinPrepare) then (yes) 
:needsJoinPrepare=false;
if (!onJoinPrepare(generation.generationId,
generation.memberId)) then (yes) 
:needsJoinPrepare=true;
:return false;;
stop
endif 
endif 
:client.poll(future,timer);
if (!future.isDone()) then (yes) 
:return false;;
stop
endif 
if (future.succeeded()) then (yes) 
:generationSnapshot=this.generation;
:stateSnapshot=this.state;
if (!hasGenerationReset(generationSnapshot)&&stateSnapshot==MemberState.STABLE) then (yes) 
:onJoinComplete(generationSnapshot.generationId,
generationSnapshot.memberId,generationSnapshot.protocolName,
memberAssignment);
:resetJoinGroupFuture();
:needsJoinPrepare=true;
else (no)
:resetStateAndRejoin(reason,true);
:resetJoinGroupFuture();
endif 
else (no)
:resetJoinGroupFuture();
:requestRejoin(shortReason,fullReason);
if (exception instanceof UnknownMemberIdException||exception instanceof IllegalGenerationException||exception instanceof RebalanceInProgressException||exception instanceof MemberIdRequiredException) then (yes) 
endif 
:timer.sleep(rebalanceConfig.retryBackoffMs);
endif 
endwhile
:return true;;
stop
end
start
note right
void resetJoinGroupFuture()
end note
:this.joinFuture=null ;
end
start
note right
RequestFuture<ByteBuffer>initiateJoinGroup()
end note
if (joinFuture==null ) then (yes) 
:state=MemberState.PREPARING_REBALANCE;
if (lastRebalanceStartMs==-1L) then (yes) 
:lastRebalanceStartMs=time.milliseconds();
endif 
:joinFuture=sendJoinGroupRequest();
:joinFuture.addListener(new RequestFutureListener<ByteBuffer>(){@Override public void onSuccess(ByteBuffervalue){}@Override public void onFailure(RuntimeExceptione){synchronized (AbstractCoordinator.this){sensors.failedRebalanceSensor.record();}}});
endif 
:return joinFuture;;
stop
end
start
note right
RequestFuture<ByteBuffer>sendJoinGroupRequest()
end note
if (coordinatorUnknown()) then (yes) 
:return RequestFuture.coordinatorNotAvailable();;
stop
endif 
:log.info("(Re-)joining group");
:log.debug("Sending JoinGroup ({}) to coordinator {}",
requestBuilder,this.coordinator);
:return client.send(coordinator,requestBuilder,
joinGroupTimeoutMs).compose(new JoinGroupResponseHandler(generation));;
stop
end
start
note right
constructor JoinGroupResponseHandler(final Generationgeneration)
end note
:super (generation);
end
start
note right
void handle(JoinGroupResponsejoinResponse,
RequestFuture<ByteBuffer>future)
end note
if (error==Errors.NONE) then (yes) 
if (isProtocolTypeInconsistent(joinResponse.data()
.protocolType())) then (yes) 
:log.error("JoinGroup failed: Inconsistent Protocol Type,
 received {} but expected {}",joinResponse.data().protocolType(),
protocolType());
:future.raise(Errors.INCONSISTENT_GROUP_PROTOCOL);
else (no)
:log.debug("Received successful JoinGroup response: {}",
joinResponse);
:sensors.joinSensor.record(response.requestLatencyMs());
if (state!=MemberState.PREPARING_REBALANCE) then (yes) 
:future.raise(new UnjoinedGroupException());
else (no)
:state=MemberState.COMPLETING_REBALANCE;
if (heartbeatThread!=null ) then (yes) 
:heartbeatThread.enable();
endif 
:AbstractCoordinator.this.generation=new Generation(joinResponse.data().generationId(),
joinResponse.data().memberId(),joinResponse.data()
.protocolName());
:log.info("Successfully joined group with generation {}",
AbstractCoordinator.this.generation);
if (joinResponse.isLeader()) then (yes) 
:onLeaderElected(joinResponse).chain(future);
else (no)
:onJoinFollower().chain(future);
endif 
endif 
endif 
else (no)
:log.error("JoinGroup failed due to unexpected error: {}",
error.message());
:future.raise(new KafkaException("Unexpected error in join group response: "+
error.message()));
endif 
end
start
note right
RequestFuture<ByteBuffer>onJoinFollower()
end note
:log.debug("Sending follower SyncGroup to coordinator {}: {}",
this.coordinator,requestBuilder);
:return sendSyncGroupRequest(requestBuilder);;
stop
end
start
note right
RequestFuture<ByteBuffer>onLeaderElected(JoinGroupResponsejoinResponse)
end note
:groupAssignmentList.add(new SyncGroupRequestData.SyncGroupRequestAssignment()
.setMemberId(assignment.getKey()).setAssignment(Utils.toArray(assignment.getValue())));
:log.debug("Sending leader SyncGroup to coordinator {}: {}",
this.coordinator,requestBuilder);
:return sendSyncGroupRequest(requestBuilder);;
stop
:return RequestFuture.failure(e);;
stop
end
start
note right
RequestFuture<ByteBuffer>sendSyncGroupRequest(SyncGroupRequest.BuilderrequestBuilder)
end note
if (coordinatorUnknown()) then (yes) 
:return RequestFuture.coordinatorNotAvailable();;
stop
endif 
:return client.send(coordinator,requestBuilder)
.compose(new SyncGroupResponseHandler(generation));;
stop
end
start
note right
booleanhasGenerationReset(Generationgen)
end note
:return gen.generationId==Generation.NO_GENERATION.generationId&&gen.protocolName==null ;;
stop
end
start
note right
constructor SyncGroupResponseHandler(final Generationgeneration)
end note
:super (generation);
end
start
note right
void handle(SyncGroupResponsesyncResponse,
RequestFuture<ByteBuffer>future)
end note
if (error==Errors.NONE) then (yes) 
if (isProtocolTypeInconsistent(syncResponse.data()
.protocolType())) then (yes) 
:log.error("SyncGroup failed due to inconsistent Protocol Type,
 received {} but expected {}",syncResponse.data().protocolType(),
protocolType());
:future.raise(Errors.INCONSISTENT_GROUP_PROTOCOL);
else (no)
:log.debug("Received successful SyncGroup response: {}",
syncResponse);
:sensors.syncSensor.record(response.requestLatencyMs());
if (!hasGenerationReset(generation)&&state==MemberState.COMPLETING_REBALANCE) then (yes) 
if (protocolNameInconsistent) then (yes) 
:log.error("SyncGroup failed due to inconsistent Protocol Name,
 received {} but expected {}",protocolName,
generation.protocolName);
:future.raise(Errors.INCONSISTENT_GROUP_PROTOCOL);
else (no)
:log.info("Successfully synced group in generation {}",
generation);
:state=MemberState.STABLE;
:rejoinReason="";
:rejoinNeeded=false;
:lastRebalanceEndMs=time.milliseconds();
:sensors.successfulRebalanceSensor.record(lastRebalanceEndMs-lastRebalanceStartMs);
:lastRebalanceStartMs=-1L;
:future.complete(ByteBuffer.wrap(syncResponse.data()
.assignment()));
endif 
else (no)
:log.info("Generation data was cleared by heartbeat thread to {} and state is now {} before "+
"receiving SyncGroup response, marking this rebalance as failed and retry",
generation,state);
:future.raise(Errors.ILLEGAL_GENERATION);
endif 
endif 
else (no)
if (error==Errors.GROUP_AUTHORIZATION_FAILED) then (yes) 
:future.raise(GroupAuthorizationException.forGroupId(rebalanceConfig.groupId));
else (no)
:future.raise(new KafkaException("Unexpected error from SyncGroup: "+
error.message()));
endif 
endif 
end
start
note right
RequestFuture<Void>sendFindCoordinatorRequest(Nodenode)
end note
:log.debug("Sending FindCoordinator request to broker {}",
node);
:return client.send(node,requestBuilder).compose(new FindCoordinatorResponseHandler());;
stop
end
start
note right
void onSuccess(ClientResponseresp,RequestFuture<Void>future)
end note
:log.debug("Received FindCoordinator response {}",
resp);
if (coordinators.size()!=1) then (yes) 
:log.error("Group coordinator lookup failed: Invalid response containing more than a single coordinator");
:future.raise(new IllegalStateException("Group coordinator lookup failed: Invalid response containing more than a single coordinator"));
endif 
if (error==Errors.NONE) then (yes) 
:AbstractCoordinator.this.coordinator=new Node(coordinatorConnectionId,
coordinatorData.host(),coordinatorData.port());
:log.info("Discovered group coordinator {}",
coordinator);
:client.tryConnect(coordinator);
:heartbeat.resetSessionTimeout();
:future.complete(null );
else (no)
:log.debug("Group coordinator lookup failed: {}",
coordinatorData.errorMessage());
:future.raise(error);
endif 
end
start
note right
void onFailure(RuntimeExceptione,RequestFuture<Void>future)
end note
:log.debug("FindCoordinator request failed due to {}",
e.toString());
if (!(e instanceof RetriableException)) then (yes) 
:fatalFindCoordinatorException=e;
endif 
:super .onFailure(e,future);
end
start
note right
booleancoordinatorUnknown()
end note
:return checkAndGetCoordinator()==null ;;
stop
end
start
note right
NodecheckAndGetCoordinator()
end note
if (coordinator!=null &&client.isUnavailable(coordinator)) then (yes) 
:markCoordinatorUnknown(true,"coordinator unavailable");
:return null ;;
stop
endif 
:return this.coordinator;;
stop
end
start
note right
Nodecoordinator()
end note
:return this.coordinator;;
stop
end
start
note right
void markCoordinatorUnknown(Errorserror)
end note
:markCoordinatorUnknown(false,"error response "+
error.name());
end
start
note right
void markCoordinatorUnknown(Stringcause)
end note
:markCoordinatorUnknown(false,cause);
end
start
note right
void markCoordinatorUnknown(booleanisDisconnected,
Stringcause)
end note
if (this.coordinator!=null ) then (yes) 
:log.info("Group coordinator {} is unavailable or invalid due to cause: {}. "+
"isDisconnected: {}. Rediscovery will be attempted.",
this.coordinator,cause,isDisconnected);
:this.coordinator=null ;
if (!isDisconnected) then (yes) 
:log.info("Requesting disconnect from last known coordinator {}",
oldCoordinator);
:client.disconnectAsync(oldCoordinator);
endif 
:lastTimeOfConnectionMs=time.milliseconds();
else (no)
if (durationOfOngoingDisconnect>rebalanceConfig.rebalanceTimeoutMs) then (yes) 
:log.warn("Consumer has been disconnected from the group coordinator for {}ms",
durationOfOngoingDisconnect);
endif 
endif 
end
start
note right
Generationgeneration()
end note
:return generation;;
stop
end
start
note right
GenerationgenerationIfStable()
end note
if (this.state!=MemberState.STABLE) then (yes) 
:return null ;;
stop
endif 
:return generation;;
stop
end
start
note right
booleanrebalanceInProgress()
end note
:return this.state==MemberState.PREPARING_REBALANCE||this.state==MemberState.COMPLETING_REBALANCE;;
stop
end
start
note right
StringmemberId()
end note
:return generation.memberId;;
stop
end
start
note right
void resetStateAndGeneration(final Stringreason,
final booleanshouldResetMemberId)
end note
:log.info("Resetting generation {}due to: {}",
shouldResetMemberId? String.valueOf("and member id "):String.valueOf(""),
reason);
:state=MemberState.UNJOINED;
if (shouldResetMemberId) then (yes) 
:generation=Generation.NO_GENERATION;
else (no)
:generation=new Generation(Generation.NO_GENERATION.generationId,
generation.memberId,null );
endif 
end
start
note right
void resetStateAndRejoin(final Stringreason,
final booleanshouldResetMemberId)
end note
:resetStateAndGeneration(reason,shouldResetMemberId);
:requestRejoin(reason);
:needsJoinPrepare=true;
end
start
note right
void resetStateOnResponseError(ApiKeysapi,
Errorserror,booleanshouldResetMemberId)
end note
:resetStateAndRejoin(reason,shouldResetMemberId);
end
start
note right
void resetGenerationOnLeaveGroup()
end note
:resetStateAndRejoin("consumer pro-actively leaving the group",
true);
end
start
note right
void requestRejoinIfNecessary(final StringshortReason,
final StringfullReason)
end note
if (!this.rejoinNeeded) then (yes) 
:requestRejoin(shortReason,fullReason);
endif 
end
start
note right
void requestRejoin(final StringshortReason)
end note
:requestRejoin(shortReason,shortReason);
end
start
note right
void requestRejoin(final StringshortReason,
final StringfullReason)
end note
:log.info("Request joining group due to: {}",
fullReason);
:this.rejoinReason=shortReason;
:this.rejoinNeeded=true;
end
start
note right
booleanisProtocolTypeInconsistent(StringprotocolType)
end note
:return protocolType!=null &&!protocolType.equals(protocolType());;
stop
end
start
note right
void close()
end note
:close(time.timer(0));
end
start
note right
void close(Timertimer)
end note
:closeHeartbeatThread();
if (rebalanceConfig.leaveGroupOnClose) then (yes) 
:onLeavePrepare();
:maybeLeaveGroup("the consumer is being closed");
endif 
if (coordinator!=null &&!client.awaitPendingRequests(coordinator,
timer)) then (yes) 
:log.warn("Close timed out with {} pending requests to coordinator,
 terminating client connections",client.pendingRequestCount(coordinator));
endif 
end
start
note right
RequestFuture<Void>maybeLeaveGroup(StringleaveReason)
end note
if (isDynamicMember()&&!coordinatorUnknown()&&state!=MemberState.UNJOINED&&generation.hasMemberId()) then (yes) 
:log.info("Member {} sending LeaveGroup request to coordinator {} due to {}",
generation.memberId,coordinator,leaveReason);
:future=client.send(coordinator,request).compose(new LeaveGroupResponseHandler(generation));
:client.pollNoWakeup();
endif 
:resetGenerationOnLeaveGroup();
:return future;;
stop
end
start
note right
booleanisDynamicMember()
end note
:return !rebalanceConfig.groupInstanceId.isPresent();;
stop
end
start
note right
constructor LeaveGroupResponseHandler(final Generationgeneration)
end note
:super (generation);
end
start
note right
void handle(LeaveGroupResponseleaveResponse,
RequestFuture<Void>future)
end note
if (members.size()>1) then (yes) 
:future.raise(new IllegalStateException("The expected leave group response "+
"should only contain no more than one member info, however get "+
members));
endif 
if (error==Errors.NONE) then (yes) 
:log.debug("LeaveGroup response with {} returned successfully: {}",
sentGeneration,response);
:future.complete(null );
else (no)
:log.error("LeaveGroup request with {} failed with error: {}",
sentGeneration,error.message());
:future.raise(error);
endif 
end
start
note right
RequestFuture<Void>sendHeartbeatRequest()
end note
:log.debug("Sending Heartbeat request with generation {} and member id {} to coordinator {}",
generation.generationId,generation.memberId,
coordinator);
:return client.send(coordinator,requestBuilder)
.compose(new HeartbeatResponseHandler(generation));;
stop
end
start
note right
constructor HeartbeatResponseHandler(final Generationgeneration)
end note
:super (generation);
end
start
note right
void handle(HeartbeatResponseheartbeatResponse,
RequestFuture<Void>future)
end note
:sensors.heartbeatSensor.record(response.requestLatencyMs());
if (error==Errors.NONE) then (yes) 
:log.debug("Received successful Heartbeat response");
:future.complete(null );
else (no)
:future.raise(new KafkaException("Unexpected error in heartbeat response: "+
error.message()));
endif 
end
start
note right
constructor CoordinatorResponseHandler(final Generationgeneration)
end note
:this.sentGeneration=generation;
end
start
note right
void handle(Rresponse,RequestFuture<T>future)
end note
end
start
note right
void onFailure(RuntimeExceptione,RequestFuture<T>future)
end note
if (e instanceof DisconnectException) then (yes) 
:markCoordinatorUnknown(true,e.getMessage());
endif 
:future.raise(e);
end
start
note right
void onSuccess(ClientResponseclientResponse,
RequestFuture<T>future)
end note
:this.response=clientResponse;
:handle(responseObj,future);
if (!future.isDone()) then (yes) 
:future.raise(e);
endif 
end
start
note right
booleangenerationUnchanged()
end note
:return generation.equals(sentGeneration);;
stop
end
start
note right
MetercreateMeter(Metricsmetrics,StringgroupName,
StringbaseName,StringdescriptiveName)
end note
:return new Meter(new WindowedCount(),metrics.metricName(baseName+
"-rate",groupName,String.format("The number of %s per second",descriptiveName)),metrics.metricName(baseName+
"-total",groupName,String.format("The total number of %s",
descriptiveName)));;
stop
end
start
note right
constructor GroupCoordinatorMetrics(Metricsmetrics,
StringmetricGrpPrefix)
end note
:this.metricGrpName=metricGrpPrefix+"-coordinator-metrics";
:this.heartbeatSensor=metrics.sensor("heartbeat-latency");
:this.heartbeatSensor.add(metrics.metricName("heartbeat-response-time-max",
this.metricGrpName,"The max time taken to receive a response to a heartbeat request"),
new Max());
:this.heartbeatSensor.add(createMeter(metrics,
metricGrpName,"heartbeat","heartbeats"));
:this.joinSensor=metrics.sensor("join-latency");
:this.joinSensor.add(metrics.metricName("join-time-avg",
this.metricGrpName,"The average time taken for a group rejoin"),
new Avg());
:this.joinSensor.add(metrics.metricName("join-time-max",
this.metricGrpName,"The max time taken for a group rejoin"),
new Max());
:this.joinSensor.add(createMeter(metrics,
metricGrpName,"join","group joins"));
:this.syncSensor=metrics.sensor("sync-latency");
:this.syncSensor.add(metrics.metricName("sync-time-avg",
this.metricGrpName,"The average time taken for a group sync"),
new Avg());
:this.syncSensor.add(metrics.metricName("sync-time-max",
this.metricGrpName,"The max time taken for a group sync"),
new Max());
:this.syncSensor.add(createMeter(metrics,
metricGrpName,"sync","group syncs"));
:this.successfulRebalanceSensor=metrics.sensor("rebalance-latency");
:this.successfulRebalanceSensor.add(metrics.metricName("rebalance-latency-avg",
this.metricGrpName,"The average time taken for a group to complete a successful rebalance, which may be composed of "+
"several failed re-trials until it succeeded"),
new Avg());
:this.successfulRebalanceSensor.add(metrics.metricName("rebalance-latency-max",
this.metricGrpName,"The max time taken for a group to complete a successful rebalance, which may be composed of "+
"several failed re-trials until it succeeded"),
new Max());
:this.successfulRebalanceSensor.add(metrics.metricName("rebalance-latency-total",
this.metricGrpName,"The total number of milliseconds this consumer has spent in successful rebalances since creation"),
new CumulativeSum());
:this.successfulRebalanceSensor.add(metrics.metricName("rebalance-total",
this.metricGrpName,"The total number of successful rebalance events, each event is composed of "+
"several failed re-trials until it succeeded"),
new CumulativeCount());
:this.successfulRebalanceSensor.add(metrics.metricName("rebalance-rate-per-hour",
this.metricGrpName,"The number of successful rebalance events per hour, each event is composed of "+
"several failed re-trials until it succeeded"),
new Rate(TimeUnit.HOURS,new WindowedCount()));
:this.failedRebalanceSensor=metrics.sensor("failed-rebalance");
:this.failedRebalanceSensor.add(metrics.metricName("failed-rebalance-total",
this.metricGrpName,"The total number of failed rebalance events"),
new CumulativeCount());
:this.failedRebalanceSensor.add(metrics.metricName("failed-rebalance-rate-per-hour",
this.metricGrpName,"The number of failed rebalance events per hour"),
new Rate(TimeUnit.HOURS,new WindowedCount()));
if (lastRebalanceEndMs==-1L) then (yes) 
:return -1d;;
stop
else (no)
:return TimeUnit.SECONDS.convert(now-lastRebalanceEndMs,
TimeUnit.MILLISECONDS);;
stop
endif 
:metrics.addMetric(metrics.metricName("last-rebalance-seconds-ago",
this.metricGrpName,"The number of seconds since the last successful rebalance event"),
lastRebalance);
if (heartbeat.lastHeartbeatSend()==0L) then (yes) 
:return -1d;;
stop
else (no)
:return TimeUnit.SECONDS.convert(now-heartbeat.lastHeartbeatSend(),
TimeUnit.MILLISECONDS);;
stop
endif 
:metrics.addMetric(metrics.metricName("last-heartbeat-seconds-ago",
this.metricGrpName,"The number of seconds since the last coordinator heartbeat was sent"),
lastHeartbeat);
end
start
note right
constructor HeartbeatThread()
end note
:super (HEARTBEAT_THREAD_PREFIX+(rebalanceConfig.groupId.isEmpty()? String.valueOf(""):String.valueOf(" | ")+
rebalanceConfig.groupId),true);
end
start
note right
void enable()
end note
:log.debug("Enabling heartbeat thread");
:this.enabled=true;
:heartbeat.resetTimeouts();
:AbstractCoordinator.this.notify();
end
start
note right
void disable()
end note
:log.debug("Disabling heartbeat thread");
:this.enabled=false;
end
start
note right
void close()
end note
:this.closed=true;
:AbstractCoordinator.this.notify();
end
start
note right
booleanhasFailed()
end note
:return failed.get()!=null ;;
stop
end
start
note right
RuntimeExceptionfailureCause()
end note
:return failed.get();;
stop
end
start
note right
void run()
end note
:log.debug("Heartbeat thread started");
while (true)
if (closed) then (yes) 
:return ;;
stop
endif 
if (!enabled) then (yes) 
:AbstractCoordinator.this.wait();
endif 
if (state.hasNotJoinedGroup()||hasFailed()) then (yes) 
:disable();
endif 
:client.pollNoWakeup();
if (coordinatorUnknown()) then (yes) 
if (findCoordinatorFuture!=null ) then (yes) 
:clearFindCoordinatorFuture();
else (no)
:lookupCoordinator();
endif 
:AbstractCoordinator.this.wait(rebalanceConfig.retryBackoffMs);
else (no)
:heartbeat.sentHeartbeat(now);
:heartbeatFuture.addListener(new RequestFutureListener<Void>(){@Override public void onSuccess(Voidvalue){synchronized (AbstractCoordinator.this){heartbeat.receiveHeartbeat();}}@Override public void onFailure(RuntimeExceptione){synchronized (AbstractCoordinator.this){if (e instanceof RebalanceInProgressException){heartbeat.receiveHeartbeat();}else if (e instanceof FencedInstanceIdException){log.error("Caught fenced group.instance.id {} error in heartbeat thread",rebalanceConfig.groupInstanceId);heartbeatThread.failed.set(e);}else {heartbeat.failHeartbeat();AbstractCoordinator.this.notify();}}}});
endif 
endwhile
:log.error("An authentication error occurred in the heartbeat thread",e);
:this.failed.set(e);
:log.error("A group authorization error occurred in the heartbeat thread",e);
:this.failed.set(e);
:Thread.interrupted();
:log.error("Unexpected interrupt received in heartbeat thread",e);
:this.failed.set(new RuntimeException(e));
:log.error("Heartbeat thread failed due to unexpected error",e);
if (e instanceof RuntimeException) then (yes) 
:this.failed.set((RuntimeException)e);
else (no)
:this.failed.set(new RuntimeException(e));
endif 
:log.debug("Heartbeat thread has closed");
end
start
note right
constructor Generation(intgenerationId,StringmemberId,
StringprotocolName)
end note
:this.generationId=generationId;
:this.memberId=memberId;
:this.protocolName=protocolName;
end
start
note right
booleanhasMemberId()
end note
:return !memberId.isEmpty();;
stop
end
start
note right
booleanequals(final Objecto)
end note
if (this==o) then (yes) 
:return true;;
stop
endif 
if (o==null ||getClass()!=o.getClass()) then (yes) 
:return false;;
stop
endif 
:return generationId==that.generationId&&Objects.equals(memberId,
that.memberId)&&Objects.equals(protocolName,
that.protocolName);;
stop
end
start
note right
inthashCode()
end note
:return Objects.hash(generationId,memberId,
protocolName);;
stop
end
start
note right
StringtoString()
end note
:return "Generation{"+"generationId="+generationId+
", memberId='"+memberId+'\''+", protocol='"+
protocolName+'\''+'}';;
stop
end
start
note right
Heartbeatheartbeat()
end note
:return heartbeat;;
stop
end
start
note right
StringrejoinReason()
end note
:return rejoinReason;;
stop
end
start
note right
void setLastRebalanceTime(final longtimestamp)
end note
:lastRebalanceEndMs=timestamp;
end
start
note right
booleanhasMatchingGenerationId(intgenerationId)
end note
:return !generation.equals(Generation.NO_GENERATION)&&generation.generationId==generationId;;
stop
end
start
note right
booleanhasUnknownGeneration()
end note
:return generation.equals(Generation.NO_GENERATION);;
stop
end
start
note right
booleanhasValidMemberId()
end note
:return !hasUnknownGeneration()&&generation.hasMemberId();;
stop
end
start
note right
void setNewGeneration(final Generationgeneration)
end note
:this.generation=generation;
end
start
note right
void setNewState(final MemberStatestate)
end note
:this.state=state;
end
@enduml

PlantUML version 1.2022.7(Mon Aug 22 17:01:30 UTC 2022)
(GPL source distribution)
Java Runtime: OpenJDK Runtime Environment
JVM: OpenJDK 64-Bit Server VM
Default Encoding: UTF-8
Language: en
Country: null
--></g></svg>