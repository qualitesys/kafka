@startuml
!pragma layout smetana
AbstractRequest <|-- org.apache.kafka.common.requests.FetchRequest
class org.apache.kafka.common.requests.FetchRequest {
+  optionalEpoch(int)
+  FetchRequest(FetchRequestData;short)
+  getErrorResponse(int;Throwable)
+  replicaId()
+  maxWait()
+  minBytes()
+  maxBytes()
+  fetchData(Map<Uuid,String>)
+  forgottenTopics(Map<Uuid,String>)
+  isFromFollower()
+  isolationLevel()
+  metadata()
+  rackId()
+  parse(ByteBuffer;short)
+  data()
} 
@enduml
