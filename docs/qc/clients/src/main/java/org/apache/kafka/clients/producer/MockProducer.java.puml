@startuml
!pragma layout smetana
Producer_K,V_ <|-- org.apache.kafka.clients.producer.MockProducer
class org.apache.kafka.clients.producer.MockProducer {
+  MockProducer(Cluster;boolean;Partitioner;Serializer<K>;Serializer<V>)
+  MockProducer(boolean;Serializer<K>;Serializer<V>)
+  MockProducer(Cluster;boolean;Serializer<K>;Serializer<V>)
+  MockProducer(boolean;Partitioner;Serializer<K>;Serializer<V>)
+  MockProducer()
+  initTransactions()
+  beginTransaction()
+  sendOffsetsToTransaction(Map<TopicPartition,OffsetAndMetadata>;String)
+  sendOffsetsToTransaction(Map<TopicPartition,OffsetAndMetadata>;ConsumerGroupMetadata)
+  commitTransaction()
+  abortTransaction()
+  verifyProducerState()
+  verifyTransactionsInitialized()
+  verifyTransactionInFlight()
+  send(ProducerRecord<K,V>)
+  send(ProducerRecord<K,V>;Callback)
+  nextOffset(TopicPartition)
+  flush()
+  partitionsFor(String)
+  metrics()
+  setMockMetrics(MetricName;Metric)
+  close()
+  close(Duration)
+  closed()
+  fenceProducer()
+  transactionInitialized()
+  transactionInFlight()
+  transactionCommitted()
+  transactionAborted()
+  flushed()
+  sentOffsets()
+  commitCount()
+  history()
+  uncommittedRecords()
+  consumerGroupOffsetsHistory()
+  uncommittedOffsets()
+  clear()
+  completeNext()
+  errorNext(RuntimeException)
+  partition(ProducerRecord<K,V>;Cluster)
} 
@enduml
