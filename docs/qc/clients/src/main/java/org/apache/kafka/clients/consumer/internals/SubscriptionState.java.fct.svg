<?xml version="1.0" encoding="UTF-8" standalone="no"?><svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" contentStyleType="text/css" height="472px" preserveAspectRatio="none" style="width:480px;height:472px;background:#000000;" version="1.1" viewBox="0 0 480 472" width="480px" zoomAndPan="magnify"><defs/><g><rect fill="#250613" height="1" style="stroke:#250613;stroke-width:1.0;" width="1" x="0" y="0"/><text fill="#33FF02" font-family="sans-serif" font-size="12" font-style="italic" font-weight="bold" lengthAdjust="spacing" textLength="127" x="5" y="17">PlantUML 1.2022.7</text><text fill="#33FF02" font-family="sans-serif" font-size="12" font-style="italic" font-weight="bold" lengthAdjust="spacing" textLength="4" x="5" y="30.9688"> </text><text fill="#33FF02" font-family="sans-serif" font-size="12" font-style="italic" font-weight="bold" lengthAdjust="spacing" textLength="399" x="5" y="44.9375">&lt;b&gt;This version of PlantUML is 138 days old, so you should</text><text fill="#33FF02" font-family="sans-serif" font-size="12" font-style="italic" font-weight="bold" lengthAdjust="spacing" textLength="408" x="5" y="58.9063">&lt;b&gt;consider upgrading from https://plantuml.com/download</text><rect fill="#33FF02" height="21.2969" style="stroke:#33FF02;stroke-width:1.0;" width="468" x="5" y="68.875"/><text fill="#000000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="178" x="6" y="83.875">[From string (line 21) ]</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="5" x="5" y="104.1719"> </text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="79" x="5" y="120.4688">@startuml</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="190" x="5" y="136.7656">!pragma layout smetana</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="153.0625">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="169.3594">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="122" x="5" y="185.6563">StringtoString()</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="71" x="5" y="201.9531">end note</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="459" x="5" y="218.25">:return "SubscriptionState{"+"type="+subscriptionType+</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="352" x="5" y="234.5469">", subscribedPattern="+subscribedPattern+</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="372" x="5" y="250.8438">", subscription="+String.join(",",subscription)+</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="468" x="5" y="267.1406">", groupSubscription="+String.join(",",groupSubscription)+</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="394" x="5" y="283.4375">", defaultResetStrategy="+defaultResetStrategy+</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="419" x="5" y="299.7344">", assignment="+assignment.partitionStateValues()+</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="222" x="5" y="316.0313">" (id="+assignmentId+")}";;</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="35" x="5" y="332.3281">stop</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="30" x="5" y="348.625">end</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="364.9219">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="381.2188">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="154" x="5" y="397.5156">StringprettyString()</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="71" x="5" y="413.8125">end note</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="275" x="5" y="430.1094">switch (switch (subscriptionType))</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" text-decoration="wavy underline" textLength="73" x="5" y="446.4063">endswith</text><text fill="#FF0000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="106" x="10" y="462.7031">Syntax Error?</text><!--MD5=[f8f30b6ec0014717e15484140f0cc232]
@startuml
!pragma layout smetana
start
note right
StringtoString()
end note
:return "SubscriptionState{"+"type="+subscriptionType+
", subscribedPattern="+subscribedPattern+
", subscription="+String.join(",",subscription)+
", groupSubscription="+String.join(",",groupSubscription)+
", defaultResetStrategy="+defaultResetStrategy+
", assignment="+assignment.partitionStateValues()+
" (id="+assignmentId+")}";;
stop
end
start
note right
StringprettyString()
end note
switch (switch (subscriptionType))
endswith
end
start
note right
constructor SubscriptionState(LogContextlogContext,
OffsetResetStrategydefaultResetStrategy)
end note
:this.log=logContext.logger(this.getClass());;
:this.defaultResetStrategy=defaultResetStrategy;;
:this.subscription=new TreeSet<>();;
:this.assignment=new PartitionStates<>();;
:this.groupSubscription=new HashSet<>();;
:this.subscribedPattern=null ;;
:this.subscriptionType=SubscriptionType.NONE;;
end
start
note right
intassignmentId()
end note
:return assignmentId;;
stop
end
start
note right
void setSubscriptionType(SubscriptionTypetype)
end note
if (this.subscriptionType==SubscriptionType.NONE) then (yes) 
:this.subscriptionType=type;;
else if (this.subscriptionType!=type) then (yes) 
:throw new IllegalStateException(SUBSCRIPTION_EXCEPTION_MESSAGE);;
stop
endif 
end
start
note right
booleansubscribe(Set<String>topics,ConsumerRebalanceListenerlistener)
end note
:registerRebalanceListener(listener);;
:setSubscriptionType(SubscriptionType.AUTO_TOPICS);;
:return changeSubscription(topics);;
stop
end
start
note right
void subscribe(Patternpattern,ConsumerRebalanceListenerlistener)
end note
:registerRebalanceListener(listener);;
:setSubscriptionType(SubscriptionType.AUTO_PATTERN);;
:this.subscribedPattern=pattern;;
end
start
note right
booleansubscribeFromPattern(Set<String>topics)
end note
if (subscriptionType!=SubscriptionType.AUTO_PATTERN) then (yes) 
:throw new IllegalArgumentException("Attempt to subscribe from pattern while subscription type set to "+
subscriptionType);;
stop
endif 
:return changeSubscription(topics);;
stop
end
start
note right
booleanchangeSubscription(Set<String>topicsToSubscribe)
end note
if (subscription.equals(topicsToSubscribe)) then (yes) 
:return false;;
stop
endif 
:subscription=topicsToSubscribe;;
:return true;;
stop
end
start
note right
booleangroupSubscribe(Collection<String>topics)
end note
if (!hasAutoAssignedPartitions()) then (yes) 
:throw new IllegalStateException(SUBSCRIPTION_EXCEPTION_MESSAGE);;
stop
endif 
:groupSubscription=new HashSet<>(topics);;
:return !subscription.containsAll(groupSubscription);;
stop
end
start
note right
void resetGroupSubscription()
end note
:groupSubscription=Collections.emptySet();;
end
start
note right
booleanassignFromUser(Set<TopicPartition>partitions)
end note
:setSubscriptionType(SubscriptionType.USER_ASSIGNED);;
if (this.assignment.partitionSet().equals(partitions)) then (yes) 
:return false;;
stop
endif 
:assignmentId++;;
:Set<String>manualSubscribedTopics=new HashSet<>();;
:Map<TopicPartition,TopicPartitionState>partitionToState=new HashMap<>();;
:TopicPartitionStatestate=assignment.stateValue(partition);;
if (state==null ) then (yes) 
:state=new TopicPartitionState();;
endif 
:partitionToState.put(partition,state);;
:manualSubscribedTopics.add(partition.topic());;
:this.assignment.set(partitionToState);;
:return changeSubscription(manualSubscribedTopics);;
stop
end
start
note right
booleancheckAssignmentMatchedSubscription(Collection<TopicPartition>assignments)
end note
if (this.subscribedPattern!=null ) then (yes) 
if (!this.subscribedPattern.matcher(topicPartition.topic()).matches()) then (yes) 
:log.info("Assigned partition {} for non-subscribed topic regex pattern; subscription pattern is {}",
topicPartition,this.subscribedPattern);;
:return false;;
stop
endif 
else (no)
if (!this.subscription.contains(topicPartition.topic())) then (yes) 
:log.info("Assigned partition {} for non-subscribed topic; subscription is {}",
topicPartition,this.subscription);;
:return false;;
stop
endif 
endif 
:return true;;
stop
end
start
note right
void assignFromSubscribed(Collection<TopicPartition>assignments)
end note
if (!this.hasAutoAssignedPartitions()) then (yes) 
:throw new IllegalArgumentException("Attempt to dynamically assign partitions while manual assignment in use");;
stop
endif 
:Map<TopicPartition,TopicPartitionState>assignedPartitionStates=new HashMap<>(assignments.size());;
:TopicPartitionStatestate=this.assignment.stateValue(tp);;
if (state==null ) then (yes) 
:state=new TopicPartitionState();;
endif 
:assignedPartitionStates.put(tp,state);;
:assignmentId++;;
:this.assignment.set(assignedPartitionStates);;
end
start
note right
void registerRebalanceListener(ConsumerRebalanceListenerlistener)
end note
if (listener==null ) then (yes) 
:throw new IllegalArgumentException("RebalanceListener cannot be null");;
stop
endif 
:this.rebalanceListener=listener;;
end
start
note right
booleanhasPatternSubscription()
end note
:return this.subscriptionType==SubscriptionType.AUTO_PATTERN;;
stop
end
start
note right
booleanhasNoSubscriptionOrUserAssignment()
end note
:return this.subscriptionType==SubscriptionType.NONE;;
stop
end
start
note right
void unsubscribe()
end note
:this.subscription=Collections.emptySet();;
:this.groupSubscription=Collections.emptySet();;
:this.assignment.clear();;
:this.subscribedPattern=null ;;
:this.subscriptionType=SubscriptionType.NONE;;
:this.assignmentId++;;
end
start
note right
booleanmatchesSubscribedPattern(Stringtopic)
end note
:Patternpattern=this.subscribedPattern;;
if (hasPatternSubscription()&&pattern!=null ) then (yes) 
:return pattern.matcher(topic).matches();;
stop
endif 
:return false;;
stop
end
start
note right
Set<String>subscription()
end note
if (hasAutoAssignedPartitions()) then (yes) 
:return this.subscription;;
stop
endif 
:return Collections.emptySet();;
stop
end
start
note right
Set<TopicPartition>pausedPartitions()
end note
:return collectPartitions(TopicPartitionState::isPaused);;
stop
end
start
note right
Set<String>metadataTopics()
end note
if (groupSubscription.isEmpty()) then (yes) 
:return subscription;;
stop
else if (groupSubscription.containsAll(subscription)) then (yes) 
:return groupSubscription;;
stop
else (no)
:Set<String>topics=new HashSet<>(groupSubscription);;
:topics.addAll(subscription);;
:return topics;;
stop
endif 
end
start
note right
booleanneedsMetadata(Stringtopic)
end note
:return subscription.contains(topic)||groupSubscription.contains(topic);;
stop
end
start
note right
TopicPartitionStateassignedState(TopicPartitiontp)
end note
:TopicPartitionStatestate=this.assignment.stateValue(tp);;
if (state==null ) then (yes) 
:throw new IllegalStateException("No current assignment for partition "+
tp);;
stop
endif 
:return state;;
stop
end
start
note right
TopicPartitionStateassignedStateOrNull(TopicPartitiontp)
end note
:return this.assignment.stateValue(tp);;
stop
end
start
note right
void seekValidated(TopicPartitiontp,FetchPositionposition)
end note
:assignedState(tp).seekValidated(position);;
end
start
note right
void seek(TopicPartitiontp,longoffset)
end note
:seekValidated(tp,new FetchPosition(offset));;
end
start
note right
void seekUnvalidated(TopicPartitiontp,FetchPositionposition)
end note
:assignedState(tp).seekUnvalidated(position);;
end
start
note right
void maybeSeekUnvalidated(TopicPartitiontp,
FetchPositionposition,OffsetResetStrategyrequestedResetStrategy)
end note
:TopicPartitionStatestate=assignedStateOrNull(tp);;
if (state==null ) then (yes) 
:log.debug("Skipping reset of partition {} since it is no longer assigned",
tp);;
else if (!state.awaitingReset()) then (yes) 
:log.debug("Skipping reset of partition {} since reset is no longer needed",
tp);;
else if (requestedResetStrategy!=state.resetStrategy) then (yes) 
:log.debug("Skipping reset of partition {} since an alternative reset has been requested",
tp);;
else (no)
:log.info("Resetting offset for partition {} to position {}.",
tp,position);;
:state.seekUnvalidated(position);;
endif 
end
start
note right
Set<TopicPartition>assignedPartitions()
end note
:return new HashSet<>(this.assignment.partitionSet());;
stop
end
start
note right
List<TopicPartition>assignedPartitionsList()
end note
:return new ArrayList<>(this.assignment.partitionSet());;
stop
end
start
note right
intnumAssignedPartitions()
end note
:return this.assignment.size();;
stop
end
start
note right
List<TopicPartition>fetchablePartitions(Predicate<TopicPartition>isAvailable)
end note
:List<TopicPartition>result=new ArrayList<>();;
:assignment.forEach((topicPartition,topicPartitionState)->{if (topicPartitionState.isFetchable()&&isAvailable.test(topicPartition)){result.add(topicPartition);
:}});;
:return result;;
stop
end
start
note right
booleanhasAutoAssignedPartitions()
end note
:return this.subscriptionType==SubscriptionType.AUTO_TOPICS||this.subscriptionType==SubscriptionType.AUTO_PATTERN;;
stop
end
start
note right
void position(TopicPartitiontp,FetchPositionposition)
end note
:assignedState(tp).position(position);;
end
start
note right
booleanmaybeValidatePositionForCurrentLeader(ApiVersionsapiVersions,
TopicPartitiontp,Metadata.LeaderAndEpochleaderAndEpoch)
end note
if (leaderAndEpoch.leader.isPresent()) then (yes) 
:NodeApiVersionsnodeApiVersions=apiVersions.get(leaderAndEpoch.leader.get()
.idString());;
if (nodeApiVersions==null ||hasUsableOffsetForLeaderEpochVersion(nodeApiVersions)) then (yes) 
:return assignedState(tp).maybeValidatePosition(leaderAndEpoch);;
stop
else (no)
:assignedState(tp).updatePositionLeaderNoValidation(leaderAndEpoch);;
:return false;;
stop
endif 
else (no)
:return assignedState(tp).maybeValidatePosition(leaderAndEpoch);;
stop
endif 
end
start
note right
Optional<LogTruncation>maybeCompleteValidation(TopicPartitiontp,
FetchPositionrequestPosition,EpochEndOffsetepochEndOffset)
end note
:TopicPartitionStatestate=assignedStateOrNull(tp);;
if (state==null ) then (yes) 
:log.debug("Skipping completed validation for partition {} which is not currently assigned.",
tp);;
else if (!state.awaitingValidation()) then (yes) 
:log.debug("Skipping completed validation for partition {} which is no longer expecting validation.",
tp);;
else (no)
:SubscriptionState.FetchPositioncurrentPosition=state.position;;
if (!currentPosition.equals(requestPosition)) then (yes) 
:log.debug("Skipping completed validation for partition {} since the current position {} "+
"no longer matches the position {} when the request was sent",
tp,currentPosition,requestPosition);;
else if (epochEndOffset.endOffset()==UNDEFINED_EPOCH_OFFSET||epochEndOffset.leaderEpoch()==UNDEFINED_EPOCH) then (yes) 
if (hasDefaultOffsetResetPolicy()) then (yes) 
:log.info("Truncation detected for partition {} at offset {},
 resetting offset",tp,currentPosition);;
:requestOffsetReset(tp);;
else (no)
:log.warn("Truncation detected for partition {} at offset {},
 but no reset policy is set",tp,currentPosition);;
:return Optional.of(new LogTruncation(tp,
requestPosition,Optional.empty()));;
stop
endif 
else if (epochEndOffset.endOffset()<currentPosition.offset) then (yes) 
if (hasDefaultOffsetResetPolicy()) then (yes) 
:SubscriptionState.FetchPositionnewPosition=new SubscriptionState.FetchPosition(epochEndOffset.endOffset(),
Optional.of(epochEndOffset.leaderEpoch()),
currentPosition.currentLeader);;
:log.info("Truncation detected for partition {} at offset {}, resetting offset to "+
"the first offset known to diverge {}",tp,
currentPosition,newPosition);;
:state.seekValidated(newPosition);;
else (no)
:OffsetAndMetadatadivergentOffset=new OffsetAndMetadata(epochEndOffset.endOffset(),
Optional.of(epochEndOffset.leaderEpoch()),
null );;
:log.warn("Truncation detected for partition {} at offset {} (the end offset from the "+
"broker is {}), but no reset policy is set",
tp,currentPosition,divergentOffset);;
:return Optional.of(new LogTruncation(tp,
requestPosition,Optional.of(divergentOffset)));;
stop
endif 
else (no)
:state.completeValidation();;
endif 
endif 
:return Optional.empty();;
stop
end
start
note right
booleanawaitingValidation(TopicPartitiontp)
end note
:return assignedState(tp).awaitingValidation();;
stop
end
start
note right
void completeValidation(TopicPartitiontp)
end note
:assignedState(tp).completeValidation();;
end
start
note right
FetchPositionvalidPosition(TopicPartitiontp)
end note
:return assignedState(tp).validPosition();;
stop
end
start
note right
FetchPositionposition(TopicPartitiontp)
end note
:return assignedState(tp).position;;
stop
end
start
note right
LongpartitionLag(TopicPartitiontp,IsolationLevelisolationLevel)
end note
:TopicPartitionStatetopicPartitionState=assignedState(tp);;
if (topicPartitionState.position==null ) then (yes) 
:return null ;;
stop
else if (isolationLevel==IsolationLevel.READ_COMMITTED) then (yes) 
:return topicPartitionState.lastStableOffset==null ? null :topicPartitionState.lastStableOffset-topicPartitionState.position.offset;;
stop
else (no)
:return topicPartitionState.highWatermark==null ? null :topicPartitionState.highWatermark-topicPartitionState.position.offset;;
stop
endif 
end
start
note right
LongpartitionEndOffset(TopicPartitiontp,IsolationLevelisolationLevel)
end note
:TopicPartitionStatetopicPartitionState=assignedState(tp);;
if (isolationLevel==IsolationLevel.READ_COMMITTED) then (yes) 
:return topicPartitionState.lastStableOffset;;
stop
else (no)
:return topicPartitionState.highWatermark;;
stop
endif 
end
start
note right
void requestPartitionEndOffset(TopicPartitiontp)
end note
:TopicPartitionStatetopicPartitionState=assignedState(tp);;
:topicPartitionState.requestEndOffset();;
end
start
note right
booleanpartitionEndOffsetRequested(TopicPartitiontp)
end note
:TopicPartitionStatetopicPartitionState=assignedState(tp);;
:return topicPartitionState.endOffsetRequested();;
stop
end
start
note right
LongpartitionLead(TopicPartitiontp)
end note
:TopicPartitionStatetopicPartitionState=assignedState(tp);;
:return topicPartitionState.logStartOffset==null ? null :topicPartitionState.position.offset-topicPartitionState.logStartOffset;;
stop
end
start
note right
void updateHighWatermark(TopicPartitiontp,
longhighWatermark)
end note
:assignedState(tp).highWatermark(highWatermark);;
end
start
note right
void updateLogStartOffset(TopicPartitiontp,
longlogStartOffset)
end note
:assignedState(tp).logStartOffset(logStartOffset);;
end
start
note right
void updateLastStableOffset(TopicPartitiontp,
longlastStableOffset)
end note
:assignedState(tp).lastStableOffset(lastStableOffset);;
end
start
note right
void updatePreferredReadReplica(TopicPartitiontp,
intpreferredReadReplicaId,LongSuppliertimeMs)
end note
:assignedState(tp).updatePreferredReadReplica(preferredReadReplicaId,
timeMs);;
end
start
note right
Optional<Integer>preferredReadReplica(TopicPartitiontp,
longtimeMs)
end note
:final TopicPartitionStatetopicPartitionState=assignedStateOrNull(tp);;
if (topicPartitionState==null ) then (yes) 
:return Optional.empty();;
stop
else (no)
:return topicPartitionState.preferredReadReplica(timeMs);;
stop
endif 
end
start
note right
Optional<Integer>clearPreferredReadReplica(TopicPartitiontp)
end note
:return assignedState(tp).clearPreferredReadReplica();;
stop
end
start
note right
Map<TopicPartition,OffsetAndMetadata>allConsumed()
end note
:Map<TopicPartition,OffsetAndMetadata>allConsumed=new HashMap<>();;
:assignment.forEach((topicPartition,partitionState)->{if (partitionState.hasValidPosition())allConsumed.put(topicPartition,
new OffsetAndMetadata(partitionState.position.offset,
partitionState.position.offsetEpoch,""));});;
:return allConsumed;;
stop
end
start
note right
void requestOffsetReset(TopicPartitionpartition,
OffsetResetStrategyoffsetResetStrategy)
end note
:assignedState(partition).reset(offsetResetStrategy);;
end
start
note right
void requestOffsetReset(Collection<TopicPartition>partitions,
OffsetResetStrategyoffsetResetStrategy)
end note
:partitions.forEach(tp->{log.info("Seeking to {} offset of partition {}",
offsetResetStrategy,tp);assignedState(tp).reset(offsetResetStrategy);});;
end
start
note right
void requestOffsetReset(TopicPartitionpartition)
end note
:requestOffsetReset(partition,defaultResetStrategy);;
end
start
note right
void setNextAllowedRetry(Set<TopicPartition>partitions,
longnextAllowResetTimeMs)
end note
:assignedState(partition).setNextAllowedRetry(nextAllowResetTimeMs);;
end
start
note right
booleanhasDefaultOffsetResetPolicy()
end note
:return defaultResetStrategy!=OffsetResetStrategy.NONE;;
stop
end
start
note right
booleanisOffsetResetNeeded(TopicPartitionpartition)
end note
:return assignedState(partition).awaitingReset();;
stop
end
start
note right
OffsetResetStrategyresetStrategy(TopicPartitionpartition)
end note
:return assignedState(partition).resetStrategy();;
stop
end
start
note right
booleanhasAllFetchPositions()
end note
:Iterator<TopicPartitionState>it=assignment.stateIterator();;
while (it.hasNext())
if (!it.next().hasValidPosition()) then (yes) 
:return false;;
stop
endif 
endwhile
:return true;;
stop
end
start
note right
Set<TopicPartition>initializingPartitions()
end note
:return collectPartitions(state->state.fetchState.equals(FetchStates.INITIALIZING));;
stop
end
start
note right
Set<TopicPartition>collectPartitions(Predicate<TopicPartitionState>filter)
end note
:Set<TopicPartition>result=new HashSet<>();;
:assignment.forEach((topicPartition,topicPartitionState)->{if (filter.test(topicPartitionState)){result.add(topicPartition);
:}});;
:return result;;
stop
end
start
note right
void resetInitializingPositions()
end note
:final Set<TopicPartition>partitionsWithNoOffsets=new HashSet<>();;
:assignment.forEach((tp,partitionState)->{if (partitionState.fetchState.equals(FetchStates.INITIALIZING)){if (defaultResetStrategy==OffsetResetStrategy.NONE)partitionsWithNoOffsets.add(tp);
:else requestOffsetReset(tp);}});;
if (!partitionsWithNoOffsets.isEmpty()) then (yes) 
:throw new NoOffsetForPartitionException(partitionsWithNoOffsets);;
stop
endif 
end
start
note right
Set<TopicPartition>partitionsNeedingReset(longnowMs)
end note
:return collectPartitions(state->state.awaitingReset()&&!state.awaitingRetryBackoff(nowMs));;
stop
end
start
note right
Set<TopicPartition>partitionsNeedingValidation(longnowMs)
end note
:return collectPartitions(state->state.awaitingValidation()&&!state.awaitingRetryBackoff(nowMs));;
stop
end
start
note right
booleanisAssigned(TopicPartitiontp)
end note
:return assignment.contains(tp);;
stop
end
start
note right
booleanisPaused(TopicPartitiontp)
end note
:TopicPartitionStateassignedOrNull=assignedStateOrNull(tp);;
:return assignedOrNull!=null &&assignedOrNull.isPaused();;
stop
end
start
note right
booleanisFetchable(TopicPartitiontp)
end note
:TopicPartitionStateassignedOrNull=assignedStateOrNull(tp);;
:return assignedOrNull!=null &&assignedOrNull.isFetchable();;
stop
end
start
note right
booleanhasValidPosition(TopicPartitiontp)
end note
:TopicPartitionStateassignedOrNull=assignedStateOrNull(tp);;
:return assignedOrNull!=null &&assignedOrNull.hasValidPosition();;
stop
end
start
note right
void pause(TopicPartitiontp)
end note
:assignedState(tp).pause();;
end
start
note right
void resume(TopicPartitiontp)
end note
:assignedState(tp).resume();;
end
start
note right
void requestFailed(Set<TopicPartition>partitions,
longnextRetryTimeMs)
end note
:final TopicPartitionStatestate=assignedStateOrNull(partition);;
if (state!=null ) then (yes) 
:state.requestFailed(nextRetryTimeMs);;
endif 
end
start
note right
void movePartitionToEnd(TopicPartitiontp)
end note
:assignment.moveToEnd(tp);;
end
start
note right
ConsumerRebalanceListenerrebalanceListener()
end note
:return rebalanceListener;;
stop
end
start
note right
constructor TopicPartitionState()
end note
:this.paused=false;;
:this.endOffsetRequested=false;;
:this.fetchState=FetchStates.INITIALIZING;;
:this.position=null ;;
:this.highWatermark=null ;;
:this.logStartOffset=null ;;
:this.lastStableOffset=null ;;
:this.resetStrategy=null ;;
:this.nextRetryTimeMs=null ;;
:this.preferredReadReplica=null ;;
end
start
note right
booleanendOffsetRequested()
end note
:return endOffsetRequested;;
stop
end
start
note right
void requestEndOffset()
end note
:endOffsetRequested=true;;
end
start
note right
void transitionState(FetchStatenewState,RunnablerunIfTransitioned)
end note
:FetchStatenextState=this.fetchState.transitionTo(newState);;
if (nextState.equals(newState)) then (yes) 
:this.fetchState=nextState;;
:runIfTransitioned.run();;
if (this.position==null &&nextState.requiresPosition()) then (yes) 
:throw new IllegalStateException("Transitioned subscription state to "+
nextState+", but position is null");;
stop
else if (!nextState.requiresPosition()) then (yes) 
:this.position=null ;;
endif 
endif 
end
start
note right
Optional<Integer>preferredReadReplica(longtimeMs)
end note
if (preferredReadReplicaExpireTimeMs!=null &&timeMs>preferredReadReplicaExpireTimeMs) then (yes) 
:preferredReadReplica=null ;;
:return Optional.empty();;
stop
else (no)
:return Optional.ofNullable(preferredReadReplica);;
stop
endif 
end
start
note right
void updatePreferredReadReplica(intpreferredReadReplica,
LongSuppliertimeMs)
end note
if (this.preferredReadReplica==null ||preferredReadReplica!=this.preferredReadReplica) then (yes) 
:this.preferredReadReplica=preferredReadReplica;;
:this.preferredReadReplicaExpireTimeMs=timeMs.getAsLong();;
endif 
end
start
note right
Optional<Integer>clearPreferredReadReplica()
end note
if (preferredReadReplica!=null ) then (yes) 
:intremovedReplicaId=this.preferredReadReplica;;
:this.preferredReadReplica=null ;;
:this.preferredReadReplicaExpireTimeMs=null ;;
:return Optional.of(removedReplicaId);;
stop
else (no)
:return Optional.empty();;
stop
endif 
end
start
note right
void reset(OffsetResetStrategystrategy)
end note
:transitionState(FetchStates.AWAIT_RESET,
()->{this.resetStrategy=strategy;this.nextRetryTimeMs=null ;});;
end
start
note right
booleanmaybeValidatePosition(Metadata.LeaderAndEpochcurrentLeaderAndEpoch)
end note
if (this.fetchState.equals(FetchStates.AWAIT_RESET)) then (yes) 
:return false;;
stop
endif 
if (!currentLeaderAndEpoch.leader.isPresent()) then (yes) 
:return false;;
stop
endif 
if (position!=null &&!position.currentLeader.equals(currentLeaderAndEpoch)) then (yes) 
:FetchPositionnewPosition=new FetchPosition(position.offset,
position.offsetEpoch,currentLeaderAndEpoch);;
:validatePosition(newPosition);;
:preferredReadReplica=null ;;
endif 
:return this.fetchState.equals(FetchStates.AWAIT_VALIDATION);;
stop
end
start
note right
void updatePositionLeaderNoValidation(Metadata.LeaderAndEpochcurrentLeaderAndEpoch)
end note
if (position!=null ) then (yes) 
:transitionState(FetchStates.FETCHING,()->{this.position=new FetchPosition(position.offset,
position.offsetEpoch,currentLeaderAndEpoch);this.nextRetryTimeMs=null ;});;
endif 
end
start
note right
void validatePosition(FetchPositionposition)
end note
if (position.offsetEpoch.isPresent()&&position.currentLeader.epoch.isPresent()) then (yes) 
:transitionState(FetchStates.AWAIT_VALIDATION,
()->{this.position=position;this.nextRetryTimeMs=null ;});;
else (no)
:transitionState(FetchStates.FETCHING,()->{this.position=position;
:this.nextRetryTimeMs=null ;});;
endif 
end
start
note right
void completeValidation()
end note
if (hasPosition()) then (yes) 
:transitionState(FetchStates.FETCHING,()->this.nextRetryTimeMs=null );;
endif 
end
start
note right
booleanawaitingValidation()
end note
:return fetchState.equals(FetchStates.AWAIT_VALIDATION);;
stop
end
start
note right
booleanawaitingRetryBackoff(longnowMs)
end note
:return nextRetryTimeMs!=null &&nowMs<nextRetryTimeMs;;
stop
end
start
note right
booleanawaitingReset()
end note
:return fetchState.equals(FetchStates.AWAIT_RESET);;
stop
end
start
note right
void setNextAllowedRetry(longnextAllowedRetryTimeMs)
end note
:this.nextRetryTimeMs=nextAllowedRetryTimeMs;;
end
start
note right
void requestFailed(longnextAllowedRetryTimeMs)
end note
:this.nextRetryTimeMs=nextAllowedRetryTimeMs;;
end
start
note right
booleanhasValidPosition()
end note
:return fetchState.hasValidPosition();;
stop
end
start
note right
booleanhasPosition()
end note
:return position!=null ;;
stop
end
start
note right
booleanisPaused()
end note
:return paused;;
stop
end
start
note right
void seekValidated(FetchPositionposition)
end note
:transitionState(FetchStates.FETCHING,()->{this.position=position;
:this.resetStrategy=null ;this.nextRetryTimeMs=null ;});;
end
start
note right
void seekUnvalidated(FetchPositionfetchPosition)
end note
:seekValidated(fetchPosition);;
:validatePosition(fetchPosition);;
end
start
note right
void position(FetchPositionposition)
end note
if (!hasValidPosition()) then (yes) 
:throw new IllegalStateException("Cannot set a new position without a valid current position");;
stop
endif 
:this.position=position;;
end
start
note right
FetchPositionvalidPosition()
end note
if (hasValidPosition()) then (yes) 
:return position;;
stop
else (no)
:return null ;;
stop
endif 
end
start
note right
void pause()
end note
:this.paused=true;;
end
start
note right
void resume()
end note
:this.paused=false;;
end
start
note right
booleanisFetchable()
end note
:return !paused&&hasValidPosition();;
stop
end
start
note right
void highWatermark(LonghighWatermark)
end note
:this.highWatermark=highWatermark;;
:this.endOffsetRequested=false;;
end
start
note right
void logStartOffset(LonglogStartOffset)
end note
:this.logStartOffset=logStartOffset;;
end
start
note right
void lastStableOffset(LonglastStableOffset)
end note
:this.lastStableOffset=lastStableOffset;;
:this.endOffsetRequested=false;;
end
start
note right
OffsetResetStrategyresetStrategy()
end note
:return resetStrategy;;
stop
end
if (validTransitions().contains(newState)) then (yes) 
:return newState;;
stop
else (no)
:return this;;
stop
endif 
start
note right
Collection<FetchState>validTransitions()
end note
:return Arrays.asList(FetchStates.FETCHING,
FetchStates.AWAIT_RESET,FetchStates.AWAIT_VALIDATION);;
stop
end
start
note right
booleanrequiresPosition()
end note
:return false;;
stop
end
start
note right
booleanhasValidPosition()
end note
:return false;;
stop
end
start
note right
Collection<FetchState>validTransitions()
end note
:return Arrays.asList(FetchStates.FETCHING,
FetchStates.AWAIT_RESET,FetchStates.AWAIT_VALIDATION);;
stop
end
start
note right
booleanrequiresPosition()
end note
:return true;;
stop
end
start
note right
booleanhasValidPosition()
end note
:return true;;
stop
end
start
note right
Collection<FetchState>validTransitions()
end note
:return Arrays.asList(FetchStates.FETCHING,
FetchStates.AWAIT_RESET);;
stop
end
start
note right
booleanrequiresPosition()
end note
:return false;;
stop
end
start
note right
booleanhasValidPosition()
end note
:return false;;
stop
end
start
note right
Collection<FetchState>validTransitions()
end note
:return Arrays.asList(FetchStates.FETCHING,
FetchStates.AWAIT_RESET,FetchStates.AWAIT_VALIDATION);;
stop
end
start
note right
booleanrequiresPosition()
end note
:return true;;
stop
end
start
note right
booleanhasValidPosition()
end note
:return false;;
stop
end
start
note right
constructor FetchPosition(longoffset)
end note
:this(offset,Optional.empty(),Metadata.LeaderAndEpoch.noLeaderOrEpoch());;
end
start
note right
constructor FetchPosition(longoffset,Optional<Integer>offsetEpoch,
Metadata.LeaderAndEpochcurrentLeader)
end note
:this.offset=offset;;
:this.offsetEpoch=Objects.requireNonNull(offsetEpoch);;
:this.currentLeader=Objects.requireNonNull(currentLeader);;
end
start
note right
booleanequals(Objecto)
end note
if (this==o) then (yes) 
:return true;;
stop
endif 
if (o==null ||getClass()!=o.getClass()) then (yes) 
:return false;;
stop
endif 
:FetchPositionthat=(FetchPosition)o;;
:return offset==that.offset&&offsetEpoch.equals(that.offsetEpoch)&&currentLeader.equals(that.currentLeader);;
stop
end
start
note right
inthashCode()
end note
:return Objects.hash(offset,offsetEpoch,currentLeader);;
stop
end
start
note right
StringtoString()
end note
:return "FetchPosition{"+"offset="+offset+
", offsetEpoch="+offsetEpoch+", currentLeader="+
currentLeader+'}';;
stop
end
start
note right
constructor LogTruncation(TopicPartitiontopicPartition,
FetchPositionfetchPosition,Optional<OffsetAndMetadata>divergentOffsetOpt)
end note
:this.topicPartition=topicPartition;;
:this.fetchPosition=fetchPosition;;
:this.divergentOffsetOpt=divergentOffsetOpt;;
end
start
note right
StringtoString()
end note
:StringBuilderbldr=new StringBuilder().append("(partition=").append(topicPartition).append(",
 fetchOffset=").append(fetchPosition.offset).append(",
 fetchEpoch=").append(fetchPosition.offsetEpoch);;
if (divergentOffsetOpt.isPresent()) then (yes) 
:OffsetAndMetadatadivergentOffset=divergentOffsetOpt.get();;
:bldr.append(", divergentOffset=").append(divergentOffset.offset()).append(",
 divergentEpoch=").append(divergentOffset.leaderEpoch());;
else (no)
:bldr.append(", divergentOffset=unknown").append(",
 divergentEpoch=unknown");;
endif 
:return bldr.append(")").toString();;
stop
end
@enduml

PlantUML version 1.2022.7(Mon Aug 22 17:01:30 UTC 2022)
(GPL source distribution)
Java Runtime: OpenJDK Runtime Environment
JVM: OpenJDK 64-Bit Server VM
Default Encoding: UTF-8
Language: en
Country: null
--></g></svg>