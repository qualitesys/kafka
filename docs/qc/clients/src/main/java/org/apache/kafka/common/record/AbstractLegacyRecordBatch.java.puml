@startuml
!pragma layout smetana
AbstractRecordBatchRecord <|-- org.apache.kafka.common.record.AbstractLegacyRecordBatch
class org.apache.kafka.common.record.AbstractLegacyRecordBatch {
+  outerRecord()
+  lastOffset()
+  isValid()
+  ensureValid()
+  keySize()
+  hasKey()
+  key()
+  valueSize()
+  hasValue()
+  value()
+  headers()
+  hasMagic(byte)
+  hasTimestampType(TimestampType)
+  checksum()
+  maxTimestamp()
+  timestamp()
+  timestampType()
+  baseOffset()
+  magic()
+  compressionType()
+  sizeInBytes()
+  countOrNull()
+  toString()
+  writeTo(ByteBuffer)
+  producerId()
+  producerEpoch()
+  hasProducerId()
+  sequence()
+  baseSequence()
+  lastSequence()
+  isTransactional()
+  partitionLeaderEpoch()
+  isControlBatch()
+  deleteHorizonMs()
+  iterator()
+  iterator(BufferSupplier)
+  streamingIterator(BufferSupplier)
+  writeHeader(ByteBuffer;long;int)
+  writeHeader(DataOutputStream;long;int)
} 
@enduml
