<?xml version="1.0" encoding="UTF-8" standalone="no"?><svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" contentStyleType="text/css" height="544px" preserveAspectRatio="none" style="width:625px;height:544px;background:#000000;" version="1.1" viewBox="0 0 625 544" width="625px" zoomAndPan="magnify"><defs/><g><rect fill="#020D0D" height="1" style="stroke:#020D0D;stroke-width:1.0;" width="1" x="0" y="0"/><text fill="#33FF02" font-family="sans-serif" font-size="12" font-style="italic" font-weight="bold" lengthAdjust="spacing" textLength="127" x="5" y="17">PlantUML 1.2022.7</text><rect fill="#33FF02" height="21.2969" style="stroke:#33FF02;stroke-width:1.0;" width="613" x="5" y="26.9688"/><text fill="#000000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="188" x="6" y="41.9688">[From string (line 219) ]</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="5" x="5" y="62.2656"> </text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="79" x="5" y="78.5625">@startuml</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="190" x="5" y="94.8594">!pragma layout smetana</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="111.1563">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="127.4531">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="360" x="5" y="143.75">constructor Builder(intnodeId,StringclusterId)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="15" x="5" y="160.0469">...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="185" x="5" y="176.3438">... ( skipping 194 lines )</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="15" x="5" y="192.6406">...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="502" x="5" y="208.9375">quorumFeatures,defaultReplicationFactor,defaultNumPartitions,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="613" x="5" y="225.2344">replicaPlacer,snapshotMaxNewRecordBytes,leaderImbalanceCheckIntervalNs,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="434" x="5" y="241.5313">maxIdleIntervalNs,sessionTimeoutNs,controllerMetrics,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="463" x="5" y="257.8281">createTopicPolicy,alterConfigPolicy,configurationValidator,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="353" x="5" y="274.125">authorizer,staticConfig,bootstrapMetadata);;</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="35" x="5" y="290.4219">stop</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="329" x="5" y="306.7188">:Utils.closeQuietly(queue,"event queue");</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="80" x="5" y="323.0156">:throw e;;</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="30" x="5" y="339.3125">end</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="355.6094">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="371.9063">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="353" x="5" y="388.2031">void accept(ConfigResourceconfigResource)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="71" x="5" y="404.5">end note</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="375" x="5" y="420.7969">if (configResource.name().isEmpty()) then (yes)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="40" x="5" y="437.0938">endif</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="414" x="5" y="453.3906">:brokerId=Integer.parseInt(configResource.name());</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="482" x="5" y="469.6875">:throw new InvalidRequestException("Invalid broker name "+</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="202" x="5" y="485.9844">configResource.name());;</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="314" x="5" y="502.2813">if (!clusterControl.brokerRegistrations()</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" text-decoration="wavy underline" textLength="273" x="5" y="518.5781">.containsKey(brokerId)) then (yes)</text><text fill="#FF0000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="106" x="10" y="534.875">Syntax Error?</text><!--MD5=[c2d8f1415276cacff9336837bdf965ac]
@startuml
!pragma layout smetana
start
note right
constructor Builder(intnodeId,StringclusterId)
end note
:this.nodeId=nodeId;
:this.clusterId=clusterId;
end
start
note right
intnodeId()
end note
:return nodeId;;
stop
end
start
note right
BuildersetTime(Timetime)
end note
:this.time=time;
:return this;;
stop
end
start
note right
BuildersetThreadNamePrefix(StringthreadNamePrefix)
end note
:this.threadNamePrefix=threadNamePrefix;
:return this;;
stop
end
start
note right
BuildersetLogContext(LogContextlogContext)
end note
:this.logContext=logContext;
:return this;;
stop
end
start
note right
BuildersetConfigSchema(KafkaConfigSchemaconfigSchema)
end note
:this.configSchema=configSchema;
:return this;;
stop
end
start
note right
BuildersetRaftClient(RaftClient<ApiMessageAndVersion>logManager)
end note
:this.raftClient=logManager;
:return this;;
stop
end
start
note right
BuildersetQuorumFeatures(QuorumFeaturesquorumFeatures)
end note
:this.quorumFeatures=quorumFeatures;
:return this;;
stop
end
start
note right
BuildersetDefaultReplicationFactor(shortdefaultReplicationFactor)
end note
:this.defaultReplicationFactor=defaultReplicationFactor;
:return this;;
stop
end
start
note right
BuildersetDefaultNumPartitions(intdefaultNumPartitions)
end note
:this.defaultNumPartitions=defaultNumPartitions;
:return this;;
stop
end
start
note right
BuildersetReplicaPlacer(ReplicaPlacerreplicaPlacer)
end note
:this.replicaPlacer=replicaPlacer;
:return this;;
stop
end
start
note right
BuildersetSnapshotMaxNewRecordBytes(longvalue)
end note
:this.snapshotMaxNewRecordBytes=value;
:return this;;
stop
end
start
note right
BuildersetLeaderImbalanceCheckIntervalNs(OptionalLongvalue)
end note
:this.leaderImbalanceCheckIntervalNs=value;
:return this;;
stop
end
start
note right
BuildersetMaxIdleIntervalNs(OptionalLongvalue)
end note
:this.maxIdleIntervalNs=value;
:return this;;
stop
end
start
note right
BuildersetSessionTimeoutNs(longsessionTimeoutNs)
end note
:this.sessionTimeoutNs=sessionTimeoutNs;
:return this;;
stop
end
start
note right
BuildersetMetrics(ControllerMetricscontrollerMetrics)
end note
:this.controllerMetrics=controllerMetrics;
:return this;;
stop
end
start
note right
BuildersetBootstrapMetadata(BootstrapMetadatabootstrapMetadata)
end note
:this.bootstrapMetadata=bootstrapMetadata;
:return this;;
stop
end
start
note right
BuildersetCreateTopicPolicy(Optional<CreateTopicPolicy>createTopicPolicy)
end note
:this.createTopicPolicy=createTopicPolicy;
:return this;;
stop
end
start
note right
BuildersetAlterConfigPolicy(Optional<AlterConfigPolicy>alterConfigPolicy)
end note
:this.alterConfigPolicy=alterConfigPolicy;
:return this;;
stop
end
start
note right
BuildersetConfigurationValidator(ConfigurationValidatorconfigurationValidator)
end note
:this.configurationValidator=configurationValidator;
:return this;;
stop
end
start
note right
BuildersetAuthorizer(ClusterMetadataAuthorizerauthorizer)
end note
:this.authorizer=Optional.of(authorizer);
:return this;;
stop
end
start
note right
BuildersetStaticConfig(Map<String,Object>staticConfig)
end note
:this.staticConfig=staticConfig;
:return this;;
stop
end
start
note right
QuorumControllerbuild()
end note
if (raftClient==null ) then (yes) 
:throw new IllegalStateException("You must set a raft client.");;
endif 
if (threadNamePrefix==null ) then (yes) 
:threadNamePrefix=String.format("Node%d_",
nodeId);
endif 
if (logContext==null ) then (yes) 
:logContext=new LogContext(String.format("[Controller %d] ",
nodeId));
endif 
if (controllerMetrics==null ) then (yes) 
:controllerMetrics=(ControllerMetrics)Class.forName("org.apache.kafka.controller.MockControllerMetrics")
.getConstructor().newInstance();
endif 
:queue=new KafkaEventQueue(time,logContext,threadNamePrefix+
"QuorumController");
:return new QuorumController(logContext,nodeId,
clusterId,queue,time,configSchema,raftClient,
quorumFeatures,defaultReplicationFactor,defaultNumPartitions,
replicaPlacer,snapshotMaxNewRecordBytes,leaderImbalanceCheckIntervalNs,
maxIdleIntervalNs,sessionTimeoutNs,controllerMetrics,
createTopicPolicy,alterConfigPolicy,configurationValidator,
authorizer,staticConfig,bootstrapMetadata);;
stop
:Utils.closeQuietly(queue,"event queue");
:throw e;;
end
start
note right
void accept(ConfigResourceconfigResource)
end note
if (configResource.name().isEmpty()) then (yes) 
endif 
:brokerId=Integer.parseInt(configResource.name());
:throw new InvalidRequestException("Invalid broker name "+
configResource.name());;
if (!clusterControl.brokerRegistrations()
.containsKey(brokerId)) then (yes) 
:throw new BrokerIdNotRegisteredException("No broker with id "+
brokerId+" found.");;
endif 
if (replicationControl.getTopicId(configResource.name())==null ) then (yes) 
:throw new UnknownTopicOrPartitionException("The topic '"+
configResource.name()+"' does not exist.");;
endif 
end
start
note right
NotControllerExceptionnewNotControllerException()
end note
if (latestController.isPresent()) then (yes) 
:return new NotControllerException(ACTIVE_CONTROLLER_EXCEPTION_TEXT_PREFIX+
latestController.getAsInt());;
stop
else (no)
:return new NotControllerException("No controller appears to be active.");;
stop
endif 
end
start
note right
intexceptionToApparentController(NotControllerExceptione)
end note
if (e.getMessage().startsWith(ACTIVE_CONTROLLER_EXCEPTION_TEXT_PREFIX)) then (yes) 
:return Integer.parseInt(e.getMessage().substring(ACTIVE_CONTROLLER_EXCEPTION_TEXT_PREFIX.length()));;
stop
else (no)
:return -1;;
stop
endif 
end
start
note right
void handleEventEnd(Stringname,longstartProcessingTimeNs)
end note
:log.debug("Processed {} in {} us",name,MICROSECONDS.convert(deltaNs,
NANOSECONDS));
:controllerMetrics.updateEventQueueProcessingTime(NANOSECONDS.toMillis(deltaNs));
end
start
note right
ThrowablehandleEventException(Stringname,
OptionalLongstartProcessingTimeNs,Throwableexception)
end note
if (!startProcessingTimeNs.isPresent()) then (yes) 
:log.error("{}: unable to start processing because of {}.",
name,exception.getClass().getSimpleName());
if (exception instanceof ApiException) then (yes) 
:return exception;;
stop
else (no)
:return new UnknownServerException(exception);;
stop
endif 
endif 
if (exception instanceof ApiException) then (yes) 
:log.info("{}: failed with {} in {} us",name,
exception.getClass().getSimpleName(),deltaUs);
:return exception;;
stop
endif 
:log.warn("{}: failed with unknown server exception {} at epoch {} in {} us.  "+
"Renouncing leadership and reverting to the last committed offset {}.",
name,exception.getClass().getSimpleName(),
curClaimEpoch,deltaUs,lastCommittedOffset,
exception);
:raftClient.resign(curClaimEpoch);
:renounce();
:return new UnknownServerException(exception);;
stop
end
start
note right
constructor ControlEvent(Stringname,Runnablehandler)
end note
:this.name=name;
:this.handler=handler;
end
start
note right
void run()
end note
:controllerMetrics.updateEventQueueTime(NANOSECONDS.toMillis(now-eventCreatedTimeNs));
:startProcessingTimeNs=OptionalLong.of(now);
:log.debug("Executing {}.",this);
:handler.run();
:handleEventEnd(this.toString(),startProcessingTimeNs.getAsLong());
end
start
note right
void handleException(Throwableexception)
end note
:handleEventException(name,startProcessingTimeNs,
exception);
end
start
note right
StringtoString()
end note
:return name;;
stop
end
start
note right
void appendControlEvent(Stringname,Runnablehandler)
end note
:queue.append(event);
end
start
note right
void createSnapshotGenerator(longcommittedOffset,
intcommittedEpoch,longcommittedTimestamp)
end note
if (generator!=null ) then (yes) 
:throw new RuntimeException("Snapshot generator already exists.");;
endif 
if (!snapshotRegistry.hasSnapshot(committedOffset)) then (yes) 
:throw new RuntimeException(String.format("Cannot generate a snapshot at committed offset %d because it does not exists in the snapshot registry.",committedOffset));;
endif 
if (writer.isPresent()) then (yes) 
:generator=new SnapshotGenerator(logContext,
writer.get(),MAX_BATCHES_PER_GENERATE_CALL,
Arrays.asList(new Section("features",featureControl.iterator(committedOffset)),
new Section("cluster",clusterControl.iterator(committedOffset)),
new Section("replication",replicationControl.iterator(committedOffset)),
new Section("configuration",configurationControl.iterator(committedOffset)),
new Section("clientQuotas",clientQuotaControlManager.iterator(committedOffset)),
new Section("producerIds",producerIdControlManager.iterator(committedOffset)),
new Section("acls",aclControlManager.iterator(committedOffset))));
:reschedule(0);
else (no)
:log.info("Skipping generation of snapshot for committed offset {} and epoch {} since it already exists",
committedOffset,committedEpoch);
endif 
end
start
note right
void cancel()
end note
if (generator==null ) then (yes) 
:return ;;
stop
endif 
:log.error("Cancelling snapshot {}",generator.lastContainedLogOffset());
:generator.writer().close();
:generator=null ;
:snapshotRegistry.deleteSnapshotsUpTo(lastCommittedOffset);
:queue.cancelDeferred(GENERATE_SNAPSHOT);
end
start
note right
void reschedule(longdelayNs)
end note
:queue.scheduleDeferred(event.name,new EarliestDeadlineFunction(time.nanoseconds()+
delayNs),event);
end
start
note right
void run()
end note
if (generator==null ) then (yes) 
:log.debug("No snapshot is in progress.");
:return ;;
stop
endif 
:nextDelay=generator.generateBatches();
:log.error("Error while generating snapshot {}",
generator.lastContainedLogOffset(),e);
:generator.writer().close();
:generator=null ;
:return ;;
stop
if (!nextDelay.isPresent()) then (yes) 
:log.info("Finished generating snapshot {}.",
generator.lastContainedLogOffset());
:generator.writer().close();
:generator=null ;
:snapshotRegistry.deleteSnapshotsUpTo(lastCommittedOffset);
:return ;;
stop
endif 
:reschedule(nextDelay.getAsLong());
end
start
note right
OptionalLongsnapshotLastOffsetFromLog()
end note
if (generator==null ) then (yes) 
:return OptionalLong.empty();;
stop
endif 
:return OptionalLong.of(generator.lastContainedLogOffset());;
stop
end
start
note right
constructor ControllerReadEvent(Stringname,
Supplier<T>handler)
end note
:this.name=name;
:this.future=new CompletableFuture<T>();
:this.handler=handler;
end
start
note right
CompletableFuture<T>future()
end note
:return future;;
stop
end
start
note right
void run()
end note
:controllerMetrics.updateEventQueueTime(NANOSECONDS.toMillis(now-eventCreatedTimeNs));
:startProcessingTimeNs=OptionalLong.of(now);
:handleEventEnd(this.toString(),startProcessingTimeNs.getAsLong());
:future.complete(value);
end
start
note right
void handleException(Throwableexception)
end note
:future.completeExceptionally(handleEventException(name,
startProcessingTimeNs,exception));
end
start
note right
StringtoString()
end note
:return name+"("+System.identityHashCode(this)+
")";;
stop
end
start
note right
ReplicationControlManagerreplicationControl()
end note
:return replicationControl;;
stop
end
start
note right
ClusterControlManagerclusterControl()
end note
:return clusterControl;;
stop
end
if (deadlineNs.isPresent()) then (yes) 
:queue.appendWithDeadline(deadlineNs.getAsLong(),
event);
else (no)
:queue.append(event);
endif 
:return event.future();;
stop
start
note right
constructor ControllerWriteEvent(Stringname,
ControllerWriteOperation<T>op)
end note
:this.name=name;
:this.future=new CompletableFuture<T>();
:this.op=op;
:this.resultAndOffset=null ;
end
start
note right
CompletableFuture<T>future()
end note
:return future;;
stop
end
start
note right
void run()
end note
:controllerMetrics.updateEventQueueTime(NANOSECONDS.toMillis(now-eventCreatedTimeNs));
if (controllerEpoch==-1) then (yes) 
:throw newNotControllerException();;
endif 
:startProcessingTimeNs=OptionalLong.of(now);
if (result.records().isEmpty()) then (yes) 
:op.processBatchEndOffset(writeOffset);
if (!maybeOffset.isPresent()) then (yes) 
:resultAndOffset=ControllerResultAndOffset.of(-1,
result);
:log.debug("Completing read-only operation {} immediately because "+
"the purgatory is empty.",this);
:complete(null );
else (no)
:resultAndOffset=ControllerResultAndOffset.of(maybeOffset.getAsLong(),
result);
:log.debug("Read-only operation {} will be completed when the log "+
"reaches offset {}",this,resultAndOffset.offset());
endif 
else (no)
if (result.isAtomic()) then (yes) 
:offset=raftClient.scheduleAtomicAppend(controllerEpoch,
result.records());
else (no)
:offset=raftClient.scheduleAppend(controllerEpoch,
result.records());
endif 
:op.processBatchEndOffset(offset);
:updateWriteOffset(offset);
:resultAndOffset=ControllerResultAndOffset.of(offset,
result);
:replay(message.message(),Optional.empty(),
offset);
:snapshotRegistry.getOrCreateSnapshot(offset);
:log.debug("Read-write operation {} will be completed when the log "+
"reaches offset {}.",this,resultAndOffset.offset());
endif 
:maybeScheduleNextBalancePartitionLeaders();
if (!future.isDone()) then (yes) 
:purgatory.add(resultAndOffset.offset(),this);
endif 
end
start
note right
void handleException(Throwableexception)
end note
:complete(exception);
end
start
note right
void complete(Throwableexception)
end note
if (exception==null ) then (yes) 
:handleEventEnd(this.toString(),startProcessingTimeNs.getAsLong());
:future.complete(resultAndOffset.response());
else (no)
:future.completeExceptionally(handleEventException(name,
startProcessingTimeNs,exception));
endif 
end
start
note right
StringtoString()
end note
:return name+"("+System.identityHashCode(this)+
")";;
stop
end
:queue.prepend(event);
:return event.future();;
stop
if (deadlineNs.isPresent()) then (yes) 
:queue.appendWithDeadline(deadlineNs.getAsLong(),
event);
else (no)
:queue.append(event);
endif 
:return event.future();;
stop
start
note right
void handleCommit(BatchReader<ApiMessageAndVersion>reader)
end note
:appendRaftEvent("handleCommit[baseOffset="+
reader.baseOffset()+"]",()->{try {maybeCompleteAuthorizerInitialLoad();longprocessedRecordsSize=0;while (reader.hasNext()){Batch<ApiMessageAndVersion>batch=reader.next();longoffset=batch.lastOffset();intepoch=batch.epoch();List<ApiMessageAndVersion>messages=batch.records();if (isActiveController()){log.debug("Completing purgatory items up to offset {} and epoch {}.",offset,epoch);purgatory.completeUpTo(offset);snapshotRegistry.deleteSnapshotsUpTo(snapshotGeneratorManager.snapshotLastOffsetFromLog().orElse(offset));}else {if (log.isDebugEnabled()){if (log.isTraceEnabled()){log.trace("Replaying commits from the active node up to "+"offset {} and epoch {}: {}.",offset,epoch,messages.stream().map(ApiMessageAndVersion::toString).collect(Collectors.joining(", ")));}else {log.debug("Replaying commits from the active node up to "+"offset {} and epoch {}.",offset,epoch);}}for (ApiMessageAndVersionmessageAndVersion:messages){replay(messageAndVersion.message(),Optional.empty(),offset);}}updateLastCommittedState(offset,epoch,batch.appendTimestamp());processedRecordsSize+=batch.sizeInBytes();}maybeGenerateSnapshot(processedRecordsSize);}finally {reader.close();}});
end
start
note right
void handleSnapshot(SnapshotReader<ApiMessageAndVersion>reader)
end note
:appendRaftEvent(String.format("handleSnapshot[snapshotId=%s]",
reader.snapshotId()),()->{try {if (isActiveController()){throw new IllegalStateException(String.format("Asked to load snapshot (%s) when it is the active controller (%d)",reader.snapshotId(),curClaimEpoch));}log.info("Starting to replay snapshot ({}), from last commit offset ({}) and epoch ({})",reader.snapshotId(),lastCommittedOffset,lastCommittedEpoch);resetState();while (reader.hasNext()){Batch<ApiMessageAndVersion>batch=reader.next();longoffset=batch.lastOffset();List<ApiMessageAndVersion>messages=batch.records();if (log.isDebugEnabled()){if (log.isTraceEnabled()){log.trace("Replaying snapshot ({}) batch with last offset of {}: {}",reader.snapshotId(),offset,messages.stream().map(ApiMessageAndVersion::toString).collect(Collectors.joining(", ")));}else {log.debug("Replaying snapshot ({}) batch with last offset of {}",reader.snapshotId(),offset);}}for (ApiMessageAndVersionmessageAndVersion:messages){replay(messageAndVersion.message(),Optional.of(reader.snapshotId()),offset);}}updateLastCommittedState(reader.lastContainedLogOffset(),reader.lastContainedLogEpoch(),reader.lastContainedLogTimestamp());snapshotRegistry.getOrCreateSnapshot(lastCommittedOffset);authorizer.ifPresent(a->a.loadSnapshot(aclControlManager.idToAcl()));}finally {reader.close();}});
end
start
note right
void handleLeaderChange(LeaderAndEpochnewLeader)
end note
if (newLeader.isLeader(nodeId)) then (yes) 
:appendRaftEvent("handleLeaderChange["+newEpoch+
"]",()->{intcurEpoch=curClaimEpoch;if (curEpoch!=-1){throw new RuntimeException("Tried to claim controller epoch "+
newEpoch+", but we never renounced controller epoch "+
curEpoch);}curClaimEpoch=newEpoch;controllerMetrics.setActive(true);updateWriteOffset(lastCommittedOffset);clusterControl.activate();final MetadataVersionmetadataVersion;if (!featureControl.sawMetadataVersion()){final CompletableFuture<Map<String,ApiError>>future;if (!bootstrapMetadata.metadataVersion().isKRaftSupported()){metadataVersion=MetadataVersion.MINIMUM_KRAFT_VERSION;future=new CompletableFuture<>();future.completeExceptionally(new IllegalStateException("Cannot become leader without a KRaft supported version. "+"Got "+bootstrapMetadata.metadataVersion()));}else {metadataVersion=bootstrapMetadata.metadataVersion();final List<ApiMessageAndVersion>bootstrapRecords=bootstrapMetadata.records();future=prependWriteEvent("bootstrapMetadata",()->{if (metadataVersion.isAtLeast(MetadataVersion.IBP_3_3_IV0)){log.info("Initializing metadata.version to {}",metadataVersion.featureLevel());}else {log.info("Upgrading KRaft cluster and initializing metadata.version to {}",metadataVersion.featureLevel());}return ControllerResult.atomicOf(bootstrapRecords,null );});}future.whenComplete((result,exception)->{if (exception!=null ){log.error("Failed to bootstrap metadata.",exception);appendRaftEvent("bootstrapMetadata["+curClaimEpoch+"]",()->{log.warn("Renouncing the leadership at oldEpoch {} since we could not bootstrap "+"metadata. Reverting to last committed offset {}.",curClaimEpoch,lastCommittedOffset);renounce();});}});}else {metadataVersion=featureControl.metadataVersion();}log.info("Becoming the active controller at epoch {}, committed offset {}, committed epoch {}, and metadata.version {}",newEpoch,lastCommittedOffset,lastCommittedEpoch,metadataVersion.featureLevel());snapshotRegistry.getOrCreateSnapshot(lastCommittedOffset);maybeScheduleNextBalancePartitionLeaders();maybeScheduleNextWriteNoOpRecord();});
endif 
end
start
note right
void beginShutdown()
end note
:queue.beginShutdown("MetaLogManager.Listener");
end
start
note right
void appendRaftEvent(Stringname,Runnablerunnable)
end note
:appendControlEvent(name,()->{if (this!=metaLogListener){log.debug("Ignoring {} raft event from an old registration",
name);}else {try {runnable.run();}finally {maybeCompleteAuthorizerInitialLoad();}}});
end
start
note right
void maybeCompleteAuthorizerInitialLoad()
end note
if (!needToCompleteAuthorizerLoad) then (yes) 
:return ;;
stop
endif 
if (highWatermark.isPresent()) then (yes) 
if (lastCommittedOffset+1>=highWatermark.getAsLong()) then (yes) 
:log.info("maybeCompleteAuthorizerInitialLoad: completing authorizer "+
"initial load at last committed offset {}.",
lastCommittedOffset);
:authorizer.get().completeInitialLoad();
:needToCompleteAuthorizerLoad=false;
else (no)
:log.trace("maybeCompleteAuthorizerInitialLoad: can't proceed because "+
"lastCommittedOffset  = {}, but highWatermark = {}.",
lastCommittedOffset,highWatermark.getAsLong());
endif 
else (no)
:log.trace("maybeCompleteAuthorizerInitialLoad: highWatermark not set.");
endif 
end
start
note right
booleanisActiveController()
end note
:return curClaimEpoch!=-1;;
stop
end
start
note right
void updateWriteOffset(longoffset)
end note
:writeOffset=offset;
if (isActiveController()) then (yes) 
:controllerMetrics.setLastAppliedRecordOffset(writeOffset);
:controllerMetrics.setLastAppliedRecordTimestamp(time.milliseconds());
else (no)
:controllerMetrics.setLastAppliedRecordOffset(lastCommittedOffset);
:controllerMetrics.setLastAppliedRecordTimestamp(lastCommittedTimestamp);
endif 
end
start
note right
void updateLastCommittedState(longoffset,
intepoch,longtimestamp)
end note
:lastCommittedOffset=offset;
:lastCommittedEpoch=epoch;
:lastCommittedTimestamp=timestamp;
:controllerMetrics.setLastCommittedRecordOffset(offset);
if (!isActiveController()) then (yes) 
:controllerMetrics.setLastAppliedRecordOffset(offset);
:controllerMetrics.setLastAppliedRecordTimestamp(timestamp);
endif 
end
start
note right
void renounce()
end note
:curClaimEpoch=-1;
:controllerMetrics.setActive(false);
:purgatory.failAll(newNotControllerException());
if (snapshotRegistry.hasSnapshot(lastCommittedOffset)) then (yes) 
:newBytesSinceLastSnapshot=0;
:snapshotRegistry.revertToSnapshot(lastCommittedOffset);
:authorizer.ifPresent(a->a.loadSnapshot(aclControlManager.idToAcl()));
else (no)
:resetState();
:raftClient.unregister(metaLogListener);
:metaLogListener=new QuorumMetaLogListener();
:raftClient.register(metaLogListener);
endif 
:updateWriteOffset(-1);
:clusterControl.deactivate();
:cancelMaybeFenceReplicas();
:cancelMaybeBalancePartitionLeaders();
:cancelNextWriteNoOpRecord();
end
:queue.scheduleDeferred(name,new EarliestDeadlineFunction(deadlineNs),
event);
:event.future.exceptionally(e->{if (e instanceof UnknownServerException&&e.getCause()!=null &&e.getCause() instanceof RejectedExecutionException){log.error("Cancelling deferred write event {} because the event queue "+"is now closed.",name);return null ;}else if (e instanceof NotControllerException){log.debug("Cancelling deferred write event {} because this controller "+"is no longer active.",name);return null ;}log.error("Unexpected exception while executing deferred write event {}. "+"Rescheduling for a minute from now.",name,e);scheduleDeferredWriteEvent(name,deadlineNs+NANOSECONDS.convert(1,TimeUnit.MINUTES),op);return null ;});
start
note right
void rescheduleMaybeFenceStaleBrokers()
end note
if (nextCheckTimeNs==Long.MAX_VALUE) then (yes) 
:cancelMaybeFenceReplicas();
:return ;;
stop
endif 
:scheduleDeferredWriteEvent(MAYBE_FENCE_REPLICAS,
nextCheckTimeNs,()->{ControllerResult<Void>result=replicationControl.maybeFenceOneStaleBroker();rescheduleMaybeFenceStaleBrokers();return result;});
end
start
note right
void cancelMaybeFenceReplicas()
end note
:queue.cancelDeferred(MAYBE_FENCE_REPLICAS);
end
start
note right
void maybeScheduleNextBalancePartitionLeaders()
end note
if (imbalancedScheduled!=ImbalanceSchedule.SCHEDULED&&leaderImbalanceCheckIntervalNs.isPresent()&&replicationControl.arePartitionLeadersImbalanced()) then (yes) 
:log.debug("Scheduling write event for {} because scheduled ({}),
 checkIntervalNs ({}) and isImbalanced ({})",
MAYBE_BALANCE_PARTITION_LEADERS,imbalancedScheduled,
leaderImbalanceCheckIntervalNs,replicationControl.arePartitionLeadersImbalanced());
if (result.response()) then (yes) 
:imbalancedScheduled=ImbalanceSchedule.IMMEDIATELY;
else (no)
:imbalancedScheduled=ImbalanceSchedule.DEFERRED;
endif 
:return result;;
stop
if (imbalancedScheduled==ImbalanceSchedule.DEFERRED) then (yes) 
:delayNs+=leaderImbalanceCheckIntervalNs.getAsLong();
else (no)
:delayNs+=NANOSECONDS.convert(10,TimeUnit.MILLISECONDS);
endif 
:queue.scheduleDeferred(MAYBE_BALANCE_PARTITION_LEADERS,
new EarliestDeadlineFunction(delayNs),event);
:imbalancedScheduled=ImbalanceSchedule.SCHEDULED;
endif 
end
start
note right
void cancelMaybeBalancePartitionLeaders()
end note
:imbalancedScheduled=ImbalanceSchedule.DEFERRED;
:queue.cancelDeferred(MAYBE_BALANCE_PARTITION_LEADERS);
end
start
note right
void maybeScheduleNextWriteNoOpRecord()
end note
if (!noOpRecordScheduled&&maxIdleIntervalNs.isPresent()&&featureControl.metadataVersion()
.isNoOpRecordSupported()) then (yes) 
:log.debug("Scheduling write event for {} because maxIdleIntervalNs ({}) and metadataVersion ({})",
WRITE_NO_OP_RECORD,maxIdleIntervalNs.getAsLong(),
featureControl.metadataVersion());
:noOpRecordScheduled=false;
:maybeScheduleNextWriteNoOpRecord();
:return ControllerResult.of(Arrays.asList(new ApiMessageAndVersion(new NoOpRecord(),
(short)0)),null );;
stop
:queue.scheduleDeferred(WRITE_NO_OP_RECORD,
new EarliestDeadlineFunction(delayNs),event);
:noOpRecordScheduled=true;
endif 
end
start
note right
void cancelNextWriteNoOpRecord()
end note
:noOpRecordScheduled=false;
:queue.cancelDeferred(WRITE_NO_OP_RECORD);
end
start
note right
void handleFeatureControlChange()
end note
if (isActiveController()) then (yes) 
if (featureControl.metadataVersion().isNoOpRecordSupported()) then (yes) 
:maybeScheduleNextWriteNoOpRecord();
else (no)
:cancelNextWriteNoOpRecord();
endif 
endif 
end
start
note right
void replay(ApiMessagemessage,Optional<OffsetAndEpoch>snapshotId,
longoffset)
end note
:clusterControl.replay((RegisterBrokerRecord)message);
:clusterControl.replay((UnregisterBrokerRecord)message);
:replicationControl.replay((TopicRecord)message);
:replicationControl.replay((PartitionRecord)message);
:configurationControl.replay((ConfigRecord)message);
:replicationControl.replay((PartitionChangeRecord)message);
:clusterControl.replay((FenceBrokerRecord)message);
:clusterControl.replay((UnfenceBrokerRecord)message);
:replicationControl.replay((RemoveTopicRecord)message);
:featureControl.replay((FeatureLevelRecord)message);
:handleFeatureControlChange();
:clientQuotaControlManager.replay((ClientQuotaRecord)message);
:producerIdControlManager.replay((ProducerIdsRecord)message);
:clusterControl.replay((BrokerRegistrationChangeRecord)message);
:aclControlManager.replay((AccessControlEntryRecord)message,
snapshotId);
:aclControlManager.replay((RemoveAccessControlEntryRecord)message,
snapshotId);
:throw new RuntimeException("Unhandled record type "+
type);;
if (snapshotId.isPresent()) then (yes) 
:log.error("Error replaying record {} from snapshot {} at last offset {}.",
message.toString(),snapshotId.get(),offset,e);
else (no)
:log.error("Error replaying record {} at last offset {}.",
message.toString(),offset,e);
endif 
end
start
note right
void maybeGenerateSnapshot(longbatchSizeInBytes)
end note
:newBytesSinceLastSnapshot+=batchSizeInBytes;
if (newBytesSinceLastSnapshot>=snapshotMaxNewRecordBytes&&snapshotGeneratorManager.generator==null ) then (yes) 
if (!isActiveController()) then (yes) 
:snapshotRegistry.getOrCreateSnapshot(lastCommittedOffset);
endif 
:log.info("Generating a snapshot that includes (epoch={},
 offset={}) after {} committed bytes since the last snapshot.",
lastCommittedEpoch,lastCommittedOffset,newBytesSinceLastSnapshot);
:snapshotGeneratorManager.createSnapshotGenerator(lastCommittedOffset,
lastCommittedEpoch,lastCommittedTimestamp);
:newBytesSinceLastSnapshot=0;
endif 
end
start
note right
void resetState()
end note
:snapshotGeneratorManager.cancel();
:snapshotRegistry.reset();
:newBytesSinceLastSnapshot=0;
:updateLastCommittedState(-1,-1,-1);
end
start
note right
constructor QuorumController(LogContextlogContext,
intnodeId,StringclusterId,KafkaEventQueuequeue,
Timetime,KafkaConfigSchemaconfigSchema,RaftClient<ApiMessageAndVersion>raftClient,
QuorumFeaturesquorumFeatures,shortdefaultReplicationFactor,
intdefaultNumPartitions,ReplicaPlacerreplicaPlacer,
longsnapshotMaxNewRecordBytes,OptionalLongleaderImbalanceCheckIntervalNs,
OptionalLongmaxIdleIntervalNs,longsessionTimeoutNs,
ControllerMetricscontrollerMetrics,Optional<CreateTopicPolicy>createTopicPolicy,
Optional<AlterConfigPolicy>alterConfigPolicy,
ConfigurationValidatorconfigurationValidator,
Optional<ClusterMetadataAuthorizer>authorizer,
Map<String,Object>staticConfig,BootstrapMetadatabootstrapMetadata)
end note
:this.logContext=logContext;
:this.log=logContext.logger(QuorumController.class );
:this.nodeId=nodeId;
:this.clusterId=clusterId;
:this.queue=queue;
:this.time=time;
:this.controllerMetrics=controllerMetrics;
:this.snapshotRegistry=new SnapshotRegistry(logContext);
:this.purgatory=new ControllerPurgatory();
:this.resourceExists=new ConfigResourceExistenceChecker();
:this.configurationControl=new ConfigurationControlManager.Builder()
.setLogContext(logContext).setSnapshotRegistry(snapshotRegistry)
.setKafkaConfigSchema(configSchema).setExistenceChecker(resourceExists)
.setAlterConfigPolicy(alterConfigPolicy).setValidator(configurationValidator)
.setStaticConfig(staticConfig).setNodeId(nodeId)
.build();
:this.clientQuotaControlManager=new ClientQuotaControlManager(snapshotRegistry);
:this.featureControl=new FeatureControlManager.Builder()
.setLogContext(logContext).setQuorumFeatures(quorumFeatures)
.setSnapshotRegistry(snapshotRegistry).build();
:this.clusterControl=new ClusterControlManager.Builder()
.setLogContext(logContext).setClusterId(clusterId)
.setTime(time).setSnapshotRegistry(snapshotRegistry)
.setSessionTimeoutNs(sessionTimeoutNs).setReplicaPlacer(replicaPlacer)
.setControllerMetrics(controllerMetrics).setFeatureControlManager(featureControl)
.build();
:this.producerIdControlManager=new ProducerIdControlManager(clusterControl,
snapshotRegistry);
:this.snapshotMaxNewRecordBytes=snapshotMaxNewRecordBytes;
:this.leaderImbalanceCheckIntervalNs=leaderImbalanceCheckIntervalNs;
:this.maxIdleIntervalNs=maxIdleIntervalNs;
:this.replicationControl=new ReplicationControlManager.Builder()
.setSnapshotRegistry(snapshotRegistry).setLogContext(logContext)
.setDefaultReplicationFactor(defaultReplicationFactor)
.setDefaultNumPartitions(defaultNumPartitions)
.setMaxElectionsPerImbalance(ReplicationControlManager.MAX_ELECTIONS_PER_IMBALANCE)
.setConfigurationControl(configurationControl)
.setClusterControl(clusterControl).setControllerMetrics(controllerMetrics)
.setCreateTopicPolicy(createTopicPolicy).setFeatureControl(featureControl)
.build();
:this.authorizer=authorizer;
:authorizer.ifPresent(a->a.setAclMutator(this));
:this.aclControlManager=new AclControlManager(snapshotRegistry,
authorizer);
:this.raftClient=raftClient;
:this.bootstrapMetadata=bootstrapMetadata;
:this.metaLogListener=new QuorumMetaLogListener();
:this.curClaimEpoch=-1;
:this.needToCompleteAuthorizerLoad=authorizer.isPresent();
:updateWriteOffset(-1);
:resetState();
:log.info("Creating new QuorumController with clusterId {},
 authorizer {}.",clusterId,authorizer);
:this.raftClient.register(metaLogListener);
end
start
note right
CompletableFuture<AlterPartitionResponseData>alterPartition(ControllerRequestContextcontext,
AlterPartitionRequestDatarequest)
end note
if (request.topics().isEmpty()) then (yes) 
:return CompletableFuture.completedFuture(new AlterPartitionResponseData());;
stop
endif 
:return appendWriteEvent("alterPartition",
context.deadlineNs(),()->replicationControl.alterPartition(context,
request));;
stop
end
start
note right
CompletableFuture<CreateTopicsResponseData>createTopics(ControllerRequestContextcontext,
CreateTopicsRequestDatarequest,Set<String>describable)
end note
if (request.topics().isEmpty()) then (yes) 
:return CompletableFuture.completedFuture(new CreateTopicsResponseData());;
stop
endif 
:return appendWriteEvent("createTopics",context.deadlineNs(),
()->replicationControl.createTopics(request,
describable));;
stop
end
start
note right
CompletableFuture<Void>unregisterBroker(ControllerRequestContextcontext,
intbrokerId)
end note
:return appendWriteEvent("unregisterBroker",
context.deadlineNs(),()->replicationControl.unregisterBroker(brokerId));;
stop
end
start
note right
CompletableFuture<Map<String,ResultOrError<Uuid>>>findTopicIds(ControllerRequestContextcontext,
Collection<String>names)
end note
if (names.isEmpty()) then (yes) 
:return CompletableFuture.completedFuture(Collections.emptyMap());;
stop
endif 
:return appendReadEvent("findTopicIds",context.deadlineNs(),
()->replicationControl.findTopicIds(lastCommittedOffset,
names));;
stop
end
start
note right
CompletableFuture<Map<String,Uuid>>findAllTopicIds(ControllerRequestContextcontext)
end note
:return appendReadEvent("findAllTopicIds",
context.deadlineNs(),()->replicationControl.findAllTopicIds(lastCommittedOffset));;
stop
end
start
note right
CompletableFuture<Map<Uuid,ResultOrError<String>>>findTopicNames(ControllerRequestContextcontext,
Collection<Uuid>ids)
end note
if (ids.isEmpty()) then (yes) 
:return CompletableFuture.completedFuture(Collections.emptyMap());;
stop
endif 
:return appendReadEvent("findTopicNames",
context.deadlineNs(),()->replicationControl.findTopicNames(lastCommittedOffset,
ids));;
stop
end
start
note right
CompletableFuture<Map<Uuid,ApiError>>deleteTopics(ControllerRequestContextcontext,
Collection<Uuid>ids)
end note
if (ids.isEmpty()) then (yes) 
:return CompletableFuture.completedFuture(Collections.emptyMap());;
stop
endif 
:return appendWriteEvent("deleteTopics",context.deadlineNs(),
()->replicationControl.deleteTopics(ids));;
stop
end
start
note right
CompletableFuture<Map<ConfigResource,ResultOrError<Map<String,
String>>>>describeConfigs(ControllerRequestContextcontext,
Map<ConfigResource,Collection<String>>resources)
end note
:return appendReadEvent("describeConfigs",
context.deadlineNs(),()->configurationControl.describeConfigs(lastCommittedOffset,
resources));;
stop
end
start
note right
CompletableFuture<ElectLeadersResponseData>electLeaders(ControllerRequestContextcontext,
ElectLeadersRequestDatarequest)
end note
if (request.topicPartitions()!=null &&request.topicPartitions()
.isEmpty()) then (yes) 
:return CompletableFuture.completedFuture(new ElectLeadersResponseData());;
stop
endif 
:return appendWriteEvent("electLeaders",context.deadlineNs(),
()->replicationControl.electLeaders(request));;
stop
end
start
note right
CompletableFuture<FinalizedControllerFeatures>finalizedFeatures(ControllerRequestContextcontext)
end note
:return appendReadEvent("getFinalizedFeatures",
context.deadlineNs(),()->featureControl.finalizedFeatures(lastCommittedOffset));;
stop
end
start
note right
CompletableFuture<Map<ConfigResource,ApiError>>incrementalAlterConfigs(ControllerRequestContextcontext,
Map<ConfigResource,Map<String,Entry<OpType,
String>>>configChanges,booleanvalidateOnly)
end note
if (configChanges.isEmpty()) then (yes) 
:return CompletableFuture.completedFuture(Collections.emptyMap());;
stop
endif 
:return appendWriteEvent("incrementalAlterConfigs",
context.deadlineNs(),()->{ControllerResult<Map<ConfigResource,
ApiError>>result=configurationControl.incrementalAlterConfigs(configChanges,
false);if (validateOnly){return result.withoutRecords();}else {return result;}});;
stop
end
start
note right
CompletableFuture<AlterPartitionReassignmentsResponseData>alterPartitionReassignments(ControllerRequestContextcontext,
AlterPartitionReassignmentsRequestDatarequest)
end note
if (request.topics().isEmpty()) then (yes) 
:return CompletableFuture.completedFuture(new AlterPartitionReassignmentsResponseData());;
stop
endif 
:return appendWriteEvent("alterPartitionReassignments",
context.deadlineNs(),()->replicationControl.alterPartitionReassignments(request));;
stop
end
start
note right
CompletableFuture<ListPartitionReassignmentsResponseData>listPartitionReassignments(ControllerRequestContextcontext,
ListPartitionReassignmentsRequestDatarequest)
end note
if (request.topics()!=null &&request.topics()
.isEmpty()) then (yes) 
:return CompletableFuture.completedFuture(new ListPartitionReassignmentsResponseData()
.setErrorMessage(null ));;
stop
endif 
:return appendReadEvent("listPartitionReassignments",
context.deadlineNs(),()->replicationControl.listPartitionReassignments(request.topics()));;
stop
end
start
note right
CompletableFuture<Map<ConfigResource,ApiError>>legacyAlterConfigs(ControllerRequestContextcontext,
Map<ConfigResource,Map<String,String>>newConfigs,
booleanvalidateOnly)
end note
if (newConfigs.isEmpty()) then (yes) 
:return CompletableFuture.completedFuture(Collections.emptyMap());;
stop
endif 
:return appendWriteEvent("legacyAlterConfigs",
context.deadlineNs(),()->{ControllerResult<Map<ConfigResource,
ApiError>>result=configurationControl.legacyAlterConfigs(newConfigs,
false);if (validateOnly){return result.withoutRecords();}else {return result;}});;
stop
end
start
note right
CompletableFuture<BrokerHeartbeatReply>processBrokerHeartbeat(ControllerRequestContextcontext,
BrokerHeartbeatRequestDatarequest)
end note
:return appendWriteEvent("processBrokerHeartbeat",
context.deadlineNs(),new ControllerWriteOperation<BrokerHeartbeatReply>(){private final intbrokerId=request.brokerId();private booleaninControlledShutdown=false;@Override public ControllerResult<BrokerHeartbeatReply>generateRecordsAndResult(){ControllerResult<BrokerHeartbeatReply>result=replicationControl.processBrokerHeartbeat(request,lastCommittedOffset);inControlledShutdown=result.response().inControlledShutdown();rescheduleMaybeFenceStaleBrokers();return result;}@Override public void processBatchEndOffset(longoffset){if (inControlledShutdown){clusterControl.heartbeatManager().updateControlledShutdownOffset(brokerId,offset);}}});;
stop
end
start
note right
CompletableFuture<BrokerRegistrationReply>registerBroker(ControllerRequestContextcontext,
BrokerRegistrationRequestDatarequest)
end note
:return appendWriteEvent("registerBroker",
context.deadlineNs(),()->{ControllerResult<BrokerRegistrationReply>result=clusterControl.registerBroker(request,
writeOffset+1,featureControl.finalizedFeatures(Long.MAX_VALUE));rescheduleMaybeFenceStaleBrokers();return result;});;
stop
end
start
note right
CompletableFuture<Map<ClientQuotaEntity,ApiError>>alterClientQuotas(ControllerRequestContextcontext,
Collection<ClientQuotaAlteration>quotaAlterations,
booleanvalidateOnly)
end note
if (quotaAlterations.isEmpty()) then (yes) 
:return CompletableFuture.completedFuture(Collections.emptyMap());;
stop
endif 
:return appendWriteEvent("alterClientQuotas",
context.deadlineNs(),()->{ControllerResult<Map<ClientQuotaEntity,
ApiError>>result=clientQuotaControlManager.alterClientQuotas(quotaAlterations);if (validateOnly){return result.withoutRecords();}else {return result;}});;
stop
end
start
note right
CompletableFuture<AllocateProducerIdsResponseData>allocateProducerIds(ControllerRequestContextcontext,
AllocateProducerIdsRequestDatarequest)
end note
:return appendWriteEvent("allocateProducerIds",
context.deadlineNs(),()->producerIdControlManager.generateNextProducerId(request.brokerId(),
request.brokerEpoch())).thenApply(result->new AllocateProducerIdsResponseData()
.setProducerIdStart(result.firstProducerId())
.setProducerIdLen(result.size()));;
stop
end
start
note right
CompletableFuture<UpdateFeaturesResponseData>updateFeatures(ControllerRequestContextcontext,
UpdateFeaturesRequestDatarequest)
end note
:return appendWriteEvent("updateFeatures",
context.deadlineNs(),()->{Map<String,Short>updates=new HashMap<>();Map<String,
FeatureUpdate.UpgradeType>upgradeTypes=new HashMap<>();request.featureUpdates()
.forEach(featureUpdate->{StringfeatureName=featureUpdate.feature();upgradeTypes.put(featureName,
FeatureUpdate.UpgradeType.fromCode(featureUpdate.upgradeType()));updates.put(featureName,
featureUpdate.maxVersionLevel());});return featureControl.updateFeatures(updates,
upgradeTypes,clusterControl.brokerSupportedVersions(),
request.validateOnly());}).thenApply(result->{UpdateFeaturesResponseDataresponseData=new UpdateFeaturesResponseData();responseData.setResults(new UpdateFeaturesResponseData.UpdatableFeatureResultCollection(result.size()));result.forEach((featureName,error)->responseData.results().add(new UpdateFeaturesResponseData.UpdatableFeatureResult().setFeature(featureName).setErrorCode(error.error().code()).setErrorMessage(error.message())));return responseData;});;
stop
end
start
note right
CompletableFuture<List<CreatePartitionsTopicResult>>createPartitions(ControllerRequestContextcontext,
List<CreatePartitionsTopic>topics,booleanvalidateOnly)
end note
if (topics.isEmpty()) then (yes) 
:return CompletableFuture.completedFuture(Collections.emptyList());;
stop
endif 
:return appendWriteEvent("createPartitions",
context.deadlineNs(),()->{final ControllerResult<List<CreatePartitionsTopicResult>>result=replicationControl.createPartitions(topics);if (validateOnly){log.debug("Validate-only CreatePartitions result(s): {}",result.response());return result.withoutRecords();}else {log.debug("CreatePartitions result(s): {}",result.response());return result;}});;
stop
end
start
note right
CompletableFuture<Long>beginWritingSnapshot()
end note
:appendControlEvent("beginWritingSnapshot",
()->{if (snapshotGeneratorManager.generator==null ){snapshotGeneratorManager.createSnapshotGenerator(lastCommittedOffset,lastCommittedEpoch,lastCommittedTimestamp);}future.complete(snapshotGeneratorManager.generator.lastContainedLogOffset());});
:return future;;
stop
end
start
note right
CompletableFuture<List<AclCreateResult>>createAcls(ControllerRequestContextcontext,
List<AclBinding>aclBindings)
end note
:return appendWriteEvent("createAcls",context.deadlineNs(),
()->aclControlManager.createAcls(aclBindings));;
stop
end
start
note right
CompletableFuture<List<AclDeleteResult>>deleteAcls(ControllerRequestContextcontext,
List<AclBindingFilter>filters)
end note
:return appendWriteEvent("deleteAcls",context.deadlineNs(),
()->aclControlManager.deleteAcls(filters));;
stop
end
start
note right
CompletableFuture<Void>waitForReadyBrokers(intminBrokers)
end note
:appendControlEvent("waitForReadyBrokers",
()->{clusterControl.addReadyBrokersFuture(future,
minBrokers);});
:return future;;
stop
end
start
note right
void beginShutdown()
end note
:queue.beginShutdown("QuorumController#beginShutdown");
end
start
note right
intnodeId()
end note
:return nodeId;;
stop
end
start
note right
StringclusterId()
end note
:return clusterId;;
stop
end
start
note right
intcurClaimEpoch()
end note
:return curClaimEpoch;;
stop
end
start
note right
MetadataVersionmetadataVersion()
end note
:return featureControl.metadataVersion();;
stop
end
start
note right
void close()
end note
:queue.close();
:controllerMetrics.close();
end
start
note right
CountDownLatchpause()
end note
:appendControlEvent("pause",()->{try {latch.await();
:}catch (InterruptedExceptione){log.info("Interrupted while waiting for unpause.",
e);}});
:return latch;;
stop
end
start
note right
Timetime()
end note
:return time;;
stop
end
@enduml

PlantUML version 1.2022.7(Mon Aug 22 17:01:30 UTC 2022)
(GPL source distribution)
Java Runtime: OpenJDK Runtime Environment
JVM: OpenJDK 64-Bit Server VM
Default Encoding: UTF-8
Language: en
Country: null
--></g></svg>