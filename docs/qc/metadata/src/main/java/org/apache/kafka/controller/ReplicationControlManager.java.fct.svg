<?xml version="1.0" encoding="UTF-8" standalone="no"?><svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" contentStyleType="text/css" height="544px" preserveAspectRatio="none" style="width:851px;height:544px;background:#000000;" version="1.1" viewBox="0 0 851 544" width="851px" zoomAndPan="magnify"><defs/><g><rect fill="#161514" height="1" style="stroke:#161514;stroke-width:1.0;" width="1" x="0" y="0"/><text fill="#33FF02" font-family="sans-serif" font-size="12" font-style="italic" font-weight="bold" lengthAdjust="spacing" textLength="127" x="5" y="17">PlantUML 1.2022.7</text><rect fill="#33FF02" height="21.2969" style="stroke:#33FF02;stroke-width:1.0;" width="839" x="5" y="26.9688"/><text fill="#000000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="198" x="6" y="41.9688">[From string (line 1035) ]</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="5" x="5" y="62.2656"> </text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="79" x="5" y="78.5625">@startuml</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="190" x="5" y="94.8594">!pragma layout smetana</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="111.1563">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="127.4531">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="508" x="5" y="143.75">BuildersetSnapshotRegistry(SnapshotRegistrysnapshotRegistry)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="15" x="5" y="160.0469">...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="195" x="5" y="176.3438">... ( skipping 1010 lines )</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="15" x="5" y="192.6406">...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="513" x="5" y="208.9375">:records.add(new ApiMessageAndVersion(new PartitionRecord()</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="354" x="5" y="225.2344">.setPartitionId(partitionId).setTopicId(topicId)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="789" x="5" y="241.5313">.setReplicas(replicas).setIsr(isr).setLeaderRecoveryState(LeaderRecoveryState.RECOVERED.value())</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="365" x="5" y="257.8281">.setRemovingReplicas(Collections.emptyList())</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="839" x="5" y="274.125">.setAddingReplicas(Collections.emptyList()).setLeader(isr.get(0)).setLeaderEpoch(0).setPartitionEpoch(0),</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="390" x="5" y="290.4219">PARTITION_RECORD.highestSupportedVersion()));</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="116" x="5" y="306.7188">:partitionId++;</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="30" x="5" y="323.0156">end</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="339.3125">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="355.6094">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="524" x="5" y="371.9063">void validateManualPartitionAssignment(List&lt;Integer&gt;assignment,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="224" x="5" y="388.2031">OptionalIntreplicationFactor)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="71" x="5" y="404.5">end note</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="282" x="5" y="420.7969">if (assignment.isEmpty()) then (yes)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="572" x="5" y="437.0938">:throw new InvalidReplicaAssignmentException("The manual partition "+</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="363" x="5" y="453.3906">"assignment includes an empty replica list.");;</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="40" x="5" y="469.6875">endif</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="327" x="5" y="485.9844">:sortedBrokerIds.sort(Integer::compare);</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="314" x="5" y="502.2813">if (!clusterControl.brokerRegistrations()</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" text-decoration="wavy underline" textLength="273" x="5" y="518.5781">.containsKey(brokerId)) then (yes)</text><text fill="#FF0000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="106" x="10" y="534.875">Syntax Error?</text><!--MD5=[ed7c4b812a3b7bfdc64acebdd7e6782b]
@startuml
!pragma layout smetana
start
note right
BuildersetSnapshotRegistry(SnapshotRegistrysnapshotRegistry)
end note
:this.snapshotRegistry=snapshotRegistry;
:return this;;
stop
end
start
note right
BuildersetLogContext(LogContextlogContext)
end note
:this.logContext=logContext;
:return this;;
stop
end
start
note right
BuildersetDefaultReplicationFactor(shortdefaultReplicationFactor)
end note
:this.defaultReplicationFactor=defaultReplicationFactor;
:return this;;
stop
end
start
note right
BuildersetDefaultNumPartitions(intdefaultNumPartitions)
end note
:this.defaultNumPartitions=defaultNumPartitions;
:return this;;
stop
end
start
note right
BuildersetMaxElectionsPerImbalance(intmaxElectionsPerImbalance)
end note
:this.maxElectionsPerImbalance=maxElectionsPerImbalance;
:return this;;
stop
end
start
note right
BuildersetConfigurationControl(ConfigurationControlManagerconfigurationControl)
end note
:this.configurationControl=configurationControl;
:return this;;
stop
end
start
note right
BuildersetClusterControl(ClusterControlManagerclusterControl)
end note
:this.clusterControl=clusterControl;
:return this;;
stop
end
start
note right
BuildersetControllerMetrics(ControllerMetricscontrollerMetrics)
end note
:this.controllerMetrics=controllerMetrics;
:return this;;
stop
end
start
note right
BuildersetCreateTopicPolicy(Optional<CreateTopicPolicy>createTopicPolicy)
end note
:this.createTopicPolicy=createTopicPolicy;
:return this;;
stop
end
start
note right
BuildersetFeatureControl(FeatureControlManagerfeatureControl)
end note
:this.featureControl=featureControl;
:return this;;
stop
end
start
note right
ReplicationControlManagerbuild()
end note
if (configurationControl==null ) then (yes) 
:throw new IllegalStateException("Configuration control must be set before building");;
endif 
if (logContext==null ) then (yes) 
:logContext=new LogContext();
endif 
if (snapshotRegistry==null ) then (yes) 
:snapshotRegistry=configurationControl.snapshotRegistry();
endif 
if (featureControl==null ) then (yes) 
:featureControl=new FeatureControlManager.Builder()
.setLogContext(logContext).setSnapshotRegistry(snapshotRegistry).setQuorumFeatures(new QuorumFeatures(0,
new ApiVersions(),QuorumFeatures.defaultFeatureMap(),
Collections.singletonList(0))).setMetadataVersion(MetadataVersion.latest())
.build();
endif 
:return new ReplicationControlManager(snapshotRegistry,
logContext,defaultReplicationFactor,defaultNumPartitions,
maxElectionsPerImbalance,configurationControl,
clusterControl,controllerMetrics,createTopicPolicy,
featureControl);;
stop
end
start
note right
Iterator<UsableBroker>usableBrokers()
end note
:return clusterControl.usableBrokers();;
stop
end
start
note right
constructor TopicControlInfo(Stringname,SnapshotRegistrysnapshotRegistry,
Uuidid)
end note
:this.name=name;
:this.id=id;
:this.parts=new TimelineHashMap<>(snapshotRegistry,0);
end
start
note right
Stringname()
end note
:return name;;
stop
end
start
note right
UuidtopicId()
end note
:return id;;
stop
end
start
note right
Map<String,String>translateCreationConfigs(CreateableTopicConfigCollectioncollection)
end note
:collection.forEach(config->result.put(config.name(),
config.value()));
:return Collections.unmodifiableMap(result);;
stop
end
start
note right
constructor ReplicationControlManager(SnapshotRegistrysnapshotRegistry,
LogContextlogContext,shortdefaultReplicationFactor,
intdefaultNumPartitions,intmaxElectionsPerImbalance,
ConfigurationControlManagerconfigurationControl,
ClusterControlManagerclusterControl,ControllerMetricscontrollerMetrics,
Optional<CreateTopicPolicy>createTopicPolicy,
FeatureControlManagerfeatureControl)
end note
:this.snapshotRegistry=snapshotRegistry;
:this.log=logContext.logger(ReplicationControlManager.class );
:this.defaultReplicationFactor=defaultReplicationFactor;
:this.defaultNumPartitions=defaultNumPartitions;
:this.maxElectionsPerImbalance=maxElectionsPerImbalance;
:this.configurationControl=configurationControl;
:this.controllerMetrics=controllerMetrics;
:this.createTopicPolicy=createTopicPolicy;
:this.featureControl=featureControl;
:this.clusterControl=clusterControl;
:this.globalPartitionCount=new TimelineInteger(snapshotRegistry);
:this.topicsByName=new TimelineHashMap<>(snapshotRegistry,0);
:this.topicsWithCollisionChars=new TimelineHashMap<>(snapshotRegistry,0);
:this.topics=new TimelineHashMap<>(snapshotRegistry,0);
:this.brokersToIsrs=new BrokersToIsrs(snapshotRegistry);
:this.reassigningTopics=new TimelineHashMap<>(snapshotRegistry,0);
:this.imbalancedPartitions=new TimelineHashSet<>(snapshotRegistry,0);
end
start
note right
void replay(TopicRecordrecord)
end note
:topicsByName.put(record.name(),record.topicId());
if (Topic.hasCollisionChars(record.name())) then (yes) 
if (topicNames==null ) then (yes) 
:topicNames=new TimelineHashSet<>(snapshotRegistry,1);
:topicsWithCollisionChars.put(normalizedName,
topicNames);
endif 
:topicNames.add(record.name());
endif 
:topics.put(record.topicId(),new TopicControlInfo(record.name(),
snapshotRegistry,record.topicId()));
:controllerMetrics.setGlobalTopicsCount(topics.size());
:log.info("Created topic {} with topic ID {}.",
record.name(),record.topicId());
end
start
note right
void replay(PartitionRecordrecord)
end note
if (topicInfo==null ) then (yes) 
:throw new RuntimeException("Tried to create partition "+
record.topicId()+":"+record.partitionId()+
", but no topic with that ID was found.");;
endif 
if (prevPartInfo==null ) then (yes) 
:log.info("Created partition {} and {}.",
description,newPartInfo);
:topicInfo.parts.put(record.partitionId(),
newPartInfo);
:brokersToIsrs.update(record.topicId(),record.partitionId(),
null ,newPartInfo.isr,NO_LEADER,newPartInfo.leader);
:globalPartitionCount.increment();
:controllerMetrics.setGlobalPartitionCount(globalPartitionCount.get());
:updateReassigningTopicsIfNeeded(record.topicId(),
record.partitionId(),false,newPartInfo.isReassigning());
endif 
if (newPartInfo.hasPreferredLeader()) then (yes) 
:imbalancedPartitions.remove(new TopicIdPartition(record.topicId(),
record.partitionId()));
else (no)
:imbalancedPartitions.add(new TopicIdPartition(record.topicId(),
record.partitionId()));
endif 
:controllerMetrics.setOfflinePartitionCount(brokersToIsrs.offlinePartitionCount());
:controllerMetrics.setPreferredReplicaImbalanceCount(imbalancedPartitions.size());
end
start
note right
void updateReassigningTopicsIfNeeded(UuidtopicId,
intpartitionId,booleanwasReassigning,booleanisReassigning)
end note
if (!wasReassigning) then (yes) 
if (isReassigning) then (yes) 
:reassigningTopics.put(topicId,Replicas.copyWith(prevReassigningParts,
partitionId));
endif 
endif 
end
start
note right
void replay(PartitionChangeRecordrecord)
end note
if (topicInfo==null ) then (yes) 
:throw new RuntimeException("Tried to create partition "+
record.topicId()+":"+record.partitionId()+
", but no topic with that ID was found.");;
endif 
if (prevPartitionInfo==null ) then (yes) 
:throw new RuntimeException("Tried to create partition "+
record.topicId()+":"+record.partitionId()+
", but no partition with that id was found.");;
endif 
:updateReassigningTopicsIfNeeded(record.topicId(),
record.partitionId(),prevPartitionInfo.isReassigning(),
newPartitionInfo.isReassigning());
:topicInfo.parts.put(record.partitionId(),
newPartitionInfo);
:brokersToIsrs.update(record.topicId(),record.partitionId(),
prevPartitionInfo.isr,newPartitionInfo.isr,
prevPartitionInfo.leader,newPartitionInfo.leader);
:newPartitionInfo.maybeLogPartitionChange(log,
topicPart,prevPartitionInfo);
if (newPartitionInfo.hasPreferredLeader()) then (yes) 
:imbalancedPartitions.remove(new TopicIdPartition(record.topicId(),
record.partitionId()));
else (no)
:imbalancedPartitions.add(new TopicIdPartition(record.topicId(),
record.partitionId()));
endif 
:controllerMetrics.setOfflinePartitionCount(brokersToIsrs.offlinePartitionCount());
:controllerMetrics.setPreferredReplicaImbalanceCount(imbalancedPartitions.size());
if (record.removingReplicas()!=null ||record.addingReplicas()!=null ) then (yes) 
:log.info("Replayed partition assignment change {} for topic {}",
record,topicInfo.name);
endif 
end
start
note right
void replay(RemoveTopicRecordrecord)
end note
if (topic==null ) then (yes) 
:throw new UnknownTopicIdException("Can't find topic with ID "+
record.topicId()+" to remove.");;
endif 
:topicsByName.remove(topic.name);
if (Topic.hasCollisionChars(topic.name)) then (yes) 
if (colliding!=null ) then (yes) 
:colliding.remove(topic.name);
if (colliding.isEmpty()) then (yes) 
:topicsWithCollisionChars.remove(topic.name);
endif 
endif 
endif 
:reassigningTopics.remove(record.topicId());
:configurationControl.deleteTopicConfigs(topic.name);
:i++;
:brokersToIsrs.removeTopicEntryForBroker(topic.id,
partition.isr[i]);
:imbalancedPartitions.remove(new TopicIdPartition(record.topicId(),
partitionId));
:globalPartitionCount.decrement();
:brokersToIsrs.removeTopicEntryForBroker(topic.id,
NO_LEADER);
:controllerMetrics.setGlobalTopicsCount(topics.size());
:controllerMetrics.setGlobalPartitionCount(globalPartitionCount.get());
:controllerMetrics.setOfflinePartitionCount(brokersToIsrs.offlinePartitionCount());
:controllerMetrics.setPreferredReplicaImbalanceCount(imbalancedPartitions.size());
:log.info("Removed topic {} with ID {}.",
topic.name,record.topicId());
end
start
note right
ControllerResult<CreateTopicsResponseData>createTopics(CreateTopicsRequestDatarequest,
Set<String>describable)
end note
:validateNewTopicNames(topicErrors,request.topics(),
topicsWithCollisionChars);
:request.topics().stream().filter(creatableTopic->topicsByName.containsKey(creatableTopic.name()))
.forEach(t->topicErrors.put(t.name(),new ApiError(Errors.TOPIC_ALREADY_EXISTS,"Topic '"+
t.name()+"' already exists.")));
if (entry.getValue().isFailure()) then (yes) 
:topicErrors.put(entry.getKey().name(),entry.getValue());
endif 
:records.addAll(configResult.records());
if (topicErrors.containsKey(topic.name())) then (yes) 
endif 
:error=createTopic(topic,records,successes,
describable.contains(topic.name()));
:error=ApiError.fromThrowable(e);
if (error.isFailure()) then (yes) 
:topicErrors.put(topic.name(),error);
endif 
if (error!=null ) then (yes) 
:data.topics().add(new CreatableTopicResult()
.setName(topic.name()).setErrorCode(error.error()
.code()).setErrorMessage(error.message()));
:resultsBuilder.append(resultsPrefix).append(topic)
.append(": ").append(error.error()).append(" (")
.append(error.message()).append(")");
:resultsPrefix=", ";
endif 
:data.topics().add(result);
:resultsBuilder.append(resultsPrefix).append(topic)
.append(": ").append("SUCCESS");
:resultsPrefix=", ";
if (request.validateOnly()) then (yes) 
:log.info("Validate-only CreateTopics result(s): {}",
resultsBuilder.toString());
:return ControllerResult.atomicOf(Collections.emptyList(),
data);;
stop
else (no)
:log.info("CreateTopics result(s): {}",resultsBuilder.toString());
:return ControllerResult.atomicOf(records,
data);;
stop
endif 
end
start
note right
ApiErrorcreateTopic(CreatableTopictopic,List<ApiMessageAndVersion>records,
Map<String,CreatableTopicResult>successes,
booleanauthorizedToReturnConfigs)
end note
if (!topic.assignments().isEmpty()) then (yes) 
if (topic.replicationFactor()!=-1) then (yes) 
:return new ApiError(INVALID_REQUEST,"A manual partition assignment was specified, but replication "+
"factor was not set to -1.");;
stop
endif 
if (topic.numPartitions()!=-1) then (yes) 
:return new ApiError(INVALID_REQUEST,"A manual partition assignment was specified, but numPartitions "+
"was not set to -1.");;
stop
endif 
if (newParts.containsKey(assignment.partitionIndex())) then (yes) 
:return new ApiError(Errors.INVALID_REPLICA_ASSIGNMENT,"Found multiple manual partition assignments for partition "+
assignment.partitionIndex());;
stop
endif 
:validateManualPartitionAssignment(assignment.brokerIds(),
replicationFactor);
:replicationFactor=OptionalInt.of(assignment.brokerIds()
.size());
if (isr.isEmpty()) then (yes) 
:return new ApiError(Errors.INVALID_REPLICA_ASSIGNMENT,"All brokers specified in the manual partition assignment for "+
"partition "+assignment.partitionIndex()+
" are fenced or in controlled shutdown.");;
stop
endif 
:newParts.put(assignment.partitionIndex(),
new PartitionRegistration(Replicas.toArray(assignment.brokerIds()),
Replicas.toArray(isr),Replicas.NONE,Replicas.NONE,
isr.get(0),LeaderRecoveryState.RECOVERED,
0,0));
:newParts.entrySet().forEach(e->assignments.put(e.getKey(),
Replicas.toList(e.getValue().replicas)));
:return new CreateTopicPolicy.RequestMetadata(topic.name(),
null ,null ,assignments,creationConfigs);;
stop
if (error.isFailure()) then (yes) 
:return error;;
stop
endif 
else (no)
:partitionId++;
if (isr.isEmpty()) then (yes) 
:return new ApiError(Errors.INVALID_REPLICATION_FACTOR,"Unable to replicate the partition "+
replicationFactor+" time(s): All brokers are currently fenced or in controlled shutdown.");;
stop
endif 
:newParts.put(partitionId,new PartitionRegistration(Replicas.toArray(replicas),
Replicas.toArray(isr),Replicas.NONE,Replicas.NONE,
isr.get(0),LeaderRecoveryState.RECOVERED,
0,0));
:return new ApiError(Errors.INVALID_REPLICATION_FACTOR,"Unable to replicate the partition "+
replicationFactor+" time(s): "+e.getMessage());;
stop
if (error.isFailure()) then (yes) 
:return error;;
stop
endif 
endif 
if (authorizedToReturnConfigs) then (yes) 
:configNames.sort(String::compareTo);
:result.configs().add(new CreateTopicsResponseData.CreatableTopicConfigs()
.setName(entry.name()).setValue(entry.isSensitive()? null :entry.value())
.setReadOnly(entry.isReadOnly()).setConfigSource(KafkaConfigSchema.translateConfigSource(entry.source())
.id()).setIsSensitive(entry.isSensitive()));
:result.setNumPartitions(newParts.size());
:result.setReplicationFactor((short)newParts.get(0)
.replicas.length);
:result.setTopicConfigErrorCode(NONE.code());
else (no)
:result.setTopicConfigErrorCode(TOPIC_AUTHORIZATION_FAILED.code());
endif 
:successes.put(topic.name(),result);
:records.add(new ApiMessageAndVersion(new TopicRecord().setName(topic.name()).setTopicId(topicId),
TOPIC_RECORD.highestSupportedVersion()));
:records.add(info.toRecord(topicId,partitionIndex));
:return ApiError.NONE;;
stop
end
start
note right
ApiErrormaybeCheckCreateTopicPolicy(Supplier<CreateTopicPolicy.RequestMetadata>supplier)
end note
if (createTopicPolicy.isPresent()) then (yes) 
:createTopicPolicy.get().validate(supplier.get());
:return new ApiError(Errors.POLICY_VIOLATION,
e.getMessage());;
stop
endif 
:return ApiError.NONE;;
stop
end
start
note right
void validateNewTopicNames(Map<String,ApiError>topicErrors,
CreatableTopicCollectiontopics,Map<String,
?  extends Set<String>>topicsWithCollisionChars)
end note
if (topicErrors.containsKey(topic.name())) then (yes) 
endif 
:Topic.validate(topic.name());
:topicErrors.put(topic.name(),new ApiError(Errors.INVALID_TOPIC_EXCEPTION,
e.getMessage()));
if (Topic.hasCollisionChars(topic.name())) then (yes) 
if (colliding!=null ) then (yes) 
:topicErrors.put(topic.name(),new ApiError(Errors.INVALID_TOPIC_EXCEPTION,"Topic '"+
topic.name()+"' collides with existing topic: "+
colliding.iterator().next()));
endif 
endif 
end
start
note right
Map<ConfigResource,Map<String,Entry<OpType,
String>>>computeConfigChanges(Map<String,
ApiError>topicErrors,CreatableTopicCollectiontopics)
end note
if (topicErrors.containsKey(topic.name())) then (yes) 
endif 
if (config.value()==null ) then (yes) 
:nullConfigs.add(config.name());
else (no)
:topicConfigs.put(config.name(),new SimpleImmutableEntry<>(SET,
config.value()));
endif 
if (!nullConfigs.isEmpty()) then (yes) 
:topicErrors.put(topic.name(),new ApiError(Errors.INVALID_CONFIG,"Null value not supported for topic configs: "+
String.join(",",nullConfigs)));
endif 
:return configChanges;;
stop
end
start
note right
Map<String,ResultOrError<Uuid>>findTopicIds(longoffset,
Collection<String>names)
end note
if (name==null ) then (yes) 
:results.put(null ,new ResultOrError<>(INVALID_REQUEST,
"Invalid null topic name."));
else (no)
if (id==null ) then (yes) 
:results.put(name,new ResultOrError<>(new ApiError(UNKNOWN_TOPIC_OR_PARTITION)));
else (no)
:results.put(name,new ResultOrError<>(id));
endif 
endif 
:return results;;
stop
end
start
note right
Map<String,Uuid>findAllTopicIds(longoffset)
end note
:result.put(entry.getKey(),entry.getValue());
:return result;;
stop
end
start
note right
Map<Uuid,ResultOrError<String>>findTopicNames(longoffset,
Collection<Uuid>ids)
end note
if (id==null ||id.equals(Uuid.ZERO_UUID)) then (yes) 
:results.put(id,new ResultOrError<>(new ApiError(INVALID_REQUEST,"Attempt to find topic with invalid topicId "+
id)));
else (no)
if (topic==null ) then (yes) 
:results.put(id,new ResultOrError<>(new ApiError(UNKNOWN_TOPIC_ID)));
else (no)
:results.put(id,new ResultOrError<>(topic.name));
endif 
endif 
:return results;;
stop
end
start
note right
ControllerResult<Map<Uuid,ApiError>>deleteTopics(Collection<Uuid>ids)
end note
:deleteTopic(id,records);
:results.put(id,ApiError.NONE);
:results.put(id,ApiError.fromThrowable(e));
:log.error("Unexpected deleteTopics error for {}",
id,e);
:results.put(id,ApiError.fromThrowable(e));
:return ControllerResult.atomicOf(records,
results);;
stop
end
start
note right
void deleteTopic(Uuidid,List<ApiMessageAndVersion>records)
end note
if (topic==null ) then (yes) 
:throw new UnknownTopicIdException(UNKNOWN_TOPIC_ID.message());;
endif 
:records.add(new ApiMessageAndVersion(new RemoveTopicRecord().setTopicId(id),
REMOVE_TOPIC_RECORD.highestSupportedVersion()));
end
start
note right
PartitionRegistrationgetPartition(UuidtopicId,
intpartitionId)
end note
if (topic==null ) then (yes) 
:return null ;;
stop
endif 
:return topic.parts.get(partitionId);;
stop
end
start
note right
TopicControlInfogetTopic(UuidtopicId)
end note
:return topics.get(topicId);;
stop
end
start
note right
UuidgetTopicId(Stringname)
end note
:return topicsByName.get(name);;
stop
end
start
note right
BrokersToIsrsbrokersToIsrs()
end note
:return brokersToIsrs;;
stop
end
start
note right
Set<TopicIdPartition>imbalancedPartitions()
end note
:return new HashSet<>(imbalancedPartitions);;
stop
end
start
note right
ControllerResult<AlterPartitionResponseData>alterPartition(ControllerRequestContextcontext,
AlterPartitionRequestDatarequest)
end note
:clusterControl.checkBrokerEpoch(request.brokerId(),
request.brokerEpoch());
:response.topics().add(responseTopicData);
if (topicId==null ||topicId.equals(Uuid.ZERO_UUID)||!topics.containsKey(topicId)) then (yes) 
:responseTopicData.partitions().add(new AlterPartitionResponseData.PartitionData()
.setPartitionIndex(partitionData.partitionIndex())
.setErrorCode(error.code()));
:log.info("Rejecting AlterPartition request for unknown topic ID {} or name {}.",
topicData.topicId(),topicData.topicName());
endif 
if (validationError!=Errors.NONE) then (yes) 
:responseTopicData.partitions().add(new AlterPartitionResponseData.PartitionData()
.setPartitionIndex(partitionId).setErrorCode(validationError.code()));
endif 
if (configurationControl.uncleanLeaderElectionEnabledForTopic(topic.name())) then (yes) 
:builder.setElection(PartitionChangeBuilder.Election.UNCLEAN);
endif 
:builder.setTargetIsr(partitionData.newIsr());
:builder.setTargetLeaderRecoveryState(LeaderRecoveryState.of(partitionData.leaderRecoveryState()));
if (record.isPresent()) then (yes) 
:records.add(record.get());
:partition=partition.merge(change);
if (log.isDebugEnabled()) then (yes) 
:log.debug("Node {} has altered ISR for {}-{} to {}.",
request.brokerId(),topic.name,partitionId,
change.isr());
endif 
if (change.leader()!=request.brokerId()&&change.leader()!=NO_LEADER_CHANGE) then (yes) 
:log.info("AlterPartition request from node {} for {}-{} completed "+
"the ongoing partition reassignment and triggered a "+
"leadership change. Returning {}.",request.brokerId(),
topic.name,partitionId,error);
:responseTopicData.partitions().add(new AlterPartitionResponseData.PartitionData()
.setPartitionIndex(partitionId).setErrorCode(error.code()));
endif 
endif 
:responseTopicData.partitions().add(new AlterPartitionResponseData.PartitionData()
.setPartitionIndex(partitionId).setErrorCode(Errors.NONE.code())
.setLeaderId(partition.leader).setIsr(Replicas.toList(partition.isr))
.setLeaderRecoveryState(partition.leaderRecoveryState.value())
.setLeaderEpoch(partition.leaderEpoch).setPartitionEpoch(partition.partitionEpoch));
:return ControllerResult.of(records,response);;
stop
end
start
note right
ErrorsvalidateAlterPartitionData(intbrokerId,
TopicControlInfotopic,intpartitionId,PartitionRegistrationpartition,
shortrequestApiVersion,AlterPartitionRequestData.PartitionDatapartitionData)
end note
if (partition==null ) then (yes) 
:log.info("Rejecting AlterPartition request for unknown partition {}-{}.",
topic.name,partitionId);
:return UNKNOWN_TOPIC_OR_PARTITION;;
stop
endif 
if (partitionData.leaderEpoch()!=partition.leaderEpoch) then (yes) 
:log.debug("Rejecting AlterPartition request from node {} for {}-{} because "+
"the current leader epoch is {}, not {}.",
brokerId,topic.name,partitionId,partition.leaderEpoch,
partitionData.leaderEpoch());
:return FENCED_LEADER_EPOCH;;
stop
endif 
if (brokerId!=partition.leader) then (yes) 
:log.info("Rejecting AlterPartition request from node {} for {}-{} because "+
"the current leader is {}.",brokerId,topic.name,
partitionId,partition.leader);
:return INVALID_REQUEST;;
stop
endif 
if (partitionData.partitionEpoch()!=partition.partitionEpoch) then (yes) 
:log.info("Rejecting AlterPartition request from node {} for {}-{} because "+
"the current partition epoch is {}, not {}.",
brokerId,topic.name,partitionId,partition.partitionEpoch,
partitionData.partitionEpoch());
:return INVALID_UPDATE_VERSION;;
stop
endif 
if (!Replicas.validateIsr(partition.replicas,
newIsr)) then (yes) 
:log.error("Rejecting AlterPartition request from node {} for {}-{} because "+
"it specified an invalid ISR {}.",brokerId,
topic.name,partitionId,partitionData.newIsr());
:return INVALID_REQUEST;;
stop
endif 
if (!Replicas.contains(newIsr,partition.leader)) then (yes) 
:log.error("Rejecting AlterPartition request from node {} for {}-{} because "+
"it specified an invalid ISR {} that doesn't include itself.",
brokerId,topic.name,partitionId,partitionData.newIsr());
:return INVALID_REQUEST;;
stop
endif 
if (leaderRecoveryState==LeaderRecoveryState.RECOVERING&&newIsr.length>1) then (yes) 
:log.info("Rejecting AlterPartition request from node {} for {}-{} because "+
"the ISR {} had more than one replica while the leader was still "+
"recovering from an unclean leader election {}.",
brokerId,topic.name,partitionId,partitionData.newIsr(),
leaderRecoveryState);
:return INVALID_REQUEST;;
stop
endif 
if (partition.leaderRecoveryState==LeaderRecoveryState.RECOVERED&&leaderRecoveryState==LeaderRecoveryState.RECOVERING) then (yes) 
:log.info("Rejecting AlterPartition request from node {} for {}-{} because "+
"the leader recovery state cannot change from RECOVERED to RECOVERING.",
brokerId,topic.name,partitionId);
:return INVALID_REQUEST;;
stop
endif 
if (!ineligibleReplicas.isEmpty()) then (yes) 
:log.info("Rejecting AlterPartition request from node {} for {}-{} because "+
"it specified ineligible replicas {} in the new ISR {}.",
brokerId,topic.name,partitionId,ineligibleReplicas,
partitionData.newIsr());
if (requestApiVersion>1) then (yes) 
:return INELIGIBLE_REPLICA;;
stop
else (no)
:return OPERATION_NOT_ATTEMPTED;;
stop
endif 
endif 
:return Errors.NONE;;
stop
end
start
note right
List<IneligibleReplica>ineligibleReplicasForIsr(int[]replicas)
end note
if (registration==null ) then (yes) 
:ineligibleReplicas.add(new IneligibleReplica(replicaId,
"not registered"));
endif 
:return ineligibleReplicas;;
stop
end
start
note right
void handleBrokerFenced(intbrokerId,List<ApiMessageAndVersion>records)
end note
if (brokerRegistration==null ) then (yes) 
:throw new RuntimeException("Can't find broker registration for broker "+
brokerId);;
endif 
:generateLeaderAndIsrUpdates("handleBrokerFenced",
brokerId,NO_LEADER,records,brokersToIsrs.partitionsWithBrokerInIsr(brokerId));
if (featureControl.metadataVersion().isBrokerRegistrationChangeRecordSupported()) then (yes) 
:records.add(new ApiMessageAndVersion(new BrokerRegistrationChangeRecord()
.setBrokerId(brokerId).setBrokerEpoch(brokerRegistration.epoch())
.setFenced(BrokerRegistrationFencingChange.UNFENCE.value()),
(short)0));
else (no)
:records.add(new ApiMessageAndVersion(new FenceBrokerRecord().setId(brokerId).setEpoch(brokerRegistration.epoch()),
(short)0));
endif 
end
start
note right
void handleBrokerUnregistered(intbrokerId,
longbrokerEpoch,List<ApiMessageAndVersion>records)
end note
:generateLeaderAndIsrUpdates("handleBrokerUnregistered",
brokerId,NO_LEADER,records,brokersToIsrs.partitionsWithBrokerInIsr(brokerId));
:records.add(new ApiMessageAndVersion(new UnregisterBrokerRecord().setBrokerId(brokerId).setBrokerEpoch(brokerEpoch),
(short)0));
end
start
note right
void handleBrokerUnfenced(intbrokerId,longbrokerEpoch,
List<ApiMessageAndVersion>records)
end note
if (featureControl.metadataVersion().isBrokerRegistrationChangeRecordSupported()) then (yes) 
:records.add(new ApiMessageAndVersion(new BrokerRegistrationChangeRecord()
.setBrokerId(brokerId).setBrokerEpoch(brokerEpoch).setFenced(BrokerRegistrationFencingChange.FENCE.value()),
(short)0));
else (no)
:records.add(new ApiMessageAndVersion(new UnfenceBrokerRecord().setId(brokerId).setEpoch(brokerEpoch),
(short)0));
endif 
:generateLeaderAndIsrUpdates("handleBrokerUnfenced",
NO_LEADER,brokerId,records,brokersToIsrs.partitionsWithNoLeader());
end
start
note right
void handleBrokerInControlledShutdown(intbrokerId,
longbrokerEpoch,List<ApiMessageAndVersion>records)
end note
if (featureControl.metadataVersion().isInControlledShutdownStateSupported()&&!clusterControl.inControlledShutdown(brokerId)) then (yes) 
:records.add(new ApiMessageAndVersion(new BrokerRegistrationChangeRecord()
.setBrokerId(brokerId).setBrokerEpoch(brokerEpoch)
.setInControlledShutdown(BrokerRegistrationInControlledShutdownChange.IN_CONTROLLED_SHUTDOWN.value()),
(short)1));
endif 
:generateLeaderAndIsrUpdates("enterControlledShutdown["+
brokerId+"]",brokerId,NO_LEADER,records,brokersToIsrs.partitionsWithBrokerInIsr(brokerId));
end
start
note right
ControllerResult<ElectLeadersResponseData>electLeaders(ElectLeadersRequestDatarequest)
end note
if (request.topicPartitions()==null ) then (yes) 
:response.replicaElectionResults().add(topicResults);
if (topic!=null ) then (yes) 
if (error.error()!=Errors.ELECTION_NOT_NEEDED) then (yes) 
:topicResults.partitionResult().add(new PartitionResult()
.setPartitionId(partitionId).setErrorCode(error.error()
.code()).setErrorMessage(error.message()));
endif 
endif 
else (no)
:response.replicaElectionResults().add(topicResults);
:topicResults.partitionResult().add(new PartitionResult()
.setPartitionId(partitionId).setErrorCode(error.error()
.code()).setErrorMessage(error.message()));
endif 
:return ControllerResult.of(records,response);;
stop
end
start
note right
ElectionTypeelectionType(byteelectionType)
end note
:return ElectionType.valueOf(electionType);;
stop
:throw new InvalidRequestException("Unknown election type "+
(int)electionType);;
end
start
note right
ApiErrorelectLeader(Stringtopic,intpartitionId,
ElectionTypeelectionType,List<ApiMessageAndVersion>records)
end note
if (topicId==null ) then (yes) 
:return new ApiError(UNKNOWN_TOPIC_OR_PARTITION,"No such topic as "+
topic);;
stop
endif 
if (topicInfo==null ) then (yes) 
:return new ApiError(UNKNOWN_TOPIC_OR_PARTITION,"No such topic id as "+
topicId);;
stop
endif 
if (partition==null ) then (yes) 
:return new ApiError(UNKNOWN_TOPIC_OR_PARTITION,"No such partition as "+
topic+"-"+partitionId);;
stop
endif 
if ((electionType==ElectionType.PREFERRED&&partition.hasPreferredLeader())||(electionType==ElectionType.UNCLEAN&&partition.hasLeader())) then (yes) 
:return new ApiError(Errors.ELECTION_NOT_NEEDED);;
stop
endif 
if (electionType==ElectionType.UNCLEAN) then (yes) 
:election=PartitionChangeBuilder.Election.UNCLEAN;
endif 
:builder.setElection(election);
if (!record.isPresent()) then (yes) 
if (electionType==ElectionType.PREFERRED) then (yes) 
:return new ApiError(Errors.PREFERRED_LEADER_NOT_AVAILABLE);;
stop
else (no)
:return new ApiError(Errors.ELIGIBLE_LEADERS_NOT_AVAILABLE);;
stop
endif 
endif 
:records.add(record.get());
:return ApiError.NONE;;
stop
end
start
note right
ControllerResult<BrokerHeartbeatReply>processBrokerHeartbeat(BrokerHeartbeatRequestDatarequest,
longlastCommittedOffset)
end note
:clusterControl.checkBrokerEpoch(brokerId,
brokerEpoch);
if (states.current()!=states.next()) then (yes) 
:handleBrokerFenced(brokerId,records);
:handleBrokerUnfenced(brokerId,brokerEpoch,
records);
:handleBrokerInControlledShutdown(brokerId,
brokerEpoch,records);
:handleBrokerFenced(brokerId,records);
endif 
:heartbeatManager.touch(brokerId,states.next().fenced(),
request.currentMetadataOffset());
:return ControllerResult.of(records,reply);;
stop
end
start
note right
ControllerResult<Void>unregisterBroker(intbrokerId)
end note
if (registration==null ) then (yes) 
:throw new BrokerIdNotRegisteredException("Broker ID "+
brokerId+" is not currently registered");;
endif 
:handleBrokerUnregistered(brokerId,registration.epoch(),
records);
:return ControllerResult.of(records,null );;
stop
end
start
note right
ControllerResult<Void>maybeFenceOneStaleBroker()
end note
:heartbeatManager.findOneStaleBroker().ifPresent(brokerId->{log.info("Fencing broker {} because its session has timed out.",brokerId);handleBrokerFenced(brokerId,records);heartbeatManager.fence(brokerId);});
:return ControllerResult.of(records,null );;
stop
end
start
note right
booleanarePartitionLeadersImbalanced()
end note
:return !imbalancedPartitions.isEmpty();;
stop
end
start
note right
ControllerResult<Boolean>maybeBalancePartitionLeaders()
end note
if (records.size()>=maxElectionsPerImbalance) then (yes) 
:rescheduleImmidiately=true;
endif 
if (topic==null ) then (yes) 
:log.error("Skipping unknown imbalanced topic {}",
topicPartition);
endif 
if (partition==null ) then (yes) 
:log.error("Skipping unknown imbalanced partition {}",
topicPartition);
endif 
:builder.setElection(PartitionChangeBuilder.Election.PREFERRED);
:builder.build().ifPresent(records::add);
:return ControllerResult.of(records,rescheduleImmidiately);;
stop
end
start
note right
ControllerResult<List<CreatePartitionsTopicResult>>createPartitions(List<CreatePartitionsTopic>topics)
end note
:createPartitions(topic,records);
:apiError=ApiError.fromThrowable(e);
:log.error("Unexpected createPartitions error for {}",
topic,e);
:apiError=ApiError.fromThrowable(e);
:results.add(new CreatePartitionsTopicResult()
.setName(topic.name()).setErrorCode(apiError.error()
.code()).setErrorMessage(apiError.message()));
:return ControllerResult.atomicOf(records,
results);;
stop
end
start
note right
void createPartitions(CreatePartitionsTopictopic,
List<ApiMessageAndVersion>records)
end note
if (topicId==null ) then (yes) 
:throw new UnknownTopicOrPartitionException();;
endif 
if (topicInfo==null ) then (yes) 
:throw new UnknownTopicOrPartitionException();;
endif 
if (topic.count()==topicInfo.parts.size()) then (yes) 
:throw new InvalidPartitionsException("Topic already has "+
topicInfo.parts.size()+" partition(s).");;
endif 
if (topic.assignments()!=null ) then (yes) 
if (topic.assignments().size()!=additional) then (yes) 
:throw new InvalidReplicaAssignmentException("Attempted to add "+
additional+" additional partition(s), but only "+
topic.assignments().size()+" assignment(s) were specified.");;
endif 
endif 
if (!iterator.hasNext()) then (yes) 
:throw new UnknownServerException("Invalid state: topic "+
topic.name()+" appears to have no partitions.");;
endif 
if (partitionInfo.replicas.length>Short.MAX_VALUE) then (yes) 
:throw new UnknownServerException("Invalid replication factor "+
partitionInfo.replicas.length+": expected a number equal to less than "+
Short.MAX_VALUE);;
endif 
if (topic.assignments()!=null ) then (yes) 
:placements=new ArrayList<>();
:isrs=new ArrayList<>();
:i++;
:validateManualPartitionAssignment(assignment.brokerIds(),
OptionalInt.of(replicationFactor));
:placements.add(assignment.brokerIds());
if (isr.isEmpty()) then (yes) 
:throw new InvalidReplicaAssignmentException("All brokers specified in the manual partition assignment for "+
"partition "+(startPartitionId+i)+" are fenced or in controlled shutdown.");;
endif 
:isrs.add(isr);
else (no)
:placements=clusterControl.replicaPlacer().place(new PlacementSpec(startPartitionId,
additional,replicationFactor),clusterDescriber);
:isrs=placements;
endif 
:i++;
if (isr.isEmpty()) then (yes) 
:throw new InvalidReplicationFactorException("Unable to replicate the partition "+
replicationFactor+" time(s): All brokers are currently fenced or in controlled shutdown.");;
endif 
:records.add(new ApiMessageAndVersion(new PartitionRecord()
.setPartitionId(partitionId).setTopicId(topicId)
.setReplicas(replicas).setIsr(isr).setLeaderRecoveryState(LeaderRecoveryState.RECOVERED.value())
.setRemovingReplicas(Collections.emptyList())
.setAddingReplicas(Collections.emptyList()).setLeader(isr.get(0)).setLeaderEpoch(0).setPartitionEpoch(0),
PARTITION_RECORD.highestSupportedVersion()));
:partitionId++;
end
start
note right
void validateManualPartitionAssignment(List<Integer>assignment,
OptionalIntreplicationFactor)
end note
if (assignment.isEmpty()) then (yes) 
:throw new InvalidReplicaAssignmentException("The manual partition "+
"assignment includes an empty replica list.");;
endif 
:sortedBrokerIds.sort(Integer::compare);
if (!clusterControl.brokerRegistrations()
.containsKey(brokerId)) then (yes) 
:throw new InvalidReplicaAssignmentException("The manual partition "+
"assignment includes broker "+brokerId+", but no such broker is "+
"registered.");;
endif 
if (brokerId.equals(prevBrokerId)) then (yes) 
:throw new InvalidReplicaAssignmentException("The manual partition "+
"assignment includes the broker "+prevBrokerId+
" more than "+"once.");;
endif 
:prevBrokerId=brokerId;
if (replicationFactor.isPresent()&&sortedBrokerIds.size()!=replicationFactor.getAsInt()) then (yes) 
:throw new InvalidReplicaAssignmentException("The manual partition "+
"assignment includes a partition with "+sortedBrokerIds.size()+
" replica(s), but this is not consistent with previous "+
"partitions, which have "+replicationFactor.getAsInt()+
" replica(s).");;
endif 
end
start
note right
void generateLeaderAndIsrUpdates(Stringcontext,
intbrokerToRemove,intbrokerToAdd,List<ApiMessageAndVersion>records,
Iterator<TopicIdPartition>iterator)
end note
while (iterator.hasNext())
if (topic==null ) then (yes) 
:throw new RuntimeException("Topic ID "+topicIdPart.topicId()+
" existed in isrMembers, but not in the topics map.");;
endif 
if (partition==null ) then (yes) 
:throw new RuntimeException("Partition "+
topicIdPart+" existed in isrMembers, but not in the partitions map.");;
endif 
if (configurationControl.uncleanLeaderElectionEnabledForTopic(topic.name)) then (yes) 
:builder.setElection(PartitionChangeBuilder.Election.UNCLEAN);
endif 
:builder.setTargetIsr(Replicas.toList(Replicas.copyWithout(partition.isr,
brokerToRemove)));
:builder.build().ifPresent(records::add);
endwhile
if (records.size()!=oldSize) then (yes) 
if (log.isDebugEnabled()) then (yes) 
:ListIterator<ApiMessageAndVersion>iter=records.listIterator(oldSize);
:bld.append(prefix).append(topics.get(record.topicId())
.name).append("-").append(record.partitionId());
:prefix=", ";
:log.debug("{}: changing partition(s): {}",
context,bld.toString());
endif 
endif 
end
start
note right
ControllerResult<AlterPartitionReassignmentsResponseData>alterPartitionReassignments(AlterPartitionReassignmentsRequestDatarequest)
end note
:alterPartitionReassignment(topic.name(),
partition,records);
:successfulAlterations++;
:log.info("Unable to alter partition reassignment for "+
topic.name()+":"+partition.partitionIndex()+
" because "+"of an "+e.getClass().getSimpleName()+
" error: "+e.getMessage());
:error=ApiError.fromThrowable(e);
:totalAlterations++;
:topicResponse.partitions().add(new ReassignablePartitionResponse()
.setPartitionIndex(partition.partitionIndex())
.setErrorCode(error.error().code()).setErrorMessage(error.message()));
:result.responses().add(topicResponse);
:log.info("Successfully altered {} out of {} partition reassignment(s).",
successfulAlterations,totalAlterations);
:return ControllerResult.atomicOf(records,
result);;
stop
end
start
note right
void alterPartitionReassignment(StringtopicName,
ReassignablePartitiontarget,List<ApiMessageAndVersion>records)
end note
if (topicId==null ) then (yes) 
:throw new UnknownTopicOrPartitionException("Unable to find a topic "+
"named "+topicName+".");;
endif 
if (topicInfo==null ) then (yes) 
:throw new UnknownTopicOrPartitionException("Unable to find a topic "+
"with ID "+topicId+".");;
endif 
if (part==null ) then (yes) 
:throw new UnknownTopicOrPartitionException("Unable to find partition "+
topicName+":"+target.partitionIndex()+".");;
endif 
if (target.replicas()==null ) then (yes) 
:record=cancelPartitionReassignment(topicName,
tp,part);
else (no)
:record=changePartitionReassignment(tp,part,
target);
endif 
:record.ifPresent(records::add);
end
start
note right
Optional<ApiMessageAndVersion>cancelPartitionReassignment(StringtopicName,
TopicIdPartitiontp,PartitionRegistrationpart)
end note
if (!part.isReassigning()) then (yes) 
:throw new NoReassignmentInProgressException(NO_REASSIGNMENT_IN_PROGRESS.message());;
endif 
if (revert.unclean()) then (yes) 
if (!configurationControl.uncleanLeaderElectionEnabledForTopic(topicName)) then (yes) 
:throw new InvalidReplicaAssignmentException("Unable to revert partition "+
"assignment for "+topicName+":"+tp.partitionId()+
" because "+"it would require an unclean leader election.");;
endif 
endif 
if (configurationControl.uncleanLeaderElectionEnabledForTopic(topicName)) then (yes) 
:builder.setElection(PartitionChangeBuilder.Election.UNCLEAN);
endif 
:builder.setTargetIsr(revert.isr()).setTargetReplicas(revert.replicas())
.setTargetRemoving(Collections.emptyList())
.setTargetAdding(Collections.emptyList());
:return builder.build();;
stop
end
start
note right
Optional<ApiMessageAndVersion>changePartitionReassignment(TopicIdPartitiontp,
PartitionRegistrationpart,ReassignablePartitiontarget)
end note
:validateManualPartitionAssignment(target.replicas(),
OptionalInt.empty());
if (!reassignment.merged().equals(currentReplicas)) then (yes) 
:builder.setTargetReplicas(reassignment.merged());
endif 
if (!reassignment.removing().isEmpty()) then (yes) 
:builder.setTargetRemoving(reassignment.removing());
endif 
if (!reassignment.adding().isEmpty()) then (yes) 
:builder.setTargetAdding(reassignment.adding());
endif 
:return builder.build();;
stop
end
start
note right
ListPartitionReassignmentsResponseDatalistPartitionReassignments(List<ListPartitionReassignmentsTopics>topicList)
end note
if (topicList==null ) then (yes) 
:listReassigningTopic(response,entry.getKey(),
Replicas.toList(entry.getValue()));
else (no)
if (topicId!=null ) then (yes) 
:listReassigningTopic(response,topicId,topic.partitionIndexes());
endif 
endif 
:return response;;
stop
end
start
note right
void listReassigningTopic(ListPartitionReassignmentsResponseDataresponse,
UuidtopicId,List<Integer>partitionIds)
end note
if (topicInfo==null ) then (yes) 
:return ;;
stop
endif 
if (ongoing.isPresent()) then (yes) 
:ongoingTopic.partitions().add(ongoing.get());
endif 
if (!ongoingTopic.partitions().isEmpty()) then (yes) 
:response.topics().add(ongoingTopic);
endif 
end
start
note right
Optional<OngoingPartitionReassignment>getOngoingPartitionReassignment(TopicControlInfotopicInfo,
intpartitionId)
end note
if (partition==null ||!partition.isReassigning()) then (yes) 
:return Optional.empty();;
stop
endif 
:return Optional.of(new OngoingPartitionReassignment()
.setAddingReplicas(Replicas.toList(partition.addingReplicas))
.setRemovingReplicas(Replicas.toList(partition.removingReplicas))
.setPartitionIndex(partitionId).setReplicas(Replicas.toList(partition.replicas)));;
stop
end
start
note right
constructor ReplicationControlIterator(longepoch)
end note
:this.epoch=epoch;
:this.iterator=topics.values(epoch).iterator();
end
start
note right
booleanhasNext()
end note
:return iterator.hasNext();;
stop
end
start
note right
List<ApiMessageAndVersion>next()
end note
if (!hasNext()) then (yes) 
:throw new NoSuchElementException();;
endif 
:records.add(new ApiMessageAndVersion(new TopicRecord().setName(topic.name).setTopicId(topic.id),
TOPIC_RECORD.highestSupportedVersion()));
:records.add(entry.getValue().toRecord(topic.id,
entry.getKey()));
:return records;;
stop
end
start
note right
ReplicationControlIteratoriterator(longepoch)
end note
:return new ReplicationControlIterator(epoch);;
stop
end
start
note right
constructor IneligibleReplica(intreplicaId,
Stringreason)
end note
:this.replicaId=replicaId;
:this.reason=reason;
end
start
note right
StringtoString()
end note
:return replicaId+" ("+reason+")";;
stop
end
@enduml

PlantUML version 1.2022.7(Mon Aug 22 17:01:30 UTC 2022)
(GPL source distribution)
Java Runtime: OpenJDK Runtime Environment
JVM: OpenJDK 64-Bit Server VM
Default Encoding: UTF-8
Language: en
Country: null
--></g></svg>