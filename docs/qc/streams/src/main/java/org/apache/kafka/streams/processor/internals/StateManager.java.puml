@startuml
!pragma layout smetana
java.lang.Object <|-- org.apache.kafka.streams.processor.internals.StateManager
class org.apache.kafka.streams.processor.internals.StateManager {
+  baseDir()
+  registerStore(StateStore;StateRestoreCallback;CommitCallback)
+  getStore(String)
+  flush()
+  updateChangelogOffsets(Map<TopicPartition,Long>)
+  checkpoint()
+  changelogOffsets()
+  close()
+  taskType()
+  changelogFor(String)
+  getGlobalStore(String)
} 
@enduml
