@startuml
!pragma layout smetana
Comparable_BufferKey_ <|-- org.apache.kafka.streams.state.internals.BufferKey
class org.apache.kafka.streams.state.internals.BufferKey {
+  BufferKey(long;Bytes)
+  time()
+  key()
+  equals(Object)
+  hashCode()
+  compareTo(BufferKey)
+  toString()
} 
@enduml
