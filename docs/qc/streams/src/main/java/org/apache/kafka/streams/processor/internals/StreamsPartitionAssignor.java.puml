@startuml
!pragma layout smetana
ConsumerPartitionAssignor <|-- org.apache.kafka.streams.processor.internals.StreamsPartitionAssignor
class org.apache.kafka.streams.processor.internals.StreamsPartitionAssignor {
+  configure(Map<String,?>)
+  name()
+  supportedProtocols()
+  subscriptionUserData(Set<String>)
+  errorAssignment(Map<UUID,ClientMetadata>;int)
+  assign(Cluster;GroupSubscription)
+  checkMetadataVersions(int;int;int)
+  prepareRepartitionTopics(Cluster)
+  populateTasksForMaps(Map<TopicPartition,TaskId>;Map<Subtopology,Set<TaskId>>;Set<String>;Map<TaskId,Set<TopicPartition>>;Cluster)
+  checkAllPartitions(Set<String>;Map<TaskId,Set<TopicPartition>>;Set<TopicPartition>;Cluster)
+  assignTasksToClients(Cluster;Set<String>;Map<Subtopology,TopicsInfo>;Map<UUID,ClientMetadata>;Map<TaskId,Set<TopicPartition>>;Set<TaskId>)
+  createTaskAssignor(boolean)
+  populateClientStatesMap(Map<UUID,ClientState>;Map<UUID,ClientMetadata>;Map<TopicPartition,TaskId>;ChangelogTopics)
+  computeEndOffsetSumsByTask(Map<TopicPartition,ListOffsetsResultInfo>;Map<TopicPartition,Long>;ChangelogTopics)
+  populatePartitionsByHostMaps(Map<HostInfo,Set<TopicPartition>>;Map<HostInfo,Set<TopicPartition>>;Map<TaskId,Set<TopicPartition>>;Map<UUID,ClientMetadata>)
+  computeNewAssignment(Set<TaskId>;Map<UUID,ClientMetadata>;Map<TaskId,Set<TopicPartition>>;Map<HostInfo,Set<TopicPartition>>;Map<HostInfo,Set<TopicPartition>>;Set<TopicPartition>;int;int;boolean;boolean)
+  addClientAssignments(Set<TaskId>;Map<String,Assignment>;ClientMetadata;Map<TaskId,Set<TopicPartition>>;Map<HostInfo,Set<TopicPartition>>;Map<HostInfo,Set<TopicPartition>>;Set<TopicPartition>;Map<String,List<TaskId>>;Map<String,List<TaskId>>;int;int;boolean)
+  populateActiveTaskAndPartitionsLists(List<TopicPartition>;List<TaskId>;String;ClientState;List<TaskId>;Map<TaskId,Set<TopicPartition>>;Set<TopicPartition>)
+  buildStandbyTaskMap(String;Iterable<TaskId>;Iterable<TaskId>;Set<TaskId>;Map<TaskId,Set<TopicPartition>>;ClientState)
+  assignTasksToThreads(Collection<TaskId>;boolean;SortedSet<String>;ClientState;Map<String,Integer>)
+  validateMetadataVersions(int;int)
+  maybeUpdateSubscriptionVersion(int;int)
+  onAssignment(Assignment;ConsumerGroupMetadata)
+  maybeScheduleFollowupRebalance(long;int;int;Set<HostInfo>)
+  verifyHostInfo(Set<HostInfo>)
+  getActiveTasks(List<TopicPartition>;AssignmentInfo)
+  getTopicPartitionInfo(Map<HostInfo,Set<TopicPartition>>)
+  validateActiveTaskEncoding(List<TopicPartition>;AssignmentInfo;String)
+  updateMinReceivedVersion(int;int)
+  updateMinSupportedVersion(int;int)
+  setInternalTopicManager(InternalTopicManager)
+  rebalanceProtocol()
+  userEndPoint()
+  taskManager()
+  uniqueField()
+  clientTags()
+  handleRebalanceStart(Set<String>)
+  acceptableRecoveryLag()
+  maxWarmupReplicas()
+  numStandbyReplicas()
+  probingRebalanceIntervalMs()
} 
@enduml
