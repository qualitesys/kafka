@startuml
!pragma layout smetana
AbstractTaskProcessorNodePunctuator <|-- org.apache.kafka.streams.processor.internals.StreamTask
class org.apache.kafka.streams.processor.internals.StreamTask {
+  StreamTask(TaskId;Set<TopicPartition>;ProcessorTopology;Consumer<byte[],byte[]>;TaskConfig;StreamsMetricsImpl;StateDirectory;ThreadCache;Time;ProcessorStateManager;RecordCollector;InternalProcessorContext;LogContext)
+  createPartitionQueues()
+  isActive()
+  initializeIfNeeded()
+  addPartitionsForOffsetReset(Set<TopicPartition>)
+  completeRestoration(java.util.function.Consumer<Set<TopicPartition>>)
+  suspend()
+  closeTopology()
+  resume()
+  prepareCommit()
+  findOffset(TopicPartition)
+  committableOffsetsAndMetadata()
+  postCommit(boolean)
+  clearCommitStatuses()
+  extractPartitionTimes()
+  closeClean()
+  closeDirty()
+  updateInputPartitions(Set<TopicPartition>;Map<String,List<String>>)
+  closeCleanAndRecycleState()
+  maybeWriteCheckpoint(boolean)
+  validateClean()
+  removeAllSensors()
+  close(boolean)
+  isProcessable(long)
+  process(long)
+  doProcess(long)
+  recordProcessBatchTime(long)
+  recordProcessTimeRatioAndBufferSize(long;long)
+  getStacktraceString(RuntimeException)
+  punctuate(ProcessorNode<?,?,?,?>;long;PunctuationType;Punctuator)
+  updateProcessorContext(ProcessorNode<?,?,?,?>;long;ProcessorRecordContext)
+  checkpointableOffsets()
+  resetOffsetsIfNeededAndInitializeMetadata(java.util.function.Consumer<Set<TopicPartition>>)
+  initializeTaskTimeAndProcessorMetadata(Map<TopicPartition,OffsetAndMetadata>)
+  purgeableOffsets()
+  initializeTopology()
+  addRecords(TopicPartition;Iterable<ConsumerRecord<byte[],byte[]>>)
+  schedule(long;PunctuationType;Punctuator)
+  schedule(long;long;PunctuationType;Punctuator)
+  maybePunctuateStreamTime()
+  maybePunctuateSystemTime()
+  maybeRecordE2ELatency(long;long;String)
+  requestCommit()
+  commitRequested()
+  processorContext()
+  toString()
+  toString(String)
+  commitNeeded()
+  changelogOffsets()
+  committedOffsets()
+  highWaterMark()
+  transitToSuspend()
+  timeCurrentIdlingStarted()
+  updateCommittedOffsets(TopicPartition;Long)
+  updateEndOffsets(TopicPartition;Long)
+  hasRecordsQueued()
+  recordCollector()
+  getNonEmptyTopicPartitions()
+  totalBytesBuffered()
+  numBuffered()
+  streamTime()
} 
@enduml
