@startuml
!pragma layout smetana
java.lang.Object <|-- org.apache.kafka.streams.processor.internals.TopologyMetadata
class org.apache.kafka.streams.processor.internals.TopologyMetadata {
+  TopologyMetadata(InternalTopologyBuilder;StreamsConfig)
+  TopologyMetadata(ConcurrentNavigableMap<String,InternalTopologyBuilder>;StreamsConfig)
+  setLog(LogContext)
+  processingMode()
+  topologyVersion()
+  lock()
+  unlock()
+  sourceTopicsForTopology(String)
+  needsUpdate(String)
+  registerThread(String)
+  unregisterThread(String)
+  taskExecutionMetadata()
+  executeTopologyUpdatesAndBumpThreadVersion(Consumer<Set<String>>;Consumer<Set<String>>)
+  maybeNotifyTopologyVersionListeners()
+  getMinimumThreadVersion()
+  wakeupThreads()
+  maybeWaitForNonEmptyTopology(Supplier<StreamThread.State>)
+  registerAndBuildNewTopology(KafkaFutureImpl<Void>;InternalTopologyBuilder)
+  pauseTopology(String)
+  isPaused(String)
+  resumeTopology(String)
+  unregisterTopology(KafkaFutureImpl<Void>;String)
+  getTaskConfigFor(TaskId)
+  buildAndRewriteTopology()
+  buildAndVerifyTopology(InternalTopologyBuilder)
+  getNumStreamThreads(StreamsConfig)
+  hasNamedTopologies()
+  namedTopologiesView()
+  hasGlobalTopology()
+  hasNoLocalTopology()
+  hasPersistentStores()
+  hasStore(String)
+  hasOffsetResetOverrides()
+  offsetResetStrategy(String)
+  fullSourceTopicNamesForTopology(String)
+  allFullSourceTopicNames()
+  sourceTopicPattern()
+  usesPatternSubscription()
+  isEmpty()
+  topologyDescriptionString()
+  buildSubtopology(TaskId)
+  globalTaskTopology()
+  globalStateStores()
+  stateStoreNameToSourceTopicsForTopology(String)
+  stateStoreNameToSourceTopics()
+  getStoreForChangelogTopic(String)
+  sourceTopicsForStore(String;String)
+  getTopologyNameOrElseUnnamed(String)
+  subtopologyTopicsInfoMapExcluding(Set<String>)
+  topologyToSubtopologyTopicsInfoMap()
+  nodeToSourceTopics(TaskId)
+  addSubscribedTopicsFromMetadata(Set<String>;String)
+  addSubscribedTopicsFromAssignment(List<TopicPartition>;String)
+  copartitionGroups()
+  lookupBuilderForTask(TaskId)
+  getAllNamedTopologies()
+  lookupBuilderForNamedTopology(String)
+  evaluateConditionIsTrueForAnyBuilders(Function<InternalTopologyBuilder,Boolean>)
+  applyToEachBuilder(Consumer<InternalTopologyBuilder>)
} 
@enduml
