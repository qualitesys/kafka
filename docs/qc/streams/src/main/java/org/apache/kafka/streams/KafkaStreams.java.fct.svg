<?xml version="1.0" encoding="UTF-8" standalone="no"?><svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" contentStyleType="text/css" height="544px" preserveAspectRatio="none" style="width:861px;height:544px;background:#000000;" version="1.1" viewBox="0 0 861 544" width="861px" zoomAndPan="magnify"><defs/><g><rect fill="#0D030B" height="1" style="stroke:#0D030B;stroke-width:1.0;" width="1" x="0" y="0"/><text fill="#33FF02" font-family="sans-serif" font-size="12" font-style="italic" font-weight="bold" lengthAdjust="spacing" textLength="127" x="5" y="17">PlantUML 1.2022.7</text><rect fill="#33FF02" height="21.2969" style="stroke:#33FF02;stroke-width:1.0;" width="849" x="5" y="26.9688"/><text fill="#000000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="188" x="6" y="41.9688">[From string (line 196) ]</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="5" x="5" y="62.2656"> </text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="79" x="5" y="78.5625">@startuml</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="190" x="5" y="94.8594">!pragma layout smetana</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="111.1563">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="127.4531">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="390" x="5" y="143.75">constructor State(final Integer... validTransitions)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="15" x="5" y="160.0469">...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="185" x="5" y="176.3438">... ( skipping 171 lines )</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="15" x="5" y="192.6406">...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="71" x="5" y="208.9375">end note</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="198" x="5" y="225.2344">if (oldHandler) then (yes)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="338" x="5" y="241.5313">:threads.remove(Thread.currentThread());</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="427" x="5" y="257.8281">if (throwable instanceof RuntimeException) then (yes)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="303" x="5" y="274.125">:throw (RuntimeException)throwable;;</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="70" x="5" y="290.4219">else (no)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="849" x="5" y="306.7188">:throw new RuntimeException("Unexpected checked exception caught in the uncaught exception handler",</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="98" x="5" y="323.0156">throwable);;</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="40" x="5" y="339.3125">endif</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="70" x="5" y="355.6094">else (no)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="367" x="5" y="371.9063">:handleStreamsUncaughtException(throwable,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="383" x="5" y="388.2031">t-&gt;SHUTDOWN_CLIENT,skipThreadReplacement);</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="40" x="5" y="404.5">endif</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="30" x="5" y="420.7969">end</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="437.0938">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="453.3906">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="423" x="5" y="469.6875">void replaceStreamThread(final Throwablethrowable)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="71" x="5" y="485.9844">end note</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="445" x="5" y="502.2813">if (globalStreamThread!=null &amp;&amp;Thread.currentThread()</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" text-decoration="wavy underline" textLength="494" x="5" y="518.5781">.getName().equals(globalStreamThread.getName())) then (yes)</text><text fill="#FF0000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="106" x="10" y="534.875">Syntax Error?</text><!--MD5=[90cd018b842d78036929b5f5613988bb]
@startuml
!pragma layout smetana
start
note right
constructor State(final Integer... validTransitions)
end note
:this.validTransitions.addAll(Arrays.asList(validTransitions));
end
start
note right
booleanhasNotStarted()
end note
:return equals(CREATED);;
stop
end
start
note right
booleanisRunningOrRebalancing()
end note
:return equals(RUNNING)||equals(REBALANCING);;
stop
end
start
note right
booleanisShuttingDown()
end note
:return equals(PENDING_SHUTDOWN)||equals(PENDING_ERROR);;
stop
end
start
note right
booleanhasCompletedShutdown()
end note
:return equals(NOT_RUNNING)||equals(ERROR);;
stop
end
start
note right
booleanhasStartedOrFinishedShuttingDown()
end note
:return isShuttingDown()||hasCompletedShutdown();;
stop
end
start
note right
booleanisValidTransition(final StatenewState)
end note
:return validTransitions.contains(newState.ordinal());;
stop
end
start
note right
booleanwaitOnState(final StatetargetState,
final longwaitMs)
end note
while (state!=targetState)
if (waitMs>elapsedMs) then (yes) 
:stateLock.wait(remainingMs);
:interrupted=true;
else (no)
:log.debug("Cannot transit to {} within {}ms",
targetState,waitMs);
:return false;;
stop
endif 
:elapsedMs=time.milliseconds()-begin;
endwhile
if (interrupted) then (yes) 
:Thread.currentThread().interrupt();
endif 
:return true;;
stop
end
start
note right
booleansetState(final StatenewState)
end note
:oldState=state;
if (state==State.PENDING_SHUTDOWN&&newState!=State.NOT_RUNNING) then (yes) 
:return false;;
stop
else (no)
:log.info("State transition from {} to {}",
oldState,newState);
endif 
:state=newState;
:stateLock.notifyAll();
if (stateListener!=null ) then (yes) 
:stateListener.onChange(newState,oldState);
endif 
:return true;;
stop
end
start
note right
Statestate()
end note
:return state;;
stop
end
start
note right
booleanisRunningOrRebalancing()
end note
:return state.isRunningOrRebalancing();;
stop
end
start
note right
booleanhasStartedOrFinishedShuttingDown()
end note
:return state.hasStartedOrFinishedShuttingDown();;
stop
end
start
note right
void validateIsRunningOrRebalancing()
end note
if (state.hasNotStarted()) then (yes) 
:throw new StreamsNotStartedException("KafkaStreams has not been started,
 you can retry after calling start()");;
endif 
if (!state.isRunningOrRebalancing()) then (yes) 
:throw new IllegalStateException("KafkaStreams is not running. State is "+
state+".");;
endif 
end
start
note right
void setStateListener(final KafkaStreams.StateListenerlistener)
end note
if (state.hasNotStarted()) then (yes) 
:stateListener=listener;
else (no)
:throw new IllegalStateException("Can only set StateListener before calling start(). Current state is: "+
state);;
endif 
end
start
note right
void setUncaughtExceptionHandler(final Thread.UncaughtExceptionHandleruncaughtExceptionHandler)
end note
if (state.hasNotStarted()) then (yes) 
:oldHandler=true;
:processStreamThread(thread->thread.setUncaughtExceptionHandler(uncaughtExceptionHandler));
if (globalStreamThread!=null ) then (yes) 
:globalStreamThread.setUncaughtExceptionHandler(uncaughtExceptionHandler);
endif 
else (no)
:throw new IllegalStateException("Can only set UncaughtExceptionHandler before calling start(). "+
"Current state is: "+state);;
endif 
end
start
note right
void setUncaughtExceptionHandler(final StreamsUncaughtExceptionHandleruserStreamsUncaughtExceptionHandler)
end note
if (state.hasNotStarted()) then (yes) 
:Objects.requireNonNull(userStreamsUncaughtExceptionHandler);
:streamsUncaughtExceptionHandler=(exception,
skipThreadReplacement)->handleStreamsUncaughtException(exception,
userStreamsUncaughtExceptionHandler,skipThreadReplacement);
:processStreamThread(thread->thread.setStreamsUncaughtExceptionHandler(streamsUncaughtExceptionHandler));
if (globalStreamThread!=null ) then (yes) 
:globalStreamThread.setUncaughtExceptionHandler(exception->handleStreamsUncaughtException(exception,
userStreamsUncaughtExceptionHandler,false));
endif 
else (no)
:throw new IllegalStateException("Can only set UncaughtExceptionHandler before calling start(). "+
"Current state is: "+state);;
endif 
end
start
note right
void defaultStreamsUncaughtExceptionHandler(final Throwablethrowable,
final booleanskipThreadReplacement)
end note
if (oldHandler) then (yes) 
:threads.remove(Thread.currentThread());
if (throwable instanceof RuntimeException) then (yes) 
:throw (RuntimeException)throwable;;
else (no)
:throw new RuntimeException("Unexpected checked exception caught in the uncaught exception handler",
throwable);;
endif 
else (no)
:handleStreamsUncaughtException(throwable,
t->SHUTDOWN_CLIENT,skipThreadReplacement);
endif 
end
start
note right
void replaceStreamThread(final Throwablethrowable)
end note
if (globalStreamThread!=null &&Thread.currentThread()
.getName().equals(globalStreamThread.getName())) then (yes) 
:log.warn("The global thread cannot be replaced. Reverting to shutting down the client.");
:log.error("Encountered the following exception during processing "+
" The streams client is going to shut down now. ",
throwable);
:closeToError();
endif 
:deadThread.shutdown();
:addStreamThread();
if (throwable instanceof RuntimeException) then (yes) 
:throw (RuntimeException)throwable;;
else (no)
:throw new RuntimeException("Unexpected checked exception caught in the uncaught exception handler",
throwable);;
endif 
end
start
note right
booleanwrappedExceptionIsIn(final Throwablethrowable,
final Set<Class<?  extends Throwable>>exceptionsOfInterest)
end note
:return throwable.getCause()!=null &&exceptionsOfInterest.contains(throwable.getCause()
.getClass());;
stop
end
start
note right
StreamsUncaughtExceptionHandler.StreamThreadExceptionResponsegetActionForThrowable(final Throwablethrowable,
final StreamsUncaughtExceptionHandlerstreamsUncaughtExceptionHandler)
end note
if (wrappedExceptionIsIn(throwable,EXCEPTIONS_NOT_TO_BE_HANDLED_BY_USERS)) then (yes) 
:action=SHUTDOWN_CLIENT;
else (no)
:action=streamsUncaughtExceptionHandler.handle(throwable);
endif 
:return action;;
stop
end
start
note right
void handleStreamsUncaughtException(final Throwablethrowable,
final StreamsUncaughtExceptionHandlerstreamsUncaughtExceptionHandler,
final booleanskipThreadReplacement)
end note
if (oldHandler) then (yes) 
:log.warn("Stream's new uncaught exception handler is set as well as the deprecated old handler."+
"The old handler will be ignored as long as a new handler is set.");
endif 
if (!skipThreadReplacement) then (yes) 
:log.error("Replacing thread in the streams uncaught exception handler",
throwable);
:replaceStreamThread(throwable);
else (no)
:log.debug("Skipping thread replacement for recoverable error");
endif 
:log.error("Encountered the following exception during processing "+
"and Kafka Streams opted to "+action+"."+
" The streams client is going to shut down now. ",
throwable);
:closeToError();
if (getNumLiveStreamThreads()==1) then (yes) 
:log.warn("Attempt to shut down the application requires adding a thread to communicate the shutdown. No processing will be done on this thread");
:addStreamThread();
endif 
if (throwable instanceof Error) then (yes) 
:log.error("This option requires running threads to shut down the application."+
"but the uncaught exception was an Error, which means this runtime is no "+
"longer in a well-defined state. Attempting to send the shutdown command anyway.",
throwable);
endif 
if (Thread.currentThread().equals(globalStreamThread)&&getNumLiveStreamThreads()==0) then (yes) 
:log.error("Exception in global thread caused the application to attempt to shutdown."+
" This action will succeed only if there is at least one StreamThread running on this client."+
" Currently there are no running threads so will now close the client.");
:closeToError();
endif 
:processStreamThread(thread->thread.sendShutdownRequest(AssignorError.SHUTDOWN_REQUESTED));
:log.error("Encountered the following exception during processing "+
"and sent shutdown request for the entire application.",
throwable);
end
start
note right
void setGlobalStateRestoreListener(final StateRestoreListenerglobalStateRestoreListener)
end note
if (state.hasNotStarted()) then (yes) 
:this.globalStateRestoreListener=globalStateRestoreListener;
else (no)
:throw new IllegalStateException("Can only set GlobalStateRestoreListener before calling start(). "+
"Current state is: "+state);;
endif 
end
start
note right
Map<MetricName,?  extends Metric>metrics()
end note
:processStreamThread(thread->{result.putAll(thread.producerMetrics());
:result.putAll(thread.consumerMetrics());
:result.putAll(thread.adminClientMetrics());});
if (globalStreamThread!=null ) then (yes) 
:result.putAll(globalStreamThread.consumerMetrics());
endif 
:result.putAll(metrics.metrics());
:return Collections.unmodifiableMap(result);;
stop
end
start
note right
constructor StreamStateListener(final Map<Long,
StreamThread.State>threadState,final GlobalStreamThread.StateglobalThreadState)
end note
:this.threadState=threadState;
:this.globalThreadState=globalThreadState;
:this.threadStatesLock=new Object();
end
start
note right
void maybeSetRunning()
end note
if (state!=StreamThread.State.RUNNING&&state!=StreamThread.State.DEAD) then (yes) 
:return ;;
stop
endif 
if (globalThreadState!=null &&globalThreadState!=GlobalStreamThread.State.RUNNING) then (yes) 
:return ;;
stop
endif 
:setState(State.RUNNING);
end
start
note right
void onChange(final Threadthread,final ThreadStateTransitionValidatorabstractNewState,
final ThreadStateTransitionValidatorabstractOldState)
end note
if (thread instanceof StreamThread) then (yes) 
:threadState.put(thread.getId(),newState);
if (newState==StreamThread.State.PARTITIONS_REVOKED||newState==StreamThread.State.PARTITIONS_ASSIGNED) then (yes) 
:setState(State.REBALANCING);
endif 
endif 
end
start
note right
void throwOnFatalException(final ExceptionfatalUserException,
final TopicPartitiontopicPartition,final StringstoreName)
end note
:throw new StreamsException(String.format("Fatal user code error in store restore listener for store %s,
 partition %s.",storeName,topicPartition),
fatalUserException);;
end
start
note right
void onRestoreStart(final TopicPartitiontopicPartition,
final StringstoreName,final longstartingOffset,
final longendingOffset)
end note
if (globalStateRestoreListener!=null ) then (yes) 
:globalStateRestoreListener.onRestoreStart(topicPartition,
storeName,startingOffset,endingOffset);
:throwOnFatalException(fatalUserException,
topicPartition,storeName);
endif 
end
start
note right
void onBatchRestored(final TopicPartitiontopicPartition,
final StringstoreName,final longbatchEndOffset,
final longnumRestored)
end note
if (globalStateRestoreListener!=null ) then (yes) 
:globalStateRestoreListener.onBatchRestored(topicPartition,
storeName,batchEndOffset,numRestored);
:throwOnFatalException(fatalUserException,
topicPartition,storeName);
endif 
end
start
note right
void onRestoreEnd(final TopicPartitiontopicPartition,
final StringstoreName,final longtotalRestored)
end note
if (globalStateRestoreListener!=null ) then (yes) 
:globalStateRestoreListener.onRestoreEnd(topicPartition,
storeName,totalRestored);
:throwOnFatalException(fatalUserException,
topicPartition,storeName);
endif 
end
start
note right
constructor KafkaStreams(final Topologytopology,
final Propertiesprops)
end note
:this(topology,new StreamsConfig(props),new DefaultKafkaClientSupplier());
end
start
note right
constructor KafkaStreams(final Topologytopology,
final Propertiesprops,final KafkaClientSupplierclientSupplier)
end note
:this(topology,new StreamsConfig(props),clientSupplier,
Time.SYSTEM);
end
start
note right
constructor KafkaStreams(final Topologytopology,
final Propertiesprops,final Timetime)
end note
:this(topology,new StreamsConfig(props),new DefaultKafkaClientSupplier(),
time);
end
start
note right
constructor KafkaStreams(final Topologytopology,
final Propertiesprops,final KafkaClientSupplierclientSupplier,
final Timetime)
end note
:this(topology,new StreamsConfig(props),clientSupplier,
time);
end
start
note right
constructor KafkaStreams(final Topologytopology,
final StreamsConfigapplicationConfigs)
end note
:this(topology,applicationConfigs,new DefaultKafkaClientSupplier());
end
start
note right
constructor KafkaStreams(final Topologytopology,
final StreamsConfigapplicationConfigs,final KafkaClientSupplierclientSupplier)
end note
:this(new TopologyMetadata(topology.internalTopologyBuilder,
applicationConfigs),applicationConfigs,clientSupplier);
end
start
note right
constructor KafkaStreams(final Topologytopology,
final StreamsConfigapplicationConfigs,final Timetime)
end note
:this(new TopologyMetadata(topology.internalTopologyBuilder,
applicationConfigs),applicationConfigs,new DefaultKafkaClientSupplier(),
time);
end
start
note right
constructor KafkaStreams(final Topologytopology,
final StreamsConfigapplicationConfigs,final KafkaClientSupplierclientSupplier,
final Timetime)
end note
:this(new TopologyMetadata(topology.internalTopologyBuilder,
applicationConfigs),applicationConfigs,clientSupplier,
time);
end
start
note right
constructor KafkaStreams(final TopologyMetadatatopologyMetadata,
final StreamsConfigapplicationConfigs,final KafkaClientSupplierclientSupplier)
end note
:this(topologyMetadata,applicationConfigs,
clientSupplier,Time.SYSTEM);
end
start
note right
constructor KafkaStreams(final TopologyMetadatatopologyMetadata,
final StreamsConfigapplicationConfigs,final KafkaClientSupplierclientSupplier,
final Timetime)
end note
:this.applicationConfigs=applicationConfigs;
:this.time=time;
:this.topologyMetadata=topologyMetadata;
:this.topologyMetadata.buildAndRewriteTopology();
:stateDirectory=new StateDirectory(applicationConfigs,
time,topologyMetadata.hasPersistentStores(),
topologyMetadata.hasNamedTopologies());
:processId=stateDirectory.initializeProcessId();
:throw new StreamsException(fatal);;
if (userClientId.length()<=0) then (yes) 
:clientId=applicationId+"-"+processId;
else (no)
:clientId=userClientId;
endif 
:this.log=logContext.logger(getClass());
:topologyMetadata.setLog(logContext);
:this.clientSupplier=clientSupplier;
:adminClient=clientSupplier.getAdmin(applicationConfigs.getAdminConfigs(ClientUtils.getSharedAdminClientId(clientId)));
:log.info("Kafka Streams version: {}",ClientMetrics.version());
:log.info("Kafka Streams commit ID: {}",ClientMetrics.commitId());
:metrics=getMetrics(applicationConfigs,time,
clientId);
:streamsMetrics=new StreamsMetricsImpl(metrics,
clientId,applicationConfigs.getString(StreamsConfig.BUILT_IN_METRICS_VERSION_CONFIG),
time);
:ClientMetrics.addVersionMetric(streamsMetrics);
:ClientMetrics.addCommitIdMetric(streamsMetrics);
:ClientMetrics.addApplicationIdMetric(streamsMetrics,
applicationConfigs.getString(StreamsConfig.APPLICATION_ID_CONFIG));
:ClientMetrics.addTopologyDescriptionMetric(streamsMetrics,
(metricsConfig,now)->this.topologyMetadata.topologyDescriptionString());
:ClientMetrics.addStateMetric(streamsMetrics,
(metricsConfig,now)->state);
:threads=Collections.synchronizedList(new LinkedList<>());
:ClientMetrics.addNumAliveStreamThreadMetric(streamsMetrics,
(metricsConfig,now)->getNumLiveStreamThreads());
:streamsMetadataState=new StreamsMetadataState(this.topologyMetadata,
parseHostInfo(applicationConfigs.getString(StreamsConfig.APPLICATION_SERVER_CONFIG)),
logContext);
:oldHandler=false;
:streamsUncaughtExceptionHandler=this::defaultStreamsUncaughtExceptionHandler;
:delegatingStateRestoreListener=new DelegatingStateRestoreListener();
:totalCacheSize=applicationConfigs.getLong(StreamsConfig.CACHE_MAX_BYTES_BUFFERING_CONFIG);
if (hasGlobalTopology) then (yes) 
:globalStreamThread=new GlobalStreamThread(topologyMetadata.globalTaskTopology(),
applicationConfigs,clientSupplier.getGlobalConsumer(applicationConfigs.getGlobalConsumerConfigs(clientId)),
stateDirectory,cacheSizePerThread,streamsMetrics,
time,globalThreadId,delegatingStateRestoreListener,
exception->defaultStreamsUncaughtExceptionHandler(exception,
false));
:globalThreadState=globalStreamThread.state();
endif 
:threadState=new HashMap<>(numStreamThreads);
:streamStateListener=new StreamStateListener(threadState,
globalThreadState);
if (hasGlobalTopology) then (yes) 
:globalStreamThread.setStateListener(streamStateListener);
endif 
:queryableStoreProvider=new QueryableStoreProvider(globalStateStoreProvider);
:i++;
:createAndAddStreamThread(cacheSizePerThread,i);
:stateDirCleaner=setupStateDirCleaner();
:rocksDBMetricsRecordingService=maybeCreateRocksDBMetricsRecordingService(clientId,
applicationConfigs);
end
start
note right
StreamThreadcreateAndAddStreamThread(final longcacheSizePerThread,
final intthreadIdx)
end note
:streamThread.setStateListener(streamStateListener);
:threads.add(streamThread);
:threadState.put(streamThread.getId(),streamThread.state());
:queryableStoreProvider.addStoreProviderForThread(streamThread.getName(),
new StreamThreadStateStoreProvider(streamThread));
:return streamThread;;
stop
end
start
note right
MetricsgetMetrics(final StreamsConfigconfig,
final Timetime,final StringclientId)
end note
:jmxReporter.configure(config.originals());
:reporters.add(jmxReporter);
:return new Metrics(metricConfig,reporters,
time,metricsContext);;
stop
end
start
note right
Optional<String>addStreamThread()
end note
if (isRunningOrRebalancing()) then (yes) 
:log.info("Adding StreamThread-{}, there will now be {} live threads and the new cache size per thread is {}",
threadIdx,numLiveThreads+1,cacheSizePerThread);
:resizeThreadCache(cacheSizePerThread);
:streamThread=createAndAddStreamThread(cacheSizePerThread,
threadIdx);
if (isRunningOrRebalancing()) then (yes) 
:streamThread.start();
:return Optional.of(streamThread.getName());;
stop
else (no)
:log.warn("Terminating the new thread because the Kafka Streams client is in state {}",
state);
:streamThread.shutdown();
:threads.remove(streamThread);
:log.info("Resizing thread cache due to terminating added thread,
 new cache size per thread is {}",cacheSizePerThread);
:resizeThreadCache(cacheSizePerThread);
:return Optional.empty();;
stop
endif 
else (no)
:log.warn("Cannot add a stream thread when Kafka Streams client is in state {}",
state);
:return Optional.empty();;
stop
endif 
end
start
note right
Optional<String>removeStreamThread()
end note
:return removeStreamThread(Long.MAX_VALUE);;
stop
end
start
note right
Optional<String>removeStreamThread(final Durationtimeout)
end note
:return removeStreamThread(timeoutMs);;
stop
end
start
note right
Optional<String>removeStreamThread(final longtimeoutMs)
end note
if (isRunningOrRebalancing()) then (yes) 
if (streamThread.isAlive()&&(callingThreadIsNotCurrentStreamThread||getNumLiveStreamThreads()==1)) then (yes) 
:log.info("Removing StreamThread "+streamThread.getName());
:streamThread.requestLeaveGroupDuringShutdown();
:streamThread.shutdown();
if (!streamThread.getName().equals(Thread.currentThread()
.getName())) then (yes) 
if (remainingTimeMs<=0||!streamThread.waitOnThreadState(StreamThread.State.DEAD,
remainingTimeMs)) then (yes) 
:log.warn("{} did not shutdown in the allotted time.",
streamThread.getName());
else (no)
:log.info("Successfully removed {} in {}ms",
streamThread.getName(),time.milliseconds()-startMs);
:threads.remove(streamThread);
:queryableStoreProvider.removeStoreProviderForThread(streamThread.getName());
endif 
else (no)
:log.info("{} is the last remaining thread and must remove itself, therefore we cannot wait "+
"for it to complete shutdown as this will result in deadlock.",
streamThread.getName());
endif 
:log.info("Resizing thread cache due to thread removal,
 new cache size per thread is {}",cacheSizePerThread);
:resizeThreadCache(cacheSizePerThread);
if (groupInstanceID.isPresent()&&callingThreadIsNotCurrentStreamThread) then (yes) 
:removeMembersFromConsumerGroupResult.memberResult(memberToRemove).get(remainingTimeMs,
TimeUnit.MILLISECONDS);
:log.error("Could not remove static member {} from consumer group {} due to a timeout: {}",
groupInstanceID.get(),applicationConfigs.getString(StreamsConfig.APPLICATION_ID_CONFIG),e);
:throw new TimeoutException(e.getMessage(),e);;
:Thread.currentThread().interrupt();
:log.error("Could not remove static member {} from consumer group {} due to: {}",
groupInstanceID.get(),applicationConfigs.getString(StreamsConfig.APPLICATION_ID_CONFIG),e);
:throw new StreamsException("Could not remove static member "+
groupInstanceID.get()+" from consumer group "+
applicationConfigs.getString(StreamsConfig.APPLICATION_ID_CONFIG)+
" for the following reason: ",e.getCause());;
endif 
if (remainingTimeMs<=0) then (yes) 
:throw new TimeoutException("Thread "+streamThread.getName()+
" did not stop in the allotted time");;
endif 
:return Optional.of(streamThread.getName());;
stop
endif 
:log.warn("There are no threads eligible for removal");
else (no)
:log.warn("Cannot remove a stream thread when Kafka Streams client is in state  "+
state());
endif 
:return Optional.empty();;
stop
end
start
note right
intgetNumLiveStreamThreads()
end note
:processStreamThread(thread->{if (thread.state()==StreamThread.State.DEAD){log.debug("Trimming thread {} from the threads list since it's state is {}",thread.getName(),StreamThread.State.DEAD);threads.remove(thread);}else if (thread.state()==StreamThread.State.PENDING_SHUTDOWN){log.debug("Skipping thread {} from num live threads computation since it's state is {}",thread.getName(),StreamThread.State.PENDING_SHUTDOWN);}else {numLiveThreads.incrementAndGet();}});
:return numLiveThreads.get();;
stop
end
start
note right
intgetNextThreadIndex()
end note
:processStreamThread(thread->{if (thread.state()==StreamThread.State.DEAD){threads.remove(thread);
:}else {allLiveThreadNames.add(thread.getName());
:final intthreadId=Integer.parseInt(thread.getName().substring(thread.getName().lastIndexOf("-")+
1));if (threadId>maxThreadId.get()){maxThreadId.set(threadId);}}});
:i++;
if (!allLiveThreadNames.contains(name)) then (yes) 
:return i;;
stop
endif 
:return threads.size()+1;;
stop
end
start
note right
longgetCacheSizePerThread(final intnumStreamThreads)
end note
if (numStreamThreads==0) then (yes) 
:return totalCacheSize;;
stop
endif 
:return totalCacheSize/(numStreamThreads+
(topologyMetadata.hasGlobalTopology()? 1:0));;
stop
end
start
note right
void resizeThreadCache(final longcacheSizePerThread)
end note
:processStreamThread(thread->thread.resizeCache(cacheSizePerThread));
if (globalStreamThread!=null ) then (yes) 
:globalStreamThread.resize(cacheSizePerThread);
endif 
end
start
note right
ScheduledExecutorServicesetupStateDirCleaner()
end note
:return Executors.newSingleThreadScheduledExecutor(r->{final Threadthread=new Thread(r,clientId+
"-CleanupThread");thread.setDaemon(true);return thread;});;
stop
end
start
note right
ScheduledExecutorServicemaybeCreateRocksDBMetricsRecordingService(final StringclientId,
final StreamsConfigconfig)
end note
if (RecordingLevel.forName(config.getString(METRICS_RECORDING_LEVEL_CONFIG))==RecordingLevel.DEBUG) then (yes) 
:return Executors.newSingleThreadScheduledExecutor(r->{final Threadthread=new Thread(r,clientId+
"-RocksDBMetricsRecordingTrigger");thread.setDaemon(true);return thread;});;
stop
endif 
:return null ;;
stop
end
start
note right
HostInfoparseHostInfo(final StringendPoint)
end note
if (hostInfo==null ) then (yes) 
:return StreamsMetadataState.UNKNOWN_HOST;;
stop
else (no)
:return hostInfo;;
stop
endif 
end
start
note right
void start()
end note
if (setState(State.REBALANCING)) then (yes) 
:log.debug("Starting Streams client");
if (globalStreamThread!=null ) then (yes) 
:globalStreamThread.start();
endif 
:log.info("Started {} stream threads",numThreads);
:stateDirCleaner.scheduleAtFixedRate(()->{if (state==State.RUNNING){stateDirectory.cleanRemovedTasks(cleanupDelay);}},
cleanupDelay,cleanupDelay,TimeUnit.MILLISECONDS);
if (rocksDBMetricsRecordingService!=null ) then (yes) 
:rocksDBMetricsRecordingService.scheduleAtFixedRate(streamsMetrics.rocksDBMetricsRecordingTrigger(),
recordingDelay,recordingInterval,TimeUnit.MINUTES);
endif 
else (no)
:throw new IllegalStateException("The client is either already started or already stopped,
 cannot re-start");;
endif 
end
start
note right
CloseOptionstimeout(final Durationtimeout)
end note
:this.timeout=timeout;
:return this;;
stop
end
start
note right
CloseOptionsleaveGroup(final booleanleaveGroup)
end note
:this.leaveGroup=leaveGroup;
:return this;;
stop
end
start
note right
void close()
end note
:close(Long.MAX_VALUE);
end
start
note right
ThreadshutdownHelper(final booleanerror)
end note
:stateDirCleaner.shutdownNow();
if (rocksDBMetricsRecordingService!=null ) then (yes) 
:rocksDBMetricsRecordingService.shutdownNow();
endif 
:return new Thread(()->{intnumStreamThreads=processStreamThread(StreamThread::shutdown);
:log.info("Shutting down {} stream threads",
numStreamThreads);topologyMetadata.wakeupThreads();numStreamThreads=processStreamThread(thread->{try {if (!thread.isRunning()){log.debug("Shutdown {} complete",thread.getName());thread.join();}}catch (final InterruptedExceptionex){log.warn("Shutdown {} interrupted",thread.getName());Thread.currentThread().interrupt();}});log.info("Shutdown {} stream threads complete",numStreamThreads);if (globalStreamThread!=null ){log.info("Shutting down the global stream threads");globalStreamThread.shutdown();}if (globalStreamThread!=null &&!globalStreamThread.stillRunning()){try {globalStreamThread.join();}catch (final InterruptedExceptione){log.warn("Shutdown the global stream thread interrupted");Thread.currentThread().interrupt();}globalStreamThread=null ;log.info("Shutdown global stream threads complete");}stateDirectory.close();adminClient.close();streamsMetrics.removeAllClientLevelSensorsAndMetrics();metrics.close();if (!error){setState(State.NOT_RUNNING);}else {setState(State.ERROR);}},clientId+"-CloseThread");;
stop
end
start
note right
booleanclose(final longtimeoutMs)
end note
if (state.hasCompletedShutdown()) then (yes) 
:log.info("Streams client is already in the terminal {} state,
 all resources are closed and the client has stopped.",
state);
:return true;;
stop
endif 
if (state.isShuttingDown()) then (yes) 
:log.info("Streams client is in {}, all resources are being closed and the client will be stopped.",
state);
if (state==State.PENDING_ERROR&&waitOnState(State.ERROR,
timeoutMs)) then (yes) 
:log.info("Streams client stopped to ERROR completely");
:return true;;
stop
else (no)
:log.warn("Streams client cannot transition to {}} completely within the timeout",
state==State.PENDING_SHUTDOWN? State.NOT_RUNNING:State.ERROR);
:return false;;
stop
endif 
endif 
if (!setState(State.PENDING_SHUTDOWN)) then (yes) 
:log.error("Failed to transition to PENDING_SHUTDOWN,
 current state is {}",state);
:throw new StreamsException("Failed to shut down while in state "+
state);;
else (no)
:shutdownThread.setDaemon(true);
:shutdownThread.start();
endif 
if (waitOnState(State.NOT_RUNNING,timeoutMs)) then (yes) 
:log.info("Streams client stopped completely");
:return true;;
stop
else (no)
:log.info("Streams client cannot stop completely within the timeout");
:return false;;
stop
endif 
end
start
note right
void closeToError()
end note
if (!setState(State.PENDING_ERROR)) then (yes) 
:log.info("Skipping shutdown since we are already in "+
state());
else (no)
:shutdownThread.setDaemon(true);
:shutdownThread.start();
endif 
end
start
note right
booleanclose(final Durationtimeout)
end note
if (timeoutMs<0) then (yes) 
:throw new IllegalArgumentException("Timeout can't be negative.");;
endif 
:log.debug("Stopping Streams client with timeoutMillis = {} ms.",
timeoutMs);
:return close(timeoutMs);;
stop
end
start
note right
booleanclose(final CloseOptionsoptions)
end note
if (timeoutMs<0) then (yes) 
:throw new IllegalArgumentException("Timeout can't be negative.");;
endif 
if (options.leaveGroup&&groupInstanceId.isPresent()) then (yes) 
:log.debug("Sending leave group trigger to removing instance from consumer group");
:removeMembersFromConsumerGroupResult.memberResult(memberToRemove).get(remainingTimeMs,
TimeUnit.MILLISECONDS);
:log.error("Could not remove static member {} from consumer group {} due to a: {}",
groupInstanceId.get(),applicationConfigs.getString(StreamsConfig.APPLICATION_ID_CONFIG),e);
endif 
:log.debug("Stopping Streams client with timeoutMillis = {} ms.",
timeoutMs);
:return closeStatus;;
stop
end
start
note right
void cleanUp()
end note
if (!(state.hasNotStarted()||state.hasCompletedShutdown())) then (yes) 
:throw new IllegalStateException("Cannot clean up while running.");;
endif 
:stateDirectory.clean();
end
start
note right
Collection<org.apache.kafka.streams.state.StreamsMetadata>allMetadata()
end note
:validateIsRunningOrRebalancing();
:return streamsMetadataState.getAllMetadata()
.stream().map(streamsMetadata->new org.apache.kafka.streams.state.StreamsMetadata(streamsMetadata.hostInfo(),
streamsMetadata.stateStoreNames(),streamsMetadata.topicPartitions(),
streamsMetadata.standbyStateStoreNames(),
streamsMetadata.standbyTopicPartitions()))
.collect(Collectors.toSet());;
stop
end
start
note right
Collection<StreamsMetadata>metadataForAllStreamsClients()
end note
:validateIsRunningOrRebalancing();
:return streamsMetadataState.getAllMetadata();;
stop
end
start
note right
Collection<org.apache.kafka.streams.state.StreamsMetadata>allMetadataForStore(final StringstoreName)
end note
:validateIsRunningOrRebalancing();
:return streamsMetadataState.getAllMetadataForStore(storeName)
.stream().map(streamsMetadata->new org.apache.kafka.streams.state.StreamsMetadata(streamsMetadata.hostInfo(),
streamsMetadata.stateStoreNames(),streamsMetadata.topicPartitions(),
streamsMetadata.standbyStateStoreNames(),
streamsMetadata.standbyTopicPartitions()))
.collect(Collectors.toSet());;
stop
end
start
note right
Collection<StreamsMetadata>streamsMetadataForStore(final StringstoreName)
end note
:validateIsRunningOrRebalancing();
:return streamsMetadataState.getAllMetadataForStore(storeName);;
stop
end
:validateIsRunningOrRebalancing();
:return streamsMetadataState.getKeyQueryMetadataForKey(storeName,
key,keySerializer);;
stop
:validateIsRunningOrRebalancing();
:return streamsMetadataState.getKeyQueryMetadataForKey(storeName,
key,partitioner);;
stop
:validateIsRunningOrRebalancing();
if (!topologyMetadata.hasStore(storeName)) then (yes) 
:throw new UnknownStateStoreException("Cannot get state store "+
storeName+" because no such store is registered in the topology.");;
endif 
:return queryableStoreProvider.getStore(storeQueryParameters);;
stop
start
note right
void pause()
end note
if (topologyMetadata.hasNamedTopologies()) then (yes) 
:topologyMetadata.pauseTopology(namedTopology.name());
else (no)
:topologyMetadata.pauseTopology(UNNAMED_TOPOLOGY);
endif 
end
start
note right
booleanisPaused()
end note
if (topologyMetadata.hasNamedTopologies()) then (yes) 
:return topologyMetadata.getAllNamedTopologies()
.stream().map(NamedTopology::name).allMatch(topologyMetadata::isPaused);;
stop
else (no)
:return topologyMetadata.isPaused(UNNAMED_TOPOLOGY);;
stop
endif 
end
start
note right
void resume()
end note
if (topologyMetadata.hasNamedTopologies()) then (yes) 
:topologyMetadata.resumeTopology(namedTopology.name());
else (no)
:topologyMetadata.resumeTopology(UNNAMED_TOPOLOGY);
endif 
end
start
note right
intprocessStreamThread(final Consumer<StreamThread>consumer)
end note
:consumer.accept(thread);
:return copy.size();;
stop
end
start
note right
Set<org.apache.kafka.streams.processor.ThreadMetadata>localThreadsMetadata()
end note
:return metadataForLocalThreads().stream()
.map(threadMetadata->new org.apache.kafka.streams.processor.ThreadMetadata(threadMetadata.threadName(),
threadMetadata.threadState(),threadMetadata.consumerClientId(),
threadMetadata.restoreConsumerClientId(),
threadMetadata.producerClientIds(),threadMetadata.adminClientId(),
threadMetadata.activeTasks().stream().map(taskMetadata->new org.apache.kafka.streams.processor.TaskMetadata(taskMetadata.taskId().toString(),taskMetadata.topicPartitions(),taskMetadata.committedOffsets(),taskMetadata.endOffsets(),taskMetadata.timeCurrentIdlingStarted())).collect(Collectors.toSet()),threadMetadata.standbyTasks().stream().map(taskMetadata->new org.apache.kafka.streams.processor.TaskMetadata(taskMetadata.taskId().toString(),taskMetadata.topicPartitions(),taskMetadata.committedOffsets(),taskMetadata.endOffsets(),taskMetadata.timeCurrentIdlingStarted())).collect(Collectors.toSet()))).collect(Collectors.toSet());;
stop
end
start
note right
Set<ThreadMetadata>metadataForLocalThreads()
end note
:processStreamThread(thread->{synchronized (thread.getStateLock()){if (thread.state()!=StreamThread.State.DEAD){threadMetadata.add(thread.threadMetadata());}}});
:return threadMetadata;;
stop
end
start
note right
Map<String,Map<Integer,LagInfo>>allLocalStorePartitionLags()
end note
:processStreamThread(thread->allTasks.addAll(thread.allTasks()
.values()));
:return allLocalStorePartitionLags(allTasks);;
stop
end
start
note right
Map<String,Map<Integer,LagInfo>>allLocalStorePartitionLags(final List<Task>tasksToCollectLagFor)
end note
:allPartitions.addAll(task.changelogPartitions());
:allChangelogPositions.putAll(task.changelogOffsets());
:log.debug("Current changelog positions: {}",
allChangelogPositions);
:allEndOffsets=fetchEndOffsets(allPartitions,
adminClient);
:log.debug("Current end offsets :{}",allEndOffsets);
:localStorePartitionLags.computeIfAbsent(storeName,
ignored->new TreeMap<>()).put(entry.getKey().partition(),
lagInfo);
:return Collections.unmodifiableMap(localStorePartitionLags);;
stop
end
if (!topologyMetadata.hasStore(storeName)) then (yes) 
:throw new UnknownStateStoreException("Cannot get state store "+
storeName+" because no such store is registered in the topology.");;
endif 
if (state().hasNotStarted()) then (yes) 
:throw new StreamsNotStartedException("KafkaStreams has not been started,
 you can retry after calling start().");;
endif 
if (state().isShuttingDown()||state.hasCompletedShutdown()) then (yes) 
:throw new StreamsStoppedException("KafkaStreams has been stopped ("+
state+")."+" This instance can no longer serve queries.");;
endif 
if (globalStateStores.containsKey(storeName)) then (yes) 
:result.setGlobalResult(QueryResult.forFailure(FailureReason.UNKNOWN_QUERY_TYPE,
"Global stores do not yet support the KafkaStreams#query API. Use KafkaStreams#store instead."));
else (no)
if (request.isAllPartitions()||request.getPartitions()
.contains(partition)) then (yes) 
if (store!=null ) then (yes) 
if (request.isRequireActive()&&(state!=StreamThread.State.RUNNING||!active)) then (yes) 
:result.addResult(partition,QueryResult.forFailure(FailureReason.NOT_ACTIVE,"Query requires a running active task,"+
" but partition was in state "+state+" and was "+
(active? String.valueOf("active"):String.valueOf("not active"))+
"."));
else (no)
:result.addResult(partition,r);
endif 
if (!request.isAllPartitions()&&result.getPartitionResults()
.keySet().containsAll(request.getPartitions())) then (yes) 
:return result;;
stop
endif 
endif 
endif 
endif 
if (!request.isAllPartitions()) then (yes) 
if (!result.getPartitionResults().containsKey(partition)) then (yes) 
:result.addResult(partition,QueryResult.forFailure(FailureReason.NOT_PRESENT,
"The requested partition was not present at the time of the query."));
endif 
endif 
:return result;;
stop
@enduml

PlantUML version 1.2022.7(Mon Aug 22 17:01:30 UTC 2022)
(GPL source distribution)
Java Runtime: OpenJDK Runtime Environment
JVM: OpenJDK 64-Bit Server VM
Default Encoding: UTF-8
Language: en
Country: null
--></g></svg>