@startuml
!pragma layout smetana
java.lang.Object <|-- org.apache.kafka.streams.state.internals.StoreQueryUtils
class org.apache.kafka.streams.state.internals.StoreQueryUtils {
+  StoreQueryUtils()
+  updatePosition(Position;StateStoreContext)
+  isPermitted(Position;PositionBound;int)
+  checkpointPosition(OffsetCheckpoint;Position)
+  readPositionFromCheckpoint(OffsetCheckpoint)
+  positionToTopicPartitionMap(Position)
+  topicPartitionMapToPosition(Map<TopicPartition,Long>)
} 
@enduml
