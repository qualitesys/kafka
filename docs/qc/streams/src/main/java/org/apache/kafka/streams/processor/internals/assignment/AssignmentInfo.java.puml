@startuml
!pragma layout smetana
java.lang.Object <|-- org.apache.kafka.streams.processor.internals.assignment.AssignmentInfo
class org.apache.kafka.streams.processor.internals.assignment.AssignmentInfo {
+  AssignmentInfo(int;int)
+  AssignmentInfo(int;List<TaskId>;Map<TaskId,Set<TopicPartition>>;Map<HostInfo,Set<TopicPartition>>;Map<HostInfo,Set<TopicPartition>>;int)
+  AssignmentInfo(int;int;List<TaskId>;Map<TaskId,Set<TopicPartition>>;Map<HostInfo,Set<TopicPartition>>;Map<HostInfo,Set<TopicPartition>>;int)
+  setNextRebalanceTime(long)
+  version()
+  errCode()
+  commonlySupportedVersion()
+  activeTasks()
+  standbyTasks()
+  partitionsByHost()
+  standbyPartitionByHost()
+  nextRebalanceMs()
+  encode()
+  encodeActiveAndStandbyTaskAssignment(DataOutputStream)
+  encodePartitionsByHost(DataOutputStream)
+  encodeHostPartitionMapUsingDictionary(DataOutputStream;Map<String,Integer>;Map<HostInfo,Set<TopicPartition>>)
+  encodeTopicDictionaryAndGet(DataOutputStream;Set<TopicPartition>)
+  encodePartitionsByHostAsDictionary(DataOutputStream)
+  encodeActiveAndStandbyHostPartitions(DataOutputStream)
+  writeHostInfo(DataOutputStream;HostInfo)
+  writeTopicPartitions(DataOutputStream;Set<TopicPartition>)
+  decode(ByteBuffer)
+  decodeActiveTasks(AssignmentInfo;DataInputStream)
+  decodeStandbyTasks(AssignmentInfo;DataInputStream)
+  decodePartitionsByHost(AssignmentInfo;DataInputStream)
+  readTopicPartitions(DataInputStream)
+  decodeTopicIndexAndGet(DataInputStream)
+  decodeHostPartitionMapUsingDictionary(DataInputStream;Map<Integer,String>)
+  decodePartitionsByHostUsingDictionary(AssignmentInfo;DataInputStream)
+  decodeActiveAndStandbyHostPartitions(AssignmentInfo;DataInputStream)
+  readTopicPartitions(DataInputStream;Map<Integer,String>)
+  hashCode()
+  equals(Object)
+  toString()
} 
@enduml
