<?xml version="1.0" encoding="UTF-8" standalone="no"?><svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" contentStyleType="text/css" height="544px" preserveAspectRatio="none" style="width:1063px;height:544px;background:#000000;" version="1.1" viewBox="0 0 1063 544" width="1063px" zoomAndPan="magnify"><defs/><g><rect fill="#040506" height="1" style="stroke:#040506;stroke-width:1.0;" width="1" x="0" y="0"/><text fill="#33FF02" font-family="sans-serif" font-size="12" font-style="italic" font-weight="bold" lengthAdjust="spacing" textLength="127" x="5" y="17">PlantUML 1.2022.7</text><rect fill="#33FF02" height="21.2969" style="stroke:#33FF02;stroke-width:1.0;" width="1051" x="5" y="26.9688"/><text fill="#000000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="188" x="6" y="41.9688">[From string (line 354) ]</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="5" x="5" y="62.2656"> </text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="79" x="5" y="78.5625">@startuml</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="190" x="5" y="94.8594">!pragma layout smetana</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="111.1563">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="127.4531">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="362" x="5" y="143.75">constructor ChangelogState(final Stringname,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="15" x="5" y="160.0469">...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="185" x="5" y="176.3438">... ( skipping 329 lines )</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="15" x="5" y="192.6406">...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="70" x="5" y="208.9375">else (no)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="200" x="5" y="225.2344">:lastRestoreLogTime=0L;</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="40" x="5" y="241.5313">endif</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="30" x="5" y="257.8281">end</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="274.125">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="290.4219">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="407" x="5" y="306.7188">StringgetPositionString(final TopicPartitionpartition,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="354" x="5" y="323.0156">final ChangelogMetadatachangelogMetadata)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="71" x="5" y="339.3125">end note</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="596" x="5" y="355.6094">:return offsets==null ? String.valueOf("unknown"):String.valueOf(offsets);;</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="35" x="5" y="371.9063">stop</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="30" x="5" y="388.2031">end</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="404.5">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="420.7969">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="568" x="5" y="437.0938">void maybeUpdateLimitOffsetsForStandbyChangelogs(final Map&lt;TaskId,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="97" x="5" y="453.3906">Task&gt;tasks)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="71" x="5" y="469.6875">end note</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="1051" x="5" y="485.9844">if (state==ChangelogReaderState.STANDBY_UPDATING&amp;&amp;updateOffsetIntervalMs&lt;time.milliseconds()-lastUpdateOffsetTime) then  ...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="378" x="5" y="502.2813">if (!changelogs.get(partition).bufferedRecords()</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" text-decoration="wavy underline" textLength="170" x="5" y="518.5781">.isEmpty()) then (yes)</text><text fill="#FF0000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="106" x="10" y="534.875">Syntax Error?</text><!--MD5=[e90f44c27388dcf3847326eabfba84d7]
@startuml
!pragma layout smetana
start
note right
constructor ChangelogState(final Stringname,
final Integer... prevStates)
end note
:this.name=name;
:this.prevStates=Arrays.asList(prevStates);
end
start
note right
constructor ChangelogReaderState(final Stringname)
end note
:this.name=name;
end
start
note right
constructor ChangelogMetadata(final StateStoreMetadatastoreMetadata,
final ProcessorStateManagerstateManager)
end note
:this.changelogState=ChangelogState.REGISTERED;
:this.storeMetadata=storeMetadata;
:this.stateManager=stateManager;
:this.restoreEndOffset=null ;
:this.totalRestored=0L;
:this.bufferedRecords=new ArrayList<>();
:this.bufferedLimitIndex=0;
end
start
note right
void clear()
end note
:this.bufferedRecords.clear();
end
start
note right
void transitTo(final ChangelogStatenewState)
end note
if (newState.prevStates.contains(changelogState.ordinal())) then (yes) 
:changelogState=newState;
else (no)
:throw new IllegalStateException("Invalid transition from "+
changelogState+" to "+newState);;
endif 
end
start
note right
StringtoString()
end note
:return changelogState+" "+stateManager.taskType()+
" (currentOffset "+currentOffset+", endOffset "+
restoreEndOffset+")";;
stop
end
start
note right
ChangelogStatestate()
end note
:return changelogState;;
stop
end
start
note right
longtotalRestored()
end note
:return totalRestored;;
stop
end
start
note right
LongendOffset()
end note
:return restoreEndOffset;;
stop
end
start
note right
List<ConsumerRecord<byte[],byte[]>>bufferedRecords()
end note
:return bufferedRecords;;
stop
end
start
note right
intbufferedLimitIndex()
end note
:return bufferedLimitIndex;;
stop
end
start
note right
constructor StoreChangelogReader(final Timetime,
final StreamsConfigconfig,final LogContextlogContext,
final AdminadminClient,final Consumer<byte[],
byte[]>restoreConsumer,final StateRestoreListenerstateRestoreListener)
end note
:this.time=time;
:this.log=logContext.logger(StoreChangelogReader.class );
:this.state=ChangelogReaderState.ACTIVE_RESTORING;
:this.adminClient=adminClient;
:this.restoreConsumer=restoreConsumer;
:this.stateRestoreListener=stateRestoreListener;
:this.groupId=config.getString(StreamsConfig.APPLICATION_ID_CONFIG);
:this.pollTime=Duration.ofMillis(config.getLong(StreamsConfig.POLL_MS_CONFIG));
:this.updateOffsetIntervalMs=config.getLong(StreamsConfig.COMMIT_INTERVAL_MS_CONFIG)==Long.MAX_VALUE? DEFAULT_OFFSET_UPDATE_MS:config.getLong(StreamsConfig.COMMIT_INTERVAL_MS_CONFIG);
:this.lastUpdateOffsetTime=0L;
:this.changelogs=new HashMap<>();
end
start
note right
StringrecordEndOffset(final LongendOffset)
end note
:return endOffset==null ? String.valueOf("UNKNOWN (since it is for standby task)"):endOffset.toString();;
stop
end
start
note right
booleanhasRestoredToEnd(final ChangelogMetadatametadata)
end note
if (endOffset==null ) then (yes) 
:throw new IllegalStateException("End offset for changelog "+
metadata+" is unknown when deciding "+"if it has completed restoration,
 this should never happen.");;
else (no)
:return metadata.bufferedRecords.get(0).offset()>=endOffset;;
stop
endif 
end
start
note right
void enforceRestoreActive()
end note
if (state!=ChangelogReaderState.ACTIVE_RESTORING) then (yes) 
:log.debug("Transiting to restore active tasks: {}",
changelogs);
:lastRestoreLogTime=0L;
:pauseChangelogsFromRestoreConsumer(standbyRestoringChangelogs());
:state=ChangelogReaderState.ACTIVE_RESTORING;
endif 
end
start
note right
void transitToUpdateStandby()
end note
if (state!=ChangelogReaderState.ACTIVE_RESTORING) then (yes) 
:throw new IllegalStateException("The changelog reader is not restoring active tasks (is "+
state+") while trying to "+"transit to update standby tasks: "+
changelogs);;
endif 
:log.debug("Transiting to update standby tasks: {}",
changelogs);
:resumeChangelogsFromRestoreConsumer(standbyRestoringChangelogs());
:state=ChangelogReaderState.STANDBY_UPDATING;
end
start
note right
void register(final TopicPartitionpartition,
final ProcessorStateManagerstateManager)
end note
if (storeMetadata==null ) then (yes) 
:throw new IllegalStateException("Cannot find the corresponding state store metadata for changelog "+
partition);;
endif 
if (stateManager.taskType()==Task.TaskType.STANDBY&&stateManager.changelogAsSource(partition)) then (yes) 
:changelogMetadata.restoreEndOffset=0L;
endif 
if (changelogs.putIfAbsent(partition,changelogMetadata)!=null ) then (yes) 
:throw new IllegalStateException("There is already a changelog registered for "+
partition+", this should not happen: "+changelogs);;
endif 
end
start
note right
ChangelogMetadatarestoringChangelogByPartition(final TopicPartitionpartition)
end note
if (changelogMetadata==null ) then (yes) 
:throw new IllegalStateException("The corresponding changelog restorer for "+
partition+" does not exist, this should not happen.");;
endif 
if (changelogMetadata.changelogState!=ChangelogState.RESTORING) then (yes) 
:throw new IllegalStateException("The corresponding changelog restorer for "+
partition+" has already transited to completed state,
 this should not happen.");;
endif 
:return changelogMetadata;;
stop
end
start
note right
Set<ChangelogMetadata>registeredChangelogs()
end note
:return changelogs.values().stream().filter(metadata->metadata.changelogState==ChangelogState.REGISTERED)
.collect(Collectors.toSet());;
stop
end
start
note right
Set<TopicPartition>restoringChangelogs()
end note
:return changelogs.values().stream().filter(metadata->metadata.changelogState==ChangelogState.RESTORING)
.map(metadata->metadata.storeMetadata.changelogPartition())
.collect(Collectors.toSet());;
stop
end
start
note right
Set<TopicPartition>activeRestoringChangelogs()
end note
:return changelogs.values().stream().filter(metadata->metadata.changelogState==ChangelogState.RESTORING&&metadata.stateManager.taskType()==Task.TaskType.ACTIVE).map(metadata->metadata.storeMetadata.changelogPartition()).collect(Collectors.toSet());;
stop
end
start
note right
Set<TopicPartition>standbyRestoringChangelogs()
end note
:return changelogs.values().stream().filter(metadata->metadata.changelogState==ChangelogState.RESTORING&&metadata.stateManager.taskType()==Task.TaskType.STANDBY).map(metadata->metadata.storeMetadata.changelogPartition()).collect(Collectors.toSet());;
stop
end
start
note right
booleanallChangelogsCompleted()
end note
:return changelogs.values().stream().allMatch(metadata->metadata.changelogState==ChangelogState.COMPLETED);;
stop
end
start
note right
Set<TopicPartition>completedChangelogs()
end note
:return changelogs.values().stream().filter(metadata->metadata.changelogState==ChangelogState.COMPLETED)
.map(metadata->metadata.storeMetadata.changelogPartition())
.collect(Collectors.toSet());;
stop
end
start
note right
void restore(final Map<TaskId,Task>tasks)
end note
:initializeChangelogs(tasks,registeredChangelogs());
if (!activeRestoringChangelogs().isEmpty()&&state==ChangelogReaderState.STANDBY_UPDATING) then (yes) 
:throw new IllegalStateException("Should not be in standby updating state if there are still un-completed active changelogs");;
endif 
if (allChangelogsCompleted()) then (yes) 
:log.debug("Finished restoring all changelogs {}",
changelogs.keySet());
:return ;;
stop
endif 
if (!restoringChangelogs.isEmpty()) then (yes) 
:pauseResumePartitions(tasks,restoringChangelogs);
:polledRecords=restoreConsumer.poll(state==ChangelogReaderState.STANDBY_UPDATING? Duration.ZERO:pollTime);
:log.warn("Encountered "+e.getClass().getName()+
" fetching records from restore consumer for partitions "+
e.partitions()+", it is likely that "+"the consumer's position has fallen out of the topic partition offset range because the topic was "+"truncated or compacted on the broker, marking the corresponding tasks as corrupted and re-initializing"+" it later.",e);
:e.partitions().forEach(partition->corruptedTasks.add(changelogs.get(partition)
.stateManager.taskId()));
:throw new TaskCorruptedException(corruptedTasks,e);;
:throw new StreamsException("Restore consumer get unexpected error polling records.",e);;
:bufferChangelogRecords(restoringChangelogByPartition(partition),
polledRecords.records(partition));
if (restoreChangelog(changelogs.get(partition))) then (yes) 
if (task!=null ) then (yes) 
:task.clearTaskTimeout();
endif 
endif 
:tasks.get(taskId).maybeInitTaskTimeoutOrThrow(time.milliseconds(),
timeoutException);
:maybeUpdateLimitOffsetsForStandbyChangelogs(tasks);
:maybeLogRestorationProgress();
endif 
end
start
note right
void pauseResumePartitions(final Map<TaskId,
Task>tasks,final Set<TopicPartition>restoringChangelogs)
end note
if (state==ChangelogReaderState.ACTIVE_RESTORING) then (yes) 
:updatePartitionsByType(tasks,restoringChangelogs,
TaskType.ACTIVE);
endif 
if (state==ChangelogReaderState.STANDBY_UPDATING) then (yes) 
:updatePartitionsByType(tasks,restoringChangelogs,
TaskType.STANDBY);
endif 
end
start
note right
void updatePartitionsByType(final Map<TaskId,
Task>tasks,final Set<TopicPartition>restoringChangelogs,
final TaskTypetaskType)
end note
:restoreConsumer.resume(toResume);
:restoreConsumer.pause(toPause);
end
start
note right
booleanshouldResume(final Map<TaskId,Task>tasks,
final TopicPartitionpartition,final TaskTypetaskType)
end note
if (manager.taskType()==taskType) then (yes) 
:return task!=null ;;
stop
endif 
:return false;;
stop
end
start
note right
booleanshouldPause(final Map<TaskId,Task>tasks,
final TopicPartitionpartition,final TaskTypetaskType)
end note
if (manager.taskType()==taskType) then (yes) 
:return task==null ;;
stop
endif 
:return false;;
stop
end
start
note right
void maybeLogRestorationProgress()
end note
if (state==ChangelogReaderState.ACTIVE_RESTORING) then (yes) 
if (time.milliseconds()-lastRestoreLogTime>RESTORE_LOG_INTERVAL_MS) then (yes) 
if (!topicPartitions.isEmpty()) then (yes) 
:builder.append(" {").append(partition).append(": ").append("position=").append(getPositionString(partition,
changelogMetadata)).append(", end=").append(changelogMetadata.restoreEndOffset).append(",
 totalRestored=").append(changelogMetadata.totalRestored)
.append("}");
:log.info(builder.toString());
:lastRestoreLogTime=time.milliseconds();
endif 
endif 
else (no)
:lastRestoreLogTime=0L;
endif 
end
start
note right
StringgetPositionString(final TopicPartitionpartition,
final ChangelogMetadatachangelogMetadata)
end note
:return offsets==null ? String.valueOf("unknown"):String.valueOf(offsets);;
stop
end
start
note right
void maybeUpdateLimitOffsetsForStandbyChangelogs(final Map<TaskId,
Task>tasks)
end note
if (state==ChangelogReaderState.STANDBY_UPDATING&&updateOffsetIntervalMs<time.milliseconds()-lastUpdateOffsetTime) then (yes) 
if (!changelogs.get(partition).bufferedRecords()
.isEmpty()) then (yes) 
:updateLimitOffsetsForStandbyChangelogs(committedOffsetForChangelogs(tasks,
changelogsWithLimitOffsets));
:lastUpdateOffsetTime=time.milliseconds();
endif 
endif 
end
start
note right
void bufferChangelogRecords(final ChangelogMetadatachangelogMetadata,
final List<ConsumerRecord<byte[],byte[]>>records)
end note
if (record.key()==null ) then (yes) 
:log.warn("Read changelog record with null key from changelog {} at offset {}, "+
"skipping it for restoration",changelogMetadata.storeMetadata.changelogPartition(),
record.offset());
else (no)
:changelogMetadata.bufferedRecords.add(record);
if (changelogMetadata.restoreEndOffset==null ||offset<changelogMetadata.restoreEndOffset) then (yes) 
:changelogMetadata.bufferedLimitIndex=changelogMetadata.bufferedRecords.size();
endif 
endif 
end
start
note right
booleanrestoreChangelog(final ChangelogMetadatachangelogMetadata)
end note
if (numRecords!=0) then (yes) 
:madeProgress=true;
:stateManager.restore(storeMetadata,records);
if (numRecords<changelogMetadata.bufferedRecords.size()) then (yes) 
:records.clear();
else (no)
:changelogMetadata.bufferedRecords.clear();
endif 
:log.trace("Restored {} records from changelog {} to store {},
 end offset is {}, current offset is {}",
partition,storeName,numRecords,recordEndOffset(changelogMetadata.restoreEndOffset),
currentOffset);
:changelogMetadata.bufferedLimitIndex=0;
:changelogMetadata.totalRestored+=numRecords;
if (changelogMetadata.stateManager.taskType()==Task.TaskType.ACTIVE) then (yes) 
:stateRestoreListener.onBatchRestored(partition,
storeName,currentOffset,numRecords);
:throw new StreamsException("State restore listener failed on batch restored",e);;
endif 
endif 
if (changelogMetadata.stateManager.taskType()==Task.TaskType.ACTIVE&&hasRestoredToEnd(changelogMetadata)) then (yes) 
:madeProgress=true;
:log.info("Finished restoring changelog {} to store {} with a total number of {} records",
partition,storeName,changelogMetadata.totalRestored);
:changelogMetadata.transitTo(ChangelogState.COMPLETED);
:pauseChangelogsFromRestoreConsumer(Collections.singleton(partition));
:stateRestoreListener.onRestoreEnd(partition,
storeName,changelogMetadata.totalRestored);
:throw new StreamsException("State restore listener failed on restore completed",e);;
endif 
:return madeProgress;;
stop
end
start
note right
Set<Task>getTasksFromPartitions(final Map<TaskId,
Task>tasks,final Set<TopicPartition>partitions)
end note
:result.add(tasks.get(changelogs.get(partition)
.stateManager.taskId()));
:return result;;
stop
end
start
note right
void clearTaskTimeout(final Set<Task>tasks)
end note
:tasks.forEach(t->{if (t!=null ){t.clearTaskTimeout();}});
end
start
note right
void maybeInitTaskTimeoutOrThrow(final Set<Task>tasks,
final Exceptioncause)
end note
:tasks.forEach(t->t.maybeInitTaskTimeoutOrThrow(now,
cause));
end
start
note right
Map<TopicPartition,Long>committedOffsetForChangelogs(final Map<TaskId,
Task>tasks,final Set<TopicPartition>partitions)
end note
if (partitions.isEmpty()) then (yes) 
:return Collections.emptyMap();;
stop
endif 
:options.topicPartitions(new ArrayList<>(partitions));
:options.requireStable(true);
:clearTaskTimeout(getTasksFromPartitions(tasks,
partitions));
:return committedOffsets;;
stop
:log.debug("Could not retrieve the committed offsets for partitions {} due to {},
 will retry in the next run loop",partitions,
retriableException.toString());
:maybeInitTaskTimeoutOrThrow(getTasksFromPartitions(tasks,
partitions),retriableException);
:return Collections.emptyMap();;
stop
:throw new StreamsException(String.format("Failed to retrieve committed offsets for %s",
partitions),e);;
end
start
note right
Map<TopicPartition,Long>endOffsetForChangelogs(final Map<TaskId,
Task>tasks,final Set<TopicPartition>partitions)
end note
if (partitions.isEmpty()) then (yes) 
:return Collections.emptyMap();;
stop
endif 
:clearTaskTimeout(getTasksFromPartitions(tasks,
partitions));
:return logEndOffsets;;
stop
:log.debug("Could not fetch all end offsets for {} due to {},
 will retry in the next run loop",partitions,
retriableException.toString());
:maybeInitTaskTimeoutOrThrow(getTasksFromPartitions(tasks,
partitions),retriableException);
:return Collections.emptyMap();;
stop
:throw new StreamsException(String.format("Failed to retrieve end offsets for %s",
partitions),e);;
end
start
note right
void updateLimitOffsetsForStandbyChangelogs(final Map<TopicPartition,
Long>committedOffsets)
end note
if (metadata.stateManager.taskType()==Task.TaskType.STANDBY&&metadata.stateManager.changelogAsSource(partition)&&committedOffsets.containsKey(partition)) then (yes) 
if (previousLimit!=null &&previousLimit>newLimit) then (yes) 
:throw new IllegalStateException("Offset limit should monotonically increase, but was reduced for partition "+
partition+". New limit: "+newLimit+". Previous limit: "+
previousLimit);;
endif 
:metadata.restoreEndOffset=newLimit;
while (metadata.bufferedLimitIndex<metadata.bufferedRecords.size()&&metadata.bufferedRecords.get(metadata.bufferedLimitIndex).offset()<metadata.restoreEndOffset)
:metadata.bufferedLimitIndex++;
endwhile
endif 
end
start
note right
void initializeChangelogs(final Map<TaskId,
Task>tasks,final Set<ChangelogMetadata>newPartitionsToRestore)
end note
if (newPartitionsToRestore.isEmpty()) then (yes) 
:return ;;
stop
endif 
if (metadata.stateManager.taskType()==Task.TaskType.ACTIVE) then (yes) 
:newPartitionsToFindEndOffset.add(partition);
endif 
if (metadata.stateManager.changelogAsSource(partition)) then (yes) 
:newPartitionsToFindCommittedOffset.add(partition);
endif 
if (endOffset!=null &&committedOffset!=null ) then (yes) 
if (changelogMetadata.restoreEndOffset!=null ) then (yes) 
:throw new IllegalStateException("End offset for "+
partition+" should only be initialized once. Existing value: "+
changelogMetadata.restoreEndOffset+", new value: ("+
endOffset+", "+committedOffset+")");;
endif 
:changelogMetadata.restoreEndOffset=Math.min(endOffset,
committedOffset);
:log.debug("End offset for changelog {} initialized as {}.",
partition,changelogMetadata.restoreEndOffset);
else (no)
if (!newPartitionsToRestore.remove(changelogMetadata)) then (yes) 
:throw new IllegalStateException("New changelogs to restore "+
newPartitionsToRestore+" does not contain the one looking for end offset: "+
partition+", this should not happen.");;
endif 
:log.info("End offset for changelog {} cannot be found; will retry in the next time.",
partition);
endif 
if (!committedOffsets.isEmpty()) then (yes) 
:updateLimitOffsetsForStandbyChangelogs(committedOffsets);
endif 
:addChangelogsToRestoreConsumer(newPartitionsToRestore.stream()
.map(metadata->metadata.storeMetadata.changelogPartition())
.collect(Collectors.toSet()));
:newPartitionsToRestore.forEach(metadata->metadata.transitTo(ChangelogState.RESTORING));
if (state==ChangelogReaderState.ACTIVE_RESTORING) then (yes) 
:pauseChangelogsFromRestoreConsumer(standbyRestoringChangelogs());
endif 
:prepareChangelogs(newPartitionsToRestore);
end
start
note right
void addChangelogsToRestoreConsumer(final Set<TopicPartition>partitions)
end note
if (assignment.removeAll(partitions)) then (yes) 
:throw new IllegalStateException("The current assignment "+
restoreConsumer.assignment()+" "+"already contains some of the new partitions "+
partitions);;
endif 
:assignment.addAll(partitions);
:restoreConsumer.assign(assignment);
:log.debug("Added partitions {} to the restore consumer,
 current assignment is {}",partitions,assignment);
end
start
note right
void pauseChangelogsFromRestoreConsumer(final Collection<TopicPartition>partitions)
end note
if (!assignment.containsAll(partitions)) then (yes) 
:throw new IllegalStateException("The current assignment "+
assignment+" "+"does not contain some of the partitions "+
partitions+" for pausing.");;
endif 
:restoreConsumer.pause(partitions);
:log.debug("Paused partitions {} from the restore consumer",
partitions);
end
start
note right
void removeChangelogsFromRestoreConsumer(final Collection<TopicPartition>partitions)
end note
if (!assignment.containsAll(partitions)) then (yes) 
:throw new IllegalStateException("The current assignment "+
assignment+" "+"does not contain some of the partitions "+
partitions+" for removing.");;
endif 
:assignment.removeAll(partitions);
:restoreConsumer.assign(assignment);
end
start
note right
void resumeChangelogsFromRestoreConsumer(final Collection<TopicPartition>partitions)
end note
if (!assignment.containsAll(partitions)) then (yes) 
:throw new IllegalStateException("The current assignment "+
assignment+" "+"does not contain some of the partitions "+
partitions+" for resuming.");;
endif 
:restoreConsumer.resume(partitions);
:log.debug("Resumed partitions {} from the restore consumer",
partitions);
end
start
note right
void prepareChangelogs(final Set<ChangelogMetadata>newPartitionsToRestore)
end note
if (currentOffset!=null ) then (yes) 
:restoreConsumer.seek(partition,currentOffset+1);
:log.debug("Start restoring changelog partition {} from current offset {} to end offset {}.",
partition,currentOffset,recordEndOffset(endOffset));
else (no)
:log.debug("Start restoring changelog partition {} from the beginning offset to end offset {} "+
"since we cannot find current offset.",partition,
recordEndOffset(endOffset));
:newPartitionsWithoutStartOffset.add(partition);
endif 
if (!newPartitionsWithoutStartOffset.isEmpty()) then (yes) 
:restoreConsumer.seekToBeginning(newPartitionsWithoutStartOffset);
endif 
if (changelogMetadata.stateManager.taskType()==Task.TaskType.ACTIVE) then (yes) 
:startOffset=restoreConsumer.position(partition);
:throw new StreamsException("Restore consumer get unexpected error trying to get the position "+
" of "+partition,e);;
:stateRestoreListener.onRestoreStart(partition,
storeName,startOffset,changelogMetadata.restoreEndOffset);
:throw new StreamsException("State restore listener failed on batch restored",e);;
endif 
end
start
note right
void unregister(final Collection<TopicPartition>revokedChangelogs)
end note
if (changelogMetadata!=null ) then (yes) 
if (!changelogMetadata.state().equals(ChangelogState.REGISTERED)) then (yes) 
:revokedInitializedChangelogs.add(partition);
endif 
:changelogMetadata.clear();
else (no)
:log.debug("Changelog partition {} could not be found,"+
" it could be already cleaned up during the handling"+
" of task corruption and never restore again",
partition);
endif 
:removeChangelogsFromRestoreConsumer(revokedInitializedChangelogs);
end
start
note right
void clear()
end note
:changelogMetadata.clear();
:changelogs.clear();
:restoreConsumer.unsubscribe();
:throw new StreamsException("Restore consumer get unexpected error unsubscribing",e);;
end
start
note right
booleanisEmpty()
end note
:return changelogs.isEmpty();;
stop
end
start
note right
StringtoString()
end note
:return "StoreChangelogReader: "+changelogs+
"\n";;
stop
end
start
note right
ChangelogMetadatachangelogMetadata(final TopicPartitionpartition)
end note
:return changelogs.get(partition);;
stop
end
start
note right
ChangelogReaderStatestate()
end note
:return state;;
stop
end
@enduml

PlantUML version 1.2022.7(Mon Aug 22 17:01:30 UTC 2022)
(GPL source distribution)
Java Runtime: OpenJDK Runtime Environment
JVM: OpenJDK 64-Bit Server VM
Default Encoding: UTF-8
Language: en
Country: null
--></g></svg>