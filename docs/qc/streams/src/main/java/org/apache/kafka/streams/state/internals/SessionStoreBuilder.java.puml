@startuml
!pragma layout smetana
AbstractStoreBuilder_K,V,SessionStore_K,V__ <|-- org.apache.kafka.streams.state.internals.SessionStoreBuilder
class org.apache.kafka.streams.state.internals.SessionStoreBuilder {
+  SessionStoreBuilder(SessionBytesStoreSupplier;Serde<K>;Serde<V>;Time)
+  build()
+  maybeWrapCaching(SessionStore<Bytes,byte[]>)
+  maybeWrapLogging(SessionStore<Bytes,byte[]>)
+  retentionPeriod()
} 
@enduml
