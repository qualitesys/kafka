<?xml version="1.0" encoding="UTF-8" standalone="no"?><svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" contentStyleType="text/css" height="1193px" preserveAspectRatio="none" style="width:1027px;height:1193px;background:#FFFFFF;" version="1.1" viewBox="0 0 1027 1193" width="1027px" zoomAndPan="magnify"><defs/><g><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="603" x="5" y="16.1387">An error has occured : java.lang.IndexOutOfBoundsException: Index 0 out of bounds for length 0</text><text fill="#000000" font-family="sans-serif" font-size="12" font-style="italic" lengthAdjust="spacing" textLength="457" x="5" y="30.1074">We all must learn from small misfortune, count the blessings that are real</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="44.0762"> </text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="280" x="5" y="58.0449">Diagram size: 1007 lines / 39821 characters.</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="72.0137"> </text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="374" x="5" y="85.9824">PlantUML (1.2022.7) cannot parse result from dot/GraphViz.</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="99.9512"> </text><text fill="#000000" font-family="sans-serif" font-size="12" font-weight="bold" lengthAdjust="spacing" textLength="390" x="5" y="113.9199">This version of PlantUML is 358 days old, so you should</text><text fill="#000000" font-family="sans-serif" font-size="12" font-weight="bold" lengthAdjust="spacing" textLength="398" x="5" y="127.8887">consider upgrading from https://plantuml.com/download</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="141.8574"> </text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="492" x="5" y="155.8262">Please go to https://plantuml.com/graphviz-dot to check your GraphViz version.</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="169.7949"> </text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="277" x="5" y="183.7637">Java Runtime: OpenJDK Runtime Environment</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="187" x="5" y="197.7324">JVM: OpenJDK 64-Bit Server VM</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="150" x="5" y="211.7012">Default Encoding: UTF-8</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="86" x="5" y="225.6699">Language: en</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="78" x="5" y="239.6387">Country: null</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="253.6074"> </text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="172" x="5" y="267.5762">PLANTUML_LIMIT_SIZE: 4096</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="281.5449"> </text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="150" x="5" y="295.5137">This may be caused by :</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="120" x="9" y="309.4824">- a bug in PlantUML</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="144" x="9" y="323.4512">- a problem in GraphViz</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="337.4199"> </text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="300" x="5" y="351.3887">You should send this diagram and this image to</text><text fill="#000000" font-family="sans-serif" font-size="12" font-weight="bold" lengthAdjust="spacing" textLength="145" x="309" y="351.3887">plantuml@gmail.com</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="13" x="458" y="351.3887">or</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="45" x="5" y="365.3574">post to</text><text fill="#000000" font-family="sans-serif" font-size="12" font-weight="bold" lengthAdjust="spacing" textLength="168" x="54" y="365.3574">https://plantuml.com/qa</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="117" x="226" y="365.3574">to solve this issue.</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="405" x="5" y="379.3262">You can try to turn arround this issue by simplifing your diagram.</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="393.2949"> </text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="458" x="5" y="407.2637">java.lang.IndexOutOfBoundsException: Index 0 out of bounds for length 0</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="477" x="13" y="421.2324">java.base/jdk.internal.util.Preconditions.outOfBounds(Preconditions.java:64)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="548" x="13" y="435.2012">java.base/jdk.internal.util.Preconditions.outOfBoundsCheckIndex(Preconditions.java:70)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="474" x="13" y="449.1699">java.base/jdk.internal.util.Preconditions.checkIndex(Preconditions.java:248)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="351" x="13" y="463.1387">java.base/java.util.Objects.checkIndex(Objects.java:372)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="314" x="13" y="477.1074">java.base/java.util.ArrayList.get(ArrayList.java:459)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="878" x="13" y="491.0762">net.sourceforge.plantuml.activitydiagram3.ftile.vcompact.FtileFactoryDelegatorSwitch.createWithLinks(FtileFactoryDelegatorSwitch.java:103)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="852" x="13" y="505.0449">net.sourceforge.plantuml.activitydiagram3.ftile.vcompact.FtileFactoryDelegatorSwitch.createSwitch(FtileFactoryDelegatorSwitch.java:79)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="716" x="13" y="519.0137">net.sourceforge.plantuml.activitydiagram3.ftile.FtileFactoryDelegator.createSwitch(FtileFactoryDelegator.java:185)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="716" x="13" y="532.9824">net.sourceforge.plantuml.activitydiagram3.ftile.FtileFactoryDelegator.createSwitch(FtileFactoryDelegator.java:185)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="716" x="13" y="546.9512">net.sourceforge.plantuml.activitydiagram3.ftile.FtileFactoryDelegator.createSwitch(FtileFactoryDelegator.java:185)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="716" x="13" y="560.9199">net.sourceforge.plantuml.activitydiagram3.ftile.FtileFactoryDelegator.createSwitch(FtileFactoryDelegator.java:185)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="716" x="13" y="574.8887">net.sourceforge.plantuml.activitydiagram3.ftile.FtileFactoryDelegator.createSwitch(FtileFactoryDelegator.java:185)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="621" x="13" y="588.8574">net.sourceforge.plantuml.activitydiagram3.InstructionSwitch.createFtile(InstructionSwitch.java:119)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="583" x="13" y="602.8262">net.sourceforge.plantuml.activitydiagram3.InstructionList.createFtile(InstructionList.java:131)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="536" x="13" y="616.7949">net.sourceforge.plantuml.activitydiagram3.ftile.Swimlanes.drawU(Swimlanes.java:229)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="805" x="13" y="630.7637">net.sourceforge.plantuml.ugraphic.comp.CompressionXorYBuilder.getPiecewiseAffineTransform(CompressionXorYBuilder.java:51)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="655" x="13" y="644.7324">net.sourceforge.plantuml.ugraphic.comp.CompressionXorYBuilder.build(CompressionXorYBuilder.java:44)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="686" x="13" y="658.7012">net.sourceforge.plantuml.activitydiagram3.ActivityDiagram3.exportDiagramInternal(ActivityDiagram3.java:210)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="502" x="13" y="672.6699">net.sourceforge.plantuml.UmlDiagram.exportDiagramNow(UmlDiagram.java:135)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="538" x="13" y="686.6387">net.sourceforge.plantuml.AbstractPSystem.exportDiagram(AbstractPSystem.java:178)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="566" x="13" y="700.6074">net.sourceforge.plantuml.SourceStringReader.outputImage(SourceStringReader.java:172)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="566" x="13" y="714.5762">net.sourceforge.plantuml.SourceStringReader.outputImage(SourceStringReader.java:143)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="693" x="13" y="728.5449">com.qualitesys.qcrmavenplugin.commun.QcrCommunPlantUml.genererFichierSvg(QcrCommunPlantUml.java:82)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="585" x="13" y="742.5137">com.qualitesys.qcrmavenplugin.QcrReporting01Mojo.lireFichier(QcrReporting01Mojo.java:927)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="556" x="13" y="756.4824">com.qualitesys.qcrmavenplugin.QcrReporting01Mojo.lister(QcrReporting01Mojo.java:453)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="591" x="13" y="770.4512">com.qualitesys.qcrmavenplugin.QcrReporting01Mojo.doReport(QcrReporting01Mojo.java:2037)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="625" x="13" y="784.4199">com.qualitesys.qcrmavenplugin.QcrReporting01Mojo.executeReport(QcrReporting01Mojo.java:1739)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="582" x="13" y="798.3887">org.apache.maven.reporting.AbstractMavenReport.generate(AbstractMavenReport.java:251)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="748" x="13" y="812.3574">org.apache.maven.plugins.site.render.ReportDocumentRenderer.renderDocument(ReportDocumentRenderer.java:226)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="607" x="13" y="826.3262">org.apache.maven.doxia.siterenderer.DefaultSiteRenderer.render(DefaultSiteRenderer.java:348)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="502" x="13" y="840.2949">org.apache.maven.plugins.site.render.SiteMojo.renderLocale(SiteMojo.java:194)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="470" x="13" y="854.2637">org.apache.maven.plugins.site.render.SiteMojo.execute(SiteMojo.java:143)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="649" x="13" y="868.2324">org.apache.maven.plugin.DefaultBuildPluginManager.executeMojo(DefaultBuildPluginManager.java:137)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="539" x="13" y="882.2012">org.apache.maven.lifecycle.internal.MojoExecutor.doExecute2(MojoExecutor.java:370)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="531" x="13" y="896.1699">org.apache.maven.lifecycle.internal.MojoExecutor.doExecute(MojoExecutor.java:351)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="515" x="13" y="910.1387">org.apache.maven.lifecycle.internal.MojoExecutor.execute(MojoExecutor.java:215)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="515" x="13" y="924.1074">org.apache.maven.lifecycle.internal.MojoExecutor.execute(MojoExecutor.java:171)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="515" x="13" y="938.0762">org.apache.maven.lifecycle.internal.MojoExecutor.execute(MojoExecutor.java:163)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="651" x="13" y="952.0449">org.apache.maven.lifecycle.internal.LifecycleModuleBuilder.buildProject(LifecycleModuleBuilder.java:117)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="643" x="13" y="966.0137">org.apache.maven.lifecycle.internal.LifecycleModuleBuilder.buildProject(LifecycleModuleBuilder.java:81)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="747" x="13" y="979.9824">org.apache.maven.lifecycle.internal.builder.singlethreaded.SingleThreadedBuilder.build(SingleThreadedBuilder.java:56)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="539" x="13" y="993.9512">org.apache.maven.lifecycle.internal.LifecycleStarter.execute(LifecycleStarter.java:128)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="428" x="13" y="1007.9199">org.apache.maven.DefaultMaven.doExecute(DefaultMaven.java:299)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="428" x="13" y="1021.8887">org.apache.maven.DefaultMaven.doExecute(DefaultMaven.java:193)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="412" x="13" y="1035.8574">org.apache.maven.DefaultMaven.execute(DefaultMaven.java:106)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="367" x="13" y="1049.8262">org.apache.maven.cli.MavenCli.execute(MavenCli.java:963)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="362" x="13" y="1063.7949">org.apache.maven.cli.MavenCli.doMain(MavenCli.java:296)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="347" x="13" y="1077.7637">org.apache.maven.cli.MavenCli.main(MavenCli.java:199)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="502" x="13" y="1091.7324">java.base/jdk.internal.reflect.NativeMethodAccessorImpl.invoke0(Native Method)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="621" x="13" y="1105.7012">java.base/jdk.internal.reflect.NativeMethodAccessorImpl.invoke(NativeMethodAccessorImpl.java:62)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="679" x="13" y="1119.6699">java.base/jdk.internal.reflect.DelegatingMethodAccessorImpl.invoke(DelegatingMethodAccessorImpl.java:43)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="373" x="13" y="1133.6387">java.base/java.lang.reflect.Method.invoke(Method.java:566)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="562" x="13" y="1147.6074">org.codehaus.plexus.classworlds.launcher.Launcher.launchEnhanced(Launcher.java:282)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="499" x="13" y="1161.5762">org.codehaus.plexus.classworlds.launcher.Launcher.launch(Launcher.java:225)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="568" x="13" y="1175.5449">org.codehaus.plexus.classworlds.launcher.Launcher.mainWithExitCode(Launcher.java:406)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="487" x="13" y="1189.5137">org.codehaus.plexus.classworlds.launcher.Launcher.main(Launcher.java:347)</text><image height="46" width="107" x="920" xlink:href="data:image/png;base64," y="6"/><!--MD5=[09f899c3bd974b86307bde19ee4fca88]
@startuml
!pragma layout smetana
start
note right
constructor AssignedPartition(final TaskId taskId,
final TopicPartition partition)
end note
:this.taskId=taskId;;
:this.partition=partition;;
end
start
note right
int compareTo(final AssignedPartition that)
end note
:return PARTITION_COMPARATOR.compare(partition,
that.partition);;
stop
end
start
note right
boolean equals(final Object o)
end note
if (!(o instanceof AssignedPartition )) then (yes) 
:return false;;
stop
endif 
:final AssignedPartition other=(AssignedPartition )o;;
:return compareTo(other)==0;;
stop
end
start
note right
int hashCode()
end note
:return partition.hashCode();;
stop
end
start
note right
constructor ClientMetadata(final String endPoint,
final Map<String,String> clientTags)
end note
:hostInfo=HostInfo.buildFromEndpoint(endPoint);;
:consumers=new TreeSet<>();;
:state=new ClientState(clientTags);;
end
start
note right
void addConsumer(final String consumerMemberId,
final List<TopicPartition> ownedPartitions)
end note
:consumers.add(consumerMemberId);;
:state.incrementCapacity();;
:state.addOwnedPartitions(ownedPartitions,
consumerMemberId);;
end
start
note right
void addPreviousTasksAndOffsetSums(final String consumerId,
final Map<TaskId,Long> taskOffsetSums)
end note
:state.addPreviousTasksAndOffsetSums(consumerId,
taskOffsetSums);;
end
start
note right
String toString()
end note
:return "ClientMetadata{"+"hostInfo="+hostInfo+
", consumers="+consumers+", state="+state+
'}';;
stop
end
start
note right
void configure(final Map<String,?> configs)
end note
:final AssignorConfiguration assignorConfiguration=new AssignorConfiguration(configs);;
:logPrefix=assignorConfiguration.logPrefix();;
:log=new LogContext(logPrefix).logger(getClass());;
:usedSubscriptionMetadataVersion=assignorConfiguration.configuredMetadataVersion(usedSubscriptionMetadataVersion);;
:final ReferenceContainer referenceContainer=assignorConfiguration.referenceContainer();;
:mainConsumerSupplier=()->Objects.requireNonNull(referenceContainer.mainConsumer,
"Main consumer was not specified");;
:adminClient=Objects.requireNonNull(referenceContainer.adminClient,
"Admin client was not specified");;
:taskManager=Objects.requireNonNull(referenceContainer.taskManager,
"TaskManager was not specified");;
:streamsMetadataState=Objects.requireNonNull(referenceContainer.streamsMetadataState,
"StreamsMetadataState was not specified");;
:assignmentErrorCode=referenceContainer.assignmentErrorCode;;
:nextScheduledRebalanceMs=referenceContainer.nextScheduledRebalanceMs;;
:nonFatalExceptionsToHandle=referenceContainer.nonFatalExceptionsToHandle;;
:time=Objects.requireNonNull(referenceContainer.time,
"Time was not specified");;
:assignmentConfigs=assignorConfiguration.assignmentConfigs();;
:partitionGrouper=new PartitionGrouper();;
:userEndPoint=assignorConfiguration.userEndPoint();;
:internalTopicManager=assignorConfiguration.internalTopicManager();;
:copartitionedTopicsEnforcer=assignorConfiguration.copartitionedTopicsEnforcer();;
:rebalanceProtocol=assignorConfiguration.rebalanceProtocol();;
:taskAssignorSupplier=assignorConfiguration::taskAssignor;;
:assignmentListener=assignorConfiguration.assignmentListener();;
:uniqueField=0;;
:clientTags=referenceContainer.clientTags;;
end
start
note right
String name()
end note
:return "stream";;
stop
end
start
note right
List<RebalanceProtocol> supportedProtocols()
end note
:final List<RebalanceProtocol> supportedProtocols=new ArrayList<>();;
:supportedProtocols.add(RebalanceProtocol.EAGER);;
if (rebalanceProtocol==RebalanceProtocol.COOPERATIVE) then (yes) 
:supportedProtocols.add(rebalanceProtocol);;
endif 
:return supportedProtocols;;
stop
end
start
note right
ByteBuffer subscriptionUserData(final Set<String> topics)
end note
:handleRebalanceStart(topics);;
:uniqueField++;;
:final Set<String> currentNamedTopologies=taskManager.topologyMetadata()
.namedTopologiesView();;
:final Map<TaskId,Long> taskOffsetSums=taskManager.topologyMetadata().hasNamedTopologies()? filterMap(taskManager.getTaskOffsetSums(),
t->currentNamedTopologies.contains(t.getKey().topologyName())):taskManager.getTaskOffsetSums();;
:return new SubscriptionInfo(usedSubscriptionMetadataVersion,
LATEST_SUPPORTED_VERSION,taskManager.processId(),
userEndPoint,taskOffsetSums,uniqueField,assignmentErrorCode.get(),
clientTags).encode();;
stop
end
start
note right
Map<String,Assignment> errorAssignment(final Map<UUID,
ClientMetadata> clientsMetadata,final int errorCode)
end note
:final Map<String,Assignment> assignment=new HashMap<>();;
:assignment.put(consumerId,new Assignment(Collections.emptyList(),
new AssignmentInfo(LATEST_SUPPORTED_VERSION,
Collections.emptyList(),Collections.emptyMap(),
Collections.emptyMap(),Collections.emptyMap(),
errorCode).encode()));;
:return assignment;;
stop
end
start
note right
GroupAssignment assign(final Cluster metadata,
final GroupSubscription groupSubscription)
end note
:final Map<String,Subscription> subscriptions=groupSubscription.groupSubscription();;
:final Map<UUID,ClientMetadata> clientMetadataMap=new HashMap<>();;
:final Set<TopicPartition> allOwnedPartitions=new HashSet<>();;
:int minReceivedMetadataVersion=LATEST_SUPPORTED_VERSION;;
:int minSupportedMetadataVersion=LATEST_SUPPORTED_VERSION;;
:boolean shutdownRequested=false;;
:boolean assignmentErrorFound=false;;
:int futureMetadataVersion=UNKNOWN;;
:final String consumerId=entry.getKey();;
:final Subscription subscription=entry.getValue();;
:final SubscriptionInfo info=SubscriptionInfo.decode(subscription.userData());;
:final int usedVersion=info.version();;
if (info.errorCode()==AssignorError.SHUTDOWN_REQUESTED.code()) then (yes) 
:shutdownRequested=true;;
endif 
:minReceivedMetadataVersion=updateMinReceivedVersion(usedVersion,
minReceivedMetadataVersion);;
:minSupportedMetadataVersion=updateMinSupportedVersion(info.latestSupportedVersion(),
minSupportedMetadataVersion);;
:final UUID processId;;
if (usedVersion>LATEST_SUPPORTED_VERSION) then (yes) 
:futureMetadataVersion=usedVersion;;
:processId=FUTURE_ID;;
if (!clientMetadataMap.containsKey(FUTURE_ID)) then (yes) 
:clientMetadataMap.put(FUTURE_ID,new ClientMetadata(null ,
Collections.emptyMap()));;
endif 
else (no)
:processId=info.processId();;
endif 
:ClientMetadata clientMetadata=clientMetadataMap.get(processId);;
if (clientMetadata==null ) then (yes) 
:clientMetadata=new ClientMetadata(info.userEndPoint(),
info.clientTags());;
:clientMetadataMap.put(info.processId(),clientMetadata);;
endif 
:clientMetadata.addConsumer(consumerId,subscription.ownedPartitions());;
:final int prevSize=allOwnedPartitions.size();;
:allOwnedPartitions.addAll(subscription.ownedPartitions());;
if (allOwnedPartitions.size()<prevSize+subscription.ownedPartitions().size()) then (yes) 
:assignmentErrorFound=true;;
endif 
:clientMetadata.addPreviousTasksAndOffsetSums(consumerId,
info.taskOffsetSums());;
if (assignmentErrorFound) then (yes) 
:log.warn("The previous assignment contains a partition more than once. "+
"\t Mapping: {}",subscriptions);;
endif 
switch (try/catch block)
case (try)
:final boolean versionProbing=checkMetadataVersions(minReceivedMetadataVersion,
minSupportedMetadataVersion,futureMetadataVersion);;
:log.debug("Constructed client metadata {} from the member subscriptions.",
clientMetadataMap);;
if (shutdownRequested) then (yes) 
:return new GroupAssignment(errorAssignment(clientMetadataMap,
AssignorError.SHUTDOWN_REQUESTED.code()));;
stop
endif 
:final RepartitionTopics repartitionTopics=prepareRepartitionTopics(metadata);;
:final Map<TopicPartition,PartitionInfo> allRepartitionTopicPartitions=repartitionTopics.topicPartitionsInfo();;
:final Cluster fullMetadata=metadata.withPartitions(allRepartitionTopicPartitions);;
:log.debug("Created repartition topics {} from the parsed topology.",
allRepartitionTopicPartitions.values());;
:final Map<Subtopology,TopicsInfo> topicGroups=taskManager.topologyMetadata()
.subtopologyTopicsInfoMapExcluding(repartitionTopics.topologiesWithMissingInputTopics());;
:final Set<String> allSourceTopics=new HashSet<>();;
:final Map<Subtopology,Set<String>> sourceTopicsByGroup=new HashMap<>();;
:allSourceTopics.addAll(entry.getValue().sourceTopics);;
:sourceTopicsByGroup.put(entry.getKey(),entry.getValue()
.sourceTopics);;
:final Map<TaskId,Set<TopicPartition>> partitionsForTask=partitionGrouper.partitionGroups(sourceTopicsByGroup,
fullMetadata);;
:final Set<TaskId> statefulTasks=new HashSet<>();;
:final boolean probingRebalanceNeeded=assignTasksToClients(fullMetadata,
allSourceTopics,topicGroups,clientMetadataMap,
partitionsForTask,statefulTasks);;
:final Map<HostInfo,Set<TopicPartition>> partitionsByHost=new HashMap<>();;
:final Map<HostInfo,Set<TopicPartition>> standbyPartitionsByHost=new HashMap<>();;
if (minReceivedMetadataVersion>=2) then (yes) 
:populatePartitionsByHostMaps(partitionsByHost,
standbyPartitionsByHost,partitionsForTask,
clientMetadataMap);;
endif 
:streamsMetadataState.onChange(partitionsByHost,
standbyPartitionsByHost,fullMetadata);;
:final Map<String,Assignment> assignment=computeNewAssignment(statefulTasks,
clientMetadataMap,partitionsForTask,partitionsByHost,
standbyPartitionsByHost,allOwnedPartitions,
minReceivedMetadataVersion,minSupportedMetadataVersion,
versionProbing,probingRebalanceNeeded);;
:return new GroupAssignment(assignment);;
stop
case (catch (final MissingSourceTopicException e))
:log.error("Caught an error in the task assignment. Returning an error assignment.",e);;
:return new GroupAssignment(errorAssignment(clientMetadataMap,
AssignorError.INCOMPLETE_SOURCE_TOPIC_METADATA.code()));;
stop
case (catch (final TaskAssignmentException e))
:log.error("Caught an error in the task assignment. Returning an error assignment.",e);;
:return new GroupAssignment(errorAssignment(clientMetadataMap,
AssignorError.ASSIGNMENT_ERROR.code()));;
stop
endswitch
end
start
note right
boolean checkMetadataVersions(final int minReceivedMetadataVersion,
final int minSupportedMetadataVersion,final int futureMetadataVersion)
end note
:final boolean versionProbing;;
if (futureMetadataVersion==UNKNOWN) then (yes) 
:versionProbing=false;;
else if (minReceivedMetadataVersion>=EARLIEST_PROBEABLE_VERSION) then (yes) 
:versionProbing=true;;
:log.info("Received a future (version probing) subscription (version: {})."+
" Sending assignment back (with supported version {}).",
futureMetadataVersion,minSupportedMetadataVersion);;
else (no)
:throw new TaskAssignmentException("Received a future (version probing) subscription (version: "+
futureMetadataVersion+") and an incompatible pre Kafka 2.0 subscription (version: "+
minReceivedMetadataVersion+") at the same time.");;
stop
endif 
if (minReceivedMetadataVersion<LATEST_SUPPORTED_VERSION) then (yes) 
:log.info("Downgrade metadata to version {}. Latest supported version is {}.",
minReceivedMetadataVersion,LATEST_SUPPORTED_VERSION);;
endif 
if (minSupportedMetadataVersion<LATEST_SUPPORTED_VERSION) then (yes) 
:log.info("Downgrade latest supported metadata to version {}. Latest supported version is {}.",
minSupportedMetadataVersion,LATEST_SUPPORTED_VERSION);;
endif 
:return versionProbing;;
stop
end
start
note right
RepartitionTopics prepareRepartitionTopics(final Cluster metadata)
end note
:final RepartitionTopics repartitionTopics=new RepartitionTopics(taskManager.topologyMetadata(),
internalTopicManager,copartitionedTopicsEnforcer,
metadata,logPrefix);;
:repartitionTopics.setup();;
:final boolean isMissingInputTopics=!repartitionTopics.missingSourceTopicExceptions()
.isEmpty();;
if (isMissingInputTopics) then (yes) 
if (!taskManager.topologyMetadata().hasNamedTopologies()) then (yes) 
:throw new MissingSourceTopicException("Missing source topics.");;
stop
else (no)
:nonFatalExceptionsToHandle.addAll(repartitionTopics.missingSourceTopicExceptions());;
endif 
endif 
:return repartitionTopics;;
stop
end
start
note right
void populateTasksForMaps(final Map<TopicPartition,
TaskId> taskForPartition,final Map<Subtopology,
Set<TaskId>> tasksForTopicGroup,final Set<String> allSourceTopics,
final Map<TaskId,Set<TopicPartition>> partitionsForTask,
final Cluster fullMetadata)
end note
:final Set<TopicPartition> allAssignedPartitions=new HashSet<>();;
:final TaskId id=entry.getKey();;
:final Set<TopicPartition> partitions=entry.getValue();;
:taskForPartition.put(partition,id);;
if (allAssignedPartitions.contains(partition)) then (yes) 
:log.warn("Partition {} is assigned to more than one tasks: {}",
partition,partitionsForTask);;
endif 
:allAssignedPartitions.addAll(partitions);;
:tasksForTopicGroup.computeIfAbsent(new Subtopology(id.subtopology(),
id.topologyName()),k->new HashSet<>()).add(id);;
:checkAllPartitions(allSourceTopics,partitionsForTask,
allAssignedPartitions,fullMetadata);;
end
start
note right
void checkAllPartitions(final Set<String> allSourceTopics,
final Map<TaskId,Set<TopicPartition>> partitionsForTask,
final Set<TopicPartition> allAssignedPartitions,
final Cluster fullMetadata)
end note
:final List<PartitionInfo> partitionInfoList=fullMetadata.partitionsForTopic(topic);;
if (partitionInfoList.isEmpty()) then (yes) 
:log.warn("No partitions found for topic {}",
topic);;
else (no)
:final TopicPartition partition=new TopicPartition(partitionInfo.topic(),
partitionInfo.partition());;
if (!allAssignedPartitions.contains(partition)) then (yes) 
:log.warn("Partition {} is not assigned to any tasks: {}"+
" Possible causes of a partition not getting assigned"+
" is that another topic defined in the topology has not been"+
" created when starting your streams application,"+
" resulting in no tasks created for this topology at all.",
partition,partitionsForTask);;
endif 
endif 
end
start
note right
boolean assignTasksToClients(final Cluster fullMetadata,
final Set<String> allSourceTopics,final Map<Subtopology,
TopicsInfo> topicGroups,final Map<UUID,ClientMetadata> clientMetadataMap,
final Map<TaskId,Set<TopicPartition>> partitionsForTask,
final Set<TaskId> statefulTasks)
end note
if (!statefulTasks.isEmpty()) then (yes) 
:throw new TaskAssignmentException("The stateful tasks should not be populated before assigning tasks to clients");;
stop
endif 
:final Map<TopicPartition,TaskId> taskForPartition=new HashMap<>();;
:final Map<Subtopology,Set<TaskId>> tasksForTopicGroup=new HashMap<>();;
:populateTasksForMaps(taskForPartition,tasksForTopicGroup,
allSourceTopics,partitionsForTask,fullMetadata);;
:final ChangelogTopics changelogTopics=new ChangelogTopics(internalTopicManager,
topicGroups,tasksForTopicGroup,logPrefix);;
:changelogTopics.setup();;
:final Map<UUID,ClientState> clientStates=new HashMap<>();;
:final boolean lagComputationSuccessful=populateClientStatesMap(clientStates,
clientMetadataMap,taskForPartition,changelogTopics);;
:log.info("All members participating in this rebalance: \n{}.",clientStates.entrySet().stream().map(entry->entry.getKey()+
": "+entry.getValue().consumers()).collect(Collectors.joining(Utils.NL)));;
:final Set<TaskId> allTasks=partitionsForTask.keySet();;
:statefulTasks.addAll(changelogTopics.statefulTaskIds());;
:log.debug("Assigning tasks {} including stateful {} to clients {} with number of replicas {}",
allTasks,statefulTasks,clientStates,numStandbyReplicas());;
:final TaskAssignor taskAssignor=createTaskAssignor(lagComputationSuccessful);;
:final boolean probingRebalanceNeeded=taskAssignor.assign(clientStates,
allTasks,statefulTasks,assignmentConfigs);;
:log.info("Assigned tasks {} including stateful {} to clients as: \n{}.",allTasks,statefulTasks,clientStates.entrySet().stream().map(entry->entry.getKey()+
"="+entry.getValue().currentAssignment()).collect(Collectors.joining(Utils.NL)));;
:return probingRebalanceNeeded;;
stop
end
start
note right
TaskAssignor createTaskAssignor(final boolean lagComputationSuccessful)
end note
:final TaskAssignor taskAssignor=taskAssignorSupplier.get();;
if (taskAssignor instanceof StickyTaskAssignor ) then (yes) 
:return taskAssignor;;
stop
else if (lagComputationSuccessful) then (yes) 
:return taskAssignor;;
stop
else (no)
:log.info("Failed to fetch end offsets for changelogs, will return previous assignment to clients and "+
"trigger another rebalance to retry.");;
:return new FallbackPriorTaskAssignor();;
stop
endif 
end
start
note right
boolean populateClientStatesMap(final Map<UUID,
ClientState> clientStates,final Map<UUID,
ClientMetadata> clientMetadataMap,final Map<TopicPartition,
TaskId> taskForPartition,final ChangelogTopics changelogTopics)
end note
:boolean fetchEndOffsetsSuccessful;;
:Map<TaskId,Long> allTaskEndOffsetSums;;
switch (try/catch block)
case (try)
:final KafkaFuture<Map<TopicPartition,ListOffsetsResultInfo>> endOffsetsFuture=fetchEndOffsetsFuture(changelogTopics.preExistingNonSourceTopicBasedPartitions(),
adminClient);;
:final Map<TopicPartition,Long> sourceChangelogEndOffsets=fetchCommittedOffsets(changelogTopics.preExistingSourceTopicBasedPartitions(),
mainConsumerSupplier.get());;
:final Map<TopicPartition,ListOffsetsResultInfo> endOffsets=ClientUtils.getEndOffsets(endOffsetsFuture);;
:allTaskEndOffsetSums=computeEndOffsetSumsByTask(endOffsets,
sourceChangelogEndOffsets,changelogTopics);;
:fetchEndOffsetsSuccessful=true;;
case (catch (final StreamsException |TimeoutException e))
:allTaskEndOffsetSums=changelogTopics.statefulTaskIds().stream().collect(Collectors.toMap(t->t,
t->UNKNOWN_OFFSET_SUM));;
:fetchEndOffsetsSuccessful=false;;
endswitch
:final UUID uuid=entry.getKey();;
:final ClientState state=entry.getValue()
.state;;
:state.initializePrevTasks(taskForPartition,
taskManager.topologyMetadata().hasNamedTopologies());;
:state.computeTaskLags(uuid,allTaskEndOffsetSums);;
:clientStates.put(uuid,state);;
:return fetchEndOffsetsSuccessful;;
stop
end
start
note right
Map<TaskId,Long> computeEndOffsetSumsByTask(final Map<TopicPartition,
ListOffsetsResultInfo> endOffsets,final Map<TopicPartition,
Long> sourceChangelogEndOffsets,final ChangelogTopics changelogTopics)
end note
:final Map<TaskId,Long> taskEndOffsetSums=new HashMap<>();;
:taskEndOffsetSums.put(taskId,0L);;
:final long changelogPartitionEndOffset;;
if (sourceChangelogEndOffsets.containsKey(changelogPartition)) then (yes) 
:changelogPartitionEndOffset=sourceChangelogEndOffsets.get(changelogPartition);;
else if (endOffsets.containsKey(changelogPartition)) then (yes) 
:changelogPartitionEndOffset=endOffsets.get(changelogPartition)
.offset();;
else (no)
:log.debug("Fetched offsets did not contain the changelog {} of task {}",
changelogPartition,taskId);;
:throw new IllegalStateException("Could not get end offset for "+
changelogPartition);;
stop
endif 
:final long newEndOffsetSum=taskEndOffsetSums.get(taskId)+
changelogPartitionEndOffset;;
if (newEndOffsetSum<0) then (yes) 
:taskEndOffsetSums.put(taskId,Long.MAX_VALUE);;
:break ;;
else (no)
:taskEndOffsetSums.put(taskId,newEndOffsetSum);;
endif 
:return taskEndOffsetSums;;
stop
end
start
note right
void populatePartitionsByHostMaps(final Map<HostInfo,
Set<TopicPartition>> partitionsByHost,final Map<HostInfo,
Set<TopicPartition>> standbyPartitionsByHost,
final Map<TaskId,Set<TopicPartition>> partitionsForTask,
final Map<UUID,ClientMetadata> clientMetadataMap)
end note
:final HostInfo hostInfo=entry.getValue()
.hostInfo;;
if (hostInfo!=null ) then (yes) 
:final Set<TopicPartition> topicPartitions=new HashSet<>();;
:final Set<TopicPartition> standbyPartitions=new HashSet<>();;
:final ClientState state=entry.getValue()
.state;;
:topicPartitions.addAll(partitionsForTask.get(id));;
:standbyPartitions.addAll(partitionsForTask.get(id));;
:partitionsByHost.put(hostInfo,topicPartitions);;
:standbyPartitionsByHost.put(hostInfo,standbyPartitions);;
endif 
end
start
note right
Map<String,Assignment> computeNewAssignment(final Set<TaskId> statefulTasks,
final Map<UUID,ClientMetadata> clientsMetadata,
final Map<TaskId,Set<TopicPartition>> partitionsForTask,
final Map<HostInfo,Set<TopicPartition>> partitionsByHostState,
final Map<HostInfo,Set<TopicPartition>> standbyPartitionsByHost,
final Set<TopicPartition> allOwnedPartitions,
final int minUserMetadataVersion,final int minSupportedMetadataVersion,
final boolean versionProbing,final boolean shouldTriggerProbingRebalance)
end note
:boolean rebalanceRequired=shouldTriggerProbingRebalance||versionProbing;;
:final Map<String,Assignment> assignment=new HashMap<>();;
:final UUID clientId=clientEntry.getKey();;
:final ClientMetadata clientMetadata=clientEntry.getValue();;
:final ClientState state=clientMetadata.state;;
:final SortedSet<String> consumers=clientMetadata.consumers;;
:final Map<String,Integer> threadTaskCounts=new HashMap<>();;
:final Map<String,List<TaskId>> activeTaskStatefulAssignment=assignTasksToThreads(state.statefulActiveTasks(),
true,consumers,state,threadTaskCounts);;
:final Map<String,List<TaskId>> standbyTaskAssignment=assignTasksToThreads(state.standbyTasks(),
true,consumers,state,threadTaskCounts);;
:final Map<String,List<TaskId>> activeTaskStatelessAssignment=assignTasksToThreads(state.statelessActiveTasks(),
false,consumers,state,threadTaskCounts);;
:final Map<String,List<TaskId>> activeTaskAssignment=activeTaskStatefulAssignment;;
:activeTaskAssignment.get(threadEntry.getKey())
.addAll(threadEntry.getValue());;
:final boolean encodeNextProbingRebalanceTime=shouldTriggerProbingRebalance&&clientId.equals(taskManager.processId());;
:final boolean tasksRevoked=addClientAssignments(statefulTasks,
assignment,clientMetadata,partitionsForTask,
partitionsByHostState,standbyPartitionsByHost,
allOwnedPartitions,activeTaskAssignment,standbyTaskAssignment,
minUserMetadataVersion,minSupportedMetadataVersion,
encodeNextProbingRebalanceTime);;
if (tasksRevoked||encodeNextProbingRebalanceTime) then (yes) 
:rebalanceRequired=true;;
:log.debug("Requested client {} to schedule a followup rebalance",
clientId);;
endif 
:log.info("Client {} per-consumer assignment:\n"+
"\tprev owned active {}\n"+"\tprev owned standby {}\n"+
"\tassigned active {}\n"+"\trevoking active {}\n"+
"\tassigned standby {}\n",clientId,clientMetadata.state.prevOwnedActiveTasksByConsumer(),
clientMetadata.state.prevOwnedStandbyByConsumer(),
clientMetadata.state.assignedActiveTasksByConsumer(),
clientMetadata.state.revokingActiveTasksByConsumer(),
clientMetadata.state.assignedStandbyTasksByConsumer());;
if (rebalanceRequired) then (yes) 
:assignmentListener.onAssignmentComplete(false);;
:log.info("Finished unstable assignment of tasks,
 a followup rebalance will be scheduled.");;
else (no)
:assignmentListener.onAssignmentComplete(true);;
:log.info("Finished stable assignment of tasks,
 no followup rebalances required.");;
endif 
:return assignment;;
stop
end
start
note right
boolean addClientAssignments(final Set<TaskId> statefulTasks,
final Map<String,Assignment> assignment,final ClientMetadata clientMetadata,
final Map<TaskId,Set<TopicPartition>> partitionsForTask,
final Map<HostInfo,Set<TopicPartition>> partitionsByHostState,
final Map<HostInfo,Set<TopicPartition>> standbyPartitionsByHost,
final Set<TopicPartition> allOwnedPartitions,
final Map<String,List<TaskId>> activeTaskAssignments,
final Map<String,List<TaskId>> standbyTaskAssignments,
final int minUserMetadataVersion,final int minSupportedMetadataVersion,
final boolean probingRebalanceNeeded)
end note
:boolean followupRebalanceRequiredForRevokedTasks=false;;
:boolean shouldEncodeProbingRebalance=probingRebalanceNeeded;;
:final List<TaskId> activeTasksForConsumer=activeTaskAssignments.get(consumer);;
:final List<TopicPartition> activePartitionsList=new ArrayList<>();;
:final List<TaskId> assignedActiveList=new ArrayList<>();;
:final Set<TaskId> activeTasksRemovedPendingRevokation=populateActiveTaskAndPartitionsLists(activePartitionsList,
assignedActiveList,consumer,clientMetadata.state,
activeTasksForConsumer,partitionsForTask,
allOwnedPartitions);;
:final Map<TaskId,Set<TopicPartition>> standbyTaskMap=buildStandbyTaskMap(consumer,
standbyTaskAssignments.get(consumer),activeTasksRemovedPendingRevokation,
statefulTasks,partitionsForTask,clientMetadata.state);;
:final AssignmentInfo info=new AssignmentInfo(minUserMetadataVersion,
minSupportedMetadataVersion,assignedActiveList,
standbyTaskMap,partitionsByHostState,standbyPartitionsByHost,
AssignorError.NONE.code());;
if (!activeTasksRemovedPendingRevokation.isEmpty()) then (yes) 
:log.info("Requesting followup rebalance be scheduled immediately by {} due to tasks changing ownership.",
consumer);;
:info.setNextRebalanceTime(0L);;
:followupRebalanceRequiredForRevokedTasks=true;;
:shouldEncodeProbingRebalance=false;;
else if (shouldEncodeProbingRebalance) then (yes) 
:final long nextRebalanceTimeMs=time.milliseconds()+
probingRebalanceIntervalMs();;
:log.info("Requesting followup rebalance be scheduled by {} for {} ms to probe for caught-up replica tasks.",
consumer,nextRebalanceTimeMs);;
:info.setNextRebalanceTime(nextRebalanceTimeMs);;
:shouldEncodeProbingRebalance=false;;
endif 
:assignment.put(consumer,new Assignment(activePartitionsList,
info.encode()));;
:return followupRebalanceRequiredForRevokedTasks;;
stop
end
start
note right
Set<TaskId> populateActiveTaskAndPartitionsLists(final List<TopicPartition> activePartitionsList,
final List<TaskId> assignedActiveList,final String consumer,
final ClientState clientState,final List<TaskId> activeTasksForConsumer,
final Map<TaskId,Set<TopicPartition>> partitionsForTask,
final Set<TopicPartition> allOwnedPartitions)
end note
:final List<AssignedPartition> assignedPartitions=new ArrayList<>();;
:final Set<TaskId> removedActiveTasks=new TreeSet<>();;
:clientState.assignActiveToConsumer(taskId,
consumer);;
:final List<AssignedPartition> assignedPartitionsForTask=new ArrayList<>();;
:final String oldOwner=clientState.previousOwnerForPartition(partition);;
:final boolean newPartitionForConsumer=oldOwner==null ||!oldOwner.equals(consumer);;
if (newPartitionForConsumer&&allOwnedPartitions.contains(partition)) then (yes) 
:log.info("Removing task {} from {} active assignment until it is safely revoked in followup rebalance",
taskId,consumer);;
:removedActiveTasks.add(taskId);;
:clientState.revokeActiveFromConsumer(taskId,
consumer);;
:assignedPartitionsForTask.clear();;
:clientState.unassignActive(taskId);;
:break ;;
else (no)
:assignedPartitionsForTask.add(new AssignedPartition(taskId,
partition));;
endif 
:assignedPartitions.addAll(assignedPartitionsForTask);;
:Collections.sort(assignedPartitions);;
:assignedActiveList.add(partition.taskId);;
:activePartitionsList.add(partition.partition);;
:return removedActiveTasks;;
stop
end
start
note right
Map<TaskId,Set<TopicPartition>> buildStandbyTaskMap(final String consumer,
final Iterable<TaskId> standbyTasks,final Iterable<TaskId> revokedTasks,
final Set<TaskId> allStatefulTasks,final Map<TaskId,
Set<TopicPartition>> partitionsForTask,final ClientState clientState)
end note
:final Map<TaskId,Set<TopicPartition>> standbyTaskMap=new HashMap<>();;
:clientState.assignStandbyToConsumer(task,
consumer);;
:standbyTaskMap.put(task,partitionsForTask.get(task));;
if (allStatefulTasks.contains(task)) then (yes) 
:log.info("Adding removed stateful active task {} as a standby for {} before it is revoked in followup rebalance",
task,consumer);;
:clientState.assignStandbyToConsumer(task,
consumer);;
:clientState.assignStandby(task);;
:standbyTaskMap.put(task,partitionsForTask.get(task));;
endif 
:return standbyTaskMap;;
stop
end
start
note right
Map<String,List<TaskId>> assignTasksToThreads(final Collection<TaskId> tasksToAssign,
final boolean isStateful,final SortedSet<String> consumers,
final ClientState state,final Map<String,
Integer> threadLoad)
end note
:final Map<String,List<TaskId>> assignment=new HashMap<>();;
:assignment.put(consumer,new ArrayList<>());;
:final int totalTasks=threadLoad.values().stream().reduce(tasksToAssign.size(),
Integer::sum);;
:final int minTasksPerThread=(int )Math.floor(((double )totalTasks)/consumers.size());;
:final PriorityQueue<TaskId> unassignedTasks=new PriorityQueue<>(tasksToAssign);;
:final Queue<String> consumersToFill=new LinkedList<>();;
:final Map<TaskId,String> unassignedTaskToPreviousOwner=new TreeMap<>();;
if (!unassignedTasks.isEmpty()) then (yes) 
:final List<TaskId> threadAssignment=assignment.get(consumer);;
:final int tasksTargetCount=minTasksPerThread-threadLoad.getOrDefault(consumer,0);;
if (isStateful) then (yes) 
if (unassignedTasks.contains(task)) then (yes) 
if (threadAssignment.size()<tasksTargetCount) then (yes) 
:threadAssignment.add(task);;
:unassignedTasks.remove(task);;
else (no)
:unassignedTaskToPreviousOwner.put(task,consumer);;
endif 
endif 
endif 
if (threadAssignment.size()<tasksTargetCount) then (yes) 
:consumersToFill.offer(consumer);;
endif 
while (!consumersToFill.isEmpty())
:final TaskId task=unassignedTasks.poll();;
if (task!=null ) then (yes) 
:final String consumer=consumersToFill.poll();;
:final List<TaskId> threadAssignment=assignment.get(consumer);;
:threadAssignment.add(task);;
:final int threadTaskCount=threadAssignment.size()+
threadLoad.getOrDefault(consumer,0);;
if (threadTaskCount<minTasksPerThread) then (yes) 
:consumersToFill.offer(consumer);;
endif 
else (no)
:throw new TaskAssignmentException("Ran out of unassigned stateful tasks but some members were not at capacity");;
stop
endif 
endwhile
if (!unassignedTasks.isEmpty()) then (yes) 
:final int taskCount=assignment.get(consumer).size()+
threadLoad.getOrDefault(consumer,0);;
if (taskCount==minTasksPerThread) then (yes) 
:consumersToFill.add(consumer);;
endif 
:final TaskId task=taskEntry.getKey();;
:final String consumer=taskEntry.getValue();;
if (consumersToFill.contains(consumer)&&unassignedTasks.contains(task)) then (yes) 
:assignment.get(consumer).add(task);;
:unassignedTasks.remove(task);;
:consumersToFill.remove(consumer);;
endif 
:final String consumer=consumersToFill.poll();;
:final List<TaskId> threadAssignment=assignment.get(consumer);;
:threadAssignment.add(task);;
endif 
endif 
:final String consumer=taskEntry.getKey();;
:final int totalCount=threadLoad.getOrDefault(consumer,0)+
taskEntry.getValue().size();;
:threadLoad.put(consumer,totalCount);;
:return assignment;;
stop
end
start
note right
void validateMetadataVersions(final int receivedAssignmentMetadataVersion,
final int latestCommonlySupportedVersion)
end note
if (receivedAssignmentMetadataVersion>usedSubscriptionMetadataVersion) then (yes) 
:log.error("Leader sent back an assignment with version {} which was greater than our used version {}",
receivedAssignmentMetadataVersion,usedSubscriptionMetadataVersion);;
:throw new TaskAssignmentException("Sent a version "+
usedSubscriptionMetadataVersion+" subscription but got an assignment with higher version "+
receivedAssignmentMetadataVersion+".");;
stop
endif 
if (latestCommonlySupportedVersion>LATEST_SUPPORTED_VERSION) then (yes) 
:log.error("Leader sent back assignment with commonly supported version {} that is greater than our "+
"actual latest supported version {}",latestCommonlySupportedVersion,
LATEST_SUPPORTED_VERSION);;
:throw new TaskAssignmentException("Can't upgrade to metadata version greater than we support");;
stop
endif 
end
start
note right
boolean maybeUpdateSubscriptionVersion(final int receivedAssignmentMetadataVersion,
final int latestCommonlySupportedVersion)
end note
if (receivedAssignmentMetadataVersion>=EARLIEST_PROBEABLE_VERSION) then (yes) 
if (latestCommonlySupportedVersion>usedSubscriptionMetadataVersion) then (yes) 
:log.info("Sent a version {} subscription and group's latest commonly supported version is {} (successful "+
"version probing and end of rolling upgrade). Upgrading subscription metadata version to "+
"{} for next rebalance.",usedSubscriptionMetadataVersion,
latestCommonlySupportedVersion,latestCommonlySupportedVersion);;
:usedSubscriptionMetadataVersion=latestCommonlySupportedVersion;;
:return true;;
stop
endif 
if (receivedAssignmentMetadataVersion<usedSubscriptionMetadataVersion) then (yes) 
:log.info("Sent a version {} subscription and got version {} assignment back (successful version probing). "+
"Downgrade subscription metadata to commonly supported version {} and trigger new rebalance.",
usedSubscriptionMetadataVersion,receivedAssignmentMetadataVersion,
latestCommonlySupportedVersion);;
:usedSubscriptionMetadataVersion=latestCommonlySupportedVersion;;
:return true;;
stop
endif 
else (no)
:log.debug("Received an assignment version {} that is less than the earliest version that allows version "+
"probing {}. If this is not during a rolling upgrade from version 2.0 or below,
 this is an error.",receivedAssignmentMetadataVersion,
EARLIEST_PROBEABLE_VERSION);;
endif 
:return false;;
stop
end
start
note right
void onAssignment(final Assignment assignment,
final ConsumerGroupMetadata metadata)
end note
:final List<TopicPartition> partitions=new ArrayList<>(assignment.partitions());;
:partitions.sort(PARTITION_COMPARATOR);;
:final AssignmentInfo info=AssignmentInfo.decode(assignment.userData());;
if (info.errCode()!=AssignorError.NONE.code()) then (yes) 
:assignmentErrorCode.set(info.errCode());;
:return ;;
stop
endif 
:final int receivedAssignmentMetadataVersion=info.version();;
:final int latestCommonlySupportedVersion=info.commonlySupportedVersion();;
:validateMetadataVersions(receivedAssignmentMetadataVersion,
latestCommonlySupportedVersion);;
:final Map<TaskId,Set<TopicPartition>> activeTasks;;
:final Map<TopicPartition,PartitionInfo> topicToPartitionInfo;;
:final Map<HostInfo,Set<TopicPartition>> partitionsByHost;;
:final Map<HostInfo,Set<TopicPartition>> standbyPartitionsByHost;;
:final long encodedNextScheduledRebalanceMs;;
switch (switch receivedAssignmentMetadataVersion)
endswitch
:maybeScheduleFollowupRebalance(encodedNextScheduledRebalanceMs,
receivedAssignmentMetadataVersion,latestCommonlySupportedVersion,
partitionsByHost.keySet());;
:final Cluster fakeCluster=Cluster.empty()
.withPartitions(topicToPartitionInfo);;
:streamsMetadataState.onChange(partitionsByHost,
standbyPartitionsByHost,fakeCluster);;
:taskManager.handleAssignment(activeTasks,
info.standbyTasks());;
end
start
note right
void maybeScheduleFollowupRebalance(final long encodedNextScheduledRebalanceMs,
final int receivedAssignmentMetadataVersion,
final int latestCommonlySupportedVersion,
final Set<HostInfo> groupHostInfo)
end note
if (maybeUpdateSubscriptionVersion(receivedAssignmentMetadataVersion,latestCommonlySupportedVersion)) then (yes) 
:log.info("Requested to schedule immediate rebalance due to version probing.");;
:nextScheduledRebalanceMs.set(0L);;
else if (!verifyHostInfo(groupHostInfo)) then (yes) 
:log.info("Requested to schedule immediate rebalance to update group with new host endpoint = {}.",
userEndPoint);;
:nextScheduledRebalanceMs.set(0L);;
else if (encodedNextScheduledRebalanceMs==0L) then (yes) 
:log.info("Requested to schedule immediate rebalance for new tasks to be safely revoked from current owner.");;
:nextScheduledRebalanceMs.set(0L);;
else if (encodedNextScheduledRebalanceMs<Long.MAX_VALUE) then (yes) 
:log.info("Requested to schedule probing rebalance for {} ms.",
encodedNextScheduledRebalanceMs);;
:nextScheduledRebalanceMs.set(encodedNextScheduledRebalanceMs);;
else (no)
:log.info("No followup rebalance was requested,
 resetting the rebalance schedule.");;
:nextScheduledRebalanceMs.set(Long.MAX_VALUE);;
endif 
end
start
note right
boolean verifyHostInfo(final Set<HostInfo> groupHostInfo)
end note
if (userEndPoint!=null &&!groupHostInfo.isEmpty()) then (yes) 
:final HostInfo myHostInfo=HostInfo.buildFromEndpoint(userEndPoint);;
:return groupHostInfo.contains(myHostInfo);;
stop
else (no)
:return true;;
stop
endif 
end
start
note right
Map<TaskId,Set<TopicPartition>> getActiveTasks(final List<TopicPartition> partitions,
final AssignmentInfo info)
end note
:final Map<TaskId,Set<TopicPartition>> activeTasks=new HashMap<>();;
:final TopicPartition partition=partitions.get(i);;
:final TaskId id=info.activeTasks().get(i);;
:activeTasks.computeIfAbsent(id,k1->new HashSet<>())
.add(partition);;
:return activeTasks;;
stop
end
start
note right
Map<TopicPartition,PartitionInfo> getTopicPartitionInfo(final Map<HostInfo,
Set<TopicPartition>> partitionsByHost)
end note
:final Map<TopicPartition,PartitionInfo> topicToPartitionInfo=new HashMap<>();;
:topicToPartitionInfo.put(topicPartition,
new PartitionInfo(topicPartition.topic(),
topicPartition.partition(),null ,new Node[0],
new Node[0]));;
:return topicToPartitionInfo;;
stop
end
start
note right
void validateActiveTaskEncoding(final List<TopicPartition> partitions,
final AssignmentInfo info,final String logPrefix)
end note
if (partitions.size()!=info.activeTasks().size()) then (yes) 
:throw new TaskAssignmentException(String.format("%sNumber of assigned partitions %d is not equal to "+
"the number of active taskIds %d, assignmentInfo=%s",
logPrefix,partitions.size(),info.activeTasks().size(),
info.toString()));;
stop
endif 
end
start
note right
int updateMinReceivedVersion(final int usedVersion,
final int minReceivedMetadataVersion)
end note
:return Math.min(usedVersion,minReceivedMetadataVersion);;
stop
end
start
note right
int updateMinSupportedVersion(final int supportedVersion,
final int minSupportedMetadataVersion)
end note
if (supportedVersion<minSupportedMetadataVersion) then (yes) 
:log.debug("Downgrade the current minimum supported version {} to the smaller seen supported version {}",
minSupportedMetadataVersion,supportedVersion);;
:return supportedVersion;;
stop
else (no)
:log.debug("Current minimum supported version remains at {},
 last seen supported version was {}",minSupportedMetadataVersion,
supportedVersion);;
:return minSupportedMetadataVersion;;
stop
endif 
end
start
note right
void setInternalTopicManager(final InternalTopicManager internalTopicManager)
end note
:this.internalTopicManager=internalTopicManager;;
end
start
note right
RebalanceProtocol rebalanceProtocol()
end note
:return rebalanceProtocol;;
stop
end
start
note right
String userEndPoint()
end note
:return userEndPoint;;
stop
end
start
note right
TaskManager taskManager()
end note
:return taskManager;;
stop
end
start
note right
byte uniqueField()
end note
:return uniqueField;;
stop
end
start
note right
Map<String,String> clientTags()
end note
:return clientTags;;
stop
end
start
note right
void handleRebalanceStart(final Set<String> topics)
end note
:taskManager.handleRebalanceStart(topics);;
end
start
note right
long acceptableRecoveryLag()
end note
:return assignmentConfigs.acceptableRecoveryLag;;
stop
end
start
note right
int maxWarmupReplicas()
end note
:return assignmentConfigs.maxWarmupReplicas;;
stop
end
start
note right
int numStandbyReplicas()
end note
:return assignmentConfigs.numStandbyReplicas;;
stop
end
start
note right
long probingRebalanceIntervalMs()
end note
:return assignmentConfigs.probingRebalanceIntervalMs;;
stop
end
@enduml

@startuml
!pragma layout smetana
start
note right
constructor AssignedPartition(final TaskId taskId,
final TopicPartition partition)
end note
:this.taskId=taskId;;
:this.partition=partition;;
end
start
note right
int compareTo(final AssignedPartition that)
end note
:return PARTITION_COMPARATOR.compare(partition,
that.partition);;
stop
end
start
note right
boolean equals(final Object o)
end note
if (!(o instanceof AssignedPartition )) then (yes) 
:return false;;
stop
endif 
:final AssignedPartition other=(AssignedPartition )o;;
:return compareTo(other)==0;;
stop
end
start
note right
int hashCode()
end note
:return partition.hashCode();;
stop
end
start
note right
constructor ClientMetadata(final String endPoint,
final Map<String,String> clientTags)
end note
:hostInfo=HostInfo.buildFromEndpoint(endPoint);;
:consumers=new TreeSet<>();;
:state=new ClientState(clientTags);;
end
start
note right
void addConsumer(final String consumerMemberId,
final List<TopicPartition> ownedPartitions)
end note
:consumers.add(consumerMemberId);;
:state.incrementCapacity();;
:state.addOwnedPartitions(ownedPartitions,
consumerMemberId);;
end
start
note right
void addPreviousTasksAndOffsetSums(final String consumerId,
final Map<TaskId,Long> taskOffsetSums)
end note
:state.addPreviousTasksAndOffsetSums(consumerId,
taskOffsetSums);;
end
start
note right
String toString()
end note
:return "ClientMetadata{"+"hostInfo="+hostInfo+
", consumers="+consumers+", state="+state+
stop
end
start
note right
void configure(final Map<String,?> configs)
end note
:final AssignorConfiguration assignorConfiguration=new AssignorConfiguration(configs);;
:logPrefix=assignorConfiguration.logPrefix();;
:log=new LogContext(logPrefix).logger(getClass());;
:usedSubscriptionMetadataVersion=assignorConfiguration.configuredMetadataVersion(usedSubscriptionMetadataVersion);;
:final ReferenceContainer referenceContainer=assignorConfiguration.referenceContainer();;
:mainConsumerSupplier=()->Objects.requireNonNull(referenceContainer.mainConsumer,
"Main consumer was not specified");;
:adminClient=Objects.requireNonNull(referenceContainer.adminClient,
"Admin client was not specified");;
:taskManager=Objects.requireNonNull(referenceContainer.taskManager,
"TaskManager was not specified");;
:streamsMetadataState=Objects.requireNonNull(referenceContainer.streamsMetadataState,
"StreamsMetadataState was not specified");;
:assignmentErrorCode=referenceContainer.assignmentErrorCode;;
:nextScheduledRebalanceMs=referenceContainer.nextScheduledRebalanceMs;;
:nonFatalExceptionsToHandle=referenceContainer.nonFatalExceptionsToHandle;;
:time=Objects.requireNonNull(referenceContainer.time,
"Time was not specified");;
:assignmentConfigs=assignorConfiguration.assignmentConfigs();;
:partitionGrouper=new PartitionGrouper();;
:userEndPoint=assignorConfiguration.userEndPoint();;
:internalTopicManager=assignorConfiguration.internalTopicManager();;
:copartitionedTopicsEnforcer=assignorConfiguration.copartitionedTopicsEnforcer();;
:rebalanceProtocol=assignorConfiguration.rebalanceProtocol();;
:taskAssignorSupplier=assignorConfiguration::taskAssignor;;
:assignmentListener=assignorConfiguration.assignmentListener();;
:uniqueField=0;;
:clientTags=referenceContainer.clientTags;;
end
start
note right
String name()
end note
:return "stream";;
stop
end
start
note right
List<RebalanceProtocol> supportedProtocols()
end note
:final List<RebalanceProtocol> supportedProtocols=new ArrayList<>();;
:supportedProtocols.add(RebalanceProtocol.EAGER);;
if (rebalanceProtocol==RebalanceProtocol.COOPERATIVE) then (yes) 
:supportedProtocols.add(rebalanceProtocol);;
endif 
:return supportedProtocols;;
stop
end
start
note right
ByteBuffer subscriptionUserData(final Set<String> topics)
end note
:handleRebalanceStart(topics);;
:uniqueField++;;
:final Set<String> currentNamedTopologies=taskManager.topologyMetadata()
.namedTopologiesView();;
:final Map<TaskId,Long> taskOffsetSums=taskManager.topologyMetadata().hasNamedTopologies()? filterMap(taskManager.getTaskOffsetSums(),
t->currentNamedTopologies.contains(t.getKey().topologyName())):taskManager.getTaskOffsetSums();;
:return new SubscriptionInfo(usedSubscriptionMetadataVersion,
LATEST_SUPPORTED_VERSION,taskManager.processId(),
userEndPoint,taskOffsetSums,uniqueField,assignmentErrorCode.get(),
clientTags).encode();;
stop
end
start
note right
Map<String,Assignment> errorAssignment(final Map<UUID,
ClientMetadata> clientsMetadata,final int errorCode)
end note
:final Map<String,Assignment> assignment=new HashMap<>();;
:assignment.put(consumerId,new Assignment(Collections.emptyList(),
new AssignmentInfo(LATEST_SUPPORTED_VERSION,
Collections.emptyList(),Collections.emptyMap(),
Collections.emptyMap(),Collections.emptyMap(),
errorCode).encode()));;
:return assignment;;
stop
end
start
note right
GroupAssignment assign(final Cluster metadata,
final GroupSubscription groupSubscription)
end note
:final Map<String,Subscription> subscriptions=groupSubscription.groupSubscription();;
:final Map<UUID,ClientMetadata> clientMetadataMap=new HashMap<>();;
:final Set<TopicPartition> allOwnedPartitions=new HashSet<>();;
:int minReceivedMetadataVersion=LATEST_SUPPORTED_VERSION;;
:int minSupportedMetadataVersion=LATEST_SUPPORTED_VERSION;;
:boolean shutdownRequested=false;;
:boolean assignmentErrorFound=false;;
:int futureMetadataVersion=UNKNOWN;;
:final String consumerId=entry.getKey();;
:final Subscription subscription=entry.getValue();;
:final SubscriptionInfo info=SubscriptionInfo.decode(subscription.userData());;
:final int usedVersion=info.version();;
if (info.errorCode()==AssignorError.SHUTDOWN_REQUESTED.code()) then (yes) 
:shutdownRequested=true;;
endif 
:minReceivedMetadataVersion=updateMinReceivedVersion(usedVersion,
minReceivedMetadataVersion);;
:minSupportedMetadataVersion=updateMinSupportedVersion(info.latestSupportedVersion(),
minSupportedMetadataVersion);;
:final UUID processId;;
if (usedVersion>LATEST_SUPPORTED_VERSION) then (yes) 
:futureMetadataVersion=usedVersion;;
:processId=FUTURE_ID;;
if (!clientMetadataMap.containsKey(FUTURE_ID)) then (yes) 
:clientMetadataMap.put(FUTURE_ID,new ClientMetadata(null ,
Collections.emptyMap()));;
endif 
else (no)
:processId=info.processId();;
endif 
:ClientMetadata clientMetadata=clientMetadataMap.get(processId);;
if (clientMetadata==null ) then (yes) 
:clientMetadata=new ClientMetadata(info.userEndPoint(),
info.clientTags());;
:clientMetadataMap.put(info.processId(),clientMetadata);;
endif 
:clientMetadata.addConsumer(consumerId,subscription.ownedPartitions());;
:final int prevSize=allOwnedPartitions.size();;
:allOwnedPartitions.addAll(subscription.ownedPartitions());;
if (allOwnedPartitions.size()<prevSize+subscription.ownedPartitions().size()) then (yes) 
:assignmentErrorFound=true;;
endif 
:clientMetadata.addPreviousTasksAndOffsetSums(consumerId,
info.taskOffsetSums());;
if (assignmentErrorFound) then (yes) 
:log.warn("The previous assignment contains a partition more than once. "+
"\t Mapping: {}",subscriptions);;
endif 
switch (try/catch block)
case (try)
:final boolean versionProbing=checkMetadataVersions(minReceivedMetadataVersion,
minSupportedMetadataVersion,futureMetadataVersion);;
:log.debug("Constructed client metadata {} from the member subscriptions.",
clientMetadataMap);;
if (shutdownRequested) then (yes) 
:return new GroupAssignment(errorAssignment(clientMetadataMap,
AssignorError.SHUTDOWN_REQUESTED.code()));;
stop
endif 
:final RepartitionTopics repartitionTopics=prepareRepartitionTopics(metadata);;
:final Map<TopicPartition,PartitionInfo> allRepartitionTopicPartitions=repartitionTopics.topicPartitionsInfo();;
:final Cluster fullMetadata=metadata.withPartitions(allRepartitionTopicPartitions);;
:log.debug("Created repartition topics {} from the parsed topology.",
allRepartitionTopicPartitions.values());;
:final Map<Subtopology,TopicsInfo> topicGroups=taskManager.topologyMetadata()
.subtopologyTopicsInfoMapExcluding(repartitionTopics.topologiesWithMissingInputTopics());;
:final Set<String> allSourceTopics=new HashSet<>();;
:final Map<Subtopology,Set<String>> sourceTopicsByGroup=new HashMap<>();;
:allSourceTopics.addAll(entry.getValue().sourceTopics);;
:sourceTopicsByGroup.put(entry.getKey(),entry.getValue()
.sourceTopics);;
:final Map<TaskId,Set<TopicPartition>> partitionsForTask=partitionGrouper.partitionGroups(sourceTopicsByGroup,
fullMetadata);;
:final Set<TaskId> statefulTasks=new HashSet<>();;
:final boolean probingRebalanceNeeded=assignTasksToClients(fullMetadata,
allSourceTopics,topicGroups,clientMetadataMap,
partitionsForTask,statefulTasks);;
:final Map<HostInfo,Set<TopicPartition>> partitionsByHost=new HashMap<>();;
:final Map<HostInfo,Set<TopicPartition>> standbyPartitionsByHost=new HashMap<>();;
if (minReceivedMetadataVersion>=2) then (yes) 
:populatePartitionsByHostMaps(partitionsByHost,
standbyPartitionsByHost,partitionsForTask,
clientMetadataMap);;
endif 
:streamsMetadataState.onChange(partitionsByHost,
standbyPartitionsByHost,fullMetadata);;
:final Map<String,Assignment> assignment=computeNewAssignment(statefulTasks,
clientMetadataMap,partitionsForTask,partitionsByHost,
standbyPartitionsByHost,allOwnedPartitions,
minReceivedMetadataVersion,minSupportedMetadataVersion,
versionProbing,probingRebalanceNeeded);;
:return new GroupAssignment(assignment);;
stop
case (catch (final MissingSourceTopicException e))
:log.error("Caught an error in the task assignment. Returning an error assignment.",e);;
:return new GroupAssignment(errorAssignment(clientMetadataMap,
AssignorError.INCOMPLETE_SOURCE_TOPIC_METADATA.code()));;
stop
case (catch (final TaskAssignmentException e))
:log.error("Caught an error in the task assignment. Returning an error assignment.",e);;
:return new GroupAssignment(errorAssignment(clientMetadataMap,
AssignorError.ASSIGNMENT_ERROR.code()));;
stop
endswitch
end
start
note right
boolean checkMetadataVersions(final int minReceivedMetadataVersion,
final int minSupportedMetadataVersion,final int futureMetadataVersion)
end note
:final boolean versionProbing;;
if (futureMetadataVersion==UNKNOWN) then (yes) 
:versionProbing=false;;
else if (minReceivedMetadataVersion>=EARLIEST_PROBEABLE_VERSION) then (yes) 
:versionProbing=true;;
:log.info("Received a future (version probing) subscription (version: {})."+
" Sending assignment back (with supported version {}).",
futureMetadataVersion,minSupportedMetadataVersion);;
else (no)
:throw new TaskAssignmentException("Received a future (version probing) subscription (version: "+
futureMetadataVersion+") and an incompatible pre Kafka 2.0 subscription (version: "+
minReceivedMetadataVersion+") at the same time.");;
stop
endif 
if (minReceivedMetadataVersion<LATEST_SUPPORTED_VERSION) then (yes) 
:log.info("Downgrade metadata to version {}. Latest supported version is {}.",
minReceivedMetadataVersion,LATEST_SUPPORTED_VERSION);;
endif 
if (minSupportedMetadataVersion<LATEST_SUPPORTED_VERSION) then (yes) 
:log.info("Downgrade latest supported metadata to version {}. Latest supported version is {}.",
minSupportedMetadataVersion,LATEST_SUPPORTED_VERSION);;
endif 
:return versionProbing;;
stop
end
start
note right
RepartitionTopics prepareRepartitionTopics(final Cluster metadata)
end note
:final RepartitionTopics repartitionTopics=new RepartitionTopics(taskManager.topologyMetadata(),
internalTopicManager,copartitionedTopicsEnforcer,
metadata,logPrefix);;
:repartitionTopics.setup();;
:final boolean isMissingInputTopics=!repartitionTopics.missingSourceTopicExceptions()
.isEmpty();;
if (isMissingInputTopics) then (yes) 
if (!taskManager.topologyMetadata().hasNamedTopologies()) then (yes) 
:throw new MissingSourceTopicException("Missing source topics.");;
stop
else (no)
:nonFatalExceptionsToHandle.addAll(repartitionTopics.missingSourceTopicExceptions());;
endif 
endif 
:return repartitionTopics;;
stop
end
start
note right
void populateTasksForMaps(final Map<TopicPartition,
TaskId> taskForPartition,final Map<Subtopology,
Set<TaskId>> tasksForTopicGroup,final Set<String> allSourceTopics,
final Map<TaskId,Set<TopicPartition>> partitionsForTask,
final Cluster fullMetadata)
end note
:final Set<TopicPartition> allAssignedPartitions=new HashSet<>();;
:final TaskId id=entry.getKey();;
:final Set<TopicPartition> partitions=entry.getValue();;
:taskForPartition.put(partition,id);;
if (allAssignedPartitions.contains(partition)) then (yes) 
:log.warn("Partition {} is assigned to more than one tasks: {}",
partition,partitionsForTask);;
endif 
:allAssignedPartitions.addAll(partitions);;
:tasksForTopicGroup.computeIfAbsent(new Subtopology(id.subtopology(),
id.topologyName()),k->new HashSet<>()).add(id);;
:checkAllPartitions(allSourceTopics,partitionsForTask,
allAssignedPartitions,fullMetadata);;
end
start
note right
void checkAllPartitions(final Set<String> allSourceTopics,
final Map<TaskId,Set<TopicPartition>> partitionsForTask,
final Set<TopicPartition> allAssignedPartitions,
final Cluster fullMetadata)
end note
:final List<PartitionInfo> partitionInfoList=fullMetadata.partitionsForTopic(topic);;
if (partitionInfoList.isEmpty()) then (yes) 
:log.warn("No partitions found for topic {}",
topic);;
else (no)
:final TopicPartition partition=new TopicPartition(partitionInfo.topic(),
partitionInfo.partition());;
if (!allAssignedPartitions.contains(partition)) then (yes) 
:log.warn("Partition {} is not assigned to any tasks: {}"+
" Possible causes of a partition not getting assigned"+
" is that another topic defined in the topology has not been"+
" created when starting your streams application,"+
" resulting in no tasks created for this topology at all.",
partition,partitionsForTask);;
endif 
endif 
end
start
note right
boolean assignTasksToClients(final Cluster fullMetadata,
final Set<String> allSourceTopics,final Map<Subtopology,
TopicsInfo> topicGroups,final Map<UUID,ClientMetadata> clientMetadataMap,
final Map<TaskId,Set<TopicPartition>> partitionsForTask,
final Set<TaskId> statefulTasks)
end note
if (!statefulTasks.isEmpty()) then (yes) 
:throw new TaskAssignmentException("The stateful tasks should not be populated before assigning tasks to clients");;
stop
endif 
:final Map<TopicPartition,TaskId> taskForPartition=new HashMap<>();;
:final Map<Subtopology,Set<TaskId>> tasksForTopicGroup=new HashMap<>();;
:populateTasksForMaps(taskForPartition,tasksForTopicGroup,
allSourceTopics,partitionsForTask,fullMetadata);;
:final ChangelogTopics changelogTopics=new ChangelogTopics(internalTopicManager,
topicGroups,tasksForTopicGroup,logPrefix);;
:changelogTopics.setup();;
:final Map<UUID,ClientState> clientStates=new HashMap<>();;
:final boolean lagComputationSuccessful=populateClientStatesMap(clientStates,
clientMetadataMap,taskForPartition,changelogTopics);;
:log.info("All members participating in this rebalance: \n{}.",clientStates.entrySet().stream().map(entry->entry.getKey()+
": "+entry.getValue().consumers()).collect(Collectors.joining(Utils.NL)));;
:final Set<TaskId> allTasks=partitionsForTask.keySet();;
:statefulTasks.addAll(changelogTopics.statefulTaskIds());;
:log.debug("Assigning tasks {} including stateful {} to clients {} with number of replicas {}",
allTasks,statefulTasks,clientStates,numStandbyReplicas());;
:final TaskAssignor taskAssignor=createTaskAssignor(lagComputationSuccessful);;
:final boolean probingRebalanceNeeded=taskAssignor.assign(clientStates,
allTasks,statefulTasks,assignmentConfigs);;
:log.info("Assigned tasks {} including stateful {} to clients as: \n{}.",allTasks,statefulTasks,clientStates.entrySet().stream().map(entry->entry.getKey()+
"="+entry.getValue().currentAssignment()).collect(Collectors.joining(Utils.NL)));;
:return probingRebalanceNeeded;;
stop
end
start
note right
TaskAssignor createTaskAssignor(final boolean lagComputationSuccessful)
end note
:final TaskAssignor taskAssignor=taskAssignorSupplier.get();;
if (taskAssignor instanceof StickyTaskAssignor ) then (yes) 
:return taskAssignor;;
stop
else if (lagComputationSuccessful) then (yes) 
:return taskAssignor;;
stop
else (no)
:log.info("Failed to fetch end offsets for changelogs, will return previous assignment to clients and "+
"trigger another rebalance to retry.");;
:return new FallbackPriorTaskAssignor();;
stop
endif 
end
start
note right
boolean populateClientStatesMap(final Map<UUID,
ClientState> clientStates,final Map<UUID,
ClientMetadata> clientMetadataMap,final Map<TopicPartition,
TaskId> taskForPartition,final ChangelogTopics changelogTopics)
end note
:boolean fetchEndOffsetsSuccessful;;
:Map<TaskId,Long> allTaskEndOffsetSums;;
switch (try/catch block)
case (try)
:final KafkaFuture<Map<TopicPartition,ListOffsetsResultInfo>> endOffsetsFuture=fetchEndOffsetsFuture(changelogTopics.preExistingNonSourceTopicBasedPartitions(),
adminClient);;
:final Map<TopicPartition,Long> sourceChangelogEndOffsets=fetchCommittedOffsets(changelogTopics.preExistingSourceTopicBasedPartitions(),
mainConsumerSupplier.get());;
:final Map<TopicPartition,ListOffsetsResultInfo> endOffsets=ClientUtils.getEndOffsets(endOffsetsFuture);;
:allTaskEndOffsetSums=computeEndOffsetSumsByTask(endOffsets,
sourceChangelogEndOffsets,changelogTopics);;
:fetchEndOffsetsSuccessful=true;;
case (catch (final StreamsException |TimeoutException e))
:allTaskEndOffsetSums=changelogTopics.statefulTaskIds().stream().collect(Collectors.toMap(t->t,
t->UNKNOWN_OFFSET_SUM));;
:fetchEndOffsetsSuccessful=false;;
endswitch
:final UUID uuid=entry.getKey();;
:final ClientState state=entry.getValue()
.state;;
:state.initializePrevTasks(taskForPartition,
taskManager.topologyMetadata().hasNamedTopologies());;
:state.computeTaskLags(uuid,allTaskEndOffsetSums);;
:clientStates.put(uuid,state);;
:return fetchEndOffsetsSuccessful;;
stop
end
start
note right
Map<TaskId,Long> computeEndOffsetSumsByTask(final Map<TopicPartition,
ListOffsetsResultInfo> endOffsets,final Map<TopicPartition,
Long> sourceChangelogEndOffsets,final ChangelogTopics changelogTopics)
end note
:final Map<TaskId,Long> taskEndOffsetSums=new HashMap<>();;
:taskEndOffsetSums.put(taskId,0L);;
:final long changelogPartitionEndOffset;;
if (sourceChangelogEndOffsets.containsKey(changelogPartition)) then (yes) 
:changelogPartitionEndOffset=sourceChangelogEndOffsets.get(changelogPartition);;
else if (endOffsets.containsKey(changelogPartition)) then (yes) 
:changelogPartitionEndOffset=endOffsets.get(changelogPartition)
.offset();;
else (no)
:log.debug("Fetched offsets did not contain the changelog {} of task {}",
changelogPartition,taskId);;
:throw new IllegalStateException("Could not get end offset for "+
changelogPartition);;
stop
endif 
:final long newEndOffsetSum=taskEndOffsetSums.get(taskId)+
changelogPartitionEndOffset;;
if (newEndOffsetSum<0) then (yes) 
:taskEndOffsetSums.put(taskId,Long.MAX_VALUE);;
:break ;;
else (no)
:taskEndOffsetSums.put(taskId,newEndOffsetSum);;
endif 
:return taskEndOffsetSums;;
stop
end
start
note right
void populatePartitionsByHostMaps(final Map<HostInfo,
Set<TopicPartition>> partitionsByHost,final Map<HostInfo,
Set<TopicPartition>> standbyPartitionsByHost,
final Map<TaskId,Set<TopicPartition>> partitionsForTask,
final Map<UUID,ClientMetadata> clientMetadataMap)
end note
:final HostInfo hostInfo=entry.getValue()
.hostInfo;;
if (hostInfo!=null ) then (yes) 
:final Set<TopicPartition> topicPartitions=new HashSet<>();;
:final Set<TopicPartition> standbyPartitions=new HashSet<>();;
:final ClientState state=entry.getValue()
.state;;
:topicPartitions.addAll(partitionsForTask.get(id));;
:standbyPartitions.addAll(partitionsForTask.get(id));;
:partitionsByHost.put(hostInfo,topicPartitions);;
:standbyPartitionsByHost.put(hostInfo,standbyPartitions);;
endif 
end
start
note right
Map<String,Assignment> computeNewAssignment(final Set<TaskId> statefulTasks,
final Map<UUID,ClientMetadata> clientsMetadata,
final Map<TaskId,Set<TopicPartition>> partitionsForTask,
final Map<HostInfo,Set<TopicPartition>> partitionsByHostState,
final Map<HostInfo,Set<TopicPartition>> standbyPartitionsByHost,
final Set<TopicPartition> allOwnedPartitions,
final int minUserMetadataVersion,final int minSupportedMetadataVersion,
final boolean versionProbing,final boolean shouldTriggerProbingRebalance)
end note
:boolean rebalanceRequired=shouldTriggerProbingRebalance||versionProbing;;
:final Map<String,Assignment> assignment=new HashMap<>();;
:final UUID clientId=clientEntry.getKey();;
:final ClientMetadata clientMetadata=clientEntry.getValue();;
:final ClientState state=clientMetadata.state;;
:final SortedSet<String> consumers=clientMetadata.consumers;;
:final Map<String,Integer> threadTaskCounts=new HashMap<>();;
:final Map<String,List<TaskId>> activeTaskStatefulAssignment=assignTasksToThreads(state.statefulActiveTasks(),
true,consumers,state,threadTaskCounts);;
:final Map<String,List<TaskId>> standbyTaskAssignment=assignTasksToThreads(state.standbyTasks(),
true,consumers,state,threadTaskCounts);;
:final Map<String,List<TaskId>> activeTaskStatelessAssignment=assignTasksToThreads(state.statelessActiveTasks(),
false,consumers,state,threadTaskCounts);;
:final Map<String,List<TaskId>> activeTaskAssignment=activeTaskStatefulAssignment;;
:activeTaskAssignment.get(threadEntry.getKey())
.addAll(threadEntry.getValue());;
:final boolean encodeNextProbingRebalanceTime=shouldTriggerProbingRebalance&&clientId.equals(taskManager.processId());;
:final boolean tasksRevoked=addClientAssignments(statefulTasks,
assignment,clientMetadata,partitionsForTask,
partitionsByHostState,standbyPartitionsByHost,
allOwnedPartitions,activeTaskAssignment,standbyTaskAssignment,
minUserMetadataVersion,minSupportedMetadataVersion,
encodeNextProbingRebalanceTime);;
if (tasksRevoked||encodeNextProbingRebalanceTime) then (yes) 
:rebalanceRequired=true;;
:log.debug("Requested client {} to schedule a followup rebalance",
clientId);;
endif 
:log.info("Client {} per-consumer assignment:\n"+
"\tprev owned active {}\n"+"\tprev owned standby {}\n"+
"\tassigned active {}\n"+"\trevoking active {}\n"+
"\tassigned standby {}\n",clientId,clientMetadata.state.prevOwnedActiveTasksByConsumer(),
clientMetadata.state.prevOwnedStandbyByConsumer(),
clientMetadata.state.assignedActiveTasksByConsumer(),
clientMetadata.state.revokingActiveTasksByConsumer(),
clientMetadata.state.assignedStandbyTasksByConsumer());;
if (rebalanceRequired) then (yes) 
:assignmentListener.onAssignmentComplete(false);;
:log.info("Finished unstable assignment of tasks,
 a followup rebalance will be scheduled.");;
else (no)
:assignmentListener.onAssignmentComplete(true);;
:log.info("Finished stable assignment of tasks,
 no followup rebalances required.");;
endif 
:return assignment;;
stop
end
start
note right
boolean addClientAssignments(final Set<TaskId> statefulTasks,
final Map<String,Assignment> assignment,final ClientMetadata clientMetadata,
final Map<TaskId,Set<TopicPartition>> partitionsForTask,
final Map<HostInfo,Set<TopicPartition>> partitionsByHostState,
final Map<HostInfo,Set<TopicPartition>> standbyPartitionsByHost,
final Set<TopicPartition> allOwnedPartitions,
final Map<String,List<TaskId>> activeTaskAssignments,
final Map<String,List<TaskId>> standbyTaskAssignments,
final int minUserMetadataVersion,final int minSupportedMetadataVersion,
final boolean probingRebalanceNeeded)
end note
:boolean followupRebalanceRequiredForRevokedTasks=false;;
:boolean shouldEncodeProbingRebalance=probingRebalanceNeeded;;
:final List<TaskId> activeTasksForConsumer=activeTaskAssignments.get(consumer);;
:final List<TopicPartition> activePartitionsList=new ArrayList<>();;
:final List<TaskId> assignedActiveList=new ArrayList<>();;
:final Set<TaskId> activeTasksRemovedPendingRevokation=populateActiveTaskAndPartitionsLists(activePartitionsList,
assignedActiveList,consumer,clientMetadata.state,
activeTasksForConsumer,partitionsForTask,
allOwnedPartitions);;
:final Map<TaskId,Set<TopicPartition>> standbyTaskMap=buildStandbyTaskMap(consumer,
standbyTaskAssignments.get(consumer),activeTasksRemovedPendingRevokation,
statefulTasks,partitionsForTask,clientMetadata.state);;
:final AssignmentInfo info=new AssignmentInfo(minUserMetadataVersion,
minSupportedMetadataVersion,assignedActiveList,
standbyTaskMap,partitionsByHostState,standbyPartitionsByHost,
AssignorError.NONE.code());;
if (!activeTasksRemovedPendingRevokation.isEmpty()) then (yes) 
:log.info("Requesting followup rebalance be scheduled immediately by {} due to tasks changing ownership.",
consumer);;
:info.setNextRebalanceTime(0L);;
:followupRebalanceRequiredForRevokedTasks=true;;
:shouldEncodeProbingRebalance=false;;
else if (shouldEncodeProbingRebalance) then (yes) 
:final long nextRebalanceTimeMs=time.milliseconds()+
probingRebalanceIntervalMs();;
:log.info("Requesting followup rebalance be scheduled by {} for {} ms to probe for caught-up replica tasks.",
consumer,nextRebalanceTimeMs);;
:info.setNextRebalanceTime(nextRebalanceTimeMs);;
:shouldEncodeProbingRebalance=false;;
endif 
:assignment.put(consumer,new Assignment(activePartitionsList,
info.encode()));;
:return followupRebalanceRequiredForRevokedTasks;;
stop
end
start
note right
Set<TaskId> populateActiveTaskAndPartitionsLists(final List<TopicPartition> activePartitionsList,
final List<TaskId> assignedActiveList,final String consumer,
final ClientState clientState,final List<TaskId> activeTasksForConsumer,
final Map<TaskId,Set<TopicPartition>> partitionsForTask,
final Set<TopicPartition> allOwnedPartitions)
end note
:final List<AssignedPartition> assignedPartitions=new ArrayList<>();;
:final Set<TaskId> removedActiveTasks=new TreeSet<>();;
:clientState.assignActiveToConsumer(taskId,
consumer);;
:final List<AssignedPartition> assignedPartitionsForTask=new ArrayList<>();;
:final String oldOwner=clientState.previousOwnerForPartition(partition);;
:final boolean newPartitionForConsumer=oldOwner==null ||!oldOwner.equals(consumer);;
if (newPartitionForConsumer&&allOwnedPartitions.contains(partition)) then (yes) 
:log.info("Removing task {} from {} active assignment until it is safely revoked in followup rebalance",
taskId,consumer);;
:removedActiveTasks.add(taskId);;
:clientState.revokeActiveFromConsumer(taskId,
consumer);;
:assignedPartitionsForTask.clear();;
:clientState.unassignActive(taskId);;
:break ;;
else (no)
:assignedPartitionsForTask.add(new AssignedPartition(taskId,
partition));;
endif 
:assignedPartitions.addAll(assignedPartitionsForTask);;
:Collections.sort(assignedPartitions);;
:assignedActiveList.add(partition.taskId);;
:activePartitionsList.add(partition.partition);;
:return removedActiveTasks;;
stop
end
start
note right
Map<TaskId,Set<TopicPartition>> buildStandbyTaskMap(final String consumer,
final Iterable<TaskId> standbyTasks,final Iterable<TaskId> revokedTasks,
final Set<TaskId> allStatefulTasks,final Map<TaskId,
Set<TopicPartition>> partitionsForTask,final ClientState clientState)
end note
:final Map<TaskId,Set<TopicPartition>> standbyTaskMap=new HashMap<>();;
:clientState.assignStandbyToConsumer(task,
consumer);;
:standbyTaskMap.put(task,partitionsForTask.get(task));;
if (allStatefulTasks.contains(task)) then (yes) 
:log.info("Adding removed stateful active task {} as a standby for {} before it is revoked in followup rebalance",
task,consumer);;
:clientState.assignStandbyToConsumer(task,
consumer);;
:clientState.assignStandby(task);;
:standbyTaskMap.put(task,partitionsForTask.get(task));;
endif 
:return standbyTaskMap;;
stop
end
start
note right
Map<String,List<TaskId>> assignTasksToThreads(final Collection<TaskId> tasksToAssign,
final boolean isStateful,final SortedSet<String> consumers,
final ClientState state,final Map<String,
Integer> threadLoad)
end note
:final Map<String,List<TaskId>> assignment=new HashMap<>();;
:assignment.put(consumer,new ArrayList<>());;
:final int totalTasks=threadLoad.values().stream().reduce(tasksToAssign.size(),
Integer::sum);;
:final int minTasksPerThread=(int )Math.floor(((double )totalTasks)/consumers.size());;
:final PriorityQueue<TaskId> unassignedTasks=new PriorityQueue<>(tasksToAssign);;
:final Queue<String> consumersToFill=new LinkedList<>();;
:final Map<TaskId,String> unassignedTaskToPreviousOwner=new TreeMap<>();;
if (!unassignedTasks.isEmpty()) then (yes) 
:final List<TaskId> threadAssignment=assignment.get(consumer);;
:final int tasksTargetCount=minTasksPerThread-threadLoad.getOrDefault(consumer,0);;
if (isStateful) then (yes) 
if (unassignedTasks.contains(task)) then (yes) 
if (threadAssignment.size()<tasksTargetCount) then (yes) 
:threadAssignment.add(task);;
:unassignedTasks.remove(task);;
else (no)
:unassignedTaskToPreviousOwner.put(task,consumer);;
endif 
endif 
endif 
if (threadAssignment.size()<tasksTargetCount) then (yes) 
:consumersToFill.offer(consumer);;
endif 
while (!consumersToFill.isEmpty())
:final TaskId task=unassignedTasks.poll();;
if (task!=null ) then (yes) 
:final String consumer=consumersToFill.poll();;
:final List<TaskId> threadAssignment=assignment.get(consumer);;
:threadAssignment.add(task);;
:final int threadTaskCount=threadAssignment.size()+
threadLoad.getOrDefault(consumer,0);;
if (threadTaskCount<minTasksPerThread) then (yes) 
:consumersToFill.offer(consumer);;
endif 
else (no)
:throw new TaskAssignmentException("Ran out of unassigned stateful tasks but some members were not at capacity");;
stop
endif 
endwhile
if (!unassignedTasks.isEmpty()) then (yes) 
:final int taskCount=assignment.get(consumer).size()+
threadLoad.getOrDefault(consumer,0);;
if (taskCount==minTasksPerThread) then (yes) 
:consumersToFill.add(consumer);;
endif 
:final TaskId task=taskEntry.getKey();;
:final String consumer=taskEntry.getValue();;
if (consumersToFill.contains(consumer)&&unassignedTasks.contains(task)) then (yes) 
:assignment.get(consumer).add(task);;
:unassignedTasks.remove(task);;
:consumersToFill.remove(consumer);;
endif 
:final String consumer=consumersToFill.poll();;
:final List<TaskId> threadAssignment=assignment.get(consumer);;
:threadAssignment.add(task);;
endif 
endif 
:final String consumer=taskEntry.getKey();;
:final int totalCount=threadLoad.getOrDefault(consumer,0)+
taskEntry.getValue().size();;
:threadLoad.put(consumer,totalCount);;
:return assignment;;
stop
end
start
note right
void validateMetadataVersions(final int receivedAssignmentMetadataVersion,
final int latestCommonlySupportedVersion)
end note
if (receivedAssignmentMetadataVersion>usedSubscriptionMetadataVersion) then (yes) 
:log.error("Leader sent back an assignment with version {} which was greater than our used version {}",
receivedAssignmentMetadataVersion,usedSubscriptionMetadataVersion);;
:throw new TaskAssignmentException("Sent a version "+
usedSubscriptionMetadataVersion+" subscription but got an assignment with higher version "+
receivedAssignmentMetadataVersion+".");;
stop
endif 
if (latestCommonlySupportedVersion>LATEST_SUPPORTED_VERSION) then (yes) 
:log.error("Leader sent back assignment with commonly supported version {} that is greater than our "+
"actual latest supported version {}",latestCommonlySupportedVersion,
LATEST_SUPPORTED_VERSION);;
:throw new TaskAssignmentException("Can't upgrade to metadata version greater than we support");;
stop
endif 
end
start
note right
boolean maybeUpdateSubscriptionVersion(final int receivedAssignmentMetadataVersion,
final int latestCommonlySupportedVersion)
end note
if (receivedAssignmentMetadataVersion>=EARLIEST_PROBEABLE_VERSION) then (yes) 
if (latestCommonlySupportedVersion>usedSubscriptionMetadataVersion) then (yes) 
:log.info("Sent a version {} subscription and group's latest commonly supported version is {} (successful "+
"version probing and end of rolling upgrade). Upgrading subscription metadata version to "+
"{} for next rebalance.",usedSubscriptionMetadataVersion,
latestCommonlySupportedVersion,latestCommonlySupportedVersion);;
:usedSubscriptionMetadataVersion=latestCommonlySupportedVersion;;
:return true;;
stop
endif 
if (receivedAssignmentMetadataVersion<usedSubscriptionMetadataVersion) then (yes) 
:log.info("Sent a version {} subscription and got version {} assignment back (successful version probing). "+
"Downgrade subscription metadata to commonly supported version {} and trigger new rebalance.",
usedSubscriptionMetadataVersion,receivedAssignmentMetadataVersion,
latestCommonlySupportedVersion);;
:usedSubscriptionMetadataVersion=latestCommonlySupportedVersion;;
:return true;;
stop
endif 
else (no)
:log.debug("Received an assignment version {} that is less than the earliest version that allows version "+
"probing {}. If this is not during a rolling upgrade from version 2.0 or below,
 this is an error.",receivedAssignmentMetadataVersion,
EARLIEST_PROBEABLE_VERSION);;
endif 
:return false;;
stop
end
start
note right
void onAssignment(final Assignment assignment,
final ConsumerGroupMetadata metadata)
end note
:final List<TopicPartition> partitions=new ArrayList<>(assignment.partitions());;
:partitions.sort(PARTITION_COMPARATOR);;
:final AssignmentInfo info=AssignmentInfo.decode(assignment.userData());;
if (info.errCode()!=AssignorError.NONE.code()) then (yes) 
:assignmentErrorCode.set(info.errCode());;
:return ;;
stop
endif 
:final int receivedAssignmentMetadataVersion=info.version();;
:final int latestCommonlySupportedVersion=info.commonlySupportedVersion();;
:validateMetadataVersions(receivedAssignmentMetadataVersion,
latestCommonlySupportedVersion);;
:final Map<TaskId,Set<TopicPartition>> activeTasks;;
:final Map<TopicPartition,PartitionInfo> topicToPartitionInfo;;
:final Map<HostInfo,Set<TopicPartition>> partitionsByHost;;
:final Map<HostInfo,Set<TopicPartition>> standbyPartitionsByHost;;
:final long encodedNextScheduledRebalanceMs;;
switch (switch receivedAssignmentMetadataVersion)
endswitch
:maybeScheduleFollowupRebalance(encodedNextScheduledRebalanceMs,
receivedAssignmentMetadataVersion,latestCommonlySupportedVersion,
partitionsByHost.keySet());;
:final Cluster fakeCluster=Cluster.empty()
.withPartitions(topicToPartitionInfo);;
:streamsMetadataState.onChange(partitionsByHost,
standbyPartitionsByHost,fakeCluster);;
:taskManager.handleAssignment(activeTasks,
info.standbyTasks());;
end
start
note right
void maybeScheduleFollowupRebalance(final long encodedNextScheduledRebalanceMs,
final int receivedAssignmentMetadataVersion,
final int latestCommonlySupportedVersion,
final Set<HostInfo> groupHostInfo)
end note
if (maybeUpdateSubscriptionVersion(receivedAssignmentMetadataVersion,latestCommonlySupportedVersion)) then (yes) 
:log.info("Requested to schedule immediate rebalance due to version probing.");;
:nextScheduledRebalanceMs.set(0L);;
else if (!verifyHostInfo(groupHostInfo)) then (yes) 
:log.info("Requested to schedule immediate rebalance to update group with new host endpoint = {}.",
userEndPoint);;
:nextScheduledRebalanceMs.set(0L);;
else if (encodedNextScheduledRebalanceMs==0L) then (yes) 
:log.info("Requested to schedule immediate rebalance for new tasks to be safely revoked from current owner.");;
:nextScheduledRebalanceMs.set(0L);;
else if (encodedNextScheduledRebalanceMs<Long.MAX_VALUE) then (yes) 
:log.info("Requested to schedule probing rebalance for {} ms.",
encodedNextScheduledRebalanceMs);;
:nextScheduledRebalanceMs.set(encodedNextScheduledRebalanceMs);;
else (no)
:log.info("No followup rebalance was requested,
 resetting the rebalance schedule.");;
:nextScheduledRebalanceMs.set(Long.MAX_VALUE);;
endif 
end
start
note right
boolean verifyHostInfo(final Set<HostInfo> groupHostInfo)
end note
if (userEndPoint!=null &&!groupHostInfo.isEmpty()) then (yes) 
:final HostInfo myHostInfo=HostInfo.buildFromEndpoint(userEndPoint);;
:return groupHostInfo.contains(myHostInfo);;
stop
else (no)
:return true;;
stop
endif 
end
start
note right
Map<TaskId,Set<TopicPartition>> getActiveTasks(final List<TopicPartition> partitions,
final AssignmentInfo info)
end note
:final Map<TaskId,Set<TopicPartition>> activeTasks=new HashMap<>();;
:final TopicPartition partition=partitions.get(i);;
:final TaskId id=info.activeTasks().get(i);;
:activeTasks.computeIfAbsent(id,k1->new HashSet<>())
.add(partition);;
:return activeTasks;;
stop
end
start
note right
Map<TopicPartition,PartitionInfo> getTopicPartitionInfo(final Map<HostInfo,
Set<TopicPartition>> partitionsByHost)
end note
:final Map<TopicPartition,PartitionInfo> topicToPartitionInfo=new HashMap<>();;
:topicToPartitionInfo.put(topicPartition,
new PartitionInfo(topicPartition.topic(),
topicPartition.partition(),null ,new Node[0],
new Node[0]));;
:return topicToPartitionInfo;;
stop
end
start
note right
void validateActiveTaskEncoding(final List<TopicPartition> partitions,
final AssignmentInfo info,final String logPrefix)
end note
if (partitions.size()!=info.activeTasks().size()) then (yes) 
:throw new TaskAssignmentException(String.format("%sNumber of assigned partitions %d is not equal to "+
"the number of active taskIds %d, assignmentInfo=%s",
logPrefix,partitions.size(),info.activeTasks().size(),
info.toString()));;
stop
endif 
end
start
note right
int updateMinReceivedVersion(final int usedVersion,
final int minReceivedMetadataVersion)
end note
:return Math.min(usedVersion,minReceivedMetadataVersion);;
stop
end
start
note right
int updateMinSupportedVersion(final int supportedVersion,
final int minSupportedMetadataVersion)
end note
if (supportedVersion<minSupportedMetadataVersion) then (yes) 
:log.debug("Downgrade the current minimum supported version {} to the smaller seen supported version {}",
minSupportedMetadataVersion,supportedVersion);;
:return supportedVersion;;
stop
else (no)
:log.debug("Current minimum supported version remains at {},
 last seen supported version was {}",minSupportedMetadataVersion,
supportedVersion);;
:return minSupportedMetadataVersion;;
stop
endif 
end
start
note right
void setInternalTopicManager(final InternalTopicManager internalTopicManager)
end note
:this.internalTopicManager=internalTopicManager;;
end
start
note right
RebalanceProtocol rebalanceProtocol()
end note
:return rebalanceProtocol;;
stop
end
start
note right
String userEndPoint()
end note
:return userEndPoint;;
stop
end
start
note right
TaskManager taskManager()
end note
:return taskManager;;
stop
end
start
note right
byte uniqueField()
end note
:return uniqueField;;
stop
end
start
note right
Map<String,String> clientTags()
end note
:return clientTags;;
stop
end
start
note right
void handleRebalanceStart(final Set<String> topics)
end note
:taskManager.handleRebalanceStart(topics);;
end
start
note right
long acceptableRecoveryLag()
end note
:return assignmentConfigs.acceptableRecoveryLag;;
stop
end
start
note right
int maxWarmupReplicas()
end note
:return assignmentConfigs.maxWarmupReplicas;;
stop
end
start
note right
int numStandbyReplicas()
end note
:return assignmentConfigs.numStandbyReplicas;;
stop
end
start
note right
long probingRebalanceIntervalMs()
end note
:return assignmentConfigs.probingRebalanceIntervalMs;;
stop
end
@enduml

PlantUML version 1.2022.7(Mon Aug 22 17:01:30 UTC 2022)
(GPL source distribution)
Java Runtime: OpenJDK Runtime Environment
JVM: OpenJDK 64-Bit Server VM
Default Encoding: UTF-8
Language: en
Country: null
--></g></svg>