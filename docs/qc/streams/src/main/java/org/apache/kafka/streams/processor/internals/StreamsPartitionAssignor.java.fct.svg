<?xml version="1.0" encoding="UTF-8" standalone="no"?><svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" contentStyleType="text/css" height="544px" preserveAspectRatio="none" style="width:608px;height:544px;background:#000000;" version="1.1" viewBox="0 0 608 544" width="608px" zoomAndPan="magnify"><defs/><g><rect fill="#0E0522" height="1" style="stroke:#0E0522;stroke-width:1.0;" width="1" x="0" y="0"/><text fill="#33FF02" font-family="sans-serif" font-size="12" font-style="italic" font-weight="bold" lengthAdjust="spacing" textLength="127" x="5" y="17">PlantUML 1.2022.7</text><rect fill="#33FF02" height="21.2969" style="stroke:#33FF02;stroke-width:1.0;" width="596" x="5" y="26.9688"/><text fill="#000000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="188" x="6" y="41.9688">[From string (line 177) ]</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="5" x="5" y="62.2656"> </text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="79" x="5" y="78.5625">@startuml</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="190" x="5" y="94.8594">!pragma layout smetana</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="111.1563">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="127.4531">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="384" x="5" y="143.75">constructor AssignedPartition(final TaskIdtaskId,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="15" x="5" y="160.0469">...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="185" x="5" y="176.3438">... ( skipping 151 lines )</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="15" x="5" y="192.6406">...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="256" x="5" y="208.9375">minSupportedMetadataVersion);</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="458" x="5" y="225.2344">if (usedVersion&gt;LATEST_SUPPORTED_VERSION) then (yes)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="306" x="5" y="241.5313">:futureMetadataVersion=usedVersion;</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="184" x="5" y="257.8281">:processId=FUTURE_ID;</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="462" x="5" y="274.125">if (!clientMetadataMap.containsKey(FUTURE_ID)) then (yes)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="477" x="5" y="290.4219">:clientMetadataMap.put(FUTURE_ID,new ClientMetadata(null ,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="203" x="5" y="306.7188">Collections.emptyMap()));</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="40" x="5" y="323.0156">endif</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="70" x="5" y="339.3125">else (no)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="227" x="5" y="355.6094">:processId=info.processId();</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="40" x="5" y="371.9063">endif</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="285" x="5" y="388.2031">if (clientMetadata==null ) then (yes)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="452" x="5" y="404.5">:clientMetadata=new ClientMetadata(info.userEndPoint(),</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="140" x="5" y="420.7969">info.clientTags());</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="447" x="5" y="437.0938">:clientMetadataMap.put(info.processId(),clientMetadata);</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="40" x="5" y="453.3906">endif</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="596" x="5" y="469.6875">:clientMetadata.addConsumer(consumerId,subscription.ownedPartitions());</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="469" x="5" y="485.9844">:allOwnedPartitions.addAll(subscription.ownedPartitions());</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="555" x="5" y="502.2813">if (allOwnedPartitions.size()&lt;prevSize+subscription.ownedPartitions()</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" text-decoration="wavy underline" textLength="141" x="5" y="518.5781">.size()) then (yes)</text><text fill="#FF0000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="106" x="10" y="534.875">Syntax Error?</text><!--MD5=[7a55a561a5a8d8b33d56fb7f2c10465f]
@startuml
!pragma layout smetana
start
note right
constructor AssignedPartition(final TaskIdtaskId,
final TopicPartitionpartition)
end note
:this.taskId=taskId;
:this.partition=partition;
end
start
note right
intcompareTo(final AssignedPartitionthat)
end note
:return PARTITION_COMPARATOR.compare(partition,
that.partition);;
stop
end
start
note right
booleanequals(final Objecto)
end note
if (!(o instanceof AssignedPartition)) then (yes) 
:return false;;
stop
endif 
:return compareTo(other)==0;;
stop
end
start
note right
inthashCode()
end note
:return partition.hashCode();;
stop
end
start
note right
constructor ClientMetadata(final StringendPoint,
final Map<String,String>clientTags)
end note
:hostInfo=HostInfo.buildFromEndpoint(endPoint);
:consumers=new TreeSet<>();
:state=new ClientState(clientTags);
end
start
note right
void addConsumer(final StringconsumerMemberId,
final List<TopicPartition>ownedPartitions)
end note
:consumers.add(consumerMemberId);
:state.incrementCapacity();
:state.addOwnedPartitions(ownedPartitions,
consumerMemberId);
end
start
note right
void addPreviousTasksAndOffsetSums(final StringconsumerId,
final Map<TaskId,Long>taskOffsetSums)
end note
:state.addPreviousTasksAndOffsetSums(consumerId,
taskOffsetSums);
end
start
note right
StringtoString()
end note
:return "ClientMetadata{"+"hostInfo="+hostInfo+
", consumers="+consumers+", state="+state+
'}';;
stop
end
start
note right
void configure(final Map<String,? >configs)
end note
:logPrefix=assignorConfiguration.logPrefix();
:log=new LogContext(logPrefix).logger(getClass());
:usedSubscriptionMetadataVersion=assignorConfiguration.configuredMetadataVersion(usedSubscriptionMetadataVersion);
:mainConsumerSupplier=()->Objects.requireNonNull(referenceContainer.mainConsumer,
"Main consumer was not specified");
:adminClient=Objects.requireNonNull(referenceContainer.adminClient,
"Admin client was not specified");
:taskManager=Objects.requireNonNull(referenceContainer.taskManager,
"TaskManager was not specified");
:streamsMetadataState=Objects.requireNonNull(referenceContainer.streamsMetadataState,
"StreamsMetadataState was not specified");
:assignmentErrorCode=referenceContainer.assignmentErrorCode;
:nextScheduledRebalanceMs=referenceContainer.nextScheduledRebalanceMs;
:nonFatalExceptionsToHandle=referenceContainer.nonFatalExceptionsToHandle;
:time=Objects.requireNonNull(referenceContainer.time,
"Time was not specified");
:assignmentConfigs=assignorConfiguration.assignmentConfigs();
:partitionGrouper=new PartitionGrouper();
:userEndPoint=assignorConfiguration.userEndPoint();
:internalTopicManager=assignorConfiguration.internalTopicManager();
:copartitionedTopicsEnforcer=assignorConfiguration.copartitionedTopicsEnforcer();
:rebalanceProtocol=assignorConfiguration.rebalanceProtocol();
:taskAssignorSupplier=assignorConfiguration::taskAssignor;
:assignmentListener=assignorConfiguration.assignmentListener();
:uniqueField=0;
:clientTags=referenceContainer.clientTags;
end
start
note right
Stringname()
end note
:return "stream";;
stop
end
start
note right
List<RebalanceProtocol>supportedProtocols()
end note
:supportedProtocols.add(RebalanceProtocol.EAGER);
if (rebalanceProtocol==RebalanceProtocol.COOPERATIVE) then (yes) 
:supportedProtocols.add(rebalanceProtocol);
endif 
:return supportedProtocols;;
stop
end
start
note right
ByteBuffersubscriptionUserData(final Set<String>topics)
end note
:handleRebalanceStart(topics);
:uniqueField++;
:return new SubscriptionInfo(usedSubscriptionMetadataVersion,
LATEST_SUPPORTED_VERSION,taskManager.processId(),
userEndPoint,taskOffsetSums,uniqueField,assignmentErrorCode.get(),
clientTags).encode();;
stop
end
start
note right
Map<String,Assignment>errorAssignment(final Map<UUID,
ClientMetadata>clientsMetadata,final interrorCode)
end note
:assignment.put(consumerId,new Assignment(Collections.emptyList(),
new AssignmentInfo(LATEST_SUPPORTED_VERSION,
Collections.emptyList(),Collections.emptyMap(),
Collections.emptyMap(),Collections.emptyMap(),
errorCode).encode()));
:return assignment;;
stop
end
start
note right
GroupAssignmentassign(final Clustermetadata,
final GroupSubscriptiongroupSubscription)
end note
if (info.errorCode()==AssignorError.SHUTDOWN_REQUESTED.code()) then (yes) 
:shutdownRequested=true;
endif 
:minReceivedMetadataVersion=updateMinReceivedVersion(usedVersion,
minReceivedMetadataVersion);
:minSupportedMetadataVersion=updateMinSupportedVersion(info.latestSupportedVersion(),
minSupportedMetadataVersion);
if (usedVersion>LATEST_SUPPORTED_VERSION) then (yes) 
:futureMetadataVersion=usedVersion;
:processId=FUTURE_ID;
if (!clientMetadataMap.containsKey(FUTURE_ID)) then (yes) 
:clientMetadataMap.put(FUTURE_ID,new ClientMetadata(null ,
Collections.emptyMap()));
endif 
else (no)
:processId=info.processId();
endif 
if (clientMetadata==null ) then (yes) 
:clientMetadata=new ClientMetadata(info.userEndPoint(),
info.clientTags());
:clientMetadataMap.put(info.processId(),clientMetadata);
endif 
:clientMetadata.addConsumer(consumerId,subscription.ownedPartitions());
:allOwnedPartitions.addAll(subscription.ownedPartitions());
if (allOwnedPartitions.size()<prevSize+subscription.ownedPartitions()
.size()) then (yes) 
:assignmentErrorFound=true;
endif 
:clientMetadata.addPreviousTasksAndOffsetSums(consumerId,
info.taskOffsetSums());
if (assignmentErrorFound) then (yes) 
:log.warn("The previous assignment contains a partition more than once. "+
"\t Mapping: {}",subscriptions);
endif 
:log.debug("Constructed client metadata {} from the member subscriptions.",
clientMetadataMap);
if (shutdownRequested) then (yes) 
:return new GroupAssignment(errorAssignment(clientMetadataMap,
AssignorError.SHUTDOWN_REQUESTED.code()));;
stop
endif 
:log.debug("Created repartition topics {} from the parsed topology.",
allRepartitionTopicPartitions.values());
:allSourceTopics.addAll(entry.getValue().sourceTopics);
:sourceTopicsByGroup.put(entry.getKey(),entry.getValue()
.sourceTopics);
if (minReceivedMetadataVersion>=2) then (yes) 
:populatePartitionsByHostMaps(partitionsByHost,
standbyPartitionsByHost,partitionsForTask,
clientMetadataMap);
endif 
:streamsMetadataState.onChange(partitionsByHost,
standbyPartitionsByHost,fullMetadata);
:return new GroupAssignment(assignment);;
stop
:log.error("Caught an error in the task assignment. Returning an error assignment.",e);
:return new GroupAssignment(errorAssignment(clientMetadataMap,
AssignorError.INCOMPLETE_SOURCE_TOPIC_METADATA.code()));;
stop
:log.error("Caught an error in the task assignment. Returning an error assignment.",e);
:return new GroupAssignment(errorAssignment(clientMetadataMap,
AssignorError.ASSIGNMENT_ERROR.code()));;
stop
end
start
note right
booleancheckMetadataVersions(final intminReceivedMetadataVersion,
final intminSupportedMetadataVersion,final intfutureMetadataVersion)
end note
if (futureMetadataVersion==UNKNOWN) then (yes) 
:versionProbing=false;
else (no)
:throw new TaskAssignmentException("Received a future (version probing) subscription (version: "+
futureMetadataVersion+") and an incompatible pre Kafka 2.0 subscription (version: "+
minReceivedMetadataVersion+") at the same time.");;
endif 
if (minReceivedMetadataVersion<LATEST_SUPPORTED_VERSION) then (yes) 
:log.info("Downgrade metadata to version {}. Latest supported version is {}.",
minReceivedMetadataVersion,LATEST_SUPPORTED_VERSION);
endif 
if (minSupportedMetadataVersion<LATEST_SUPPORTED_VERSION) then (yes) 
:log.info("Downgrade latest supported metadata to version {}. Latest supported version is {}.",
minSupportedMetadataVersion,LATEST_SUPPORTED_VERSION);
endif 
:return versionProbing;;
stop
end
start
note right
RepartitionTopicsprepareRepartitionTopics(final Clustermetadata)
end note
:repartitionTopics.setup();
if (isMissingInputTopics) then (yes) 
if (!taskManager.topologyMetadata().hasNamedTopologies()) then (yes) 
:throw new MissingSourceTopicException("Missing source topics.");;
else (no)
:nonFatalExceptionsToHandle.addAll(repartitionTopics.missingSourceTopicExceptions());
endif 
endif 
:return repartitionTopics;;
stop
end
start
note right
void populateTasksForMaps(final Map<TopicPartition,
TaskId>taskForPartition,final Map<Subtopology,
Set<TaskId>>tasksForTopicGroup,final Set<String>allSourceTopics,
final Map<TaskId,Set<TopicPartition>>partitionsForTask,
final ClusterfullMetadata)
end note
:taskForPartition.put(partition,id);
if (allAssignedPartitions.contains(partition)) then (yes) 
:log.warn("Partition {} is assigned to more than one tasks: {}",
partition,partitionsForTask);
endif 
:allAssignedPartitions.addAll(partitions);
:tasksForTopicGroup.computeIfAbsent(new Subtopology(id.subtopology(),
id.topologyName()),k->new HashSet<>()).add(id);
:checkAllPartitions(allSourceTopics,partitionsForTask,
allAssignedPartitions,fullMetadata);
end
start
note right
void checkAllPartitions(final Set<String>allSourceTopics,
final Map<TaskId,Set<TopicPartition>>partitionsForTask,
final Set<TopicPartition>allAssignedPartitions,
final ClusterfullMetadata)
end note
if (partitionInfoList.isEmpty()) then (yes) 
:log.warn("No partitions found for topic {}",
topic);
else (no)
if (!allAssignedPartitions.contains(partition)) then (yes) 
:log.warn("Partition {} is not assigned to any tasks: {}"+
" Possible causes of a partition not getting assigned"+
" is that another topic defined in the topology has not been"+
" created when starting your streams application,"+
" resulting in no tasks created for this topology at all.",
partition,partitionsForTask);
endif 
endif 
end
start
note right
booleanassignTasksToClients(final ClusterfullMetadata,
final Set<String>allSourceTopics,final Map<Subtopology,
TopicsInfo>topicGroups,final Map<UUID,ClientMetadata>clientMetadataMap,
final Map<TaskId,Set<TopicPartition>>partitionsForTask,
final Set<TaskId>statefulTasks)
end note
if (!statefulTasks.isEmpty()) then (yes) 
:throw new TaskAssignmentException("The stateful tasks should not be populated before assigning tasks to clients");;
endif 
:populateTasksForMaps(taskForPartition,tasksForTopicGroup,
allSourceTopics,partitionsForTask,fullMetadata);
:changelogTopics.setup();
:log.info("All members participating in this rebalance: \n{}.",
clientStates.entrySet().stream().map(entry->entry.getKey()+
": "+entry.getValue().consumers()).collect(Collectors.joining(Utils.NL)));
:statefulTasks.addAll(changelogTopics.statefulTaskIds());
:log.debug("Assigning tasks {} including stateful {} to clients {} with number of replicas {}",
allTasks,statefulTasks,clientStates,numStandbyReplicas());
:log.info("Assigned tasks {} including stateful {} to clients as: \n{}.",
allTasks,statefulTasks,clientStates.entrySet().stream().map(entry->entry.getKey()+
"="+entry.getValue().currentAssignment())
.collect(Collectors.joining(Utils.NL)));
:return probingRebalanceNeeded;;
stop
end
start
note right
TaskAssignorcreateTaskAssignor(final booleanlagComputationSuccessful)
end note
if (taskAssignor instanceof StickyTaskAssignor) then (yes) 
:return taskAssignor;;
stop
else (no)
:log.info("Failed to fetch end offsets for changelogs, will return previous assignment to clients and "+
"trigger another rebalance to retry.");
:return new FallbackPriorTaskAssignor();;
stop
endif 
end
start
note right
booleanpopulateClientStatesMap(final Map<UUID,
ClientState>clientStates,final Map<UUID,ClientMetadata>clientMetadataMap,
final Map<TopicPartition,TaskId>taskForPartition,
final ChangelogTopicschangelogTopics)
end note
:allTaskEndOffsetSums=computeEndOffsetSumsByTask(endOffsets,
sourceChangelogEndOffsets,changelogTopics);
:fetchEndOffsetsSuccessful=true;
:allTaskEndOffsetSums=changelogTopics.statefulTaskIds().stream().collect(Collectors.toMap(t->t,
t->UNKNOWN_OFFSET_SUM));
:fetchEndOffsetsSuccessful=false;
:state.initializePrevTasks(taskForPartition,
taskManager.topologyMetadata().hasNamedTopologies());
:state.computeTaskLags(uuid,allTaskEndOffsetSums);
:clientStates.put(uuid,state);
:return fetchEndOffsetsSuccessful;;
stop
end
start
note right
Map<TaskId,Long>computeEndOffsetSumsByTask(final Map<TopicPartition,
ListOffsetsResultInfo>endOffsets,final Map<TopicPartition,
Long>sourceChangelogEndOffsets,final ChangelogTopicschangelogTopics)
end note
:taskEndOffsetSums.put(taskId,0L);
if (sourceChangelogEndOffsets.containsKey(changelogPartition)) then (yes) 
:changelogPartitionEndOffset=sourceChangelogEndOffsets.get(changelogPartition);
else (no)
:log.debug("Fetched offsets did not contain the changelog {} of task {}",
changelogPartition,taskId);
:throw new IllegalStateException("Could not get end offset for "+
changelogPartition);;
endif 
if (newEndOffsetSum<0) then (yes) 
:taskEndOffsetSums.put(taskId,Long.MAX_VALUE);
else (no)
:taskEndOffsetSums.put(taskId,newEndOffsetSum);
endif 
:return taskEndOffsetSums;;
stop
end
start
note right
void populatePartitionsByHostMaps(final Map<HostInfo,
Set<TopicPartition>>partitionsByHost,final Map<HostInfo,
Set<TopicPartition>>standbyPartitionsByHost,
final Map<TaskId,Set<TopicPartition>>partitionsForTask,
final Map<UUID,ClientMetadata>clientMetadataMap)
end note
if (hostInfo!=null ) then (yes) 
:topicPartitions.addAll(partitionsForTask.get(id));
:standbyPartitions.addAll(partitionsForTask.get(id));
:partitionsByHost.put(hostInfo,topicPartitions);
:standbyPartitionsByHost.put(hostInfo,standbyPartitions);
endif 
end
start
note right
Map<String,Assignment>computeNewAssignment(final Set<TaskId>statefulTasks,
final Map<UUID,ClientMetadata>clientsMetadata,
final Map<TaskId,Set<TopicPartition>>partitionsForTask,
final Map<HostInfo,Set<TopicPartition>>partitionsByHostState,
final Map<HostInfo,Set<TopicPartition>>standbyPartitionsByHost,
final Set<TopicPartition>allOwnedPartitions,
final intminUserMetadataVersion,final intminSupportedMetadataVersion,
final booleanversionProbing,final booleanshouldTriggerProbingRebalance)
end note
:activeTaskAssignment.get(threadEntry.getKey())
.addAll(threadEntry.getValue());
if (tasksRevoked||encodeNextProbingRebalanceTime) then (yes) 
:rebalanceRequired=true;
:log.debug("Requested client {} to schedule a followup rebalance",
clientId);
endif 
:log.info("Client {} per-consumer assignment:\n"+
"\tprev owned active {}\n"+"\tprev owned standby {}\n"+
"\tassigned active {}\n"+"\trevoking active {}\n"+
"\tassigned standby {}\n",clientId,clientMetadata.state.prevOwnedActiveTasksByConsumer(),
clientMetadata.state.prevOwnedStandbyByConsumer(),
clientMetadata.state.assignedActiveTasksByConsumer(),
clientMetadata.state.revokingActiveTasksByConsumer(),
clientMetadata.state.assignedStandbyTasksByConsumer());
if (rebalanceRequired) then (yes) 
:assignmentListener.onAssignmentComplete(false);
:log.info("Finished unstable assignment of tasks,
 a followup rebalance will be scheduled.");
else (no)
:assignmentListener.onAssignmentComplete(true);
:log.info("Finished stable assignment of tasks,
 no followup rebalances required.");
endif 
:return assignment;;
stop
end
start
note right
booleanaddClientAssignments(final Set<TaskId>statefulTasks,
final Map<String,Assignment>assignment,final ClientMetadataclientMetadata,
final Map<TaskId,Set<TopicPartition>>partitionsForTask,
final Map<HostInfo,Set<TopicPartition>>partitionsByHostState,
final Map<HostInfo,Set<TopicPartition>>standbyPartitionsByHost,
final Set<TopicPartition>allOwnedPartitions,
final Map<String,List<TaskId>>activeTaskAssignments,
final Map<String,List<TaskId>>standbyTaskAssignments,
final intminUserMetadataVersion,final intminSupportedMetadataVersion,
final booleanprobingRebalanceNeeded)
end note
if (!activeTasksRemovedPendingRevokation.isEmpty()) then (yes) 
:log.info("Requesting followup rebalance be scheduled immediately by {} due to tasks changing ownership.",
consumer);
:info.setNextRebalanceTime(0L);
:followupRebalanceRequiredForRevokedTasks=true;
:shouldEncodeProbingRebalance=false;
endif 
:assignment.put(consumer,new Assignment(activePartitionsList,
info.encode()));
:return followupRebalanceRequiredForRevokedTasks;;
stop
end
start
note right
Set<TaskId>populateActiveTaskAndPartitionsLists(final List<TopicPartition>activePartitionsList,
final List<TaskId>assignedActiveList,final Stringconsumer,
final ClientStateclientState,final List<TaskId>activeTasksForConsumer,
final Map<TaskId,Set<TopicPartition>>partitionsForTask,
final Set<TopicPartition>allOwnedPartitions)
end note
:clientState.assignActiveToConsumer(taskId,
consumer);
if (newPartitionForConsumer&&allOwnedPartitions.contains(partition)) then (yes) 
:log.info("Removing task {} from {} active assignment until it is safely revoked in followup rebalance",
taskId,consumer);
:removedActiveTasks.add(taskId);
:clientState.revokeActiveFromConsumer(taskId,
consumer);
:assignedPartitionsForTask.clear();
:clientState.unassignActive(taskId);
else (no)
:assignedPartitionsForTask.add(new AssignedPartition(taskId,
partition));
endif 
:assignedPartitions.addAll(assignedPartitionsForTask);
:Collections.sort(assignedPartitions);
:assignedActiveList.add(partition.taskId);
:activePartitionsList.add(partition.partition);
:return removedActiveTasks;;
stop
end
start
note right
Map<TaskId,Set<TopicPartition>>buildStandbyTaskMap(final Stringconsumer,
final Iterable<TaskId>standbyTasks,final Iterable<TaskId>revokedTasks,
final Set<TaskId>allStatefulTasks,final Map<TaskId,
Set<TopicPartition>>partitionsForTask,final ClientStateclientState)
end note
:clientState.assignStandbyToConsumer(task,
consumer);
:standbyTaskMap.put(task,partitionsForTask.get(task));
if (allStatefulTasks.contains(task)) then (yes) 
:log.info("Adding removed stateful active task {} as a standby for {} before it is revoked in followup rebalance",
task,consumer);
:clientState.assignStandbyToConsumer(task,
consumer);
:clientState.assignStandby(task);
:standbyTaskMap.put(task,partitionsForTask.get(task));
endif 
:return standbyTaskMap;;
stop
end
start
note right
Map<String,List<TaskId>>assignTasksToThreads(final Collection<TaskId>tasksToAssign,
final booleanisStateful,final SortedSet<String>consumers,
final ClientStatestate,final Map<String,Integer>threadLoad)
end note
:assignment.put(consumer,new ArrayList<>());
if (!unassignedTasks.isEmpty()) then (yes) 
if (isStateful) then (yes) 
if (unassignedTasks.contains(task)) then (yes) 
if (threadAssignment.size()<tasksTargetCount) then (yes) 
:threadAssignment.add(task);
:unassignedTasks.remove(task);
else (no)
:unassignedTaskToPreviousOwner.put(task,consumer);
endif 
endif 
endif 
if (threadAssignment.size()<tasksTargetCount) then (yes) 
:consumersToFill.offer(consumer);
endif 
while (!consumersToFill.isEmpty())
if (task!=null ) then (yes) 
:threadAssignment.add(task);
if (threadTaskCount<minTasksPerThread) then (yes) 
:consumersToFill.offer(consumer);
endif 
else (no)
:throw new TaskAssignmentException("Ran out of unassigned stateful tasks but some members were not at capacity");;
endif 
endwhile
if (!unassignedTasks.isEmpty()) then (yes) 
if (taskCount==minTasksPerThread) then (yes) 
:consumersToFill.add(consumer);
endif 
if (consumersToFill.contains(consumer)&&unassignedTasks.contains(task)) then (yes) 
:assignment.get(consumer).add(task);
:unassignedTasks.remove(task);
:consumersToFill.remove(consumer);
endif 
:threadAssignment.add(task);
endif 
endif 
:threadLoad.put(consumer,totalCount);
:return assignment;;
stop
end
start
note right
void validateMetadataVersions(final intreceivedAssignmentMetadataVersion,
final intlatestCommonlySupportedVersion)
end note
if (receivedAssignmentMetadataVersion>usedSubscriptionMetadataVersion) then (yes) 
:log.error("Leader sent back an assignment with version {} which was greater than our used version {}",
receivedAssignmentMetadataVersion,usedSubscriptionMetadataVersion);
:throw new TaskAssignmentException("Sent a version "+
usedSubscriptionMetadataVersion+" subscription but got an assignment with higher version "+
receivedAssignmentMetadataVersion+".");;
endif 
if (latestCommonlySupportedVersion>LATEST_SUPPORTED_VERSION) then (yes) 
:log.error("Leader sent back assignment with commonly supported version {} that is greater than our "+
"actual latest supported version {}",latestCommonlySupportedVersion,
LATEST_SUPPORTED_VERSION);
:throw new TaskAssignmentException("Can't upgrade to metadata version greater than we support");;
endif 
end
start
note right
booleanmaybeUpdateSubscriptionVersion(final intreceivedAssignmentMetadataVersion,
final intlatestCommonlySupportedVersion)
end note
if (receivedAssignmentMetadataVersion>=EARLIEST_PROBEABLE_VERSION) then (yes) 
if (latestCommonlySupportedVersion>usedSubscriptionMetadataVersion) then (yes) 
:log.info("Sent a version {} subscription and group's latest commonly supported version is {} (successful "+
"version probing and end of rolling upgrade). Upgrading subscription metadata version to "+
"{} for next rebalance.",usedSubscriptionMetadataVersion,
latestCommonlySupportedVersion,latestCommonlySupportedVersion);
:usedSubscriptionMetadataVersion=latestCommonlySupportedVersion;
:return true;;
stop
endif 
if (receivedAssignmentMetadataVersion<usedSubscriptionMetadataVersion) then (yes) 
:log.info("Sent a version {} subscription and got version {} assignment back (successful version probing). "+
"Downgrade subscription metadata to commonly supported version {} and trigger new rebalance.",
usedSubscriptionMetadataVersion,receivedAssignmentMetadataVersion,
latestCommonlySupportedVersion);
:usedSubscriptionMetadataVersion=latestCommonlySupportedVersion;
:return true;;
stop
endif 
else (no)
:log.debug("Received an assignment version {} that is less than the earliest version that allows version "+
"probing {}. If this is not during a rolling upgrade from version 2.0 or below,
 this is an error.",receivedAssignmentMetadataVersion,
EARLIEST_PROBEABLE_VERSION);
endif 
:return false;;
stop
end
start
note right
void onAssignment(final Assignmentassignment,
final ConsumerGroupMetadatametadata)
end note
:partitions.sort(PARTITION_COMPARATOR);
if (info.errCode()!=AssignorError.NONE.code()) then (yes) 
:assignmentErrorCode.set(info.errCode());
:return ;;
stop
endif 
:validateMetadataVersions(receivedAssignmentMetadataVersion,
latestCommonlySupportedVersion);
:validateActiveTaskEncoding(partitions,info,
logPrefix);
:activeTasks=getActiveTasks(partitions,info);
:partitionsByHost=Collections.emptyMap();
:standbyPartitionsByHost=Collections.emptyMap();
:topicToPartitionInfo=Collections.emptyMap();
:encodedNextScheduledRebalanceMs=Long.MAX_VALUE;
:validateActiveTaskEncoding(partitions,info,
logPrefix);
:activeTasks=getActiveTasks(partitions,info);
:partitionsByHost=info.partitionsByHost();
:standbyPartitionsByHost=Collections.emptyMap();
:topicToPartitionInfo=getTopicPartitionInfo(partitionsByHost);
:encodedNextScheduledRebalanceMs=Long.MAX_VALUE;
:validateActiveTaskEncoding(partitions,info,
logPrefix);
:activeTasks=getActiveTasks(partitions,info);
:partitionsByHost=info.partitionsByHost();
:standbyPartitionsByHost=info.standbyPartitionByHost();
:topicToPartitionInfo=getTopicPartitionInfo(partitionsByHost);
:encodedNextScheduledRebalanceMs=Long.MAX_VALUE;
:validateActiveTaskEncoding(partitions,info,
logPrefix);
:activeTasks=getActiveTasks(partitions,info);
:partitionsByHost=info.partitionsByHost();
:standbyPartitionsByHost=info.standbyPartitionByHost();
:topicToPartitionInfo=getTopicPartitionInfo(partitionsByHost);
:encodedNextScheduledRebalanceMs=info.nextRebalanceMs();
:throw new IllegalStateException("This code should never be reached."+
" Please file a bug report at https://issues.apache.org/jira/projects/KAFKA/");;
:maybeScheduleFollowupRebalance(encodedNextScheduledRebalanceMs,
receivedAssignmentMetadataVersion,latestCommonlySupportedVersion,
partitionsByHost.keySet());
:streamsMetadataState.onChange(partitionsByHost,
standbyPartitionsByHost,fakeCluster);
:taskManager.handleAssignment(activeTasks,
info.standbyTasks());
end
start
note right
void maybeScheduleFollowupRebalance(final longencodedNextScheduledRebalanceMs,
final intreceivedAssignmentMetadataVersion,
final intlatestCommonlySupportedVersion,final Set<HostInfo>groupHostInfo)
end note
if (maybeUpdateSubscriptionVersion(receivedAssignmentMetadataVersion,
latestCommonlySupportedVersion)) then (yes) 
:log.info("Requested to schedule immediate rebalance due to version probing.");
:nextScheduledRebalanceMs.set(0L);
else (no)
:log.info("No followup rebalance was requested,
 resetting the rebalance schedule.");
:nextScheduledRebalanceMs.set(Long.MAX_VALUE);
endif 
end
start
note right
booleanverifyHostInfo(final Set<HostInfo>groupHostInfo)
end note
if (userEndPoint!=null &&!groupHostInfo.isEmpty()) then (yes) 
:return groupHostInfo.contains(myHostInfo);;
stop
else (no)
:return true;;
stop
endif 
end
start
note right
Map<TaskId,Set<TopicPartition>>getActiveTasks(final List<TopicPartition>partitions,
final AssignmentInfoinfo)
end note
:i++;
:activeTasks.computeIfAbsent(id,k1->new HashSet<>())
.add(partition);
:return activeTasks;;
stop
end
start
note right
Map<TopicPartition,PartitionInfo>getTopicPartitionInfo(final Map<HostInfo,
Set<TopicPartition>>partitionsByHost)
end note
:topicToPartitionInfo.put(topicPartition,
new PartitionInfo(topicPartition.topic(),
topicPartition.partition(),null ,new Node[0],
new Node[0]));
:return topicToPartitionInfo;;
stop
end
start
note right
void validateActiveTaskEncoding(final List<TopicPartition>partitions,
final AssignmentInfoinfo,final StringlogPrefix)
end note
if (partitions.size()!=info.activeTasks()
.size()) then (yes) 
:throw new TaskAssignmentException(String.format("%sNumber of assigned partitions %d is not equal to "+
"the number of active taskIds %d, assignmentInfo=%s",
logPrefix,partitions.size(),info.activeTasks().size(),
info.toString()));;
endif 
end
start
note right
intupdateMinReceivedVersion(final intusedVersion,
final intminReceivedMetadataVersion)
end note
:return Math.min(usedVersion,minReceivedMetadataVersion);;
stop
end
start
note right
intupdateMinSupportedVersion(final intsupportedVersion,
final intminSupportedMetadataVersion)
end note
if (supportedVersion<minSupportedMetadataVersion) then (yes) 
:log.debug("Downgrade the current minimum supported version {} to the smaller seen supported version {}",
minSupportedMetadataVersion,supportedVersion);
:return supportedVersion;;
stop
else (no)
:log.debug("Current minimum supported version remains at {},
 last seen supported version was {}",minSupportedMetadataVersion,
supportedVersion);
:return minSupportedMetadataVersion;;
stop
endif 
end
start
note right
void setInternalTopicManager(final InternalTopicManagerinternalTopicManager)
end note
:this.internalTopicManager=internalTopicManager;
end
start
note right
RebalanceProtocolrebalanceProtocol()
end note
:return rebalanceProtocol;;
stop
end
start
note right
StringuserEndPoint()
end note
:return userEndPoint;;
stop
end
start
note right
TaskManagertaskManager()
end note
:return taskManager;;
stop
end
start
note right
byteuniqueField()
end note
:return uniqueField;;
stop
end
start
note right
Map<String,String>clientTags()
end note
:return clientTags;;
stop
end
start
note right
void handleRebalanceStart(final Set<String>topics)
end note
:taskManager.handleRebalanceStart(topics);
end
start
note right
longacceptableRecoveryLag()
end note
:return assignmentConfigs.acceptableRecoveryLag;;
stop
end
start
note right
intmaxWarmupReplicas()
end note
:return assignmentConfigs.maxWarmupReplicas;;
stop
end
start
note right
intnumStandbyReplicas()
end note
:return assignmentConfigs.numStandbyReplicas;;
stop
end
start
note right
longprobingRebalanceIntervalMs()
end note
:return assignmentConfigs.probingRebalanceIntervalMs;;
stop
end
@enduml

@startuml
!pragma layout smetana
start
note right
constructor AssignedPartition(final TaskIdtaskId,
final TopicPartitionpartition)
end note
:this.taskId=taskId;
:this.partition=partition;
end
start
note right
intcompareTo(final AssignedPartitionthat)
end note
:return PARTITION_COMPARATOR.compare(partition,
that.partition);;
stop
end
start
note right
booleanequals(final Objecto)
end note
if (!(o instanceof AssignedPartition)) then (yes) 
:return false;;
stop
endif 
:return compareTo(other)==0;;
stop
end
start
note right
inthashCode()
end note
:return partition.hashCode();;
stop
end
start
note right
constructor ClientMetadata(final StringendPoint,
final Map<String,String>clientTags)
end note
:hostInfo=HostInfo.buildFromEndpoint(endPoint);
:consumers=new TreeSet<>();
:state=new ClientState(clientTags);
end
start
note right
void addConsumer(final StringconsumerMemberId,
final List<TopicPartition>ownedPartitions)
end note
:consumers.add(consumerMemberId);
:state.incrementCapacity();
:state.addOwnedPartitions(ownedPartitions,
consumerMemberId);
end
start
note right
void addPreviousTasksAndOffsetSums(final StringconsumerId,
final Map<TaskId,Long>taskOffsetSums)
end note
:state.addPreviousTasksAndOffsetSums(consumerId,
taskOffsetSums);
end
start
note right
StringtoString()
end note
:return "ClientMetadata{"+"hostInfo="+hostInfo+
", consumers="+consumers+", state="+state+
stop
end
start
note right
void configure(final Map<String,? >configs)
end note
:logPrefix=assignorConfiguration.logPrefix();
:log=new LogContext(logPrefix).logger(getClass());
:usedSubscriptionMetadataVersion=assignorConfiguration.configuredMetadataVersion(usedSubscriptionMetadataVersion);
:mainConsumerSupplier=()->Objects.requireNonNull(referenceContainer.mainConsumer,
"Main consumer was not specified");
:adminClient=Objects.requireNonNull(referenceContainer.adminClient,
"Admin client was not specified");
:taskManager=Objects.requireNonNull(referenceContainer.taskManager,
"TaskManager was not specified");
:streamsMetadataState=Objects.requireNonNull(referenceContainer.streamsMetadataState,
"StreamsMetadataState was not specified");
:assignmentErrorCode=referenceContainer.assignmentErrorCode;
:nextScheduledRebalanceMs=referenceContainer.nextScheduledRebalanceMs;
:nonFatalExceptionsToHandle=referenceContainer.nonFatalExceptionsToHandle;
:time=Objects.requireNonNull(referenceContainer.time,
"Time was not specified");
:assignmentConfigs=assignorConfiguration.assignmentConfigs();
:partitionGrouper=new PartitionGrouper();
:userEndPoint=assignorConfiguration.userEndPoint();
:internalTopicManager=assignorConfiguration.internalTopicManager();
:copartitionedTopicsEnforcer=assignorConfiguration.copartitionedTopicsEnforcer();
:rebalanceProtocol=assignorConfiguration.rebalanceProtocol();
:taskAssignorSupplier=assignorConfiguration::taskAssignor;
:assignmentListener=assignorConfiguration.assignmentListener();
:uniqueField=0;
:clientTags=referenceContainer.clientTags;
end
start
note right
Stringname()
end note
:return "stream";;
stop
end
start
note right
List<RebalanceProtocol>supportedProtocols()
end note
:supportedProtocols.add(RebalanceProtocol.EAGER);
if (rebalanceProtocol==RebalanceProtocol.COOPERATIVE) then (yes) 
:supportedProtocols.add(rebalanceProtocol);
endif 
:return supportedProtocols;;
stop
end
start
note right
ByteBuffersubscriptionUserData(final Set<String>topics)
end note
:handleRebalanceStart(topics);
:uniqueField++;
:return new SubscriptionInfo(usedSubscriptionMetadataVersion,
LATEST_SUPPORTED_VERSION,taskManager.processId(),
userEndPoint,taskOffsetSums,uniqueField,assignmentErrorCode.get(),
clientTags).encode();;
stop
end
start
note right
Map<String,Assignment>errorAssignment(final Map<UUID,
ClientMetadata>clientsMetadata,final interrorCode)
end note
:assignment.put(consumerId,new Assignment(Collections.emptyList(),
new AssignmentInfo(LATEST_SUPPORTED_VERSION,
Collections.emptyList(),Collections.emptyMap(),
Collections.emptyMap(),Collections.emptyMap(),
errorCode).encode()));
:return assignment;;
stop
end
start
note right
GroupAssignmentassign(final Clustermetadata,
final GroupSubscriptiongroupSubscription)
end note
if (info.errorCode()==AssignorError.SHUTDOWN_REQUESTED.code()) then (yes) 
:shutdownRequested=true;
endif 
:minReceivedMetadataVersion=updateMinReceivedVersion(usedVersion,
minReceivedMetadataVersion);
:minSupportedMetadataVersion=updateMinSupportedVersion(info.latestSupportedVersion(),
minSupportedMetadataVersion);
if (usedVersion>LATEST_SUPPORTED_VERSION) then (yes) 
:futureMetadataVersion=usedVersion;
:processId=FUTURE_ID;
if (!clientMetadataMap.containsKey(FUTURE_ID)) then (yes) 
:clientMetadataMap.put(FUTURE_ID,new ClientMetadata(null ,
Collections.emptyMap()));
endif 
else (no)
:processId=info.processId();
endif 
if (clientMetadata==null ) then (yes) 
:clientMetadata=new ClientMetadata(info.userEndPoint(),
info.clientTags());
:clientMetadataMap.put(info.processId(),clientMetadata);
endif 
:clientMetadata.addConsumer(consumerId,subscription.ownedPartitions());
:allOwnedPartitions.addAll(subscription.ownedPartitions());
if (allOwnedPartitions.size()<prevSize+subscription.ownedPartitions()
.size()) then (yes) 
:assignmentErrorFound=true;
endif 
:clientMetadata.addPreviousTasksAndOffsetSums(consumerId,
info.taskOffsetSums());
if (assignmentErrorFound) then (yes) 
:log.warn("The previous assignment contains a partition more than once. "+
"\t Mapping: {}",subscriptions);
endif 
:log.debug("Constructed client metadata {} from the member subscriptions.",
clientMetadataMap);
if (shutdownRequested) then (yes) 
:return new GroupAssignment(errorAssignment(clientMetadataMap,
AssignorError.SHUTDOWN_REQUESTED.code()));;
stop
endif 
:log.debug("Created repartition topics {} from the parsed topology.",
allRepartitionTopicPartitions.values());
:allSourceTopics.addAll(entry.getValue().sourceTopics);
:sourceTopicsByGroup.put(entry.getKey(),entry.getValue()
.sourceTopics);
if (minReceivedMetadataVersion>=2) then (yes) 
:populatePartitionsByHostMaps(partitionsByHost,
standbyPartitionsByHost,partitionsForTask,
clientMetadataMap);
endif 
:streamsMetadataState.onChange(partitionsByHost,
standbyPartitionsByHost,fullMetadata);
:return new GroupAssignment(assignment);;
stop
:log.error("Caught an error in the task assignment. Returning an error assignment.",e);
:return new GroupAssignment(errorAssignment(clientMetadataMap,
AssignorError.INCOMPLETE_SOURCE_TOPIC_METADATA.code()));;
stop
:log.error("Caught an error in the task assignment. Returning an error assignment.",e);
:return new GroupAssignment(errorAssignment(clientMetadataMap,
AssignorError.ASSIGNMENT_ERROR.code()));;
stop
end
start
note right
booleancheckMetadataVersions(final intminReceivedMetadataVersion,
final intminSupportedMetadataVersion,final intfutureMetadataVersion)
end note
if (futureMetadataVersion==UNKNOWN) then (yes) 
:versionProbing=false;
else (no)
:throw new TaskAssignmentException("Received a future (version probing) subscription (version: "+
futureMetadataVersion+") and an incompatible pre Kafka 2.0 subscription (version: "+
minReceivedMetadataVersion+") at the same time.");;
endif 
if (minReceivedMetadataVersion<LATEST_SUPPORTED_VERSION) then (yes) 
:log.info("Downgrade metadata to version {}. Latest supported version is {}.",
minReceivedMetadataVersion,LATEST_SUPPORTED_VERSION);
endif 
if (minSupportedMetadataVersion<LATEST_SUPPORTED_VERSION) then (yes) 
:log.info("Downgrade latest supported metadata to version {}. Latest supported version is {}.",
minSupportedMetadataVersion,LATEST_SUPPORTED_VERSION);
endif 
:return versionProbing;;
stop
end
start
note right
RepartitionTopicsprepareRepartitionTopics(final Clustermetadata)
end note
:repartitionTopics.setup();
if (isMissingInputTopics) then (yes) 
if (!taskManager.topologyMetadata().hasNamedTopologies()) then (yes) 
:throw new MissingSourceTopicException("Missing source topics.");;
else (no)
:nonFatalExceptionsToHandle.addAll(repartitionTopics.missingSourceTopicExceptions());
endif 
endif 
:return repartitionTopics;;
stop
end
start
note right
void populateTasksForMaps(final Map<TopicPartition,
TaskId>taskForPartition,final Map<Subtopology,
Set<TaskId>>tasksForTopicGroup,final Set<String>allSourceTopics,
final Map<TaskId,Set<TopicPartition>>partitionsForTask,
final ClusterfullMetadata)
end note
:taskForPartition.put(partition,id);
if (allAssignedPartitions.contains(partition)) then (yes) 
:log.warn("Partition {} is assigned to more than one tasks: {}",
partition,partitionsForTask);
endif 
:allAssignedPartitions.addAll(partitions);
:tasksForTopicGroup.computeIfAbsent(new Subtopology(id.subtopology(),
id.topologyName()),k->new HashSet<>()).add(id);
:checkAllPartitions(allSourceTopics,partitionsForTask,
allAssignedPartitions,fullMetadata);
end
start
note right
void checkAllPartitions(final Set<String>allSourceTopics,
final Map<TaskId,Set<TopicPartition>>partitionsForTask,
final Set<TopicPartition>allAssignedPartitions,
final ClusterfullMetadata)
end note
if (partitionInfoList.isEmpty()) then (yes) 
:log.warn("No partitions found for topic {}",
topic);
else (no)
if (!allAssignedPartitions.contains(partition)) then (yes) 
:log.warn("Partition {} is not assigned to any tasks: {}"+
" Possible causes of a partition not getting assigned"+
" is that another topic defined in the topology has not been"+
" created when starting your streams application,"+
" resulting in no tasks created for this topology at all.",
partition,partitionsForTask);
endif 
endif 
end
start
note right
booleanassignTasksToClients(final ClusterfullMetadata,
final Set<String>allSourceTopics,final Map<Subtopology,
TopicsInfo>topicGroups,final Map<UUID,ClientMetadata>clientMetadataMap,
final Map<TaskId,Set<TopicPartition>>partitionsForTask,
final Set<TaskId>statefulTasks)
end note
if (!statefulTasks.isEmpty()) then (yes) 
:throw new TaskAssignmentException("The stateful tasks should not be populated before assigning tasks to clients");;
endif 
:populateTasksForMaps(taskForPartition,tasksForTopicGroup,
allSourceTopics,partitionsForTask,fullMetadata);
:changelogTopics.setup();
:log.info("All members participating in this rebalance: \n{}.",
clientStates.entrySet().stream().map(entry->entry.getKey()+
": "+entry.getValue().consumers()).collect(Collectors.joining(Utils.NL)));
:statefulTasks.addAll(changelogTopics.statefulTaskIds());
:log.debug("Assigning tasks {} including stateful {} to clients {} with number of replicas {}",
allTasks,statefulTasks,clientStates,numStandbyReplicas());
:log.info("Assigned tasks {} including stateful {} to clients as: \n{}.",
allTasks,statefulTasks,clientStates.entrySet().stream().map(entry->entry.getKey()+
"="+entry.getValue().currentAssignment())
.collect(Collectors.joining(Utils.NL)));
:return probingRebalanceNeeded;;
stop
end
start
note right
TaskAssignorcreateTaskAssignor(final booleanlagComputationSuccessful)
end note
if (taskAssignor instanceof StickyTaskAssignor) then (yes) 
:return taskAssignor;;
stop
else (no)
:log.info("Failed to fetch end offsets for changelogs, will return previous assignment to clients and "+
"trigger another rebalance to retry.");
:return new FallbackPriorTaskAssignor();;
stop
endif 
end
start
note right
booleanpopulateClientStatesMap(final Map<UUID,
ClientState>clientStates,final Map<UUID,ClientMetadata>clientMetadataMap,
final Map<TopicPartition,TaskId>taskForPartition,
final ChangelogTopicschangelogTopics)
end note
:allTaskEndOffsetSums=computeEndOffsetSumsByTask(endOffsets,
sourceChangelogEndOffsets,changelogTopics);
:fetchEndOffsetsSuccessful=true;
:allTaskEndOffsetSums=changelogTopics.statefulTaskIds().stream().collect(Collectors.toMap(t->t,
t->UNKNOWN_OFFSET_SUM));
:fetchEndOffsetsSuccessful=false;
:state.initializePrevTasks(taskForPartition,
taskManager.topologyMetadata().hasNamedTopologies());
:state.computeTaskLags(uuid,allTaskEndOffsetSums);
:clientStates.put(uuid,state);
:return fetchEndOffsetsSuccessful;;
stop
end
start
note right
Map<TaskId,Long>computeEndOffsetSumsByTask(final Map<TopicPartition,
ListOffsetsResultInfo>endOffsets,final Map<TopicPartition,
Long>sourceChangelogEndOffsets,final ChangelogTopicschangelogTopics)
end note
:taskEndOffsetSums.put(taskId,0L);
if (sourceChangelogEndOffsets.containsKey(changelogPartition)) then (yes) 
:changelogPartitionEndOffset=sourceChangelogEndOffsets.get(changelogPartition);
else (no)
:log.debug("Fetched offsets did not contain the changelog {} of task {}",
changelogPartition,taskId);
:throw new IllegalStateException("Could not get end offset for "+
changelogPartition);;
endif 
if (newEndOffsetSum<0) then (yes) 
:taskEndOffsetSums.put(taskId,Long.MAX_VALUE);
else (no)
:taskEndOffsetSums.put(taskId,newEndOffsetSum);
endif 
:return taskEndOffsetSums;;
stop
end
start
note right
void populatePartitionsByHostMaps(final Map<HostInfo,
Set<TopicPartition>>partitionsByHost,final Map<HostInfo,
Set<TopicPartition>>standbyPartitionsByHost,
final Map<TaskId,Set<TopicPartition>>partitionsForTask,
final Map<UUID,ClientMetadata>clientMetadataMap)
end note
if (hostInfo!=null ) then (yes) 
:topicPartitions.addAll(partitionsForTask.get(id));
:standbyPartitions.addAll(partitionsForTask.get(id));
:partitionsByHost.put(hostInfo,topicPartitions);
:standbyPartitionsByHost.put(hostInfo,standbyPartitions);
endif 
end
start
note right
Map<String,Assignment>computeNewAssignment(final Set<TaskId>statefulTasks,
final Map<UUID,ClientMetadata>clientsMetadata,
final Map<TaskId,Set<TopicPartition>>partitionsForTask,
final Map<HostInfo,Set<TopicPartition>>partitionsByHostState,
final Map<HostInfo,Set<TopicPartition>>standbyPartitionsByHost,
final Set<TopicPartition>allOwnedPartitions,
final intminUserMetadataVersion,final intminSupportedMetadataVersion,
final booleanversionProbing,final booleanshouldTriggerProbingRebalance)
end note
:activeTaskAssignment.get(threadEntry.getKey())
.addAll(threadEntry.getValue());
if (tasksRevoked||encodeNextProbingRebalanceTime) then (yes) 
:rebalanceRequired=true;
:log.debug("Requested client {} to schedule a followup rebalance",
clientId);
endif 
:log.info("Client {} per-consumer assignment:\n"+
"\tprev owned active {}\n"+"\tprev owned standby {}\n"+
"\tassigned active {}\n"+"\trevoking active {}\n"+
"\tassigned standby {}\n",clientId,clientMetadata.state.prevOwnedActiveTasksByConsumer(),
clientMetadata.state.prevOwnedStandbyByConsumer(),
clientMetadata.state.assignedActiveTasksByConsumer(),
clientMetadata.state.revokingActiveTasksByConsumer(),
clientMetadata.state.assignedStandbyTasksByConsumer());
if (rebalanceRequired) then (yes) 
:assignmentListener.onAssignmentComplete(false);
:log.info("Finished unstable assignment of tasks,
 a followup rebalance will be scheduled.");
else (no)
:assignmentListener.onAssignmentComplete(true);
:log.info("Finished stable assignment of tasks,
 no followup rebalances required.");
endif 
:return assignment;;
stop
end
start
note right
booleanaddClientAssignments(final Set<TaskId>statefulTasks,
final Map<String,Assignment>assignment,final ClientMetadataclientMetadata,
final Map<TaskId,Set<TopicPartition>>partitionsForTask,
final Map<HostInfo,Set<TopicPartition>>partitionsByHostState,
final Map<HostInfo,Set<TopicPartition>>standbyPartitionsByHost,
final Set<TopicPartition>allOwnedPartitions,
final Map<String,List<TaskId>>activeTaskAssignments,
final Map<String,List<TaskId>>standbyTaskAssignments,
final intminUserMetadataVersion,final intminSupportedMetadataVersion,
final booleanprobingRebalanceNeeded)
end note
if (!activeTasksRemovedPendingRevokation.isEmpty()) then (yes) 
:log.info("Requesting followup rebalance be scheduled immediately by {} due to tasks changing ownership.",
consumer);
:info.setNextRebalanceTime(0L);
:followupRebalanceRequiredForRevokedTasks=true;
:shouldEncodeProbingRebalance=false;
endif 
:assignment.put(consumer,new Assignment(activePartitionsList,
info.encode()));
:return followupRebalanceRequiredForRevokedTasks;;
stop
end
start
note right
Set<TaskId>populateActiveTaskAndPartitionsLists(final List<TopicPartition>activePartitionsList,
final List<TaskId>assignedActiveList,final Stringconsumer,
final ClientStateclientState,final List<TaskId>activeTasksForConsumer,
final Map<TaskId,Set<TopicPartition>>partitionsForTask,
final Set<TopicPartition>allOwnedPartitions)
end note
:clientState.assignActiveToConsumer(taskId,
consumer);
if (newPartitionForConsumer&&allOwnedPartitions.contains(partition)) then (yes) 
:log.info("Removing task {} from {} active assignment until it is safely revoked in followup rebalance",
taskId,consumer);
:removedActiveTasks.add(taskId);
:clientState.revokeActiveFromConsumer(taskId,
consumer);
:assignedPartitionsForTask.clear();
:clientState.unassignActive(taskId);
else (no)
:assignedPartitionsForTask.add(new AssignedPartition(taskId,
partition));
endif 
:assignedPartitions.addAll(assignedPartitionsForTask);
:Collections.sort(assignedPartitions);
:assignedActiveList.add(partition.taskId);
:activePartitionsList.add(partition.partition);
:return removedActiveTasks;;
stop
end
start
note right
Map<TaskId,Set<TopicPartition>>buildStandbyTaskMap(final Stringconsumer,
final Iterable<TaskId>standbyTasks,final Iterable<TaskId>revokedTasks,
final Set<TaskId>allStatefulTasks,final Map<TaskId,
Set<TopicPartition>>partitionsForTask,final ClientStateclientState)
end note
:clientState.assignStandbyToConsumer(task,
consumer);
:standbyTaskMap.put(task,partitionsForTask.get(task));
if (allStatefulTasks.contains(task)) then (yes) 
:log.info("Adding removed stateful active task {} as a standby for {} before it is revoked in followup rebalance",
task,consumer);
:clientState.assignStandbyToConsumer(task,
consumer);
:clientState.assignStandby(task);
:standbyTaskMap.put(task,partitionsForTask.get(task));
endif 
:return standbyTaskMap;;
stop
end
start
note right
Map<String,List<TaskId>>assignTasksToThreads(final Collection<TaskId>tasksToAssign,
final booleanisStateful,final SortedSet<String>consumers,
final ClientStatestate,final Map<String,Integer>threadLoad)
end note
:assignment.put(consumer,new ArrayList<>());
if (!unassignedTasks.isEmpty()) then (yes) 
if (isStateful) then (yes) 
if (unassignedTasks.contains(task)) then (yes) 
if (threadAssignment.size()<tasksTargetCount) then (yes) 
:threadAssignment.add(task);
:unassignedTasks.remove(task);
else (no)
:unassignedTaskToPreviousOwner.put(task,consumer);
endif 
endif 
endif 
if (threadAssignment.size()<tasksTargetCount) then (yes) 
:consumersToFill.offer(consumer);
endif 
while (!consumersToFill.isEmpty())
if (task!=null ) then (yes) 
:threadAssignment.add(task);
if (threadTaskCount<minTasksPerThread) then (yes) 
:consumersToFill.offer(consumer);
endif 
else (no)
:throw new TaskAssignmentException("Ran out of unassigned stateful tasks but some members were not at capacity");;
endif 
endwhile
if (!unassignedTasks.isEmpty()) then (yes) 
if (taskCount==minTasksPerThread) then (yes) 
:consumersToFill.add(consumer);
endif 
if (consumersToFill.contains(consumer)&&unassignedTasks.contains(task)) then (yes) 
:assignment.get(consumer).add(task);
:unassignedTasks.remove(task);
:consumersToFill.remove(consumer);
endif 
:threadAssignment.add(task);
endif 
endif 
:threadLoad.put(consumer,totalCount);
:return assignment;;
stop
end
start
note right
void validateMetadataVersions(final intreceivedAssignmentMetadataVersion,
final intlatestCommonlySupportedVersion)
end note
if (receivedAssignmentMetadataVersion>usedSubscriptionMetadataVersion) then (yes) 
:log.error("Leader sent back an assignment with version {} which was greater than our used version {}",
receivedAssignmentMetadataVersion,usedSubscriptionMetadataVersion);
:throw new TaskAssignmentException("Sent a version "+
usedSubscriptionMetadataVersion+" subscription but got an assignment with higher version "+
receivedAssignmentMetadataVersion+".");;
endif 
if (latestCommonlySupportedVersion>LATEST_SUPPORTED_VERSION) then (yes) 
:log.error("Leader sent back assignment with commonly supported version {} that is greater than our "+
"actual latest supported version {}",latestCommonlySupportedVersion,
LATEST_SUPPORTED_VERSION);
:throw new TaskAssignmentException("Can't upgrade to metadata version greater than we support");;
endif 
end
start
note right
booleanmaybeUpdateSubscriptionVersion(final intreceivedAssignmentMetadataVersion,
final intlatestCommonlySupportedVersion)
end note
if (receivedAssignmentMetadataVersion>=EARLIEST_PROBEABLE_VERSION) then (yes) 
if (latestCommonlySupportedVersion>usedSubscriptionMetadataVersion) then (yes) 
:log.info("Sent a version {} subscription and group's latest commonly supported version is {} (successful "+
"version probing and end of rolling upgrade). Upgrading subscription metadata version to "+
"{} for next rebalance.",usedSubscriptionMetadataVersion,
latestCommonlySupportedVersion,latestCommonlySupportedVersion);
:usedSubscriptionMetadataVersion=latestCommonlySupportedVersion;
:return true;;
stop
endif 
if (receivedAssignmentMetadataVersion<usedSubscriptionMetadataVersion) then (yes) 
:log.info("Sent a version {} subscription and got version {} assignment back (successful version probing). "+
"Downgrade subscription metadata to commonly supported version {} and trigger new rebalance.",
usedSubscriptionMetadataVersion,receivedAssignmentMetadataVersion,
latestCommonlySupportedVersion);
:usedSubscriptionMetadataVersion=latestCommonlySupportedVersion;
:return true;;
stop
endif 
else (no)
:log.debug("Received an assignment version {} that is less than the earliest version that allows version "+
"probing {}. If this is not during a rolling upgrade from version 2.0 or below,
 this is an error.",receivedAssignmentMetadataVersion,
EARLIEST_PROBEABLE_VERSION);
endif 
:return false;;
stop
end
start
note right
void onAssignment(final Assignmentassignment,
final ConsumerGroupMetadatametadata)
end note
:partitions.sort(PARTITION_COMPARATOR);
if (info.errCode()!=AssignorError.NONE.code()) then (yes) 
:assignmentErrorCode.set(info.errCode());
:return ;;
stop
endif 
:validateMetadataVersions(receivedAssignmentMetadataVersion,
latestCommonlySupportedVersion);
:validateActiveTaskEncoding(partitions,info,
logPrefix);
:activeTasks=getActiveTasks(partitions,info);
:partitionsByHost=Collections.emptyMap();
:standbyPartitionsByHost=Collections.emptyMap();
:topicToPartitionInfo=Collections.emptyMap();
:encodedNextScheduledRebalanceMs=Long.MAX_VALUE;
:validateActiveTaskEncoding(partitions,info,
logPrefix);
:activeTasks=getActiveTasks(partitions,info);
:partitionsByHost=info.partitionsByHost();
:standbyPartitionsByHost=Collections.emptyMap();
:topicToPartitionInfo=getTopicPartitionInfo(partitionsByHost);
:encodedNextScheduledRebalanceMs=Long.MAX_VALUE;
:validateActiveTaskEncoding(partitions,info,
logPrefix);
:activeTasks=getActiveTasks(partitions,info);
:partitionsByHost=info.partitionsByHost();
:standbyPartitionsByHost=info.standbyPartitionByHost();
:topicToPartitionInfo=getTopicPartitionInfo(partitionsByHost);
:encodedNextScheduledRebalanceMs=Long.MAX_VALUE;
:validateActiveTaskEncoding(partitions,info,
logPrefix);
:activeTasks=getActiveTasks(partitions,info);
:partitionsByHost=info.partitionsByHost();
:standbyPartitionsByHost=info.standbyPartitionByHost();
:topicToPartitionInfo=getTopicPartitionInfo(partitionsByHost);
:encodedNextScheduledRebalanceMs=info.nextRebalanceMs();
:throw new IllegalStateException("This code should never be reached."+
" Please file a bug report at https://issues.apache.org/jira/projects/KAFKA/");;
:maybeScheduleFollowupRebalance(encodedNextScheduledRebalanceMs,
receivedAssignmentMetadataVersion,latestCommonlySupportedVersion,
partitionsByHost.keySet());
:streamsMetadataState.onChange(partitionsByHost,
standbyPartitionsByHost,fakeCluster);
:taskManager.handleAssignment(activeTasks,
info.standbyTasks());
end
start
note right
void maybeScheduleFollowupRebalance(final longencodedNextScheduledRebalanceMs,
final intreceivedAssignmentMetadataVersion,
final intlatestCommonlySupportedVersion,final Set<HostInfo>groupHostInfo)
end note
if (maybeUpdateSubscriptionVersion(receivedAssignmentMetadataVersion,
latestCommonlySupportedVersion)) then (yes) 
:log.info("Requested to schedule immediate rebalance due to version probing.");
:nextScheduledRebalanceMs.set(0L);
else (no)
:log.info("No followup rebalance was requested,
 resetting the rebalance schedule.");
:nextScheduledRebalanceMs.set(Long.MAX_VALUE);
endif 
end
start
note right
booleanverifyHostInfo(final Set<HostInfo>groupHostInfo)
end note
if (userEndPoint!=null &&!groupHostInfo.isEmpty()) then (yes) 
:return groupHostInfo.contains(myHostInfo);;
stop
else (no)
:return true;;
stop
endif 
end
start
note right
Map<TaskId,Set<TopicPartition>>getActiveTasks(final List<TopicPartition>partitions,
final AssignmentInfoinfo)
end note
:i++;
:activeTasks.computeIfAbsent(id,k1->new HashSet<>())
.add(partition);
:return activeTasks;;
stop
end
start
note right
Map<TopicPartition,PartitionInfo>getTopicPartitionInfo(final Map<HostInfo,
Set<TopicPartition>>partitionsByHost)
end note
:topicToPartitionInfo.put(topicPartition,
new PartitionInfo(topicPartition.topic(),
topicPartition.partition(),null ,new Node[0],
new Node[0]));
:return topicToPartitionInfo;;
stop
end
start
note right
void validateActiveTaskEncoding(final List<TopicPartition>partitions,
final AssignmentInfoinfo,final StringlogPrefix)
end note
if (partitions.size()!=info.activeTasks()
.size()) then (yes) 
:throw new TaskAssignmentException(String.format("%sNumber of assigned partitions %d is not equal to "+
"the number of active taskIds %d, assignmentInfo=%s",
logPrefix,partitions.size(),info.activeTasks().size(),
info.toString()));;
endif 
end
start
note right
intupdateMinReceivedVersion(final intusedVersion,
final intminReceivedMetadataVersion)
end note
:return Math.min(usedVersion,minReceivedMetadataVersion);;
stop
end
start
note right
intupdateMinSupportedVersion(final intsupportedVersion,
final intminSupportedMetadataVersion)
end note
if (supportedVersion<minSupportedMetadataVersion) then (yes) 
:log.debug("Downgrade the current minimum supported version {} to the smaller seen supported version {}",
minSupportedMetadataVersion,supportedVersion);
:return supportedVersion;;
stop
else (no)
:log.debug("Current minimum supported version remains at {},
 last seen supported version was {}",minSupportedMetadataVersion,
supportedVersion);
:return minSupportedMetadataVersion;;
stop
endif 
end
start
note right
void setInternalTopicManager(final InternalTopicManagerinternalTopicManager)
end note
:this.internalTopicManager=internalTopicManager;
end
start
note right
RebalanceProtocolrebalanceProtocol()
end note
:return rebalanceProtocol;;
stop
end
start
note right
StringuserEndPoint()
end note
:return userEndPoint;;
stop
end
start
note right
TaskManagertaskManager()
end note
:return taskManager;;
stop
end
start
note right
byteuniqueField()
end note
:return uniqueField;;
stop
end
start
note right
Map<String,String>clientTags()
end note
:return clientTags;;
stop
end
start
note right
void handleRebalanceStart(final Set<String>topics)
end note
:taskManager.handleRebalanceStart(topics);
end
start
note right
longacceptableRecoveryLag()
end note
:return assignmentConfigs.acceptableRecoveryLag;;
stop
end
start
note right
intmaxWarmupReplicas()
end note
:return assignmentConfigs.maxWarmupReplicas;;
stop
end
start
note right
intnumStandbyReplicas()
end note
:return assignmentConfigs.numStandbyReplicas;;
stop
end
start
note right
longprobingRebalanceIntervalMs()
end note
:return assignmentConfigs.probingRebalanceIntervalMs;;
stop
end
@enduml

PlantUML version 1.2022.7(Mon Aug 22 17:01:30 UTC 2022)
(GPL source distribution)
Java Runtime: OpenJDK Runtime Environment
JVM: OpenJDK 64-Bit Server VM
Default Encoding: UTF-8
Language: en
Country: null
--></g></svg>