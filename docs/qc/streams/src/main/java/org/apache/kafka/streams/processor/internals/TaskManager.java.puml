@startuml
!pragma layout smetana
java.lang.Object <|-- org.apache.kafka.streams.processor.internals.TaskManager
class org.apache.kafka.streams.processor.internals.TaskManager {
+  TaskManager(Time;ChangelogReader;UUID;String;StreamsMetricsImpl;ActiveTaskCreator;StandbyTaskCreator;TopologyMetadata;Admin;StateDirectory)
+  setMainConsumer(Consumer<byte[],byte[]>)
+  totalProducerBlockedTime()
+  processId()
+  topologyMetadata()
+  isRebalanceInProgress()
+  handleRebalanceStart(Set<String>)
+  handleRebalanceComplete()
+  handleCorruption(Set<TaskId>)
+  closeDirtyAndRevive(Collection<Task>;boolean)
+  handleAssignment(Map<TaskId,Set<TopicPartition>>;Map<TaskId,Set<TopicPartition>>)
+  handleCloseAndRecycle(Set<Task>;Set<Task>;Set<Task>;Map<TaskId,Set<TopicPartition>>;Map<TaskId,Set<TopicPartition>>;LinkedHashMap<TaskId,RuntimeException>)
+  tryToCompleteRestoration(long;java.util.function.Consumer<Set<TopicPartition>>)
+  handleRevocation(Collection<TopicPartition>)
+  prepareCommitAndAddOffsetsToMap(Set<Task>;Map<Task,Map<TopicPartition,OffsetAndMetadata>>)
+  handleLostAll()
+  getTaskOffsetSums()
+  tryToLockAllNonEmptyTaskDirectories()
+  releaseLockedDirectoriesForTasks(Set<TaskId>)
+  releaseLockedUnassignedTaskDirectories()
+  sumOfChangelogOffsets(TaskId;Map<TopicPartition,Long>)
+  closeTaskDirty(Task)
+  completeTaskCloseClean(Task)
+  shutdown(boolean)
+  closeAndCleanUpTasks(Collection<Task>;Collection<Task>;boolean)
+  tryCloseCleanActiveTasks(Collection<Task>;boolean;AtomicReference<RuntimeException>)
+  tryCloseCleanStandbyTasks(Collection<Task>;boolean;AtomicReference<RuntimeException>)
+  activeTaskIds()
+  standbyTaskIds()
+  tasks()
+  notPausedTasks()
+  activeTaskMap()
+  activeTaskIterable()
+  activeTaskStream()
+  standbyTaskMap()
+  standbyTaskIterable()
+  standbyTaskStream()
+  commitAll()
+  addRecordsToTasks(ConsumerRecords<byte[],byte[]>)
+  commit(Collection<Task>)
+  maybeCommitActiveTasksPerUserRequested()
+  commitTasksAndMaybeUpdateCommittableOffsets(Collection<Task>;Map<Task,Map<TopicPartition,OffsetAndMetadata>>)
+  updateTaskEndMetadata(TopicPartition;Long)
+  handleTopologyUpdates()
+  maybeCloseTasksFromRemovedTopologies(Set<String>)
+  process(int;Time)
+  recordTaskProcessRatio(long;long)
+  punctuate()
+  maybePurgeCommittedRecords()
+  toString()
+  toString(String)
+  producerMetrics()
+  producerClientIds()
+  lockedTaskDirectories()
+  maybeWrapAndSetFirstException(AtomicReference<RuntimeException>;RuntimeException;TaskId)
+  executeAndMaybeSwallow(boolean;Runnable;java.util.function.Consumer<RuntimeException>;java.util.function.Consumer<RuntimeException>)
+  executeAndMaybeSwallow(boolean;Runnable;String;Logger)
+  needsInitializationOrRestoration()
+  addTask(Task)
} 
@enduml
