<?xml version="1.0" encoding="UTF-8" standalone="no"?><svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" contentStyleType="text/css" height="544px" preserveAspectRatio="none" style="width:936px;height:544px;background:#000000;" version="1.1" viewBox="0 0 936 544" width="936px" zoomAndPan="magnify"><defs/><g><rect fill="#1D201C" height="1" style="stroke:#1D201C;stroke-width:1.0;" width="1" x="0" y="0"/><text fill="#33FF02" font-family="sans-serif" font-size="12" font-style="italic" font-weight="bold" lengthAdjust="spacing" textLength="127" x="5" y="17">PlantUML 1.2022.7</text><rect fill="#33FF02" height="21.2969" style="stroke:#33FF02;stroke-width:1.0;" width="924" x="5" y="26.9688"/><text fill="#000000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="188" x="6" y="41.9688">[From string (line 708) ]</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="5" x="5" y="62.2656"> </text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="79" x="5" y="78.5625">@startuml</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="190" x="5" y="94.8594">!pragma layout smetana</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="111.1563">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="127.4531">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="645" x="5" y="143.75">constructor TaskManager(final Timetime,final ChangelogReaderchangelogReader,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="15" x="5" y="160.0469">...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="185" x="5" y="176.3438">... ( skipping 683 lines )</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="15" x="5" y="192.6406">...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="40" x="5" y="208.9375">endif</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="30" x="5" y="225.2344">end</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="241.5313">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="257.8281">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="241" x="5" y="274.125">void handleTopologyUpdates()</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="71" x="5" y="290.4219">end note</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="924" x="5" y="306.7188">:topologyMetadata.executeTopologyUpdatesAndBumpThreadVersion(tasks::maybeCreateTasksFromNewTopologies,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="395" x="5" y="323.0156">this::maybeCloseTasksFromRemovedTopologies);</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="332" x="5" y="339.3125">if (topologyMetadata.isEmpty()) then (yes)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="602" x="5" y="355.6094">:log.info("Proactively unsubscribing from all topics due to empty topology");</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="245" x="5" y="371.9063">:mainConsumer.unsubscribe();</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="40" x="5" y="388.2031">endif</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="476" x="5" y="404.5">:topologyMetadata.maybeNotifyTopologyVersionListeners();</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="30" x="5" y="420.7969">end</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="437.0938">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="453.3906">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="727" x="5" y="469.6875">void maybeCloseTasksFromRemovedTopologies(final Set&lt;String&gt;currentNamedTopologies)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="71" x="5" y="485.9844">end note</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="369" x="5" y="502.2813">if (!currentNamedTopologies.contains(task.id()</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" text-decoration="wavy underline" textLength="228" x="5" y="518.5781">.topologyName())) then (yes)</text><text fill="#FF0000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="106" x="10" y="534.875">Syntax Error?</text><!--MD5=[ff8b3906bc51973ffa2d141747908235]
@startuml
!pragma layout smetana
start
note right
constructor TaskManager(final Timetime,final ChangelogReaderchangelogReader,
final UUIDprocessId,final StringlogPrefix,
final StreamsMetricsImplstreamsMetrics,final ActiveTaskCreatoractiveTaskCreator,
final StandbyTaskCreatorstandbyTaskCreator,
final TopologyMetadatatopologyMetadata,final AdminadminClient,
final StateDirectorystateDirectory)
end note
:this.time=time;
:this.changelogReader=changelogReader;
:this.processId=processId;
:this.logPrefix=logPrefix;
:this.topologyMetadata=topologyMetadata;
:this.adminClient=adminClient;
:this.stateDirectory=stateDirectory;
:this.processingMode=topologyMetadata.processingMode();
:this.log=logContext.logger(getClass());
:this.tasks=new Tasks(logContext,topologyMetadata,
activeTaskCreator,standbyTaskCreator);
:this.taskExecutor=new TaskExecutor(tasks,
topologyMetadata.taskExecutionMetadata(),
processingMode,topologyMetadata.hasNamedTopologies(),
logContext);
end
start
note right
void setMainConsumer(final Consumer<byte[],
byte[]>mainConsumer)
end note
:this.mainConsumer=mainConsumer;
:tasks.setMainConsumer(mainConsumer);
end
start
note right
doubletotalProducerBlockedTime()
end note
:return tasks.totalProducerBlockedTime();;
stop
end
start
note right
UUIDprocessId()
end note
:return processId;;
stop
end
start
note right
TopologyMetadatatopologyMetadata()
end note
:return topologyMetadata;;
stop
end
start
note right
booleanisRebalanceInProgress()
end note
:return rebalanceInProgress;;
stop
end
start
note right
void handleRebalanceStart(final Set<String>subscribedTopics)
end note
:topologyMetadata.addSubscribedTopicsFromMetadata(subscribedTopics,
logPrefix);
:tryToLockAllNonEmptyTaskDirectories();
:rebalanceInProgress=true;
end
start
note right
void handleRebalanceComplete()
end note
:mainConsumer.pause(mainConsumer.assignment());
:releaseLockedUnassignedTaskDirectories();
:rebalanceInProgress=false;
end
start
note right
booleanhandleCorruption(final Set<TaskId>corruptedTasks)
end note
if (task.isActive()) then (yes) 
:corruptedActiveTasks.add(task);
else (no)
:corruptedStandbyTasks.add(task);
endif 
:closeDirtyAndRevive(corruptedStandbyTasks,
true);
:commitTasksAndMaybeUpdateCommittableOffsets(tasksToCommit,
new HashMap<>());
:log.info("Some additional tasks were found corrupted while trying to commit, these will be added to the "+
"tasks to clean and revive: {}",e.corruptedTasks());
:corruptedActiveTasks.addAll(tasks.tasks(e.corruptedTasks()));
:log.info("Hit TimeoutException when committing all non-corrupted tasks,
 these will be closed and revived");
:uncorruptedTasks.removeAll(corruptedActiveTasks);
:closeDirtyAndRevive(uncorruptedTasks,false);
:closeDirtyAndRevive(corruptedActiveTasks,
true);
:return !corruptedActiveTasks.isEmpty();;
stop
end
start
note right
void closeDirtyAndRevive(final Collection<Task>taskWithChangelogs,
final booleanmarkAsCorrupted)
end note
if (markAsCorrupted) then (yes) 
:task.markChangelogAsCorrupted(corruptedPartitions);
endif 
:task.prepareCommit();
:log.error("Error flushing cache for corrupted task {} ",
task.id(),swallow);
:task.suspend();
if (markAsCorrupted) then (yes) 
:task.postCommit(true);
endif 
:log.error("Error suspending corrupted task {} ",
task.id(),swallow);
:task.closeDirty();
if (task.isActive()) then (yes) 
if (!assignedToPauseAndReset.equals(taskInputPartitions)) then (yes) 
:log.warn("Expected the current consumer assignment {} to contain the input partitions {}. "+
"Will proceed to recover.",currentAssignment,
taskInputPartitions);
endif 
:task.addPartitionsForOffsetReset(assignedToPauseAndReset);
endif 
:task.revive();
end
start
note right
void handleAssignment(final Map<TaskId,Set<TopicPartition>>activeTasks,
final Map<TaskId,Set<TopicPartition>>standbyTasks)
end note
:log.info("Handle new assignment with:\n"+
"\tNew active tasks: {}\n"+"\tNew standby tasks: {}\n"+
"\tExisting active tasks: {}\n"+"\tExisting standby tasks: {}",
activeTasks.keySet(),standbyTasks.keySet(),
activeTaskIds(),standbyTaskIds());
:topologyMetadata.addSubscribedTopicsFromAssignment(activeTasks.values()
.stream().flatMap(Collection::stream).collect(Collectors.toList()),
logPrefix);
if (activeTasks.containsKey(task.id())&&task.isActive()) then (yes) 
:tasks.updateInputPartitionsAndResume(task,
activeTasks.get(task.id()));
:activeTasksToCreate.remove(task.id());
else (no)
:tasksToCloseClean.add(task);
endif 
:handleCloseAndRecycle(tasksToRecycle,tasksToCloseClean,
tasksToCloseDirty,activeTasksToCreate,standbyTasksToCreate,
taskCloseExceptions);
if (!taskCloseExceptions.isEmpty()) then (yes) 
:log.error("Hit exceptions while closing / recycling tasks: {}",
taskCloseExceptions);
if (!(entry.getValue() instanceof TaskMigratedException)) then (yes) 
if (exception instanceof StreamsException) then (yes) 
:((StreamsException)exception).setTaskId(taskId);
:throw exception;;
else (no)
:throw new StreamsException("Unexpected failure to close "+
taskCloseExceptions.size()+" task(s) ["+taskCloseExceptions.keySet()+
"]. "+"First unexpected exception (for task "+
taskId+") follows.",exception,taskId);;
endif 
endif 
:throw first.getValue();;
endif 
:tasks.handleNewAssignmentAndCreateTasks(activeTasksToCreate,
standbyTasksToCreate,activeTasks.keySet(),
standbyTasks.keySet());
end
start
note right
void handleCloseAndRecycle(final Set<Task>tasksToRecycle,
final Set<Task>tasksToCloseClean,final Set<Task>tasksToCloseDirty,
final Map<TaskId,Set<TopicPartition>>activeTasksToCreate,
final Map<TaskId,Set<TopicPartition>>standbyTasksToCreate,
final LinkedHashMap<TaskId,RuntimeException>taskCloseExceptions)
end note
if (!tasksToCloseDirty.isEmpty()) then (yes) 
:throw new IllegalArgumentException("Tasks to close-dirty should be empty");;
endif 
:tasksToCheckpoint.addAll(tasksToRecycle);
if (!offsets.isEmpty()) then (yes) 
:log.error("Task {} should have been committed when it was suspended, but it reports non-empty "+
"offsets {} to commit; this means it failed during last commit and hence should be closed dirty",
task.id(),offsets);
:tasksToCloseDirty.add(task);
endif 
:log.error(uncleanMessage,e);
:taskCloseExceptions.putIfAbsent(task.id(),e);
:tasksToCloseDirty.add(task);
:tasksToCloseClean.removeAll(tasksToCloseDirty);
:completeTaskCloseClean(task);
if (task.isActive()) then (yes) 
:tasks.cleanUpTaskProducerAndRemoveTask(task.id(),
taskCloseExceptions);
endif 
:log.error(uncleanMessage,e);
:taskCloseExceptions.putIfAbsent(task.id(),e);
:tasksToCloseDirty.add(task);
:tasksToRecycle.removeAll(tasksToCloseDirty);
if (oldTask.isActive()) then (yes) 
:tasks.convertActiveToStandby((StreamTask)oldTask,
partitions,taskCloseExceptions);
else (no)
:tasks.convertStandbyToActive((StandbyTask)oldTask,
partitions);
endif 
:log.error(uncleanMessage,e);
:taskCloseExceptions.putIfAbsent(oldTask.id(),e);
:tasksToCloseDirty.add(oldTask);
:closeTaskDirty(task);
:tasks.cleanUpTaskProducerAndRemoveTask(task.id(),
taskCloseExceptions);
end
start
note right
booleantryToCompleteRestoration(final longnow,
final java.util.function .Consumer<Set<TopicPartition>>offsetResetter)
end note
:task.initializeIfNeeded();
:task.clearTaskTimeout();
:log.debug("Could not initialize task {} since: {}; will retry",
task.id(),lockException.getMessage());
:allRunning=false;
:task.maybeInitTaskTimeoutOrThrow(now,timeoutException);
:allRunning=false;
if (task.isActive()) then (yes) 
:activeTasks.add(task);
endif 
if (allRunning&&!activeTasks.isEmpty()) then (yes) 
if (restored.containsAll(task.changelogPartitions())) then (yes) 
:task.completeRestoration(offsetResetter);
:task.clearTaskTimeout();
:task.maybeInitTaskTimeoutOrThrow(now,timeoutException);
:log.debug(String.format("Could not complete restoration for %s due to the following exception; will retry",
task.id()),timeoutException);
:allRunning=false;
else (no)
:allRunning=false;
endif 
endif 
if (allRunning) then (yes) 
:mainConsumer.resume(mainConsumer.assignment());
endif 
:return allRunning;;
stop
end
start
note right
void handleRevocation(final Collection<TopicPartition>revokedPartitions)
end note
if (remainingRevokedPartitions.containsAll(task.inputPartitions())) then (yes) 
:revokedActiveTasks.add(task);
:remainingRevokedPartitions.removeAll(task.inputPartitions());
endif 
if (!remainingRevokedPartitions.isEmpty()) then (yes) 
:log.debug("The following revoked partitions {} are missing from the current task partitions. It could "+
"potentially be due to race condition of consumer detecting the heartbeat failure, or the tasks "+
"have been cleaned up by the handleAssignment callback.",
remainingRevokedPartitions);
endif 
:prepareCommitAndAddOffsetsToMap(revokedActiveTasks,
consumedOffsetsPerTask);
if (shouldCommitAdditionalTasks) then (yes) 
:prepareCommitAndAddOffsetsToMap(commitNeededActiveTasks,
consumedOffsetsPerTask);
endif 
:taskExecutor.commitOffsetsOrTransaction(consumedOffsetsPerTask);
:log.warn("Some tasks were corrupted when trying to commit offsets,
 these will be cleaned and revived: {}",e.corruptedTasks());
:dirtyTasks.addAll(tasks.tasks(e.corruptedTasks()));
:closeDirtyAndRevive(dirtyTasks,true);
:log.warn("Timed out while trying to commit all tasks during revocation,
 these will be cleaned and revived");
:dirtyTasks.addAll(consumedOffsetsPerTask.keySet());
:closeDirtyAndRevive(dirtyTasks,false);
:log.error("Exception caught while committing those revoked tasks "+
revokedActiveTasks,e);
:firstException.compareAndSet(null ,e);
:dirtyTasks.addAll(consumedOffsetsPerTask.keySet());
if (!dirtyTasks.contains(task)) then (yes) 
:task.postCommit(true);
:log.error("Exception caught while post-committing task "+
task.id(),e);
:maybeWrapAndSetFirstException(firstException,
e,task.id());
endif 
if (shouldCommitAdditionalTasks) then (yes) 
if (!dirtyTasks.contains(task)) then (yes) 
:task.postCommit(false);
:log.error("Exception caught while post-committing task "+
task.id(),e);
:maybeWrapAndSetFirstException(firstException,
e,task.id());
endif 
endif 
:task.suspend();
:log.error("Caught the following exception while trying to suspend revoked task "+
task.id(),e);
:maybeWrapAndSetFirstException(firstException,
e,task.id());
if (firstException.get()!=null ) then (yes) 
:throw firstException.get();;
endif 
end
start
note right
void prepareCommitAndAddOffsetsToMap(final Set<Task>tasksToPrepare,
final Map<Task,Map<TopicPartition,OffsetAndMetadata>>consumedOffsetsPerTask)
end note
if (!committableOffsets.isEmpty()) then (yes) 
:consumedOffsetsPerTask.put(task,committableOffsets);
endif 
:e.setTaskId(task.id());
:throw e;;
:throw new StreamsException(e,task.id());;
end
start
note right
void handleLostAll()
end note
:log.debug("Closing lost active tasks as zombies.");
if (task.isActive()) then (yes) 
:closeTaskDirty(task);
:tasks.cleanUpTaskProducerAndRemoveTask(task.id(),
new HashMap<>());
endif 
if (processingMode==EXACTLY_ONCE_V2) then (yes) 
:tasks.reInitializeThreadProducer();
endif 
end
start
note right
Map<TaskId,Long>getTaskOffsetSums()
end note
if (task!=null &&task.state()!=State.CREATED&&task.state()!=State.CLOSED) then (yes) 
if (changelogOffsets.isEmpty()) then (yes) 
:log.debug("Skipping to encode apparently stateless (or non-logged) offset sum for task {}",id);
else (no)
:taskOffsetSums.put(id,sumOfChangelogOffsets(id,
changelogOffsets));
endif 
else (no)
if (checkpointFile.exists()) then (yes) 
:taskOffsetSums.put(id,sumOfChangelogOffsets(id,
new OffsetCheckpoint(checkpointFile).read()));
endif 
:log.warn(String.format("Exception caught while trying to read checkpoint for task %s:",
id),e);
endif 
:return taskOffsetSums;;
stop
end
start
note right
void tryToLockAllNonEmptyTaskDirectories()
end note
:lockedTaskDirectories.clear();
if (stateDirectory.lock(id)) then (yes) 
:lockedTaskDirectories.add(id);
if (!tasks.owned(id)) then (yes) 
:log.debug("Temporarily locked unassigned task {} for the upcoming rebalance",id);
endif 
endif 
end
start
note right
void releaseLockedDirectoriesForTasks(final Set<TaskId>tasksToUnlock)
end note
while (taskIdIterator.hasNext())
if (tasksToUnlock.contains(id)) then (yes) 
:stateDirectory.unlock(id);
:taskIdIterator.remove();
endif 
endwhile
end
start
note right
void releaseLockedUnassignedTaskDirectories()
end note
while (taskIdIterator.hasNext())
if (!tasks.owned(id)) then (yes) 
:stateDirectory.unlock(id);
:taskIdIterator.remove();
endif 
endwhile
end
start
note right
longsumOfChangelogOffsets(final TaskIdid,
final Map<TopicPartition,Long>changelogOffsets)
end note
if (offset==Task.LATEST_OFFSET) then (yes) 
:return Task.LATEST_OFFSET;;
stop
endif 
:return offsetSum;;
stop
end
start
note right
void closeTaskDirty(final Tasktask)
end note
:task.prepareCommit();
:log.error("Error flushing caches of dirty task {} ",
task.id(),swallow);
:task.suspend();
:log.error("Error suspending dirty task {} ",
task.id(),swallow);
:tasks.removeTaskBeforeClosing(task.id());
:task.closeDirty();
end
start
note right
void completeTaskCloseClean(final Tasktask)
end note
:tasks.removeTaskBeforeClosing(task.id());
:task.closeClean();
end
start
note right
void shutdown(final booleanclean)
end note
:activeTasks.addAll(tasks.activeTasks());
:executeAndMaybeSwallow(clean,()->closeAndCleanUpTasks(activeTasks,
standbyTaskIterable(),clean),e->firstException.compareAndSet(null ,
e),e->log.warn("Ignoring an exception while unlocking remaining task directories.",e));
:executeAndMaybeSwallow(clean,tasks::closeThreadProducerIfNeeded,
e->firstException.compareAndSet(null ,e),
e->log.warn("Ignoring an exception while closing thread producer.",e));
:tasks.clear();
:executeAndMaybeSwallow(clean,this::releaseLockedUnassignedTaskDirectories,
e->firstException.compareAndSet(null ,e),
e->log.warn("Ignoring an exception while unlocking remaining task directories.",e));
if (fatalException!=null ) then (yes) 
:throw new RuntimeException("Unexpected exception while closing task",
fatalException);;
endif 
end
start
note right
void closeAndCleanUpTasks(final Collection<Task>activeTasks,
final Collection<Task>standbyTasks,final booleanclean)
end note
:tasksToCloseDirty.addAll(tryCloseCleanActiveTasks(activeTasks,
clean,firstException));
:tasksToCloseDirty.addAll(tryCloseCleanStandbyTasks(standbyTasks,
clean,firstException));
:closeTaskDirty(task);
:executeAndMaybeSwallow(clean,()->tasks.closeAndRemoveTaskProducerIfNeeded(activeTask),
e->firstException.compareAndSet(null ,e),e->log.warn("Ignoring an exception while closing task "+
activeTask.id()+" producer.",e));
if (exception!=null ) then (yes) 
:throw exception;;
endif 
end
start
note right
Collection<Task>tryCloseCleanActiveTasks(final Collection<Task>activeTasksToClose,
final booleanclean,final AtomicReference<RuntimeException>firstException)
end note
if (!clean) then (yes) 
:return activeTaskIterable();;
stop
endif 
:tasksToCommit.add(task);
if (!committableOffsets.isEmpty()) then (yes) 
:consumedOffsetsAndMetadataPerTask.put(task,
committableOffsets);
endif 
:tasksToCloseClean.add(task);
:tasksToCloseDirty.add(task);
:e.setTaskId(task.id());
:firstException.compareAndSet(null ,e);
:tasksToCloseDirty.add(task);
:firstException.compareAndSet(null ,new StreamsException(e,
task.id()));
:tasksToCloseDirty.add(task);
if (processingMode==EXACTLY_ONCE_V2&&!tasksToCloseDirty.isEmpty()) then (yes) 
:tasksToCloseClean.removeAll(tasksToCommit);
:tasksToCloseDirty.addAll(tasksToCommit);
else (no)
:taskExecutor.commitOffsetsOrTransaction(consumedOffsetsAndMetadataPerTask);
:task.postCommit(true);
:log.error("Exception caught while post-committing task "+
task.id(),e);
:maybeWrapAndSetFirstException(firstException,
e,task.id());
:tasksToCloseDirty.add(task);
:tasksToCloseClean.remove(task);
:firstException.compareAndSet(null ,timeoutException);
:tasksToCloseClean.removeAll(tasksToCommit);
:tasksToCloseDirty.addAll(tasksToCommit);
:firstException.compareAndSet(null ,taskCorruptedException);
:tasksToCloseClean.removeAll(corruptedTasks);
:tasksToCloseDirty.addAll(corruptedTasks);
:log.error("Exception caught while committing tasks during shutdown",e);
:firstException.compareAndSet(null ,e);
:tasksToCloseClean.removeAll(tasksToCommit);
:tasksToCloseDirty.addAll(tasksToCommit);
endif 
:task.suspend();
:completeTaskCloseClean(task);
:log.error("Exception caught while clean-closing task "+
task.id(),e);
:e.setTaskId(task.id());
:firstException.compareAndSet(null ,e);
:tasksToCloseDirty.add(task);
:log.error("Exception caught while clean-closing task "+
task.id(),e);
:firstException.compareAndSet(null ,new StreamsException(e,
task.id()));
:tasksToCloseDirty.add(task);
:return tasksToCloseDirty;;
stop
end
start
note right
Collection<Task>tryCloseCleanStandbyTasks(final Collection<Task>standbyTasksToClose,
final booleanclean,final AtomicReference<RuntimeException>firstException)
end note
if (!clean) then (yes) 
:return standbyTaskIterable();;
stop
endif 
:task.prepareCommit();
:task.postCommit(true);
:task.suspend();
:completeTaskCloseClean(task);
:tasksToCloseDirty.add(task);
:maybeWrapAndSetFirstException(firstException,
e,task.id());
:tasksToCloseDirty.add(task);
:return tasksToCloseDirty;;
stop
end
start
note right
Set<TaskId>activeTaskIds()
end note
:return activeTaskStream().map(Task::id).collect(Collectors.toSet());;
stop
end
start
note right
Set<TaskId>standbyTaskIds()
end note
:return standbyTaskStream().map(Task::id)
.collect(Collectors.toSet());;
stop
end
start
note right
Map<TaskId,Task>tasks()
end note
:return tasks.tasksPerId();;
stop
end
start
note right
Map<TaskId,Task>notPausedTasks()
end note
:return Collections.unmodifiableMap(tasks.notPausedTasks().stream().collect(Collectors.toMap(Task::id,
v->v)));;
stop
end
start
note right
Map<TaskId,Task>activeTaskMap()
end note
:return activeTaskStream().collect(Collectors.toMap(Task::id,
t->t));;
stop
end
start
note right
List<Task>activeTaskIterable()
end note
:return activeTaskStream().collect(Collectors.toList());;
stop
end
start
note right
Stream<Task>activeTaskStream()
end note
:return tasks.allTasks().stream().filter(Task::isActive);;
stop
end
start
note right
Map<TaskId,Task>standbyTaskMap()
end note
:return standbyTaskStream().collect(Collectors.toMap(Task::id,
t->t));;
stop
end
start
note right
List<Task>standbyTaskIterable()
end note
:return standbyTaskStream().collect(Collectors.toList());;
stop
end
start
note right
Stream<Task>standbyTaskStream()
end note
:return tasks.allTasks().stream().filter(t->!t.isActive());;
stop
end
start
note right
intcommitAll()
end note
:return commit(new HashSet<>(tasks.allTasks()));;
stop
end
start
note right
void addRecordsToTasks(final ConsumerRecords<byte[],
byte[]>records)
end note
if (activeTask==null ) then (yes) 
:log.error("Unable to locate active task for received-record partition {}. Current tasks: {}",
partition,toString(">"));
:throw new NullPointerException("Task was unexpectedly missing for partition "+
partition);;
endif 
:activeTask.addRecords(partition,records.records(partition));
end
start
note right
intcommit(final Collection<Task>tasksToCommit)
end note
:committed=commitTasksAndMaybeUpdateCommittableOffsets(tasksToCommit,
consumedOffsetsAndMetadataPerTask);
:consumedOffsetsAndMetadataPerTask.keySet().forEach(t->t.maybeInitTaskTimeoutOrThrow(time.milliseconds(),
timeoutException));
:return committed;;
stop
end
start
note right
intmaybeCommitActiveTasksPerUserRequested()
end note
if (rebalanceInProgress) then (yes) 
:return -1;;
stop
else (no)
if (task.commitRequested()&&task.commitNeeded()) then (yes) 
:return commit(activeTaskIterable());;
stop
endif 
:return 0;;
stop
endif 
end
start
note right
intcommitTasksAndMaybeUpdateCommittableOffsets(final Collection<Task>tasksToCommit,
final Map<Task,Map<TopicPartition,OffsetAndMetadata>>consumedOffsetsAndMetadata)
end note
if (rebalanceInProgress) then (yes) 
:return -1;;
stop
else (no)
:return taskExecutor.commitTasksAndMaybeUpdateCommittableOffsets(tasksToCommit,
consumedOffsetsAndMetadata);;
stop
endif 
end
start
note right
void updateTaskEndMetadata(final TopicPartitiontopicPartition,
final Longoffset)
end note
if (task instanceof StreamTask) then (yes) 
if (task.inputPartitions().contains(topicPartition)) then (yes) 
:((StreamTask)task).updateEndOffsets(topicPartition,
offset);
endif 
endif 
end
start
note right
void handleTopologyUpdates()
end note
:topologyMetadata.executeTopologyUpdatesAndBumpThreadVersion(tasks::maybeCreateTasksFromNewTopologies,
this::maybeCloseTasksFromRemovedTopologies);
if (topologyMetadata.isEmpty()) then (yes) 
:log.info("Proactively unsubscribing from all topics due to empty topology");
:mainConsumer.unsubscribe();
endif 
:topologyMetadata.maybeNotifyTopologyVersionListeners();
end
start
note right
void maybeCloseTasksFromRemovedTopologies(final Set<String>currentNamedTopologies)
end note
if (!currentNamedTopologies.contains(task.id()
.topologyName())) then (yes) 
if (task.isActive()) then (yes) 
:activeTasksToRemove.add(task);
else (no)
:standbyTasksToRemove.add(task);
endif 
endif 
:closeAndCleanUpTasks(activeTasksToRemove,
standbyTasksToRemove,true);
:allRemovedTasks.forEach(tasks::removeTaskBeforeClosing);
:releaseLockedDirectoriesForTasks(allRemovedTasks);
:log.error("Caught the following exception while closing tasks from a removed topology:",e);
end
start
note right
intprocess(final intmaxNumRecords,final Timetime)
end note
:return taskExecutor.process(maxNumRecords,
time);;
stop
end
start
note right
void recordTaskProcessRatio(final longtotalProcessLatencyMs,
final longnow)
end note
:task.recordProcessTimeRatioAndBufferSize(totalProcessLatencyMs,
now);
end
start
note right
intpunctuate()
end note
:return taskExecutor.punctuate();;
stop
end
start
note right
void maybePurgeCommittedRecords()
end note
if (deleteRecordsResult==null ||deleteRecordsResult.all()
.isDone()) then (yes) 
if (deleteRecordsResult!=null &&deleteRecordsResult.all()
.isCompletedExceptionally()) then (yes) 
:log.debug("Previous delete-records request has failed: {}. Try sending the new request now",
deleteRecordsResult.lowWatermarks());
endif 
:recordsToDelete.put(entry.getKey(),RecordsToDelete.beforeOffset(entry.getValue()));
if (!recordsToDelete.isEmpty()) then (yes) 
:deleteRecordsResult=adminClient.deleteRecords(recordsToDelete);
:log.trace("Sent delete-records request: {}",
recordsToDelete);
endif 
endif 
end
start
note right
StringtoString()
end note
:return toString("");;
stop
end
start
note right
StringtoString(final Stringindent)
end note
:stringBuilder.append("TaskManager\n");
:stringBuilder.append(indent).append("\tMetadataState:\n");
:stringBuilder.append(indent).append("\tTasks:\n");
:stringBuilder.append(indent).append("\t\t")
.append(task.id()).append(" ").append(task.state())
.append(" ").append(task.getClass().getSimpleName())
.append('(').append(task.isActive()? String.valueOf("active"):String.valueOf("standby"))
.append(')');
:return stringBuilder.toString();;
stop
end
start
note right
Map<MetricName,Metric>producerMetrics()
end note
:return tasks.producerMetrics();;
stop
end
start
note right
Set<String>producerClientIds()
end note
:return tasks.producerClientIds();;
stop
end
start
note right
Set<TaskId>lockedTaskDirectories()
end note
:return Collections.unmodifiableSet(lockedTaskDirectories);;
stop
end
start
note right
void maybeWrapAndSetFirstException(final AtomicReference<RuntimeException>firstException,
final RuntimeExceptionexception,final TaskIdtaskId)
end note
if (exception instanceof StreamsException) then (yes) 
:((StreamsException)exception).setTaskId(taskId);
:firstException.compareAndSet(null ,exception);
else (no)
:firstException.compareAndSet(null ,new StreamsException(exception,
taskId));
endif 
end
start
note right
void executeAndMaybeSwallow(final booleanclean,
final Runnablerunnable,final java.util.function .Consumer<RuntimeException>actionIfClean,
final java.util.function .Consumer<RuntimeException>actionIfNotClean)
end note
:runnable.run();
if (clean) then (yes) 
:actionIfClean.accept(e);
else (no)
:actionIfNotClean.accept(e);
endif 
end
start
note right
void executeAndMaybeSwallow(final booleanclean,
final Runnablerunnable,final Stringname,final Loggerlog)
end note
:executeAndMaybeSwallow(clean,runnable,e->{throw e;},
e->log.debug("Ignoring error in unclean {}",
name));
end
start
note right
booleanneedsInitializationOrRestoration()
end note
:return tasks().values().stream().anyMatch(Task::needsInitializationOrRestoration);;
stop
end
start
note right
void addTask(final Tasktask)
end note
:tasks.addTask(task);
end
@enduml

PlantUML version 1.2022.7(Mon Aug 22 17:01:30 UTC 2022)
(GPL source distribution)
Java Runtime: OpenJDK Runtime Environment
JVM: OpenJDK 64-Bit Server VM
Default Encoding: UTF-8
Language: en
Country: null
--></g></svg>