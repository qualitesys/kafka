@startuml
!pragma layout smetana
ReadOnlySessionStore<K,V> <|-- org.apache.kafka.streams.state.internals.CompositeReadOnlySessionStore
class org.apache.kafka.streams.state.internals.CompositeReadOnlySessionStore {
+  CompositeReadOnlySessionStore(StateStoreProvider;QueryableStoreType<ReadOnlySessionStore<K,V>>;String)
+  findSessions(K;long;long)
+  backwardFindSessions(K;long;long)
+  findSessions(K;K;long;long)
+  backwardFindSessions(K;K;long;long)
+  fetchSession(K;long;long)
+  fetch(K)
+  backwardFetch(K)
+  fetch(K;K)
+  backwardFetch(K;K)
} 
@enduml
