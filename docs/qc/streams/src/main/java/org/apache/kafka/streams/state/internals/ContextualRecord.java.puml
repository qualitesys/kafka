@startuml
!pragma layout smetana
java.lang.Object <|-- org.apache.kafka.streams.state.internals.ContextualRecord
class org.apache.kafka.streams.state.internals.ContextualRecord {
+  ContextualRecord(byte[];ProcessorRecordContext)
+  recordContext()
+  value()
+  residentMemorySizeEstimate()
+  deserialize(ByteBuffer)
+  equals(Object)
+  hashCode()
+  toString()
} 
@enduml
