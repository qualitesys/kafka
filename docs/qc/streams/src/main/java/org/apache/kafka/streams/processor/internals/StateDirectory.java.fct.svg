<?xml version="1.0" encoding="UTF-8" standalone="no"?><svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" contentStyleType="text/css" height="544px" preserveAspectRatio="none" style="width:900px;height:544px;background:#000000;" version="1.1" viewBox="0 0 900 544" width="900px" zoomAndPan="magnify"><defs/><g><rect fill="#0D1F18" height="1" style="stroke:#0D1F18;stroke-width:1.0;" width="1" x="0" y="0"/><text fill="#33FF02" font-family="sans-serif" font-size="12" font-style="italic" font-weight="bold" lengthAdjust="spacing" textLength="127" x="5" y="17">PlantUML 1.2022.7</text><rect fill="#33FF02" height="21.2969" style="stroke:#33FF02;stroke-width:1.0;" width="888" x="5" y="26.9688"/><text fill="#000000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="178" x="6" y="41.9688">[From string (line 49) ]</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="5" x="5" y="62.2656"> </text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="79" x="5" y="78.5625">@startuml</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="190" x="5" y="94.8594">!pragma layout smetana</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="111.1563">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="127.4531">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="315" x="5" y="143.75">constructor StateDirectoryProcessFile()</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="15" x="5" y="160.0469">...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="175" x="5" y="176.3438">... ( skipping 24 lines )</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="15" x="5" y="192.6406">...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="40" x="5" y="208.9375">endif</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="400" x="5" y="225.2344">if (!stateDir.exists()&amp;&amp;!stateDir.mkdir()) then (yes)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="888" x="5" y="241.5313">:throw new ProcessorStateException(String.format("state directory [%s] doesn't exist and couldn't be created",</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="164" x="5" y="257.8281">stateDir.getPath()));;</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="40" x="5" y="274.125">endif</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="607" x="5" y="290.4219">if (stateDirName.startsWith(System.getProperty("java.io.tmpdir"))) then (yes)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="767" x="5" y="306.7188">:log.warn("Using an OS temp directory in the state.dir property can cause failures with writing"+</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="656" x="5" y="323.0156">" the checkpoint file due to the fact that this directory can be cleared by the OS."+</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="341" x="5" y="339.3125">" Resolved state.dir: ["+stateDirName+"]");</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="40" x="5" y="355.6094">endif</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="256" x="5" y="371.9063">:configurePermissions(baseDir);</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="258" x="5" y="388.2031">:configurePermissions(stateDir);</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="40" x="5" y="404.5">endif</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="30" x="5" y="420.7969">end</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="437.0938">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="453.3906">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="311" x="5" y="469.6875">void configurePermissions(final Filefile)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="71" x="5" y="485.9844">end note</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="428" x="5" y="502.2813">if (path.getFileSystem().supportedFileAttributeViews()</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" text-decoration="wavy underline" textLength="232" x="5" y="518.5781">.contains("posix")) then (yes)</text><text fill="#FF0000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="106" x="10" y="534.875">Syntax Error?</text><!--MD5=[c21a3964de3d213dbe5ed7030c2a480f]
@startuml
!pragma layout smetana
start
note right
constructor StateDirectoryProcessFile()
end note
:this.processId=null ;
end
start
note right
constructor StateDirectoryProcessFile(final UUIDprocessId)
end note
:this.processId=processId;
end
start
note right
constructor StateDirectory(final StreamsConfigconfig,
final Timetime,final booleanhasPersistentStores,
final booleanhasNamedTopologies)
end note
:this.time=time;
:this.hasPersistentStores=hasPersistentStores;
:this.hasNamedTopologies=hasNamedTopologies;
:this.appId=config.getString(StreamsConfig.APPLICATION_ID_CONFIG);
:stateDir=new File(baseDir,appId);
if (this.hasPersistentStores) then (yes) 
if (!baseDir.exists()&&!baseDir.mkdirs()) then (yes) 
:throw new ProcessorStateException(String.format("base state directory [%s] doesn't exist and couldn't be created",
stateDirName));;
endif 
if (!stateDir.exists()&&!stateDir.mkdir()) then (yes) 
:throw new ProcessorStateException(String.format("state directory [%s] doesn't exist and couldn't be created",
stateDir.getPath()));;
endif 
if (stateDirName.startsWith(System.getProperty("java.io.tmpdir"))) then (yes) 
:log.warn("Using an OS temp directory in the state.dir property can cause failures with writing"+
" the checkpoint file due to the fact that this directory can be cleared by the OS."+
" Resolved state.dir: ["+stateDirName+"]");
endif 
:configurePermissions(baseDir);
:configurePermissions(stateDir);
endif 
end
start
note right
void configurePermissions(final Filefile)
end note
if (path.getFileSystem().supportedFileAttributeViews()
.contains("posix")) then (yes) 
:Files.setPosixFilePermissions(path,perms);
:log.error("Error changing permissions for the directory {} ",
path,e);
else (no)
:set&=file.setWritable(true,true);
:set&=file.setExecutable(true,true);
if (!set) then (yes) 
:log.error("Failed to change permissions for the directory {}",
file);
endif 
endif 
end
start
note right
booleanlockStateDirectory()
end note
:stateDirLockChannel=FileChannel.open(lockFile.toPath(),
StandardOpenOption.CREATE,StandardOpenOption.WRITE);
:stateDirLock=tryLock(stateDirLockChannel);
:log.error("Unable to lock the state directory due to unexpected exception",e);
:throw new ProcessorStateException(String.format("Failed to lock the state directory [%s] during startup",
stateDir.getAbsolutePath()),e);;
:return stateDirLock!=null ;;
stop
end
start
note right
UUIDinitializeProcessId()
end note
if (!hasPersistentStores) then (yes) 
:return UUID.randomUUID();;
stop
endif 
if (!lockStateDirectory()) then (yes) 
:log.error("Unable to obtain lock as state directory is already locked by another process");
:throw new StreamsException(String.format("Unable to initialize state, this can happen if multiple instances of "+
"Kafka Streams are running in the same state directory "+
"(current state directory is [%s]",stateDir.getAbsolutePath()));;
endif 
if (processFile.exists()) then (yes) 
:log.info("Reading UUID from process file: {}",
processFileData.processId);
if (processFileData.processId!=null ) then (yes) 
:return processFileData.processId;;
stop
endif 
:log.warn("Failed to read json process file",e);
endif 
:log.info("No process id found on disk, got fresh process id {}",
processFileData.processId);
:mapper.writeValue(processFile,processFileData);
:return processFileData.processId;;
stop
:log.error("Unable to read/write process file due to unexpected exception",e);
:throw new ProcessorStateException(e);;
end
start
note right
FilegetOrCreateDirectoryForTask(final TaskIdtaskId)
end note
if (hasPersistentStores) then (yes) 
if (!taskDir.exists()) then (yes) 
if (!taskParentDir.exists()&&!taskParentDir.mkdir()) then (yes) 
:throw new ProcessorStateException(String.format("Parent [%s] of task directory [%s] doesn't exist and couldn't be created",taskParentDir.getPath(),taskDir.getPath()));;
endif 
if (!taskDir.exists()&&!taskDir.mkdir()) then (yes) 
:throw new ProcessorStateException(String.format("task directory [%s] doesn't exist and couldn't be created",
taskDir.getPath()));;
endif 
endif 
endif 
:return taskDir;;
stop
end
start
note right
FilegetTaskDirectoryParentName(final TaskIdtaskId)
end note
if (namedTopology!=null ) then (yes) 
if (!hasNamedTopologies) then (yes) 
:throw new IllegalStateException("Tried to lookup taskId with named topology,
 but StateDirectory thinks hasNamedTopologies = false");;
endif 
:return new File(stateDir,getNamedTopologyDirName(namedTopology));;
stop
else (no)
:return stateDir;;
stop
endif 
end
start
note right
StringgetNamedTopologyDirName(final StringtopologyName)
end note
:return "__"+topologyName+"__";;
stop
end
start
note right
FilecheckpointFileFor(final TaskIdtaskId)
end note
:return new File(getOrCreateDirectoryForTask(taskId),
StateManagerUtil.CHECKPOINT_FILE_NAME);;
stop
end
start
note right
booleandirectoryForTaskIsEmpty(final TaskIdtaskId)
end note
:return taskDirIsEmpty(taskDir);;
stop
end
start
note right
booleantaskDirIsEmpty(final FiletaskDir)
end note
if (storeDirs!=null &&storeDirs.length>0) then (yes) 
if (file.getName().endsWith(LOCK_FILE_NAME)) then (yes) 
if (!file.delete()) then (yes) 
:log.warn("Error encountered deleting lock file in {}",
taskDir);
endif 
else (no)
:log.trace("TaskDir {} was not empty, found {}",
taskDir,file);
:taskDirEmpty=false;
endif 
endif 
:return taskDirEmpty;;
stop
end
start
note right
FileglobalStateDir()
end note
if (hasPersistentStores) then (yes) 
if (!dir.exists()&&!dir.mkdir()) then (yes) 
:throw new ProcessorStateException(String.format("global state directory [%s] doesn't exist and couldn't be created",
dir.getPath()));;
endif 
endif 
:return dir;;
stop
end
start
note right
StringlogPrefix()
end note
:return String.format("stream-thread [%s]",
Thread.currentThread().getName());;
stop
end
start
note right
booleanlock(final TaskIdtaskId)
end note
if (!hasPersistentStores) then (yes) 
:return true;;
stop
endif 
if (lockOwner!=null ) then (yes) 
if (lockOwner.equals(Thread.currentThread())) then (yes) 
:log.trace("{} Found cached state dir lock for task {}",
logPrefix(),taskId);
:return true;;
stop
else (no)
:return false;;
stop
endif 
else (no)
:lockedTasksToOwner.put(taskId,Thread.currentThread());
:getOrCreateDirectoryForTask(taskId);
:return true;;
stop
endif 
end
start
note right
void unlock(final TaskIdtaskId)
end note
if (lockOwner!=null &&lockOwner.equals(Thread.currentThread())) then (yes) 
:lockedTasksToOwner.remove(taskId);
:log.debug("{} Released state dir lock for task {}",
logPrefix(),taskId);
endif 
end
start
note right
void close()
end note
if (hasPersistentStores) then (yes) 
:stateDirLock.release();
:stateDirLockChannel.close();
:stateDirLock=null ;
:stateDirLockChannel=null ;
:log.error("Unexpected exception while unlocking the state dir",e);
:throw new StreamsException(String.format("Failed to release the lock on the state directory [%s]",
stateDir.getAbsolutePath()),e);;
if (!lockedTasksToOwner.isEmpty()) then (yes) 
:log.error("Some task directories still locked while closing state,
 this indicates unclean shutdown: {}",lockedTasksToOwner);
endif 
endif 
end
start
note right
void clean()
end note
:cleanStateAndTaskDirectoriesCalledByUser();
:throw new StreamsException(e);;
if (stateDir.exists()) then (yes) 
:Utils.delete(globalStateDir().getAbsoluteFile());
endif 
:log.error(String.format("%s Failed to delete global state directory of %s due to an unexpected exception",
logPrefix(),appId),exception);
:throw new StreamsException(exception);;
if (hasPersistentStores&&stateDir.exists()&&!stateDir.delete()) then (yes) 
:log.warn(String.format("%s Failed to delete state store directory of %s for it is not empty",
logPrefix(),stateDir.getAbsolutePath()));
endif 
:log.error(String.format("%s Failed to delete state store directory of %s due to an unexpected exception",
logPrefix(),stateDir.getAbsolutePath()),exception);
:throw new StreamsException(exception);;
end
start
note right
void cleanRemovedTasks(final longcleanupDelayMs)
end note
:cleanRemovedTasksCalledByCleanerThread(cleanupDelayMs);
:throw new IllegalStateException("Should have swallowed exception.",
cannotHappen);;
end
start
note right
void cleanRemovedTasksCalledByCleanerThread(final longcleanupDelayMs)
end note
if (!lockedTasksToOwner.containsKey(id)) then (yes) 
if (lock(id)) then (yes) 
if (now-cleanupDelayMs>lastModifiedMs) then (yes) 
:log.info("{} Deleting obsolete state directory {} for task {} as {}ms has elapsed (cleanup delay is {}ms).",
logPrefix(),dirName,id,now-lastModifiedMs,
cleanupDelayMs);
:Utils.delete(taskDir.file());
endif 
endif 
:log.warn(String.format("%s Swallowed the following exception during deletion of obsolete state directory %s for task %s:",logPrefix(),dirName,id),exception);
:unlock(id);
endif 
:maybeCleanEmptyNamedTopologyDirs(true);
end
start
note right
IOExceptionmaybeCleanEmptyNamedTopologyDirs(final booleanlogExceptionAsWarn)
end note
if (!hasNamedTopologies) then (yes) 
:return null ;;
stop
endif 
if (namedTopologyDirs!=null ) then (yes) 
if (contents!=null &&contents.length==0) then (yes) 
:Utils.delete(namedTopologyDir);
if (logExceptionAsWarn) then (yes) 
:log.warn(String.format("%sSwallowed the following exception during deletion of named topology directory %s",
logPrefix(),namedTopologyDir.getName()),exception);
else (no)
:log.error(String.format("%s Failed to delete named topology directory %s with exception:",
logPrefix(),namedTopologyDir.getName()),exception);
endif 
:firstException.compareAndSet(null ,exception);
endif 
endif 
:return firstException.get();;
stop
end
start
note right
void clearLocalStateForNamedTopology(final StringtopologyName)
end note
if (!namedTopologyDir.exists()||!namedTopologyDir.isDirectory()) then (yes) 
:log.debug("Tried to clear out the local state for NamedTopology {} but none was found",
topologyName);
endif 
:Utils.delete(namedTopologyDir);
:log.error("Hit an unexpected error while clearing local state for topology "+
topologyName,e);
:throw new StreamsException("Unable to delete state for the named topology "+
topologyName,e,new TaskId(-1,-1,topologyName));;
end
start
note right
void cleanStateAndTaskDirectoriesCalledByUser()
end note
if (!lockedTasksToOwner.isEmpty()) then (yes) 
:log.warn("Found some still-locked task directories when user requested to cleaning up the state, "+
"since Streams is not running any more these will be ignored to complete the cleanup");
endif 
:log.info("{} Deleting task directory {} for {} as user calling cleanup.",
logPrefix(),dirName,id);
if (lockedTasksToOwner.containsKey(id)) then (yes) 
:log.warn("{} Task {} in state directory {} was still locked by {}",
logPrefix(),dirName,id,lockedTasksToOwner.get(id));
endif 
:Utils.delete(taskDir.file());
:log.error(String.format("%s Failed to delete task directory %s for %s with exception:",
logPrefix(),dirName,id),exception);
:firstException.compareAndSet(null ,exception);
:firstException.compareAndSet(null ,maybeCleanEmptyNamedTopologyDirs(false));
if (exception!=null ) then (yes) 
:throw exception;;
endif 
end
start
note right
List<TaskDirectory>listNonEmptyTaskDirectories()
end note
:return listTaskDirectories(pathname->{if (!pathname.isDirectory()||!TASK_DIR_PATH_NAME.matcher(pathname.getName())
.matches()){return false;}else {return !taskDirIsEmpty(pathname);}});;
stop
end
start
note right
List<TaskDirectory>listAllTaskDirectories()
end note
:return listTaskDirectories(pathname->pathname.isDirectory()&&TASK_DIR_PATH_NAME.matcher(pathname.getName())
.matches());;
stop
end
start
note right
List<TaskDirectory>listTaskDirectories(final FileFilterfilter)
end note
if (hasPersistentStores&&stateDir.exists()) then (yes) 
if (hasNamedTopologies) then (yes) 
if (taskDirs!=null ) then (yes) 
:taskDirectories.addAll(Arrays.stream(taskDirs).map(f->new TaskDirectory(f,
namedTopology)).collect(Collectors.toList()));
endif 
else (no)
if (taskDirs!=null ) then (yes) 
:taskDirectories.addAll(Arrays.stream(taskDirs).map(f->new TaskDirectory(f,
null )).collect(Collectors.toList()));
endif 
endif 
endif 
:return taskDirectories;;
stop
end
start
note right
List<File>listNamedTopologyDirs()
end note
:return namedTopologyDirectories!=null ? Arrays.asList(namedTopologyDirectories):Collections.emptyList();;
stop
end
start
note right
StringparseNamedTopologyFromDirectory(final StringdirName)
end note
:return dirName.substring(2,dirName.length()-2);;
stop
end
start
note right
FileLocktryLock(final FileChannelchannel)
end note
:return channel.tryLock();;
stop
:return null ;;
stop
end
start
note right
constructor TaskDirectory(final Filefile,
final StringnamedTopology)
end note
:this.file=file;
:this.namedTopology=namedTopology;
end
start
note right
Filefile()
end note
:return file;;
stop
end
start
note right
StringnamedTopology()
end note
:return namedTopology;;
stop
end
start
note right
booleanequals(final Objecto)
end note
if (this==o) then (yes) 
:return true;;
stop
endif 
if (o==null ||getClass()!=o.getClass()) then (yes) 
:return false;;
stop
endif 
:return file.equals(that.file)&&Objects.equals(namedTopology,
that.namedTopology);;
stop
end
start
note right
inthashCode()
end note
:return Objects.hash(file,namedTopology);;
stop
end
@enduml

PlantUML version 1.2022.7(Mon Aug 22 17:01:30 UTC 2022)
(GPL source distribution)
Java Runtime: OpenJDK Runtime Environment
JVM: OpenJDK 64-Bit Server VM
Default Encoding: UTF-8
Language: en
Country: null
--></g></svg>