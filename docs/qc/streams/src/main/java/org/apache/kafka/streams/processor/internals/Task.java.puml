@startuml
!pragma layout smetana
java.lang.Object <|-- org.apache.kafka.streams.processor.internals.Task
class org.apache.kafka.streams.processor.internals.Task {
+  initializeIfNeeded()
+  addPartitionsForOffsetReset(Set<TopicPartition>)
+  completeRestoration(java.util.function.Consumer<Set<TopicPartition>>)
+  suspend()
+  resume()
+  closeDirty()
+  closeClean()
+  updateInputPartitions(Set<TopicPartition>;Map<String,List<String>>)
+  maybeCheckpoint(boolean)
+  markChangelogAsCorrupted(Collection<TopicPartition>)
+  revive()
+  closeCleanAndRecycleState()
+  addRecords(TopicPartition;Iterable<ConsumerRecord<byte[],byte[]>>)
+  process(long)
+  recordProcessBatchTime(long)
+  recordProcessTimeRatioAndBufferSize(long;long)
+  maybePunctuateStreamTime()
+  maybePunctuateSystemTime()
+  prepareCommit()
+  postCommit(boolean)
+  purgeableOffsets()
+  maybeInitTaskTimeoutOrThrow(long;Exception)
+  clearTaskTimeout()
+  id()
+  isActive()
+  inputPartitions()
+  changelogPartitions()
+  state()
+  needsInitializationOrRestoration()
+  commitNeeded()
+  commitRequested()
+  getStore(String)
+  changelogOffsets()
+  committedOffsets()
+  highWaterMark()
+  timeCurrentIdlingStarted()
} 
@enduml
