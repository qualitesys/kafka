@startuml
!pragma layout smetana
AbstractConfig <|-- org.apache.kafka.streams.TopologyConfig
class org.apache.kafka.streams.TopologyConfig {
+  TopologyConfig(StreamsConfig)
+  TopologyConfig(String;StreamsConfig;Properties)
+  parseStoreType()
+  isNamedTopology()
+  isTopologyOverride(String;Properties)
+  getTaskConfig()
} 
@enduml
