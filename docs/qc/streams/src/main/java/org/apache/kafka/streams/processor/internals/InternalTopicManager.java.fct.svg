<?xml version="1.0" encoding="UTF-8" standalone="no"?><svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" contentStyleType="text/css" height="544px" preserveAspectRatio="none" style="width:521px;height:544px;background:#000000;" version="1.1" viewBox="0 0 521 544" width="521px" zoomAndPan="magnify"><defs/><g><rect fill="#1D2421" height="1" style="stroke:#1D2421;stroke-width:1.0;" width="1" x="0" y="0"/><text fill="#33FF02" font-family="sans-serif" font-size="12" font-style="italic" font-weight="bold" lengthAdjust="spacing" textLength="127" x="5" y="17">PlantUML 1.2022.7</text><rect fill="#33FF02" height="21.2969" style="stroke:#33FF02;stroke-width:1.0;" width="509" x="5" y="26.9688"/><text fill="#000000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="188" x="6" y="41.9688">[From string (line 319) ]</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="5" x="5" y="62.2656"> </text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="79" x="5" y="78.5625">@startuml</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="190" x="5" y="94.8594">!pragma layout smetana</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="111.1563">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="127.4531">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="390" x="5" y="143.75">constructor InternalTopicManager(final Timetime,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="15" x="5" y="160.0469">...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="185" x="5" y="176.3438">... ( skipping 294 lines )</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="15" x="5" y="192.6406">...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="208.9375">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="225.2344">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="487" x="5" y="241.5313">Set&lt;String&gt;validateTopics(final Set&lt;String&gt;topicsToValidate,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="385" x="5" y="257.8281">final Map&lt;String,InternalTopicConfig&gt;topicsMap,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="307" x="5" y="274.125">final Set&lt;String&gt;tempUnknownTopics)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="71" x="5" y="290.4219">end note</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="504" x="5" y="306.7188">if (!topicsMap.keySet().containsAll(topicsToValidate)) then (yes)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="418" x="5" y="323.0156">:throw new IllegalStateException("The topics map "+</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="438" x="5" y="339.3125">topicsMap.keySet()+" does not contain all the topics "+</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="318" x="5" y="355.6094">topicsToValidate+" trying to validate.");;</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="40" x="5" y="371.9063">endif</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="365" x="5" y="388.2031">if (!numberOfPartitions.isPresent()) then (yes)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="499" x="5" y="404.5">:log.error("Found undefined number of partitions for topic {}",</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="95" x="5" y="420.7969">topicName);</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="414" x="5" y="437.0938">:throw new StreamsException("Topic "+topicName+</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="295" x="5" y="453.3906">" number of partitions not defined");;</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="40" x="5" y="469.6875">endif</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="477" x="5" y="485.9844">if (existedTopicPartition.containsKey(topicName)) then (yes)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="320" x="5" y="502.2813">if (!existedTopicPartition.get(topicName)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" text-decoration="wavy underline" textLength="358" x="5" y="518.5781">.equals(numberOfPartitions.get())) then (yes)</text><text fill="#FF0000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="106" x="10" y="534.875">Syntax Error?</text><!--MD5=[a54eb26cbcec17f4554d437cda81135c]
@startuml
!pragma layout smetana
start
note right
constructor InternalTopicManager(final Timetime,
final AdminadminClient,final StreamsConfigstreamsConfig)
end note
:this.time=time;
:this.adminClient=adminClient;
:log=logContext.logger(getClass());
:replicationFactor=streamsConfig.getInt(StreamsConfig.REPLICATION_FACTOR_CONFIG)
.shortValue();
:windowChangeLogAdditionalRetention=streamsConfig.getLong(StreamsConfig.WINDOW_STORE_CHANGE_LOG_ADDITIONAL_RETENTION_MS_CONFIG);
:retryBackOffMs=streamsConfig.getLong(StreamsConfig.RETRY_BACKOFF_MS_CONFIG);
:consumerConfig.put(ConsumerConfig.KEY_DESERIALIZER_CLASS_CONFIG,
ByteArrayDeserializer.class );
:consumerConfig.put(ConsumerConfig.VALUE_DESERIALIZER_CLASS_CONFIG,
ByteArrayDeserializer.class );
:retryTimeoutMs=new QuietConsumerConfig(consumerConfig)
.getInt(ConsumerConfig.MAX_POLL_INTERVAL_MS_CONFIG)/2L;
:log.debug("Configs:"+Utils.NL+"\t{} = {}"+
Utils.NL+"\t{} = {}",StreamsConfig.REPLICATION_FACTOR_CONFIG,
replicationFactor,StreamsConfig.WINDOW_STORE_CHANGE_LOG_ADDITIONAL_RETENTION_MS_CONFIG,
windowChangeLogAdditionalRetention);
if (entry.getValue()!=null ) then (yes) 
:defaultTopicConfigs.put(entry.getKey(),entry.getValue()
.toString());
endif 
end
start
note right
void addMissingTopic(final Stringtopic)
end note
:missingTopics.add(topic);
end
start
note right
Set<String>missingTopics()
end note
:return Collections.unmodifiableSet(missingTopics);;
stop
end
start
note right
void addMisconfiguration(final Stringtopic,
final Stringmessage)
end note
:misconfigurationsForTopics.computeIfAbsent(topic,
ignored->new ArrayList<>()).add(message);
end
start
note right
Map<String,List<String>>misconfigurationsForTopics()
end note
:return Collections.unmodifiableMap(misconfigurationsForTopics);;
stop
end
start
note right
ValidationResultvalidate(final Map<String,
InternalTopicConfig>topicConfigs)
end note
:log.info("Starting to validate internal topics {}.",
topicConfigs.keySet());
while (!topicDescriptionsStillToValidate.isEmpty()||!topicConfigsStillToValidate.isEmpty())
if (!topicDescriptionsStillToValidate.isEmpty()) then (yes) 
:descriptionsForTopic=describeTopicsResult.topicNameValues();
endif 
if (!topicConfigsStillToValidate.isEmpty()) then (yes) 
:configsForTopic=describeConfigsResult.values()
.entrySet().stream().collect(Collectors.toMap(entry->entry.getKey().name(),
Map.Entry::getValue));
endif 
while (!descriptionsForTopic.isEmpty()||!configsForTopic.isEmpty())
if (!descriptionsForTopic.isEmpty()) then (yes) 
:doValidateTopic(validationResult,descriptionsForTopic,
topicConfigs,topicDescriptionsStillToValidate,
(streamsSide,brokerSide)->validatePartitionCount(validationResult,
streamsSide,brokerSide));
endif 
if (!configsForTopic.isEmpty()) then (yes) 
:doValidateTopic(validationResult,configsForTopic,
topicConfigs,topicConfigsStillToValidate,
(streamsSide,brokerSide)->validateCleanupPolicy(validationResult,
streamsSide,brokerSide));
endif 
:maybeThrowTimeoutException(Arrays.asList(topicDescriptionsStillToValidate,
topicConfigsStillToValidate),deadline,String.format("Could not validate internal topics within %d milliseconds. "+
"This can happen if the Kafka cluster is temporarily not available.",
retryTimeoutMs));
if (!descriptionsForTopic.isEmpty()||!configsForTopic.isEmpty()) then (yes) 
:Utils.sleep(100);
endif 
endwhile
:maybeSleep(Arrays.asList(topicDescriptionsStillToValidate,
topicConfigsStillToValidate),deadline,"validated");
endwhile
:log.info("Completed validation of internal topics {}.",
topicConfigs.keySet());
:return validationResult;;
stop
end
if (!futuresForTopic.containsKey(topicName)) then (yes) 
:throw new IllegalStateException("Description results do not contain topics to validate. "+
BUG_ERROR_MESSAGE);;
endif 
if (future.isDone()) then (yes) 
:validator.accept(streamsSideTopicConfig,
brokerSideTopicConfig);
:topicsStillToValidate.remove(topicName);
if (cause instanceof UnknownTopicOrPartitionException) then (yes) 
:log.info("Internal topic {} is missing",
topicName);
:validationResult.addMissingTopic(topicName);
:topicsStillToValidate.remove(topicName);
else (no)
:log.error("Unexpected error during internal topic validation: ",
cause);
:throw new StreamsException(String.format("Could not validate internal topic %s for the following reason: ",
topicName),cause);;
endif 
:throw new InterruptException(interruptedException);;
:futuresForTopic.remove(topicName);
endif 
start
note right
void validatePartitionCount(final ValidationResultvalidationResult,
final InternalTopicConfigtopicConfig,final TopicDescriptiontopicDescription)
end note
if (actualPartitionCount!=requiredPartitionCount) then (yes) 
:validationResult.addMisconfiguration(topicName,"Internal topic "+
topicName+" requires "+requiredPartitionCount+
" partitions, "+"but the existing topic on the broker has "+
actualPartitionCount+" partitions.");
endif 
end
start
note right
void validateCleanupPolicy(final ValidationResultvalidationResult,
final InternalTopicConfigtopicConfig,final ConfigbrokerSideTopicConfig)
end note
if (topicConfig instanceof UnwindowedChangelogTopicConfig) then (yes) 
:validateCleanupPolicyForUnwindowedChangelogs(validationResult,
topicConfig,brokerSideTopicConfig);
else (no)
:throw new IllegalStateException("Internal topic "+
topicConfig.name()+" has unknown type.");;
endif 
end
start
note right
void validateCleanupPolicyForUnwindowedChangelogs(final ValidationResultvalidationResult,
final InternalTopicConfigtopicConfig,final ConfigbrokerSideTopicConfig)
end note
if (cleanupPolicy.contains(TopicConfig.CLEANUP_POLICY_DELETE)) then (yes) 
:validationResult.addMisconfiguration(topicName,"Cleanup policy ("+
TopicConfig.CLEANUP_POLICY_CONFIG+") of existing internal topic "+
topicName+" should not contain \""+TopicConfig.CLEANUP_POLICY_DELETE+
"\".");
endif 
end
start
note right
void validateCleanupPolicyForWindowedChangelogs(final ValidationResultvalidationResult,
final InternalTopicConfigtopicConfig,final ConfigbrokerSideTopicConfig)
end note
if (cleanupPolicy.contains(TopicConfig.CLEANUP_POLICY_DELETE)) then (yes) 
if (brokerSideRetentionMs<streamsSideRetentionMs) then (yes) 
:validationResult.addMisconfiguration(topicName,"Retention time ("+
TopicConfig.RETENTION_MS_CONFIG+") of existing internal topic "+
topicName+" is "+brokerSideRetentionMs+" but should be "+
streamsSideRetentionMs+" or larger.");
endif 
if (brokerSideRetentionBytes!=null ) then (yes) 
:validationResult.addMisconfiguration(topicName,"Retention byte ("+
TopicConfig.RETENTION_BYTES_CONFIG+") of existing internal topic "+
topicName+" is set but it should be unset.");
endif 
endif 
end
start
note right
void validateCleanupPolicyForRepartitionTopic(final ValidationResultvalidationResult,
final InternalTopicConfigtopicConfig,final ConfigbrokerSideTopicConfig)
end note
if (cleanupPolicy.contains(TopicConfig.CLEANUP_POLICY_COMPACT)) then (yes) 
:validationResult.addMisconfiguration(topicName,"Cleanup policy ("+
TopicConfig.CLEANUP_POLICY_CONFIG+") of existing internal topic "+
topicName+" should not contain \""+TopicConfig.CLEANUP_POLICY_COMPACT+
"\".");
endif 
end
start
note right
StringgetBrokerSideConfigValue(final ConfigbrokerSideTopicConfig,
final StringconfigName,final StringtopicName)
end note
if (brokerSideConfigEntry==null ) then (yes) 
:throw new IllegalStateException("The config "+
configName+" for topic "+topicName+" could not be "+
"retrieved from the brokers. "+BUG_ERROR_MESSAGE);;
endif 
:return brokerSideConfigEntry.value();;
stop
end
start
note right
Set<String>makeReady(final Map<String,InternalTopicConfig>topics)
end note
:log.debug("Starting to validate internal topics {} in partition assignor.",
topics);
while (!topicsNotReady.isEmpty())
:topicsNotReady=validateTopics(topicsNotReady,
topics,tempUnknownTopics);
:newlyCreatedTopics.addAll(topicsNotReady);
if (!topicsNotReady.isEmpty()) then (yes) 
if (tempUnknownTopics.contains(topicName)) then (yes) 
endif 
:log.debug("Going to create topic {} with {} partitions and config {}.",
internalTopicConfig.name(),internalTopicConfig.numberOfPartitions(),
topicConfig);
:newTopics.add(new NewTopic(internalTopicConfig.name(),
internalTopicConfig.numberOfPartitions(),
Optional.of(replicationFactor)).configs(topicConfig));
if (!newTopics.isEmpty()) then (yes) 
:createTopicResult.getValue().get();
:topicsNotReady.remove(topicName);
:Thread.currentThread().interrupt();
:log.error(INTERRUPTED_ERROR_MESSAGE,fatalException);
:throw new IllegalStateException(INTERRUPTED_ERROR_MESSAGE,
fatalException);;
if (cause instanceof TopicExistsException) then (yes) 
:log.info("Could not create topic {}. Topic is probably marked for deletion (number of partitions is unknown).\n"+
"Will retry to create this topic in {} ms (to let broker finish async delete operation first).\n"+
"Error message was: {}",topicName,retryBackOffMs,
cause.toString());
else (no)
:log.error("Unexpected error during topic creation for {}.\n"+
"Error message was: {}",topicName,cause.toString());
if (cause instanceof UnsupportedVersionException) then (yes) 
if (errorMessage!=null &&errorMessage.startsWith("Creating topics with default partitions/replication factor are only supported in CreateTopicRequest version 4+")) then (yes) 
:throw new StreamsException(String.format("Could not create topic %s,
 because brokers don't support configuration replication.factor=-1."+
" You can change the replication.factor config or upgrade your brokers to version 2.4 or newer to avoid this error.",
topicName));;
endif 
else (no)
:throw new StreamsException(String.format("Could not create topic %s.",
topicName),cause);;
endif 
endif 
:log.error("Creating topic {} timed out.\n"+
"Error message was: {}",topicName,retriableException.toString());
endif 
endif 
if (!topicsNotReady.isEmpty()) then (yes) 
:currentWallClockMs=time.milliseconds();
if (currentWallClockMs>=deadlineMs) then (yes) 
:log.error(timeoutError);
:throw new TimeoutException(timeoutError);;
endif 
:log.info("Topics {} could not be made ready. Will retry in {} milliseconds. Remaining time in milliseconds: {}",
topicsNotReady,retryBackOffMs,deadlineMs-currentWallClockMs);
:Utils.sleep(retryBackOffMs);
endif 
endwhile
:log.debug("Completed validating internal topics and created {}",
newlyCreatedTopics);
:return newlyCreatedTopics;;
stop
end
start
note right
Map<String,Integer>getNumPartitions(final Set<String>topics,
final Set<String>tempUnknownTopics)
end note
:log.debug("Trying to check if topics {} have been created with expected number of partitions.",
topics);
:existedTopicPartition.put(topicName,topicDescription.partitions()
.size());
:Thread.currentThread().interrupt();
:log.error(INTERRUPTED_ERROR_MESSAGE,fatalException);
:throw new IllegalStateException(INTERRUPTED_ERROR_MESSAGE,
fatalException);;
if (cause instanceof UnknownTopicOrPartitionException) then (yes) 
:log.debug("Topic {} is unknown or not found, hence not existed yet.\n"+
"Error message was: {}",topicName,cause.toString());
else (no)
:log.error("Unexpected error during topic description for {}.\n"+
"Error message was: {}",topicName,cause.toString());
:throw new StreamsException(String.format("Could not create topic %s.",
topicName),cause);;
endif 
:tempUnknownTopics.add(topicName);
:log.debug("Describing topic {} (to get number of partitions) timed out.\n"+
"Error message was: {}",topicName,retriableException.toString());
:return existedTopicPartition;;
stop
end
start
note right
Set<String>validateTopics(final Set<String>topicsToValidate,
final Map<String,InternalTopicConfig>topicsMap,
final Set<String>tempUnknownTopics)
end note
if (!topicsMap.keySet().containsAll(topicsToValidate)) then (yes) 
:throw new IllegalStateException("The topics map "+
topicsMap.keySet()+" does not contain all the topics "+
topicsToValidate+" trying to validate.");;
endif 
if (!numberOfPartitions.isPresent()) then (yes) 
:log.error("Found undefined number of partitions for topic {}",
topicName);
:throw new StreamsException("Topic "+topicName+
" number of partitions not defined");;
endif 
if (existedTopicPartition.containsKey(topicName)) then (yes) 
if (!existedTopicPartition.get(topicName)
.equals(numberOfPartitions.get())) then (yes) 
:log.error(errorMsg);
:throw new StreamsException(errorMsg);;
endif 
else (no)
:topicsToCreate.add(topicName);
endif 
:return topicsToCreate;;
stop
end
start
note right
void setup(final Map<String,InternalTopicConfig>topicConfigs)
end note
:log.info("Starting to setup internal topics {}.",
topicConfigs.keySet());
while (!topicStillToCreate.isEmpty())
:log.info("Going to create internal topics: "+
newTopics);
:processCreateTopicResults(createTopicsResult,
topicStillToCreate,createdTopics,deadline);
:maybeSleep(Collections.singletonList(topicStillToCreate),
deadline,"created");
endwhile
:log.info("Completed setup of internal topics {}.",
topicConfigs.keySet());
end
start
note right
void processCreateTopicResults(final CreateTopicsResultcreateTopicsResult,
final Set<String>topicStillToCreate,final Set<String>createdTopics,
final longdeadline)
end note
while (!createResultForTopic.isEmpty())
if (!createResultForTopic.containsKey(topicName)) then (yes) 
:cleanUpCreatedTopics(createdTopics);
:throw new IllegalStateException("Create topic results do not contain internal topic "+
topicName+" to setup. "+BUG_ERROR_MESSAGE);;
endif 
if (createResult.isDone()) then (yes) 
:createResult.get();
:createdTopics.add(topicName);
:topicStillToCreate.remove(topicName);
if (cause instanceof TopicExistsException) then (yes) 
:lastErrorsSeenForTopic.put(topicName,cause);
:log.info("Internal topic {} already exists. Topic is probably marked for deletion. "+
"Will retry to create this topic later (to let broker complete async delete operation first)",
topicName);
else (no)
:cleanUpCreatedTopics(createdTopics);
:log.error("Unexpected error during creation of internal topic: ",
cause);
:throw new StreamsException(String.format("Could not create internal topic %s for the following reason: ",
topicName),cause);;
endif 
:throw new InterruptException(interruptedException);;
:createResultForTopic.remove(topicName);
endif 
:maybeThrowTimeoutExceptionDuringSetup(topicStillToCreate,
createdTopics,lastErrorsSeenForTopic,deadline);
if (!createResultForTopic.isEmpty()) then (yes) 
:Utils.sleep(100);
endif 
endwhile
end
start
note right
void cleanUpCreatedTopics(final Set<String>topicsToCleanUp)
end note
:log.info("Starting to clean up internal topics {}.",
topicsToCleanUp);
while (!topicsStillToCleanup.isEmpty())
:log.info("Going to cleanup internal topics: "+
topicsStillToCleanup);
while (!deleteResultForTopic.isEmpty())
if (!deleteResultForTopic.containsKey(topicName)) then (yes) 
:throw new IllegalStateException("Delete topic results do not contain internal topic "+
topicName+" to clean up. "+BUG_ERROR_MESSAGE);;
endif 
if (deleteResult.isDone()) then (yes) 
:deleteResult.get();
:topicsStillToCleanup.remove(topicName);
if (cause instanceof UnknownTopicOrPartitionException) then (yes) 
:log.info("Internal topic {} to clean up is missing",
topicName);
else (no)
:log.error("Unexpected error during cleanup of internal topics: ",
cause);
:throw new StreamsException(String.format("Could not clean up internal topics %s, because during the cleanup "+
"of topic %s the following error occurred: ",
topicsStillToCleanup,topicName),cause);;
endif 
:throw new InterruptException(interruptedException);;
:deleteResultForTopic.remove(topicName);
endif 
:maybeThrowTimeoutException(Collections.singletonList(topicsStillToCleanup),
deadline,String.format("Could not cleanup internal topics within %d milliseconds. This can happen if the "+
"Kafka cluster is temporarily not available or the broker did not complete topic creation "+
"before the cleanup. The following internal topics could not be cleaned up: %s",
retryTimeoutMs,topicsStillToCleanup));
if (!deleteResultForTopic.isEmpty()) then (yes) 
:Utils.sleep(100);
endif 
endwhile
:maybeSleep(Collections.singletonList(topicsStillToCleanup),
deadline,"validated");
endwhile
:log.info("Completed cleanup of internal topics {}.",
topicsToCleanUp);
end
start
note right
void maybeThrowTimeoutException(final List<Set<String>>topicStillToProcess,
final longdeadline,final StringerrorMessage)
end note
if (topicStillToProcess.stream().anyMatch(resultSet->!resultSet.isEmpty())) then (yes) 
if (now>=deadline) then (yes) 
:log.error(errorMessage);
:throw new TimeoutException(errorMessage);;
endif 
endif 
end
start
note right
void maybeThrowTimeoutExceptionDuringSetup(final Set<String>topicStillToProcess,
final Set<String>createdTopics,final Map<String,
Throwable>lastErrorsSeenForTopic,final longdeadline)
end note
if (topicStillToProcess.stream().anyMatch(resultSet->!resultSet.isEmpty())) then (yes) 
if (now>=deadline) then (yes) 
:cleanUpCreatedTopics(createdTopics);
:log.error(errorMessage);
:throw new TimeoutException(errorMessage);;
endif 
endif 
end
start
note right
void maybeSleep(final List<Set<String>>resultSetsStillToValidate,
final longdeadline,final Stringaction)
end note
if (resultSetsStillToValidate.stream().anyMatch(resultSet->!resultSet.isEmpty())) then (yes) 
:log.info("Internal topics {} could not be {}. Will retry in {} milliseconds. Remaining time in milliseconds: {}",
resultSetsStillToValidate.stream().flatMap(Collection::stream).collect(Collectors.toSet()),
action,retryBackOffMs,deadline-now);
:Utils.sleep(retryBackOffMs);
endif 
end
@enduml

PlantUML version 1.2022.7(Mon Aug 22 17:01:30 UTC 2022)
(GPL source distribution)
Java Runtime: OpenJDK Runtime Environment
JVM: OpenJDK 64-Bit Server VM
Default Encoding: UTF-8
Language: en
Country: null
--></g></svg>