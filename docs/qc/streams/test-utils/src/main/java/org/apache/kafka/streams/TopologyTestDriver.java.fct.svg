<?xml version="1.0" encoding="UTF-8" standalone="no"?><svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" contentStyleType="text/css" height="544px" preserveAspectRatio="none" style="width:796px;height:544px;background:#000000;" version="1.1" viewBox="0 0 796 544" width="796px" zoomAndPan="magnify"><defs/><g><rect fill="#19241B" height="1" style="stroke:#19241B;stroke-width:1.0;" width="1" x="0" y="0"/><text fill="#33FF02" font-family="sans-serif" font-size="12" font-style="italic" font-weight="bold" lengthAdjust="spacing" textLength="127" x="5" y="17">PlantUML 1.2022.7</text><rect fill="#33FF02" height="21.2969" style="stroke:#33FF02;stroke-width:1.0;" width="784" x="5" y="26.9688"/><text fill="#000000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="188" x="6" y="41.9688">[From string (line 247) ]</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="5" x="5" y="62.2656"> </text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="79" x="5" y="78.5625">@startuml</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="190" x="5" y="94.8594">!pragma layout smetana</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="111.1563">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="127.4531">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="446" x="5" y="143.75">constructor TopologyTestDriver(final Topologytopology)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="15" x="5" y="160.0469">...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="185" x="5" y="176.3438">... ( skipping 222 lines )</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="15" x="5" y="192.6406">...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="208.9375">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="225.2344">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="561" x="5" y="241.5313">void processGlobalRecord(final TopicPartitionglobalInputTopicPartition,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="409" x="5" y="257.8281">final longtimestamp,final byte[]key,final byte[]value,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="175" x="5" y="274.125">final Headersheaders)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="71" x="5" y="290.4219">end note</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="660" x="5" y="306.7188">:globalStateTask.update(new ConsumerRecord&lt;&gt;(globalInputTopicPartition.topic(),</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="784" x="5" y="323.0156">globalInputTopicPartition.partition(),offsetsByTopicOrPatternPartition.get(globalInputTopicPartition)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="502" x="5" y="339.3125">.incrementAndGet()-1,timestamp,TimestampType.CREATE_TIME,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="415" x="5" y="355.6094">key==null ? ConsumerRecord.NULL_SIZE:key.length,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="443" x="5" y="371.9063">value==null ? ConsumerRecord.NULL_SIZE:value.length,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="299" x="5" y="388.2031">key,value,headers,Optional.empty()));</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="234" x="5" y="404.5">:globalStateTask.flushState();</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="30" x="5" y="420.7969">end</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="437.0938">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="453.3906">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="585" x="5" y="469.6875">void validateSourceTopicNameRegexPattern(final StringinputRecordTopic)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="71" x="5" y="485.9844">end note</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="681" x="5" y="502.2813">if (!sourceTopicName.equals(inputRecordTopic)&amp;&amp;Pattern.compile(sourceTopicName)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" text-decoration="wavy underline" textLength="397" x="5" y="518.5781">.matcher(inputRecordTopic).matches()) then (yes)</text><text fill="#FF0000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="106" x="10" y="534.875">Syntax Error?</text><!--MD5=[108819e62c96f9a6b21f61041757ab72]
@startuml
!pragma layout smetana
start
note right
constructor TopologyTestDriver(final Topologytopology)
end note
:this(topology,new Properties());
end
start
note right
constructor TopologyTestDriver(final Topologytopology,
final Propertiesconfig)
end note
:this(topology,config,null );
end
start
note right
constructor TopologyTestDriver(final Topologytopology,
final InstantinitialWallClockTimeMs)
end note
:this(topology,new Properties(),initialWallClockTimeMs);
end
start
note right
constructor TopologyTestDriver(final Topologytopology,
final Propertiesconfig,final InstantinitialWallClockTime)
end note
:this(topology.internalTopologyBuilder,config,
initialWallClockTime==null ? System.currentTimeMillis():initialWallClockTime.toEpochMilli());
end
start
note right
constructor TopologyTestDriver(final InternalTopologyBuilderbuilder,
final Propertiesconfig,final longinitialWallClockTimeMs)
end note
:configCopy.putAll(config);
:configCopy.putIfAbsent(StreamsConfig.BOOTSTRAP_SERVERS_CONFIG,
"dummy-bootstrap-host:0");
:configCopy.putIfAbsent(StreamsConfig.APPLICATION_ID_CONFIG,"dummy-topology-test-driver-app-id-"+
ThreadLocalRandom.current().nextInt());
:logIfTaskIdleEnabled(streamsConfig);
:logContext=new LogContext("topology-test-driver ");
:mockWallClockTime=new MockTime(initialWallClockTimeMs);
:processingMode=StreamsConfigUtils.processingMode(streamsConfig);
:setupTopology(builder,streamsConfig);
:consumer=new MockConsumer<>(OffsetResetStrategy.EARLIEST);
:producer=new MockProducer<byte[],byte[]>(true,
bytesSerializer,bytesSerializer){@Override public List<PartitionInfo>partitionsFor(final Stringtopic){return Collections.singletonList(new PartitionInfo(topic,PARTITION_ID,null ,null ,null ));}};
:testDriverProducer=new TestDriverProducer(streamsConfig,
new KafkaClientSupplier(){@Override public Producer<byte[],
byte[]>getProducer(final Map<String,Object>config){return producer;}@Override public Consumer<byte[],
byte[]>getConsumer(final Map<String,Object>config){throw new IllegalStateException();}@Override public Consumer<byte[],
byte[]>getRestoreConsumer(final Map<String,
Object>config){throw new IllegalStateException();}@Override public Consumer<byte[],
byte[]>getGlobalConsumer(final Map<String,
Object>config){throw new IllegalStateException();}},
logContext,mockWallClockTime);
:setupGlobalTask(mockWallClockTime,streamsConfig,
streamsMetrics,cache);
:setupTask(streamsConfig,streamsMetrics,cache,
internalTopologyBuilder.topologyConfigs()
.getTaskConfig());
end
start
note right
void logIfTaskIdleEnabled(final StreamsConfigstreamsConfig)
end note
if (taskIdleTime>0) then (yes) 
:log.info("Detected {} config in use with TopologyTestDriver (set to {}ms)."+
" This means you might need to use TopologyTestDriver#advanceWallClockTime()"+
" or enqueue records on all partitions to allow Steams to make progress."+
" TopologyTestDriver will log a message each time it cannot process enqueued"+
" records due to {}.",StreamsConfig.MAX_TASK_IDLE_MS_CONFIG,
taskIdleTime,StreamsConfig.MAX_TASK_IDLE_MS_CONFIG);
endif 
end
start
note right
StreamsMetricsImplsetupMetrics(final StreamsConfigstreamsConfig)
end note
:metrics=new Metrics(metricConfig,mockWallClockTime);
:TaskMetrics.droppedRecordsSensor(threadId,
TASK_ID.toString(),streamsMetrics);
:return streamsMetrics;;
stop
end
start
note right
void setupTopology(final InternalTopologyBuilderbuilder,
final StreamsConfigstreamsConfig)
end note
:internalTopologyBuilder=builder;
:internalTopologyBuilder.rewriteTopology(streamsConfig);
:processorTopology=internalTopologyBuilder.buildTopology();
:globalTopology=internalTopologyBuilder.buildGlobalStateTopology();
:partitionsByInputTopic.put(topic,tp);
:offsetsByTopicOrPatternPartition.put(tp,
new AtomicLong());
:stateDirectory=new StateDirectory(streamsConfig,
mockWallClockTime,internalTopologyBuilder.hasPersistentStores(),
false);
end
start
note right
void setupGlobalTask(final TimemockWallClockTime,
final StreamsConfigstreamsConfig,final StreamsMetricsImplstreamsMetrics,
final ThreadCachecache)
end note
if (globalTopology!=null ) then (yes) 
:globalPartitionsByInputTopic.put(topicName,
partition);
:offsetsByTopicOrPatternPartition.put(partition,
new AtomicLong());
:globalConsumer.updatePartitions(topicName,
Collections.singletonList(new PartitionInfo(topicName,
0,null ,null ,null )));
:globalConsumer.updateBeginningOffsets(Collections.singletonMap(partition,
0L));
:globalConsumer.updateEndOffsets(Collections.singletonMap(partition,
0L));
:globalStateManager=new GlobalStateManagerImpl(logContext,
mockWallClockTime,globalTopology,globalConsumer,
stateDirectory,stateRestoreListener,streamsConfig);
:globalStateManager.setGlobalProcessorContext(globalProcessorContext);
:globalStateTask=new GlobalStateUpdateTask(logContext,
globalTopology,globalProcessorContext,globalStateManager,
new LogAndContinueExceptionHandler());
:globalStateTask.initialize();
:globalProcessorContext.setRecordContext(null );
else (no)
:globalStateManager=null ;
:globalStateTask=null ;
endif 
end
start
note right
void setupTask(final StreamsConfigstreamsConfig,
final StreamsMetricsImplstreamsMetrics,final ThreadCachecache,
final TaskConfigtaskConfig)
end note
if (!partitionsByInputTopic.isEmpty()) then (yes) 
:consumer.assign(partitionsByInputTopic.values());
:startOffsets.put(topicPartition,0L);
:consumer.updateBeginningOffsets(startOffsets);
:task=new StreamTask(TASK_ID,new HashSet<>(partitionsByInputTopic.values()),
processorTopology,consumer,taskConfig,streamsMetrics,
stateDirectory,cache,mockWallClockTime,stateManager,
recordCollector,context,logContext);
:task.initializeIfNeeded();
:task.completeRestoration(noOpResetter->{});
:task.processorContext().setRecordContext(null );
else (no)
:task=null ;
endif 
end
start
note right
Map<MetricName,?  extends Metric>metrics()
end note
:return Collections.unmodifiableMap(metrics.metrics());;
stop
end
start
note right
void pipeRecord(final StringtopicName,final longtimestamp,
final byte[]key,final byte[]value,final Headersheaders)
end note
if (inputTopicOrPatternPartition==null &&globalInputTopicPartition==null ) then (yes) 
:throw new IllegalArgumentException("Unknown topic: "+
topicName);;
endif 
if (inputTopicOrPatternPartition!=null ) then (yes) 
:enqueueTaskRecord(topicName,inputTopicOrPatternPartition,
timestamp,key,value,headers);
:completeAllProcessableWork();
endif 
if (globalInputTopicPartition!=null ) then (yes) 
:processGlobalRecord(globalInputTopicPartition,
timestamp,key,value,headers);
endif 
end
start
note right
void enqueueTaskRecord(final StringinputTopic,
final TopicPartitiontopicOrPatternPartition,
final longtimestamp,final byte[]key,final byte[]value,
final Headersheaders)
end note
:task.addRecords(topicOrPatternPartition,
Collections.singleton(new ConsumerRecord<>(inputTopic,
topicOrPatternPartition.partition(),offset,
timestamp,TimestampType.CREATE_TIME,key==null ? ConsumerRecord.NULL_SIZE:key.length,
value==null ? ConsumerRecord.NULL_SIZE:value.length,
key,value,headers,Optional.empty())));
end
start
note right
void completeAllProcessableWork()
end note
:captureOutputsAndReEnqueueInternalResults();
if (task!=null ) then (yes) 
while (task.hasRecordsQueued()&&task.isProcessable(mockWallClockTime.milliseconds()))
:task.process(mockWallClockTime.milliseconds());
:task.maybePunctuateStreamTime();
:commit(task.prepareCommit());
:task.postCommit(true);
:captureOutputsAndReEnqueueInternalResults();
endwhile
if (task.hasRecordsQueued()) then (yes) 
:log.info("Due to the {} configuration, there are currently some records"+
" that cannot be processed. Advancing wall-clock time or"+
" enqueuing records on the empty topics will allow"+
" Streams to process more.",StreamsConfig.MAX_TASK_IDLE_MS_CONFIG);
endif 
endif 
end
start
note right
void commit(final Map<TopicPartition,OffsetAndMetadata>offsets)
end note
if (processingMode==EXACTLY_ONCE_ALPHA||processingMode==EXACTLY_ONCE_V2) then (yes) 
:testDriverProducer.commitTransaction(offsets,
new ConsumerGroupMetadata("dummy-app-id"));
else (no)
:consumer.commitSync(offsets);
endif 
end
start
note right
void processGlobalRecord(final TopicPartitionglobalInputTopicPartition,
final longtimestamp,final byte[]key,final byte[]value,
final Headersheaders)
end note
:globalStateTask.update(new ConsumerRecord<>(globalInputTopicPartition.topic(),
globalInputTopicPartition.partition(),offsetsByTopicOrPatternPartition.get(globalInputTopicPartition)
.incrementAndGet()-1,timestamp,TimestampType.CREATE_TIME,
key==null ? ConsumerRecord.NULL_SIZE:key.length,
value==null ? ConsumerRecord.NULL_SIZE:value.length,
key,value,headers,Optional.empty()));
:globalStateTask.flushState();
end
start
note right
void validateSourceTopicNameRegexPattern(final StringinputRecordTopic)
end note
if (!sourceTopicName.equals(inputRecordTopic)&&Pattern.compile(sourceTopicName)
.matcher(inputRecordTopic).matches()) then (yes) 
:throw new TopologyException("Topology add source of type String for topic: "+
sourceTopicName+" cannot contain regex pattern for input record topic: "+
inputRecordTopic+" and hence cannot process the message.");;
endif 
end
start
note right
TopicPartitiongetInputTopicOrPatternPartition(final StringtopicName)
end note
if (!internalTopologyBuilder.fullSourceTopicNames()
.isEmpty()) then (yes) 
:validateSourceTopicNameRegexPattern(topicName);
endif 
if (topicPartition==null ) then (yes) 
if (Pattern.compile(entry.getKey()).matcher(topicName)
.matches()) then (yes) 
:return entry.getValue();;
stop
endif 
endif 
:return topicPartition;;
stop
end
start
note right
void captureOutputsAndReEnqueueInternalResults()
end note
:producer.clear();
:outputRecordsByTopic.computeIfAbsent(record.topic(),
k->new LinkedList<>()).add(record);
if (inputTopicOrPatternPartition!=null ) then (yes) 
:enqueueTaskRecord(outputTopicName,inputTopicOrPatternPartition,
record.timestamp(),record.key(),record.value(),
record.headers());
endif 
if (globalInputTopicPartition!=null ) then (yes) 
:processGlobalRecord(globalInputTopicPartition,
record.timestamp(),record.key(),record.value(),
record.headers());
endif 
end
start
note right
void advanceWallClockTime(final Durationadvance)
end note
:Objects.requireNonNull(advance,"advance cannot be null");
:mockWallClockTime.sleep(advance.toMillis());
if (task!=null ) then (yes) 
:task.maybePunctuateSystemTime();
:commit(task.prepareCommit());
:task.postCommit(true);
endif 
:completeAllProcessableWork();
end
start
note right
Queue<ProducerRecord<byte[],byte[]>>getRecordsQueue(final StringtopicName)
end note
if (outputRecords==null &&!processorTopology.sinkTopics()
.contains(topicName)) then (yes) 
:log.warn("Unrecognized topic: {}, this can occur if dynamic routing is used and no output has been "+
"sent to this topic yet. If not using a TopicNameExtractor, check that the output topic "+
"is correct.",topicName);
endif 
:return outputRecords;;
stop
end
:return new TestInputTopic<>(this,topicName,
keySerializer,valueSerializer,Instant.now(),
Duration.ZERO);;
stop
:return new TestInputTopic<>(this,topicName,
keySerializer,valueSerializer,startTimestamp,
autoAdvance);;
stop
:return new TestOutputTopic<>(this,topicName,
keyDeserializer,valueDeserializer);;
stop
start
note right
Set<String>producedTopicNames()
end note
:return Collections.unmodifiableSet(outputRecordsByTopic.keySet());;
stop
end
start
note right
ProducerRecord<byte[],byte[]>readRecord(final Stringtopic)
end note
if (outputRecords==null ) then (yes) 
:return null ;;
stop
endif 
:return outputRecords.poll();;
stop
end
if (outputRecords==null ) then (yes) 
:throw new NoSuchElementException("Uninitialized topic: "+
topic);;
endif 
if (record==null ) then (yes) 
:throw new NoSuchElementException("Empty topic: "+
topic);;
endif 
:return new TestRecord<>(key,value,record.headers(),
record.timestamp());;
stop
if (time!=null ) then (yes) 
:timestamp=time.toEpochMilli();
else (no)
:throw new IllegalStateException("Provided `TestRecord` does not have a timestamp and no timestamp overwrite was provided via `time` parameter.");;
endif 
:pipeRecord(topic,timestamp,serializedKey,
serializedValue,record.headers());
start
note right
longgetQueueSize(final Stringtopic)
end note
if (queue==null ) then (yes) 
:return 0;;
stop
endif 
:return queue.size();;
stop
end
start
note right
booleanisEmpty(final Stringtopic)
end note
:return getQueueSize(topic)==0;;
stop
end
start
note right
Map<String,StateStore>getAllStateStores()
end note
:allStores.put(storeName,getStateStore(storeName,
false));
:return allStores;;
stop
end
start
note right
StateStoregetStateStore(final Stringname)
end note
:return getStateStore(name,true);;
stop
end
start
note right
StateStoregetStateStore(final Stringname,
final booleanthrowForBuiltInStores)
end note
if (task!=null ) then (yes) 
if (stateStore!=null ) then (yes) 
if (throwForBuiltInStores) then (yes) 
:throwIfBuiltInStore(stateStore);
endif 
:return stateStore;;
stop
endif 
endif 
if (globalStateManager!=null ) then (yes) 
if (stateStore!=null ) then (yes) 
if (throwForBuiltInStores) then (yes) 
:throwIfBuiltInStore(stateStore);
endif 
:return stateStore;;
stop
endif 
endif 
:return null ;;
stop
end
start
note right
void throwIfBuiltInStore(final StateStorestateStore)
end note
if (stateStore instanceof TimestampedKeyValueStore) then (yes) 
:throw new IllegalArgumentException("Store "+
stateStore.name()+" is a timestamped key-value store and should be accessed via `getTimestampedKeyValueStore()`");;
endif 
if (stateStore instanceof ReadOnlyKeyValueStore) then (yes) 
:throw new IllegalArgumentException("Store "+
stateStore.name()+" is a key-value store and should be accessed via `getKeyValueStore()`");;
endif 
if (stateStore instanceof TimestampedWindowStore) then (yes) 
:throw new IllegalArgumentException("Store "+
stateStore.name()+" is a timestamped window store and should be accessed via `getTimestampedWindowStore()`");;
endif 
if (stateStore instanceof ReadOnlyWindowStore) then (yes) 
:throw new IllegalArgumentException("Store "+
stateStore.name()+" is a window store and should be accessed via `getWindowStore()`");;
endif 
if (stateStore instanceof ReadOnlySessionStore) then (yes) 
:throw new IllegalArgumentException("Store "+
stateStore.name()+" is a session store and should be accessed via `getSessionStore()`");;
endif 
end
if (store instanceof TimestampedKeyValueStore) then (yes) 
:log.info("Method #getTimestampedKeyValueStore() should be used to access a TimestampedKeyValueStore.");
:return new KeyValueStoreFacade<>((TimestampedKeyValueStore<K,
V>)store);;
stop
endif 
:return store instanceof KeyValueStore? (KeyValueStore<K,
V>)store:null ;;
stop
:return store instanceof TimestampedKeyValueStore? (TimestampedKeyValueStore<K,
V>)store:null ;;
stop
if (store instanceof TimestampedWindowStore) then (yes) 
:log.info("Method #getTimestampedWindowStore() should be used to access a TimestampedWindowStore.");
:return new WindowStoreFacade<>((TimestampedWindowStore<K,
V>)store);;
stop
endif 
:return store instanceof WindowStore? (WindowStore<K,
V>)store:null ;;
stop
:return store instanceof TimestampedWindowStore? (TimestampedWindowStore<K,
V>)store:null ;;
stop
:return store instanceof SessionStore? (SessionStore<K,
V>)store:null ;;
stop
start
note right
void close()
end note
if (task!=null ) then (yes) 
:task.suspend();
:task.prepareCommit();
:task.postCommit(true);
:task.closeClean();
endif 
if (globalStateTask!=null ) then (yes) 
:globalStateTask.close(false);
endif 
:completeAllProcessableWork();
if (task!=null &&task.hasRecordsQueued()) then (yes) 
:log.warn("Found some records that cannot be processed due to the"+
" {} configuration during TopologyTestDriver#close().",
StreamsConfig.MAX_TASK_IDLE_MS_CONFIG);
endif 
if (processingMode==AT_LEAST_ONCE) then (yes) 
:producer.close();
endif 
:stateDirectory.clean();
end
start
note right
void register(final TopicPartitionpartition,
final ProcessorStateManagerstateManager)
end note
:restoringPartitions.add(partition);
end
start
note right
void unregister(final Collection<TopicPartition>partitions)
end note
:restoringPartitions.removeAll(partitions);
end
start
note right
constructor MockTime(final longstartTimestampMs)
end note
:this.timeMs=new AtomicLong(startTimestampMs);
:this.highResTimeNs=new AtomicLong(startTimestampMs*1000L*1000L);
end
start
note right
longmilliseconds()
end note
:return timeMs.get();;
stop
end
start
note right
longnanoseconds()
end note
:return highResTimeNs.get();;
stop
end
start
note right
longhiResClockMs()
end note
:return TimeUnit.NANOSECONDS.toMillis(nanoseconds());;
stop
end
start
note right
void sleep(final longms)
end note
if (ms<0) then (yes) 
:throw new IllegalArgumentException("Sleep ms cannot be negative.");;
endif 
:timeMs.addAndGet(ms);
:highResTimeNs.addAndGet(TimeUnit.MILLISECONDS.toNanos(ms));
end
start
note right
void waitObject(final Objectobj,final Supplier<Boolean>condition,
final longtimeoutMs)
end note
:throw new UnsupportedOperationException();;
end
start
note right
constructor KeyValueStoreFacade(final TimestampedKeyValueStore<K,
V>inner)
end note
:super (inner);
end
start
note right
void init(final ProcessorContextcontext,final StateStoreroot)
end note
:inner.init(context,root);
end
start
note right
void init(final StateStoreContextcontext,
final StateStoreroot)
end note
:inner.init(context,root);
end
start
note right
void put(final Kkey,final Vvalue)
end note
:inner.put(key,ValueAndTimestamp.make(value,
ConsumerRecord.NO_TIMESTAMP));
end
start
note right
VputIfAbsent(final Kkey,final Vvalue)
end note
:return getValueOrNull(inner.putIfAbsent(key,
ValueAndTimestamp.make(value,ConsumerRecord.NO_TIMESTAMP)));;
stop
end
start
note right
void putAll(final List<KeyValue<K,V>>entries)
end note
:inner.put(entry.key,ValueAndTimestamp.make(entry.value,
ConsumerRecord.NO_TIMESTAMP));
end
start
note right
Vdelete(final Kkey)
end note
:return getValueOrNull(inner.delete(key));;
stop
end
start
note right
void flush()
end note
:inner.flush();
end
start
note right
void close()
end note
:inner.close();
end
start
note right
Stringname()
end note
:return inner.name();;
stop
end
start
note right
booleanpersistent()
end note
:return inner.persistent();;
stop
end
start
note right
booleanisOpen()
end note
:return inner.isOpen();;
stop
end
start
note right
PositiongetPosition()
end note
:return inner.getPosition();;
stop
end
start
note right
constructor WindowStoreFacade(final TimestampedWindowStore<K,
V>store)
end note
:super (store);
end
start
note right
void init(final ProcessorContextcontext,final StateStoreroot)
end note
:inner.init(context,root);
end
start
note right
void init(final StateStoreContextcontext,
final StateStoreroot)
end note
:inner.init(context,root);
end
start
note right
void put(final Kkey,final Vvalue,final longwindowStartTimestamp)
end note
:inner.put(key,ValueAndTimestamp.make(value,
ConsumerRecord.NO_TIMESTAMP),windowStartTimestamp);
end
start
note right
WindowStoreIterator<V>fetch(final Kkey,final longtimeFrom,
final longtimeTo)
end note
:return fetch(key,Instant.ofEpochMilli(timeFrom),
Instant.ofEpochMilli(timeTo));;
stop
end
start
note right
WindowStoreIterator<V>backwardFetch(final Kkey,
final longtimeFrom,final longtimeTo)
end note
:return backwardFetch(key,Instant.ofEpochMilli(timeFrom),
Instant.ofEpochMilli(timeTo));;
stop
end
start
note right
KeyValueIterator<Windowed<K>,V>fetch(final KkeyFrom,
final KkeyTo,final longtimeFrom,final longtimeTo)
end note
:return fetch(keyFrom,keyTo,Instant.ofEpochMilli(timeFrom),
Instant.ofEpochMilli(timeTo));;
stop
end
start
note right
KeyValueIterator<Windowed<K>,V>backwardFetch(final KkeyFrom,
final KkeyTo,final longtimeFrom,final longtimeTo)
end note
:return backwardFetch(keyFrom,keyTo,Instant.ofEpochMilli(timeFrom),
Instant.ofEpochMilli(timeTo));;
stop
end
start
note right
KeyValueIterator<Windowed<K>,V>fetchAll(final longtimeFrom,
final longtimeTo)
end note
:return fetchAll(Instant.ofEpochMilli(timeFrom),
Instant.ofEpochMilli(timeTo));;
stop
end
start
note right
KeyValueIterator<Windowed<K>,V>backwardFetchAll(final longtimeFrom,
final longtimeTo)
end note
:return backwardFetchAll(Instant.ofEpochMilli(timeFrom),
Instant.ofEpochMilli(timeTo));;
stop
end
start
note right
void flush()
end note
:inner.flush();
end
start
note right
void close()
end note
:inner.close();
end
start
note right
Stringname()
end note
:return inner.name();;
stop
end
start
note right
booleanpersistent()
end note
:return inner.persistent();;
stop
end
start
note right
booleanisOpen()
end note
:return inner.isOpen();;
stop
end
start
note right
PositiongetPosition()
end note
:return inner.getPosition();;
stop
end
start
note right
constructor TestDriverProducer(final StreamsConfigconfig,
final KafkaClientSupplierclientSupplier,final LogContextlogContext,
final Timetime)
end note
:super (config,"TopologyTestDriver-StreamThread-1",
clientSupplier,new TaskId(0,0),UUID.randomUUID(),
logContext,time);
end
start
note right
void commitTransaction(final Map<TopicPartition,
OffsetAndMetadata>offsets,final ConsumerGroupMetadataconsumerGroupMetadata)
end note
:super .commitTransaction(offsets,consumerGroupMetadata);
end
@enduml

PlantUML version 1.2022.7(Mon Aug 22 17:01:30 UTC 2022)
(GPL source distribution)
Java Runtime: OpenJDK Runtime Environment
JVM: OpenJDK 64-Bit Server VM
Default Encoding: UTF-8
Language: en
Country: null
--></g></svg>