@startuml
!pragma layout smetana
WorkerTask <|-- org.apache.kafka.connect.runtime.WorkerSinkTask
class org.apache.kafka.connect.runtime.WorkerSinkTask {
+  WorkerSinkTask(ConnectorTaskId;SinkTask;TaskStatus.Listener;TargetState;WorkerConfig;ClusterConfigState;ConnectMetrics;Converter;Converter;HeaderConverter;TransformationChain<SinkRecord>;KafkaConsumer<byte[],byte[]>;ClassLoader;Time;RetryWithToleranceOperator;WorkerErrantRecordReporter;StatusBackingStore)
+  initialize(TaskConfig)
+  stop()
+  close()
+  removeMetrics()
+  transitionTo(TargetState)
+  execute()
+  iteration()
+  onCommitCompleted(Throwable;long;Map<TopicPartition,OffsetAndMetadata>)
+  commitFailures()
+  initializeAndStart()
+  poll(long)
+  isCommitting()
+  doCommitSync(Map<TopicPartition,OffsetAndMetadata>;int)
+  doCommitAsync(Map<TopicPartition,OffsetAndMetadata>;int)
+  doCommit(Map<TopicPartition,OffsetAndMetadata>;boolean;int)
+  commitOffsets(long;boolean)
+  commitOffsets(long;boolean;Collection<TopicPartition>)
+  toString()
+  pollConsumer(long)
+  convertMessages(ConsumerRecords<byte[],byte[]>)
+  convertAndTransformRecord(ConsumerRecord<byte[],byte[]>)
+  convertHeadersFor(ConsumerRecord<byte[],byte[]>)
+  workerErrantRecordReporter()
+  resumeAll()
+  pauseAll()
+  deliverMessages()
+  rewind()
+  openPartitions(Collection<TopicPartition>)
+  closeAllPartitions()
+  closePartitions(Collection<TopicPartition>;boolean)
+  updatePartitionCount()
+  recordBatch(int)
+  recordCommitFailure(long;Throwable)
+  recordCommitSuccess(long)
+  sinkTaskMetricsGroup()
+  getNextCommit()
} 
@enduml
