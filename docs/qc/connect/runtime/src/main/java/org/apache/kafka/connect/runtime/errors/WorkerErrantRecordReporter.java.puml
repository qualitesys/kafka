@startuml
!pragma layout smetana
ErrantRecordReporter <|-- org.apache.kafka.connect.runtime.errors.WorkerErrantRecordReporter
class org.apache.kafka.connect.runtime.errors.WorkerErrantRecordReporter {
+  WorkerErrantRecordReporter(RetryWithToleranceOperator;Converter;Converter;HeaderConverter)
+  report(SinkRecord;Throwable)
+  awaitFutures(Collection<TopicPartition>)
+  cancelFutures(Collection<TopicPartition>)
+  futuresFor(Collection<TopicPartition>)
} 
@enduml
