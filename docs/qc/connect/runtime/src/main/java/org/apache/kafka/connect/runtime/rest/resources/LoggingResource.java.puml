@startuml
!pragma layout smetana
ConnectResource <|-- org.apache.kafka.connect.runtime.rest.resources.LoggingResource
class org.apache.kafka.connect.runtime.rest.resources.LoggingResource {
+  requestTimeout(long)
+  listLoggers()
+  getLogger(String)
+  setLevel(String;Map<String,String>)
+  lookupLogger(String)
+  currentLoggers()
+  rootLogger()
+  effectiveLevelToMap(Logger)
+  levelToMap(Logger)
} 
@enduml
