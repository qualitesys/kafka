@startuml
!pragma layout smetana
AbstractConfig <|-- org.apache.kafka.connect.runtime.ConnectorConfig
class org.apache.kafka.connect.runtime.ConnectorConfig {
+  configDef()
+  aliasValidator(String)
+  ConnectorConfig(Plugins)
+  ConnectorConfig(Plugins;Map<String,String>)
+  ConnectorConfig(Plugins;ConfigDef;Map<String,String>)
+  get(String)
+  errorRetryTimeout()
+  errorMaxDelayInMillis()
+  errorToleranceType()
+  enableErrorLog()
+  includeRecordDetailsInErrorLog()
+  enrich(Plugins;ConfigDef;Map<String,String>;boolean)
} 
@enduml
