<?xml version="1.0" encoding="UTF-8" standalone="no"?><svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" contentStyleType="text/css" height="544px" preserveAspectRatio="none" style="width:774px;height:544px;background:#000000;" version="1.1" viewBox="0 0 774 544" width="774px" zoomAndPan="magnify"><defs/><g><rect fill="#032526" height="1" style="stroke:#032526;stroke-width:1.0;" width="1" x="0" y="0"/><text fill="#33FF02" font-family="sans-serif" font-size="12" font-style="italic" font-weight="bold" lengthAdjust="spacing" textLength="127" x="5" y="17">PlantUML 1.2022.7</text><rect fill="#33FF02" height="21.2969" style="stroke:#33FF02;stroke-width:1.0;" width="762" x="5" y="26.9688"/><text fill="#000000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="178" x="6" y="41.9688">[From string (line 98) ]</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="5" x="5" y="62.2656"> </text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="79" x="5" y="78.5625">@startuml</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="190" x="5" y="94.8594">!pragma layout smetana</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="37" x="5" y="111.1563">start</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="78" x="5" y="127.4531">note right</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="545" x="5" y="143.75">constructor IncrementalCooperativeAssignor(LogContextlogContext,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="15" x="5" y="160.0469">...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="175" x="5" y="176.3438">... ( skipping 73 lines )</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="15" x="5" y="192.6406">...</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="426" x="5" y="208.9375">intlastCompletedGenerationId,intcurrentGenerationId,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="450" x="5" y="225.2344">Map&lt;String,ConnectorsAndTasks&gt;memberAssignments)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="71" x="5" y="241.5313">end note</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="489" x="5" y="257.8281">:log.debug("Previous assignments: {}",previousAssignment);</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="517" x="5" y="274.125">if (previousGenerationId!=lastCompletedGenerationId) then (yes)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="762" x="5" y="290.4219">:log.debug("Clearing the view of previous assignments due to generation mismatch between "+</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="603" x="5" y="306.7188">"previous generation ID {} and last completed generation ID {}. This can "+</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="639" x="5" y="323.0156">"happen if the leader fails to sync the assignment within a rebalancing round. "+</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="620" x="5" y="339.3125">"The following view of previous assignments might be outdated and will be "+</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="589" x="5" y="355.6094">"ignored by the leader in the current computation of new assignments. "+</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="370" x="5" y="371.9063">"Possibly outdated previous assignments: {}",</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="396" x="5" y="388.2031">previousGenerationId,lastCompletedGenerationId,</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="174" x="5" y="404.5">previousAssignment);</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="446" x="5" y="420.7969">:this.previousAssignment=ConnectorsAndTasks.EMPTY;</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="40" x="5" y="437.0938">endif</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="432" x="5" y="453.3906">:log.debug("Configured assignments: {}",configured);</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="456" x="5" y="469.6875">:log.debug("Active assignments: {}",activeAssignments);</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="353" x="5" y="485.9844">if (!previousRevocation.isEmpty()) then (yes)</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="356" x="5" y="502.2813">if (previousRevocation.connectors().stream()</text><text fill="#33FF02" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" text-decoration="wavy underline" textLength="369" x="5" y="518.5781">.anyMatch(c-&gt;activeAssignments.connectors()</text><text fill="#FF0000" font-family="sans-serif" font-size="14" font-weight="bold" lengthAdjust="spacing" textLength="106" x="10" y="534.875">Syntax Error?</text><!--MD5=[52fc45f99d4e4c4b7280fa1ae1a55d61]
@startuml
!pragma layout smetana
start
note right
constructor IncrementalCooperativeAssignor(LogContextlogContext,
Timetime,intmaxDelay)
end note
:this.log=logContext.logger(IncrementalCooperativeAssignor.class );
:this.time=time;
:this.maxDelay=maxDelay;
:this.previousAssignment=ConnectorsAndTasks.EMPTY;
:this.previousRevocation=new ConnectorsAndTasks.Builder()
.build();
:this.canRevoke=true;
:this.scheduledRebalance=0;
:this.candidateWorkersForReassignment=new LinkedHashSet<>();
:this.delay=0;
:this.previousGenerationId=-1;
:this.previousMembers=Collections.emptySet();
end
start
note right
Map<String,ByteBuffer>performAssignment(StringleaderId,
Stringprotocol,List<JoinGroupResponseMember>allMemberMetadata,
WorkerCoordinatorcoordinator)
end note
:log.debug("Performing task assignment");
:memberConfigs.put(member.memberId(),IncrementalCooperativeConnectProtocol.deserializeMetadata(ByteBuffer.wrap(member.metadata())));
:log.debug("Member configs: {}",memberConfigs);
:log.debug("Max config offset root: {}, local snapshot config offsets root: {}",
maxOffset,coordinator.configSnapshot().offset());
if (leaderOffset==null ) then (yes) 
:return serializeAssignments(assignments,
protocolVersion);;
stop
endif 
:return performTaskAssignment(leaderId,leaderOffset,
memberConfigs,coordinator,protocolVersion);;
stop
end
start
note right
LongensureLeaderConfig(longmaxOffset,WorkerCoordinatorcoordinator)
end note
if (coordinator.configSnapshot().offset()<maxOffset) then (yes) 
if (updatedSnapshot.offset()<maxOffset) then (yes) 
:log.info("Was selected to perform assignments, but do not have latest config found in sync request. "+
"Returning an empty configuration to trigger re-sync.");
:return null ;;
stop
else (no)
:coordinator.configSnapshot(updatedSnapshot);
:return updatedSnapshot.offset();;
stop
endif 
endif 
:return maxOffset;;
stop
end
start
note right
Map<String,ByteBuffer>performTaskAssignment(StringleaderId,
longmaxOffset,Map<String,ExtendedWorkerState>memberConfigs,
WorkerCoordinatorcoordinator,shortprotocolVersion)
end note
:log.debug("Performing task assignment during generation: {} with memberId: {}",
coordinator.generationId(),coordinator.memberId());
:coordinator.leaderState(new LeaderState(memberConfigs,
clusterAssignment.allAssignedConnectors(),
clusterAssignment.allAssignedTasks()));
:log.debug("Actual assignments: {}",assignments);
:return serializeAssignments(assignments,
protocolVersion);;
stop
end
start
note right
ClusterAssignmentperformTaskAssignment(ClusterConfigStateconfigSnapshot,
intlastCompletedGenerationId,intcurrentGenerationId,
Map<String,ConnectorsAndTasks>memberAssignments)
end note
:log.debug("Previous assignments: {}",previousAssignment);
if (previousGenerationId!=lastCompletedGenerationId) then (yes) 
:log.debug("Clearing the view of previous assignments due to generation mismatch between "+
"previous generation ID {} and last completed generation ID {}. This can "+
"happen if the leader fails to sync the assignment within a rebalancing round. "+
"The following view of previous assignments might be outdated and will be "+
"ignored by the leader in the current computation of new assignments. "+
"Possibly outdated previous assignments: {}",
previousGenerationId,lastCompletedGenerationId,
previousAssignment);
:this.previousAssignment=ConnectorsAndTasks.EMPTY;
endif 
:log.debug("Configured assignments: {}",configured);
:log.debug("Active assignments: {}",activeAssignments);
if (!previousRevocation.isEmpty()) then (yes) 
if (previousRevocation.connectors().stream()
.anyMatch(c->activeAssignments.connectors()
.contains(c))||previousRevocation.tasks()
.stream().anyMatch(t->activeAssignments.tasks()
.contains(t))) then (yes) 
:previousAssignment=activeAssignments;
:canRevoke=true;
endif 
:previousRevocation.connectors().clear();
:previousRevocation.tasks().clear();
endif 
:log.debug("Deleted assignments: {}",deleted);
:log.debug("Remaining (excluding deleted) active assignments: {}",
remainingActive);
:log.debug("Lost assignments: {}",lostAssignments);
:log.debug("New assignments: {}",newSubmissions);
:log.debug("Complete (ignoring deletions) worker assignments: {}",
completeWorkerAssignment);
:log.debug("Complete (ignoring deletions) connector assignments: {}",
connectorAssignments);
:log.debug("Complete (ignoring deletions) task assignments: {}",
taskAssignments);
:log.debug("Connector and task to delete assignments: {}",
toRevoke);
:toRevoke.putAll(computeDuplicatedAssignments(memberAssignments,
connectorAssignments,taskAssignments));
:log.debug("Connector and task to revoke assignments (include duplicated assignments): {}",
toRevoke);
:completeWorkerAssignment=workerAssignment(memberAssignments,
deleted);
:connectorAssignments=completeWorkerAssignment.stream().collect(Collectors.toMap(WorkerLoad::worker,
WorkerLoad::connectors));
:taskAssignments=completeWorkerAssignment.stream().collect(Collectors.toMap(WorkerLoad::worker,
WorkerLoad::tasks));
:handleLostAssignments(lostAssignments,newSubmissions,
completeWorkerAssignment);
:canRevoke=delay==0&&canRevoke;
:log.debug("Can leader revoke tasks in this assignment? {} (delay: {})",
canRevoke,delay);
if (canRevoke) then (yes) 
:log.debug("Connector and task to revoke assignments: {}",
toRevoke);
:toExplicitlyRevoke.forEach((worker,assignment)->{ConnectorsAndTasksexisting=toRevoke.computeIfAbsent(worker,
v->new ConnectorsAndTasks.Builder().build());existing.connectors()
.addAll(assignment.connectors());existing.tasks()
.addAll(assignment.tasks());});
:canRevoke=toExplicitlyRevoke.size()==0;
else (no)
:canRevoke=delay==0;
endif 
:assignConnectors(completeWorkerAssignment,
newSubmissions.connectors());
:assignTasks(completeWorkerAssignment,newSubmissions.tasks());
:log.debug("Current complete assignments: {}",
currentWorkerAssignment);
:log.debug("New complete assignments: {}",
completeWorkerAssignment);
:previousAssignment=computePreviousAssignment(toRevoke,
connectorAssignments,taskAssignments,lostAssignments);
:previousGenerationId=currentGenerationId;
:previousMembers=memberAssignments.keySet();
:log.debug("Incremental connector assignments: {}",
incrementalConnectorAssignments);
:log.debug("Incremental task assignments: {}",
incrementalTaskAssignments);
:return new ClusterAssignment(incrementalConnectorAssignments,
incrementalTaskAssignments,revokedConnectors,
revokedTasks,diff(connectorAssignments,revokedConnectors),
diff(taskAssignments,revokedTasks));;
stop
end
start
note right
Map<String,ConnectorsAndTasks>computeDeleted(ConnectorsAndTasksdeleted,
Map<String,Collection<String>>connectorAssignments,
Map<String,Collection<ConnectorTaskId>>taskAssignments)
end note
:deleted.connectors().forEach(c->toRevoke.computeIfAbsent(connectorOwners.get(c),
v->new ConnectorsAndTasks.Builder().build())
.connectors().add(c));
:deleted.tasks().forEach(t->toRevoke.computeIfAbsent(taskOwners.get(t),
v->new ConnectorsAndTasks.Builder().build())
.tasks().add(t));
:log.debug("Connectors and tasks to delete assignments: {}",
toRevoke);
:return toRevoke;;
stop
end
start
note right
ConnectorsAndTaskscomputePreviousAssignment(Map<String,
ConnectorsAndTasks>toRevoke,Map<String,Collection<String>>connectorAssignments,
Map<String,Collection<ConnectorTaskId>>taskAssignments,
ConnectorsAndTaskslostAssignments)
end note
:previousAssignment.connectors().removeAll(revoked.connectors());
:previousAssignment.tasks().removeAll(revoked.tasks());
:previousRevocation.connectors().addAll(revoked.connectors());
:previousRevocation.tasks().addAll(revoked.tasks());
:previousAssignment.connectors().addAll(lostAssignments.connectors());
:previousAssignment.tasks().addAll(lostAssignments.tasks());
:return previousAssignment;;
stop
end
start
note right
ConnectorsAndTasksduplicatedAssignments(Map<String,
ConnectorsAndTasks>memberAssignments)
end note
:return new ConnectorsAndTasks.Builder().with(duplicatedConnectors,
duplicatedTasks).build();;
stop
end
start
note right
Map<String,ConnectorsAndTasks>computeDuplicatedAssignments(Map<String,
ConnectorsAndTasks>memberAssignments,Map<String,
Collection<String>>connectorAssignments,Map<String,
Collection<ConnectorTaskId>>taskAssignment)
end note
:log.debug("Duplicated assignments: {}",duplicatedAssignments);
if (!duplicatedAssignments.connectors().isEmpty()) then (yes) 
:connectorAssignments.entrySet().stream()
.forEach(entry->{Set<String>duplicatedConnectors=new HashSet<>(duplicatedAssignments.connectors());duplicatedConnectors.retainAll(entry.getValue());if (!duplicatedConnectors.isEmpty()){toRevoke.computeIfAbsent(entry.getKey(),v->new ConnectorsAndTasks.Builder().build()).connectors().addAll(duplicatedConnectors);}});
endif 
if (!duplicatedAssignments.tasks().isEmpty()) then (yes) 
:taskAssignment.entrySet().stream().forEach(entry->{Set<ConnectorTaskId>duplicatedTasks=new HashSet<>(duplicatedAssignments.tasks());duplicatedTasks.retainAll(entry.getValue());if (!duplicatedTasks.isEmpty()){toRevoke.computeIfAbsent(entry.getKey(),v->new ConnectorsAndTasks.Builder().build()).tasks().addAll(duplicatedTasks);}});
endif 
:return toRevoke;;
stop
end
start
note right
void handleLostAssignments(ConnectorsAndTaskslostAssignments,
ConnectorsAndTasksnewSubmissions,List<WorkerLoad>completeWorkerAssignment)
end note
if (lostAssignments.isEmpty()) then (yes) 
:resetDelay();
:return ;;
stop
endif 
:log.debug("Found the following connectors and tasks missing from previous assignments: "+
lostAssignments);
if (scheduledRebalance<=0&&activeMembers.containsAll(previousMembers)) then (yes) 
:log.debug("No worker seems to have departed the group during the rebalance. The "+
"missing assignments that the leader is detecting are probably due to some "+
"workers failing to receive the new assignments in the previous rebalance. "+
"Will reassign missing tasks as new tasks");
:newSubmissions.connectors().addAll(lostAssignments.connectors());
:newSubmissions.tasks().addAll(lostAssignments.tasks());
:return ;;
stop
endif 
if (scheduledRebalance>0&&now>=scheduledRebalance) then (yes) 
:log.debug("Delayed rebalance expired. Reassigning lost tasks");
if (!candidateWorkersForReassignment.isEmpty()) then (yes) 
:candidateWorkerLoad=pickCandidateWorkerForReassignment(completeWorkerAssignment);
endif 
if (!candidateWorkerLoad.isEmpty()) then (yes) 
:log.debug("Assigning lost tasks to {} candidate workers: {}",
candidateWorkerLoad.size(),candidateWorkerLoad.stream().map(WorkerLoad::worker).collect(Collectors.joining(",
")));
if (!candidateWorkerIterator.hasNext()) then (yes) 
:candidateWorkerIterator=candidateWorkerLoad.iterator();
endif 
:log.debug("Assigning connector id {} to member {}",
connector,worker.worker());
:worker.assign(connector);
:candidateWorkerIterator=candidateWorkerLoad.iterator();
if (!candidateWorkerIterator.hasNext()) then (yes) 
:candidateWorkerIterator=candidateWorkerLoad.iterator();
endif 
:log.debug("Assigning task id {} to member {}",
task,worker.worker());
:worker.assign(task);
else (no)
:log.debug("No single candidate worker was found to assign lost tasks. Treating lost tasks as new tasks");
:newSubmissions.connectors().addAll(lostAssignments.connectors());
:newSubmissions.tasks().addAll(lostAssignments.tasks());
endif 
:resetDelay();
else (no)
:candidateWorkersForReassignment.addAll(candidateWorkersForReassignment(completeWorkerAssignment));
if (now<scheduledRebalance) then (yes) 
:delay=calculateDelay(now);
:log.debug("Delayed rebalance in progress. Task reassignment is postponed. New computed rebalance delay: {}",
delay);
else (no)
:delay=maxDelay;
:log.debug("Resetting rebalance delay to the max: {}. scheduledRebalance: {} now: {} diff scheduledRebalance - now: {}",
delay,scheduledRebalance,now,scheduledRebalance-now);
endif 
:scheduledRebalance=now+delay;
endif 
end
start
note right
void resetDelay()
end note
:candidateWorkersForReassignment.clear();
:scheduledRebalance=0;
if (delay!=0) then (yes) 
:log.debug("Resetting delay from previous value: {} to 0",
delay);
endif 
:delay=0;
end
start
note right
Set<String>candidateWorkersForReassignment(List<WorkerLoad>completeWorkerAssignment)
end note
:return completeWorkerAssignment.stream()
.filter(WorkerLoad::isEmpty).map(WorkerLoad::worker)
.collect(Collectors.toSet());;
stop
end
start
note right
List<WorkerLoad>pickCandidateWorkerForReassignment(List<WorkerLoad>completeWorkerAssignment)
end note
:return candidateWorkersForReassignment.stream()
.map(activeWorkers::get).filter(Objects::nonNull)
.collect(Collectors.toList());;
stop
end
start
note right
Map<String,ConnectorsAndTasks>performTaskRevocation(ConnectorsAndTasksactiveAssignments,
Collection<WorkerLoad>completeWorkerAssignment)
end note
if (log.isDebugEnabled()) then (yes) 
:completeWorkerAssignment.forEach(wl->log.debug("Per worker current load size; worker: {} connectors: {} tasks: {}",
wl.worker(),wl.connectorsSize(),wl.tasksSize()));
endif 
if (!(newWorkersNum>0&&existingWorkersNum>0)) then (yes) 
:log.debug("No task revocation required; workers with existing load: {} workers with "+
"no load {} total workers {}",existingWorkersNum,
newWorkersNum,totalWorkersNum);
:return revoking;;
stop
endif 
:log.debug("Task revocation is required; workers with existing load: {} workers with "+
"no load {} total workers {}",existingWorkersNum,
newWorkersNum,totalWorkersNum);
:log.debug("Previous rounded down (floor) average number of connectors per worker {}",
totalActiveConnectorsNum/existingWorkersNum);
:log.debug("New average number of connectors per worker rounded down (floor) {} and rounded up (ceil) {}",
floorConnectors,ceilConnectors);
:log.debug("Previous rounded down (floor) average number of tasks per worker {}",
totalActiveTasksNum/existingWorkersNum);
:log.debug("New average number of tasks per worker rounded down (floor) {} and rounded up (ceil) {}",
floorTasks,ceilTasks);
:numToRevoke=existing.connectorsSize()-ceilConnectors;
:- -i;
:- -numToRevoke;
:resources.connectors().add(connectors.next());
:numToRevoke=existing.tasksSize()-ceilTasks;
:log.debug("Tasks on worker {} is higher than ceiling,
 so revoking {} tasks",existing,numToRevoke);
:- -i;
:- -numToRevoke;
:resources.tasks().add(tasks.next());
:return revoking;;
stop
end
start
note right
Map<String,ExtendedAssignment>fillAssignments(Collection<String>members,
shorterror,StringleaderId,StringleaderUrl,
longmaxOffset,ClusterAssignmentclusterAssignment,
intdelay,shortprotocolVersion)
end note
:log.debug("Filling assignment: {} -> {}",
member,assignment);
:groupAssignment.put(member,assignment);
:log.debug("Finished assignment");
:return groupAssignment;;
stop
end
start
note right
Map<String,ByteBuffer>serializeAssignments(Map<String,
ExtendedAssignment>assignments,shortprotocolVersion)
end note
:return assignments.entrySet().stream().collect(Collectors.toMap(Map.Entry::getKey,
e->IncrementalCooperativeConnectProtocol.serializeAssignment(e.getValue(),
sessioned)));;
stop
end
start
note right
ConnectorsAndTasksdiff(ConnectorsAndTasksbase,
ConnectorsAndTasks... toSubtract)
end note
:connectors.removeAll(sub.connectors());
:tasks.removeAll(sub.tasks());
:return new ConnectorsAndTasks.Builder().with(connectors,
tasks).build();;
stop
end
:values.removeAll(toSubtract.getOrDefault(entry.getKey(),
Collections.emptySet()));
:incremental.put(entry.getKey(),values);
:return incremental;;
stop
start
note right
ConnectorsAndTasksassignment(Map<String,ConnectorsAndTasks>memberAssignments)
end note
:log.debug("Received assignments: {}",memberAssignments);
:return new ConnectorsAndTasks.Builder().with(ConnectUtils.combineCollections(memberAssignments.values(),
ConnectorsAndTasks::connectors),ConnectUtils.combineCollections(memberAssignments.values(),
ConnectorsAndTasks::tasks)).build();;
stop
end
start
note right
intcalculateDelay(longnow)
end note
:return diff>0? (int)Math.min(diff,maxDelay):0;;
stop
end
start
note right
void assignConnectors(List<WorkerLoad>workerAssignment,
Collection<String>connectors)
end note
:workerAssignment.sort(WorkerLoad.connectorComparator());
while (load.hasNext())
:log.debug("Assigning connector {} to {}",
connector,worker.worker());
:worker.assign(connector);
if (!load.hasNext()) then (yes) 
endif 
endwhile
end
start
note right
void assignTasks(List<WorkerLoad>workerAssignment,
Collection<ConnectorTaskId>tasks)
end note
:workerAssignment.sort(WorkerLoad.taskComparator());
while (load.hasNext())
:log.debug("Assigning task {} to {}",task,
worker.worker());
:worker.assign(task);
if (!load.hasNext()) then (yes) 
endif 
endwhile
end
start
note right
List<WorkerLoad>workerAssignment(Map<String,
ConnectorsAndTasks>memberAssignments,ConnectorsAndTaskstoExclude)
end note
:return memberAssignments.entrySet().stream()
.map(e->new WorkerLoad.Builder(e.getKey())
.with(e.getValue().connectors().stream().filter(v->!ignore.connectors().contains(v)).collect(Collectors.toList()),
e.getValue().tasks().stream().filter(v->!ignore.tasks()
.contains(v)).collect(Collectors.toList()))
.build()).collect(Collectors.toList());;
stop
end
start
note right
constructor ClusterAssignment(Map<String,
Collection<String>>newlyAssignedConnectors,
Map<String,Collection<ConnectorTaskId>>newlyAssignedTasks,
Map<String,Collection<String>>newlyRevokedConnectors,
Map<String,Collection<ConnectorTaskId>>newlyRevokedTasks,
Map<String,Collection<String>>allAssignedConnectors,
Map<String,Collection<ConnectorTaskId>>allAssignedTasks)
end note
:this.newlyAssignedConnectors=newlyAssignedConnectors;
:this.newlyAssignedTasks=newlyAssignedTasks;
:this.newlyRevokedConnectors=newlyRevokedConnectors;
:this.newlyRevokedTasks=newlyRevokedTasks;
:this.allAssignedConnectors=allAssignedConnectors;
:this.allAssignedTasks=allAssignedTasks;
:this.allWorkers=combineCollections(Arrays.asList(newlyAssignedConnectors,
newlyAssignedTasks,newlyRevokedConnectors,
newlyRevokedTasks,allAssignedConnectors,allAssignedTasks),
Map::keySet,Collectors.toSet());
end
start
note right
Map<String,Collection<String>>newlyAssignedConnectors()
end note
:return newlyAssignedConnectors;;
stop
end
start
note right
Collection<String>newlyAssignedConnectors(Stringworker)
end note
:return newlyAssignedConnectors.getOrDefault(worker,
Collections.emptySet());;
stop
end
start
note right
Map<String,Collection<ConnectorTaskId>>newlyAssignedTasks()
end note
:return newlyAssignedTasks;;
stop
end
start
note right
Collection<ConnectorTaskId>newlyAssignedTasks(Stringworker)
end note
:return newlyAssignedTasks.getOrDefault(worker,
Collections.emptySet());;
stop
end
start
note right
Map<String,Collection<String>>newlyRevokedConnectors()
end note
:return newlyRevokedConnectors;;
stop
end
start
note right
Collection<String>newlyRevokedConnectors(Stringworker)
end note
:return newlyRevokedConnectors.getOrDefault(worker,
Collections.emptySet());;
stop
end
start
note right
Map<String,Collection<ConnectorTaskId>>newlyRevokedTasks()
end note
:return newlyRevokedTasks;;
stop
end
start
note right
Collection<ConnectorTaskId>newlyRevokedTasks(Stringworker)
end note
:return newlyRevokedTasks.getOrDefault(worker,
Collections.emptySet());;
stop
end
start
note right
Map<String,Collection<String>>allAssignedConnectors()
end note
:return allAssignedConnectors;;
stop
end
start
note right
Map<String,Collection<ConnectorTaskId>>allAssignedTasks()
end note
:return allAssignedTasks;;
stop
end
start
note right
Set<String>allWorkers()
end note
:return allWorkers;;
stop
end
start
note right
StringtoString()
end note
:return "ClusterAssignment{"+"newlyAssignedConnectors="+
newlyAssignedConnectors+", newlyAssignedTasks="+
newlyAssignedTasks+", newlyRevokedConnectors="+
newlyRevokedConnectors+", newlyRevokedTasks="+
newlyRevokedTasks+", allAssignedConnectors="+
allAssignedConnectors+", allAssignedTasks="+
allAssignedTasks+", allWorkers="+allWorkers+
'}';;
stop
end
@enduml

@startuml
!pragma layout smetana
start
note right
constructor IncrementalCooperativeAssignor(LogContextlogContext,
Timetime,intmaxDelay)
end note
:this.log=logContext.logger(IncrementalCooperativeAssignor.class );
:this.time=time;
:this.maxDelay=maxDelay;
:this.previousAssignment=ConnectorsAndTasks.EMPTY;
:this.previousRevocation=new ConnectorsAndTasks.Builder()
.build();
:this.canRevoke=true;
:this.scheduledRebalance=0;
:this.candidateWorkersForReassignment=new LinkedHashSet<>();
:this.delay=0;
:this.previousGenerationId=-1;
:this.previousMembers=Collections.emptySet();
end
start
note right
Map<String,ByteBuffer>performAssignment(StringleaderId,
Stringprotocol,List<JoinGroupResponseMember>allMemberMetadata,
WorkerCoordinatorcoordinator)
end note
:log.debug("Performing task assignment");
:memberConfigs.put(member.memberId(),IncrementalCooperativeConnectProtocol.deserializeMetadata(ByteBuffer.wrap(member.metadata())));
:log.debug("Member configs: {}",memberConfigs);
:log.debug("Max config offset root: {}, local snapshot config offsets root: {}",
maxOffset,coordinator.configSnapshot().offset());
if (leaderOffset==null ) then (yes) 
:return serializeAssignments(assignments,
protocolVersion);;
stop
endif 
:return performTaskAssignment(leaderId,leaderOffset,
memberConfigs,coordinator,protocolVersion);;
stop
end
start
note right
LongensureLeaderConfig(longmaxOffset,WorkerCoordinatorcoordinator)
end note
if (coordinator.configSnapshot().offset()<maxOffset) then (yes) 
if (updatedSnapshot.offset()<maxOffset) then (yes) 
:log.info("Was selected to perform assignments, but do not have latest config found in sync request. "+
"Returning an empty configuration to trigger re-sync.");
:return null ;;
stop
else (no)
:coordinator.configSnapshot(updatedSnapshot);
:return updatedSnapshot.offset();;
stop
endif 
endif 
:return maxOffset;;
stop
end
start
note right
Map<String,ByteBuffer>performTaskAssignment(StringleaderId,
longmaxOffset,Map<String,ExtendedWorkerState>memberConfigs,
WorkerCoordinatorcoordinator,shortprotocolVersion)
end note
:log.debug("Performing task assignment during generation: {} with memberId: {}",
coordinator.generationId(),coordinator.memberId());
:coordinator.leaderState(new LeaderState(memberConfigs,
clusterAssignment.allAssignedConnectors(),
clusterAssignment.allAssignedTasks()));
:log.debug("Actual assignments: {}",assignments);
:return serializeAssignments(assignments,
protocolVersion);;
stop
end
start
note right
ClusterAssignmentperformTaskAssignment(ClusterConfigStateconfigSnapshot,
intlastCompletedGenerationId,intcurrentGenerationId,
Map<String,ConnectorsAndTasks>memberAssignments)
end note
:log.debug("Previous assignments: {}",previousAssignment);
if (previousGenerationId!=lastCompletedGenerationId) then (yes) 
:log.debug("Clearing the view of previous assignments due to generation mismatch between "+
"previous generation ID {} and last completed generation ID {}. This can "+
"happen if the leader fails to sync the assignment within a rebalancing round. "+
"The following view of previous assignments might be outdated and will be "+
"ignored by the leader in the current computation of new assignments. "+
"Possibly outdated previous assignments: {}",
previousGenerationId,lastCompletedGenerationId,
previousAssignment);
:this.previousAssignment=ConnectorsAndTasks.EMPTY;
endif 
:log.debug("Configured assignments: {}",configured);
:log.debug("Active assignments: {}",activeAssignments);
if (!previousRevocation.isEmpty()) then (yes) 
if (previousRevocation.connectors().stream()
.anyMatch(c->activeAssignments.connectors()
.contains(c))||previousRevocation.tasks()
.stream().anyMatch(t->activeAssignments.tasks()
.contains(t))) then (yes) 
:previousAssignment=activeAssignments;
:canRevoke=true;
endif 
:previousRevocation.connectors().clear();
:previousRevocation.tasks().clear();
endif 
:log.debug("Deleted assignments: {}",deleted);
:log.debug("Remaining (excluding deleted) active assignments: {}",
remainingActive);
:log.debug("Lost assignments: {}",lostAssignments);
:log.debug("New assignments: {}",newSubmissions);
:log.debug("Complete (ignoring deletions) worker assignments: {}",
completeWorkerAssignment);
:log.debug("Complete (ignoring deletions) connector assignments: {}",
connectorAssignments);
:log.debug("Complete (ignoring deletions) task assignments: {}",
taskAssignments);
:log.debug("Connector and task to delete assignments: {}",
toRevoke);
:toRevoke.putAll(computeDuplicatedAssignments(memberAssignments,
connectorAssignments,taskAssignments));
:log.debug("Connector and task to revoke assignments (include duplicated assignments): {}",
toRevoke);
:completeWorkerAssignment=workerAssignment(memberAssignments,
deleted);
:connectorAssignments=completeWorkerAssignment.stream().collect(Collectors.toMap(WorkerLoad::worker,
WorkerLoad::connectors));
:taskAssignments=completeWorkerAssignment.stream().collect(Collectors.toMap(WorkerLoad::worker,
WorkerLoad::tasks));
:handleLostAssignments(lostAssignments,newSubmissions,
completeWorkerAssignment);
:canRevoke=delay==0&&canRevoke;
:log.debug("Can leader revoke tasks in this assignment? {} (delay: {})",
canRevoke,delay);
if (canRevoke) then (yes) 
:log.debug("Connector and task to revoke assignments: {}",
toRevoke);
:toExplicitlyRevoke.forEach((worker,assignment)->{ConnectorsAndTasksexisting=toRevoke.computeIfAbsent(worker,
v->new ConnectorsAndTasks.Builder().build());existing.connectors()
.addAll(assignment.connectors());existing.tasks()
.addAll(assignment.tasks());});
:canRevoke=toExplicitlyRevoke.size()==0;
else (no)
:canRevoke=delay==0;
endif 
:assignConnectors(completeWorkerAssignment,
newSubmissions.connectors());
:assignTasks(completeWorkerAssignment,newSubmissions.tasks());
:log.debug("Current complete assignments: {}",
currentWorkerAssignment);
:log.debug("New complete assignments: {}",
completeWorkerAssignment);
:previousAssignment=computePreviousAssignment(toRevoke,
connectorAssignments,taskAssignments,lostAssignments);
:previousGenerationId=currentGenerationId;
:previousMembers=memberAssignments.keySet();
:log.debug("Incremental connector assignments: {}",
incrementalConnectorAssignments);
:log.debug("Incremental task assignments: {}",
incrementalTaskAssignments);
:return new ClusterAssignment(incrementalConnectorAssignments,
incrementalTaskAssignments,revokedConnectors,
revokedTasks,diff(connectorAssignments,revokedConnectors),
diff(taskAssignments,revokedTasks));;
stop
end
start
note right
Map<String,ConnectorsAndTasks>computeDeleted(ConnectorsAndTasksdeleted,
Map<String,Collection<String>>connectorAssignments,
Map<String,Collection<ConnectorTaskId>>taskAssignments)
end note
:deleted.connectors().forEach(c->toRevoke.computeIfAbsent(connectorOwners.get(c),
v->new ConnectorsAndTasks.Builder().build())
.connectors().add(c));
:deleted.tasks().forEach(t->toRevoke.computeIfAbsent(taskOwners.get(t),
v->new ConnectorsAndTasks.Builder().build())
.tasks().add(t));
:log.debug("Connectors and tasks to delete assignments: {}",
toRevoke);
:return toRevoke;;
stop
end
start
note right
ConnectorsAndTaskscomputePreviousAssignment(Map<String,
ConnectorsAndTasks>toRevoke,Map<String,Collection<String>>connectorAssignments,
Map<String,Collection<ConnectorTaskId>>taskAssignments,
ConnectorsAndTaskslostAssignments)
end note
:previousAssignment.connectors().removeAll(revoked.connectors());
:previousAssignment.tasks().removeAll(revoked.tasks());
:previousRevocation.connectors().addAll(revoked.connectors());
:previousRevocation.tasks().addAll(revoked.tasks());
:previousAssignment.connectors().addAll(lostAssignments.connectors());
:previousAssignment.tasks().addAll(lostAssignments.tasks());
:return previousAssignment;;
stop
end
start
note right
ConnectorsAndTasksduplicatedAssignments(Map<String,
ConnectorsAndTasks>memberAssignments)
end note
:return new ConnectorsAndTasks.Builder().with(duplicatedConnectors,
duplicatedTasks).build();;
stop
end
start
note right
Map<String,ConnectorsAndTasks>computeDuplicatedAssignments(Map<String,
ConnectorsAndTasks>memberAssignments,Map<String,
Collection<String>>connectorAssignments,Map<String,
Collection<ConnectorTaskId>>taskAssignment)
end note
:log.debug("Duplicated assignments: {}",duplicatedAssignments);
if (!duplicatedAssignments.connectors().isEmpty()) then (yes) 
:connectorAssignments.entrySet().stream()
.forEach(entry->{Set<String>duplicatedConnectors=new HashSet<>(duplicatedAssignments.connectors());duplicatedConnectors.retainAll(entry.getValue());if (!duplicatedConnectors.isEmpty()){toRevoke.computeIfAbsent(entry.getKey(),v->new ConnectorsAndTasks.Builder().build()).connectors().addAll(duplicatedConnectors);}});
endif 
if (!duplicatedAssignments.tasks().isEmpty()) then (yes) 
:taskAssignment.entrySet().stream().forEach(entry->{Set<ConnectorTaskId>duplicatedTasks=new HashSet<>(duplicatedAssignments.tasks());duplicatedTasks.retainAll(entry.getValue());if (!duplicatedTasks.isEmpty()){toRevoke.computeIfAbsent(entry.getKey(),v->new ConnectorsAndTasks.Builder().build()).tasks().addAll(duplicatedTasks);}});
endif 
:return toRevoke;;
stop
end
start
note right
void handleLostAssignments(ConnectorsAndTaskslostAssignments,
ConnectorsAndTasksnewSubmissions,List<WorkerLoad>completeWorkerAssignment)
end note
if (lostAssignments.isEmpty()) then (yes) 
:resetDelay();
:return ;;
stop
endif 
:log.debug("Found the following connectors and tasks missing from previous assignments: "+
lostAssignments);
if (scheduledRebalance<=0&&activeMembers.containsAll(previousMembers)) then (yes) 
:log.debug("No worker seems to have departed the group during the rebalance. The "+
"missing assignments that the leader is detecting are probably due to some "+
"workers failing to receive the new assignments in the previous rebalance. "+
"Will reassign missing tasks as new tasks");
:newSubmissions.connectors().addAll(lostAssignments.connectors());
:newSubmissions.tasks().addAll(lostAssignments.tasks());
:return ;;
stop
endif 
if (scheduledRebalance>0&&now>=scheduledRebalance) then (yes) 
:log.debug("Delayed rebalance expired. Reassigning lost tasks");
if (!candidateWorkersForReassignment.isEmpty()) then (yes) 
:candidateWorkerLoad=pickCandidateWorkerForReassignment(completeWorkerAssignment);
endif 
if (!candidateWorkerLoad.isEmpty()) then (yes) 
:log.debug("Assigning lost tasks to {} candidate workers: {}",
candidateWorkerLoad.size(),candidateWorkerLoad.stream().map(WorkerLoad::worker).collect(Collectors.joining(",
")));
if (!candidateWorkerIterator.hasNext()) then (yes) 
:candidateWorkerIterator=candidateWorkerLoad.iterator();
endif 
:log.debug("Assigning connector id {} to member {}",
connector,worker.worker());
:worker.assign(connector);
:candidateWorkerIterator=candidateWorkerLoad.iterator();
if (!candidateWorkerIterator.hasNext()) then (yes) 
:candidateWorkerIterator=candidateWorkerLoad.iterator();
endif 
:log.debug("Assigning task id {} to member {}",
task,worker.worker());
:worker.assign(task);
else (no)
:log.debug("No single candidate worker was found to assign lost tasks. Treating lost tasks as new tasks");
:newSubmissions.connectors().addAll(lostAssignments.connectors());
:newSubmissions.tasks().addAll(lostAssignments.tasks());
endif 
:resetDelay();
else (no)
:candidateWorkersForReassignment.addAll(candidateWorkersForReassignment(completeWorkerAssignment));
if (now<scheduledRebalance) then (yes) 
:delay=calculateDelay(now);
:log.debug("Delayed rebalance in progress. Task reassignment is postponed. New computed rebalance delay: {}",
delay);
else (no)
:delay=maxDelay;
:log.debug("Resetting rebalance delay to the max: {}. scheduledRebalance: {} now: {} diff scheduledRebalance - now: {}",
delay,scheduledRebalance,now,scheduledRebalance-now);
endif 
:scheduledRebalance=now+delay;
endif 
end
start
note right
void resetDelay()
end note
:candidateWorkersForReassignment.clear();
:scheduledRebalance=0;
if (delay!=0) then (yes) 
:log.debug("Resetting delay from previous value: {} to 0",
delay);
endif 
:delay=0;
end
start
note right
Set<String>candidateWorkersForReassignment(List<WorkerLoad>completeWorkerAssignment)
end note
:return completeWorkerAssignment.stream()
.filter(WorkerLoad::isEmpty).map(WorkerLoad::worker)
.collect(Collectors.toSet());;
stop
end
start
note right
List<WorkerLoad>pickCandidateWorkerForReassignment(List<WorkerLoad>completeWorkerAssignment)
end note
:return candidateWorkersForReassignment.stream()
.map(activeWorkers::get).filter(Objects::nonNull)
.collect(Collectors.toList());;
stop
end
start
note right
Map<String,ConnectorsAndTasks>performTaskRevocation(ConnectorsAndTasksactiveAssignments,
Collection<WorkerLoad>completeWorkerAssignment)
end note
if (log.isDebugEnabled()) then (yes) 
:completeWorkerAssignment.forEach(wl->log.debug("Per worker current load size; worker: {} connectors: {} tasks: {}",
wl.worker(),wl.connectorsSize(),wl.tasksSize()));
endif 
if (!(newWorkersNum>0&&existingWorkersNum>0)) then (yes) 
:log.debug("No task revocation required; workers with existing load: {} workers with "+
"no load {} total workers {}",existingWorkersNum,
newWorkersNum,totalWorkersNum);
:return revoking;;
stop
endif 
:log.debug("Task revocation is required; workers with existing load: {} workers with "+
"no load {} total workers {}",existingWorkersNum,
newWorkersNum,totalWorkersNum);
:log.debug("Previous rounded down (floor) average number of connectors per worker {}",
totalActiveConnectorsNum/existingWorkersNum);
:log.debug("New average number of connectors per worker rounded down (floor) {} and rounded up (ceil) {}",
floorConnectors,ceilConnectors);
:log.debug("Previous rounded down (floor) average number of tasks per worker {}",
totalActiveTasksNum/existingWorkersNum);
:log.debug("New average number of tasks per worker rounded down (floor) {} and rounded up (ceil) {}",
floorTasks,ceilTasks);
:numToRevoke=existing.connectorsSize()-ceilConnectors;
:- -i;
:- -numToRevoke;
:resources.connectors().add(connectors.next());
:numToRevoke=existing.tasksSize()-ceilTasks;
:log.debug("Tasks on worker {} is higher than ceiling,
 so revoking {} tasks",existing,numToRevoke);
:- -i;
:- -numToRevoke;
:resources.tasks().add(tasks.next());
:return revoking;;
stop
end
start
note right
Map<String,ExtendedAssignment>fillAssignments(Collection<String>members,
shorterror,StringleaderId,StringleaderUrl,
longmaxOffset,ClusterAssignmentclusterAssignment,
intdelay,shortprotocolVersion)
end note
:log.debug("Filling assignment: {} -> {}",
member,assignment);
:groupAssignment.put(member,assignment);
:log.debug("Finished assignment");
:return groupAssignment;;
stop
end
start
note right
Map<String,ByteBuffer>serializeAssignments(Map<String,
ExtendedAssignment>assignments,shortprotocolVersion)
end note
:return assignments.entrySet().stream().collect(Collectors.toMap(Map.Entry::getKey,
e->IncrementalCooperativeConnectProtocol.serializeAssignment(e.getValue(),
sessioned)));;
stop
end
start
note right
ConnectorsAndTasksdiff(ConnectorsAndTasksbase,
ConnectorsAndTasks... toSubtract)
end note
:connectors.removeAll(sub.connectors());
:tasks.removeAll(sub.tasks());
:return new ConnectorsAndTasks.Builder().with(connectors,
tasks).build();;
stop
end
:values.removeAll(toSubtract.getOrDefault(entry.getKey(),
Collections.emptySet()));
:incremental.put(entry.getKey(),values);
:return incremental;;
stop
start
note right
ConnectorsAndTasksassignment(Map<String,ConnectorsAndTasks>memberAssignments)
end note
:log.debug("Received assignments: {}",memberAssignments);
:return new ConnectorsAndTasks.Builder().with(ConnectUtils.combineCollections(memberAssignments.values(),
ConnectorsAndTasks::connectors),ConnectUtils.combineCollections(memberAssignments.values(),
ConnectorsAndTasks::tasks)).build();;
stop
end
start
note right
intcalculateDelay(longnow)
end note
:return diff>0? (int)Math.min(diff,maxDelay):0;;
stop
end
start
note right
void assignConnectors(List<WorkerLoad>workerAssignment,
Collection<String>connectors)
end note
:workerAssignment.sort(WorkerLoad.connectorComparator());
while (load.hasNext())
:log.debug("Assigning connector {} to {}",
connector,worker.worker());
:worker.assign(connector);
if (!load.hasNext()) then (yes) 
endif 
endwhile
end
start
note right
void assignTasks(List<WorkerLoad>workerAssignment,
Collection<ConnectorTaskId>tasks)
end note
:workerAssignment.sort(WorkerLoad.taskComparator());
while (load.hasNext())
:log.debug("Assigning task {} to {}",task,
worker.worker());
:worker.assign(task);
if (!load.hasNext()) then (yes) 
endif 
endwhile
end
start
note right
List<WorkerLoad>workerAssignment(Map<String,
ConnectorsAndTasks>memberAssignments,ConnectorsAndTaskstoExclude)
end note
:return memberAssignments.entrySet().stream()
.map(e->new WorkerLoad.Builder(e.getKey())
.with(e.getValue().connectors().stream().filter(v->!ignore.connectors().contains(v)).collect(Collectors.toList()),
e.getValue().tasks().stream().filter(v->!ignore.tasks()
.contains(v)).collect(Collectors.toList()))
.build()).collect(Collectors.toList());;
stop
end
start
note right
constructor ClusterAssignment(Map<String,
Collection<String>>newlyAssignedConnectors,
Map<String,Collection<ConnectorTaskId>>newlyAssignedTasks,
Map<String,Collection<String>>newlyRevokedConnectors,
Map<String,Collection<ConnectorTaskId>>newlyRevokedTasks,
Map<String,Collection<String>>allAssignedConnectors,
Map<String,Collection<ConnectorTaskId>>allAssignedTasks)
end note
:this.newlyAssignedConnectors=newlyAssignedConnectors;
:this.newlyAssignedTasks=newlyAssignedTasks;
:this.newlyRevokedConnectors=newlyRevokedConnectors;
:this.newlyRevokedTasks=newlyRevokedTasks;
:this.allAssignedConnectors=allAssignedConnectors;
:this.allAssignedTasks=allAssignedTasks;
:this.allWorkers=combineCollections(Arrays.asList(newlyAssignedConnectors,
newlyAssignedTasks,newlyRevokedConnectors,
newlyRevokedTasks,allAssignedConnectors,allAssignedTasks),
Map::keySet,Collectors.toSet());
end
start
note right
Map<String,Collection<String>>newlyAssignedConnectors()
end note
:return newlyAssignedConnectors;;
stop
end
start
note right
Collection<String>newlyAssignedConnectors(Stringworker)
end note
:return newlyAssignedConnectors.getOrDefault(worker,
Collections.emptySet());;
stop
end
start
note right
Map<String,Collection<ConnectorTaskId>>newlyAssignedTasks()
end note
:return newlyAssignedTasks;;
stop
end
start
note right
Collection<ConnectorTaskId>newlyAssignedTasks(Stringworker)
end note
:return newlyAssignedTasks.getOrDefault(worker,
Collections.emptySet());;
stop
end
start
note right
Map<String,Collection<String>>newlyRevokedConnectors()
end note
:return newlyRevokedConnectors;;
stop
end
start
note right
Collection<String>newlyRevokedConnectors(Stringworker)
end note
:return newlyRevokedConnectors.getOrDefault(worker,
Collections.emptySet());;
stop
end
start
note right
Map<String,Collection<ConnectorTaskId>>newlyRevokedTasks()
end note
:return newlyRevokedTasks;;
stop
end
start
note right
Collection<ConnectorTaskId>newlyRevokedTasks(Stringworker)
end note
:return newlyRevokedTasks.getOrDefault(worker,
Collections.emptySet());;
stop
end
start
note right
Map<String,Collection<String>>allAssignedConnectors()
end note
:return allAssignedConnectors;;
stop
end
start
note right
Map<String,Collection<ConnectorTaskId>>allAssignedTasks()
end note
:return allAssignedTasks;;
stop
end
start
note right
Set<String>allWorkers()
end note
:return allWorkers;;
stop
end
start
note right
StringtoString()
end note
:return "ClusterAssignment{"+"newlyAssignedConnectors="+
newlyAssignedConnectors+", newlyAssignedTasks="+
newlyAssignedTasks+", newlyRevokedConnectors="+
newlyRevokedConnectors+", newlyRevokedTasks="+
newlyRevokedTasks+", allAssignedConnectors="+
allAssignedConnectors+", allAssignedTasks="+
allAssignedTasks+", allWorkers="+allWorkers+
stop
end
@enduml

PlantUML version 1.2022.7(Mon Aug 22 17:01:30 UTC 2022)
(GPL source distribution)
Java Runtime: OpenJDK Runtime Environment
JVM: OpenJDK 64-Bit Server VM
Default Encoding: UTF-8
Language: en
Country: null
--></g></svg>