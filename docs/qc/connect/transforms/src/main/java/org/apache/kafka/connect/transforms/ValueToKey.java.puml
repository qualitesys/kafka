@startuml
!pragma layout smetana
Transformation<R> <|-- org.apache.kafka.connect.transforms.ValueToKey
class org.apache.kafka.connect.transforms.ValueToKey {
+  configure(Map<String,?>)
+  apply(R)
+  applySchemaless(R)
+  applyWithSchema(R)
+  config()
+  close()
} 
@enduml
