@startuml
!pragma layout smetana
Transformation<R> <|-- org.apache.kafka.connect.transforms.InsertField
class org.apache.kafka.connect.transforms.InsertField {
+  configure(Map<String,?>)
+  apply(R)
+  applySchemaless(R)
+  applyWithSchema(R)
+  makeUpdatedSchema(Schema)
+  close()
+  config()
+  operatingSchema(R)
+  operatingValue(R)
+  newRecord(R;Schema;Object)
} 
@enduml
