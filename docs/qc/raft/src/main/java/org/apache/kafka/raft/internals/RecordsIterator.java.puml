@startuml
!pragma layout smetana
Iterator_Batch_T__ <|-- org.apache.kafka.raft.internals.RecordsIterator
class org.apache.kafka.raft.internals.RecordsIterator {
+  RecordsIterator(Records;RecordSerde<T>;BufferSupplier;int)
+  hasNext()
+  next()
+  close()
+  ensureOpen()
+  readFileRecords(FileRecords;ByteBuffer)
+  createMemoryRecords(FileRecords)
+  nextBatches()
+  nextBatch()
+  readBatch(DefaultRecordBatch)
+  readRecord(Readable)
} 
@enduml
