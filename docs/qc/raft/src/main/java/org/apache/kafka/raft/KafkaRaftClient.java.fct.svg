<?xml version="1.0" encoding="UTF-8" standalone="no"?><svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" contentStyleType="text/css" height="1193px" preserveAspectRatio="none" style="width:1007px;height:1193px;background:#FFFFFF;" version="1.1" viewBox="0 0 1007 1193" width="1007px" zoomAndPan="magnify"><defs/><g><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="603" x="5" y="16.1387">An error has occured : java.lang.IndexOutOfBoundsException: Index 0 out of bounds for length 0</text><text fill="#000000" font-family="sans-serif" font-size="12" font-style="italic" lengthAdjust="spacing" textLength="211" x="5" y="30.1074">Hey, what do you want? Miracles?</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="44.0762"> </text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="280" x="5" y="58.0449">Diagram size: 2118 lines / 70270 characters.</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="72.0137"> </text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="374" x="5" y="85.9824">PlantUML (1.2022.7) cannot parse result from dot/GraphViz.</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="99.9512"> </text><text fill="#000000" font-family="sans-serif" font-size="12" font-weight="bold" lengthAdjust="spacing" textLength="390" x="5" y="113.9199">This version of PlantUML is 357 days old, so you should</text><text fill="#000000" font-family="sans-serif" font-size="12" font-weight="bold" lengthAdjust="spacing" textLength="398" x="5" y="127.8887">consider upgrading from https://plantuml.com/download</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="141.8574"> </text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="492" x="5" y="155.8262">Please go to https://plantuml.com/graphviz-dot to check your GraphViz version.</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="169.7949"> </text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="277" x="5" y="183.7637">Java Runtime: OpenJDK Runtime Environment</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="187" x="5" y="197.7324">JVM: OpenJDK 64-Bit Server VM</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="150" x="5" y="211.7012">Default Encoding: UTF-8</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="86" x="5" y="225.6699">Language: en</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="78" x="5" y="239.6387">Country: null</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="253.6074"> </text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="172" x="5" y="267.5762">PLANTUML_LIMIT_SIZE: 4096</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="281.5449"> </text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="150" x="5" y="295.5137">This may be caused by :</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="120" x="9" y="309.4824">- a bug in PlantUML</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="144" x="9" y="323.4512">- a problem in GraphViz</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="337.4199"> </text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="300" x="5" y="351.3887">You should send this diagram and this image to</text><text fill="#000000" font-family="sans-serif" font-size="12" font-weight="bold" lengthAdjust="spacing" textLength="145" x="309" y="351.3887">plantuml@gmail.com</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="13" x="458" y="351.3887">or</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="45" x="5" y="365.3574">post to</text><text fill="#000000" font-family="sans-serif" font-size="12" font-weight="bold" lengthAdjust="spacing" textLength="168" x="54" y="365.3574">https://plantuml.com/qa</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="117" x="226" y="365.3574">to solve this issue.</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="405" x="5" y="379.3262">You can try to turn arround this issue by simplifing your diagram.</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="4" x="5" y="393.2949"> </text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="458" x="5" y="407.2637">java.lang.IndexOutOfBoundsException: Index 0 out of bounds for length 0</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="477" x="13" y="421.2324">java.base/jdk.internal.util.Preconditions.outOfBounds(Preconditions.java:64)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="548" x="13" y="435.2012">java.base/jdk.internal.util.Preconditions.outOfBoundsCheckIndex(Preconditions.java:70)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="474" x="13" y="449.1699">java.base/jdk.internal.util.Preconditions.checkIndex(Preconditions.java:248)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="351" x="13" y="463.1387">java.base/java.util.Objects.checkIndex(Objects.java:372)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="314" x="13" y="477.1074">java.base/java.util.ArrayList.get(ArrayList.java:459)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="878" x="13" y="491.0762">net.sourceforge.plantuml.activitydiagram3.ftile.vcompact.FtileFactoryDelegatorSwitch.createWithLinks(FtileFactoryDelegatorSwitch.java:103)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="852" x="13" y="505.0449">net.sourceforge.plantuml.activitydiagram3.ftile.vcompact.FtileFactoryDelegatorSwitch.createSwitch(FtileFactoryDelegatorSwitch.java:79)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="716" x="13" y="519.0137">net.sourceforge.plantuml.activitydiagram3.ftile.FtileFactoryDelegator.createSwitch(FtileFactoryDelegator.java:185)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="716" x="13" y="532.9824">net.sourceforge.plantuml.activitydiagram3.ftile.FtileFactoryDelegator.createSwitch(FtileFactoryDelegator.java:185)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="716" x="13" y="546.9512">net.sourceforge.plantuml.activitydiagram3.ftile.FtileFactoryDelegator.createSwitch(FtileFactoryDelegator.java:185)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="716" x="13" y="560.9199">net.sourceforge.plantuml.activitydiagram3.ftile.FtileFactoryDelegator.createSwitch(FtileFactoryDelegator.java:185)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="716" x="13" y="574.8887">net.sourceforge.plantuml.activitydiagram3.ftile.FtileFactoryDelegator.createSwitch(FtileFactoryDelegator.java:185)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="621" x="13" y="588.8574">net.sourceforge.plantuml.activitydiagram3.InstructionSwitch.createFtile(InstructionSwitch.java:119)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="583" x="13" y="602.8262">net.sourceforge.plantuml.activitydiagram3.InstructionList.createFtile(InstructionList.java:131)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="536" x="13" y="616.7949">net.sourceforge.plantuml.activitydiagram3.ftile.Swimlanes.drawU(Swimlanes.java:229)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="805" x="13" y="630.7637">net.sourceforge.plantuml.ugraphic.comp.CompressionXorYBuilder.getPiecewiseAffineTransform(CompressionXorYBuilder.java:51)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="655" x="13" y="644.7324">net.sourceforge.plantuml.ugraphic.comp.CompressionXorYBuilder.build(CompressionXorYBuilder.java:44)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="686" x="13" y="658.7012">net.sourceforge.plantuml.activitydiagram3.ActivityDiagram3.exportDiagramInternal(ActivityDiagram3.java:210)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="502" x="13" y="672.6699">net.sourceforge.plantuml.UmlDiagram.exportDiagramNow(UmlDiagram.java:135)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="538" x="13" y="686.6387">net.sourceforge.plantuml.AbstractPSystem.exportDiagram(AbstractPSystem.java:178)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="566" x="13" y="700.6074">net.sourceforge.plantuml.SourceStringReader.outputImage(SourceStringReader.java:172)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="566" x="13" y="714.5762">net.sourceforge.plantuml.SourceStringReader.outputImage(SourceStringReader.java:143)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="693" x="13" y="728.5449">com.qualitesys.qcrmavenplugin.commun.QcrCommunPlantUml.genererFichierSvg(QcrCommunPlantUml.java:82)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="585" x="13" y="742.5137">com.qualitesys.qcrmavenplugin.QcrReporting01Mojo.lireFichier(QcrReporting01Mojo.java:927)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="556" x="13" y="756.4824">com.qualitesys.qcrmavenplugin.QcrReporting01Mojo.lister(QcrReporting01Mojo.java:453)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="591" x="13" y="770.4512">com.qualitesys.qcrmavenplugin.QcrReporting01Mojo.doReport(QcrReporting01Mojo.java:2037)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="625" x="13" y="784.4199">com.qualitesys.qcrmavenplugin.QcrReporting01Mojo.executeReport(QcrReporting01Mojo.java:1739)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="582" x="13" y="798.3887">org.apache.maven.reporting.AbstractMavenReport.generate(AbstractMavenReport.java:251)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="748" x="13" y="812.3574">org.apache.maven.plugins.site.render.ReportDocumentRenderer.renderDocument(ReportDocumentRenderer.java:226)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="607" x="13" y="826.3262">org.apache.maven.doxia.siterenderer.DefaultSiteRenderer.render(DefaultSiteRenderer.java:348)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="502" x="13" y="840.2949">org.apache.maven.plugins.site.render.SiteMojo.renderLocale(SiteMojo.java:194)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="470" x="13" y="854.2637">org.apache.maven.plugins.site.render.SiteMojo.execute(SiteMojo.java:143)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="649" x="13" y="868.2324">org.apache.maven.plugin.DefaultBuildPluginManager.executeMojo(DefaultBuildPluginManager.java:137)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="539" x="13" y="882.2012">org.apache.maven.lifecycle.internal.MojoExecutor.doExecute2(MojoExecutor.java:370)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="531" x="13" y="896.1699">org.apache.maven.lifecycle.internal.MojoExecutor.doExecute(MojoExecutor.java:351)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="515" x="13" y="910.1387">org.apache.maven.lifecycle.internal.MojoExecutor.execute(MojoExecutor.java:215)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="515" x="13" y="924.1074">org.apache.maven.lifecycle.internal.MojoExecutor.execute(MojoExecutor.java:171)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="515" x="13" y="938.0762">org.apache.maven.lifecycle.internal.MojoExecutor.execute(MojoExecutor.java:163)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="651" x="13" y="952.0449">org.apache.maven.lifecycle.internal.LifecycleModuleBuilder.buildProject(LifecycleModuleBuilder.java:117)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="643" x="13" y="966.0137">org.apache.maven.lifecycle.internal.LifecycleModuleBuilder.buildProject(LifecycleModuleBuilder.java:81)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="747" x="13" y="979.9824">org.apache.maven.lifecycle.internal.builder.singlethreaded.SingleThreadedBuilder.build(SingleThreadedBuilder.java:56)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="539" x="13" y="993.9512">org.apache.maven.lifecycle.internal.LifecycleStarter.execute(LifecycleStarter.java:128)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="428" x="13" y="1007.9199">org.apache.maven.DefaultMaven.doExecute(DefaultMaven.java:299)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="428" x="13" y="1021.8887">org.apache.maven.DefaultMaven.doExecute(DefaultMaven.java:193)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="412" x="13" y="1035.8574">org.apache.maven.DefaultMaven.execute(DefaultMaven.java:106)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="367" x="13" y="1049.8262">org.apache.maven.cli.MavenCli.execute(MavenCli.java:963)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="362" x="13" y="1063.7949">org.apache.maven.cli.MavenCli.doMain(MavenCli.java:296)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="347" x="13" y="1077.7637">org.apache.maven.cli.MavenCli.main(MavenCli.java:199)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="502" x="13" y="1091.7324">java.base/jdk.internal.reflect.NativeMethodAccessorImpl.invoke0(Native Method)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="621" x="13" y="1105.7012">java.base/jdk.internal.reflect.NativeMethodAccessorImpl.invoke(NativeMethodAccessorImpl.java:62)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="679" x="13" y="1119.6699">java.base/jdk.internal.reflect.DelegatingMethodAccessorImpl.invoke(DelegatingMethodAccessorImpl.java:43)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="373" x="13" y="1133.6387">java.base/java.lang.reflect.Method.invoke(Method.java:566)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="562" x="13" y="1147.6074">org.codehaus.plexus.classworlds.launcher.Launcher.launchEnhanced(Launcher.java:282)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="499" x="13" y="1161.5762">org.codehaus.plexus.classworlds.launcher.Launcher.launch(Launcher.java:225)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="568" x="13" y="1175.5449">org.codehaus.plexus.classworlds.launcher.Launcher.mainWithExitCode(Launcher.java:406)</text><text fill="#000000" font-family="sans-serif" font-size="12" lengthAdjust="spacing" textLength="487" x="13" y="1189.5137">org.codehaus.plexus.classworlds.launcher.Launcher.main(Launcher.java:347)</text><image height="43" width="87" x="920" xlink:href="data:image/png;base64," y="6"/><!--MD5=[d4d2ac9c76108da898aa1c8933b46641]
@startuml
!pragma layout smetana
start
note right
constructor KafkaRaftClient(RecordSerde<T> serde,
NetworkChannel channel,ReplicatedLog log,
QuorumStateStore quorumStateStore,Time time,
Metrics metrics,ExpirationService expirationService,
LogContext logContext,String clusterId,OptionalInt nodeId,
RaftConfig raftConfig)
end note
:this(serde,channel,new BlockingMessageQueue(),
log,quorumStateStore,new BatchMemoryPool(5,
MAX_BATCH_SIZE_BYTES),time,metrics,expirationService,
MAX_FETCH_WAIT_MS,clusterId,nodeId,logContext,
new Random(),raftConfig);;
end
start
note right
constructor KafkaRaftClient(RecordSerde<T> serde,
NetworkChannel channel,RaftMessageQueue messageQueue,
ReplicatedLog log,QuorumStateStore quorumStateStore,
MemoryPool memoryPool,Time time,Metrics metrics,
ExpirationService expirationService,int fetchMaxWaitMs,
String clusterId,OptionalInt nodeId,LogContext logContext,
Random random,RaftConfig raftConfig)
end note
:this.serde=serde;;
:this.channel=channel;;
:this.messageQueue=messageQueue;;
:this.log=log;;
:this.memoryPool=memoryPool;;
:this.fetchPurgatory=new ThresholdPurgatory<>(expirationService);;
:this.appendPurgatory=new ThresholdPurgatory<>(expirationService);;
:this.time=time;;
:this.clusterId=clusterId;;
:this.fetchMaxWaitMs=fetchMaxWaitMs;;
:this.logger=logContext.logger(KafkaRaftClient.class );;
:this.random=random;;
:this.raftConfig=raftConfig;;
:this.snapshotCleaner=new RaftMetadataLogCleanerManager(logger,
time,60000,log::maybeClean);;
:Set<Integer> quorumVoterIds=raftConfig.quorumVoterIds();;
:this.requestManager=new RequestManager(quorumVoterIds,
raftConfig.retryBackoffMs(),raftConfig.requestTimeoutMs(),
random);;
:this.quorum=new QuorumState(nodeId,quorumVoterIds,
raftConfig.electionTimeoutMs(),raftConfig.fetchTimeoutMs(),
quorumStateStore,time,logContext,random);;
:this.kafkaRaftMetrics=new KafkaRaftMetrics(metrics,
"raft",quorum);;
:kafkaRaftMetrics.updateNumUnknownVoterConnections(quorum.remoteVoters()
.size());;
:Map<Integer,RaftConfig.AddressSpec> voterAddresses=raftConfig.quorumVoterConnections();;
:voterAddresses.entrySet().stream().filter(e->e.getValue() instanceof RaftConfig.InetAddressSpec ).forEach(e->this.channel.updateEndpoint(e.getKey(),
(RaftConfig.InetAddressSpec )e.getValue()));;
end
start
note right
void updateFollowerHighWatermark(FollowerState state,
OptionalLong highWatermarkOpt)
end note
:highWatermarkOpt.ifPresent(highWatermark->{long newHighWatermark=Math.min(endOffset().offset,
highWatermark);if (state.updateHighWatermark(OptionalLong.of(newHighWatermark))){logger.debug("Follower high watermark updated to {}",
newHighWatermark);log.updateHighWatermark(new LogOffsetMetadata(newHighWatermark));updateListenersProgress(newHighWatermark);}});;
end
start
note right
void updateLeaderEndOffsetAndTimestamp(LeaderState<T> state,
long currentTimeMs)
end note
:final LogOffsetMetadata endOffsetMetadata=log.endOffset();;
if (state.updateLocalState(currentTimeMs,endOffsetMetadata)) then (yes) 
:onUpdateLeaderHighWatermark(state,currentTimeMs);;
endif 
:fetchPurgatory.maybeComplete(endOffsetMetadata.offset,
currentTimeMs);;
end
start
note right
void onUpdateLeaderHighWatermark(LeaderState<T> state,
long currentTimeMs)
end note
:state.highWatermark().ifPresent(highWatermark->{logger.debug("Leader high watermark updated to {}",
highWatermark);log.updateHighWatermark(highWatermark);appendPurgatory.maybeComplete(highWatermark.offset,
currentTimeMs);updateListenersProgress(highWatermark.offset);});;
end
start
note right
void updateListenersProgress(long highWatermark)
end note
:listenerContext.nextExpectedOffset().ifPresent(nextExpectedOffset->{if (nextExpectedOffset<log.startOffset()&&nextExpectedOffset<highWatermark){SnapshotReader<T> snapshot=latestSnapshot()
.orElseThrow(()->new IllegalStateException(String.format("Snapshot expected since next offset of %s is %d,
 log start offset is %d and high-watermark is %d",
listenerContext.listenerName(),nextExpectedOffset,
log.startOffset(),highWatermark)));listenerContext.fireHandleSnapshot(snapshot);}});;
:listenerContext.nextExpectedOffset().ifPresent(nextExpectedOffset->{if (nextExpectedOffset<highWatermark){LogFetchInfo readInfo=log.read(nextExpectedOffset,
Isolation.COMMITTED);listenerContext.fireHandleCommit(nextExpectedOffset,
readInfo.records);}});;
end
start
note right
Optional<SnapshotReader<T>> latestSnapshot()
end note
:return log.latestSnapshot().map(reader->RecordsSnapshotReader.of(reader,
serde,BufferSupplier.create(),MAX_BATCH_SIZE_BYTES));;
stop
end
start
note right
void maybeFireHandleCommit(long baseOffset,
int epoch,long appendTimestamp,int sizeInBytes,
List<T> records)
end note
:listenerContext.nextExpectedOffset().ifPresent(nextOffset->{if (nextOffset==baseOffset){listenerContext.fireHandleCommit(baseOffset,
epoch,appendTimestamp,sizeInBytes,records);}});;
end
start
note right
void maybeFireLeaderChange(LeaderState<T> state)
end note
:listenerContext.maybeFireLeaderChange(quorum.leaderAndEpoch(),
state.epochStartOffset());;
end
start
note right
void maybeFireLeaderChange()
end note
:listenerContext.maybeFireLeaderChange(quorum.leaderAndEpoch());;
end
start
note right
void initialize()
end note
:quorum.initialize(new OffsetAndEpoch(log.endOffset().offset,
log.lastFetchedEpoch()));;
:long currentTimeMs=time.milliseconds();;
if (quorum.isLeader()) then (yes) 
:throw new IllegalStateException("Voter cannot initialize as a Leader");;
stop
else if (quorum.isCandidate()) then (yes) 
:onBecomeCandidate(currentTimeMs);;
else if (quorum.isFollower()) then (yes) 
:onBecomeFollower(currentTimeMs);;
endif 
if (quorum.isVoter()&&quorum.remoteVoters().isEmpty()&&!quorum.isCandidate()) then (yes) 
:transitionToCandidate(currentTimeMs);;
endif 
end
start
note right
void register(Listener<T> listener)
end note
:pendingRegistrations.add(Registration.register(listener));;
:wakeup();;
end
start
note right
void unregister(Listener<T> listener)
end note
:pendingRegistrations.add(Registration.unregister(listener));;
end
start
note right
LeaderAndEpoch leaderAndEpoch()
end note
:return quorum.leaderAndEpoch();;
stop
end
start
note right
OptionalInt nodeId()
end note
:return quorum.localId();;
stop
end
start
note right
OffsetAndEpoch endOffset()
end note
:return new OffsetAndEpoch(log.endOffset().offset,
log.lastFetchedEpoch());;
stop
end
start
note right
void resetConnections()
end note
:requestManager.resetAll();;
end
start
note right
void onBecomeLeader(long currentTimeMs)
end note
:long endOffset=log.endOffset().offset;;
:BatchAccumulator<T> accumulator=new BatchAccumulator<>(quorum.epoch(),
endOffset,raftConfig.appendLingerMs(),MAX_BATCH_SIZE_BYTES,
memoryPool,time,CompressionType.NONE,serde);;
:LeaderState<T> state=quorum.transitionToLeader(endOffset,
accumulator);;
:maybeFireLeaderChange(state);;
:log.initializeLeaderEpoch(quorum.epoch());;
:state.appendLeaderChangeMessage(currentTimeMs);;
:resetConnections();;
:kafkaRaftMetrics.maybeUpdateElectionLatency(currentTimeMs);;
end
start
note right
void flushLeaderLog(LeaderState<T> state,
long currentTimeMs)
end note
:updateLeaderEndOffsetAndTimestamp(state,
currentTimeMs);;
:log.flush(false);;
end
start
note right
boolean maybeTransitionToLeader(CandidateState state,
long currentTimeMs)
end note
if (state.isVoteGranted()) then (yes) 
:onBecomeLeader(currentTimeMs);;
:return true;;
stop
else (no)
:return false;;
stop
endif 
end
start
note right
void onBecomeCandidate(long currentTimeMs)
end note
:CandidateState state=quorum.candidateStateOrThrow();;
if (!maybeTransitionToLeader(state,currentTimeMs)) then (yes) 
:resetConnections();;
:kafkaRaftMetrics.updateElectionStartMs(currentTimeMs);;
endif 
end
start
note right
void transitionToCandidate(long currentTimeMs)
end note
:quorum.transitionToCandidate();;
:maybeFireLeaderChange();;
:onBecomeCandidate(currentTimeMs);;
end
start
note right
void transitionToUnattached(int epoch)
end note
:quorum.transitionToUnattached(epoch);;
:maybeFireLeaderChange();;
:resetConnections();;
end
start
note right
void transitionToResigned(List<Integer> preferredSuccessors)
end note
:fetchPurgatory.completeAllExceptionally(Errors.NOT_LEADER_OR_FOLLOWER.exception("Not handling request since this node is resigning"));;
:quorum.transitionToResigned(preferredSuccessors);;
:maybeFireLeaderChange();;
:resetConnections();;
end
start
note right
void transitionToVoted(int candidateId,int epoch)
end note
:quorum.transitionToVoted(epoch,candidateId);;
:maybeFireLeaderChange();;
:resetConnections();;
end
start
note right
void onBecomeFollower(long currentTimeMs)
end note
:kafkaRaftMetrics.maybeUpdateElectionLatency(currentTimeMs);;
:resetConnections();;
:fetchPurgatory.completeAllExceptionally(new NotLeaderOrFollowerException("Cannot process the fetch request because the node is no longer the leader."));;
:appendPurgatory.completeAllExceptionally(new NotLeaderOrFollowerException("Failed to receive sufficient acknowledgments for this append before leader change."));;
end
start
note right
void transitionToFollower(int epoch,int leaderId,
long currentTimeMs)
end note
:quorum.transitionToFollower(epoch,leaderId);;
:maybeFireLeaderChange();;
:onBecomeFollower(currentTimeMs);;
end
start
note right
VoteResponseData buildVoteResponse(Errors partitionLevelError,
boolean voteGranted)
end note
:return VoteResponse.singletonResponse(Errors.NONE,
log.topicPartition(),partitionLevelError,
quorum.epoch(),quorum.leaderIdOrSentinel(),
voteGranted);;
stop
end
start
note right
VoteResponseData handleVoteRequest(RaftRequest.Inbound requestMetadata)
end note
:VoteRequestData request=(VoteRequestData )requestMetadata.data;;
if (!hasValidClusterId(request.clusterId())) then (yes) 
:return new VoteResponseData().setErrorCode(Errors.INCONSISTENT_CLUSTER_ID.code());;
stop
endif 
if (!hasValidTopicPartition(request,log.topicPartition())) then (yes) 
:return new VoteResponseData().setErrorCode(Errors.INVALID_REQUEST.code());;
stop
endif 
:VoteRequestData.PartitionData partitionRequest=request.topics()
.get(0).partitions().get(0);;
:int candidateId=partitionRequest.candidateId();;
:int candidateEpoch=partitionRequest.candidateEpoch();;
:int lastEpoch=partitionRequest.lastOffsetEpoch();;
:long lastEpochEndOffset=partitionRequest.lastOffset();;
if (lastEpochEndOffset<0||lastEpoch<0||lastEpoch>=candidateEpoch) then (yes) 
:return buildVoteResponse(Errors.INVALID_REQUEST,
false);;
stop
endif 
:Optional<Errors> errorOpt=validateVoterOnlyRequest(candidateId,
candidateEpoch);;
if (errorOpt.isPresent()) then (yes) 
:return buildVoteResponse(errorOpt.get(),
false);;
stop
endif 
if (candidateEpoch>quorum.epoch()) then (yes) 
:transitionToUnattached(candidateEpoch);;
endif 
:OffsetAndEpoch lastEpochEndOffsetAndEpoch=new OffsetAndEpoch(lastEpochEndOffset,
lastEpoch);;
:boolean voteGranted=quorum.canGrantVote(candidateId,
lastEpochEndOffsetAndEpoch.compareTo(endOffset())>=0);;
if (voteGranted&&quorum.isUnattached()) then (yes) 
:transitionToVoted(candidateId,candidateEpoch);;
endif 
:logger.info("Vote request {} with epoch {} is {}",
request,candidateEpoch,voteGranted? String.valueOf("granted"):String.valueOf("rejected"));;
:return buildVoteResponse(Errors.NONE,voteGranted);;
stop
end
start
note right
boolean handleVoteResponse(RaftResponse.Inbound responseMetadata,
long currentTimeMs)
end note
:int remoteNodeId=responseMetadata.sourceId();;
:VoteResponseData response=(VoteResponseData )responseMetadata.data;;
:Errors topLevelError=Errors.forCode(response.errorCode());;
if (topLevelError!=Errors.NONE) then (yes) 
:return handleTopLevelError(topLevelError,
responseMetadata);;
stop
endif 
if (!hasValidTopicPartition(response,log.topicPartition())) then (yes) 
:return false;;
stop
endif 
:VoteResponseData.PartitionData partitionResponse=response.topics()
.get(0).partitions().get(0);;
:Errors error=Errors.forCode(partitionResponse.errorCode());;
:OptionalInt responseLeaderId=optionalLeaderId(partitionResponse.leaderId());;
:int responseEpoch=partitionResponse.leaderEpoch();;
:Optional<Boolean> handled=maybeHandleCommonResponse(error,
responseLeaderId,responseEpoch,currentTimeMs);;
if (handled.isPresent()) then (yes) 
:return handled.get();;
stop
else if (error==Errors.NONE) then (yes) 
if (quorum.isLeader()) then (yes) 
:logger.debug("Ignoring vote response {} since we already became leader for epoch {}",
partitionResponse,quorum.epoch());;
else if (quorum.isCandidate()) then (yes) 
:CandidateState state=quorum.candidateStateOrThrow();;
if (partitionResponse.voteGranted()) then (yes) 
:state.recordGrantedVote(remoteNodeId);;
:maybeTransitionToLeader(state,currentTimeMs);;
else (no)
:state.recordRejectedVote(remoteNodeId);;
if (state.isVoteRejected()&&!state.isBackingOff()) then (yes) 
:logger.info("Insufficient remaining votes to become leader (rejected by {}). "+
"We will backoff before retrying election again",
state.rejectingVoters());;
:state.startBackingOff(currentTimeMs,binaryExponentialElectionBackoffMs(state.retries()));;
endif 
endif 
else (no)
:logger.debug("Ignoring vote response {} since we are no longer a candidate in epoch {}",
partitionResponse,quorum.epoch());;
endif 
:return true;;
stop
else (no)
:return handleUnexpectedError(error,responseMetadata);;
stop
endif 
end
start
note right
int binaryExponentialElectionBackoffMs(int retries)
end note
if (retries<=0) then (yes) 
:throw new IllegalArgumentException("Retries "+
retries+" should be larger than zero");;
stop
endif 
:return Math.min(RETRY_BACKOFF_BASE_MS*random.nextInt(2<<Math.min(20,
retries-1)),raftConfig.electionBackoffMaxMs());;
stop
end
start
note right
int strictExponentialElectionBackoffMs(int positionInSuccessors,
int totalNumSuccessors)
end note
if (positionInSuccessors<=0||positionInSuccessors>=totalNumSuccessors) then (yes) 
:throw new IllegalArgumentException("Position "+
positionInSuccessors+" should be larger than zero"+
" and smaller than total number of successors "+
totalNumSuccessors);;
stop
endif 
:int retryBackOffBaseMs=raftConfig.electionBackoffMaxMs()>>(totalNumSuccessors-1);;
:return Math.min(raftConfig.electionBackoffMaxMs(),
retryBackOffBaseMs<<(positionInSuccessors-1));;
stop
end
start
note right
BeginQuorumEpochResponseData buildBeginQuorumEpochResponse(Errors partitionLevelError)
end note
:return BeginQuorumEpochResponse.singletonResponse(Errors.NONE,
log.topicPartition(),partitionLevelError,
quorum.epoch(),quorum.leaderIdOrSentinel());;
stop
end
start
note right
BeginQuorumEpochResponseData handleBeginQuorumEpochRequest(RaftRequest.Inbound requestMetadata,
long currentTimeMs)
end note
:BeginQuorumEpochRequestData request=(BeginQuorumEpochRequestData )requestMetadata.data;;
if (!hasValidClusterId(request.clusterId())) then (yes) 
:return new BeginQuorumEpochResponseData()
.setErrorCode(Errors.INCONSISTENT_CLUSTER_ID.code());;
stop
endif 
if (!hasValidTopicPartition(request,log.topicPartition())) then (yes) 
:return new BeginQuorumEpochResponseData()
.setErrorCode(Errors.INVALID_REQUEST.code());;
stop
endif 
:BeginQuorumEpochRequestData.PartitionData partitionRequest=request.topics()
.get(0).partitions().get(0);;
:int requestLeaderId=partitionRequest.leaderId();;
:int requestEpoch=partitionRequest.leaderEpoch();;
:Optional<Errors> errorOpt=validateVoterOnlyRequest(requestLeaderId,
requestEpoch);;
if (errorOpt.isPresent()) then (yes) 
:return buildBeginQuorumEpochResponse(errorOpt.get());;
stop
endif 
:maybeTransition(OptionalInt.of(requestLeaderId),
requestEpoch,currentTimeMs);;
:return buildBeginQuorumEpochResponse(Errors.NONE);;
stop
end
start
note right
boolean handleBeginQuorumEpochResponse(RaftResponse.Inbound responseMetadata,
long currentTimeMs)
end note
:int remoteNodeId=responseMetadata.sourceId();;
:BeginQuorumEpochResponseData response=(BeginQuorumEpochResponseData )responseMetadata.data;;
:Errors topLevelError=Errors.forCode(response.errorCode());;
if (topLevelError!=Errors.NONE) then (yes) 
:return handleTopLevelError(topLevelError,
responseMetadata);;
stop
endif 
if (!hasValidTopicPartition(response,log.topicPartition())) then (yes) 
:return false;;
stop
endif 
:BeginQuorumEpochResponseData.PartitionData partitionResponse=response.topics()
.get(0).partitions().get(0);;
:Errors partitionError=Errors.forCode(partitionResponse.errorCode());;
:OptionalInt responseLeaderId=optionalLeaderId(partitionResponse.leaderId());;
:int responseEpoch=partitionResponse.leaderEpoch();;
:Optional<Boolean> handled=maybeHandleCommonResponse(partitionError,
responseLeaderId,responseEpoch,currentTimeMs);;
if (handled.isPresent()) then (yes) 
:return handled.get();;
stop
else if (partitionError==Errors.NONE) then (yes) 
if (quorum.isLeader()) then (yes) 
:LeaderState<T> state=quorum.leaderStateOrThrow();;
:state.addAcknowledgementFrom(remoteNodeId);;
else (no)
:logger.debug("Ignoring BeginQuorumEpoch response {} since "+
"this node is not the leader anymore",response);;
endif 
:return true;;
stop
else (no)
:return handleUnexpectedError(partitionError,
responseMetadata);;
stop
endif 
end
start
note right
EndQuorumEpochResponseData buildEndQuorumEpochResponse(Errors partitionLevelError)
end note
:return EndQuorumEpochResponse.singletonResponse(Errors.NONE,
log.topicPartition(),partitionLevelError,
quorum.epoch(),quorum.leaderIdOrSentinel());;
stop
end
start
note right
EndQuorumEpochResponseData handleEndQuorumEpochRequest(RaftRequest.Inbound requestMetadata,
long currentTimeMs)
end note
:EndQuorumEpochRequestData request=(EndQuorumEpochRequestData )requestMetadata.data;;
if (!hasValidClusterId(request.clusterId())) then (yes) 
:return new EndQuorumEpochResponseData().setErrorCode(Errors.INCONSISTENT_CLUSTER_ID.code());;
stop
endif 
if (!hasValidTopicPartition(request,log.topicPartition())) then (yes) 
:return new EndQuorumEpochResponseData().setErrorCode(Errors.INVALID_REQUEST.code());;
stop
endif 
:EndQuorumEpochRequestData.PartitionData partitionRequest=request.topics()
.get(0).partitions().get(0);;
:int requestEpoch=partitionRequest.leaderEpoch();;
:int requestLeaderId=partitionRequest.leaderId();;
:Optional<Errors> errorOpt=validateVoterOnlyRequest(requestLeaderId,
requestEpoch);;
if (errorOpt.isPresent()) then (yes) 
:return buildEndQuorumEpochResponse(errorOpt.get());;
stop
endif 
:maybeTransition(OptionalInt.of(requestLeaderId),
requestEpoch,currentTimeMs);;
if (quorum.isFollower()) then (yes) 
:FollowerState state=quorum.followerStateOrThrow();;
if (state.leaderId()==requestLeaderId) then (yes) 
:List<Integer> preferredSuccessors=partitionRequest.preferredSuccessors();;
:long electionBackoffMs=endEpochElectionBackoff(preferredSuccessors);;
:logger.debug("Overriding follower fetch timeout to {} after receiving "+
"EndQuorumEpoch request from leader {} in epoch {}",
electionBackoffMs,requestLeaderId,requestEpoch);;
:state.overrideFetchTimeout(currentTimeMs,
electionBackoffMs);;
endif 
endif 
:return buildEndQuorumEpochResponse(Errors.NONE);;
stop
end
start
note right
long endEpochElectionBackoff(List<Integer> preferredSuccessors)
end note
:int position=preferredSuccessors.indexOf(quorum.localIdOrThrow());;
if (position<=0) then (yes) 
:return 0;;
stop
else (no)
:return strictExponentialElectionBackoffMs(position,
preferredSuccessors.size());;
stop
endif 
end
start
note right
boolean handleEndQuorumEpochResponse(RaftResponse.Inbound responseMetadata,
long currentTimeMs)
end note
:EndQuorumEpochResponseData response=(EndQuorumEpochResponseData )responseMetadata.data;;
:Errors topLevelError=Errors.forCode(response.errorCode());;
if (topLevelError!=Errors.NONE) then (yes) 
:return handleTopLevelError(topLevelError,
responseMetadata);;
stop
endif 
if (!hasValidTopicPartition(response,log.topicPartition())) then (yes) 
:return false;;
stop
endif 
:EndQuorumEpochResponseData.PartitionData partitionResponse=response.topics()
.get(0).partitions().get(0);;
:Errors partitionError=Errors.forCode(partitionResponse.errorCode());;
:OptionalInt responseLeaderId=optionalLeaderId(partitionResponse.leaderId());;
:int responseEpoch=partitionResponse.leaderEpoch();;
:Optional<Boolean> handled=maybeHandleCommonResponse(partitionError,
responseLeaderId,responseEpoch,currentTimeMs);;
if (handled.isPresent()) then (yes) 
:return handled.get();;
stop
else if (partitionError==Errors.NONE) then (yes) 
:ResignedState resignedState=quorum.resignedStateOrThrow();;
:resignedState.acknowledgeResignation(responseMetadata.sourceId());;
:return true;;
stop
else (no)
:return handleUnexpectedError(partitionError,
responseMetadata);;
stop
endif 
end
start
note right
FetchResponseData buildFetchResponse(Errors error,
Records records,ValidOffsetAndEpoch validOffsetAndEpoch,
Optional<LogOffsetMetadata> highWatermark)
end note
:return RaftUtil.singletonFetchResponse(log.topicPartition(),
log.topicId(),Errors.NONE,partitionData->{partitionData.setRecords(records).setErrorCode(error.code()).setLogStartOffset(log.startOffset()).setHighWatermark(highWatermark.map(offsetMetadata->offsetMetadata.offset).orElse(-1L));partitionData.currentLeader().setLeaderEpoch(quorum.epoch()).setLeaderId(quorum.leaderIdOrSentinel());switch (validOffsetAndEpoch.kind()){case DIVERGING:partitionData.divergingEpoch().setEpoch(validOffsetAndEpoch.offsetAndEpoch().epoch).setEndOffset(validOffsetAndEpoch.offsetAndEpoch().offset);break ;case SNAPSHOT:partitionData.snapshotId().setEpoch(validOffsetAndEpoch.offsetAndEpoch().epoch).setEndOffset(validOffsetAndEpoch.offsetAndEpoch().offset);break ;default :}});;
stop
end
start
note right
FetchResponseData buildEmptyFetchResponse(Errors error,
Optional<LogOffsetMetadata> highWatermark)
end note
:return buildFetchResponse(error,MemoryRecords.EMPTY,
ValidOffsetAndEpoch.valid(),highWatermark);;
stop
end
start
note right
boolean hasValidClusterId(String requestClusterId)
end note
if (requestClusterId==null ) then (yes) 
:return true;;
stop
endif 
:return clusterId.equals(requestClusterId);;
stop
end
start
note right
CompletableFuture<FetchResponseData> handleFetchRequest(RaftRequest.Inbound requestMetadata,
long currentTimeMs)
end note
:FetchRequestData request=(FetchRequestData )requestMetadata.data;;
if (!hasValidClusterId(request.clusterId())) then (yes) 
:return completedFuture(new FetchResponseData()
.setErrorCode(Errors.INCONSISTENT_CLUSTER_ID.code()));;
stop
endif 
if (!hasValidTopicPartition(request,log.topicPartition(),log.topicId())) then (yes) 
:return completedFuture(new FetchResponseData()
.setErrorCode(Errors.INVALID_REQUEST.code()));;
stop
endif 
:request.topics().get(0).setTopic(log.topicPartition()
.topic());;
:FetchRequestData.FetchPartition fetchPartition=request.topics()
.get(0).partitions().get(0);;
if (request.maxWaitMs()<0||fetchPartition.fetchOffset()<0||fetchPartition.lastFetchedEpoch()<0||fetchPartition.lastFetchedEpoch()>fetchPartition.currentLeaderEpoch()) then (yes) 
:return completedFuture(buildEmptyFetchResponse(Errors.INVALID_REQUEST,
Optional.empty()));;
stop
endif 
:FetchResponseData response=tryCompleteFetchRequest(request.replicaId(),
fetchPartition,currentTimeMs);;
:FetchResponseData.PartitionData partitionResponse=response.responses()
.get(0).partitions().get(0);;
if (partitionResponse.errorCode()!=Errors.NONE.code()||FetchResponse.recordsSize(partitionResponse)>0||request.maxWaitMs()==0) then (yes) 
:return completedFuture(response);;
stop
endif 
:CompletableFuture<Long> future=fetchPurgatory.await(fetchPartition.fetchOffset(),
request.maxWaitMs());;
:return future.handle((completionTimeMs,exception)->{if (exception!=null ){Throwable cause=exception instanceof ExecutionException ? exception.getCause():exception;
:Errors error=Errors.forException(cause);if (error!=Errors.REQUEST_TIMED_OUT){logger.debug("Failed to handle fetch from {} at {} due to {}",
request.replicaId(),fetchPartition.fetchOffset(),
error);return buildEmptyFetchResponse(error,
Optional.empty());}}logger.trace("Completing delayed fetch from {} starting at offset {} at {}",
request.replicaId(),fetchPartition.fetchOffset(),
completionTimeMs);return tryCompleteFetchRequest(request.replicaId(),
fetchPartition,time.milliseconds());});;
stop
end
start
note right
FetchResponseData tryCompleteFetchRequest(int replicaId,
FetchRequestData.FetchPartition request,long currentTimeMs)
end note
switch (try/catch block)
case (try)
:Optional<Errors> errorOpt=validateLeaderOnlyRequest(request.currentLeaderEpoch());;
if (errorOpt.isPresent()) then (yes) 
:return buildEmptyFetchResponse(errorOpt.get(),
Optional.empty());;
stop
endif 
:long fetchOffset=request.fetchOffset();;
:int lastFetchedEpoch=request.lastFetchedEpoch();;
:LeaderState<T> state=quorum.leaderStateOrThrow();;
:ValidOffsetAndEpoch validOffsetAndEpoch=log.validateOffsetAndEpoch(fetchOffset,
lastFetchedEpoch);;
:final Records records;;
if (validOffsetAndEpoch.kind()==ValidOffsetAndEpoch.Kind.VALID) then (yes) 
:LogFetchInfo info=log.read(fetchOffset,Isolation.UNCOMMITTED);;
if (state.updateReplicaState(replicaId,currentTimeMs,info.startOffsetMetadata)) then (yes) 
:onUpdateLeaderHighWatermark(state,currentTimeMs);;
endif 
:records=info.records;;
else (no)
:records=MemoryRecords.EMPTY;;
endif 
:return buildFetchResponse(Errors.NONE,records,
validOffsetAndEpoch,state.highWatermark());;
stop
case (catch (Exception e))
:logger.error("Caught unexpected error in fetch completion of request {}",
request,e);;
:return buildEmptyFetchResponse(Errors.UNKNOWN_SERVER_ERROR,
Optional.empty());;
stop
endswitch
end
start
note right
OptionalInt optionalLeaderId(int leaderIdOrNil)
end note
if (leaderIdOrNil<0) then (yes) 
:return OptionalInt.empty();;
stop
endif 
:return OptionalInt.of(leaderIdOrNil);;
stop
end
start
note right
String listenerName(Listener<?> listener)
end note
:return String.format("%s@%d",listener.getClass().getTypeName(),
System.identityHashCode(listener));;
stop
end
start
note right
boolean handleFetchResponse(RaftResponse.Inbound responseMetadata,
long currentTimeMs)
end note
:FetchResponseData response=(FetchResponseData )responseMetadata.data;;
:Errors topLevelError=Errors.forCode(response.errorCode());;
if (topLevelError!=Errors.NONE) then (yes) 
:return handleTopLevelError(topLevelError,
responseMetadata);;
stop
endif 
if (!RaftUtil.hasValidTopicPartition(response,log.topicPartition(),log.topicId())) then (yes) 
:return false;;
stop
endif 
:response.responses().get(0).setTopic(log.topicPartition()
.topic());;
:FetchResponseData.PartitionData partitionResponse=response.responses()
.get(0).partitions().get(0);;
:FetchResponseData.LeaderIdAndEpoch currentLeaderIdAndEpoch=partitionResponse.currentLeader();;
:OptionalInt responseLeaderId=optionalLeaderId(currentLeaderIdAndEpoch.leaderId());;
:int responseEpoch=currentLeaderIdAndEpoch.leaderEpoch();;
:Errors error=Errors.forCode(partitionResponse.errorCode());;
:Optional<Boolean> handled=maybeHandleCommonResponse(error,
responseLeaderId,responseEpoch,currentTimeMs);;
if (handled.isPresent()) then (yes) 
:return handled.get();;
stop
endif 
:FollowerState state=quorum.followerStateOrThrow();;
if (error==Errors.NONE) then (yes) 
:FetchResponseData.EpochEndOffset divergingEpoch=partitionResponse.divergingEpoch();;
if (divergingEpoch.epoch()>=0) then (yes) 
:final OffsetAndEpoch divergingOffsetAndEpoch=new OffsetAndEpoch(divergingEpoch.endOffset(),
divergingEpoch.epoch());;
:state.highWatermark().ifPresent(highWatermark->{if (divergingOffsetAndEpoch.offset<highWatermark.offset){throw new KafkaException("The leader requested truncation to offset "+
divergingOffsetAndEpoch.offset+", which is below the current high watermark"+
" "+highWatermark);}});;
:long truncationOffset=log.truncateToEndOffset(divergingOffsetAndEpoch);;
:logger.info("Truncated to offset {} from Fetch response from leader {}",
truncationOffset,quorum.leaderIdOrSentinel());;
else if (partitionResponse.snapshotId().epoch()>=0||partitionResponse.snapshotId().endOffset()>=0) then (yes) 
if (partitionResponse.snapshotId().epoch()<0) then (yes) 
:logger.error("The leader sent a snapshot id with a valid end offset {} but with an invalid epoch {}",
partitionResponse.snapshotId().endOffset(),
partitionResponse.snapshotId().epoch());;
:return false;;
stop
else if (partitionResponse.snapshotId().endOffset()<0) then (yes) 
:logger.error("The leader sent a snapshot id with a valid epoch {} but with an invalid end offset {}",
partitionResponse.snapshotId().epoch(),partitionResponse.snapshotId().endOffset());;
:return false;;
stop
else (no)
:final OffsetAndEpoch snapshotId=new OffsetAndEpoch(partitionResponse.snapshotId().endOffset(),
partitionResponse.snapshotId().epoch());;
:state.setFetchingSnapshot(log.storeSnapshot(snapshotId));;
endif 
else (no)
:Records records=FetchResponse.recordsOrFail(partitionResponse);;
if (records.sizeInBytes()>0) then (yes) 
:appendAsFollower(records);;
endif 
:OptionalLong highWatermark=partitionResponse.highWatermark()<0? OptionalLong.empty():OptionalLong.of(partitionResponse.highWatermark());;
:updateFollowerHighWatermark(state,highWatermark);;
endif 
:state.resetFetchTimeout(currentTimeMs);;
:return true;;
stop
else (no)
:return handleUnexpectedError(error,responseMetadata);;
stop
endif 
end
start
note right
void appendAsFollower(Records records)
end note
:LogAppendInfo info=log.appendAsFollower(records);;
:log.flush(false);;
:OffsetAndEpoch endOffset=endOffset();;
:kafkaRaftMetrics.updateFetchedRecords(info.lastOffset-info.firstOffset+1);;
:kafkaRaftMetrics.updateLogEnd(endOffset);;
:logger.trace("Follower end offset updated to {} after append",
endOffset);;
end
start
note right
LogAppendInfo appendAsLeader(Records records)
end note
:LogAppendInfo info=log.appendAsLeader(records,
quorum.epoch());;
:OffsetAndEpoch endOffset=endOffset();;
:kafkaRaftMetrics.updateAppendRecords(info.lastOffset-info.firstOffset+1);;
:kafkaRaftMetrics.updateLogEnd(endOffset);;
:logger.trace("Leader appended records at base offset {},
 new end offset is {}",info.firstOffset,endOffset);;
:return info;;
stop
end
start
note right
DescribeQuorumResponseData handleDescribeQuorumRequest(RaftRequest.Inbound requestMetadata,
long currentTimeMs)
end note
:DescribeQuorumRequestData describeQuorumRequestData=(DescribeQuorumRequestData )requestMetadata.data;;
if (!hasValidTopicPartition(describeQuorumRequestData,log.topicPartition())) then (yes) 
:return DescribeQuorumRequest.getPartitionLevelErrorResponse(describeQuorumRequestData,
Errors.UNKNOWN_TOPIC_OR_PARTITION);;
stop
endif 
if (!quorum.isLeader()) then (yes) 
:return DescribeQuorumRequest.getTopLevelErrorResponse(Errors.INVALID_REQUEST);;
stop
endif 
:LeaderState<T> leaderState=quorum.leaderStateOrThrow();;
:return DescribeQuorumResponse.singletonResponse(log.topicPartition(),
leaderState.localId(),leaderState.epoch(),
leaderState.highWatermark().isPresent()? leaderState.highWatermark().get().offset:-1,
convertToReplicaStates(leaderState.getVoterEndOffsets()),
convertToReplicaStates(leaderState.getObserverStates(currentTimeMs)));;
stop
end
start
note right
FetchSnapshotResponseData handleFetchSnapshotRequest(RaftRequest.Inbound requestMetadata)
end note
:FetchSnapshotRequestData data=(FetchSnapshotRequestData )requestMetadata.data;;
if (!hasValidClusterId(data.clusterId())) then (yes) 
:return new FetchSnapshotResponseData().setErrorCode(Errors.INCONSISTENT_CLUSTER_ID.code());;
stop
endif 
if (data.topics().size()!=1&&data.topics().get(0).partitions().size()!=1) then (yes) 
:return FetchSnapshotResponse.withTopLevelError(Errors.INVALID_REQUEST);;
stop
endif 
:Optional<FetchSnapshotRequestData.PartitionSnapshot> partitionSnapshotOpt=FetchSnapshotRequest.forTopicPartition(data,
log.topicPartition());;
if (!partitionSnapshotOpt.isPresent()) then (yes) 
:TopicPartition unknownTopicPartition=new TopicPartition(data.topics().get(0).name(),
data.topics().get(0).partitions().get(0).partition());;
:return FetchSnapshotResponse.singleton(unknownTopicPartition,
responsePartitionSnapshot->responsePartitionSnapshot.setErrorCode(Errors.UNKNOWN_TOPIC_OR_PARTITION.code()));;
stop
endif 
:FetchSnapshotRequestData.PartitionSnapshot partitionSnapshot=partitionSnapshotOpt.get();;
:Optional<Errors> leaderValidation=validateLeaderOnlyRequest(partitionSnapshot.currentLeaderEpoch());;
if (leaderValidation.isPresent()) then (yes) 
:return FetchSnapshotResponse.singleton(log.topicPartition(),
responsePartitionSnapshot->addQuorumLeader(responsePartitionSnapshot).setErrorCode(leaderValidation.get().code()));;
stop
endif 
:OffsetAndEpoch snapshotId=new OffsetAndEpoch(partitionSnapshot.snapshotId().endOffset(),
partitionSnapshot.snapshotId().epoch());;
:Optional<RawSnapshotReader> snapshotOpt=log.readSnapshot(snapshotId);;
if (!snapshotOpt.isPresent()) then (yes) 
:return FetchSnapshotResponse.singleton(log.topicPartition(),
responsePartitionSnapshot->addQuorumLeader(responsePartitionSnapshot).setErrorCode(Errors.SNAPSHOT_NOT_FOUND.code()));;
stop
endif 
:RawSnapshotReader snapshot=snapshotOpt.get();;
:long snapshotSize=snapshot.sizeInBytes();;
if (partitionSnapshot.position()<0||partitionSnapshot.position()>=snapshotSize) then (yes) 
:return FetchSnapshotResponse.singleton(log.topicPartition(),
responsePartitionSnapshot->addQuorumLeader(responsePartitionSnapshot).setErrorCode(Errors.POSITION_OUT_OF_RANGE.code()));;
stop
endif 
if (partitionSnapshot.position()>Integer.MAX_VALUE) then (yes) 
:throw new IllegalStateException(String.format("Trying to fetch a snapshot with size (%d) and a position (%d) larger than %d",
snapshotSize,partitionSnapshot.position(),
Integer.MAX_VALUE));;
stop
endif 
:int maxSnapshotSize;;
switch (try/catch block)
case (try)
:maxSnapshotSize=Math.toIntExact(snapshotSize);;
case (catch (ArithmeticException e))
:maxSnapshotSize=Integer.MAX_VALUE;;
endswitch
:UnalignedRecords records=snapshot.slice(partitionSnapshot.position(),
Math.min(data.maxBytes(),maxSnapshotSize));;
:return FetchSnapshotResponse.singleton(log.topicPartition(),
responsePartitionSnapshot->{addQuorumLeader(responsePartitionSnapshot).snapshotId().setEndOffset(snapshotId.offset).setEpoch(snapshotId.epoch);return responsePartitionSnapshot.setSize(snapshotSize).setPosition(partitionSnapshot.position()).setUnalignedRecords(records);});;
stop
end
start
note right
boolean handleFetchSnapshotResponse(RaftResponse.Inbound responseMetadata,
long currentTimeMs)
end note
:FetchSnapshotResponseData data=(FetchSnapshotResponseData )responseMetadata.data;;
:Errors topLevelError=Errors.forCode(data.errorCode());;
if (topLevelError!=Errors.NONE) then (yes) 
:return handleTopLevelError(topLevelError,
responseMetadata);;
stop
endif 
if (data.topics().size()!=1&&data.topics().get(0).partitions().size()!=1) then (yes) 
:return false;;
stop
endif 
:Optional<FetchSnapshotResponseData.PartitionSnapshot> partitionSnapshotOpt=FetchSnapshotResponse.forTopicPartition(data,
log.topicPartition());;
if (!partitionSnapshotOpt.isPresent()) then (yes) 
:return false;;
stop
endif 
:FetchSnapshotResponseData.PartitionSnapshot partitionSnapshot=partitionSnapshotOpt.get();;
:FetchSnapshotResponseData.LeaderIdAndEpoch currentLeaderIdAndEpoch=partitionSnapshot.currentLeader();;
:OptionalInt responseLeaderId=optionalLeaderId(currentLeaderIdAndEpoch.leaderId());;
:int responseEpoch=currentLeaderIdAndEpoch.leaderEpoch();;
:Errors error=Errors.forCode(partitionSnapshot.errorCode());;
:Optional<Boolean> handled=maybeHandleCommonResponse(error,
responseLeaderId,responseEpoch,currentTimeMs);;
if (handled.isPresent()) then (yes) 
:return handled.get();;
stop
endif 
:FollowerState state=quorum.followerStateOrThrow();;
if (Errors.forCode(partitionSnapshot.errorCode())==Errors.SNAPSHOT_NOT_FOUND||partitionSnapshot.snapshotId().endOffset()<0||partitionSnapshot.snapshotId().epoch()<0) then (yes) 
:logger.trace("Leader doesn't know about snapshot id {},
 returned error {} and snapshot id {}",state.fetchingSnapshot(),
partitionSnapshot.errorCode(),partitionSnapshot.snapshotId());;
:state.setFetchingSnapshot(Optional.empty());;
:state.resetFetchTimeout(currentTimeMs);;
:return true;;
stop
endif 
:OffsetAndEpoch snapshotId=new OffsetAndEpoch(partitionSnapshot.snapshotId().endOffset(),
partitionSnapshot.snapshotId().epoch());;
:RawSnapshotWriter snapshot;;
if (state.fetchingSnapshot().isPresent()) then (yes) 
:snapshot=state.fetchingSnapshot().get();;
else (no)
:throw new IllegalStateException(String.format("Received unexpected fetch snapshot response: %s",
partitionSnapshot));;
stop
endif 
if (!snapshot.snapshotId().equals(snapshotId)) then (yes) 
:throw new IllegalStateException(String.format("Received fetch snapshot response with an invalid id. Expected %s; Received %s",
snapshot.snapshotId(),snapshotId));;
stop
endif 
if (snapshot.sizeInBytes()!=partitionSnapshot.position()) then (yes) 
:throw new IllegalStateException(String.format("Received fetch snapshot response with an invalid position. Expected %d; Received %d",
snapshot.sizeInBytes(),partitionSnapshot.position()));;
stop
endif 
:final UnalignedMemoryRecords records;;
if (partitionSnapshot.unalignedRecords() instanceof MemoryRecords ) then (yes) 
:records=new UnalignedMemoryRecords(((MemoryRecords )partitionSnapshot.unalignedRecords())
.buffer());;
else if (partitionSnapshot.unalignedRecords() instanceof UnalignedMemoryRecords ) then (yes) 
:records=(UnalignedMemoryRecords )partitionSnapshot.unalignedRecords();;
else (no)
:throw new IllegalStateException(String.format("Received unexpected fetch snapshot response: %s",
partitionSnapshot));;
stop
endif 
:snapshot.append(records);;
if (snapshot.sizeInBytes()==partitionSnapshot.size()) then (yes) 
:snapshot.freeze();;
:state.setFetchingSnapshot(Optional.empty());;
if (log.truncateToLatestSnapshot()) then (yes) 
:updateFollowerHighWatermark(state,OptionalLong.of(log.highWatermark()
.offset));;
else (no)
:throw new IllegalStateException(String.format("Full log truncation expected but didn't happen. Snapshot of %s,
 log end offset %s, last fetched %d",snapshot.snapshotId(),
log.endOffset(),log.lastFetchedEpoch()));;
stop
endif 
endif 
:state.resetFetchTimeout(currentTimeMs);;
:return true;;
stop
end
start
note right
List<ReplicaState> convertToReplicaStates(Map<Integer,
Long> replicaEndOffsets)
end note
:return replicaEndOffsets.entrySet().stream()
.map(entry->new ReplicaState().setReplicaId(entry.getKey()).setLogEndOffset(entry.getValue())).collect(Collectors.toList());;
stop
end
start
note right
boolean hasConsistentLeader(int epoch,OptionalInt leaderId)
end note
if (leaderId.isPresent()&&leaderId.getAsInt()==quorum.localIdOrSentinel()) then (yes) 
:return quorum.isLeader();;
stop
else (no)
:return epoch!=quorum.epoch()||!leaderId.isPresent()||!quorum.leaderId()
.isPresent()||leaderId.equals(quorum.leaderId());;
stop
endif 
end
start
note right
Optional<Boolean> maybeHandleCommonResponse(Errors error,
OptionalInt leaderId,int epoch,long currentTimeMs)
end note
if (epoch<quorum.epoch()||error==Errors.UNKNOWN_LEADER_EPOCH) then (yes) 
:return Optional.of(true);;
stop
else if (epoch>quorum.epoch()||error==Errors.FENCED_LEADER_EPOCH||error==Errors.NOT_LEADER_OR_FOLLOWER) then (yes) 
:maybeTransition(leaderId,epoch,currentTimeMs);;
:return Optional.of(true);;
stop
else if (epoch==quorum.epoch()&&leaderId.isPresent()&&!quorum.hasLeader()) then (yes) 
:transitionToFollower(epoch,leaderId.getAsInt(),
currentTimeMs);;
if (error==Errors.NONE) then (yes) 
:return Optional.empty();;
stop
else (no)
:return Optional.of(true);;
stop
endif 
else if (error==Errors.BROKER_NOT_AVAILABLE) then (yes) 
:return Optional.of(false);;
stop
else if (error==Errors.INCONSISTENT_GROUP_PROTOCOL) then (yes) 
:throw new IllegalStateException("Received error indicating inconsistent voter sets");;
stop
else if (error==Errors.INVALID_REQUEST) then (yes) 
:throw new IllegalStateException("Received unexpected invalid request error");;
stop
endif 
:return Optional.empty();;
stop
end
start
note right
void maybeTransition(OptionalInt leaderId,
int epoch,long currentTimeMs)
end note
if (!hasConsistentLeader(epoch,leaderId)) then (yes) 
:throw new IllegalStateException("Received request or response with leader "+
leaderId+" and epoch "+epoch+" which is inconsistent with current leader "+
quorum.leaderId()+" and epoch "+quorum.epoch());;
stop
else if (epoch>quorum.epoch()) then (yes) 
if (leaderId.isPresent()) then (yes) 
:transitionToFollower(epoch,leaderId.getAsInt(),
currentTimeMs);;
else (no)
:transitionToUnattached(epoch);;
endif 
else if (leaderId.isPresent()&&!quorum.hasLeader()) then (yes) 
:transitionToFollower(epoch,leaderId.getAsInt(),
currentTimeMs);;
endif 
end
start
note right
boolean handleTopLevelError(Errors error,
RaftResponse.Inbound response)
end note
if (error==Errors.BROKER_NOT_AVAILABLE) then (yes) 
:return false;;
stop
else if (error==Errors.CLUSTER_AUTHORIZATION_FAILED) then (yes) 
:throw new ClusterAuthorizationException("Received cluster authorization error in response "+
response);;
stop
else (no)
:return handleUnexpectedError(error,response);;
stop
endif 
end
start
note right
boolean handleUnexpectedError(Errors error,
RaftResponse.Inbound response)
end note
:logger.error("Unexpected error {} in {} response: {}",
error,ApiKeys.forId(response.data.apiKey()),
response);;
:return false;;
stop
end
start
note right
void handleResponse(RaftResponse.Inbound response,
long currentTimeMs)
end note
:ApiKeys apiKey=ApiKeys.forId(response.data.apiKey());;
:final boolean handledSuccessfully;;
switch (switch apiKey)
endswitch
:ConnectionState connection=requestManager.getOrCreate(response.sourceId());;
if (handledSuccessfully) then (yes) 
:connection.onResponseReceived(response.correlationId);;
else (no)
:connection.onResponseError(response.correlationId,
currentTimeMs);;
endif 
end
start
note right
Optional<Errors> validateVoterOnlyRequest(int remoteNodeId,
int requestEpoch)
end note
if (requestEpoch<quorum.epoch()) then (yes) 
:return Optional.of(Errors.FENCED_LEADER_EPOCH);;
stop
else if (remoteNodeId<0) then (yes) 
:return Optional.of(Errors.INVALID_REQUEST);;
stop
else if (quorum.isObserver()||!quorum.isVoter(remoteNodeId)) then (yes) 
:return Optional.of(Errors.INCONSISTENT_VOTER_SET);;
stop
else (no)
:return Optional.empty();;
stop
endif 
end
start
note right
Optional<Errors> validateLeaderOnlyRequest(int requestEpoch)
end note
if (requestEpoch<quorum.epoch()) then (yes) 
:return Optional.of(Errors.FENCED_LEADER_EPOCH);;
stop
else if (requestEpoch>quorum.epoch()) then (yes) 
:return Optional.of(Errors.UNKNOWN_LEADER_EPOCH);;
stop
else if (!quorum.isLeader()) then (yes) 
:return Optional.of(Errors.NOT_LEADER_OR_FOLLOWER);;
stop
else if (shutdown.get()!=null ) then (yes) 
:return Optional.of(Errors.BROKER_NOT_AVAILABLE);;
stop
else (no)
:return Optional.empty();;
stop
endif 
end
start
note right
void handleRequest(RaftRequest.Inbound request,
long currentTimeMs)
end note
:ApiKeys apiKey=ApiKeys.forId(request.data.apiKey());;
:final CompletableFuture<?extendsApiMessage> responseFuture;;
switch (switch apiKey)
endswitch
:responseFuture.whenComplete((response,exception)->{final ApiMessage message;if (response!=null ){message=response;}else {message=RaftUtil.errorResponse(apiKey,
Errors.forException(exception));}RaftResponse.Outbound responseMessage=new RaftResponse.Outbound(request.correlationId(),
message);request.completion.complete(responseMessage);logger.trace("Sent response {} to inbound request {}",
responseMessage,request);});;
end
start
note right
void handleInboundMessage(RaftMessage message,
long currentTimeMs)
end note
:logger.trace("Received inbound message {}",
message);;
if (message instanceof RaftRequest.Inbound ) then (yes) 
:RaftRequest.Inbound request=(RaftRequest.Inbound )message;;
:handleRequest(request,currentTimeMs);;
else if (message instanceof RaftResponse.Inbound ) then (yes) 
:RaftResponse.Inbound response=(RaftResponse.Inbound )message;;
:ConnectionState connection=requestManager.getOrCreate(response.sourceId());;
if (connection.isResponseExpected(response.correlationId)) then (yes) 
:handleResponse(response,currentTimeMs);;
else (no)
:logger.debug("Ignoring response {} since it is no longer needed",
response);;
endif 
else (no)
:throw new IllegalArgumentException("Unexpected message "+
message);;
stop
endif 
end
start
note right
long maybeSendRequest(long currentTimeMs,
int destinationId,Supplier<ApiMessage> requestSupplier)
end note
:ConnectionState connection=requestManager.getOrCreate(destinationId);;
if (connection.isBackingOff(currentTimeMs)) then (yes) 
:long remainingBackoffMs=connection.remainingBackoffMs(currentTimeMs);;
:logger.debug("Connection for {} is backing off for {} ms",
destinationId,remainingBackoffMs);;
:return remainingBackoffMs;;
stop
endif 
if (connection.isReady(currentTimeMs)) then (yes) 
:int correlationId=channel.newCorrelationId();;
:ApiMessage request=requestSupplier.get();;
:RaftRequest.Outbound requestMessage=new RaftRequest.Outbound(correlationId,
request,destinationId,currentTimeMs);;
:requestMessage.completion.whenComplete((response,
exception)->{if (exception!=null ){ApiKeys api=ApiKeys.forId(request.apiKey());Errors error=Errors.forException(exception);ApiMessage errorResponse=RaftUtil.errorResponse(api,
error);response=new RaftResponse.Inbound(correlationId,
errorResponse,destinationId);}messageQueue.add(response);});;
:channel.send(requestMessage);;
:logger.trace("Sent outbound request: {}",
requestMessage);;
:connection.onRequestSent(correlationId,currentTimeMs);;
:return Long.MAX_VALUE;;
stop
endif 
:return connection.remainingRequestTimeMs(currentTimeMs);;
stop
end
start
note right
EndQuorumEpochRequestData buildEndQuorumEpochRequest(ResignedState state)
end note
:return EndQuorumEpochRequest.singletonRequest(log.topicPartition(),
clusterId,quorum.epoch(),quorum.localIdOrThrow(),
state.preferredSuccessors());;
stop
end
start
note right
long maybeSendRequests(long currentTimeMs,
Set<Integer> destinationIds,Supplier<ApiMessage> requestSupplier)
end note
:long minBackoffMs=Long.MAX_VALUE;;
:long backoffMs=maybeSendRequest(currentTimeMs,
destinationId,requestSupplier);;
if (backoffMs<minBackoffMs) then (yes) 
:minBackoffMs=backoffMs;;
endif 
:return minBackoffMs;;
stop
end
start
note right
BeginQuorumEpochRequestData buildBeginQuorumEpochRequest()
end note
:return BeginQuorumEpochRequest.singletonRequest(log.topicPartition(),
clusterId,quorum.epoch(),quorum.localIdOrThrow());;
stop
end
start
note right
VoteRequestData buildVoteRequest()
end note
:OffsetAndEpoch endOffset=endOffset();;
:return VoteRequest.singletonRequest(log.topicPartition(),
clusterId,quorum.epoch(),quorum.localIdOrThrow(),
endOffset.epoch,endOffset.offset);;
stop
end
start
note right
FetchRequestData buildFetchRequest()
end note
:FetchRequestData request=RaftUtil.singletonFetchRequest(log.topicPartition(),
log.topicId(),fetchPartition->{fetchPartition.setCurrentLeaderEpoch(quorum.epoch()).setLastFetchedEpoch(log.lastFetchedEpoch()).setFetchOffset(log.endOffset().offset);});;
:return request.setMaxBytes(MAX_FETCH_SIZE_BYTES)
.setMaxWaitMs(fetchMaxWaitMs).setClusterId(clusterId).setReplicaId(quorum.localIdOrSentinel());;
stop
end
start
note right
long maybeSendAnyVoterFetch(long currentTimeMs)
end note
:OptionalInt readyVoterIdOpt=requestManager.findReadyVoter(currentTimeMs);;
if (readyVoterIdOpt.isPresent()) then (yes) 
:return maybeSendRequest(currentTimeMs,readyVoterIdOpt.getAsInt(),
this::buildFetchRequest);;
stop
else (no)
:return requestManager.backoffBeforeAvailableVoter(currentTimeMs);;
stop
endif 
end
start
note right
FetchSnapshotRequestData buildFetchSnapshotRequest(OffsetAndEpoch snapshotId,
long snapshotSize)
end note
:FetchSnapshotRequestData.SnapshotId requestSnapshotId=new FetchSnapshotRequestData.SnapshotId()
.setEpoch(snapshotId.epoch).setEndOffset(snapshotId.offset);;
:FetchSnapshotRequestData request=FetchSnapshotRequest.singleton(clusterId,
log.topicPartition(),snapshotPartition->{return snapshotPartition.setCurrentLeaderEpoch(quorum.epoch()).setSnapshotId(requestSnapshotId).setPosition(snapshotSize);});;
:return request.setReplicaId(quorum.localIdOrSentinel());;
stop
end
start
note right
FetchSnapshotResponseData.PartitionSnapshot addQuorumLeader(FetchSnapshotResponseData.PartitionSnapshot partitionSnapshot)
end note
:partitionSnapshot.currentLeader().setLeaderEpoch(quorum.epoch())
.setLeaderId(quorum.leaderIdOrSentinel());;
:return partitionSnapshot;;
stop
end
start
note right
boolean isRunning()
end note
:GracefulShutdown gracefulShutdown=shutdown.get();;
:return gracefulShutdown==null ||!gracefulShutdown.isFinished();;
stop
end
start
note right
boolean isShuttingDown()
end note
:GracefulShutdown gracefulShutdown=shutdown.get();;
:return gracefulShutdown!=null &&!gracefulShutdown.isFinished();;
stop
end
start
note right
void appendBatch(LeaderState<T> state,BatchAccumulator.CompletedBatch<T> batch,
long appendTimeMs)
end note
switch (try/catch block)
case (try)
:int epoch=state.epoch();;
:LogAppendInfo info=appendAsLeader(batch.data);;
:OffsetAndEpoch offsetAndEpoch=new OffsetAndEpoch(info.lastOffset,
epoch);;
:CompletableFuture<Long> future=appendPurgatory.await(offsetAndEpoch.offset+
1,Integer.MAX_VALUE);;
:future.whenComplete((commitTimeMs,exception)->{if (exception!=null ){logger.debug("Failed to commit {} records at {}",
batch.numRecords,offsetAndEpoch,exception);}else {long elapsedTime=Math.max(0,
commitTimeMs-appendTimeMs);double elapsedTimePerRecord=(double )elapsedTime/batch.numRecords;kafkaRaftMetrics.updateCommitLatency(elapsedTimePerRecord,
appendTimeMs);logger.debug("Completed commit of {} records at {}",
batch.numRecords,offsetAndEpoch);batch.records.ifPresent(records->{maybeFireHandleCommit(batch.baseOffset,
epoch,batch.appendTimestamp(),batch.sizeInBytes(),
records);});}});;
endswitch
end
start
note right
long maybeAppendBatches(LeaderState<T> state,
long currentTimeMs)
end note
:long timeUntilDrain=state.accumulator().timeUntilDrain(currentTimeMs);;
if (timeUntilDrain<=0) then (yes) 
:List<BatchAccumulator.CompletedBatch<T>> batches=state.accumulator()
.drain();;
:Iterator<BatchAccumulator.CompletedBatch<T>> iterator=batches.iterator();;
switch (try/catch block)
case (try)
while (iterator.hasNext())
:BatchAccumulator.CompletedBatch<T> batch=iterator.next();;
:appendBatch(state,batch,currentTimeMs);;
endwhile
:flushLeaderLog(state,currentTimeMs);;
endswitch
endif 
:return timeUntilDrain;;
stop
end
start
note right
long pollResigned(long currentTimeMs)
end note
:ResignedState state=quorum.resignedStateOrThrow();;
:long endQuorumBackoffMs=maybeSendRequests(currentTimeMs,
state.unackedVoters(),()->buildEndQuorumEpochRequest(state));;
:GracefulShutdown shutdown=this.shutdown.get();;
:final long stateTimeoutMs;;
if (shutdown!=null ) then (yes) 
:stateTimeoutMs=shutdown.remainingTimeMs();;
else if (state.hasElectionTimeoutExpired(currentTimeMs)) then (yes) 
:transitionToCandidate(currentTimeMs);;
:stateTimeoutMs=0L;;
else (no)
:stateTimeoutMs=state.remainingElectionTimeMs(currentTimeMs);;
endif 
:return Math.min(stateTimeoutMs,endQuorumBackoffMs);;
stop
end
start
note right
long pollLeader(long currentTimeMs)
end note
:LeaderState<T> state=quorum.leaderStateOrThrow();;
:maybeFireLeaderChange(state);;
if (shutdown.get()!=null ||state.isResignRequested()) then (yes) 
:transitionToResigned(state.nonLeaderVotersByDescendingFetchOffset());;
:return 0L;;
stop
endif 
:long timeUntilFlush=maybeAppendBatches(state,
currentTimeMs);;
:long timeUntilSend=maybeSendRequests(currentTimeMs,
state.nonAcknowledgingVoters(),this::buildBeginQuorumEpochRequest);;
:return Math.min(timeUntilFlush,timeUntilSend);;
stop
end
start
note right
long maybeSendVoteRequests(CandidateState state,
long currentTimeMs)
end note
if (!state.isVoteRejected()) then (yes) 
:return maybeSendRequests(currentTimeMs,state.unrecordedVoters(),
this::buildVoteRequest);;
stop
endif 
:return Long.MAX_VALUE;;
stop
end
start
note right
long pollCandidate(long currentTimeMs)
end note
:CandidateState state=quorum.candidateStateOrThrow();;
:GracefulShutdown shutdown=this.shutdown.get();;
if (shutdown!=null ) then (yes) 
:long minRequestBackoffMs=maybeSendVoteRequests(state,
currentTimeMs);;
:return Math.min(shutdown.remainingTimeMs(),
minRequestBackoffMs);;
stop
else if (state.isBackingOff()) then (yes) 
if (state.isBackoffComplete(currentTimeMs)) then (yes) 
:logger.info("Re-elect as candidate after election backoff has completed");;
:transitionToCandidate(currentTimeMs);;
:return 0L;;
stop
endif 
:return state.remainingBackoffMs(currentTimeMs);;
stop
else if (state.hasElectionTimeoutExpired(currentTimeMs)) then (yes) 
:long backoffDurationMs=binaryExponentialElectionBackoffMs(state.retries());;
:logger.debug("Election has timed out, backing off for {}ms before becoming a candidate again",
backoffDurationMs);;
:state.startBackingOff(currentTimeMs,backoffDurationMs);;
:return backoffDurationMs;;
stop
else (no)
:long minRequestBackoffMs=maybeSendVoteRequests(state,
currentTimeMs);;
:return Math.min(minRequestBackoffMs,state.remainingElectionTimeMs(currentTimeMs));;
stop
endif 
end
start
note right
long pollFollower(long currentTimeMs)
end note
:FollowerState state=quorum.followerStateOrThrow();;
if (quorum.isVoter()) then (yes) 
:return pollFollowerAsVoter(state,currentTimeMs);;
stop
else (no)
:return pollFollowerAsObserver(state,currentTimeMs);;
stop
endif 
end
start
note right
long pollFollowerAsVoter(FollowerState state,
long currentTimeMs)
end note
:GracefulShutdown shutdown=this.shutdown.get();;
if (shutdown!=null ) then (yes) 
:return 0;;
stop
else if (state.hasFetchTimeoutExpired(currentTimeMs)) then (yes) 
:logger.info("Become candidate due to fetch timeout");;
:transitionToCandidate(currentTimeMs);;
:return 0L;;
stop
else (no)
:long backoffMs=maybeSendFetchOrFetchSnapshot(state,
currentTimeMs);;
:return Math.min(backoffMs,state.remainingFetchTimeMs(currentTimeMs));;
stop
endif 
end
start
note right
long pollFollowerAsObserver(FollowerState state,
long currentTimeMs)
end note
if (state.hasFetchTimeoutExpired(currentTimeMs)) then (yes) 
:return maybeSendAnyVoterFetch(currentTimeMs);;
stop
else (no)
:final long backoffMs;;
:ConnectionState connection=requestManager.getOrCreate(state.leaderId());;
if (connection.hasRequestTimedOut(currentTimeMs)) then (yes) 
:backoffMs=maybeSendAnyVoterFetch(currentTimeMs);;
:connection.reset();;
else if (connection.isBackingOff(currentTimeMs)) then (yes) 
:backoffMs=maybeSendAnyVoterFetch(currentTimeMs);;
else (no)
:backoffMs=maybeSendFetchOrFetchSnapshot(state,
currentTimeMs);;
endif 
:return Math.min(backoffMs,state.remainingFetchTimeMs(currentTimeMs));;
stop
endif 
end
start
note right
long maybeSendFetchOrFetchSnapshot(FollowerState state,
long currentTimeMs)
end note
:final Supplier<ApiMessage> requestSupplier;;
if (state.fetchingSnapshot().isPresent()) then (yes) 
:RawSnapshotWriter snapshot=state.fetchingSnapshot()
.get();;
:long snapshotSize=snapshot.sizeInBytes();;
:requestSupplier=()->buildFetchSnapshotRequest(snapshot.snapshotId(),
snapshotSize);;
else (no)
:requestSupplier=this::buildFetchRequest;;
endif 
:return maybeSendRequest(currentTimeMs,state.leaderId(),
requestSupplier);;
stop
end
start
note right
long pollVoted(long currentTimeMs)
end note
:VotedState state=quorum.votedStateOrThrow();;
:GracefulShutdown shutdown=this.shutdown.get();;
if (shutdown!=null ) then (yes) 
:return shutdown.remainingTimeMs();;
stop
else if (state.hasElectionTimeoutExpired(currentTimeMs)) then (yes) 
:transitionToCandidate(currentTimeMs);;
:return 0L;;
stop
else (no)
:return state.remainingElectionTimeMs(currentTimeMs);;
stop
endif 
end
start
note right
long pollUnattached(long currentTimeMs)
end note
:UnattachedState state=quorum.unattachedStateOrThrow();;
if (quorum.isVoter()) then (yes) 
:return pollUnattachedAsVoter(state,currentTimeMs);;
stop
else (no)
:return pollUnattachedAsObserver(state,currentTimeMs);;
stop
endif 
end
start
note right
long pollUnattachedAsVoter(UnattachedState state,
long currentTimeMs)
end note
:GracefulShutdown shutdown=this.shutdown.get();;
if (shutdown!=null ) then (yes) 
:return shutdown.remainingTimeMs();;
stop
else if (state.hasElectionTimeoutExpired(currentTimeMs)) then (yes) 
:transitionToCandidate(currentTimeMs);;
:return 0L;;
stop
else (no)
:return state.remainingElectionTimeMs(currentTimeMs);;
stop
endif 
end
start
note right
long pollUnattachedAsObserver(UnattachedState state,
long currentTimeMs)
end note
:long fetchBackoffMs=maybeSendAnyVoterFetch(currentTimeMs);;
:return Math.min(fetchBackoffMs,state.remainingElectionTimeMs(currentTimeMs));;
stop
end
start
note right
long pollCurrentState(long currentTimeMs)
end note
if (quorum.isLeader()) then (yes) 
:return pollLeader(currentTimeMs);;
stop
else if (quorum.isCandidate()) then (yes) 
:return pollCandidate(currentTimeMs);;
stop
else if (quorum.isFollower()) then (yes) 
:return pollFollower(currentTimeMs);;
stop
else if (quorum.isVoted()) then (yes) 
:return pollVoted(currentTimeMs);;
stop
else if (quorum.isUnattached()) then (yes) 
:return pollUnattached(currentTimeMs);;
stop
else if (quorum.isResigned()) then (yes) 
:return pollResigned(currentTimeMs);;
stop
else (no)
:throw new IllegalStateException("Unexpected quorum state "+
quorum);;
stop
endif 
end
start
note right
void pollListeners()
end note
while (true)
:Registration<T> registration=pendingRegistrations.poll();;
if (registration==null ) then (yes) 
:break ;;
endif 
:processRegistration(registration);;
endwhile
:quorum.highWatermark().ifPresent(highWatermarkMetadata->{updateListenersProgress(highWatermarkMetadata.offset);});;
end
start
note right
void processRegistration(Registration<T> registration)
end note
:Listener<T> listener=registration.listener();;
:Registration.Ops ops=registration.ops();;
if (ops==Registration.Ops.REGISTER) then (yes) 
if (listenerContexts.putIfAbsent(listener,new ListenerContext(listener))!=null ) then (yes) 
:logger.error("Attempting to add a listener that already exists: {}",
listenerName(listener));;
else (no)
:logger.info("Registered the listener {}",
listenerName(listener));;
endif 
else (no)
if (listenerContexts.remove(listener)==null ) then (yes) 
:logger.error("Attempting to remove a listener that doesn't exists: {}",
listenerName(listener));;
else (no)
:logger.info("Unregistered the listener {}",
listenerName(listener));;
endif 
endif 
end
start
note right
boolean maybeCompleteShutdown(long currentTimeMs)
end note
:GracefulShutdown shutdown=this.shutdown.get();;
if (shutdown==null ) then (yes) 
:return false;;
stop
endif 
:shutdown.update(currentTimeMs);;
if (shutdown.hasTimedOut()) then (yes) 
:shutdown.failWithTimeout();;
:return true;;
stop
endif 
if (quorum.isObserver()||quorum.remoteVoters().isEmpty()||quorum.hasRemoteLeader()) then (yes) 
:shutdown.complete();;
:return true;;
stop
endif 
:return false;;
stop
end
start
note right
constructor RaftMetadataLogCleanerManager(Logger logger,
Time time,long delayMs,Runnable cleaner)
end note
:this.logger=logger;;
:this.timer=time.timer(delayMs);;
:this.delayMs=delayMs;;
:this.cleaner=cleaner;;
end
start
note right
long maybeClean(long currentTimeMs)
end note
:timer.update(currentTimeMs);;
if (timer.isExpired()) then (yes) 
switch (try/catch block)
case (try)
:cleaner.run();;
case (catch (Throwable t))
:logger.error("Had an error during log cleaning",t);;
endswitch
:timer.reset(delayMs);;
endif 
:return timer.remainingMs();;
stop
end
start
note right
void wakeup()
end note
:messageQueue.wakeup();;
end
start
note right
void handle(RaftRequest.Inbound request)
end note
:messageQueue.add(Objects.requireNonNull(request));;
end
start
note right
void poll()
end note
:pollListeners();;
:long currentTimeMs=time.milliseconds();;
if (maybeCompleteShutdown(currentTimeMs)) then (yes) 
:return ;;
stop
endif 
:long pollStateTimeoutMs=pollCurrentState(currentTimeMs);;
:long cleaningTimeoutMs=snapshotCleaner.maybeClean(currentTimeMs);;
:long pollTimeoutMs=Math.min(pollStateTimeoutMs,
cleaningTimeoutMs);;
:kafkaRaftMetrics.updatePollStart(currentTimeMs);;
:RaftMessage message=messageQueue.poll(pollTimeoutMs);;
:currentTimeMs=time.milliseconds();;
:kafkaRaftMetrics.updatePollEnd(currentTimeMs);;
if (message!=null ) then (yes) 
:handleInboundMessage(message,currentTimeMs);;
endif 
end
start
note right
long scheduleAppend(int epoch,List<T> records)
end note
:return append(epoch,records,false);;
stop
end
start
note right
long scheduleAtomicAppend(int epoch,List<T> records)
end note
:return append(epoch,records,true);;
stop
end
start
note right
long append(int epoch,List<T> records,boolean isAtomic)
end note
:LeaderState<T> leaderState=quorum.<T >maybeLeaderState()
.orElseThrow(()->new NotLeaderException("Append failed because the replication is not the current leader"));;
:BatchAccumulator<T> accumulator=leaderState.accumulator();;
:boolean isFirstAppend=accumulator.isEmpty();;
:final long offset;;
if (isAtomic) then (yes) 
:offset=accumulator.appendAtomic(epoch,records);;
else (no)
:offset=accumulator.append(epoch,records);;
endif 
if (isFirstAppend||accumulator.needsDrain(time.milliseconds())) then (yes) 
:wakeup();;
endif 
:return offset;;
stop
end
start
note right
CompletableFuture<Void> shutdown(int timeoutMs)
end note
:logger.info("Beginning graceful shutdown");;
:CompletableFuture<Void> shutdownComplete=new CompletableFuture<>();;
:shutdown.set(new GracefulShutdown(timeoutMs,
shutdownComplete));;
:wakeup();;
:return shutdownComplete;;
stop
end
start
note right
void resign(int epoch)
end note
if (epoch<0) then (yes) 
:throw new IllegalArgumentException("Attempt to resign from an invalid negative epoch "+
epoch);;
stop
endif 
if (!quorum.isVoter()) then (yes) 
:throw new IllegalStateException("Attempt to resign by a non-voter");;
stop
endif 
:LeaderAndEpoch leaderAndEpoch=leaderAndEpoch();;
:int currentEpoch=leaderAndEpoch.epoch();;
if (epoch>currentEpoch) then (yes) 
:throw new IllegalArgumentException("Attempt to resign from epoch "+
epoch+" which is larger than the current epoch "+
currentEpoch);;
stop
else if (epoch<currentEpoch) then (yes) 
:logger.debug("Ignoring call to resign from epoch {} since it is smaller than the "+
"current epoch {}",epoch,currentEpoch);;
:return ;;
stop
else if (!leaderAndEpoch.isLeader(quorum.localIdOrThrow())) then (yes) 
:throw new IllegalArgumentException("Cannot resign from epoch "+
epoch+" since we are not the leader");;
stop
else (no)
:Optional<LeaderState<Object>> leaderStateOpt=quorum.maybeLeaderState();;
if (!leaderStateOpt.isPresent()) then (yes) 
:logger.debug("Ignoring call to resign from epoch {} since this node is "+
"no longer the leader",epoch);;
:return ;;
stop
endif 
:LeaderState<Object> leaderState=leaderStateOpt.get();;
if (leaderState.epoch()!=epoch) then (yes) 
:logger.debug("Ignoring call to resign from epoch {} since it is smaller than the "+
"current epoch {}",epoch,leaderState.epoch());;
else (no)
:logger.info("Received user request to resign from the current epoch {}",
currentEpoch);;
:leaderState.requestResign();;
:wakeup();;
endif 
endif 
end
start
note right
Optional<SnapshotWriter<T>> createSnapshot(long committedOffset,
int committedEpoch,long lastContainedLogTime)
end note
:return RecordsSnapshotWriter.createWithHeader(()->log.createNewSnapshot(new OffsetAndEpoch(committedOffset+
1,committedEpoch)),MAX_BATCH_SIZE_BYTES,memoryPool,
time,lastContainedLogTime,CompressionType.NONE,
serde);;
stop
end
start
note right
void close()
end note
:log.flush(true);;
if (kafkaRaftMetrics!=null ) then (yes) 
:kafkaRaftMetrics.close();;
endif 
end
start
note right
QuorumState quorum()
end note
:return quorum;;
stop
end
start
note right
OptionalLong highWatermark()
end note
if (quorum.highWatermark().isPresent()) then (yes) 
:return OptionalLong.of(quorum.highWatermark()
.get().offset);;
stop
else (no)
:return OptionalLong.empty();;
stop
endif 
end
start
note right
constructor GracefulShutdown(long shutdownTimeoutMs,
CompletableFuture<Void> completeFuture)
end note
:this.finishTimer=time.timer(shutdownTimeoutMs);;
:this.completeFuture=completeFuture;;
end
start
note right
void update(long currentTimeMs)
end note
:finishTimer.update(currentTimeMs);;
end
start
note right
boolean hasTimedOut()
end note
:return finishTimer.isExpired();;
stop
end
start
note right
boolean isFinished()
end note
:return completeFuture.isDone();;
stop
end
start
note right
long remainingTimeMs()
end note
:return finishTimer.remainingMs();;
stop
end
start
note right
void failWithTimeout()
end note
:logger.warn("Graceful shutdown timed out after {}ms",
finishTimer.timeoutMs());;
:completeFuture.completeExceptionally(new TimeoutException("Timeout expired before graceful shutdown completed"));;
end
start
note right
void complete()
end note
:logger.info("Graceful shutdown completed");;
:completeFuture.complete(null );;
end
start
note right
constructor Registration(Ops ops,Listener<T> listener)
end note
:this.ops=ops;;
:this.listener=listener;;
end
start
note right
Ops ops()
end note
:return ops;;
stop
end
start
note right
Listener<T> listener()
end note
:return listener;;
stop
end
:return new Registration<>(Ops.REGISTER,listener);;
stop
:return new Registration<>(Ops.UNREGISTER,
listener);;
stop
start
note right
constructor ListenerContext(Listener<T> listener)
end note
:this.listener=listener;;
end
start
note right
long nextOffset()
end note
:return nextOffset;;
stop
end
start
note right
OptionalLong nextExpectedOffset()
end note
if (lastSent!=null ) then (yes) 
:OptionalLong lastSentOffset=lastSent.lastOffset();;
if (lastSentOffset.isPresent()) then (yes) 
:return OptionalLong.of(lastSentOffset.getAsLong()+1);;
stop
else (no)
:return OptionalLong.empty();;
stop
endif 
else (no)
:return OptionalLong.of(nextOffset);;
stop
endif 
end
start
note right
void fireHandleSnapshot(SnapshotReader<T> reader)
end note
:synchronized (this){nextOffset=reader.snapshotId().offset;
:lastSent=null ;};
:logger.debug("Notifying listener {} of snapshot {}",
listenerName(),reader.snapshotId());;
:listener.handleSnapshot(reader);;
end
start
note right
void fireHandleCommit(long baseOffset,Records records)
end note
:fireHandleCommit(RecordsBatchReader.of(baseOffset,
records,serde,BufferSupplier.create(),MAX_BATCH_SIZE_BYTES,
this));;
end
start
note right
void fireHandleCommit(long baseOffset,int epoch,
long appendTimestamp,int sizeInBytes,List<T> records)
end note
:Batch<T> batch=Batch.data(baseOffset,epoch,
appendTimestamp,sizeInBytes,records);;
:MemoryBatchReader<T> reader=MemoryBatchReader.of(Collections.singletonList(batch),
this);;
:fireHandleCommit(reader);;
end
start
note right
String listenerName()
end note
:return KafkaRaftClient.listenerName(listener);;
stop
end
start
note right
void fireHandleCommit(BatchReader<T> reader)
end note
:synchronized (this){this.lastSent=reader;};
:logger.debug("Notifying listener {} of batch for baseOffset {} and lastOffset {}",
listenerName(),reader.baseOffset(),reader.lastOffset());;
:listener.handleCommit(reader);;
end
start
note right
void maybeFireLeaderChange(LeaderAndEpoch leaderAndEpoch)
end note
if (shouldFireLeaderChange(leaderAndEpoch)) then (yes) 
:lastFiredLeaderChange=leaderAndEpoch;;
:logger.debug("Notifying listener {} of leader change {}",
listenerName(),leaderAndEpoch);;
:listener.handleLeaderChange(leaderAndEpoch);;
endif 
end
start
note right
boolean shouldFireLeaderChange(LeaderAndEpoch leaderAndEpoch)
end note
if (leaderAndEpoch.equals(lastFiredLeaderChange)) then (yes) 
:return false;;
stop
else if (leaderAndEpoch.epoch()>lastFiredLeaderChange.epoch()) then (yes) 
:return true;;
stop
else (no)
:return leaderAndEpoch.leaderId().isPresent()&&!lastFiredLeaderChange.leaderId()
.isPresent();;
stop
endif 
end
start
note right
void maybeFireLeaderChange(LeaderAndEpoch leaderAndEpoch,
long epochStartOffset)
end note
if (shouldFireLeaderChange(leaderAndEpoch)&&nextOffset()>=epochStartOffset) then (yes) 
:lastFiredLeaderChange=leaderAndEpoch;;
:listener.handleLeaderChange(leaderAndEpoch);;
endif 
end
start
note right
void onClose(BatchReader<T> reader)
end note
:OptionalLong lastOffset=reader.lastOffset();;
if (lastOffset.isPresent()) then (yes) 
:nextOffset=lastOffset.getAsLong()+1;;
endif 
if (lastSent==reader) then (yes) 
:lastSent=null ;;
:wakeup();;
endif 
end
@enduml

PlantUML version 1.2022.7(Mon Aug 22 17:01:30 UTC 2022)
(GPL source distribution)
Java Runtime: OpenJDK Runtime Environment
JVM: OpenJDK 64-Bit Server VM
Default Encoding: UTF-8
Language: en
Country: null
--></g></svg>