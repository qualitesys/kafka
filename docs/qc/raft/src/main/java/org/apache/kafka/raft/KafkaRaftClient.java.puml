@startuml
!pragma layout smetana
RaftClient_T_ <|-- org.apache.kafka.raft.KafkaRaftClient
class org.apache.kafka.raft.KafkaRaftClient {
+  KafkaRaftClient(RecordSerde<T>;NetworkChannel;ReplicatedLog;QuorumStateStore;Time;Metrics;ExpirationService;LogContext;String;OptionalInt;RaftConfig)
+  KafkaRaftClient(RecordSerde<T>;NetworkChannel;RaftMessageQueue;ReplicatedLog;QuorumStateStore;MemoryPool;Time;Metrics;ExpirationService;int;String;OptionalInt;LogContext;Random;RaftConfig)
+  updateFollowerHighWatermark(FollowerState;OptionalLong)
+  updateLeaderEndOffsetAndTimestamp(LeaderState<T>;long)
+  onUpdateLeaderHighWatermark(LeaderState<T>;long)
+  updateListenersProgress(long)
+  latestSnapshot()
+  maybeFireHandleCommit(long;int;long;int;List<T>)
+  maybeFireLeaderChange(LeaderState<T>)
+  maybeFireLeaderChange()
+  initialize()
+  register(Listener<T>)
+  unregister(Listener<T>)
+  leaderAndEpoch()
+  nodeId()
+  endOffset()
+  resetConnections()
+  onBecomeLeader(long)
+  flushLeaderLog(LeaderState<T>;long)
+  maybeTransitionToLeader(CandidateState;long)
+  onBecomeCandidate(long)
+  transitionToCandidate(long)
+  transitionToUnattached(int)
+  transitionToResigned(List<Integer>)
+  transitionToVoted(int;int)
+  onBecomeFollower(long)
+  transitionToFollower(int;int;long)
+  buildVoteResponse(Errors;boolean)
+  handleVoteRequest(RaftRequest.Inbound)
+  handleVoteResponse(RaftResponse.Inbound;long)
+  binaryExponentialElectionBackoffMs(int)
+  strictExponentialElectionBackoffMs(int;int)
+  buildBeginQuorumEpochResponse(Errors)
+  handleBeginQuorumEpochRequest(RaftRequest.Inbound;long)
+  handleBeginQuorumEpochResponse(RaftResponse.Inbound;long)
+  buildEndQuorumEpochResponse(Errors)
+  handleEndQuorumEpochRequest(RaftRequest.Inbound;long)
+  endEpochElectionBackoff(List<Integer>)
+  handleEndQuorumEpochResponse(RaftResponse.Inbound;long)
+  buildFetchResponse(Errors;Records;ValidOffsetAndEpoch;Optional<LogOffsetMetadata>)
+  buildEmptyFetchResponse(Errors;Optional<LogOffsetMetadata>)
+  hasValidClusterId(String)
+  handleFetchRequest(RaftRequest.Inbound;long)
+  tryCompleteFetchRequest(int;FetchRequestData.FetchPartition;long)
+  optionalLeaderId(int)
+  listenerName(Listener<?>)
+  handleFetchResponse(RaftResponse.Inbound;long)
+  appendAsFollower(Records)
+  appendAsLeader(Records)
+  handleDescribeQuorumRequest(RaftRequest.Inbound;long)
+  handleFetchSnapshotRequest(RaftRequest.Inbound)
+  handleFetchSnapshotResponse(RaftResponse.Inbound;long)
+  convertToReplicaStates(Map<Integer,Long>)
+  hasConsistentLeader(int;OptionalInt)
+  maybeHandleCommonResponse(Errors;OptionalInt;int;long)
+  maybeTransition(OptionalInt;int;long)
+  handleTopLevelError(Errors;RaftResponse.Inbound)
+  handleUnexpectedError(Errors;RaftResponse.Inbound)
+  handleResponse(RaftResponse.Inbound;long)
+  validateVoterOnlyRequest(int;int)
+  validateLeaderOnlyRequest(int)
+  handleRequest(RaftRequest.Inbound;long)
+  handleInboundMessage(RaftMessage;long)
+  maybeSendRequest(long;int;Supplier<ApiMessage>)
+  buildEndQuorumEpochRequest(ResignedState)
+  maybeSendRequests(long;Set<Integer>;Supplier<ApiMessage>)
+  buildBeginQuorumEpochRequest()
+  buildVoteRequest()
+  buildFetchRequest()
+  maybeSendAnyVoterFetch(long)
+  buildFetchSnapshotRequest(OffsetAndEpoch;long)
+  addQuorumLeader(FetchSnapshotResponseData.PartitionSnapshot)
+  isRunning()
+  isShuttingDown()
+  appendBatch(LeaderState<T>;BatchAccumulator.CompletedBatch<T>;long)
+  maybeAppendBatches(LeaderState<T>;long)
+  pollResigned(long)
+  pollLeader(long)
+  maybeSendVoteRequests(CandidateState;long)
+  pollCandidate(long)
+  pollFollower(long)
+  pollFollowerAsVoter(FollowerState;long)
+  pollFollowerAsObserver(FollowerState;long)
+  maybeSendFetchOrFetchSnapshot(FollowerState;long)
+  pollVoted(long)
+  pollUnattached(long)
+  pollUnattachedAsVoter(UnattachedState;long)
+  pollUnattachedAsObserver(UnattachedState;long)
+  pollCurrentState(long)
+  pollListeners()
+  processRegistration(Registration<T>)
+  maybeCompleteShutdown(long)
+  wakeup()
+  handle(RaftRequest.Inbound)
+  poll()
+  scheduleAppend(int;List<T>)
+  scheduleAtomicAppend(int;List<T>)
+  append(int;List<T>;boolean)
+  shutdown(int)
+  resign(int)
+  createSnapshot(long;int;long)
+  close()
+  quorum()
+  highWatermark()
} 
@enduml
