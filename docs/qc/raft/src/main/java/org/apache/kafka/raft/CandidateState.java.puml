@startuml
!pragma layout smetana
EpochState <|-- org.apache.kafka.raft.CandidateState
class org.apache.kafka.raft.CandidateState {
+  CandidateState(Time;int;int;Set<Integer>;Optional<LogOffsetMetadata>;int;int;LogContext)
+  localId()
+  majoritySize()
+  numGranted()
+  numUnrecorded()
+  isBackingOff()
+  retries()
+  isVoteGranted()
+  isVoteRejected()
+  recordGrantedVote(int)
+  recordRejectedVote(int)
+  startBackingOff(long;long)
+  unrecordedVoters()
+  grantingVoters()
+  rejectingVoters()
+  votersInState(State)
+  hasElectionTimeoutExpired(long)
+  isBackoffComplete(long)
+  remainingBackoffMs(long)
+  remainingElectionTimeMs(long)
+  election()
+  epoch()
+  highWatermark()
+  canGrantVote(int;boolean)
+  toString()
+  name()
+  close()
} 
@enduml
